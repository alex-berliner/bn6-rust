# Proposed tickets 20260917-060111 (minimax/MiniMax-M3, $0.2462)

The three tickets below. None continues an objective whose last ticket was NEGATIVE or BLOCKED; M2 is closed by the T7 series and T89, M3 by T84/T81/T83, M4 by T85/T94, M5 by T92, M6 by T79, M8 by T93, M9 by T96. M7 (forms + emotion + NaviCust) and M10 (netbattle) are open. T98/T99 advance M7 (≤2-per-objective); T100 advances M10.

---

**T98 — M7 forms inventory: TF enum + charge-shot dispatch table + sprite census**

**Why.** SCOPE's M1 forms row reads 0/25. The form enum is at `reference/bn6f/constants/constants.inc:90-114` (25 entries TF_NONE..TF_FALZARBEASTOVER); the charge-shot dispatch table `ChargeShotHandlersByTransformation_80117D4` is at `reference/bn6f/asm/asm00_2.s:5789-5803` (15 entries `megamanChargeShotBPwrAtk_init_8011A26`, `busterBugChargeShotDamageCalcHappensHere_8011A7E`, `sub_8011ADA`, `sub_8011AF2`, `sub_8011B4A`, `nullsub_44`, `sub_8011BA2`, `greatfireAPwrAtk_init_8011BD0`, `sub_8011BE8`, `sub_8011C00`, `sub_8011C18`, `sub_8011C38`, `spoutcrossBPwrAtk_init_8011C5E`, `nullsub_44`, `nullsub_44`); sprite categories include CHARGEBEAST_05 (0x05), SPOUT_CROSS_CHARGE_SHOT_CANNON_55 (0x55), CHARGECROSS_9A (0x9A), CHARGEMANS_STONE_CARGO_02 (0x02), ELECMAN_ELECTRICITY_CHARGE_BF (0xBF), BUSTER_CHARGE_12 (0x12), LOCK_ON_TARGET_BEAST_OUT_4E (0x4E) (`reference/bn6f/constants/enums/sprite_categories.inc`). `tools/inventory.py:1799` already defines the `forms (M7)` field list `[("tf_value",8),("form",22),("charge_shot",34),("charge_cite",24),"status"]` but the 25 rows are blank. Closing this census is the precondition for per-form port tickets.

**Files.** `tools/inventory.py`, `docs/SCOPE.md`, `docs/coverage/forms.md` (NEW), `docs/worklog/T98.md` (NEW). NOT `src/`, NOT `tools/mgba_capture.c`, NOT `reference/bn6f/`. Touch only the named files.

**Do.**
1. Walk the TF enum in `reference/bn6f/constants/constants.inc:90-114` and list all 25 forms (id, name, family [None/Cross/CybEast/Soul+Cyb/BeastOver]) in `docs/coverage/forms.md`. Cite every line.
2. Walk `ChargeShotHandlersByTransformation_80117D4` (asm00_2.s:5789-5803). For each of the 15 entries, name the TF id(s) it dispatches for (the index byte is `oAIData_BPwrAtk`, NOT raw TF; cite which TF values canon routes to which dispatch slot — T57-style audit).
3. Walk the form-related sprite categories in `constants/enums/sprite_categories.inc`: CHARGEBEAST_05, SPOUT_CROSS_CHARGE_SHOT_CANNON_55, CHARGECROSS_9A, ELECMAN_ELECTRICITY_CHARGE_BF, CHARGEMANS_STONE_CARGO_02, CHARGEMANS_BURNING_COAL_03, BUSTER_CHARGE_12, LOCK_ON_TARGET_BEAST_OUT_4E, CHARGEMAN_33. Cite each form-coupling.
4. Edit `tools/inventory.py` to fill the 25 form rows with `tf_value`, `form`, `charge_shot`, `charge_cite`, `status` per form. Set `status=unrecorded` for all (no scenarios yet).
5. Run `python3 tools/inventory.py` to regenerate `docs/SCOPE.md`. Verify M1 forms row flips 0/25 → 25/25.
6. Pack a ROM baseline before and after; `cmp` against current main. Must be 0 differing bytes (docs/tools-only change).

**Rules.** Tools/docs only. No `src/`, no harness row, no scenario, no capture. Cite every form entry with file:line. Cite every charge-shot handler with the disassembly function offset (not line number). No fitted constants.

**Acceptance.** `tools/inventory.py` regenerates `docs/SCOPE.md` with M1 forms 25/25 (was 0/25). ROM sha256 unchanged. `docs/coverage/forms.md` lists 25 forms with id, name, family, charge-shot handler address, sprite category cite. Every form's `charge_cite` is `asm00_2.s:LINE` for the dispatch entry that fires for it (or "no-op (nullsub_44)" if applicable).

**Measure and report.** forms · frames · total · worst · region · commit · one line of mechanism · one line of what is unverified. Report: (a) M1 forms 0/25 → 25/25; (b) ROM sha256 unchanged; (c) distinct charge-shot handler addresses used (should be <15 since the `nullsub_44` entries handle multiple forms); (d) forms with a confirmed sprite category cite.

**Coordinator:** verify ROM sha unchanged (docs/tools-only); confirm inventory.py output parses; if any form's charge-shot handler cannot be mapped from the dispatch table (e.g., a TF value not in the 15-entry table because the dispatch byte is not the raw TF), report which TF values map to which dispatch entry byte and stop.

**Milestone advanced:** M7 (forms 0/25 → 25/25 inventory) and M1 (forms count).

---

**T99 — M7 first charge-shot scenario + dispatch port: TF_HEATCROSS state, off_80117D4 → src/actor.rs**

**Why.** T98 produced the per-form table and identified `ChargeShotHandlersByTransformation_80117D4` (`asm/asm00_2.s:5789-5803`) as the 15-entry per-form dispatch indexed by `oAIData_BPwrAtk` (`sub_80117A4`, asm00_2.s:5736-5786). src/ has no form-aware code (`grep TF_ src/` returns no hits; grep `charge_shot src/` finds only the AIM_FRAMES doc comment at src/actor.rs:217-218). First port: TF_HEATCROSS (0x1) → `busterBugChargeShotDamageCalcHappensHere_8011A7E`. The scenario is a descriptor-poked state with scripted B press; the harness row mirrors `chip-cannon`'s shape.

**Files.** `src/actor.rs`, `src/battle.rs`, `tools/states.py`, `tools/harness.py`, `tools/scoreboard.py`, `tools/inventory.py`, `docs/coverage/forms.md`, `docs/worklog/T99.md` (NEW). NOT `tools/mgba_capture.c`, NOT `reference/bn6f/`.

**Do.**
1. Find MegaMan's form field by searching write sites for TF values in `reference/bn6f/asm/asm00_2.s` (likely `oAIData_*` near the existing `oAIData_BPwrAtk` reader at asm00_2.s:5786). Cite file:line.
2. Add a `form` byte to the fixture descriptor (extend FIXTURE.md + src/fixture.rs), or repurpose an unused flag bit. `python3 tools/states.py build` accepts the new field.
3. Build a new `State` recipe `battlestart_heatcross` based on `battlestart.state` with `poke_at 60:<form_addr>:0x01` and `--script "B@100,B@130"`.
4. Capture 200 frames; watch the dispatch index byte (likely `oAIData_BPwrAtk` or the form byte itself) reads 0x01 from frame 60 onward on both sides. Report divergence.
5. Add harness row `chip-chargeshot-heat` to `tools/scoreboard.py` (one new tuple; shape mirrors `demo-cannon`).
6. Baseline `python3 tools/harness.py --only chip-chargeshot-heat --no-gallery`. Record total/worst/frames.
7. Port `ChargeShotHandlersByTransformation_80117D4` into `src/actor.rs` as a const `CHARGE_SHOT_HANDLERS: [fn(...); 15]` with a `// provenance: derived -- asm00_2.s:5789-5803` tag on every entry. Replace any per-form if/else with a table lookup by `form`.
8. Re-run the row. Report.

**Rules.** No allowlist change. Cite every form entry from T98's inventory. No fitted constants; the table IS canon's table. Touch only the named files. Cursor (regression set) must not exceed HEAD (1/1/170).

**Acceptance.** New harness row `chip-chargeshot-heat` reads 0/0/40 with the same shape as `chip-cannon`'s row (total 0, worst 0, frames 40). Trace: the dispatch index byte reads 0x01 on both sides from frame 100..140 (the charge-shot window). Cursor ≤ 1/1/170. ROM sha256 different from main (one row added, one src/ change). `forms (M7)` M1 row: 25/25 → 26/25 (one form pixel-verified via data-driven dispatch).

**Measure and report.** chip-chargeshot-heat · frames · total · worst · region · commit · one line of mechanism · one line of what is unverified.

**Coordinator:** verify_rows on chip-chargeshot-heat + regression set (wave, window, opening, chip-cannon, cursor, mettaur). If the trace's dispatch index byte is unreadable on canon (inlined), fall back to pixel measurement only and document. NEGATIVE closes if the scenario cannot be built (form field unwritable from the descriptor).

**Milestone advanced:** M7 (first form pixel-verified, dispatch ported as data) and M1 (forms 25/25 → 26/25 with one verified).

---

**T100 — M10: close T97's chip-trade payload semantics NEGATIVE (sub_8049D7C + unk_2010150 consumer walk)**

**Why.** T97 recon (`docs/coverage/netbattle.md`) named an UNPROVEN: the exchanged u16 at `unk_2010150+0x0b/+0x0c` (`byte_201015B`/`byte_201015C`, written at `reference/bn6f/asm/libs.s:5713-5715` via `sub_81469BC`) is seeded from `word_2006770` (`sub_803C418`, asm03_1_1.s:6884-6902) via `sub_803C40C` and GetRNG. Whether this u16 is the traded chip id/code pair or a session sync token is unproven. The session machine `cb_80395A4` (asm03_1_1.s:1309-1346) calls `sub_8049D7C` from `sub_803C418`; tracing that route and the consumers of `unk_2010150` resolves the NEGATIVE and names the real chip-exchange write if the u16 is a sync token.

**Files.** `docs/coverage/netbattle.md`, `docs/worklog/T100.md` (NEW). NOT `src/`, NOT `tools/`, NOT `reference/bn6f/`. Read-only disassembly access.

**Do.**
1. Read `sub_803C418` (`asm03_1_1.s:6884-6902`) and its callee `sub_8049D7C` (T97 named this as the next hop; if it does not exist, locate the real callee). Cite the destination register/memory write.
2. Read `sub_81469BC` (libs.s:5704-5722): confirm what it appends to `unk_2010150` (u16 split to +0x0b/+0x0c) and from where it sources the u16.
3. Find the consumer(s) of `unk_2010150` — grep `ldr* r*, [r*, #0xb]` and `[r*, #0xc]` paired with `0x2010150` in `reference/bn6f/asm/libs.s` and `asm/asm03_1_1.s`. Cite the consumer entry and what it does with the bytes.
4. Trace the chip-selection UI step inside `sub_8039B0A` (`asm03_1_1.s:2018-2065`): where the chosen chip id/code enters the payload. Cite file:line.
5. Update `docs/coverage/netbattle.md` §3 with the verdict. If the u16 is the chip id/code pair, cite the chip-selection write site; if it is a sync token, name the real chip-exchange write site and its address.
6. Append to `docs/worklog/T100.md` the chain of evidence (with file:line + ROM offset for every step), the verdict, and the next-ticket route for the two-instance capture follow-up.

**Rules.** Read-only on `reference/bn6f/`. No src/, no tools change, no harness row. Cite every function with file:line and exact ROM offset (`0x08XXXXXX`). If the consumer chain crosses into an unidentified function, name the offset and stop; do not speculate. If two consumer paths exist (cable vs wireless), name both.

**Acceptance.** T97's NEGATIVE closed: `docs/coverage/netbattle.md` §3 either (a) confirms the exchanged u16 IS the chip id/code pair with consumer cite + chip-selection entry cite, or (b) refutes it and names the real chip-exchange write (with cite + ROM offset). Section §5 updated to reflect the new evidence for the follow-up two-instance ticket.

**Measure and report.** grep-line count · cite count · chip_id_byte · payload_byte · commit · one line of mechanism · one line of what is unverified.

**Coordinator:** confirm both NEGATIVE branches covered (a or b); if branch (b) is taken, the next M10 ticket targets the real chip-exchange write directly via a two-instance capture (mgba link support still required).

**Milestone advanced:** M10 (T97 NEGATIVE closed; recon foundation for the two-instance capture ticket). No harness row yet — that requires mgba link support, which is `tools/mgba_capture.c` work (7ag-owned, off-limits).
