# Proposed tickets 20260917-063831 (minimax/MiniMax-M3, $0.2726)

Looking at this carefully:
- Only T105 is currently OPEN
- M2 battle_full has 174/540 sequencer frames still diverging but most paths are NEGATIVE/BLOCKED with 2-in-a-row partners; **T49 DONE recon** provides new evidence to re-attempt
- M5 has T87 DONE AIIndex-4 scenario (third virus after Mettaur/Gunner) — fresh angle, no NEGATIVE partner on the routine port specifically
- M7 emotion has T105 OPEN with full recon; the implementation follow-up isn't yet ticketed
- Lower milestones (M3, M4, M6, M8, M9) have 2-in-a-row NEGATIVE chains that close them; M11 is ROM-blocked

### T107. M2 battle_full sequencer: identify the post-T49 first-divergence field and port the cited edge  *(OPEN -- 2026-09-17)*

**Why.** battle_full still diverges on 174/540 sequencer frames per docs/SCOPE.md M2. Since T7p DONE landed rng_cadence 9/540 (below 10/540 threshold, head d43e4c0) and T7o DONE relocated SEQ04 seq-match block (784c8e5), the remaining divergence is in non-RNG frames. T49 DONE produced a fresh first-divergence map with the T47-fixed trace tools (head 34a4dd8); T88 DONE landed T66's POST_FLINCH_FRAMES shadow replacement; T89 RESOLVED-ALREADY landed T7q's seq.state gate in Actor::update. The T50 DONE hop executor already moved divergence k=0 → k=17; the next first divergence sits somewhere after that. **New evidence.** T49 DONE recon (head 34a4dd8) — fresh trace map made AFTER the close of T7x NEGATIVE, T7y NEGATIVE, T7z BLOCKED on the SEQ_24 / open-edge / window-duration objectives.

**Files.** `src/battle.rs` (the cited edge port + the seq.state gate if a new arm is needed), `docs/coverage/battle_full.md` (new first-divergence entry after the port), `docs/worklog/T107.md`. NOT `src/objects.rs`, `src/gunner.rs`, `src/chips.rs`, `tools/trace.py`, `tools/allowlist.py`, `tools/inventory.py`, `reference/bn6f/`.

**Do.**
1. **Recon:** re-run `tools/trace.py battle_full` on HEAD (post T7p + T7o + T7q + T88 + T89 landings); name the first divergent field + frame (post-T49 baseline). Cite the field at reference/bn6f file:line. **measurement.**
2. Localize the divergence to one source file line and one cited canon edge (an `asm/asm00_*.s:NNN` branch); if the edge is `seq.state`-gated, port the missing arm. **measurement + code change.**
3. Build the ROM, sha256 + size; run verify_rows on wave + window + mettaur + popup + buster + result + field + opening + cursor; capture battle_full with the new port; report the new battle_full divergence count and cursor isolated. **measurement.**
4. Re-run trace; new first divergence at frame STRICTLY LATER than step 1's frame OR divergence count drops by ≥1 frame with the same first divergence. **measurement.**

**Rules.** Cite canon file:line for every ported edge. Cursor veto ≤1/1/170/186300. No allowlist change; no fitted constants. ≤6 captures, ≤80 tool calls. Land via `tools/land.sh` only after step 3's verify_rows PASS.

**Acceptance.** battle_full trace: first divergent field name + frame STRICTLY LATER than the T49 baseline (or divergence count drops ≥1 frame with the same first divergence); verify_rows PASS on the 9-row guard set is the veto; cursor isolated unchanged (1/1/170). NEGATIVE naming canon's measured post-T49 first-divergence field + frame + cite closes it.

**Measure and report.** battle_full · frames 540 · first divergent field + frame (before / after) · divergence count (before / after) · cursor 1/1/170 baseline · 9-row verify_rows results · ROM sha256+size · fitted count · commit · one line of mechanism (the cited canon edge) · one line unverified (the remaining divergence frames past the ported one).

**Coordinator:** runs alone (owns src/battle.rs + tools/trace.py + the trace baseline). Free tier: verify_rows from a clean checkout; verifier for the cited canon edge + the post-T49 first-divergence field name. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M2 (battle_full divergence 174/540 → ≤173/540; first-divergence frame strictly later).

---

### T108. M5 AIIndex-4 routine port (third virus after Mettaur, Gunner)  *(OPEN -- 2026-09-17)*

**Why.** M5 needs every virus family ported. T6 DONE ported Mettaur (ForMettaur_8109EF4). T9j PARTIAL ported Gunner (ForGunner_8113078) but the gunner row at 2850534/38237/130 still diverges. T87 DONE created scenario `battlestart_ai4_rank0` (EVENT_681 flag poke 60:0x02001d58:0x0240 swaps encounter root to 0x080b50b0 = 12 ungated records naming AIIndex-4 rank0, head 6e493bc). AIIndex-4's per-type routine is `off_810B2D0` (asm/asm00_2.s:19975 byte_80182C4 row 19, version 0, enemy_idxs 19-24). This ticket ports the routine from `off_810B2D0` to `src/ai4.rs` in the ForGunner shape (init + 4-arm state machine: sub_8112F70/sub_8112FBA-style) and verifies via a new harness row `virus_ai4` aligned on the first AIIndex-4 attack event. **New evidence.** T87 DONE scenario (head 6e493bc) gives a canon recording of the AIIndex-4 family, made AFTER the close of T55 NEGATIVE on the BG3 plateau and T92 NEGATIVE on the T87 redo.

**Files.** `src/ai4.rs` (NEW: AIIndex-4 per-type routine port), `src/battle.rs` (wire `Battle::ai4_ctl` only if a hand-written arm needs migration), `tools/states.py` (verify `battlestart_ai4_rank0` recipe), `tools/harness.py` (NEW row `virus_ai4`: canon_ref=40, negative = same scenario WITHOUT the EVENT_681 poke), `docs/coverage/ai4.md` (NEW), `docs/worklog/T108.md`. NOT `src/gunner.rs`, `src/objects.rs`, `src/ai.rs`, `tools/allowlist.py`, `tools/inventory.py`, `reference/bn6f/`.

**Do.**
1. **Recon:** identify AIIndex-4's first attack event frame from the T87 scenario (already landed: peek 0x02001d58 EVENT_681 flag at 60:0x0240). Cite `off_810B2D0`'s routine body in `reference/bn6f/asm/asm00_2.s` at the byte_80182C4 row 19 entry point and each of the 4 arm sites. **measurement.**
2. Baseline, no edit: build ROM, sha256 + size; verify_rows 9-row guard set + cursor → all PASS; capture `battlestart_ai4_rank0` on both sides; confirm the encounter spawns AIIndex-4 viruses (peek enemy_kind at the AIIndex-4 slots). **measurement.**
3. Port `off_810B2D0`'s 4-arm state machine to `src/ai4.rs` (init, decide, act, react — cite each arm with file:line); add `Battle::ai4_ctl` only if the spawn path needs the per-type entry to take over from the hand-written AI. Add the row `virus_ai4` to harness (40 frames, canon=real, rust=ours, negative=plain_rom with no AIIndex-4 EVENT_681 poke). **code change + measurement.**
4. Re-run: `virus_ai4` 0/0/40 with non-blind negative (broken pair ≥1 differing pixel); trace `battlestart_ai4_rank0`: virus `ai_state_action` first divergence **none** over the row's frames; 9-row guard set + cursor identical to step 2. **measurement.**

**Rules.** Cite canon file:line for each arm. Cursor veto ≤1/1/170/186300. No allowlist change. ≤6 captures, ≤80 tool calls. Land via `tools/land.sh` only after step 3's verify_rows PASS.

**Acceptance.** `virus_ai4` 0/0/40 with non-blind negative (broken pair ≥1 differing pixel); trace `battlestart_ai4_rank0` first divergence none on `ai_state_action` over the 40 frames; verify_rows PASS on the 9-row guard set + cursor; AIIndex-4 routine cite in `docs/coverage/ai4.md` matches the ported `src/ai4.rs`.

**Measure and report.** rows: virus_ai4 + 9-row guard set + cursor · frames 40 each (cursor 170) · first attack frame · pixel totals (before / after) · `ai_state_action` trace first divergence · ROM sha256+size · fitted count · commit · one line of mechanism (which arm of off_810B2D0) · one line unverified (versions 1-5 of the same AIIndex-4 family, untouched).

**Coordinator:** runs alone (owns src/ai4.rs + tools/harness.py + tools/states.py). Free tier: verify_rows from a clean checkout; verifier for the off_810B2D0 cite + port shape. ≤$0.30 expected, ≤$0.60 cap.

**Milestone advanced:** M5 (viruses 1/187 → 1+/187 — third virus as data; AIIndex-4 routine cited).

---

### T109. M7 emotion face-selection gate for one non-calm value (T105 OPEN implementation)  *(OPEN -- 2026-09-17)*

**Why.** M7 emotion window reads 0/25 per docs/SCOPE.md M7. T105 OPEN landed the recon only (worklog-only commit): emotion byte = 0x0203528F (asm00_2.s:27649), draw routine `drawEmotionWindow_801CDEC` (asm00_2.s:27554-27583), face bank `off_801CD08` (23-entry table, stride 0x180), shared right-half dword_872D914 (0x80 bytes); draw routine skips face on values 5 or 6; values 15..19 alias 5..9. src/emotion.rs:11 explicitly says only the calm face is drawn — no ticket has ported a non-calm face. This ticket implements T105's follow-ups: pick one reachable emotion value (value 1, T105's recon choice), port the face-selection gate with `const FACE_INDEX: [u8; N]` keyed by emotion value (honoring skip 5/6 + alias 15..19 → 5..9), add `emotion_syn_full` scenario with a poke to 0x0203528F, and a harness row `emotion_syn` (canon = real + poke, rust = plain_rom, negative = same scenario without poke). **New evidence.** T105 OPEN recon (worklog-only commit, unstamped 2026-09-17) gives the emotion byte, the face bank offset, and the skip rules, made AFTER the close of the prior M7 work (no prior non-calm-face port exists).

**Files.** `src/emotion.rs` (FACE_INDEX table + face-selection gate), `assets/emotion.bin` (verify face offsets per emotion value, no redraw), `tools/emotion_export.py` (extend to full 23-face bank, 0x1B30 bytes), `src/fixture.rs` (descriptor emotion field — only if descriptor byte 64→66 needed), `tools/states.py` (NEW `emotion_syn_full` scenario: battlestart.state + poke 60:0x0203528F:0x01 + scripted none), `tools/harness.py` (NEW row `emotion_syn` canon_ref=40, negative = same scenario without the emotion poke), `docs/coverage/emotion.md` (NEW), `docs/worklog/T109.md`. NOT `src/battle.rs`, `src/objects.rs`, `src/ai.rs`, `tools/allowlist.py`, `tools/inventory.py`, `tools/mgba_capture.c`, `reference/bn6f/`.

**Do.**
1. **Recon:** read BNEM header at `assets/emotion.bin` + every face's offset/width/height (currently 1 face, calm); cite each face's data start; confirm the emotion byte's reachable value list from canon's NCP set or `sub_801CADC` (asm00_2.s:25577 updater); confirm value 1 is reachable via poke (no hand-played input). **measurement.**
2. Baseline, no edit: build ROM, sha256 + size; verify_rows 9-row guard set + cursor → 0/0/N ×8, cursor 1/1/170; capture `emotion_syn_full` WITHOUT the emotion poke (both sides render calm face); read OBJ tiles at (0,18) and (32,18) — both sides calm. **measurement.**
3. Add `emotion_syn_full` + the `emotion_syn` row (canon = REAL + poke 60:0x0203528F:0x01; rust = plain_rom; negative = same scenario WITHOUT poke → both sides stay calm). Extend `tools/emotion_export.py` to the full 23-face bank (0x1B30 bytes). **code change + measurement.**
4. Port the face-selection gate in `src/emotion.rs`: read emotion byte 0x0203528F, index into `const FACE_INDEX: [u8; N]` keyed by emotion value (1 → face 1, 5/6 → skip, 15..19 → 5..9). Rebuild, sha256. **code change + measurement.**
5. Re-run: `emotion_syn` 0/0/40 with non-blind negative (broken pair ≥1 differing pixel); trace `emotion_syn_full`: emotion byte + OBJ tile at (0,18)/(32,18) first divergence **none** over the row's frames; guard set + cursor identical to step 2. **measurement.**

**Rules.** Art from canon bytes via `emotion_export.py`, not redrawn. The FACE_INDEX table is cited from canon's per-emotion draw site (off_801CD08 stride 0x180 + dword_872D914 right-half). State poke carries `peeked` provenance. Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, canon never changes. ≤6 captures, ≤80 tool calls. Land via `tools/land.sh` only after step 4's verify_rows PASS.

**Acceptance.** `emotion_syn` 0/0/40 with non-blind negative (broken pair ≥1 differing pixel); OBJ tiles at (0,18) and (32,18) match between canon and rust at each frame; trace `emotion_syn_full` first divergence none on emotion byte + OBJ tile; verify_rows PASS on 9-row guard set + cursor; FACE_INDEX cite in src/emotion.rs matches off_801CD08 (and dword_872D914 for the right-half dword). NEGATIVE naming canon's measured no-face-change behavior (frames + bytes + cite) closes it.

**Measure and report.** rows: emotion_syn + 9-row guard set + cursor · frames 40 each (cursor 170) · emotion byte cite + face offset per value · pixel totals (before / after) · OBJ tile counts at (0,18)/(32,18) on both sides · ROM sha256+size · fitted count · commit · one line of mechanism (the FACE_INDEX table size + skip rule) · one line unverified (the other ~24 emotion values, by value).

**Coordinator:** owns src/emotion.rs + asset layout + the row; runs alone (owns tools/states.py + tools/harness.py + tools/emotion_export.py). Free tier: verify_rows from a clean checkout on the 9-row set; verifier for the BNEM header + emotion-byte cites. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M7 (emotion window 0/25 → 1/25 first port).
