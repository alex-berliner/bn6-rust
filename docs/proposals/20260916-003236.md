# Proposed tickets 20260916-003236 (hyper/qwen3.8-flash, $0.1084)

Read what I needed (HANDOFF, AGENTS.md, the open-ticket text, `docs/inventory/panels.json`, `tools/harness.py`'s gunner note + `GUNNER_ROW`, `tools/scoreboard.py`'s CHIPS list, `ChipData.inc`, the three chip arms in `src/battle.rs`). Three tickets, fresh IDs (T30/T31 are already OPEN):

---

### T32. The gunner row's gauge band: name canon's custom-gauge word and `oBattleState_Index_00`, then seed `GUNNER_ROW` from the measurement  *(OPEN -- 2026-09-16)*

**Why.** `gunner` integrated is the largest row on the board, **2105613/38237/130** (neg 2284867, non-blind), and T9l's landed note (`tools/harness.py:2392-2430`, 424814d) already splits it: (1) k=0..5 every layer full-screen, canon's fade tail (OBJ means 93,77,60,44,28,12) against our dark start — BG1-only 230400 = exactly six full frames, BG1 **0** for k≥6; (2) **k=6..75 on BG3 — "the two enemy HP boxes (y0..15, x100..190) and the bottom-left custom gauge bar (y152, x12..60): canon's bar reads full from k=0 while GUNNER_ROW's borrowed gauge=0 never fills"**; (3) k≥77 — canon's gauge-full pause opens the chip window on BG3 (slide from canon ~156; "comes up on its own at frame 165", src/battle.rs:1998) plus a ~5.8k/frame OBJ plateau downstream of that pause. Our side is a one-bit seed: `tools/harness.py:1759` `GUNNER_ROW = dict(... gauge=0 ...)` → `src/battle.rs:2150` `Some(f) => if f.gauge != 0 { GAUGE_FULL } else { 0 }` (`GAUGE_FULL = 0x4000`, :1115, cite `sub_800855E` asm00_1.s:11100) → bar at :2568, auto-open arm at :2906. F40a was BLOCKED here ("settle whether the BG3 plateau is gauge-driven… Lever not settled"), and T9l's caveat says the word it tried, 0x020352a0, "reads 0 all 170 frames peeked — the full-bar evidence is visual". T26 landed the predicate that names the real one: `isCustGaugeFullAndBattleLive_800A21C` (asm00_1.s:15305) tests `gauge == 0x4000 && live`. Band 2's other half is F44's never-read word — its record says `oBattleState_Index_00` at **0x02034880** "has NEVER been read", so the HP-box gate's faithfulness on a SKIP_INTRO fixture (flags=0x11) is still an inference. A GUNNER_ROW-only change writes the descriptor at capture time, so the ROM stays byte-identical: an A/B with zero F48 cross-build ambiguity (T24/T25's precedent). Milestones **M5** (the Gunner) and **M8** (HP displays).

**Files.** `tools/harness.py` (GUNNER_ROW's `gauge` field :1759-1762 and that row's `note` text only), `docs/worklog/T32.md` (mandatory). **NOT** src/ (if the band needs an engine change, name the site and stop), **NOT** `tools/allowlist.py`, `tools/trace.py`, `tools/mgba_capture.c`, `tools/inventory.py`, `docs/SCOPE.md`; no row added/deleted, no `Align`/frames/region edit; `reference/bn6f` read-only.

**Do.** 1. Baseline, one session: `python3 tools/harness.py --only gunner --no-gallery` → the integrated line (expect 2105613/38237/130, neg 2284867) + `sha256sum` of the built ROM. 2. Name the word: read asm00_1.s:15305 and the routine that fills the bar → report the gauge word's symbol and address (resolve via `include/structs/*` + `bn6f.map`) and the field our `hud.set_gauge` mirrors. 3. One canon capture carrying `--watch <gauge>:4:… --watch 0x02034880:2:…` over the row's window → report both series per capture frame and the capture each one changes on. 4. Verdict with numbers: does canon's gauge read full from this row's `canon_ref=80`? does Index_00 read 4 across the window (F44's latch premise) or not? 5. If full: set `gauge=1` with a `// canon: <symbol>@<addr>, capture N` cite, re-run step 1 → per-band totals for k=6..75 and k≥77 before/after. 6. Prove the blast radius: grep GUNNER_ROW's consumers and report that it reaches only this row, then `verify_rows <branch> cursor,windowclose,field,mettaur --expect cursor=1/1/170/186279 --expect windowclose=0/0/40/207166 --expect field=0/0/40/1139 --expect mettaur=0/0/70/41734` and `cmp` both ROMs → 0 differing bytes.

**Rules.** One fixture field; no src/ change; no new row; no allowlist edit; `patch_sterile.py` forbidden; negatives stay non-blind; before/after in the same session with both ROM shas beside every non-zero total (a searched row's non-zero total is the at-risk class per docs/measurement-drift.md; `--with-main` is diagnostic-only and may never gate a merge). ≤4 gunner captures; tool budget ≤70. A gauge that needs a *series* to fit is a NEGATIVE, not a seed.

**Acceptance.** Either (a) `gunner` integrated < 1,960,000 with the four guard rows identical, cmp 0 and the note rewritten with the measurement; or (b) a NEGATIVE carrying canon's gauge symbol+address, both per-capture series, and the measured statement that band 2 is not gauge-driven — which closes F40a's lever and retires the seed route for this row. Either way the report names a canon word no ticket has ever read.

**Measure and report.** rows: gunner (integrated + isolated). frames 130. total/worst/negative before and after with both ROM sha256s; the three bands' totals; the two watch series with capture numbers; the four guard lines verbatim; fitted count (19) with T29's caveat that `provenance_counts()` scans only `ROOT/src/*.rs`; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch first and alone — no other open ticket touches GUNNER_ROW. Free tier: `verify_rows` on the four guards plus the direct `--only gunner`, gated on the **integrated** line with F48's matcher. A verifier IS warranted for step 2's word and step 3's series (a memory finding T34 and every later HUD ticket builds on): it opens the kept watch file and confirms the value at the named capture. ≤$0.35 expected, ≤$0.80 cap.

---

### T33. M3's first rung from the ROM: locate the writer of every panel type by walking `_object_setPanelType`'s call sites, and close M1's 8/13  *(OPEN -- 2026-09-16)*

**Why.** M3 is "not started", and M1's panels line is the closest thing to a table it has: FOUND `word_3007924` (IWRAM copy, asm/asm38.s:4242-4249 = ROM 0x081D7E24; 13 words, stride 4, one per type 0x0..0xC, OR-ed into `oPanelData_Flags` by `_object_updatePanelParameters`, asm/asm38.s:4213-4219) — **8/13 verified**. The five unverified sit in `docs/inventory/panels.json` as `"unverified: reader-only (no writer cite)"`: **0x0** hole (flag word 0x18000; readers asm38.s:4315-4317 tst→skip, object.s:1426-1428), **0x5** holy (0x12010; readers object.s:4831-4833 `cmp r1,#5 // holy panel?` + asm00_2.s:22788-22791 `sub_801A7F4` halves the damage sum — a live M3 damage rule with no writer), **0x8** (0x11010; reader object.s:1444-1450 regen 0x258/0x708), **0x9**, **0xA** (both 0x10210; readers asm38.s:4318-4326 `Unk_12=0x708`, object.s:1452-1458, asm00_2.s:21953-21960). The eight verified types were all closed the same mechanical way — a site with an immediate: object.s:2323 (`object_breakPanel`), 2218-2222 (`object_crackPanel`), 1460-1472 (`tickPanels` regen), asm31.s:6104-6105 / 6146-6147 / 6168-6169 (`sub_80BAE16` arms → 7/4/6), asm00_2.s:8300-8301 (`sub_8012792`, the stage chip), asm31.s:27855 / 27871 (`t3_0x0_80C4E58` → 0xC/0xB), asm31.s:38397. So the remaining walk is bounded, not open-ended. And nothing on main reads the 13-word table at all: `oPanelData_Flags|updatePanelParameters|3007924` greps empty across src/ (checked today) — the per-type flag semantics M3 needs are unmodelled, which is why the ladder cannot start M3 without this. Docs/tools only, byte-identical ROM, the cheapest unmet-rung advance left (T14 55c6b51, T15 152ce3c, T18 3f09aa7 each cost ~$0.05-0.15). Milestone **M3**, and M1's panels line.

**Files.** `tools/inventory.py` (the panels row data + section note), `docs/SCOPE.md` (regenerated only), `docs/inventory/panels.json` (regenerated output), `docs/worklog/T33.md` (mandatory). **NOT** src/ — not even a comment, T30 owns src/ comments; **NOT** `tools/harness.py` (T32), `tools/allowlist.py`, `tools/trace.py`; `reference/bn6f` read-only.

**Do.** 1. Baseline: `python3 tools/inventory.py` → quote the panels line exactly as it prints (8/13) and the five `unverified` rows verbatim. 2. Walk the writers: every `_object_setPanelType` call site and every inline write of `oPanelData_Type` in `reference/bn6f/asm` → report the call-site **count** and, per site, the type value loaded; the number of distinct types with a located writer. 3. Per unknown type (0x0,0x5,0x8,0x9,0xA): writer file:line + routine symbol + the event that reaches it (chip object, virus, `tickPanels` regen, stage record), or a written GAP naming the sites walked and the arm that refused. 4. Per type, name the reader whose behaviour the writer feeds and whether a chip in `tools/scoreboard.py`'s CHIPS list reaches it (name the row) → the count of live M3 candidates. 5. Encode the verdict in tools/inventory.py's row data and regenerate → quote the panels line before/after (8/13 → N/13). 6. Prove the no-op: `cargo build --release` twice, `cmp -l` the packed .gba against main's → report the diff count (must be 0) and the sha256 (main `1997be3b…`).

**Rules.** No src/ edit anywhere — step 6's cmp is that rule's acceptance. Never hand-edit docs/SCOPE.md below the generated marker. A writer is a file:line with a value in hand, never an inference from the flag word; not-found stays a written GAP with its trail (T15's rule), and the call-site count is what makes a GAP bounded. Re-check every cite against disk (the 357da2a re-cut moved bn6f's numbers; T30 lists landed tags that point at the wrong lines). ≤30 tool calls, zero captures.

**Acceptance.** Each of the five types lands a writer cite or a GAP with the walked site list, the `_object_setPanelType` call-site count quoted, the regenerated panels line at N/13 with N named per type, the M3 candidates named with their chip rows, and cmp 0. A NEGATIVE showing the five types are written only from stage/arm data already covered also closes it — that means the vocabulary is 8, not 13, and the ladder's denominator changes.

**Measure and report.** rows: none (byte-identical ROM; cmp diff count 0, ROM sha256). Panels line before/after; call-site count; the 5-row writer table; the candidate rows; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** no row check needed (the cmp is the no-op proof, T14/T15/T18 precedent); dispatch in parallel with T32 — `**Files.**` disjoint. A verifier IS warranted: writer cites are memory findings every M3 ticket will build on; it opens two cited sites and confirms the value loaded there. ≤$0.15 expected.

---

### T34. Chips as data, third pass: the Recov, Barrier and Vulcan amounts come from their own subfamily tables, not from a `match chip.id`  *(OPEN -- 2026-09-16)*

**Why.** T17 (8c1a62d) and T19 (2baad23) made the sword/blade arm data-driven, and SCOPE's M4 line still counts **10 of 411**; T28 (fd1be73) started the second arm and stopped at a reader (`src/chips.rs:74 read_chip_state`). Three arms are still keyed by chip id: `src/battle.rs:645 RECOV_HP: [u16; 9] = [10,30,50,80,120,150,200,300,1000]` (`// provenance: derived -- byte_80EC870, asm31.s:111044`) indexed `chip.id - CHIP_RECOV10` at :4258; :673-678 `BARR100_HP`/`BARR200_HP` (`byte_8020B2C, data/dat01.s:189`) behind `match id`; :504-511 `VULCAN1..SUPRVULC_SHOTS` (`dword_80EBFEC = 0xA050403, asm31.s:109878-109892`) feeding the peeked timing table at :531. T28's verifier read those tables byte-for-byte out of the pinned ROM and named the real route: the values are **subfamily-indexed** — `RecovHealBySubfamily_80EC870` (asm31.s:111132, body :111133-111134, 9 u16 + 2 pad, ROM offset 0x0EC870), `BarrierHpByType_8020B2C` (data/dat01.s:194, rows :195-202, stride 6), `VulcanShotsBySubfamily_80EBFEC` (asm31.s:110022, `.word 0xA050403` at :110023, 4 bytes from `VulcanShotFanOffsets_80EBFF0` which `src/shot.rs:89` already cites). And the key is already in our asset: `tools/chip_export.py`'s v2 tail ships effect_flags/attack_family/**attack_subfamily**/AttackParam1..4/LockoutFrames per record (:238 pack, :264 write), with `src/chips.rs` accessors behind T19's version guard (:66-70). So the chip id is the wrong key twice over — canon's key is the subfamily, and `chip_data_struct` (include/rom_structs/ChipData.inc) also carries AttackPower u16 @0x1a to test against. The 14 affected rows are all zero rows, which F48 proved structurally safe as gates. Milestone **M4**, and M1's chips line.

**Files.** `src/chips.rs` (accessors, and any new tail read), `src/battle.rs` (**only** the three arms :4255-4260, :666-682, :498-535 — never `chip_strike`'s landed arm, never `fight_latch`/`hp_readout_live`), `tools/chip_export.py` + `assets/chips.bin` (**only** if step 2 refutes the record byte), `tools/inventory.py` (the as-data derivation only), `docs/SCOPE.md` (regenerated), `docs/worklog/T34.md`. **NOT** `tools/harness.py` (T32), `tools/allowlist.py`, `src/fixture.rs`, `src/hud*.rs`, `src/backdrop.rs`; `reference/bn6f` read-only.

**Do.** 1. Baseline, one session, with the ROM sha256: `python3 tools/harness.py --no-gallery --ui isolated` → the 14 named rows with negatives (chip-recovery + recov30/50/80/120/150/200/300, 40 frames; chip-barrier/barr100/barr200, 80; chip-vulcan/vulcan2/vulcan3, 60/70/80) — the two the coordinator reproduced are `chip-recovery 0/0/40/1731` and `chip-barrier 0/0/80/41067`. 2. Parity table, zero captures, read out of `reference/bn6f/bn6f.gba`: per chip print AttackPower (+0x1a), AttackParam1..4 (+0x10..0x13), LockoutFrames (+0x14), AttackSubFamily (+0xc), the byte at that subfamily in the named table, and the src literal → report every agreement and mismatch. 3. Convert only what step 2 proves equal (record byte, else table[subfamily]); delete the id-keyed const and the `match id` → report the count of `CHIP_*` ids still indexing an amount in the three arms, before/after. 4. If a table must ship, widen the export append-only to v3, bump the version the guard checks, `cmp` every existing record's first 36 bytes → 0 diffs, and state that this is a ROM-version event (F48: T17/T19's asset change moved `field` by -23 px deterministically), so every later number names both shas. 5. Kill-check observability without a capture: for chip-recov300 and chip-barr200 report the k and region where the amount is drawn and quote the row's negative — a row that would not change if the amount were wrong tested nothing; name it. 6. Re-run step 1 plus `chip-use,cannon,field,tiles,gauge,mettaur,cursor` → report every line (cursor 1/1/170/186279, field 0/0/40/1139, tiles/gauge 0/0/8/12197, mettaur 0/0/70/41734, chip-use 0/0/30/7768); then `python3 tools/inventory.py` if the as-data state derives without a hand-maintained list → quote SCOPE's M4 line and M1 chips count before/after.

**Rules.** The v2 asset is the file unless step 2 forces v3, and then the tail is append-only and no existing offset moves. No fix-up table, and no `match` surviving a value step 2 proved equal — a mismatch is the finding, per chip with both numbers (T17's rule). Zero rows are the veto: any chip row non-zero, cursor >1/1/170, or mettaur/tiles/gauge/field non-zero → branch unmerged, reported. `provenance_counts()` fitted must stay ≤19, and you must state that it scans only `ROOT/src/*.rs` (T29) so it cannot attest a tools/ literal. Every literal you write gets `// canon:`/`// provenance:`; re-check cites against disk. ≤8 captures, tool budget ≤80; /tmp's four canon roots untouched; artifacts under `/tmp/bn-t34-*/`.

**Acceptance.** The 14-chip parity table with zero unexplained mismatches; no `CHIP_*` id indexing an amount in the three arms (before/after counts quoted); all 14 rows still 0/0 with step 1's negatives exactly reproduced; fitted ≤19; plus either a regenerated SCOPE naming the new as-data chips from the generator's own data or the written statement that it cannot be derived without inventing a list. A NEGATIVE that a record byte contradicts a measured literal is accepted, per chip with both numbers, and becomes the next ticket's list.

**Measure and report.** rows: the 14 chip rows, chip-use, cannon, field, tiles, gauge, mettaur, cursor. frames 40/80/60/70/30/8/70/170. totals/worst/negatives before and after per row, the id-count before/after, `provenance_counts()` before/after, both ROM sha256s, SCOPE's M4/M1 lines before/after, commit, one line of mechanism, one line of what is unverified.

**Coordinator:** dispatch after T30 lands or closes (it edits these same comment lines) and after T32 returns (it owns `tools/harness.py`). Free tier: `verify_rows` over the 14 rows plus the guard set with the landed negatives as expects — every gate here is a zero total, so no drift band applies and none may be widened. A verifier for step 2's parity table (the same ROM read T17's and T28's verifiers did) and, if v3 lands, for the append-only claim. ≤$0.40 expected, ≤$0.90 cap.

**Ordering note.** T32 leads because it is the only proposal that removes six figures of row divergence (largest single number on the board) for a byte-identical ROM. T33 next: no pixel movement, but M3 is the lowest unmet milestone and its per-item rows are unwritable until each panel type has a writer. T34 advances M4 and is refactor-only, gated by 14 zero rows. All three avoid M2's sequencer timeline, closed for this phase by T27's own rule and the no-fifth-ticket clause; T20/T21 stay blocked on the user, so nothing here needs hand-played input.
