# Proposed tickets 20260914-213014 (minimax/MiniMax-M3, $0.2182)

### T7g. End-sequence state machine port: the 5 integrated variants' results-window slide, the 4 AUDIT-6 allowlist entries, and the three 0x0C-window trace fields  *(OPEN -- 2026-09-14, follows T7f, advances M2)*

**Files.** src/battle.rs (the end sequence: `over`, BANNER_TO_RESULTS, the results hand-off), src/results.rs (the slide driver + per-tick redraw cited at `src/results.rs:713` for `sub_802BD60`, asm03_0.s:11550), src/banner.rs (the banner composite for `sub_801E754`'s leave predicate, asm00_1.s:10478-10514), tools/harness.py (integrated rows' Align notes only), tools/allowlist.py (deletions only, never additions or raised bounds), docs/coverage/battle_full.md (notes)

**Why.** M2 is the lowest unmet milestone. At HEAD (`docs/reviews/2026-09-14.md` scoreboard, commit `e8c928d`) **61 of 67 rows** read 0; the six non-zero are the five integrated rows opening 72499/2691/40, field 158993/5621/40, warp 40628, buster 54672, chip-use 275307 (all four of the last have AUDIT-6 allowlist entries at `tools/allowlist.py:54-57`, dated 2026-09-08 "HUD vs a zero-enemy arena, not compared before" — these violate the cross-cutting invariant "no allowlist entries"), and the documented cursor tear 1/1/170. F38 measured them as "canon RESULT slide w/o rust counterpart" (verifier-CONFIRMED for warp); F38b NEGATIVE proved no single constant serves them (with the resolve flag on, warp 40628→45751, buster 54672→81083, chip-use 275307→269172 — one BANNER_TO_RESULTS cannot sit at canon k=24..29 with the OBJ banner tail parked over k=0..9). Canon's mechanism is a state table: `sub_800801C` at `asm/asm00_1.s:10422` dispatches through `off_8008038`; the 0x0C handler `sub_80081A4` at `:10617` does the HUD teardown. T7e put canon's predicates under the window edges (battle_full sequencer 540/540 unaligned); T7f confirmed the window opens AND closes (100/100 on `windowclose_full`); T7j PARTIAL named banner composite as the residual of T7f's fitted `SEQ04_FRAMES=60`. The end sequence itself is the gate: how it counts from the killing blow, arms the banner composite (`sub_801E754` banner-idle check against HUD bit 0x8000, asm00_1.s:10478-10514, with timer arms 0x1e and 0x293), the `sub_802D6C4` return that gates the 0x20 leave (T7f's cited lines), the 0x0C handler's HUD teardown, and the results hand-off. The same end sequence owns three of battle_full's perma-divergences in the 0x0C window (`docs/trace/t7b/battle_full.diff.txt:30-32`): `mm_timer` canon=65535 rust=0 on 239/239 frames, `enemy_state_action` canon=(8,0) rust=(4,2) on 239/239, `enemy_anim` canon=0 rust=2 on 239/239 — plus the trace NOTE that "the 3-frame drift is OURS, not a canon divergence" (last compared row counter 536 vs stall-free 539). **5 pixel rows + 4 allowlist entries + 3 trace fields + 1 fitted constant on one mechanism — the highest rows-per-dollar in the queue.**

**Do.**
1. Baseline, no edits → *report per row total/worst/frames at HEAD (expected: opening ≤72499, field ≤158993, warp ≤40628, buster ≤54672, chip-use ≤275307) and the capture index at which canon's results window first moves per row (canon's zero-enemy band is F38b's k=24..29).*
2. Trace the end sequence frame-by-frame on battle_full with `tools/probe.py` from the killing blow to the results window's first slide-in frame: watch `dword_203CA70` (ewram.s:3040), `eBattleState.Index_01` (0x02034881, ewram.s:2572), the banner composite at 0x020368C0, HUD bit 0x8000 (the one `sub_801E754` reads at asm00_1.s:10478-10514), and 0x02036500 (window-relative blink counter, asm03_0.s:4801-4804) → *report, per side, the frame each named field reaches its end-sequence value, the writer PC for each write, and the two sides' sequences on a single timeline (killing blow to first slide-in).*
3. Port the end sequence as one state machine: the count from the killing blow, the banner composite's `sub_801E754` banner-idle check against HUD bit 0x8000 (asm00_1.s:10478-10514), the timer arms 0x1e and 0x293, the `sub_802D6C4` return that gates the 0x20 leave (T7f's cited lines), the 0x0C handler `sub_80081A4`'s HUD teardown (asm00_1.s:10617), and the results hand-off driven by `sub_802BD60` (asm03_0.s:11550, cited at `src/results.rs:713`) — so the slide's first frame follows from a state entering rather than a frame count → *cite each ported arm with `file:line`; report no `SEQ04_FRAMES=60` literal survives in `src/` (`grep -n SEQ04_FRAMES src/` proves it).*
4. Re-measure battle_full's 0x0C window on the same recording → *report the three perma-divergent fields (`mm_timer` 239/239, `enemy_state_action` 239/239, `enemy_anim` 239/239) closed to 0 or restated with their canon values and the arm that writes each; and the trace NOTE's 3-frame drift either resolved or renumbered (last compared row's counter 536 vs stall-free 539).*
5. Delete an `tools/allowlist.py:54-57` entry only for a row that now reads 0; re-run the whole table from a clean detached checkout → *report rows at 0 (HEAD: 61 of 67), `allowlist entries:` count (HEAD: 4 → expected 0), and any row still non-zero with its total/worst/frame.*

**Rules.** src/ changes only in the three named files; `tools/allowlist.py` by deletion only — never an addition and never a raised bound; no new harness row; no alignment change except by measured event (F2's rule); no per-row/per-fixture branch — one mechanism serves all five; every integrated row's isolated twin must stay 0/0 (warp, buster, field, opening, result, windowclose); `BANNER_TO_RESULTS` is either deleted or carries a `// provenance:` tag naming the ROM value it comes from, updated in the same commit; `fitted constants in src/` (HEAD: 19) MUST drop by exactly 1; canon never changes; ≤8 capture runs, one at a time inside the 3-slot semaphore, count disclosed honestly.

**Measure and report.** row: opening/field/warp/buster/chip-use integrated, plus battle_full, result, windowclose, cursor. frames: 540 (battle_full) + 40 per integrated row + 40 result + 40 windowclose + 170 cursor. total: per row before/after. worst: per row before/after. region: per row, the capture index where the slide starts on each side, and the diffmask region of any frame still non-zero. commit: src/battle.rs + src/results.rs + src/banner.rs + the allowlist deletions. One line of mechanism (the end-sequence state machine's named citations); one line of what is unverified (whether the buster integrated row's residue is the same mechanism or a separate one).

**Acceptance.** the five integrated rows read 0/0 with their negatives not blind, and the four `tools/allowlist.py:54-57` AUDIT-6 entries are gone (the cross-cutting invariant "no allowlist entries" then holds by measurement); or each surviving row is named frame by frame with both sides' sequencer trace and no allowlist entry kept for an unnamed residue. battle_full's sequencer stays 540/540, the three 0x0C-window fields are closed or restated with their writers, every isolated row still 0 with cursor's tear reported as it moves from 1/1/170, and `fitted constants` drops 19→18.

**Coordinator:** dispatch first. M2 is the lowest unmet milestone and this closes 5 integrated rows the user named as the only blocker before M2 can be called "battle_full trace: first divergence none; pixels 0 on all its frames". Worker muse-spark-1.3-contributor (T7c/T7d/T7e/T7f's child class), verifier GLM-5.3-flash cross-family; ≤$0.30 expected, ≤$0.70 cap; verify_rows on every row named, plus the full table twice on the same commit (before/after) since allowlist deletions are the evidence. Advances **M2**.

---

### T9h. Gunner port upstream of JumpOffset00 in sub_8026840: the AI handshake byte inside is_MegaMan_in_range?  *(OPEN -- 2026-09-14, follows T9g, advances M5)*

**Files.** tools/states.py (only if a poke fixes the AI handshake), tools/probe.py (the AI handshake write-watch), src/fixture.rs (enemy_kind = 1 = Gunner per T9c, panel-row-aware construction), src/objects.rs (the per-type `ForGunner_8113078` entry under `enemy_think`/`enemy_act`), src/ai.rs (the per-action tables at `off_8109050[0x17]` / `off_81091D0[0x17]`, asm32.s:10060-10290; CurAction 0x00-0x0B plus the six per-version parameter tables from `byte_80182C4`), src/actor.rs (the 0x0A/0x0B hop executors ref-checked against T9b's slot probe), assets/ (Gunner art extracted at comp_825BFC4 per T9b: 4588 B / 2 gfx blobs / 1 palette / 4 anims / 16 frames / 154 OAM), tools/harness.py (one new `gunner` row aligned by the first attack event), FIXTURE.md (the kind-1 fixture), docs/coverage/battlestart_gunner.md (corrected with the measured handshake)

**Why.** T9g NEGATIVE (`d43e4c0`, wt/t9g-gunner-extended 801ecda) wrote: "the AI handshake byte inside is_MegaMan_in_range? is the next stop". T9g's three measured negatives are verifier-minimax CONFIRMED for the 637-draw RNG-cadence measurement: (1) 700-frame watch with `--poke-at 165:0x020364C0:0x08` — Gunner slot1 (`0x0203ab68`) at f69 CurState=0x04/CurAction=0x00/HP 0x3c, CurAction 0x01 at f151 parks through f699 (549 frames), HP 0x3c and gauge 0x00200000 unchanged, sequencer 0x00→0x04000000 at f166 then held; (2) `--poke-at panel-row-3 cheat 0x0203a9c3:3` + trigger poke — identical trajectory, panel row 3 alone does not unstick the row-check chain; (3) write-watch 0x020013f0 over 700 frames — 637 unique seed values (~1 draw/frame), AI loop alive at RNG cadence. So the trigger poke unblocks the sequencer (T9e proved this — Index_01 0x02034881 4→0→8→0x0C at f165, dword_203CA70 0→0x04 at f166), T9f refuted the JumpOffset00=8 hypothesis (one-shot poke-at 155:0x020364C0:0x08 held through f199; state-4 handler `custMenuMainMaybe_8026A88` never re-enters), and panel row 3 is not the gate. What remains is the byte `is_MegaMan_in_range?` reads inside `sub_8026840` (asm03_0.s:525-704; called from `sub_8026A28` at asm03_0.s:761, the state-0 self-advance to state 4) — that byte is the AI handshake that decides whether the per-virus AI registers MegaMan as in attack range. M5 is "Mettaur done; Gunner in progress (T9c)" per SCOPE.md: T6 already landed the Mettaur as canon's per-type entry (`ForMettaur_8109EF4` at objects.rs, identical mechanism), T9b measured the Gunner art reproduces `assets/gunner.bin` byte-for-byte at comp_825BFC4, T9c landed the `battlestart_gunner` recipe and the per-slot `enemy_kind` (kind 1 = Gunner, fixture byte +5, 2 bits/slot). M5's only remaining step is the Gunner-as-data port the interpreters and T6 enable. A precise negative here is still a result — it closes M5 with the AI handshake byte named as the blocker and not the recipe.

**Do.**
1. Write-watch `sub_8026840`'s read set on `battlestart_gunner` from f0 to f700 with `tools/probe.py --watch-write` — the AI handshake byte is whichever read inside `sub_8026840` (asm03_0.s:525-704) flips between f151 (when CurAction becomes 0x01) and f699 (when the trace ends); the candidate set is the panel-coord bytes `0x0203a9c2..0x0203a9c5` (MegaMan's panel) plus the per-virus AI flags at `0x0203ab68..0x0203ab6d` (slot1) and `0x0203ab70..0x0203ab75` (slot0) → *report the byte that flips, the writer PC, and the values on both sides at f151 and f699.*
2. If the byte is a panel-coord: extend the panel-row poke to set the full coordinate canon reads (column included), re-run the same watch → *report whether the Gunner reaches an attack event (HP at `0x0203ab68+0x24` changes from 0x003c or gauge changes from 0x00200000) within 700 frames and at which frame; report an empty log if no movement, that is a result.*
3. If the byte is an AI flag (or no byte flips and the port is the named next stop): port `ForGunner_8113078` under `enemy_think`/`enemy_act` (`off_8109050[0x17]` / `off_81091D0[0x17]`, asm32.s:10060-10290; CurAction table 0x00-0x0B plus the six per-version parameter tables from `byte_80182C4`); wire `assets/gunner.bin` at kind-1 art; add one harness row `gunner` aligned by the first attack event measured in step 1 (event = first frame slot1 HP drops OR `0x020352A0`/`0x020352A2` changes from frozen `0x00200000`; canonical-align by the canon frame, search band confirmed unique) → *report, per arm actually ported, the `file:line` citation (T12's `tools/rom_enemy_tables.py` is the cross-check; the table idx 0x17 must resolve to `ForGunner_8113078`); report the row = 0/0 with negative not blind (kind-1 fixture, slot1 = Gunner, slot0 = Mettaur).*
4. Re-run `tools/verify_rows.py` from a clean detached checkout → *report the table identical to HEAD except the new gunner row (or, on NEGATIVE, no new row added); mettaur 0/0/70/41734, wave 0/0/90/3840, chip-cannon 0/0/40/9505, cursor 1/1/170 (the known tear) all unchanged; `fitted constants` count unchanged at 19.*

**Rules.** Only the named files; tools/harness.py for the new gunner row only (no edit to existing rows); assets/ for art extracted byte-for-byte at comp_825BFC4 per T9b's measurement; docs/coverage/battlestart_gunner.md rewritten before this ticket copies it forward with the measured handshake; no allowlist change of any kind; no alignment change except by measured event (F2's rule); the fix is a port under the interpreters (T6's `enemy_think`/`enemy_act`/`MettaurEntry` pattern) — no new hand-written pose and no per-scenario branch; every new literal in an edited line gets a `// provenance:` tag or the canon symbol; `fitted constants in src/` (HEAD: 19) may not increase; the kind-1 fixture must be backward-compatible (every existing row unchanged, kind 0 = Mettaur unchanged, T6's `MettaurEntry` path preserved); canon never changes; ≤8 capture runs total, one at a time inside the 3-slot semaphore.

**Measure and report.** row: gunner (new, aligned by first attack event), mettaur, wave, cursor, opening, chip-cannon (regression set). frames: 700 `battlestart_gunner`, 70 mettaur, 90 wave, 170 cursor. total: gunner before/after, fitted-constant count (must stay 19). worst: any pixel row that moves. region: for gunner, the slot1 (CurState, CurAction, timer, HP) pairs frame by frame for the alignment window both sides; for any pixel regression, the diffmask region and frame. commit: src/fixture.rs + src/objects.rs + src/ai.rs + src/actor.rs + assets/ + tools/harness.py (new row) + tools/states.py (recipe if step 2 passes) + docs/coverage/battlestart_gunner.md. One line of mechanism (the AI handshake byte named with its writer PC); one line of what is unverified (whether the byte is per-slot or shared across both viruses; whether the second attack's element/effect appears on the second-vs-third beats in the row's window — that's M3, not M5, and is out of scope here).

**Acceptance.** EITHER the gunner row = 0/0 with negative not blind (kind-1 fixture, slot1 = Gunner, slot0 = Mettaur) and the trace on the Gunner scenario shows the slot's CurState/CurAction/timers matching canon from spawn through the second attack, OR the report names the AI handshake byte that did not flip and what that rules out — a precise negative closes this ticket the same way T9e/T9f/T9g did; every existing isolated row unchanged; docs/coverage/battlestart_gunner.md corrected with the measured handshake byte; `fitted constants` not increased.

**Coordinator:** dispatch second, after T7g lands. M2 is the priority over M5; this is the M5 ticket whose measurement prep T9e/T9f/T9g already paid for (combined $0.04 + $0.10 + $0.15 in the cost ledger). Files are disjoint from T7g's (T7g is `src/battle.rs + src/results.rs + src/banner.rs + tools/allowlist.py`; this is `tools/probe.py + src/fixture.rs + src/objects.rs + src/ai.rs + src/actor.rs + assets/ + tools/harness.py new-row + FIXTURE.md + docs/coverage/`), so it can run alongside T13d per the two-worker rule once T7g is underway. Worker muse-spark-1.3-contributor (T6/T9c's child class — the Mettaur port and the Gunner recipe are already landed), verifier GLM-5.3-flash cross-family; ≤$0.30 expected, ≤$0.60 cap; verify_rows on every row named (mettaur, wave, cursor, opening, chip-cannon, the new gunner row). Advances **M5**.

---

### T13d. Audio probe on the cannon route: what canon fires, what ours doesn't, then gate  *(OPEN -- 2026-09-14, re-scoped from T13c BLOCKED, advances M9)*

**Files.** src/battle.rs (the chip-fire and chip-damage `play_sound` calls and their gate predicates only — IF the dump shows they fire), tools/probe.py + the audio dump path (the dump on the cannon route), docs/audio/baseline-cannon.md (new), docs/coverage/audio-cannon.md (new)

**Why.** T13c BLOCKED on a premise error the worker's audit caught: no chip-fire or chip-damage `play_sound` calls exist in `src/battle.rs` (only the T13b-gated buster hit at line 3130), no chip-fire/chip-damage wav ships with the build (assets/ has 47 bins + 1 wav), and the citation fragments were wrong (`sub_801A308` body reads `oBattleObject_Alliance XOR 1 + ORs oAIData_Unk_10`, contains no `PlaySoundEffect`; `sub_801B720` line number points inside `sub_801ABB8`). Per T13c's option (a), this ticket re-scopes to probe what canon fires on the cannon route first, then gate after the dump tells us what fires — adding arms before the dump is what the original ticket got wrong. M9 is "not started" per docs/SCOPE.md after T13/T13b (audio is "still not a harness verdict" per T13b's rules); this advances M9 closure without changing the harness. The cannon route is `tools/harness.py`'s `_chip_canon_0c("01")` (harness.py:868), which is at 0 px isolated — a clean baseline for the audio pass. M2 is closed at 540/540 unaligned (T7e); M9 is the next milestone behind the integrated variants' drift.

**Do.**
1. Run the audio dump on the existing cannon scenario (`tools/harness.py`'s `_chip_canon_0c("01")` route, harness.py:868) with a sterile ROM, and tree-solo each of the four audio channels (ch0/1/2/3, FIFO A; ch4/5/6/7, FIFO B per mgba_capture.c:631-641) → *report both trees' peak |sample| per channel, the onset frame and onset-pair index, and the RMS in the onset frame's window (canon AND ours); the buster fixture's per-channel peaks (T13: ch4 peak 0 zero-enemy, 11273 enemy-present at frames 118..130) are the control.*
2. From the dump, identify the canon-fired SFX samples (channels, time windows, frequency envelope) on the chip-fire and chip-damage paths → *report the sample IDs and the windows where they fire, with both sides' (canon AND ours) peak |sample|; an empty canon-fire list (i.e. canon is silent on this route) is a result, reported as one with the measured proof.*
3. If canon fires SFX samples that ours doesn't: locate the corresponding arms in `src/battle.rs` (or add them if missing per T13c's audit), gate each on its cited predicate (chip-fire on the sequencer's chip-fire event; chip-damage on `take_damage[enemy_idx] != 0`), re-run the dump → *report both trees' peaks again, the onset residual in pairs and in ms at the measured 95999.1 / 95589.4 Hz, and the per-channel envelope shape; if both trees are flat, that is a result, reported as one with the doc stating the absence.*
4. Write `docs/audio/baseline-cannon.md` (the same shape as `docs/audio/baseline-buster.md`) and `docs/coverage/audio-cannon.md` recording the per-channel peaks, the onset residual, and what is now parity and what is still residue → *report the doc's new finding section and the log file list.*

**Rules.** Only the named files; `tools/harness.py` notes only (no row added — audio is still not a harness verdict); `tools/mgba_capture.c`, `tools/states.py`, `tools/trace.py`, `FIXTURE.md`, `assets/`, `reference/bn6f` read-only; ≤8 capture runs, one at a time inside the 3-slot semaphore, count disclosed honestly; every `play_sound` change gets its `// provenance:` tag updated in the same commit; `fitted constants in src/` (HEAD: 19) may not increase; canon never changes.

**Measure and report.** row: cannon + full table (regression set). frames: 40 cannon, 540 battle_full. total: per-channel peak |sample| before and after. worst: any pixel row that moves. region: the onset-frame's diffmask region if a pixel regresses. commit: src/battle.rs (if SFX arms are added) + docs/. One line of mechanism (which SFX samples canon fires on chip-fire and chip-damage and where ours diverges — or that canon is silent on this route); one line of what is unverified (whether the audio dump captures all SFX arms, or whether some fire outside the compared window).

**Acceptance.** `docs/audio/baseline-cannon.md` and `docs/coverage/audio-cannon.md` exist with the measured numbers; the chip-fire and chip-damage SFX paths in `src/battle.rs` are gated by the cited predicates (if they exist) or correctly absent (if they don't, with the doc stating the absence); our per-channel trees match canon's onset frame and onset-pair index to within the buster-residual 635 pairs / 6.62-6.64ms class (or the report names the larger residual with its cause); the full harness table is identical to HEAD (`verify_rows.py` clean run; cursor's 1/1/170 tear reported as it moves; nothing worse); `fitted constants` unchanged or lower.

**Coordinator:** dispatch third, after T7g lands. M9 is lower priority than M2 trace gate but the cannon route is at 0 px isolated so the audio pass is purely additive on a clean baseline; this is T13c's one allowed follow-up (option a). Files are disjoint from T7g's and T9h's (T7g: `src/battle.rs + src/results.rs + src/banner.rs + tools/allowlist.py`; T9h: `tools/states.py + tools/probe.py + src/fixture.rs + src/objects.rs + src/ai.rs + src/actor.rs + assets/ + tools/harness.py + FIXTURE.md + docs/coverage/`; this is `src/battle.rs` for `play_sound` arms only + `tools/probe.py` + `docs/audio/` + `docs/coverage/audio-cannon.md`) — src/battle.rs is shared with T7g so T13d runs sequentially after T7g, never paired with it. Worker muse-spark-1.3-contributor (T13b's child class), verifier GLM-5.3-flash cross-family; ≤$0.20 expected, ≤$0.50 cap; verify_rows on the full table (audio is still not a harness verdict, but no other row may move). Advances **M9**.
