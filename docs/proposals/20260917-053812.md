# Proposed tickets 20260917-053812 (zai/glm-5.3, $0.1898)

### T98. M2 — The Regular chip on window timeout: measure canon's auto-pick, then port it

**Why.** SCOPE M2 names "chip selection rules (codes, Regular, Tag, PA recognition, folder draw/reshuffle)" — PA recognition and folder draw are closed (T86 NEGATIVE, T59 NEGATIVE); **Regular has no ticket** (grep TODO.md + TODO_ARCHIVE.md for "Regular": only T97's netbattle prose). Our side has zero Regular logic: grep `src/*.rs` for "regular" hits only agb's `RegularBackground` type (src/backdrop.rs:61, src/field.rs:13, src/results.rs:14) — src/custom.rs never reads a regular slot. Canon's mechanic is real: UI strings "Choose Regular Chip" / "as a Regular Chip!" at data/textscript/compressed/CompText86CEE84.s:194/:247, written by the folder menu; the custom window's OK/close path is custMenuPressOK_8028D3A (asm03_0.s:5437) → sub_8029110 (:6029). The scenario machinery exists with no hand-played input: T7d's `windowclose_full` trace scenario opens and closes the chip window, T7f confirmed its edges (0x00 enter+leave 144..146, 0x04 enter+leave 147..206) on both sides, and T80's State.patches + T7r's fixture seeding land the peeked-poke route. A timeout is passive — no button log needed.

**Files.** `src/custom.rs` (gauge-full close arm that reads the regular slot and adds its word to the hand, cite step 2's routine), `src/fixture.rs` (descriptor regular field, `// provenance: peeked` + file:line), `tools/states.py` (ONE new scenario `window_reg_full` = windowclose_full base + regular-slot poke + NO OK press), `tools/harness.py` (ONE new row `window_reg`: watch canon's hand word (address named in step 2) vs rust mirror 0x0203FFxx), `docs/coverage/custom.md`, `docs/worklog/T98.md`. **NOT** tools/trace.py, tools/oracle.py, tools/allowlist.py, tools/inventory.py, src/battle.rs, src/objects.rs, src/chips.rs, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. Baseline, no edit: build ROM, sha256; verify_rows on `wave,window,opening,chip-cannon,mettaur,windowclose,cursor` → expect 0/0/N ×6, cursor 1/1/170; trace `windowclose_full` → quote the seq.state edges as the reference. **measurement.**
2. Canon recon: walk the "Choose Regular Chip" handler to the save-RAM regular-slot address (file:line); poke it and --watch the hand words through a no-OK window close — report whether canon auto-adds the regular on gauge-full close, at which frame, and the word written. **measurement.**
3. Add `window_reg_full` + the `window_reg` row (canon = REAL + poked state; rust = plain_rom + descriptor regular field; negative = same scenario WITHOUT poke/field). **code change + measurement.**
4. Port the auto-add arm in src/custom.rs from step 2's cite; plumb the descriptor field in src/fixture.rs; build ROM, sha256. **code change + measurement.**
5. Re-run: `window_reg` 0/0/N with non-blind negative (unset regular → empty hand: watch AND pixels differ); trace `window_reg_full` seq.state first divergence **none** over the scenario's frames; verify_rows guard set identical to step 1. **measurement.**

**Rules.** The auto-add is ported from the cited routine — no fitted hand word, no fitted frame. State/descriptor pokes carry `peeked` provenance. Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, canon never changes. ≤6 captures, tool budget ≤70.

**Acceptance.** `window_reg` 0/0/N with non-blind negative; `window_reg_full` trace `seq.state` first divergence **none** over the scene's frames (both sides' close edges at the same frames); hand-word sequence equal on both sides at the close edge; 7-row guard set + cursor identical to step 1. NEGATIVE naming canon's measured no-auto-pick behavior (frames + words + cite) closes it.

**Measure and report.** rows: window_reg + 7-row guard set; frames as run. Regular-slot address + cite, auto-add frame + word, before/after pixel totals, worst, region, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified.

**Coordinator:** owns src/custom.rs + src/fixture.rs + the row; no open ticket to pair with (T8x/T9x all stamped). Free tier: verify_rows from a clean checkout on the 8-row set. ≤$0.20 expected, ≤$0.40 cap. Advances **M2**.

---

### T99. M4 — FireBrn1-3 as data: a fresh record family through the chip dispatch

**Why.** M4 reads 43/411 with as-data families {0x13,0x15,0x21} (tools/inventory.py:237); 29 more are verified-pixels through our own code. The unrecorded triads are named in canon's own name table: FireBrn1-3 at data/textscript/TextScriptChipNames0.s:65-71 (BblStar1-3 :86-92, DolThdr1-3 :98-104, ElcPuls1-3 :107-113, RskyHny1-3 :116-122, RlngLog1-3 :125-131) — **FireBrn has no prior ticket**. Closed families stay closed: Cannon (T36/T38/T51 NEGATIVE), Vulcan (T54 NEGATIVE), TrnArrw (T91 NEGATIVE — id 26 carries family 0x2D, the over-trigger trap), StepSwrd (T85 NEGATIVE — the family-wide recovery +1 fails, 670/152/40). The port shapes are proven: T17 swords (hit shape indexed by the record's AttackSubFamily, src/battle.rs:4391), T46 Bomb (record +0x1a AttackPower, merged 85f021c), T48 Barrier (amount from the record), T52 AirShot (family 0x21). M4 advances 43/411 → 46/411.

**Files.** `src/battle.rs` (ONE family arm at the use_chip dispatch keyed on `chip.family`, T17/T46 shape; cite the family's canon attack-spawn routine file:line per arm), `tools/states.py` (3 NEW `firebrn_select_<id>` scenarios, chip-cannon fixture base), `tools/harness.py` (3 NEW rows `chip-firebrn1/2/3`: canon_ref=40, negative = no-chip-fire), `docs/coverage/chips.md`, `docs/worklog/T99.md`. **NOT** tools/allowlist.py, tools/trace.py, tools/inventory.py (re-run for the M4 count only), src/chips.rs, src/fixture.rs, reference/bn6f.

**Do.**
1. Baseline, no edit: build ROM, sha256; verify_rows on the 7-row guard set + chip-cannon + chip-bomb + chip-sword → expect all 0/0/N. **measurement.**
2. Read the three FireBrn records in data/ChipDataArr.s (attack_family, attack_sub_family, chip_element, attack_power per id, file:line each); census EVERY other id whose attack_family byte matches (grep the whole table) and report the sharer list — T91's over-trigger check. **measurement.**
3. If every sharer is dispatchable by subfamily/element inside one arm: port the family arm citing canon's spawn routine; if a sharer makes the arm over-trigger (the TrnArrw trap), STOP — close NEGATIVE naming the sharer ids + bytes. **code change + measurement.**
4. Add the 3 scenarios + 3 rows; build ROM; run each chip-firebrn row; expect 0/0/40, non-blind negative (no-chip-fire). **measurement.**
5. verify_rows on guard set + chip-cannon + chip-bomb + chip-sword + 3 chip-firebrn; expect identical to step 1 except the new rows; cursor 1/1/170 unchanged; `python3 tools/inventory.py` M4 line 43/411 → 46/411. **measurement.**

**Rules.** No `match chip.id` arm for these ids — dispatch keys on the record. Every literal in an edited line gets a `// provenance:` or `// canon:` tag. Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, no F47-style broadening, canon never changes. ≤6 captures, tool budget ≤80.

**Acceptance.** 3 chip-firebrn rows 0/0/40 with non-blind negatives; the dispatch site keys on `chip.family` with no per-id arm (verifier-checked); guard set + cursor identical; SCOPE M4 chip count 43/411 → 46/411. NEGATIVE naming the sharer ids (record bytes + cites) or the first divergent row/frame/region closes it.

**Measure and report.** rows: 3 chip-firebrn + chip-cannon + chip-bomb + chip-sword + 7-row guard set + cursor; frames 40 each (cursor 170). Family/subfamily/element/power bytes per id, sharer census, before/after pixel totals, worst, region, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified.

**Coordinator:** owns src/battle.rs's family arm + the rows; no open ticket to pair with. Free tier: verify_rows from a clean checkout on the 12-row set. Verifier for step 3's record-keyed dispatch claim. ≤$0.15 expected, ≤$0.35 cap. Advances **M4**.

---

### T100. M5 — The ai_index-4 virus as data: port its per-type routine onto T87's landed recording

**Why.** M5 is 1/187 (Mettaur only). T87 DONE (landed 6e493bc) landed the recording: State `battlestart_ai4_rank0` (EVENT_681 poke 60:0x02001d58:0x0240 → encounter root 0x080b50b0, 12 ungated records; lever 60:0x0200a210:0x37a → 891 mod 12 = 3 → rec3 0x080b50e0) fields ai_index-4 rank v0 (NameID 0x0013) at frame 69, slot1 HP 0x5a byte-matching off_8109150[4] elem_hp (0x0810ae4c, stride 6); 40-frame determinism 0 px across two builds; verifier-confirmed (map-byte wedge negative; no attack armed through frame 330). The per-type dispatch is landed twice over: T12 DONE (byte_80182C4 identity rows → AIThinkTables_8109050[ai_index]) and T6 DONE proved the entry shape (MettaurEntry ForMettaur_8109EF4 in src/objects.rs, hand-written brain deleted). Gunner's NEGATIVEs were its poked-battle route (T9d: no writer for dword_203CA70 on either battlestart route); the T87 route is a real encounter-tree battlestart — no trigger poke, the sequencer runs.

**New evidence.** T92's NEGATIVE was infra-only (worker errored on a nonexistent worklog; no measurement disputed anything); T87's landed numbers above are measurements made after that close and are the fresh basis.

**Files.** `src/objects.rs` (NEW per-type entry for ai_index 4 + its think/act arms, T6's shape, cite per arm), `src/spr.rs` (sprite/tile/palette allocation for the new virus, only if needed), `assets/<name>.bin` (NEW — canon sprite-sheet dump with OAM/tile source cites; the gunner.bin / Gunner/ pattern), `tools/harness.py` (ONE new row `ai4rank0`: canon_ref=40, aligned on the frame-69 populate event, negative = frame shift ±2), `docs/coverage/viruses.md` (NEW), `docs/worklog/T100.md`. **NOT** src/ai.rs, src/battle.rs, src/fixture.rs, tools/states.py (the state exists), tools/trace.py, tools/inventory.py (re-run for the M5 line only), tools/allowlist.py, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. Name the virus: walk byte_80182C4's identity rows for ai_index 4 (NameID 0x0013) and AIThinkTables_8109050[4] → virus name + think/act entry addresses; cite file:line. **measurement.**
2. Baseline, no edit: build ROM, sha256; verify_rows 7-row guard set; capture the existing state on both sides over 40 frames past the populate event; report total/worst/first-frame (expected large — the virus is unported). **measurement.**
3. Port the per-type routine into src/objects.rs (spawn/materialize + idle + every arm the 40 frames exercise); dump canon art to `assets/<name>.bin`; build ROM, sha256. **code change + measurement.**
4. Add the `ai4rank0` row; run it; expect 0/0/40 with a non-blind frame-shift negative. **measurement.**
5. verify_rows on guard set + ai4rank0; trace on the scenario: enemy fields (enemy_state_action, enemy_anim) first divergence **none** over the row's 40 frames; every executed arm ported or listed out of scope with a cite. **measurement.**

**Rules.** The brain is the ported per-type entry — no hand-written state machine, no fitted pose frames; every arm cites file:line. Art is dumped from canon bytes, not redrawn. Alignment by the measured populate event only (F2's rule). Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, canon never changes. ≤6 captures, tool budget ≤90.

**Acceptance.** `ai4rank0` 0/0/40 with non-blind negative; enemy trace fields first divergence **none** over the row's frames; 7-row guard set + cursor identical to step 2; SCOPE M5 1/187 → 2/187. NEGATIVE naming the first unportable arm (file:line + frame) closes it.

**Measure and report.** rows: ai4rank0 + 7-row guard set; frames 40 each. Virus name + entry addresses, art source cites, before/after pixel totals, worst, region, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (the attack arms these 40 frames do not exercise).

**Coordinator:** owns src/objects.rs + assets/<name>.bin; no open ticket to pair with. Free tier: verify_rows from a clean checkout on the 8-row set. Verifier for step 3's per-arm cites. ≤$0.25 expected, ≤$0.50 cap. Advances **M5**.
