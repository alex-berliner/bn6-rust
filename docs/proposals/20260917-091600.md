# Proposed tickets 20260917-091600 (minimax/MiniMax-M3, $0.0804)

Looking at the open tickets, the next 5 advances against the ladder (M2 battle_full is heavily BLOCKED after T7u/T7v/T7w/T7x/T7y/T7z; M3 statuses/panels are BLOCKED after T35/T40/T41/T60/T64/T69/T72/T81/T83/T84; M4 record-dispatch is BLOCKED after T54/T85/T91/T94; M5 Gunner is BLOCKED after T9/T55/T37/T45/T55). Picking the unblocked fresh angles:

---

### T106. M7 charge shot dispatch: port one shot_kind cell of off_80117D4 into a fresh `buster_charge` scenario  *(OPEN)*

**Why.** SCOPE M7 prose lists "charge shots" with the M7 row at 0/25. T15 PARTIAL (LANDED 152ce3c) finds the charge-shot dispatch table off_80117D4 (asm/asm00_2.s:5789, 8 entries 0x06..0x0D by shot_kind) and the per-shot arm sub_8011818 (asm00_2.s:5801). Charge shots are reproducible from a F5b-pattern scripted input: HOLD A from f0 of the per-attack action state, NO window. The existing buster row uses TAP-A; a fresh `buster_charge` scenario builds on `afterdissolve_0x0c` with HOLD-A scripted. shot_kind=0x06 (Cannon) is the simplest cell — same Cannon attack power, only the held pose and palette differ. Success unblocks the other 7 shot_kinds. **No prior ticket on charge shots** (grep TODO/TODO_ARCHIVE for `charge_shot` returns nothing — T105 prose cites emotion only).

**Files.** `src/charge_shot.rs` (NEW: per-shot_kind dispatch; CANNON case; const table from off_80117D4 with `// provenance: off_80117D4 asm00_2.s:5789`), `src/battle.rs` (wire HOLD-A observation into t1_player_entry so the per-attack state sees the hold timer), `tools/states.py` (ONE scenario `buster_charge` = `afterdissolve_0x0c` + scripted HOLD-A@chip_pick + L@open), `tools/harness.py` (ONE row `buster_charge`: canon=HOLD scripted, negative=TAP-A@chip_pick → matches the existing buster row TAP-A trace), `docs/coverage/forms.md` (NEW), `docs/worklog/T106.md`. **NOT** src/chips.rs, src/objects.rs, src/ai.rs, src/emotion.rs, src/fixture.rs, tools/trace.py, tools/oracle.py, tools/allowlist.py, tools/inventory.py, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. **Recon:** cite every cell of off_80117D4 (asm00_2.s:5789) with its shot_kind; cite the per-shot OBP/palette table (off_81333B0 stride 0x10) with `// unnamed: <hold-pose palette table>` if the symbol is not in bn6f; cite sub_80118A4 (asm00_2.s:5837) palette-swap; cite the hold-timer counter file:line. **measurement.**
2. Baseline, no edit: build ROM, sha256 + size; verify_rows 8-row guard set (opening/mettaur/cannon/buster/chip-use/wave/windowclose/cursor) PASS; capture `buster_charge` TAP-scripted → first 32 frames match the existing buster row pixel-for-pixel (negative confirms scenario = TAP). **measurement.**
3. Add `buster_charge` scenario with HOLD scripted + the `buster_charge` row (canon=HOLD, negative=TAP). **code change + measurement.**
4. Port one charge-shot cell: src/charge_shot.rs `ChargeShot::update(shot_kind=0x06, hold_timer)` arm; src/battle.rs invokes from t1_player_entry when hold_A ≥ 24 frames. Rebuild, sha256+size. **code change + measurement.**
5. Re-run: `buster_charge` 0/0/32 with non-blind negative; OAM byte at the shot position first divergence **none**; 8-row guard set + cursor identical to step 2; verify_rows PASS is the veto. **measurement.**

**Rules.** Art from canon bytes, not redrawn. One const table per shot_kind, cited. Scripted HOLD carries `peeked` provenance. Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, canon never changes. ≤6 captures, tool budget ≤80.

**Acceptance.** `buster_charge` 0/0/32 with non-blind negative; OAM byte at shot position first divergence **none**; 8-row guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming canon's measured no-charge behavior (frames + bytes + cite) closes it.

**Measure and report.** rows: buster_charge + 8-row guard set; frames 32 each. Charge-shot cite + OBP/palette per shot_kind, before/after pixel totals, OAM byte trace, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (the other 7 shot_kinds).

**Coordinator:** owns src/charge_shot.rs + the row; runs alone (owns tools/states.py + tools/harness.py). Free tier: verify_rows from a clean checkout on the 9-row set; verifier for step 1's off_80117D4 + off_81333B0 cites. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M7 (forms charge-shot 0/25 → 1/25 first port).

---

### T108. M9 audio parity on the mettaur attack scenario: enemy-attack SFX, no chip fire  *(OPEN)*

**Why.** SCOPE M9 reads "the sound driver's battle side ... sample-exact". T95 DONE landed the audio comparison tool (wt/t95 @ 915164a). T13 DONE landed battle_init cue parity (9d52135). T96 NEGATIVE closed chip-cannon audio (wt/t96 @ 961f24c, branch unmerged — structural offset, not a sample diff). T13c BLOCKED was on cannon SFX gating. **No ticket has compared mettaur-attack audio** — the mettaur fires its own attack SFX on the player-hit event, no chip window. The `mettaur` row is 0/0/70 on HEAD; extending the audio trace to a 70-frame capture where the mettaur's first attack SFX lands (~k=61 per F17 oracle) is a fresh angle. New evidence: T96's structural-offset finding rules out the cannon-route as a useful repeat; T95's tool exists. Two scenarios are the M9 acceptance floor.

**Files.** `tools/audio_parity.py` (NEW: invoke T95's audio-compare CLI on the `mettaur` row's 70-frame capture, both sides; emit diff count + first divergent sample index), `tools/states.py` (extend `mettaur` scenario's audio-capture window to 70 frames; preserve the pixel-capture window at 70), `tools/harness.py` (add audio diff column to `mettaur` row's report; pixel diff unchanged), `docs/coverage/audio.md` (NEW), `docs/worklog/T108.md`. **NOT** src/, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. **Recon:** identify the mettaur's attack SFX trigger (cite sub_8109A40 MettaurEntry think/act arm if it dispatches to sub_800C700 sound queue; check the per-attack arm at ForMettaur_8109EF4 for SFX dispatch); cite the SFX channel & sample id file:line; check the mettaur attack-event frame (~k=61 per F17) in the audio trace. **measurement.**
2. Baseline, no edit: build ROM, sha256 + size; verify_rows 7-row guard set PASS; `tools/audio_parity.py mettaur` capture 70 frames both sides; report baseline audio diff count + first divergent sample + matched sample count. **measurement.**
3. Audit any SFX-event frame flagged by step 2: dump canon's per-frame sound-queue writes (peeked `0x020xxxxx` sound-queue), compare against rust's per-frame writes; name the divergence (which channel, which sample id). **measurement.**
4. If divergence is a fire-arm offset: port the gate in src/battle.rs (single-line trigger `if cur_phase == Some(Phase::EnemyAttack) { sound_play(SfxChannel::Enemy, sfx_id) }`). Rebuild, sha256+size. **code change + measurement.**
5. Re-run: `mettaur` audio 0 differing samples over 70 frames with non-blind negative (rust_no_sfx row → audio diff ≥1); 7-row guard set + cursor identical to step 2; verify_rows PASS is the veto. **measurement.**

**Rules.** Audio from canon bytes, not synthesized. Cite every SFX id from sub_800C700 dispatch. Sound-queue watch address carries `peeked` provenance. No allowlist, no patch_sterile, canon never changes. ≤4 captures, tool budget ≤60.

**Acceptance.** `mettaur` audio 0/0/70 samples with non-blind negative; 7-row guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming canon's measured no-SFX-difference (frames + samples + cite) closes it.

**Measure and report.** rows: mettaur (audio column) + 7-row guard set; samples 70 frames × 2 channels. SFX cite + per-frame sound-queue writes both sides, before/after diff count + first divergent sample, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (the cannon-route audio T96/T13c missed).

**Coordinator:** owns tools/audio_parity.py + tools/states.py + tools/harness.py; runs alone. Free tier: verify_rows from a clean checkout on the 7-row set; verifier for step 1's SFX cite + sound-queue watch. ≤$0.15 expected, ≤$0.30 cap.

**Milestone advanced:** M9 (audio parity 1 scenario → 2 scenarios).

---

### T109. M6 Navi scenario for AIIndex 0x03: T79 NEGATIVE was AIIndex 0x02 only; 0x03 is a different routine (no shared state-trace blocker)  *(OPEN)*

**Why.** SCOPE M6 reads "every boss in the ROM, the Cybeasts, Bass" at 25/25 FOUND via parse_navis (T76 DONE), 0/25 recorded. T79 NEGATIVE on AIIndex 0x02 closed wt/t79 @ b35ba92 because the player-object state-trace divergence was a single field offset, and the worker's attempted fix did not land. AIIndex 0x03 is a different navi — parse_navi rows give a different think/act entry, different sprite art, different encounter root byte. New evidence: parse_navis (T76) walks byte_80182C4 → AIThinkTables_8109050[ai_index] (T12's chain for viruses, applied to navis). The M6 increment requires a fresh scenario + new row, which AIIndex 0x03 can deliver without repeating T79's blocker.

**Files.** `tools/inventory.py` (verify parse_navis identifies the 0x03 navi's name, AI think/act entry, sprite slot; cite file:line for the navi struct fields), `tools/states.py` (ONE scenario `navi_ai3_rank0` derived from `battlestart_ai4_rank0` per T87, swapping AIIndex + corresponding encounter root), `tools/harness.py` (ONE row `navi_ai3`: canon_ref=40, negative = same scenario with the navi object removed; baseline = current `mettaur` row's first 10 frames), `docs/coverage/navis.md` (NEW), `docs/worklog/T109.md`. **NOT** src/, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. **Recon:** parse_navis for AIIndex 0x03: name, AIThink entry cite (asm31.s off_80F24D8/off_80F253C/off_80F25A0), sprite category (constants/enums/sprite_categories.inc:17-18), expected battle-state dump. Write the encounter-tree root byte and the per-rank records. **measurement.**
2. Baseline, no edit: build ROM, sha256 + size; verify_rows 7-row guard set PASS; capture `navi_ai3_rank0` 40 frames both sides; report first divergent field + frame. **measurement.**
3. Add `navi_ai3_rank0` scenario + `navi_ai3` row (canon=REAL navi, negative=removed navi → matches `mettaur` row's first 10 frames since no navi yet). **code change + measurement.**
4. If step 2 surfaces a state-trace divergence on the navi object: walk the divergence to a single field (cite file:line in parse_navi output); port the field-write in src/objects.rs. Rebuild, sha256+size. **code change + measurement.**
5. Re-run: `navi_ai3` 0/0/40 with non-blind negative; state trace: navi object fields first divergence **none**; 7-row guard set + cursor identical to step 2; verify_rows PASS is the veto. **measurement.**

**Rules.** Cite every routine field from parse_navis output. Scenario via the battle-state flag poke (T87 pattern), not hand-played input. Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, canon never changes. ≤4 captures, tool budget ≤60.

**Acceptance.** `navi_ai3` 0/0/40 with non-blind negative; state trace: navi object fields first divergence **none**; 7-row guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming canon's measured no-navi behavior (frames + bytes + cite) closes it.

**Measure and report.** rows: navi_ai3 + 7-row guard set; frames 40 each (cursor 170). Navi struct cite + per-field state trace both sides, before/after pixel totals, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (the other 22 navi AIIndex entries).

**Coordinator:** owns tools/inventory.py + tools/states.py + tools/harness.py; runs alone. Free tier: verify_rows from a clean checkout on the 8-row set; verifier for step 1's parse_navis cite + scenario flag-poke. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M6 (navis 0/25 → 1/25 first recorded + ported).

---

### T110. M8 first formation row from T65 census on encounter-tree list 0x14 (16 ungated records, distinct from T90/T93's list)  *(OPEN)*

**Why.** SCOPE M8 reads "every arena backdrop ... all banners and popups ... results variants" with formations 0/1076 unrecorded. T65 DONE landed the 1076-array formation census (off_8020170 + scripted BattleSettingsList0/1). T90 PARTIAL landed a docs-only first-formation branch (wt/t90 @ 98ddac0) — no harness row. T93 NEGATIVE closed a follow-up (worklog-only, worker errored on the harness row). T90 named two encounter-tree lists as candidates (encounter tree list 0x14 = 16 ungated records, list 0x37 = 11 ungated); the harness row stayed open. **New evidence:** T90 PARTIAL named list 0x14 as a candidate that wasn't tried; T93 NEGATIVE errored before reaching the capture. List 0x14 is a fresh angle with no shared blocker.

**Files.** `tools/states.py` (ONE scenario `formation_l14` derived from T90's pointer walk, via the EVENT_681 flag poke at 60:0x02001d58:0x0240 with a list-0x14 root), `tools/harness.py` (ONE row `formation_l14`: canon=REAL formation, negative=removed formation → matches `mettaur` row's first 10 frames since no formation), `docs/coverage/formations.md` (extend with the l14 census), `docs/worklog/T110.md`. **NOT** src/, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. **Recon:** confirm the l14 encounter-tree root byte (cite T90's pointer walk + reference/bn6f/bn6f.gba record stream); write the per-rank record breakdown (16 ungated 0xF0-terminated arrays per T65). **measurement.**
2. Baseline, no edit: build ROM, sha256 + size; verify_rows 7-row guard set PASS; capture `formation_l14` 40 frames both sides; report first divergent field + frame. **measurement.**
3. Add `formation_l14` scenario + `formation_l14` row (canon=REAL formation, negative=removed formation → matches `mettaur` row's first 10 frames). **code change + measurement.**
4. If step 2 surfaces a state-trace divergence on the formation fields: walk the divergence to a single field (cite file:line in T65's parse output); port the field-write in src/objects.rs. Rebuild, sha256+size. **code change + measurement.**
5. Re-run: `formation_l14` 0/0/40 with non-blind negative; state trace: formation fields first divergence **none**; 7-row guard set + cursor identical to step 2; verify_rows PASS is the veto. **measurement.**

**Rules.** Cite every record from T65's census. Scenario via the battle-state flag poke (T87 pattern), not hand-played input. Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, canon never changes. ≤4 captures, tool budget ≤60.

**Acceptance.** `formation_l14` 0/0/40 with non-blind negative; state trace: formation fields first divergence **none**; 7-row guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming canon's measured no-formation behavior (frames + bytes + cite) closes it.

**Measure and report.** rows: formation_l14 + 7-row guard set; frames 40 each (cursor 170). Formation record cite + per-field state trace both sides, before/after pixel totals, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (the other 1075 formations).

**Coordinator:** owns tools/states.py + tools/harness.py + docs/coverage/formations.md; runs alone. Free tier: verify_rows from a clean checkout on the 8-row set; verifier for step 1's T65 record cite + scenario flag-poke. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M8 (formations 0/1076 → 1/1076 first recorded + ported).

---

### T111. M5 third-virus row: leverage T87 DONE's AIIndex-4 routine into src/rank4_virus.rs + add `virus_ai4` row  *(OPEN)*

**Why.** SCOPE M5 reads "every family and rank" with Mettaur done (1/187) and Gunner in progress. T87 DONE landed the AIIndex-4 encounter root (12 ungated records at 0x080b50b0) and a new State `battlestart_ai4_rank0`. T92 NEGATIVE closed an attempted row (worklog-only, worker errored). M1 viruses table (T12) is FOUND; byte_80182C4 identity rows + off_8109150 Struct2 are the canonical sources. The per-type routine is at the same per-type entry as T6's MettaurEntry / T9j's GunnerEntry. **New evidence:** T92 NEGATIVE was on the scenario+capture, NOT on the routine — the AIIndex-4 routine is still landable as a row when the scenario is rebuilt fresh from T87's encounter root. T87's routine file gives the bytes to port; the blocker T92 hit (capture error) is reproducible with a fresh build from a clean worktree.

**Files.** `src/rank4_virus.rs` (NEW: ForAIIndex4_810xxxx per-type entry, ported from byte_80182C4 + off_8109150 Struct2 cite), `tools/states.py` (ONE scenario `virus_ai4_rank0` = `battlestart_ai4_rank0` scenario from T87 + scripted none), `tools/harness.py` (ONE row `virus_ai4`: canon=REAL AIIndex-4 virus, negative=removed virus → matches `mettaur` row's first 10 frames), `docs/coverage/viruses.md` (NEW), `docs/worklog/T111.md`. **NOT** src/objects.rs, src/ai.rs, src/battle.rs, src/fixture.rs, tools/trace.py, tools/oracle.py, tools/allowlist.py, tools/inventory.py, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. **Recon:** identify AIIndex 4's per-type routine from byte_80182C4 + off_8109150 Struct2 (cite file:line); write the routine
