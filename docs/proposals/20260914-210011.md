# Proposed tickets 20260914-210011 (minimax/MiniMax-M3, $0.1199)

I have enough to write the three tickets. They are ordered by rows-to-zero per dollar against docs/SCOPE.md (M2 before content):

**T7g** (M2) — closes 5 integrated rows + 4 allowlist entries + 3 trace fields in one mechanism (the end-sequence state machine). The highest-leverage ticket in the queue.

**T7j** (M2) — closes 1 fitted constant (`SEQ04_FRAMES=60`) by porting the banner composite that `sub_801E754` reads. T7f's peeked residual.

**T9h** (M5) — closes the Gunner row. T9g's named next stop is the AI handshake byte inside `is_MegaMan_in_range?` (`sub_8026840`).

---

### T7g. The battle's tail as canon's sequencer arms: the five integrated rows from allowlisted to 0, and the AUDIT-6 entries from 4 to 0  *(OPEN -- 2026-09-14, follows T7e and T7f, advances M2)*

**Files.** src/battle.rs (the end sequence: `over`, BANNER_TO_RESULTS, the results hand-off), src/results.rs (the slide driver), src/banner.rs (what the 0x0C arm tears down), tools/harness.py (the integrated rows' Align notes only), tools/allowlist.py (deletions only), docs/coverage/battle_full.md (notes)

**Why.** M2 is the lowest unmet milestone and its pixel gate is the five integrated rows. At HEAD (`docs/reviews/2026-09-14.md` scoreboard, commit `e8c928d`) **61 of 67 rows** read0; the six non-zero are **opening** integrated 72499/2691/40 (negative 155601, no allowlist — it has regressed above its old 28000 bound), **field** integrated 158993/5621/40 (allowlist 28000), **warp** integrated 40628 (allowlist 19500), **buster** integrated 54672 (allowlist 28000), **chip-use** integrated 275307 (allowlist 28000), and the documented **cursor** tear 1/1/170. The four AUDIT-6 entries at `tools/allowlist.py:54-57` (dated 2026-09-08, "HUD vs a zero-enemy arena, not compared before") hold four of those five rows open and **violate the daily-review cross-cutting invariant "no allowlist entries"** — they are not measured defects, they are un-modelled coverage. F38 measured them as "canon RESULT slide w/o rust counterpart" (verifier-CONFIRMED for warp); F38b NEGATIVE proved no single constant serves them — with the resolve flag on, warp 40628→45751 (isolated 0→5123), buster 54672→81083 (isolated 0→2286), chip-use 275307→269172 (isolated 0→4026) — because one `BANNER_TO_RESULTS` cannot sit at canon k=24..29 with the OBJ banner tail parked over k=0..9. Canon's mechanism is a state table, not a frame count: `sub_800801C` at `asm/asm00_1.s:10422` dispatches through `off_8008038`; the **0x0C** handler `sub_80081A4` at `:10617` does the HUD teardown; the results driver `sub_802BD60` at `asm/asm03_0.s:11550` is already cited at `src/results.rs:713` for the per-tick redraw. T7e put canon's predicates under the **window** edges (battle_full sequencer 540/540 unaligned); T7f confirmed the window opens AND closes (100/100 on `windowclose_full`); the residual is the **end sequence itself**: how it counts from the killing blow, arms the banner composite, and hands off to the results state. The same end sequence owns three of `battle_full`'s perma-divergences in the 0x0C window (`docs/trace/t7b/battle_full.diff.txt:30-32`): `mm_timer` canon=65535 rust=0 on **239/239 frames**, `enemy_state_action` canon=(8,0) rust=(4,2) on **239/239**, `enemy_anim` canon=0 rust=2 on **239/239** — plus the trace NOTE that "the 3-frame drift is OURS, not a canon divergence" (4 repeated rows, 130 jumped, last compared row counter 536 vs stall-free 539). **Five pixel rows + four allowlist entries + three trace fields on one mechanism — the highest rows-per-dollar in the queue.**

**Do.**
1. Baseline, no edits → *report per row total/worst/frames at HEAD (expected: opening ≤72499, field ≤158993, warp ≤40628, buster ≤54672, chip-use ≤275307) and ten integers — the capture index at which canon's results window first moves and ours does, per row; canon's zero-enemy band is F38b's k=24..29.*
2. Trace the end sequence frame-by-frame on `battle_full` with `tools/probe.py` from the killing blow to the results window's first slide-in frame: watch `dword_203CA70` (ewram.s:3040), `eBattleState.Index_01` (0x02034881, ewram.s:2572), the banner composite at 0x020368C0, the HUD bit 0x8000 (the one T7f cites for `sub_801E754`'s banner-idle check at asm00_1.s:10478-10514), and 0x02036500 (window-relative blink counter, asm03_0.s:4801-4804) → *report, per side, the frame each named field reaches its end-sequence value, the writer PC for each write, and the difference between the two sides' sequences on a single timeline (killing blow to first slide-in).*
3. Port the end sequence as one state machine: the count from the killing blow, the banner composite's `sub_801E754` banner-idle check against HUD bit 0x8000 (asm00_1.s:10478-10514), the timer arms `0x1e` and `0x293`, the `sub_802D6C4` return that gates the 0x20 leave (T7f's cited lines), the 0x0C handler `sub_80081A4`'s HUD teardown, and the results hand-off driven by the per-tick redraw already cited at `src/results.rs:713` (`sub_802BD60`, asm03_0.s:11550) — so the slide's first frame follows from a state entering rather than from a frame count → *cite each ported arm with `file:line`; report no fitted frame-count constant survives on strict-provenance reading.*
4. Re-measure `battle_full`'s 0x0C window on the same recording → *report the three perma-divergent fields (`mm_timer` 239/239, `enemy_state_action` 239/239, `enemy_anim` 239/239) closed to 0 or restated with their canon values and the arm that writes each; and the trace NOTE's 3-frame drift either resolved or renumbered (currently: last compared row's counter 536 where a stall-free clock reads 539).*
5. Delete an allowlist entry only for a row that now reads 0; re-run the whole table from a clean detached checkout → *report rows at 0 (HEAD: 61 of 67), `allowlist entries:` (HEAD: 4 → expected 0), and any row still non-zero with its total, worst and frame.*

**Rules.** src/ changes only in the three named files; tools/allowlist.py by deletion only, never an addition and never a raised bound; no new harness row; no alignment change except by measured event (F2's rule); no per-row/per-fixture branch — one mechanism serves all five; every integrated row's isolated twin must stay 0/0 (warp, buster, field, opening, result, windowclose); `BANNER_TO_RESULTS` is either deleted or carries a `// provenance:` tag naming the ROM value it comes from, updated in the same commit; `fitted constants in src/` (HEAD: 19) may not increase; canon never changes; ≤8 capture runs, one at a time inside the 3-slot semaphore, count disclosed honestly.

**Measure and report.** row: opening/field/warp/buster/chip-use integrated, plus battle_full, result, windowclose, cursor. frames: 540 (battle_full) + 40 per integrated row + 40 result + 40 windowclose + 170 cursor. total: per row before/after. worst: per row before/after. region: per row, the capture index where the slide starts on each side, and the diffmask region of any frame still non-zero. commit: src/battle.rs + src/results.rs + src/banner.rs + the allowlist deletions. One line of mechanism (the end-sequence state machine's named citations); one line of what is unverified (whether the buster integrated row's residue is the same mechanism or a separate one).

**Acceptance.** the five integrated rows read 0/0 with their negatives not blind, and the four `tools/allowlist.py:54-57` AUDIT-6 entries are gone (the cross-cutting invariant "no allowlist entries" then holds by measurement); or each surviving row is named frame by frame with both sides' sequencer trace and no allowlist entry kept for an unnamed residue. `battle_full`'s sequencer stays 540/540, the three 0x0C-window fields are closed or restated with their writers, every isolated row still 0 with cursor's tear reported as it moves from 1/1/170, and `fitted constants` unchanged or lower.

**Coordinator:** dispatch first, only after T7f lands (it consumes T7f's window predicates and its acceptance restates the table T7f left). Worker muse-spark-1.3-contributor (T7c/T7d/T7e/T7f's child class), verifier GLM-5.3-flash cross-family; ≤$0.30 expected, ≤$0.70 cap; verify_rows on every row named, plus the full table twice on the same commit (before/after) since allowlist deletions are the evidence. Advances **M2**.

---

### T7j. Model the banner composite for sub_801E754's leave predicate: replace SEQ04_FRAMES=60 with the canonical banner-idle check  *(OPEN -- 2026-09-14, T7f's peeked residual, advances M2)*

**Files.** src/banner.rs (the banner composite and the HUD bit 0x8000 watch), src/battle.rs (the 0x04 leave edge only — the fitted `SEQ04_FRAMES=60` constant), tools/trace.py (only if a watch field must be added), tools/harness.py (the sequencer notes only), docs/coverage/windowclose.md (the measured predicate)

**Why.** T7f PARTIAL left one residual: the canonical leave predicate `sub_801E754`'s banner-idle check against HUD bit 0x8000 (asm00_1.s:10478-10514, with timer arms `0x1e` and `0x293`) needs **banner composite not modelled**, so `SEQ04_FRAMES=60` (`src/battle.rs`) stays peeked. T7f's result: "100/100 sequencer frame match on re-record; edges named in code as sub_8008452/sub_800840C/sub_8008064 with timer arms 0x1e/0x293 in comments; SEQ04_FRAMES=60 stays peeked because the asm timer arms are display parameters not countdown timers and the canonical leave predicate (sub_801E754 banner-idle check against HUD bit 0x8000) needs banner composite not modelled; verify_rows did not get a clean run before budget; worktree clean (no diff to land); no fitted frame-count constants survive on strict-provenance reading". The fitted constant violates the project's "no fitted constants" cross-cutting invariant — it is a literal in `src/` with no `// provenance:` tag and no ROM citation, and `AGENT_GUIDE.md:32` defines `fitted` as "matched to the picture, mechanism unknown". `SEQ04_FRAMES=60` is one of **19 fitted constants** still in `src/` (per `docs/reviews/2026-09-14.md` hygiene). M2's sequencer acceptance is "battle_full trace: first divergence none" and the fitted constant sits in the path that the T7e sequencer arms set up; closing it advances M2 by removing one fitted constant AND closes the last predicate T7f named but couldn't implement. The verifier's note: "the canonical leave predicate needs banner composite not modelled" names exactly the missing piece.

**Do.**
1. Re-record `tools/states.py`'s `windowclose_full` scenario on the current build and trace `0x020368C0` (the banner composite field, ewram.s lookup) plus HUD bit 0x8000 (the AIData bit `sub_801E754` reads) plus the `0x02036500` blink counter (asm03_0.s:4801-4804) across the 0x04 hold window (canon frames 147..206, k=25..84 per T7f) → *report, per side, the byte/halfword values for the banner composite on every frame of the 0x04 hold, the HUD bit's set/clear per frame, and the first frame `sub_801E754`'s banner-idle check would return 0.*
2. Port the banner composite model: a struct that mirrors `0x020368C0` (whatever fields it holds — read with `tools/probe.py --read` and parse the disassembly `reference/bn6f/asm/asm03_0.s` for the layout) and the HUD bit 0x8000 reader; on the 0x04 leave edge, replace `SEQ04_FRAMES=60` with the predicate `banner_composite.is_idle() && (hud & 0x8000) == 0` evaluated against the just-traced state → *cite the `sub_801E754` call site (asm00_1.s:10478-10514) and report no `SEQ04_FRAMES=60` literal remains in `src/`.*
3. Re-run `tools/verify_rows.py` and the `windowclose_full` sequencer trace → *report the table identical to HEAD except any pixel movement the new predicate causes (must be ≤ the cursor tear), the 0x04 leave frame on both sides (canon frame 206 vs ours — must match), and the fitted-constant count (must drop by1, 19→18).*

**Rules.** Only the named files; no allowlist change; no new harness row; no descriptor or alignment change; the fix is a port of `sub_801E754`'s check — no fitted countdown; every new literal gets a `// provenance:` tag with the cite `asm00_1.s:10478-10514` or the canon symbol; `fitted constants in src/` (HEAD: 19) MUST drop by exactly 1; canon never changes; ≤4 capture runs, one at a time inside the 3-slot semaphore.

**Measure and report.** row: windowclose, cursor, result, mettaur, wave, opening, chip-cannon (regression set), plus the fitted-constant count. frames: 40 windowclose, 170 cursor, 540 battle_full sequencer. total: fitted-constant count before and after (19 → 18). worst: any pixel row that moves. region: the 0x04 leave frame's diffmask region. commit: src/banner.rs + src/battle.rs. One line of mechanism (`sub_801E754`'s banner-idle check on the modeled composite); one line of what is unverified (whether the banner composite has fields not exercised by `windowclose_full` that the cannon/buster paths would see).

**Acceptance.** `SEQ04_FRAMES=60` no longer in `src/` (grep proves it); the 0x04 leave frame on `windowclose_full` matches canon's frame 206 on both sides (the sequencer trace says so); the new predicate is cited `asm00_1.s:10478-10514` with a `// provenance:` tag in the same commit; `fitted constants` count drops 19→18; the full harness table identical to HEAD except where the predicate moves cursor's tear (≤10 px); battle_full sequencer remains 540/540 unaligned.

**Coordinator:** dispatch second (after T7g lands, so the banner composite is built on top of T7g's end-sequence state machine, not against the peeked SEQ04_FRAMES=60). Worker muse-spark-1.3-contributor (T7f's child class), verifier GLM-5.3-flash cross-family; ≤$0.15 expected, ≤$0.40 cap; verify_rows on every row named plus the fitted-constant count. Advances **M2**.

---

### T9h. Gunner port upstream of JumpOffset00 in sub_8026840: the AI handshake byte inside is_MegaMan_in_range?  *(OPEN -- 2026-09-14, T9g's named next stop, advances M5)*

**Files.** tools/states.py (only if a poke fixes the AI handshake), tools/probe.py (the AI handshake write-watch), src/fixture.rs (enemy_kind = 1 = Gunner per T9c, panel-row-aware construction), src/objects.rs (the per-type `ForGunner_8113078` entry under `enemy_think`/`enemy_act`), src/ai.rs (the per-action tables at `off_8109050[0x17]` / `off_81091D0[0x17]`, asm32.s:10060-10290; CurAction 0x00-0x0B plus the six per-version parameter tables from `byte_80182C4`), src/actor.rs (the 0x0A/0x0B hop executors ref-checked against T9b's slot probe), assets/ (Gunner art extracted at comp_825BFC4 per T9b: 4588 B / 2 gfx blobs / 1 palette / 4 anims / 16 frames / 154 OAM), tools/harness.py (one new `gunner` row aligned by the first attack event), FIXTURE.md (the kind-1 fixture), docs/coverage/battlestart_gunner.md (corrected with the measured handshake)

**Why.** T9g NEGATIVE (`d43e4c0`, wt/t9g-gunner-extended 801ecda) wrote: "the AI handshake byte inside is_MegaMan_in_range? is the next stop". T9g's three measured negatives are verifier-minimax CONFIRMED for the637-draw RNG-cadence measurement: (1) 700-frame watch with `--poke-at 165:0x020364C0:0x08` — Gunner slot1 (`0x0203ab68`) at f69 CurState=0x04/CurAction=0x00/HP 0x3c, CurAction 0x01 at f151 parks through f699 (549 frames), HP 0x3c and gauge0x00200000 unchanged, sequencer 0x00→0x04000000 at f166 then held; (2) `--poke-at panel-row-3 cheat 0x0203a9c3:3` + trigger poke — identical trajectory, panel row 3 alone does not unstick the row-check chain; (3) write-watch 0x020013f0 over 700 frames — 637 unique seed values (~1 draw/frame), AI loop alive at RNG cadence. So the trigger poke unblocks the sequencer (T9e proved this — Index_01 0x02034881 4→0→8→0x0C at f165, dword_203CA70 0→0x04 at f166), T9f refuted the JumpOffset00=8 hypothesis (one-shot poke-at 155:0x020364C0:0x08 held through f199; state-4 handler `custMenuMainMaybe_8026A88` never re-enters), and panel row 3 is not the gate. What remains is the byte `is_MegaMan_in_range?` reads inside `sub_8026840` (asm03_0.s:525-704; called from `sub_8026A28` at asm03_0.s:761, the state-0 self-advance to state 4) — that byte is the AI handshake that decides whether the per-virus AI registers MegaMan as in attack range. M5 is "Mettaur done; Gunner in progress (T9c)" per SCOPE.md: T6 already landed the Mettaur as canon's per-type entry (`ForMettaur_8109EF4` at objects.rs, identical mechanism), T9b measured the Gunner art reproduces `assets/gunner.bin` byte-for-byte at comp_825BFC4, T9c landed the `battlestart_gunner` recipe and the per-slot `enemy_kind` (kind 1 = Gunner, fixture byte +5, 2 bits/slot). M5's only remaining step is the Gunner-as-data port the interpreters and T6 enable. A precise negative here is still a result — it closes M5 with the AI handshake byte named as the blocker and not the recipe.

**Do.**
1. Write-watch `sub_8026840`'s read set on `battlestart_gunner` from f0 to f700 with `tools/probe.py --watch-write` — the AI handshake byte is whichever read inside `sub_8026840` (asm03_0.s:525-704) flips between f151 (when CurAction becomes 0x01) and f699 (when the trace ends); the candidate set is the panel-coord bytes `0x0203a9c2..0x0203a9c5` (MegaMan's panel) plus the per-virus AI flags at `0x0203ab68..0x0203ab6d` (slot1) and `0x0203ab70..0x0203ab75` (slot0) → *report the byte that flips, the writer PC, and the values on both sides at f151 and f699.*
2. If the byte is a panel-coord: extend the panel-row poke to set the full coordinate canon reads (column included), re-run the same watch → *report whether the Gunner reaches an attack event (HP at `0x0203ab68+0x24` changes from 0x003c or gauge changes from 0x00200000) within 700 frames and at which frame; report an empty log if no movement, that is a result.*
3. If the byte is an AI flag (or no byte flips and the port is the named next stop): port `ForGunner_8113078` under `enemy_think`/`enemy_act` (`off_8109050[0x17]` / `off_81091D0[0x17]`, asm32.s:10060-10290; CurAction table 0x00-0x0B plus the six per-version parameter tables from `byte_80182C4`); wire `assets/gunner.bin` at kind-1 art; add one harness row `gunner` aligned by the first attack event measured in step 1 (event = first frame slot1 HP drops OR `0x020352A0`/`0x020352A2` changes from frozen `0x00200000`; canonical-align by the canon frame, search band confirmed unique) → *report, per arm actually ported, the `file:line` citation (T12's `tools/rom_enemy_tables.py` is the cross-check; the table idx 0x17 must resolve to `ForGunner_8113078`); report the row = 0/0 with negative not blind (kind-1 fixture, slot1 = Gunner, slot0 = Mettaur).*
4. Re-run `tools/verify_rows.py` from a clean detached checkout → *report the table identical to HEAD except the new gunner row (or, on NEGATIVE, no new row added); mettaur 0/0/70/41734, wave 0/0/90/3840, chip-cannon 0/0/40/9505, cursor 1/1/170 (the known tear) all unchanged; `fitted constants` count unchanged at 19.*

**Rules.** Only the named files; tools/harness.py for the new gunner row only (no edit to existing rows); assets/ for art extracted byte-for-byte at comp_825BFC4 per T9b's measurement; docs/coverage/battlestart_gunner.md rewritten before this ticket copies it forward with the measured handshake; no allowlist change of any kind; no alignment change except by measured event (F2's rule); the fix is a port under the interpreters (T6's `enemy_think`/`enemy_act`/`MettaurEntry` pattern) — no new hand-written pose and no per-scenario branch; every new literal in an edited line gets a `// provenance:` tag or the canon symbol; `fitted constants in src/` (HEAD: 19) may not increase; the kind-1 fixture must be backward-compatible (every existing row unchanged, kind 0 = Mettaur unchanged, T6's `MettaurEntry` path preserved); canon never changes; ≤8 capture runs total, one at a time inside the 3-slot semaphore.

**Measure and report.** row: gunner (new, aligned by first attack event), mettaur, wave, cursor, opening, chip-cannon (regression set). frames: 700 `battlestart_gunner`, 70 mettaur, 90 wave, 170 cursor. total: gunner before/after, fitted-constant count (must stay 19). worst: any pixel row that moves. region: for gunner, the slot1 (CurState, CurAction, timer, HP) pairs frame by frame for the alignment window both sides; for any pixel regression, the diffmask region and frame. commit: src/fixture.rs + src/objects.rs + src/ai.rs + src/actor.rs + assets/ + tools/harness.py (new row) + tools/states.py (recipe if step 2 passes) + docs/coverage/battlestart_gunner.md. One line of mechanism (the AI handshake byte named with its writer PC); one line of what is unverified (whether the byte is per-slot or shared across both viruses; whether the second attack's element/effect appears on the second-vs-third beats in the row's window — that's M3, not M5, and is out of scope here).

**Acceptance.** EITHER the gunner row = 0/0 with negative not blind (kind-1 fixture, slot1 = Gunner, slot0 = Mettaur) and the trace on the Gunner scenario shows the slot's CurState/CurAction/timers matching canon from spawn through the second attack, OR the report names the AI handshake byte that did not flip and what that rules out — a precise negative closes this ticket the same way T9e/T9f/T9g did; every existing isolated row unchanged; docs/coverage/battlestart_gunner.md corrected with the measured handshake byte; `fitted constants` not increased.

**Coordinator:** dispatch third, after T7g and T7j land. M2 is the priority over M5; this is the M5 ticket whose measurement prep T9e/T9f/T9g already paid for (combined $0.04 + $0.10 + $0.15 in the cost ledger). Files are disjoint from T7g's (T7g is `src/battle.rs + src/results.rs + src/banner.rs + tools/allowlist.py`; this is `tools/probe.py + src/fixture.rs + src/objects.rs + src/ai.rs + src/actor.rs + assets/ + tools/harness.py new-row + FIXTURE.md + docs/coverage/`), so it can run alongside T7j per the two-worker rule once T7g is underway. Worker muse-spark-1.3-contributor (T6/T9c's child class — the Mettaur port and the Gunner recipe are already landed), verifier GLM-5.3-flash cross-family; ≤$0.30 expected, ≤$0.60 cap; verify_rows on every row named (mettaur, wave, cursor, opening, chip-cannon, the new gunner row). Advances **M5**.
