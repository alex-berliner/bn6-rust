# Proposed tickets 20260916-015843 (minimax/MiniMax-M3, $0.1284)

### T40. M3's first panel-effect port: holy panel halves the damage sum at sub_801A7F4 asm00_2.s:22788-22791, with a new panel-state fixture that loads type-5 panels  *(OPEN -- 2026-09-16)*

**Why.** T33 (NEGATIVE, landed ea5d284) walked `_object_setPanelType` and the inline strb `oPanelData_Type` sites and bounded the panel-type vocabulary at 8 — the five unverified types (0x0 hole, 0x5 holy, 0x8, 0x9, 0xA) have no writers in `reference/bn6f/asm`. T33 also named the only live M3 candidate reader: type 5 (holy) at asm00_2.s:22788-22791 inside `sub_801A7F4` halves the damage sum. T35 (BLOCKED, 2026-09-16) tried to port cracked-panel (type 3 → type 0 on MegaMan's step, object.s:2218-2222) but was blocked on the GNU ARM toolchain not being installed on the box — its premise is sound, its branch is unmerged, and the build-env blocker has not been lifted. Holy panel is the cheapest live M3 rule that does not relitigate T35's writer path: its damage rule is reader-only (no writer cite needed) and observable as half-damage at the BG3 damage readout. The current `field` row is at 0/0/40 isolated and 158935/5606/40 integrated (F45/F31), so the half-damage port needs a NEW harness row whose fixture loads holy panels — append-only, no existing row's frames/region changed, no allowlist edit. Milestone **M3**, and M1's panels line.

**Files.** src/panel.rs (new, the per-type effect dispatch — there is none on main today), src/battle.rs (the damage-sum call site that invokes sub_801A7F4 only if MegaMan's panel is type 5), src/fixture.rs (a panel-state descriptor that loads holy panels — append-only if it widens an existing descriptor), tools/harness.py (a NEW scenario row for the holy-panel fixture; the existing `field` row's entry must not change), docs/coverage/holy_panel.md, docs/worklog/T40.md. **NOT** tools/allowlist.py, tools/inventory.py except the panels row, tools/trace.py, tools/mgba_capture.c; reference/bn6f read-only.

**Do.** 1. Baseline on current main, with build identity: build the existing ROM (sha256 expected 1997be3b...); `python3 tools/harness.py --only field --no-gallery` → 0/0/40 isolated, 158935/5606/40 integrated (neg 261034); record that no current row loads holy panels. 2. Read asm00_2.s:22788-22791 (`sub_801A7F4` halves damage sum) and the panel-data read it does; report the exact mask/word tested and the symbol/offset that carries the holy flag (cite file:line). 3. Build a panel-state fixture that loads holy panels (one stage byte + a panel map; cite the source stage in `tools/states.py`); add a NEW harness row at `tools/harness.py` with `canon_ref` from a measurement (--watch on the panel-type word), narrow search band, and a negative fixture from the same stage data with the holy panel absent → report the new row's totals before any src/ change. 4. Port the rule: at the damage-sum call site, if MegaMan's panel type == 5, divide by 2 BEFORE summing (cite asm00_2.s:22788-22791 in the diff); re-run step 3 → report the new totals; the half-damage rule must be measurable as the row's integrated total dropping by exactly half the summed damage at the holy-panel frames. 5. Regression: `verify_rows <branch> field,field-bg1,field-bg2,field-bg3,mettaur,cursor,opening,result,windowclose --expect field=0/0/40/1139 --expect mettaur=0/0/70/41734 --expect cursor=1/1/170/186279 --expect opening=0/0/40/86591 --expect result=0/0/40/111839 --expect windowclose=0/0/40/207166`; report every line.

**Rules.** No row's frames/`align`/`canon_ref`/`search` band/compared region changes for any EXISTING row. The NEW row's `note` states what it proves. No allowlist edit, no `patch_sterile.py`. The new row's negative fixture must stay non-blind. fitted count HEAD 19 must not rise; any new literal gets `// canon:` or `// provenance:`. Re-check every cite against disk (357da2a re-cut moved bn6f numbers). ≤6 captures, tool budget ≤80; /tmp's four canon roots untouched; artifacts under `/tmp/bn-t40-*/`. A new row that fails to load holy panels after step 3's fixture is a NEGATIVE naming the panel-data layout that refused it — not a "widen the fixture" loop, and not a re-port of T35's writer-side work.

**Acceptance.** A new `holy-panel` row reads 0/0/N with the half-damage rule measurable on it (the integrated total drops by exactly half the summed damage at the holy-panel frames), every EXISTING isolated row unchanged, and the rule cite carried in the diff. Or a NEGATIVE that names the panel-data layout the fixture cannot reach — which becomes the writer-side ticket T33's GAP left open, and is itself M3 progress because it bounds the next per-type rule.

**Measure and report.** rows: the new holy-panel row + field (both variants) + mettaur, cursor, opening, result, windowclose. frames: N/40/70/170/40/40. totals/worst/negatives before/after per row with both ROM shas, the holy-panel frames' summed damage before/after, the cite, fitted count, commit, one line of mechanism, one line of what is unverified.

**Coordinator:** dispatch first and alone — no other open ticket touches src/panel.rs or src/fixture.rs's panel map. Free tier: `verify_rows` on the named rows; a verifier for step 2's reader cite and step 4's half-damage measure (the rule's measurable signature). ≤$0.30 expected, ≤$0.60 cap. Advances **M3**.

---

### T38. M4's Cannon family: port the three chips' `match chip.id` arm to AttackPower +0x1a, finishing T36 PARTIAL's deferred rewrite  *(OPEN -- 2026-09-16)*

**Why.** T36 (PARTIAL, landed be6e706) ran chip_export.py v2 + cross-checked against reference/bn6f/data/ChipDataArr.s:33-126 byte-by-byte and recorded ZERO unexplained mismatches for Cannon 0x0028=40, HiCannon 0x0064=100, M-Cannon 0x00B4=180, and Element 0x0A=CHIP_ELEM_NONE for all three — but its worklog-only commit `bd7d258` stopped at step 2 because the soft tool budget (60/60) was already exhausted, never reaching the cargo build + src/battle.rs rewrite + regression sweep + SCOPE regeneration. T36's own stamp said "Planned rewrite is mechanical and gate-free per parity table; recorded for next worker." T34 (NEGATIVE, landed 36c7bf6) is the parallel precedent for chip-as-data conversion and is now closed by its own writeup (Barr* share sub=4; params[0] = 1/5/7 is the canonical Barrier read). The Cannon family is the cheapest one left in M4: single record read at +0x1a, no subfamily indexing, no parameter table. The 43 chip rows are zero rows, which F48 proved structurally safe as gates (a drift cannot make a mispaired frame read 0). Milestone **M4**, and M1's chips line.

**Files.** src/chips.rs (no change — accessors exist from T17/T19), src/battle.rs (**only** the Cannon arm in chip_strike :4277-4338 and the barrel-palette lookup at :4206 — never `chip_strike`'s landed sword/blade arm, never fight_latch/hp_readout_live), tools/inventory.py (the as-data category data only), docs/SCOPE.md (regenerated only), docs/worklog/T38.md. **NOT** tools/chip_export.py, assets/chips.bin, tools/harness.py, tools/allowlist.py, src/hud*.rs, src/backdrop.rs, src/fixture.rs, src/spr.rs; reference/bn6f read-only.

**Do.** 1. Baseline, one session, with the ROM sha256: `python3 tools/harness.py --no-gallery --only chip-cannon,chip-hipcannon,chip-mcannon` → three 0/0/40 lines with negatives (HEAD cannon 9505, hipcannon ?, mcannon ?); `python3 tools/inventory.py` → quote SCOPE's M4 line ("10 of 411"). 2. Re-confirm the parity table from T36 step 2 (no captures; read bytes out of `reference/bn6f/bn6f.gba` at the addresses T36 already named, including Cannon's barrel-palette subfamily lookup at byte_80EBD2C asm31.s:109657). 3. Convert only what step 2 proves equal: replace the `match chip.id` arm at src/battle.rs:4277-4338 with a single record read at +0x1a + Element + barrel-palette subfamily index → report the count of `CHIP_*` ids remaining in the Cannon arm before/after. 4. Re-run step 1 → every Cannon row must still read 0/0 with the negative non-blind. 5. Regression: `verify_rows <branch> chip-cannon,chip-hipcannon,chip-mcannon,chip-use,cannon,field,mettaur,cursor --expect ...` → report all lines; cursor's 1/1/170/186279 (known tear, Q6 NEGATIVE) reported not chased. 6. `python3 tools/inventory.py` if the as-data state derives without a hand-maintained list; quote SCOPE's M4 line and M1 chips count before/after.

**Rules.** The v2 asset is the file (T17/T19 contract; F48 saw T17's ROM change move field by -23 deterministically, so cite both shas). No `match` surviving a value step 2 proved equal — a mismatch is the finding, per chip with both numbers (T17's rule). No fix-up table. Zero rows are the gate: any chip row non-zero, or cursor >1/1/170, or windowclose/mettaur/tiles/gauge/field non-zero → branch unmerged, reported. `provenance_counts()` fitted ≤19; per T29 it scans only `ROOT/src/*.rs` so it cannot attest a tools/ literal. Every literal you write gets `// canon:`/`// provenance:`; re-check cites against disk. ≤6 captures, tool budget ≤80; /tmp's four canon roots untouched; artifacts under `/tmp/bn-t38-*/`.

**Acceptance.** The 3-chip parity table with zero unexplained mismatches, no `CHIP_*` id indexing an amount in the Cannon arm (before/after counts quoted), all 43 chip rows still 0/0 with their negatives, and SCOPE's M4 line moved through the generator (before/after quoted, expected 10 → 13). A NEGATIVE that a record byte contradicts a measured literal closes it, per chip with both numbers, and becomes the next ticket's list.

**Measure and report.** rows: chip-cannon, chip-hipcannon, chip-mcannon, chip-use, cannon, field, mettaur, cursor. frames: 40/30/40/70/170. totals/worst/negatives before/after per row, the `CHIP_*` id count in the Cannon arm before/after, `provenance_counts()` before/after, both ROM sha256s, SCOPE's M4/M1 lines before/after, commit, one line of mechanism, one line of what is unverified.

**Coordinator:** dispatch after T36 lands (it owns the same files) — `next_ticket.py --pair` cannot run them together. Free tier: `verify_rows` across the chip set with the landed negatives as expects — every gate here is a zero total, so F48's drift band does not apply and none may be widened. A verifier for step 2's parity claim only (the same ROM read T17's verifier did). ≤$0.20 expected, ≤$0.40 cap. Advances **M4**.

---

### T39. M5's Gunner port: watch-write the BG3 plateau region at k=77..129 for the chip-window auto-open trigger that T37 NEGATIVE couldn't reach  *(OPEN -- 2026-09-16)*

**Why.** T37 (NEGATIVE, landed c1c98ce) measured the gunner row's k≥77 band as a ~5.8k/frame BG3 plateau — canon's gauge-full pause opens the chip window on BG3 from canon ~156 ("comes up on its own at frame 165", src/battle.rs:1998), and our side never fires that auto-open because the poked battle "never goes live" (T9d NEGATIVE: AIAttackVars_Unk_00 at 0x02034320 stays 0x00 throughout 300 frames, CurAction at 0x0203ab69 never reaches 0x0A). T37's lever (canon_ref sensitivity over {78, 80, 33, 151}) cannot move below 1,500,000 because the 71-frame BG3 plateau gap exceeds the 40-frame search band. T32 (DONE, landed 5a5fcd9) seeded GUNNER_ROW.gauge=0x4000 with the isCustGaugeFullAndBattleLive_800A21C cite — the descriptor is set, but the rust-side gauge register still reads 0x00 because the trigger for "gauge full" is the Gunner's attack event, which itself never fires. T27 (NEGATIVE, kept as the base for any dwell ticket) closed the sequencer timeline for this phase, so the next M5 lever is NOT a sequencer rewrite — it is a watch-write on the BG3 plateau's source window (k=77..129). T9e NEGATIVE tried a different lever (writer of dword_203CA70) and found nothing writes it on the poked battle route; this ticket targets a different address set: the BG3 enable / chip-window BG3 layer words during the plateau window, where canon's chip-window BG3 enable bit flips. Milestone **M5**, and **M8** (HP boxes and chip window arrival).

**Files.** src/objects.rs (the gunner_update arm, only if the watch names a routine the current port misses), src/battle.rs **only** if the auto-open reads a battle-level field, tools/harness.py (the GUNNER_ROW descriptor's `gauge` field only if the watch names a different seed), docs/worklog/T39.md. **NOT** src/sequencer.rs / src/battle.rs's seq block (T27 closed that), tools/allowlist.py, tools/trace.py, tools/mgba_capture.c; reference/bn6f read-only.

**Do.** 1. Baseline, one session: `python3 tools/harness.py --only gunner --no-gallery` → 2105613/38237/130 neg 2284867 + ROM sha256. 2. From T9g's kept dumps (`/tmp/bn-t9g-gunner-extended/`) and T37's watch probe at `battlestart_gunner.state`, run `tools/mgba_capture.c --watch` on a candidate address set across the k=77..129 window: BG3CNT (0x0400000E) at the chip-window BG3 enable, the BG3 VOFS/HOFS pair, the gauge-halfword (the same one T32 named), and AIAttackVars_Unk_00 at 0x02034320 → report per-frame values for each and the capture each one changes on. 3. Cross-reference with T9g's measured 234-frame settle window (sub_8112D9C) and T37's CurAction/AIAttackVars watch: name the field that flips 0→nonzero during the k=77..129 window, and the routine that writes it (cite file:line). 4. If step 3 names a writer that is NOT a sequencer state (per T27's closure): port the trigger in src/objects.rs (or src/battle.rs if battle-level), re-run step 1 → report the k=6..75 and k≥77 per-band totals before/after. 5. Regression: `verify_rows <branch> cursor,windowclose,field,mettaur --expect cursor=1/1/170/186279 --expect windowclose=0/0/40/207166 --expect field=0/0/40/1139 --expect mettaur=0/0/70/41734`; report every line.

**Rules.** Watch-write only at step 2; no src/ change outside what step 3 names; no allowlist edit; `patch_sterile.py` forbidden; negatives stay non-blind. fitted count HEAD 19 must not rise; per T29 `provenance_counts()` scans only `src/*.rs`, so report what the new literal is. Re-check every cite against disk (the 357da2a re-cut moved bn6f numbers). ≤4 gunner captures; tool budget ≤70; /tmp's four canon roots and T9g's kept dumps untouched; artifacts under `/tmp/bn-t39-*/`.

**Acceptance.** `gunner integrated` strictly below 1,500,000 with the four guard rows identical, the k≥77 band's per-frame totals reduced (named writer in the diff), the watch series reproducible from T9g's dumps, and a canon file:line cite for the writer — or a NEGATIVE that names the field the plateau reads and shows the trigger is NOT a write we can port (it is a sequencer state or a fixture-side artifact), which retires the auto-open lever for M5.

**Measure and report.** rows: gunner (integrated + isolated), cursor, windowclose, field, mettaur. frames 130/170/40/70. total/worst/negatives before and after per row with both ROM shas, the k=6..75 and k≥77 per-band totals before/after, the watch series with capture numbers, the writer cite, fitted count, commit, one line of mechanism, one line of what is unverified.

**Coordinator:** dispatch after T32 lands; no other open ticket touches GUNNER_ROW's descriptor or the gunner_update arm. Free tier: `verify_rows` on the four guards plus direct `--only gunner`, gated on the integrated line with F48's matcher. A verifier for step 3's watch series and step 4's cite — a memory finding every later virus port builds on. ≤$0.40 expected, ≤$0.80 cap. Advances **M5**.
