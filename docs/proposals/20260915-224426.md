# Proposed tickets 20260915-224426 (hyper/qwen3.8-flash, $0.0461)

**T26**

### T26. Canon's battle-state 0x14 ladder: what each custom-screen edge waits on, and the pad path that arms it  *(OPEN)*

**Why.** `battle_full`'s trace diverges on **273/540** sequencer frames, first divergence **k=31** (canon 0x20, ours 0x08) — T7y step 1. Canon's k-spans: 0x1C 0..10, 0x08 11..41, **0x20 42..43, 0x24 44..143, 0x00 144..146, 0x04 147..206**, 0x08 207..315, 0x0C 316..554; ours 0x08 0..124, 0x24 125..171, 0x00 172..173, 0x04 174..233, 0x08 234..404, 0x0C 405..542. Three ports in this family came back without landing (T7u BLOCKED, T7x NEGATIVE, T7y NEGATIVE) and T7z was NOT DISPATCHED, all on the same missing read: T7y step 4 (re-verified against disk) established canon waits on **no count** between gauge-full and open — `sub_800855E` (asm00_1.s:11206, sites :11266-11274) and its twin `sub_80089CC` (:11813, :11870-11878) pair `isCustGaugeFullAndBattleLive_800A21C` (:15305, `gauge==0x4000 && live`) with PauseBattle + **battle state 0x14 in the same frame**, and every wait lives in that chain's own sub-states (`sub_8008840`, :11597), which `src/battle.rs` never modelled. Our edges are `SEQ_20 if self.seq.age >= 1` and `SEQ_00 if self.seq.age >= 2` (src/battle.rs:3459-3460, **no cite**) and a peeked `GAUGE_PAUSE = 60` (:1116, armed :2907). T7y also measured that the word this whole family watches, sequencer `0x0203ca70`, reads **0x00 for all 260 frames of the gunner capture** while that row's window opens at capture 152 — so the state word is not universal, and any port that watches the wrong word cannot be judged. This is T7z's own option (1): "the only thing that makes the SEQ_20/24/00 edges portable". Zero src/, near-zero captures. Milestone **M2** (the sequencer's custom-screen states — 165 of the 273 divergent frames are SEQ_24's duration plus the open edge).

**Files.** docs/coverage/battle_full.md, docs/worklog/T26.md (mandatory), docs/worklog/t26_watch.py (only if a watch series is needed — use T7y's harness Side-argv driver pattern, docs/worklog/t7w_step3_watch.py; `tools/probe.py` white-spins on this state and deletes its own watch files at probe.py:83-84). **NOT** src/ (nothing), **NOT** tools/harness.py, **NOT** tools/trace.py, **NOT** tools/states.py, **NOT** tools/allowlist.py; reference/bn6f read-only, never committed.

**Do.** 1. Baseline, no edit: `tools/trace.py` record+diff `--align row:battle_full` → report the divergence count and both sides' per-state k-spans (expect 273/540, first divergence k=31). 2. Walk the 0x14 chain from its entry: per sub-state report the loading instruction (the value + the compare/branch) that leaves it, its **file:line**, and the word/bit it tests. 3. Arithmetic test — the walked counts must reproduce canon's observed spans: SEQ_20 2 exports, SEQ_24 100, SEQ_00 3, SEQ_04 60 → report count-from-asm vs observed per state and name every disagreement (a disagreement is the finding, not something to patch). 4. Find the pad path in the live fight: the routine reading the held/pressed JOYPAD bits that arms the open, its cite and the L/R mask value, and confirm that src/battle.rs:2729-2732 sits under `cfg!(debug_assertions)` while every harness number comes from `cargo build --release` (tools/harness.py:272) → i.e. in every measured capture the player is not in the loop. 5. Answer the state-word question: which word holds the 0x14 chain's sub-state on a **paused, non-battle_full** fixture (gunner), with its writer's file:line, and whether it is the same word `0x0203ca70` at a different time. 6. Write the table into docs/coverage/battle_full.md: per edge → canon's predicate + cite + the exact src/battle.rs line it replaces (:2907/GAUGE_PAUSE, :3459, :3460, :1186 SEQ04_FRAMES). 7. Prove the no-op: `cargo build --release` twice, `cmp -l` the packed .gba against main's → **0**; and `python3 tools/harness.py --only mettaur,cursor --no-gallery` → 0/0/70/41734 and 1/1/170/186279.

**Rules.** Docs only — step 7's cmp=0 is the acceptance of that, not a nicety. No fitted constants: a count is quoted from the asm with its file:line, never tuned to match a span; a state whose asm count does not reproduce its measured span is reported as a mismatch. Re-check every cited file:line against disk (the 357da2a re-cut moved bn6f's numbers). ≤2 capture runs, ≤25 tool calls; never delete or move /tmp's four canon roots (`tools/check_inputs.sh` refuses you); your artifacts under `/tmp/bn-t26-*/`, named beside every number.

**Acceptance.** A cited per-edge table — for SEQ_20, SEQ_24, SEQ_00, SEQ_04 and the 0x08→0x0C hand-off: the count or condition, the word/bit tested, asm00_1.s file:line, and the src/battle.rs line it replaces — whose counts reproduce canon's spans (2/100/3/60) within ±1 export; plus the pad-read cite that replaces the debug-only arm; plus the per-fixture state-word answer; plus `cmp` 0 differing bytes and mettaur/cursor unchanged. A NEGATIVE showing the 0x14 chain has **no** per-state counts (one driver countdown instead) closes it equally and re-points T27 at that driver.

**Measure and report.** rows: battle_full (trace), mettaur, cursor. frames: 540/70/170. total: 273/540 before and after (a no-op ticket), the two rows' totals and negatives; region: the sequencer field over k=0..540 both sides; the count-vs-span table; the fitted-constant count (`python3 -c 'import sys; sys.path.insert(0,"tools"); import harness; print(harness.provenance_counts())'`, must stay 19); commit; one line of mechanism; one line of what is unverified.

**Coordinator:** free tier = the ROM `cmp` plus the two zero rows (nothing a row check can judge about asm reading). A verifier IS warranted (memory finding, and T27 is built on it): open two cited asm sites and confirm the count/compare is where the report says, and confirm the state word it names for gunner. Dispatch **in parallel with T25** — T26 touches no src/ and no tools/harness.py. ≤$0.15 expected, ≤$0.40 cap. Advances **M2**.

---

**T27**

### T27. The custom-screen open edge as canon's press→0x14 ladder: replace GAUGE_PAUSE and the two uncited `seq.age` edges  *(OPEN)*

**Why.** T26 lands the predicates; this ports them. The whole of `battle_full`'s 273/540 divergence rides one offset: our SEQ_20 enters at export **k~124** against canon's **k=42** (T7y step 1), and SEQ_24 (ours 47 exports vs canon's 100), SEQ_00 (2 vs 3), SEQ_04 (174..233 vs 147..206) and the 0x0C hand-off (405 vs 316) are all downstream. Our open is armed by `GAUGE_PAUSE = 60` (src/battle.rs:1116, `provenance: peeked — "about 60 frames of chimes"`, armed :2907) and our two short edges carry **no cite** (:3459 `SEQ_20 if self.seq.age >= 1`, :3460 `SEQ_00 if self.seq.age >= 2`). T7y's step 3 established the gate shape by healthy watch: battle_full **is press-gated** (L@40 → SEQ_20 at capture 42; gauge word 0x020352a0 static 0x4000 from capture 0, cleared at 48), and canon waits on no count between full and open. Two known landmines, both measured, are this ticket's veto set: deleting GAUGE_PAUSE outright moved **cursor 3/3/170 → 16116/175/170** (T7y step 4, reverted), and spawning the banner record at SEQ_04's first run moved **windowclose 0/0/40 → 40038/1900/40 and cursor → 16241/3385** (T7x). Third: this is the fourth M2 attempt in the family — a NEGATIVE here closes the sequencer timeline for the phase.

**Files.** src/battle.rs (the gauge branch and countdown :2643-2683/:2907, `GAUGE_PAUSE` :1116, the seq-match block :3455-3462, the pad-read site :2729-2732 — T26's cites only, in those ranges), tools/states.py (battle_full's **rust** `script` only, verbatim from canon's own string `Start@10,L@40,Start@70,A@80,A@260,A@440,A@470` at :606), docs/coverage/battle_full.md, docs/worklog/T27.md. **NOT** tools/harness.py (T25 owns its FIELD_ZERO leg this cycle), **NOT** tools/allowlist.py, **NOT** src/custom.rs unless T26 cites it, **NOT** src/hud*.rs, src/spr.rs, src/backdrop.rs, src/fixture.rs.

**Do.** 1. Baseline, one session, with build identity (`sha256sum` of the built ROM): trace `--align row:battle_full` → 273/540, first divergence k=31, our spans; `--only gunner` → 2105613/38237/130 neg 2284867; and the veto set as landed — cursor 1/1/170/186279, windowclose 0/0/40/207166, mettaur 0/0/70/41734, tiles+gauge 0/0/8/12197, chip-use 0/0/30/7768, opening isolated 0/0/40/86591, opening integrated 18740/647/40/98649. 2. Give the rust side canon's own press schedule from the same scenario string (one log, both sides) → report our first press frame and the SEQ_20 entry frame that follows; the press is data, never converted into a timer. 3. Port T26's predicates for the open edge, SEQ_20→SEQ_24 and SEQ_24→SEQ_00, deleting `GAUGE_PAUSE` and both `age >=` edges, each replacement carrying its asm00_1.s cite in the diff → re-run the trace and report the divergence count, both sides' per-state spans, and the first divergent frame. 4. Leave SEQ_04's leave (`SEQ04_FRAMES=60` :1186) and `RESULTS_DELAY` (:1197) untouched — T7x shows the banner record's existence, not its duration, is what breaks rows → report cursor/windowclose after step 3 to prove neither moved. 5. `--only gunner` again → report its k=6..75 and k≥77 bands honestly; T7y proved gunner's window is neither press- nor gauge-driven, so nil movement there is the expected and reported result. 6. `python3 tools/verify_rows.py <branch> …` over the step-1 set plus the direct `--only opening --ui integrated` and `--only cursor` calls → report every line; and the fitted count (expect 19 → ≤18 as GAUGE_PAUSE's `peeked`/fitted tag goes).

**Rules.** Only the named ranges. No new count anywhere: an edge that cannot be expressed as T26's cited predicate is reported as such, not tuned. No row's frames/`align`/`canon_ref`/`search`/region change, no new fixture field, no allowlist edit, no `patch_sterile.py`, canon never changes, negatives stay non-blind. Any zero row that stops being zero, or cursor above 1/1/170, or windowclose above 0 → **keep the branch unmerged** and report; that is a good outcome, not a failure to fix. ≤5 capture runs (the veto set is the expensive part — plan for it), tool budget ≤90.

**Acceptance.** Trace target: field = the sequencer state as exported by `tools/trace.py` on the `battle_full` scenario, frames = its 540 exported frames, **first divergent frame moves from k=31 to k≥44** with SEQ_20 entering at canon's k=42 ±1 and SEQ_24's span matching canon's 100 exports ±2, and sequencer divergence **≤120/540** from 273/540; `GAUGE_PAUSE` gone, fitted ≤18, and verify_rows identical on the step-1 veto set as the absolute veto. A NEGATIVE carrying both sides' per-state spans, the word each side tests, and the row that blocked a landing also closes it — and then the sequencer timeline is closed for this phase.

**Measure and report.** rows: battle_full (trace), gunner, cursor, windowclose, mettaur, tiles, gauge, chip-use, opening (both lines). frames: 540/130/170/40/70/8/8/30/40. total/worst/negatives before and after each, both sides' SEQ_20/24/00/04 enter+leave k, the divergence count, the ROM sha256 of both builds, the fitted count; region: the sequencer field k=0..540; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch only after T26 lands, and **alone** (it owns src/battle.rs, which T25's scroll leg and T28 both name). Free tier: verify_rows over the veto set PLUS the direct integrated/cursor calls, using F48's fixed matcher so the integrated line is actually gated. A verifier for step 3's cite-vs-asm claims and the "press, not timer" claim. If this returns NEGATIVE or BLOCKED, do not write a fifth sequencer ticket: go to M4/M5. ≤$0.60 expected, ≤$1.20 cap. Advances **M2**.

---

**T28**

### T28. Chips as data, second arm: no chip id indexes an amount — Recov, Barrier and Vulcan read the record's own bytes  *(OPEN)*

**Why.** T17 (landed 8c1a62d) made the sword/blade arm data-driven and T19 (2baad23) made the contract loud, but docs/SCOPE.md still counts **10 of 411** chips as data, and the rest of `chip_strike` and its neighbours are still per-id tables keyed by the chip id — a hand-built index into values the ROM already carries per chip. Named sites in src/battle.rs today: :645 `RECOV_HP: [u16; 9] = [10,30,50,80,120,150,200,300,1000]` (`provenance: derived — byte_80EC870, asm31.s:111044`) indexed by `chip.id - CHIP_RECOV10` at :4258; :673 `BARR100_HP = 100` (`byte_8020B2C, data/dat01.s:189`) and its Barr200 sibling at :677-678; :504 `VULCAN2_SHOTS = 4` (`dword_80EBFEC = 0xA050403, asm31.s:109878-109892`) with VULCAN3/SUPRVULC at :509-511 and the timing table they feed at :531. The record already carries all of it: `chip_data_struct` (reference/bn6f/include/rom_structs/ChipData.inc) has **AttackPower u16 at +0x1a**, AttackParam1..4 at +0x10..0x13, LockoutFrames +0x14, LockOnEnable +0xf — and `tools/chip_export.py` already ships power plus the T17 tail in `assets/chips.bin`, so **the asset must not be re-exported**. The parity is checkable against the ROM without a capture, exactly as T17's step 2 was, and the verifier there confirmed the method. 43 chip rows are free regression tests and all are zero rows, which F48 proved structurally safe (a drift cannot make a mispaired frame read 0). Milestone **M4** (chips as data) and M1's chips line.

**Files.** src/chips.rs (accessors for the record bytes used, nothing else), src/battle.rs (**only** the Recov arm :4255-4259, the barrier-amount sites :666-678, the vulcan shot/timing sites :504-531 — never `chip_strike`'s landed sword/blade arm, never `fight_latch`/`hp_readout_live` from F44), tools/inventory.py (the as-data category data only, if and only if the generator can derive the new state), docs/SCOPE.md (regenerated only, never hand-edited below the marker), docs/worklog/T28.md. **NOT** tools/chip_export.py, **NOT** assets/chips.bin, **NOT** tools/harness.py, **NOT** tools/allowlist.py, **NOT** src/hud*.rs, src/backdrop.rs, src/fixture.rs, src/spr.rs; reference/bn6f read-only.

**Do.** 1. Baseline, one session, with the built ROM's sha256: `python3 tools/harness.py --no-gallery --ui isolated` → report every chip row plus its negative (the 43 rows read 0/0/30 or 0/0/40 with non-blind negatives; name each row you will touch). 2. Parity table before any edit: for each Recov id (154..161), Barr100/Barr200/Barrier, and Vulcan/Vulc2/Vulc3/Suprvulc, print the record's AttackPower (+0x1a) and, where you claim it, AttackParam1..4 (+0x10..0x13) straight out of `ChipDataArr_8021DA8` in `reference/bn6f/bn6f.gba`, beside the literal in src and the src symbol → report every agreement and every mismatch; **a mismatch is the finding**. 3. Only for values step 2 proves equal: replace the id-indexed table with the record read, delete the literal, and report the count of `CHIP_*` ids remaining in the three arms before/after plus the new fitted/derived/peeked counts (`harness.provenance_counts()`; HEAD 19 fitted). 4. Kill-check each converted value is observable on its row: for one Recov and one Barrier, change nothing but report the frame and region where the amount appears (HP digits) and quote that row's negative total — a row whose negative is unchanged after a value change is a row that never tested the value, so say so rather than claiming the win. 5. Re-run step 1 for every chip row plus `chip-use,cannon,field,tiles,gauge,mettaur,cursor` → report all lines and negatives; nothing may move. 6. If the generator can derive an as-data state for these chips without a new hand-maintained list, edit that data and `python3 tools/inventory.py`; quote SCOPE's M4 line and M1 chips count before/after. If it cannot, report that as the finding — do not hand-edit SCOPE.

**Rules.** No asset change of any kind (the v2 file at 8c1a62d is the file); if a value you need is not in the record, stop and report which offset, do not widen the export. No per-id `match` may survive a value that step 2 proved equal, and no fix-up table may be introduced to bridge a mismatch — report the mismatch per chip with both numbers (T17's rule). Zero rows are the gate: any chip row non-zero, or cursor >1/1/170, or windowclose/mettaur/tiles/gauge non-zero → keep the branch unmerged and report. No new literal without `// canon:` / `// provenance:`; re-check every cited file:line against disk. ≤8 captures, tool budget ≤80; /tmp's four canon roots untouched, artifacts under `/tmp/bn-t28-*/`.

**Acceptance.** The record-vs-literal parity table for all 15 named chips with zero unexplained mismatches, no chip id indexing an amount in the three arms (before/after id counts quoted), the 43 chip rows still 0/0 with their landed negatives and `chip-use/cannon/field/tiles/gauge/mettaur` unmoved, fitted ≤19 and derived count falling by at least the deleted tables' size — plus either a regenerated SCOPE line naming the new as-data chips through the generator's own data, or a written statement that it cannot be derived without inventing a hand-maintained list. A NEGATIVE that a mismatched ROM byte contradicts a measured literal is accepted and becomes the list T29 works from.

**Measure and report.** rows: the 43 chip rows, chip-use, cannon, field, tiles, gauge, mettaur, cursor. frames: 30/40/40/8/70/170. totals/worst/negatives before and after per row, the `CHIP_*` id count in the three arms before/after, the parity table, `provenance_counts()` before/after, the ROM sha256 of both builds, SCOPE's M4/M1 lines before/after; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch **after T27 lands or closes** (both name src/battle.rs, so `next_ticket.py --pair` cannot run them together). Free tier: verify_rows across the chip set with the landed negatives as expects — any chip row that moves is a revert; because every gate here is a zero total, F48's drift band does not apply and none may be widened. A verifier for step 2's parity table only (it is the same ROM-read T17's verifier did, and the remaining 33 chips build on it). ≤$0.35 expected, ≤$0.70 cap. Advances **M4**.
