# Proposed tickets 20260915-060013 (minimax/MiniMax-M3, $0.0580)

### F40a. field integrated ~158935: per-region decomposition, then port the canon mechanism for each non-zero region  *(NEW)*

**Result.** *no result yet — new ticket.* Prior ticket for field integrated never written; this is the largest of the four large M2 integrated remnants and the only one without an OPEN ticket (F35a buster, F36b warp, F38h opening all OPEN).

**Why.** field integrated sits at 158935/5606/40 (per F36a PARTIAL decomposition note, AUDIT-6 allowance 28000 in tools/allowlist.py:54); field isolated is 0/0/40 PASS (F8 DONE per TODO_ARCHIVE.md). The integrated variant leaves the HUD strip + BG1 backdrop + BG3 window + OBJ field lines on, where F8 isolated only the field lines. M2 acceptance is "integrated-row pixel parity"; closing field integrated removes the largest of the four large integrated remnants (158935 px = 2.2x opening 72499, 2.9x buster 54672, 3.9x warp 40628). F36a's warp decomposition found 99.2% BG1 backdrop (sub_8001C94 BGScrollCB_BG1Diagonal3to2Scroll src/backdrop.rs:5,29); field shares the same BG1 phase class.

**Files.** tools/harness.py (only the field-integrated fixture descriptor — confirm demo-* flag and alignment), src/battle.rs (only the field path if a per-tick write is needed), src/objects.rs (only the field lines' per-state behavior in full-HUD context), src/custom.rs (only the field HUD overlay), src/backdrop.rs (only the BG1 backdrop seam), tools/diffmask.py (only for region split), tools/allowlist.py (only the field:integrated entry if the row reads 0/0/40), docs/coverage/field_integrated.md (notes)

**Do.**
1. Baseline `tools/harness.py --only field --ui integrated` on HEAD → *report: field integrated 158935/5606/40; field isolated 0/0/40 PASS; cursor ≤1/1/170; mettaur 0/0/70; windowclose/result 0/0/40; all chip rows 0/0/30.*
2. Run `tools/diffmask.py --row field --ui integrated` to split the residue by region (HUD strip y<24, BG1 backdrop, BG3 window ramp, OBJ field lines); cross-reference `tools/harness.py --only field --ui integrated --only-bg 1` and `--only-bg 3` and `--disable-obj` → *report the per-region pixel totals and the per-frame sequence k=0..N.*
3. Find the canon mechanism for each non-zero region in `reference/bn6f/asm/`: HUD strip sub_8026BF4 (per F18b cite), BG1 backdrop sub_8001C94 BGScrollCB_BG1Diagonal3to2Scroll (per F6/F29 cite, asm00_0.s:3272-3303; the per-scanline write at (counter-8)>>4 and (counter-4)>>4 to BG1HOFS/VOFS), BG3 window ramp sub_801C4E4 (per F35a cite), OBJ field lines sub_8009C1C (per F11 cite) → *report the per-region cite, file:line, and the per-region canon mechanism.*
4. Port the per-region mechanism into src/battle.rs / src/objects.rs / src/custom.rs / src/backdrop.rs; cite each fix to file:line with `// provenance:` tags; never fit a hardcoded BG layer write — port the canon routine → *report the per-region port, the cite count, the diff per file.*
5. Re-run `tools/harness.py --only field --ui integrated` and `tools/verify_rows.py` from a clean detached checkout → *report: field integrated before/after (158935/5606 → 0/0); field isolated stays 0/0/40; cursor ≤1/1/170; mettaur 0/0/70; windowclose/result 0/0/40; all chip rows 0/0/30; full regression set 0/0.*
6. Land with `tools/land.sh` and re-run the free row check on main → *report: 67-row table identical to HEAD except field integrated 0/0; remove `field:integrated` allowlist entry from tools/allowlist.py.*

**Rules.** Only the named files; the AUDIT-6 allowance for `field:integrated` is in tools/allowlist.py — removed only after verify_rows PASS at 0/0/40; no new harness row; no alignment change; the fix is a port of canon's HUD + BG1 + BG3 + OBJ routines — never a fitted layer write; every new literal gets `// provenance:`; `fitted constants in src/` (HEAD: 19) may not increase; canon never changes; ≤4 capture runs, one at a time inside the 3-slot semaphore; tool budget ≤100.

**Acceptance.** field integrated reads 0/0/40 with the per-region mechanism cited at sub_8026BF4 (HUD) + sub_8001C94 (BG1) + sub_801C4E4 (BG3) + sub_8009C1C (OBJ); field isolated stays 0/0/40; cursor ≤1/1/170; mettaur 0/0/70; windowclose/result 0/0/40; all chip rows 0/0/30; `field:integrated` removed from tools/allowlist.py; no allowlist change elsewhere; fitted-constant count unchanged or lower.

**Measure and report.** row: field (integrated) + field (isolated) + cursor + windowclose + mettaur + result + the chip rows. frames: 40 field, 170 cursor, 70 mettaur. total/worst: field integrated before/after (158935/5606 → 0/0); per-region pixel totals HUD/BG1/BG3/OBJ; per-frame sequence k=0..N. region: HUD strip y<24, BG1 backdrop y=24..143, BG3 window ramp, OBJ field lines. commit: src/battle.rs + src/objects.rs + src/custom.rs + src/backdrop.rs (per-region port) + tools/allowlist.py (AUDIT-6 removal). One line of mechanism (field integrated's residue lives in four canon routines — HUD sub_8026BF4, BG1 backdrop sub_8001C94 BGScrollCB_BG1Diagonal3to2Scroll, BG3 window ramp sub_801C4E4, OBJ field lines sub_8009C1C — each ported with cited file:line). One line of unverified (whether the OBJ layer holds additional residue beyond the field lines, since the field's per-tick art has more phases than warp's).

**Coordinator:** dispatch first. Worker muse-spark-1.3-contributor (F35a/F36b's child class — per-region integrated port, field is the largest), verifier GLM-5.3-flash cross-family; ≤$0.40 expected (largest row, longest decompose), ≤$0.80 cap; tool budget ≤100; verify_rows on the full table. Advances **M2** (integrated field; closes the largest of the four large integrated remnants — 158935 px = 2.2x opening, 2.9x buster, 3.9x warp).

---

### F38i. opening integrated 25829/931 residual: PAL_OBJ allocation order in src/spr.rs:701-761 + second-cluster materialize animation  *(NEW)*

**Result.** *no result yet — new ticket.* F38h PARTIAL reduced opening integrated 72499/2691/40 → 25829/931/40 (commit landed on wt/f38h, residual is the PAL_OBJ allocation order at src/spr.rs:701-761 per F38e BLOCKED decomposition); F38e BLOCKED on rule conflict (1) hardcoded panel triple, (2) sub_801641A does NOT do per-step y motion — only Timer/Timer2 + mosaic+alpha setters; F38h's descriptor-encoding fix (panel_override_mask=0x07 + panels=[5,5,6]/[1,3,2]) solved (1).

**Why.** F38h PARTIAL stops at 25829/931; the residual is the second-cluster materialize animation's PAL_OBJ slot allocation order in src/spr.rs:701-761 (per F38e BLOCKED: enemy 3's 24-px y delta is the PAL_OBJ alloc-order symptom, NOT a per-step y motion — sub_801641A asm00_2.s:16101-16136 calls mosaic+alpha setters once per Timer tick, not per-step). The canon mechanism is sub_801641A (asm00_2.s:16101-16136) → mosaic+alpha dispatch in PAL_OBJ allocation order: enemy 1 mosaic → enemy 1 alpha → enemy 2 mosaic → enemy 2 alpha → enemy 3 mosaic → enemy 3 alpha, where rust's src/spr.rs:701-761 currently allocates PAL_OBJ slots 0/1/2 to enemies 0/1/2 in reverse. M2 acceptance is "integrated-row pixel parity"; closing opening integrated to 0/0/40 finishes the F38 series and the F38f-PARTIAL-with-revert story (commit 63cb569).

**Files.** src/spr.rs (only the PAL_OBJ allocation order at :701-761 — never a hardcoded enemy-index → slot mapping), src/actor.rs (only the Appearing state's PAL_OBJ call site at :528 — read the per-enemy slot from the descriptor), src/objects.rs (only the materialize animation spawn frame if it needs adjustment), tools/probe.py (only for per-frame OAM watch on PAL_OBJ slot allocation), tools/diffmask.py (only for the 25829/931 region split), docs/coverage/opening_integrated.md (notes)

**Do.**
1. Baseline `tools/harness.py --only opening --ui integrated` on HEAD → *report: opening integrated 25829/931/40 (F38h PARTIAL landing); cursor ≤1/1/170; mettaur 0/0/70; windowclose/result/field 0/0/40; all chip rows 0/0/30.*
2. Run `tools/diffmask.py --row opening --ui integrated` and `tools/probe.py --oam --pal --pos` at k=10, k=20, k=33, k=39 → *report: the per-region pixel totals of the 25829/931 residual (top/mid/bot), the per-enemy OAM slot index at each frame, the PAL_OBJ slot allocated to each enemy on rust vs canon's allocation order from sub_801641A (asm00_2.s:16101-16136).*
3. Read sub_801641A (asm00_2.s:16101-16136) for the mosaic+alpha dispatch order and the PAL_OBJ slot numbering → *report: file:line for the mosaic setter call, the alpha setter call, the per-Timer-tick semantics, the per-enemy PAL_OBJ slot index canon uses (slot 0=enemy 0, slot 1=enemy 1, slot 2=enemy 2 vs reverse).*
4. Port the PAL_OBJ allocation order into src/spr.rs:701-761 — sort enemy-index → PAL_OBJ slot in canon's order (not reverse); cite sub_801641A with `// provenance: sub_801641A asm00_2.s:16101-16136` → *report: new fn body, the cite count, diff at src/spr.rs:701-761, fitted-constant count delta (HEAD: 19).*
5. Re-run `tools/harness.py --only opening --ui integrated` → *report: opening integrated before/after (25829/931 → 0/0); cursor stays ≤1/1/170; mettaur 0/0/70; windowclose/result/field 0/0/40; all chip rows 0/0/30.*
6. Re-run `tools/verify_rows.py` from a clean detached checkout → *report: 67-row table — every chip row 0/0/30, mettaur 0/0/70, wave 0/0/90, popup 0/0/80, windowclose 0/0/40, result 0/0/40, field 0/0/40, buster 0/0/28, warp 0/0/30; opening integrated 0/0/40; warp integrated stays 40628/11744/30; buster integrated stays 54672/12977/28; cursor stays ≤1/1/170.*
7. Land with `tools/land.sh` → *report: 67-row table identical to HEAD except opening integrated 0/0/40; remove `opening:integrated` allowlist entry from tools/allowlist.py.*

**Rules.** Only the named files; FIXTURE_SIZE stays 64 (F38h's descriptor-encoding change is additive; cursor fixture state file's mask=0 = no override preserved); no new harness row; no alignment change; the fix is a port of sub_801641A's PAL_OBJ allocation order (cite asm00_2.s:16101-16136) — never a hardcoded enemy-index → slot mapping; every new literal gets `// provenance:`; `fitted constants in src/` (HEAD: 19) may not increase; canon never changes; ≤4 capture runs, one at a time inside the 3-slot semaphore; tool budget ≤100.

**Acceptance.** opening integrated reads 0/0/40 with PAL_OBJ allocation order cited at sub_801641A (asm00_2.s:16101-16136) in src/spr.rs comments; opening isolated stays 0/0/40; cursor ≤1/1/170 (F38h's mask=0 preserved); mettaur 0/0/70; windowclose/result/field 0/0/40; all chip rows 0/0/30; `opening:integrated` removed from tools/allowlist.py; no allowlist change elsewhere; fitted-constant count unchanged or lower.

**Measure and report.** row: opening (integrated) + cursor + windowclose + mettaur + result + field + the chip rows. frames: 40 opening, 170 cursor. total/worst: opening integrated before/after (25829/931 → 0/0); per-region top/mid/bot pixel counts; per-enemy OAM PAL_OBJ slot index. region: k=10..39 per-frame OAM/PAL trace on the Appearing state; per-enemy slot allocation order. commit: src/spr.rs (PAL_OBJ allocation order). One line of mechanism (canon's PAL_OBJ allocation order in sub_801641A asm00_2.s:16101-16136 calls mosaic+alpha setters in enemy-0,enemy-1,enemy-2 order; rust's src/spr.rs:701-761 currently allocates in reverse; the 24-px y delta for enemy 3 is the PAL_OBJ slot-alloc-order symptom, NOT a per-step y motion). One line of unverified (whether the second-cluster materialize animation's per-Timer-tick alpha ramp (sub_801641A asm00_2.s:16101 mosaic setter) also needs a port — the 25829/931 residual may include both the alloc-order fix and a per-Timer-tick ramp).

**Coordinator:** dispatch second. Worker muse-spark-1.3-contributor (F38e/F38f/F38h's child class — focused PAL_OBJ alloc-order port on the named residual), verifier GLM-5.3-flash cross-family; ≤$0.30 expected, ≤$0.60 cap; tool budget ≤100; verify_rows on the full table. Advances **M2** (integrated opening closes — F38 series finishes).

---

### T7w. battle_full k=179 mm_state_action/mm_anim/mm_timer: port the release-edge gate so the MegaMan executor's per-tick work respects canon's release path  *(NEW)*

**Result.** *no result yet — new ticket.* T7q PARTIAL added a seq.state gate in Actor::update (t1_player_entry early-returns Update::Nothing when seq.state in {SEQ_20, SEQ_24, SEQ_00, SEQ_04}); T7o PARTIAL relocated SEQ04 seq-match block from src/battle.rs:2533 → :3268 (after t1_player_entry); T7m BLOCKED on the k=179 group (worker hit tool soft-limit 60 on baseline only); T7i BLOCKED on the same group (worker hit tool soft-limit 80 on baseline only).

**Why.** battle_full sequencer holds at 9/540 frames (T7p no-op) and the k=179 mm_state_action/mm_anim/mm_timer residue is the named third M2 trace gap (T7m/T7i BLOCKED — both blocked on tool budget, not on a refuted premise). The k=179 group is mm_state_action=76 / mm_anim=78 / mm_timer=270 (per T7n PARTIAL cite); canon's release-edge at k=179 is sub_800840C (asm00_1.s:10441) per T7d PARTIAL — the [r5+2] latch that gates 0x00→0x04 frees the executor when the banner composite's leave predicate returns 0. T7q PARTIAL's seq.state gate handles the SEQ_20/SEQ_24/SEQ_00/SEQ_04 early-return but does NOT handle the release-edge — it returns Update::Nothing too early when seq.state == SEQ_00 before the latch frees at k=179. M2 acceptance is "battle_full trace: first divergence none"; closing the k=179 group drops battle_full sequencer from 9/540 toward 0/540.

**Files.** src/battle.rs (only the release-edge gate at Actor::update — never a fitted k=179 timer value), src/objects.rs (only if the mm_state_action dispatch needs the latch), tools/trace.py (only if seq.state watch needs widening), tools/probe.py (only the per-frame mm_state_action/mm_anim/mm_timer watch at k=170..190), docs/coverage/battle_full.md (notes)

**Do.**
1. Baseline `tools/trace.py record/diff --align row:battle_full` on HEAD → *report: battle_full sequencer 9/540 with the named divergence frames; mm_state_action/mm_anim/mm_timer at k=170..190 on both sides (target 76/78/270 on canon, current rust at k=179); T7q's seq.state gate verified active; cursor stays ≤1/1/170.*
2. Run `tools/probe.py --watch mm_state_action,mm_anim,mm_timer` at k=170..190 → *report: per-frame values on both sides at k=170..190; the precise frame where canon's release-edge fires; the per-frame seq.state trace at k=170..190.*
3. Read sub_800840C (asm00_1.s:10441) for the [r5+2] latch and sub_8008064 (asm00_1.s:10514) for the banner-idle return → *report: file:line for the latch's read site, the predicate that releases (latch == 0 AND banner_idle == 0 per T7u), the SEQ_00 → SEQ_04 transition's gate.*
4. Port the release-edge gate into src/battle.rs Actor::update as `fn release_edge_gate(seq_state: u8, latch: u32, banner_idle: bool) -> bool` with cited file:line; replace T7q's seq.state early-return at SEQ_00 with the canonical latch+idle check → *report: new fn body, the cite count, diff at src/battle.rs, fitted-constant count delta (HEAD: 19).*
5. Re-run `tools/trace.py record/diff --align row:battle_full` → *report: battle_full sequencer 9/540 → 0/540 or strictly less; mm_state_action/mm_anim/mm_timer at k=179 matches canon (76/78/270); cursor stays ≤1/1/170.*
6. Re-run `tools/verify_rows.py` → *report: 67-row table — every chip row 0/0/30, mettaur 0/0/70, wave 0/0/90, popup 0/0/80, windowclose 0/0/40, result 0/0/40, field 0/0/40, buster 0/0/28, warp 0/0/30; opening integrated stays 72499/2691/40; warp integrated stays 40628/11744/30; buster integrated stays 54672/12977/28; cursor stays ≤1/1/170.*
7. Land with `tools/land.sh` → *report: 67-row table identical to HEAD except battle_full sequencer divergence drops.*

**Rules.** Only the named files; no new harness row; no alignment change; the fix ports sub_800840C's [r5+2] latch + sub_8008064's banner-idle return (cite asm00_1.s:10441 / :10514) — never a fitted k=179 timer value or a hardcoded frame count; every new literal gets `// provenance:`; `fitted constants in src/` (HEAD: 19) may not increase; canon never changes; ≤4 capture runs, one at a time inside the 3-slot semaphore; tool budget ≤100.

**Acceptance.** battle_full sequencer divergence 0/540 (from 9/540); mm_state_action/mm_anim/mm_timer at k=179 = 76/78/270 matching canon; sub_800840C + sub_8008064 cited at asm00_1.s:10441 / :10514 in src/battle.rs comments; cursor stays ≤1/1/170; mettaur 0/0/70; every isolated pixel row reads 0; no allowlist change.

**Measure and report.** row: battle_full + cursor + windowclose + mettaur + result + field + wave + popup + the chip rows. frames: 540 battle_full, 170 cursor. total: battle_full sequencer before/after (9/540 → 0/540); mm_state_action/mm_anim/mm_timer at k=170..190 on both sides; per-frame seq.state trace. region: k=170..190 mm_state_action/mm_anim/mm_timer field at the release edge. commit: src/battle.rs (release_edge_gate fn). One line of mechanism (canon's release-edge at sub_800840C asm00_1.s:10441 fires when the [r5+2] latch reads 0 AND sub_8008064's banner-idle returns 0; T7q's seq.state gate handles SEQ_20/SEQ_24/SEQ_00/SEQ_04 early-return but returns Update::Nothing too early when seq.state == SEQ_00 before the latch frees at k=179 — the release-edge port closes the gap). One line of unverified (whether the closed k=179 group also fixes the RNG cadence residue at k=271+ frames, since T7v BLOCKED on the rng_cadence mirror which may share the same release-edge path).

**Coordinator:** dispatch third. Worker muse-spark-1.3-contributor (T7m/T7q's child class — release-edge gate port), verifier GLM-5.3-flash cross-family; ≤$0.30 expected, ≤$0.60 cap; tool budget ≤100; verify_rows on the regression set. Advances **M2** (battle_full sequencer 0/540 — closes the third M2 trace gap).
