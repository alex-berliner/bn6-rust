# Proposed tickets 20260916-050406 (minimax/MiniMax-M3, $0.1165)

The three next tickets:

---

### T50. M7 first Cross form port: TF_HEATCROSS charge shot via `busterBugChargeShotDamageCalcHappe`

**Why.** docs/SCOPE.md M7 status is "base form only"; the TF enum has 25 forms (`constants/constants.inc` 0x00..0x18). Charge-shot dispatch `off_80117D4` lives at `asm/asm00_2.s:5789`; `TF_HEATCROSS = 0x01` is the first Cross form and its charge shot routes through `busterBugChargeShotDamageCalcHappe` at `asm/asm00_2.s:5806`. No form-specific fixture exists yet. The base-form buster regression set is the only regression risk (F10/F10b BLOCKED at `buster` isolated 3172; the harness route at `buster_chrgsht` is the base form). T26 NEGATIVE closed only the custom-screen ladder model (different scope). Pattern from T17/T36/T46/T48 (data-driven reads through `chip_data_struct`): the form record's bytes are read directly. Establishes one Cross form so the 24 remaining forms have a template.

**New evidence.** T19 DONE made the asset contract loud so a form port declares its sprite range; T17/T36/T46/T48 data-driven pattern carried over to `megaman.rs` form table; no TF_HEATCROSS port has been tried.

**Files.** `src/forms.rs` (new, or the file holding `Tf` enum + buster damage calc), `src/megaman.rs`, `src/fixture.rs`, `tools/states.py` (new scripted-A fixture), `tools/harness.py` (add row), `tools/verify_rows.py`, `docs/worklog/T50.md`.

**Do.**
1. Add a battle fixture in `tools/states.py` / `src/fixture.rs` with `TF_HEATCROSS` active on MegaMan and a scripted A-press buster fire; cite `tools/states.py` and `constants/constants.inc:101-110` for the TF enum. **measurement.**
2. Run `python3 tools/verify_rows.py HEAD tfheatcross buster buster_chrgsht --expect N/N/N`; record baseline (the form row will be T until added, buster rows = current). **measurement.**
3. Port: replace the base-form buster damage calc with `match cur_form { TF_HEATCROSS => busterBugChargeShotDamageCalcHappe, _ => base }` reading `ChargeShotDispatchTable_80117D4` at `asm/asm00_2.s:5789` for any future `TF_*CROSS` form; cite `reference/bn6f` file:line for every key site (the table fetch, the per-form function pointer, the damage calc body). **measurement.**
4. Rebuild, re-run `tfheatcross` + `buster` + `buster_chrgsht` rows; record before/after. **measurement.**
5. Run `python3 tools/verify_rows.py HEAD tfheatcross buster buster_chrgsht wave window opening chip-cannon mettaur --expect ...`; report identical/veto. **measurement.**

**Rules.** Cite `reference/bn6f` file:line for the dispatch table and the per-form function; if the port needs a sprite/animation swap beyond the damage calc, NEGATIVE with the named site and stop (no scope creep); do not touch `buster` regression set; no allowlist change; no fitted constants.

**Acceptance.** tfheatcross row reads 0/0/N; `buster` and `buster_chrgsht` rows identical to HEAD; verify_rows on wave/window/opening/chip-cannon/mettaur identical to HEAD (veto on regression); dispatch key named in `src/forms.rs` (e.g. `fn buster_damage_for_form(tf: Tf) -> impl DamageCalc`).

**Measure and report.** tfheatcross · frames · total · worst · region · commit · first divergent frame · cite · one line of mechanism (which `off_80117D4` slot routes) · one line of what is unverified.

**Coordinator:** Reject edits outside `src/forms.rs` + `src/megaman.rs` + the new fixture/row; verifier confirms the dispatch-table cite and the buster-bug cite; verify_rows identical on buster regression set is the veto.

**SCOPE:** advances M7.

---

### T51. M5 second virus port: family 0x2 from `off_810A550`

**Why.** SCOPE M5 is "every family and rank". Mettaur (family 0) done (T6 DONE); Gunner in progress (T9c PARTIAL, T9h/j PARTIAL, T9k NEGATIVE, T9l PARTIAL, T45 NEGATIVE today on attack-event alignment). The roster from T12 DONE has family 0x2 at `byte_80182C4` rows 7..12 (enemy_idxs 7..12, versions 0..5); per-type routine is `off_810A550` table at `asm/asm00_2.s:19975`. The family's identity rows + structs are T12-resolved; this is a fresh sub-objective (different virus from the Gunner chain — T45 NEGATIVE today was on aligning the Gunner's first attack event, not porting family 0x2). T9c PARTIAL Gunner template (`ForGunner_8113078` 4-arm dispatch `sub_8112F70`/`sub_8112FBA`/`sub_8113002`/`ai_8113038`) carries straight across.

**New evidence.** T12 DONE laid `tools/rom_enemy_tables.py` for `byte_80182C4` + `off_8109150` Struct2; T9c PARTIAL landed the Gunner per-type dispatch in `src/gunner.rs` (so the object-dispatcher arm pattern is local); T32 DONE seeds the gunner gauge word; no family-0x2 entry has been tried.

**Files.** `src/objects.rs` (per-type routine + dispatch arm), `src/fixture.rs` (battle scenario that fields a family-0x2 enemy), `tools/states.py` (new), `tools/harness.py` (add row), `tools/verify_rows.py`, `docs/worklog/T51.md`.

**Do.**
1. Read `byte_80182C4` rows 7..12 from `tools/inventory.py`; cite `asm/asm00_2.s:19975` for the per-type routine entry at `off_810A550`. **measurement.**
2. Add a battle scenario in `tools/states.py` / `src/fixture.rs` that fields an enemy_idx 7 (version 0) and cite the `data/BattleSettings.s` formation containing it. **measurement.**
3. Run `python3 tools/verify_rows.py HEAD family02 --expect N/N/N`; record baseline (row will report T until added — note in report). **measurement.**
4. Port `ForFamily02_off_810A550` into `src/objects.rs` as `Family02Entry`; dispatch in `think()` (and `act()` if the routine has both arms); mirror T9c GunnerEntry pattern. Cite `reference/bn6f/<file>:<line>` for the routine and every key site (think init, version-byte branch, damage path, sprite slot). **measurement.**
5. Rebuild, re-run `family02` + `mettaur` + `gunner` rows; record before/after. **measurement.**
6. Run `python3 tools/verify_rows.py HEAD family02 mettaur gunner wave window opening chip-cannon --expect ...`; report identical/veto. **measurement.**

**Rules.** Cite `reference/bn6f/<file>:<line>` for the routine and every key site; if any phase goes unhandled (missing version branch, sprite slot, damage predicate), NEGATIVE with the phase + cite; do not widen the chip-row allowlist; do not touch the Gunner chain (T45 NEGATIVE veto); mettaur/gunner rows unchanged.

**Acceptance.** family02 row reads 0/0/N (or measured+named residue with cite if unreachable); mettaur at 0/0/N; gunner row unchanged from its current `verify_rows` number; `src/objects.rs` has `Family02Entry` with file:line cite for each routine arm.

**Measure and report.** family02 · frames · total · worst · region · commit · first divergent frame (or row-added T) · cite · one line of mechanism (which version-byte branch the routine hits first) · one line of what is unverified.

**Coordinator:** Verifier confirms the routine cite and dispatch key; reject edits to `src/gunner.rs` (Gunner chain open elsewhere); verify_rows identical on mettaur + gunner is the veto.

**SCOPE:** advances M5.

---

### T52. M3 first panel-effect port: read `word_3007924` at the per-panel-step damage path

**Why.** docs/SCOPE.md M3 status is "not started (the Mettaur/chip rows cover a few incidentally)". The panel flag word table at `word_3007924` was FOUND via T14 PARTIAL (13 words stride 4, one per panel type 0x0..0xC, OR-ed into `oPanelData_Flags` by `_object_updatePanelParameters` at `asm/asm38.s:4213-4219`; IWRAM copy at `0x3005B00+0x1E24` from `start.s:57-63`). T40 NEGATIVE tried the holy-panel halves-damage port via `sub_801A7F4` at `asm/asm00_2.s:22788-22791` and was NEGATIVE (cursor-fragile). T41 NEGATIVE tried the OR-via-flag-word approach and was NEGATIVE (cursor-fragile). T33 NEGATIVE tried locating panel-type writers. T35 BLOCKED on cracked panel step. A fresh angle: use a single, low-risk panel type (e.g. type 0x06 cracked panel step) and read its flag word into `oPanelData_Flags` BEFORE the damage path runs, NOT on the direct sub-routine port — so we rely on the table `T14 PARTIAL` already named rather than re-deriving the dispatch.

**New evidence.** T14 PARTIAL landed the panel-flag-word table cite with file:line (`asm/asm38.s:4242-4249`, `bn6f.map:34342`); T18 DONE derived the per-bit status sites, none of which overlaps with the panel flag word; T41 NEGATIVE closed the direct OR pattern, leaving the "read flag word at the right site" path open; T40 NEGATIVE closed the holy-panel direct-port path.

**Files.** `src/panels.rs` (or the file holding `oPanelData_Flags` + the per-panel-step damage path), `src/battle.rs` (read-only), `tools/harness.py` (add a `field` row variant or use existing), `tools/verify_rows.py`, `docs/worklog/T52.md`.

**Do.**
1. Read `src/panels.rs` (or grep `oPanelData_Flags` to find the file) and confirm the current per-panel-step damage path; cite `reference/bn6f/asm/asm38.s:4242-4249` for the table source. **measurement.**
2. Run `python3 tools/verify_rows.py HEAD field field-bg1 field-bg2 --expect N/N/N`; record baseline. **measurement.**
3. Read `word_3007924[type]` and OR it into `oPanelData_Flags` at the start of the per-panel-step site (NOT in the holy-panel sub-routine, which T40 NEGATIVE closed); cite the new read site and the asm table cite. **measurement.**
4. Rebuild, re-run `field` + `field-bg1` + `field-bg2` rows; record before/after. **measurement.**
5. Run `python3 tools/verify_rows.py HEAD field wave window opening chip-cannon mettaur cursor windowclose result --expect ...`; report identical/veto. **measurement.**

**Rules.** Cite `asm/asm38.s:4242-4249` for the flag word and `_object_updatePanelParameters` for the OR site; do not touch `sub_801A7F4` (T40 NEGATIVE) or the dispatch-via-flag-word per-T41 approach; no allowlist change; if the port needs a per-panel-type fixture that doesn't exist, NEGATIVE with the fixture gap named.

**Acceptance.** field row drops by ≥ 1 px on a single frame OR a measured+named residue with cite; verify_rows on wave/window/opening/chip-cannon/mettaur/cursor/windowclose/result identical to HEAD (cursor known 1/1/170 is the veto).

**Measure and report.** field · frames · total · worst · region · commit · first divergent frame · cite (asm file:line for table AND for the OR site) · one line of mechanism (which `word_3007924[type]` bit got read first) · one line of what is unverified.

**Coordinator:** Reject edits outside `src/panels.rs` + `tools/harness.py` (no `src/battle.rs` refactor); verifier confirms the flag-word cite and the OR-site cite are the same field-symbol pairs as `asm38.s:4242-4249` and `:4213-4219`; verify_rows identical on the 8-row regression set is the veto.

**SCOPE:** advances M3.
