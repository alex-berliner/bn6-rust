# Proposed tickets 20260917-051603 (minimax/MiniMax-M3, $0.0727)

### T95. M9 — Audio comparison tool: dump-side CLI that diffs two mgba audio captures sample-exact  *(OPEN)*

**Why.** SCOPE: "M9: Audio: the sound driver's battle side (music, SFX triggers from every routine that plays one), measured by dumping both sides' audio on a scenario and comparing sample-exact — per scenario: audio parity + the triggers' trace". The M9 row reads "not started (mgba can dump audio; a comparison tool is the first ticket)". mgba_capture.c (7ag owns, off-limits) writes raw audio; a Python comparator in `tools/` is needed, mirroring `tools/probe.py diff` for pixels. T13c BLOCKED measured cannon-route audio manually on one scenario — the manual diff is the symptom this ticket cures, and a follow-up can apply the tool. No M9 row is closable without it.

**New evidence.** T13c BLOCKED (2026-09-17): chip-fire and chip-damage SFXs on the cannon route gated by canon's predicates, but the per-sample diff was manual. HANDOFF §4 (descriptor/marker) names mgba's audio-dump path; `tools/probe.py`'s `diff` ergonomics (F35/F37 cited) is the comparator model. Reference/bn6f is read-only — the format cite will be in mgba_capture.c at a grep'd site.

**Files.** `tools/audio_diff.py` (NEW — load two audio dumps, return total sample diff + first divergent sample + max abs diff; flags for `--format s16le|raw`, `--rate 16384|32768`, `--bits 8|16`, `--channels 1|2`; mirror `tools/probe.py diff` ergonomics), `docs/handbook/audio.md` (NEW — usage + format matrix), `docs/worklog/T95.md`. **NOT** mgba_capture.c (7ag owns), src/, tools/harness.py, tools/oracle.py, tools/trace.py, assets/, reference/bn6f (read-only).

**Do.**
1. Grep mgba_capture.c for the audio-dump writer; cite the `file:line` for sample width, channels, rate, and the on-disk header (if any). Read `tools/probe.py diff` to mirror its ergonomics. **measurement.**
2. Write `tools/audio_diff.py`: CLI accepting two paths; load both; diff sample-exact (u8→i8 / u16→i16); print `total · worst_sample · first_div_sample · channels` to stdout; non-zero exit on non-zero diff; `--format raw|s16le` + `--rate` + `--channels` flags; `--quiet` (exit only) and `--json` (machine-readable). **code change.**
3. Smoke test on two byte-identical copies of a dummy audio dump: expect `0 / 0 / -1 / <channels>`, exit 0. **measurement.**
4. Smoke test on a single-byte mutated dump (write one byte at offset 0x1000 to a copy): expect `N / <mutated_sample_value> / <mutated_sample_offset> / <channels>`, exit non-zero. **measurement.**
5. Smoke test on a dump shortened by one sample: expect sample-count mismatch reported (total = N, last-sample divergence), exit non-zero. **measurement.**
6. Smoke test on two dumps with mismatched format flags (s16le vs raw): expect clean error, exit non-zero. **measurement.**

**Rules.** No mgba capture changes. Comparator is sample-exact (no tolerance, no resampling, no box). CLI mirrors `tools/probe.py diff` ergonomics. No allowlist, no patch_sterile, canon never changes. Tool budget ≤30 (pure tools/, no captures).

**Acceptance.** `tools/audio_diff.py` PASS on identical dumps (0 / 0 / -1, exit 0), PASS on mutated dump (non-zero total + named worst sample, exit non-zero), PASS on truncated dump (sample-count mismatch reported), PASS on format mismatch (clean error). `docs/handbook/audio.md` covers format matrix. NEGATIVE naming mgba's capture format (sample width, channels, rate, `file:line`) closes it; a tool that requires a tolerance to match canonical mgba output closes NEGATIVE.

**Measure and report.** four CLI invocations + their stdout/stderr/exit codes; mgba format cite (`file:line`); commit; one line of mechanism; one line unverified.

**Coordinator:** owns `tools/audio_diff.py` (new) + docs only. Free tier: smoke tests on the four CLI paths from a clean detached checkout, expect byte-identical results. ≤$0.02 expected, ≤$0.05 cap. Advances **M9**.

---

### T96. M9 — Audio parity first scenario: chip-cannon audio on the existing chip-cannon row, diffed via T95  *(OPEN)*

**Why.** SCOPE: "M9: ... per scenario: audio parity + the triggers' trace". T95 lands the audio comparison tool; this ticket applies it. The chip-cannon scenario is the cheapest available (a 40-frame fixture with one SFX per fire-cue); T13c BLOCKED already measured cannon-route audio manually on it. M9 has no applied audio comparison without this ticket — the tool alone is unused.

**New evidence.** T13c BLOCKED (2026-09-17) measured cannon-route audio manually on the chip-cannon scenario and named the predicates (chip-fire, chip-damage) at `file:line` in src/chips.rs or src/battle.rs. T95 lands `tools/audio_diff.py`. The chip-cannon state at `/tmp/chip_cannon.state` feeds the existing `chip-cannon` harness row.

**Files.** `tools/harness.py` (add `audio_chip_cannon` row: canon = REAL + chip-cannon scenario + audio dump at `/tmp/audio_canon_cannon.raw`; rust = plain_rom + chip-cannon descriptor + audio dump at `/tmp/audio_rust_cannon.raw`; diff via `tools/audio_diff.py` → expect 0 / 0 / -1), `tools/audio_diff.py` (consumer, T95), `docs/coverage/audio.md` (NEW — first entry: chip-cannon scenario, both audio dumps' sha256+size, diff result, SFX triggers found by `tools/trace.py`), `docs/worklog/T96.md`. **NOT** src/ (zero edits), tools/allowlist.py, tools/trace.py (consumer only), reference/bn6f.

**Do.**
1. Baseline, no edit: build ROM, sha256; run T95's `tools/audio_diff.py` on two copies of an existing audio dump (if T13c's manual work left one in `/tmp`) → expect0/0/-1. **measurement.**
2. Run the existing `chip-cannon` harness row; collect both audio dumps into `/tmp/audio_canon_cannon.raw` and `/tmp/audio_rust_cannon.raw`; report both sha256+size. **measurement.**
3. Diff both via `tools/audio_diff.py`; report total / worst_sample / first_div_sample. **measurement.**
4. If non-zero: identify the divergent SFX (fire vs damage vs hit), walk the trigger site in src/chips.rs or src/battle.rs at the predicate cited in T13c; report cite + per-frame diff. **measurement.**
5. If zero: append a one-paragraph summary to `docs/coverage/audio.md` with both dumps' hashes + diff result + the SFX triggers found by `tools/trace.py` on the scenario. **measurement.**
6. Re-run T95's four smoke tests after the chip-cannon run; expect identical to baseline (no regression in tools/). **measurement.**

**Rules.** No src/ change. Both audio dumps come from the same chip-cannon scenario + same descriptor (canon uses state, rust uses descriptor). Comparator is `tools/audio_diff.py` (sample-exact). No allowlist, no patch_sterile, canon never changes. ≤3 captures, tool budget ≤60.

**Acceptance.** `audio_chip_cannon` row reports 0 / 0 / -1 OR a precise per-SFX divergence with `file:line` cite + per-frame diff. `docs/coverage/audio.md` carries the chip-cannon entry. NEGATIVE naming the trigger site's missing cite (which predicate gates which SFX) closes it.

**Measure and report.** rows: `audio_chip_cannon` + existing `chip-cannon` (must stay 0/0/40); audio diff result; both audio dump hashes + sizes; ROM sha256+size; commit; one line of mechanism; one line unverified.

**Coordinator:** owns `tools/harness.py` + `tools/audio_diff.py` consumer + docs. Free tier: verify_rows from clean checkout on existing rows + T95 smoke tests. ≤$0.05 expected, ≤$0.10 cap. Advances **M9**.

---

### T97. M10 — Netbattle recon: handshake + chip-trade routines + UI state field + two-instance scenario design  *(OPEN)*

**Why.** SCOPE: "M10: Netbattle: the link session (two instances, the handshake, chip trading rules, the netbattle-specific UI and timers) — two-instance recording vs two-instance replay, trace + pixels on both sides". The M10 row reads "not started (needs link emulation in the capture tool)" — mgba_capture.c (7ag owns, off-limits) is the blocker for any capture-side work. A recon-only ticket breaks ground without touching the capture path: name the handshake routine, the chip-trade routine, the netbattle UI state field, and a two-instance scenario design. A follow-up ticket can then build the harness rows once mgba's link support lands (or the recon motivates a tooling pass).

**New evidence.** Reference/bn6f (the project fork) carries the link routines: the SIO/Cable handshake driver + the chip-trade routine are canon's regular code path (not in mgba). The netbattle UI is gated by a battleSettings flag at BattleSettings+0x4 (per SCOPE M1 backdrops cite, asm03_0.s:14592). No prior M10 ticket exists, so this is the first recon.

**Files.** `docs/coverage/netbattle.md` (NEW — first entry: handshake routine `file:line`, chip-trade routine `file:line`, netbattle UI state field offset + `file:line`, two-instance scenario design with frame numbers), `docs/worklog/T97.md`. **NOT** src/, mgba_capture.c, tools/, assets/, reference/bn6f (read-only).

**Do.**
1. Grep reference/bn6f for the link handshake routine (SIO/Cable driver); cite `file:line` + the routine's entry condition (which canon event arms the handshake). **measurement.**
2. Grep reference/bn6f for the chip-trade routine; cite `file:line` + the routine's entry condition + the traded-chip data field offset (which struct holds the traded chip id/code pair). **measurement.**
3. Grep reference/bn6f for the netbattle-specific UI state field; cite `file:line` + the field's offset in the battle struct + its trigger (which canon event arms the UI). **measurement.**
4. Design a two-instance scenario: identify the first BattleSettings record that arms netbattle UI; identify the SIO-init frame (handshake completion); identify the chip-trade window frame. Cite each `file:line`. **measurement.**
5. Append a one-paragraph summary to `docs/coverage/netbattle.md` with: handshake cite, chip-trade cite, UI state cite, two-instance scenario design. **measurement.**

**Rules.** Recon-only: no src/ change, no mgba_capture.c change, no allowlist, no patch_sterile, canon never changes. Cite every site; do not write `code` until a follow-up port ticket. Tool budget ≤40 (pure recon, no captures).

**Acceptance.** `docs/coverage/netbattle.md` carries the recon: handshake cite, chip-trade cite, UI state cite, two-instance scenario design with frame numbers. NEGATIVE naming the routine's missing cite (where the handshake arms, where the chip-trade writes) closes it; a recon without a scenario design closes NEGATIVE.

**Measure and report.** docs/headline: handshake routine address + chip-trade routine address + UI state field offset; cite count (target ≥6 `file:line` cites); commit; one line of mechanism; one line unverified.

**Coordinator:** owns docs only — recon-only, no pair needed. Free tier: `git diff --stat` from clean checkout, expect docs/ + worklog-only. ≤$0.02 expected, ≤$0.05 cap. Advances **M10**.
