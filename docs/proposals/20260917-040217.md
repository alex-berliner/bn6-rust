# Proposed tickets 20260917-040217 (zai/glm-5.3, $0.1168)

### T86. M2: PA recognition end-to-end — land T80 PARTIAL's recipe walk and the `pa_recog` row

**Why.** M2's chip-selection rules name PA recognition; SCOPE's program-advances line is **0/63**. T80 PARTIAL (wt/t80 @ e7738ea, docs/worklog/T80.md) landed the measurement half and names what remains: step 5 (PA_RECIPES + the walk in src/custom.rs), step 6 (harness row `pa_recog`), plus the deck blocker — the state's folder holds ONE Cannon (folder[0]=0x0106) while canon's OK path on B[00] needs 3 picks Cannon+Cannon+TankCan1. Already measured: PARecipePtrsA off_802BCB0 = 43 live records, off_802BC60 = 20 dead (never walked); record = [count][matcher][result u16][chip,code]*n; m=0 = ascending-code run ≤1 wildcard, m=4 = id-exact; recipe ids 0x140..0x15D = chip ids 320..349. Reader chain: custMenuPressOK_8028D3A → sub_8029110 → matchPARecipes_8029520 → on match sub_80292CC + sub_802B6F2 rebuild hand at 0x02036660, result at 0x02036664, remainder copied to 0x02036674.., used-bit (result−0x140) at 0x0203CA48+player*4. tools/states.py already carries the `patches` field + `pa_chipselect_state` ((0x5DDC0,0x01)). Baseline at T80 step 1: 13-row guard set identical, cursor 1/1/170/186279 known.

**Files.** `tools/states.py` (extend the patches tuple to the further deck words; no other State), `src/custom.rs` (PA_RECIPES + the OK-press walk in navigate() before returning Phase::Closing), `src/fixture.rs` (scripted 3×A + OK on the fixture — T7r's scripted-A precedent, engine path), `tools/trace.py` (PA result + hand fields in the oracle; our-side mirror 0x0203FF00 per T71), `tools/harness.py` (NEW row `pa_recog`), `docs/coverage/chips.md`, `docs/worklog/T86.md`. NOT: mgba_capture.c, no `--script`.

**Do.**
1. Baseline, no edit: build, sha256; verify_rows HEAD on wave,window,opening,chip-cannon,mettaur,windowclose,popup,cursor,chip-areagrab,chip-invisibl,chip-barrier,chip-barr100,chip-barr200 → 12 identical + cursor 1/1/170/186279. **measurement.**
2. Deck fix: dump chipselect.state's eBattleFolder words; extend patches so the first four deck slots read Cannon,Cannon,Cannon,TankCan1; capture canon once, watch 0x02036664 after the OK press → name the B[00] result id and its frame. **measurement.**
3. Port the walk: PA_RECIPES decoded from the off_802BCB0 records; in navigate()'s OK branch run the matcher (m=0 run / m=4 exact) BEFORE Phase::Closing; on match rewrite self.picks to the recognized hand, write result + remainder + used-bit, every arm cited (`// canon: matchPARecipes_8029520 asm03_0.s`). **code change.**
4. Fixture scripts the picks; build; run `pa_recog` → expect 0/0/40 with the negative (one deck word left unpatched → no triple → recognition must not fire) non-zero. **measurement.**
5. tools/trace.py on the scene: PA-result and hand fields over the scene's frames, first divergent frame named; then verify_rows from a clean detached checkout on the 13-row set + pa_recog. **measurement.**

**Rules.** Matcher ported from the cited records, no fitted result; recognition must fire through the engine's OK path; deck edits only via the patches tuple; no allowlist, no patch_sterile; any guard row worse is a veto. ≤6 captures, tool budget ≤70.

**Acceptance.** `pa_recog` 0/0/40, negative non-zero (non-blind); trace PA fields **0 divergent frames over the scene, first divergent frame: none**; 12 guard rows identical, cursor unchanged 1/1/170; SCOPE program advances 0/63 → 1/63 recorded with the recipe walk landed as data. NEGATIVE naming the un-portable arm, with the canon watch values, closes it.

**Measure and report.** rows: pa_recog + the 13-row guard set; frames 40; before/after totals, worst, region; the B[00] result id + recognition frame; trace field list + first divergent frame; ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified.

**Coordinator:** owns src/custom.rs + src/fixture.rs scripting + the pa_recog row; tools/states.py overlaps T87's file only — serialize that one file. Free tier: verify_rows clean checkout on 14 rows. ≤$0.30 expected, ≤$0.60 cap. Advances **M2**.

---

### T87. M1/M8: the formations line's first recorded scenario — the 3-slot CentralArea1 formation on the proven lever

**Why.** SCOPE M1 formations: **0/1076 recorded**. T65 (79da4ed) walked the streams — off_8020170 encounter tree (asm29.s:10371) + scripted battleSettingsList0 0x080aee70 / BattleSettingsList1 0x080b0d88 (getBattle_SettingsFromList0/List1 asm00_1.s:16046-16062), 1240 records / 84 lists / 1076 0xF0-terminated arrays — but no row exists. T58 PARTIAL (wt/t58 @ 0302084, docs/worklog/T58.md) already built the recording's hard half and left it unused: State `battlestart_mgm` (battlestart pokes + lever 0x375) → BattleSettings 0x080b4c18 = CentralArea1 record 10; slots populate at capture frame 69 — slot0 Mettaur 0x0001/0x0028, slot1 Gunner 0x0085/0x003c, slot2 Mettaur — HP byte-equal to the off_8109150 Struct2 row-0 elems; our side deterministic **0 px across two independent builds**. Known bound, already measured: this poke route never goes live (dword_203CA70 has no writer on either battlestart route, T9d; no enemy action ≥0x20 through 300 frames), so the row's window is materialize+idle — and the canon-vs-ours comparison on it has never been made.

**Files.** `tools/states.py` (re-land `battlestart_mgm` verbatim if grep finds it absent; no other State), `tools/harness.py` (NEW row `formation-central1`), `tools/trace.py` (enemy-slot fields only if one is missing from the oracle), `docs/coverage/formations.md` (NEW), `docs/worklog/T87.md`. NOT: any src/ file, no mgba_capture.c.

**Do.**
1. Baseline, no edit: build, sha256; grep battlestart_mgm tools/states.py — if absent re-apply T58's State (tip 0302084); verify_rows HEAD on mettaur,window,opening,cursor → identical, cursor 1/1/170/186279. **measurement.**
2. Capture canon and ours on battlestart_mgm, frames 0..160; per-frame pixel diff decomposed by region; name the first non-zero frame and its region. **measurement.**
3. tools/trace.py on the same window: name the first divergent field and frame (enemy slot HP/position/state groups); if frame 0, name the field. **measurement.**
4. Land row `formation-central1`: canon_ref at the slots-populated frame, window 0..160, negative = lever 0x371 (T58-verified → rec6, a different enemy set) must read non-zero; run it 2× for determinism. **measurement.**
5. verify_rows from a clean detached checkout on the guard set + formation-central1. **measurement.**

**Rules.** Recording ticket: zero src/ edits (T58 precedent, ZERO src changes); the negative fixture must fail; no allowlist, no region masks; the row lands RED if that is what canon-vs-ours reads, with the residue decomposed — a red row with a named first divergence is the deliverable, not something to hide.

**Acceptance.** `formation-central1` lands with the 0x371 negative non-zero (non-blind); first divergent trace field + frame AND first non-zero pixel frame + region both named; SCOPE M1 formations **0/1076 → 1/1076 recorded**; guard rows identical. NEGATIVE that names the spawn/materialize mechanism behind the residue, resting on the T65/T58 cites, also closes it.

**Measure and report.** rows: formation-central1 + mettaur,window,opening,cursor; frames 160; before/after totals (before = row absent), worst, region; trace first-divergence field/frame; ROM sha256+size, commit; one line of mechanism; one line unverified.

**Coordinator:** owns tools/states.py:battlestart_mgm + the new row; serialize tools/states.py against T86's patches tuple; pair with T88 (disjoint). Free tier: verify_rows clean checkout on 5 rows. ≤$0.20 expected, ≤$0.40 cap. Advances **M1** (formations line; feeds M8 scenario coverage).

---

### T88. M4: the next id-keyed chip family onto the record — T57's census picks it, T78's pad holds cursor

**Why.** SCOPE M4: 14 chips verified AS DATA (families 0x13, 0x15 sub 0x04, 0x21, Bomb, AreaGrab/Invisibl) and 29 verified-pixels through our own code — **43/411 total**. T57 DONE (verifier-hyper re-parsed all 411 rows) holds the census: 14 record-dispatch rows + **15 id-keyed rows** among the 43, per AttackFamily, in tools/inventory.py. The known rejector is the cursor veto (T56 NEGATIVE: cursor 1/1/170/186279, spread across behaviour-identical builds): it killed Cannon (T38 unmerged, T51 NEGATIVE) and Vulcan (T54 NEGATIVE); Recov amounts closed at T34 NEGATIVE; StepSwrd's row at T85 NEGATIVE — those families stay closed. The lever that survives it landed in T78 (e7ec05d): AreaGrab+Invisibl went onto the record with F12's pure-layout pad and cursor stayed 1/1/170. No family outside the closed set has been tried with that lever.

**Files.** `src/battle.rs` (remove the family's `match chip.id` arm; dispatch on the record's AttackFamily/AttackSubFamily + AttackPower +0x1a; apply T78's pure-layout pad), `tools/inventory.py` (AS_DATA_FAMILIES + the family), `docs/coverage/chips.md`, `docs/worklog/T88.md`. NOT: src/custom.rs, src/fixture.rs, tools/harness.py row configs.

**Do.**
1. Census, no edit: from T57's table in tools/inventory.py name the lowest AttackFamily that has ≥1 existing pixel row still id-keyed and is NOT in the closed set (Cannon T38/T51, Vulcan T54, Recov amounts T34, family 0x13's StepSwrd row T85); list its ids, its rows, and the ChipDataArr file:line cites. **measurement.**
2. Read the family's ChipDataArr records (family/subfamily/AttackPower bytes) and the asm arms the ids currently encode; cite each arm to remove. **measurement.**
3. Port: one family-dispatch arm reads the record bytes (per-record-byte dispatch, no match id), pad the ROM to hold cursor's layout; build, sha256 + size delta. **code change.**
4. Run the family's rows + chip-areagrab,chip-invisibl,chip-barrier,chip-barr100,chip-airshot + cursor: every chip row 0/0/N unchanged, negatives still failing, cursor 1/1/170 unchanged. **measurement.**
5. Re-run tools/inventory.py (the M4 line regenerates); verify_rows from a clean detached checkout on the family's rows + cursor. **measurement.**

**Rules.** No fitted constants — damage/amount from the record's own bytes with `// provenance:` tags; the pad is layout-only (guard rows prove behaviour unchanged); no new match-id arm; cursor worse than 1/1/170 is a veto; no allowlist. ≤6 captures, tool budget ≤60.

**Acceptance.** The family's N rows 0/0/N unchanged with the id arm gone, negatives non-blind; cursor 1/1/170 unchanged; AS_DATA_FAMILIES grows by one family; SCOPE M4 as-data **14 → 14+N**, own-code 29 → 29−N, total 43 unchanged. NEGATIVE naming the byte the record does not carry, with the census row that proves it, closes it.

**Measure and report.** rows: the family's rows (named in step 1) + 5 chip guard rows + cursor; frames per row config; before/after totals, worst, region; ROM sha256+size before/after; fitted count, commit; one line of mechanism; one line unverified.

**Coordinator:** owns src/battle.rs's chip dispatch + tools/inventory.py's family set; pair with T87 (disjoint). Free tier: verify_rows clean checkout. ≤$0.25 expected, ≤$0.50 cap. Advances **M4**.
