# Proposed tickets 20260916-033245 (minimax/MiniMax-M3, $0.1207)

T44 -- M4 chip-data arm: AirShot (id 4, element WIND, damage 20, code *) reads `AttackPower +0x1a` and `Element` from its `ChipDataArr` record, removing the per-id `match` arm in `src/chips.rs`.
**Why.** AirShot is one of 33 verified-pixels chips in SCOPE M4 (10 AS_DATA landed: sword/blade T17, Cannon T36 PARTIAL landed `be6e706`). The T36 commit proves the `AttackPower +0x1a` + `Element` pattern works for one Cannon-family row; AirShot is the next candidate by id and has no prior ticket on its arm. T28 NEGATIVE was on Recov/Barrier/Vulcan, T34 NEGATIVE its second pass -- a different family, closed. The Cannon/HCanon/MCannon regression set is at 0 and the sword/blade set is at 0, so a clean window.
**Files.** `src/chips.rs` (or the file holding AirShot's match arm -- grep for `0x04`, `"AirShot"`, or by `AttackFamily`), `tools/inventory.py` (update `AS_DATA_FAMILIES` if the chip moves from verified-pixels to AS_DATA).
**Do.**
1. Locate AirShot's match arm in `src/`. Cite file:line. Cite `data/ChipDataArr.s:127` for the record.
2. Baseline: build the ROM with `CARGO_TARGET_DIR` from `tools/worktree.sh`, run `python3 tools/harness.py --only airshot`, record `frames / total / worst / region`.
3. Replace the per-id arm with a read of `AttackPower +0x1a` and `Element` from the record (cite the read offset against the `chip_data_struct` in `include/rom_structs/ChipData.inc`); mirror T36's pattern. Cite the changed lines.
4. Rebuild, re-run the AirShot row, plus the Cannon/HiCannon/M-Cannon rows (T36 regression set) and the sword/blade rows (T17 regression set). Report each row's number.
5. Run `python3 tools/verify_rows.py HEAD airshot cannon hicannon mcannon sword wideswrd longswrd wideblde longblde fireswrd aquaswrd elecswrd bambswrd muramasa --expect airshot=T/F cannon=T hicannon=T mcannon=T sword=T wideswrd=T longswrd=T wideblde=T longblde=T fireswrd=T aquaswrd=T elecswrd=T bambswrd=T muramasa=T`. Report.
**Rules.** No allowlist change; no fitted constants; cite `reference/bn6f` file:line for any byte offset touched; if `AttackPower +0x1a` is wrong for AirShot (e.g. AirShot uses a different field), NEGATIVE with the measured offset and the name; Cannon and sword/blade rows must not regress.
**Acceptance.** AirShot row at 0/0/N or with a measured, named residue (frame + region); T17/T36 chip rows unchanged; `tools/inventory.py AS_DATA_FAMILIES` updated if the chip moves.
**Measure and report.** row · frames · total · worst · region · commit · one line of mechanism · one line of what is unverified.
**Coordinator:** run `verify_rows` on T17 + T36 + AirShot rows; if AirShot row reads0 and the verifier confirms the cite, land via `tools/land.sh`.
docs/SCOPE.md: M4.

T45 -- M5 virus port: family 0x2 (`off_810A550`, `byte_80182C4` rows 7-12, enemy_idx 7-12, versions 0..5) implements a per-type routine in `src/objects.rs`, dispatched the same way as `ForMettaur_8109EF4`.
**Why.** M5 progress: Mettaur done (T6); Gunner in progress (T9h PARTIAL, T9i PARTIAL, T9j PARTIAL, T9k NEGATIVE, T9l PARTIAL landed `424814d`, F40a BLOCKED). Family 0x2 is the next table-ordered family after Mettaur (`asm/asm00_2.s:19975`); its per-type routine is the next M5 item, separate from Mettaur and Gunner. The Mettaur port (T6) is the template: `ForMettaur_8109EF4` -> `MettaurEntry` -> `MettaurEntry init` -> dispatch in `think()`.
**Files.** `src/objects.rs` (per-type routine + dispatch arm); `src/fixture.rs` (battle scenario that fields a family-0x2 enemy at version 0); `tools/harness.py` (add the row); `tools/trace.py` (state trace). Tools/ only if the baseline proves no `src/` file holds the arm.
**Do.**
1. Add a battle scenario in `tools/states.py` / `src/fixture.rs` with a family-0x2 enemy at version 0. Cite `tools/fixture.rs` and `data/BattleSettings.s` (formation index that includes enemy_idx 7).
2. Baseline: build, run the harness on the new row, record `frames / total / worst / region`. Cite `reference/bn6f/asm/asm00_2.s:19975` (byte_80182C4 row 7) and the routine entry at `off_810A550`.
3. Port `ForXxx_off_810A550` to a per-type function in `src/objects.rs`, dispatched in `think()` (and `act()` if the routine has both arms), mirroring T6's MettaurEntry pattern. Cite `reference/bn6f` file:line for the routine and every key site (think/act init, the version-byte branch, the damage path).
4. Rebuild, re-run the new row, the mettaur row, and the gunner row. Report each.
5. Run `python3 tools/verify_rows.py HEAD family02 mettaur gunner --expect family02=T/F mettaur=T gunner=T`. Report.
**Rules.** No allowlist change; cite `reference/bn6f` file:line for the routine and every key site; if the routine has unhandled phases (e.g. a missing version-byte branch, a sprite slot, a damage-path predicate), NEGATIVE with the phase named and the measured site; mettaur and gunner rows unchanged.
**Acceptance.** Family-0x2 row at 0/0/N or with a measured, named residue (frame + region); mettaur at 0/0/N; gunner row unchanged from its current `verify_rows` number.
**Measure and report.** row · frames · total · worst · region · commit · one line of mechanism · one line of what is unverified.
**Coordinator:** `verify_rows` on mettaur, gunner, family-0x2; verifier confirms the routine cite and the dispatch key.
docs/SCOPE.md: M5.

T46 -- M7 first form port: `TF_HEATCROSS` (0x01) routes its buster shot through `busterBugChargeShotDamageCalcHappe` at `asm/asm00_2.s:5806`, replacing the bare base-form buster damage calc.
**Why.** M7 status is "base form only" per SCOPE; the TF enum has 25 forms (`constants/constants.inc`, 0x00..0x18); the charge-shot dispatch table is `off_80117D4` at `asm/asm00_2.s:5789`. `TF_HEATCROSS` (0x01) is the first Cross form; its charge shot is `busterBugChargeShotDamageCalcHappe` (`asm/asm00_2.s:5806`). No form-specific fixture exists yet. The base form is verified-pixels; the Cross variant must overwrite the buster damage path on form-change, so a port of one row establishes the pattern for the other 24 forms.
**Files.** `src/forms.rs` (new, or the file that already holds the TF enum and the buster damage calc); `src/megaman.rs` (if MegaMan's form lives there); `tools/states.py` / `src/fixture.rs` (new TF_HEATCROSS fixture, scripted-A buster fire). Tools/ only if no `src/` file holds the form.
**Do.**
1. Add a battle fixture in `tools/states.py` / `src/fixture.rs` with `TF_HEATCROSS` active on MegaMan and a scripted buster fire (the scripted-input recipe needs only an A-press). Cite `tools/states.py`.
2. Baseline: build, run the harness on the new TF_HEATCROSS row, record `frames / total / worst / region`. Cite `asm/asm00_2.s:5806` and `constants/constants.inc:101-110` for the TF enum.
3. Port the charge shot: replace the base-form buster damage calc in `src/forms.rs` with a dispatch on `TF_HEATCROSS` -> `busterBugChargeShotDamageCalcHappe`. Cite `reference/bn6f` file:line.
4. Rebuild, re-run the TF_HEATCROSS row plus the base-form buster row. Report each.
5. Run `python3 tools/verify_rows.py HEAD tfheatcross buster --expect tfheatcross=T/F buster=T`. Report.
**Rules.** No allowlist change; cite `reference/bn6f` for the charge shot and the TF dispatch; if the form needs more than a charge shot port (e.g. a sprite change, an emotion-window transition, a buster-pose swap), NEGATIVE with the missing piece named and the measured site; base-form buster row unchanged.
**Acceptance.** TF_HEATCROSS row at 0/0/N or with a measured, named residue (frame + region); base-form buster at 0/0/N; the dispatch key is named (e.g. `fn buster_damage_for_form(tf: Tf) -> impl DamageCalc`).
**Measure and report.** row · frames · total · worst · region · commit · one line of mechanism · one line of what is unverified.
**Coordinator:** `verify_rows` on the base-form buster + TF_HEATCROSS; verifier confirms the charge-shot cite and the dispatch key; if the port is clean, this ticket establishes the pattern for `TF_ELECCROSS`/`TF_SLASHCROSS`/etc. in follow-up M7 tickets.
docs/SCOPE.md: M7.
