# Proposed tickets 20260917-070936 (zai/glm-5.3, $0.1564)

---

### T106. M2: the Regular chip's insertion into the battle folder — port sub_800A3E4's reg-chip arm (asm00_1.s:15398-15698) that src/deck.rs says it does not reproduce  *(OPEN -- 2026-09-18)*

**Why.** src/deck.rs:14-16 states it outright: "The reg chip, tags and the giga re-insertion that the shuffle also does (asm00_1.s:15398-15698) are not reproduced: the folder here has none." The folder build is sub_800A3E4 (asm00_1.s:15355), the shuffle sub_800A570 (asm00_1.s:15568), the window offers the first live entries (sub_8027EE8, asm03_0.s:3510). The regular-chip MARK is already drawn (drawChipWindowMark_8029C08, src/custom.rs:153), so the surface exists, but no row measures the insertion rule: every current fixture deck carries the reg chip pre-placed or not at all. M2 names "chip selection rules (codes, Regular, Tag, PA recognition, folder draw/reshuffle)" as open; this is the Regular rule.

**Files.** `src/deck.rs` (the build: read the PET navi's regular slot, insert per the cited arm), `tools/states.py` (ONE scenario `regdeck_full` = battlestart.state + poke of the regular slot to a chip id that is NOT first in the drawn order + scripted none), `tools/harness.py` (ONE row `regdeck`; negative = same scenario WITHOUT the poke), `docs/coverage/regdeck.md`, `docs/worklog/T106.md`. NOT src/custom.rs (the mark stays as-is), NOT any existing scenario/fixture deck, NOT tools/allowlist.py.

**Do.**
1. Recon asm00_1.s:15398-15698: which bytes the reg arm reads (regular slot id+code, address cited), the skip compare, and the insert position into eBattleFolder; cite each file:line. **measurement.**
2. Baseline, no edit: build (sha256+size); verify_rows 7-row guard set + cursor → 0/0/N ×7, cursor 1/1/170; on canon, --watch eBattleFolder's halfwords and the offered slot ids over the windowclose scenario's frames; record where the reg chip sits and whether canon's offered list carries it. **measurement.**
3. Add `regdeck_full` + the `regdeck` row (canon_ref = the frame canon's offered list stabilizes; negative without the poke). **code change + measurement.**
4. Port the insertion in src/deck.rs's build (same compare/insert as step 1); rebuild, sha256. **code change + measurement.**
5. Re-run: `regdeck` 0/0/N with non-blind negative (no poke → no reg chip offered, both sides plain list); trace `regdeck_full`: the 12 slot-record chip ids and slot-0 id, first divergence **none** over the scene's frames; guard set + cursor identical to step 2; --watch battle_full's slot-0 and report whether its 174/540 moves (report only). **measurement.**

**Rules.** No existing scenario/fixture deck changes (F12 layout sensitivity); new literals get `// canon:`/`// provenance:`; pokes carry provenance; cursor veto ≤1/1/170/186300; no allowlist, no patch_sterile, canon never changes; fitted count HEAD 19 unchanged; ≤6 captures, tool budget ≤80; re-check every cite against disk.

**Acceptance.** Trace `regdeck_full`: offered-slot chip ids + slot-0 id over the scene's frames, first divergent frame **none**; `regdeck` 0/0/N with non-blind negative; 7-row guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming canon's measured insert behavior (position, frames, cite) closes it.

**Measure and report.** rows: regdeck + 7-row guard set (frames 40 each, cursor 170); slot-0 id canon/rust before/after; first divergent field+frame; ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (Tag pairing and giga re-insertion, same cite range).

**Coordinator:** runs alone (owns src/deck.rs + tools/states.py + tools/harness.py). Free tier: verify_rows from a clean checkout on the 9-row set; verifier for step 1's cites. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M2 (custom-screen chip selection: the Regular rule).

---

### T107. M4: Cannon/HiCannon/M-Cannon (ids 1-3) onto the record — the fourth AS_DATA family, already measured byte-equivalent by T51  *(OPEN -- 2026-09-18)*

**Why.** SCOPE M4 stands at 43/411 with `AS_DATA_FAMILIES = {0x13, 0x15, 0x21}` (tools/inventory.py:237). Ids 1/2/3 (data/ChipDataArr.s:34/:65/:96, stride 0x2c) are verified-pixels through id-matched arms. T51's New evidence (TODO_ARCHIVE.md:4876, 2026-09-16) already measured "chip.subfamily for ids 1/2/3 = 0/1/2 = id-1" — the family dispatch is byte-equivalent to the id match on main — and that "the chip-family port pattern itself does NOT regress cursor when the cursor deck has no [those] ids". T57's census gives the dispatch readers (AIAttackJumptable, asm/asm31.s:107962-107968). Three rows flip verified-pixels → verified for the cheapest possible port.

**Files.** `src/battle.rs` (**only** the Cannon arms in use_chip/chip_strike — T52's sites, src/battle.rs:4281-4293 at T52), `tools/inventory.py` (AS_DATA_FAMILIES extension only), `docs/SCOPE.md` (regenerated only), `docs/worklog/T107.md`. NOT src/chips.rs, assets/, tools/chip_export.py, tools/harness.py, src/hud*.rs, src/fixture.rs, reference/bn6f.

**Do.**
1. Read the three records' attack_family and AttackSubFamily bytes (include/rom_structs/ChipData.inc layout); name the family const and value; confirm all three ids share it. **measurement.**
2. Baseline: build (sha256+size); verify_rows the three Cannon rows (row names read from tools/harness.py) + cursor + 7-row guard set; quote every line. **measurement.**
3. Swap the id-match arms to family+subfamily match (T52's shape); add the measured family to AS_DATA_FAMILIES; regenerate docs/SCOPE.md; rebuild, sha256. **code change + measurement.**
4. Re-run the same rows: all three 0/0/N identical with non-blind negatives; SCOPE M1 chips line 43/411 → 46/411; report the sha delta (code shape only — pixels must not move). **measurement.**

**Rules.** T52's file set only; every literal named per AGENTS.md; cursor veto ≤1/1/170/186300; fitted count HEAD 19 unchanged; no allowlist, no patch_sterile; ≤4 captures, tool budget ≤60; cites re-checked against disk.

**Acceptance.** The three Cannon rows 0/0/N unchanged with non-blind negatives; the family byte measured constant across ids 1-3 and named with its ChipDataArr cite; SCOPE chips 43/411 → 46/411; verify_rows PASS is the veto. NEGATIVE with the three records' family bytes closes it if they are not constant.

**Measure and report.** row totals before/after (must be identical), ROM sha256 before/after, the family byte + cite, fitted count, commit; one line of mechanism; one line unverified (Vulcan family 0x17: 4 ids, arm table dword_80EBFEC per T54's census).

**Coordinator:** pairs with any ticket whose Files are disjoint (T106's deck.rs/states.py are); free tier: verify_rows on the Cannon rows + guard set from a clean checkout. ≤$0.10 expected, ≤$0.20 cap.

**Milestone advanced:** M4 (+3 chips verified as data; M1 chips 43/411 → 46/411).

---

### T108. M3: invisibility as a hit rule — a wave through an invisible MegaMan, the miss and the expiry, as one row and trace  *(OPEN -- 2026-09-18)*

**Why.** M3 is "not started" and invisibility is one of the 69 status bits (M1 statuses 0/69) with no rule row. The port already gates hits — src/actor.rs:616 `take_damage` returns false while `invisible > 0`, `set_invisible` :566, `is_targetable` :552, timer cite sub_8010474 (asm00_2.s:3288, FlashingInvisTimer) — but every current Invisibl surface is static: tools/harness.py:973-979 records chip-invisibl as "provably static on BOTH sides … no timing in an idle navi", and its negative is pixel-static. No hit ever lands inside the window on any row. Canon's behavior is unmeasured: what a Mettaur shockwave does to an invisible MegaMan (damage? flinch? popup? hit-stop?) and on which frame the timer expires.

**Files.** `src/actor.rs` (the hit-path gate, only if step 3's canon measurement differs), `tools/states.py` (ONE scenario `invis_hit` = the wave scenario's deck + Invisibl fired via the F11/F5b AIData poke so the shockwave reaches MegaMan INSIDE the window, plus a second wave after expiry), `tools/harness.py` (ONE row `invis_hit`; negative = same deck, Invisibl not fired → the hit lands normally), `docs/coverage/invis_hit.md`, `docs/worklog/T108.md`. NOT tools/allowlist.py, NOT any existing row or scenario.

**Do.**
1. Recon: cite canon's invis bit store and its hit-path test (sub_8010474 asm00_2.s:3288 and the reader the damage path takes), the timer's initial value and per-frame decrement; name the --watch field addresses (MegaMan HP, timer, CurAction). **measurement.**
2. Baseline: build (sha256+size); verify_rows 7-row guard set + cursor; capture chip-invisibl and wave as-is, quote their lines. **measurement.**
3. Canon measurement on `invis_hit`: the miss frames (popup yes/no, HP unchanged?, CurAction unchanged?), the timer-expiry frame, and the post-expiry hit; compare against the port's gate. **measurement.**
4. Port any difference in src/actor.rs's hit path (cite); rebuild, sha256 — or record "no change needed" with the cite. **code change + measurement.**
5. Row `invis_hit` 0/0/N with non-blind negative; trace: MegaMan HP + invisible timer + CurAction, first divergence **none** over the scene's frames (through miss AND post-expiry hit); guard set + cursor identical to step 2. **measurement.**

**Rules.** Behaviour only from cited canon sites; pokes provenance-tagged; no existing row changes; no allowlist, no patch_sterile; fitted count HEAD 19 unchanged; cursor veto ≤1/1/170/186300; ≤6 captures, tool budget ≤80.

**Acceptance.** `invis_hit` 0/0/N with a negative that fails (the un-invis hit lands); trace none on HP/timer/CurAction through both the miss and the post-expiry hit; verify_rows PASS is the veto. NEGATIVE naming canon's measured miss mechanics (frames, popup, cite) closes it and bounds the rule.

**Measure and report.** rows: invis_hit + chip-invisibl + wave + guard set (frames per row); miss/expiry frames canon vs rust; first divergent field+frame before/after; ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (the OTHER Invisibl-relevant bits among the 69, e.g. Blind's render gate, closed separately).

**Coordinator:** runs alone (owns tools/states.py + tools/harness.py rows); free tier: verify_rows on invis_hit + guard set; verifier for step 1's cites. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M3 (invisibility: first status bit closed as a rule row; M1 statuses 0/69 → 1 measured end to end).

---

### T109. M7: the first NaviCust battle effect as data — the Custom-parts offered count through sub_802A40C and its navicust handler  *(OPEN -- 2026-09-18)*

**Why.** M7 navicust battle effects stand at 0/19: T15 PARTIAL found the table — navicust_jt_NCPs (asm/asm37_0.s:2111, 47 words), dispatched by applyNavicustPrograms_813C684 (asm37_0.s:2012, index = sub_813B9FC's record halfword >> 2), handlers 32× SetCurPETNaviStatsByte (asm37_0.s:2161-2600) — but moved no count. The battle-side reader is already named and half-ported: getChipsOfferedPerWindow_802A40C = "the base count before Custom parts" (renames.md:212; src/custom.rs:86), and the window's slot template already covers twelve records (initChipWindowSlots_8027E90, src/custom.rs:92), so the art exists. Unmeasured: canon's offered count with a Custom part installed, and which navicust_NCP_* handler writes the stat byte sub_802A40C adds.

**Files.** `src/custom.rs` (the offered count: sub_802A40C's Custom-parts addition, read from the stat byte via T105's peek pattern), `src/fixture.rs` (descriptor stat field, append-only), `tools/states.py` (ONE scenario `ncp_custom` = the window scenario + the program installed — battery-save NaviCust memory set at build, or a pre-battle-init poke of the NCP record array, lever named and provenance-tagged), `tools/harness.py` (ONE row `ncp_custom`; negative = same scenario without the program), `docs/coverage/ncp_custom.md`, `docs/worklog/T109.md`. NOT tools/allowlist.py, NOT assets/, NOT any existing row.

**Do.**
1. Recon: read sub_802A40C; name the stat byte it adds and the navicust_NCP_* handler that writes it (asm37_0.s:2161-2600 + the NaviStats.inc slot); cite both file:line. **measurement.**
2. Canon measurement: on the ncp_custom scenario, --watch the offered count and the 12 slot records; record the base-count frames and the +N frames, and that the slot art for the extra chip comes from the existing template. **measurement.**
3. Port the addition in src/custom.rs (stat byte read, count = base + parts, exactly sub_802A40C's arithmetic); rebuild, sha256. **code change + measurement.**
4. Row `ncp_custom` 0/0/N with non-blind negative (no program → base count on both sides); trace: offered-count byte + live slot count, first divergence **none** over the window's frames; 7-row guard set + cursor identical to the pre-change baseline. **measurement.**

**Rules.** Canon's own give/apply chain must run, or the poke is pre-battle-init and provenance-tagged; window art from canon tiles only (no redraw); no existing row changes; no allowlist, no patch_sterile; fitted count HEAD 19 unchanged; cursor veto ≤1/1/170/186300; ≤6 captures, tool budget ≤80.

**Acceptance.** `ncp_custom` 0/0/N with non-blind negative; trace none on the count byte over the scene's frames; SCOPE M7 navicust battle effects 0/19 → 1/19; verify_rows PASS is the veto. NEGATIVE naming the handler and stat bytes (or the census contradiction) closes it.

**Measure and report.** rows: ncp_custom + windowclose + cursor + guard set; base and +N counts canon/rust before/after; first divergent field+frame; ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (the other 18 battle effects, and the full give/take chain GiveNaviCustPrograms → reloadCurNaviStatBoosts).

**Coordinator:** runs alone (owns src/custom.rs + the scenario); free tier: verify_rows on ncp_custom + guard set; verifier for step 1's handler cite (cross-family). ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M7 (navicust battle effects 0/19 → 1/19) and M2 (the offered-count selection rule).

---

### T110. M10: the two-instance link capture — canon×canon over the SIO link, the handshake's first frames  *(OPEN -- 2026-09-18)*

**Why.** M10 is "not started (needs link emulation in the capture tool)" and the recon that precedes it is DONE: T97 landed docs/coverage/netbattle.md (16 cites) plus the netbattle_recon.md supplement (d63a3c7 on main) — handshake libSIOControlStart (libs.s:896-905) writes 0x80 to SIOControlRegister 0x4000128 (gba_constants.inc:58-59); session FSM cb_80395A4 (asm03_1_1.s:1309-1346) over six states off_80395C8; netbattle chip trade sub_8039B0A (asm03_1_1.s:2018-2065); per-player overlay = the BattleSettings singleton 0x0200AF60 + BattleState_Unk_0d (battle_networkInvert, asm00_1.s:16532) + BATTLE_EFFECT_NETWORK_BATTLE 0x04. That recon's own closing line names this ticket: "frame numbers need the two-instance capture tool (mgba link support) — that is the follow-up ticket." Zero frames exist today; nothing in M10 can be measured until they do.

**Files.** The capture tool's source under `tools/` (the mgba capture binary; add the two-core link join + per-frame SIO log — exact file named in step 1), `tools/states.py` (the two-instance scenario entry per the recon §F six-step event table; no existing scenario changes), `docs/coverage/netbattle_frames.md`, `docs/worklog/T110.md`. NOT src/ (no ROM code), NOT tools/harness.py's existing rows, NOT tools/allowlist.py.

**Do.**
1. Recon: read the harness's capture path and the linked mGBA's link API; name the exact functions that join two cores with a link cable (or prove their absence) with file:line + version. **measurement.**
2. Implement the two-instance path: ONE command that runs two canon instances frame-stepped in lockstep, dumping each side's frames plus a per-frame SIO log (registers 0x4000120-0x4000128); no existing row's capture path changes. **code change + measurement.**
3. Record canon×canon per the recon §F event table: both sides reach the netbattle menu and sub_81465BC returns nonzero; dump BattleState_Unk_0d and the BATTLE_EFFECT_NETWORK_BATTLE byte per frame, both sides. **measurement.**
4. Reproduce: a second run byte-identical (frames + SIO log); name the handshake's first and last SIO frame and the six FSM states' frame ranges on both sides. **measurement.**

**Rules.** No src/ ROM code; no existing harness row changes; canon ROMs and /tmp's four canon roots untouched; no allowlist, no patch_sterile; the machine's 3-slot capture semaphore bounds the two instances; budget ≤80, one harness command at a time.

**Acceptance.** A reproducible two-instance canon recording — both sides' frame dumps + the SIO byte log, two runs identical — with the handshake frames named and both sides' FSM ranges reported; or a NEGATIVE naming the exact mGBA API gap (the capture tool's file:line where the join fails), which closes M10's approach question either way. No pixel or trace claim: both sides are canon; the rust side on the link is the next ticket.

**Measure and report.** instances, frames captured per side, SIO log length, handshake first/last frame, FSM state frame ranges both sides, run-to-run diff (must be empty), commit; one line of mechanism; one line unverified (everything past the handshake: chip trade, battle, and the rust side).

**Coordinator:** infra-heavy; runs alone (owns the capture tool + the scenario entry). Free tier: the recording reproduces from a clean checkout; verifier for the mGBA API cite. ≤$0.20 expected, ≤$0.30 cap.

**Milestone advanced:** M10 (step (a): the first two-instance recording).
