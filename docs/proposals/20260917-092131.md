# Proposed tickets 20260917-092131 (minimax/MiniMax-M3, $0.3019)

# T107. M2 battle_full appear→decide arm port — port T74's named lever (object_exitAttackState predicate)  *(OPEN)*

**Why.** T74 NEGATIVE on wt/t74-refused-hop (worklog-only) named the next M2 lever end-to-end: at battle_full k=17 the Mettaur's `MettaurHopReservePanel_8109D08` (reference/bn6f/asm/asm31.s:170730-170777) refuses the hop (object_canMove fails), writes HopDone=0 then calls `object_exitAttackState` (sub_801171C, asm00_2.s:5688-5727), which writes CurAction=8 at line 5720 unconditionally. In rust, `think()` at src/objects.rs:399 gates on `me.is_busy()` so it refuses to run during the appear phase — the predicate cause is met but the `cur_action = METTAUR_ACT_DECIDE` store at the appear→decide edge never fires. `oracle_fields` at src/actor.rs:729 hardcodes `Action::Hidden | Action::Appearing { .. } => 0x0a`, masking the 0x08 read after the flip. **New evidence:** T74's 13-field oracle pins first divergence at enemy_state_action k=17 (canon=(4,8), rust=(4,10)) on a non-blind test; T50 DONE shifted first divergence k=0→k=17; T68 PARTIAL landed docs only. battle_full trace 174/540 sequencer frames diverging (SCOPE M2). This is the named successor to T74.

**Files.** `src/objects.rs` (MettaurEntry think()/hop arms; add AttackStage/Unk_00 field; one-time `cur_action = METTAUR_ACT_DECIDE` store when is_busy() clears during Appearing), `src/actor.rs` (oracle_fields: add appear→decide 0x0a→0x08 branch at line 729), `include/structs/AIAttackVars.inc` (NEW: oAIAttackVars_Unk_00 field), `tools/trace.py` (verify enemy_state_action field traces battle_full), `docs/coverage/battle_full.md` (extend with k=17 line), `docs/worklog/T107.md`. **NOT** src/battle.rs, src/ai.rs, src/chips.rs, src/fixture.rs, tools/states.py, tools/harness.py, tools/allowlist.py, reference/bn6f.

**Do.**
1. **Recon:** cite `MettaurHopReservePanel_8109D08` (asm31.s:170730-170777), `sub_801171C` line 5720 (asm00_2.s:5688-5727), `object_canMove` (T74 cites), and the AIAttackVars struct field offsets for Unk_00/Unk_1a/Unk_1b. **measurement.**
2. Baseline, no edit: build ROM, sha256+size; verify_rows 8-row guard set + cursor → 0/0/N ×7, cursor 1/1/170; oracle battle_full → enemy_state_action first divergence at k=17 (canon=(4,8), rust=(4,10)); reproduce T74's 13-field sum. **measurement.**
3. Port the cited predicate: add `attack_stage: u8` field to MettaurEntry; in think() / hop arm at src/objects.rs:399-505, when `me.is_busy() && cur_action == METTAUR_ACT_HOP`, write `hop_done = 0; cur_action = METTAUR_ACT_DECIDE`; rebuild, sha256+size. **code change + measurement.**
4. Update `oracle_fields` (src/actor.rs:729) so the post-refuse CurAction=8 exports as 8 — add a `Action::Appearing { ticks: 1 } => 0x08` branch before the existing `=> 0x0a`. Rebuild, sha256+size. **code change + measurement.**
5. Re-run: oracle battle_full → enemy_state_action first divergence k=17 → k=21 (where T74's next divergence lives, enemy_anim canon=1, rust=0); trace `battle_full`: enemy_state_action first divergence **none through k=20**; 8-row guard set + cursor identical to step 2; verify_rows PASS is the veto. **measurement.**

**Rules.** Cite every src/ change to a reference/bn6f file:line. AttackStage/Unk_00 field added with `// canon: oAIAttackVars_Unk_00` and `// provenance: T74 worklog`. Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, canon never changes. ≤6 captures, tool budget ≤80.

**Acceptance.** enemy_state_action first divergence k=17 → k=21 on battle_full oracle; 13-field sum ≤ T74's 1370 (ideally 1370 - 458 = 912); 8-row guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming canon's measured no-flip behavior (e.g., enemy_state_action stays at (4,8)) closes it.

**Measure and report.** rows: battle_full oracle + 8-row guard set + cursor; frames 540/40/170. Cite every src/ edit + reference line, before/after first-divergence frame per T74's field list, 13-field sum, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (the k=21 enemy_anim divergence, the k=124 RNG cadence, the k=250 mm_state_action group).

**Coordinator:** owns src/objects.rs + src/actor.rs + the trace; runs alone (owns include/structs/). Free tier: verify_rows from a clean checkout on the 9-row set; verifier for step 1's three cites (MettaurHopReservePanel_8109D08, sub_801171C, AIAttackVars). ≤$0.25 expected, ≤$0.50 cap.

**Milestone advanced:** M2 (battle_full first divergence k=17 → k=21).

---

# T108. M7 emotion single-pass port — src/emotion.rs FACE_INDEX using T105's verified poke mechanism  *(OPEN)*

**Why.** T105 PARTIAL on wt/t105-emotion (bb5c01a, worklog-only) discovered the poke mechanism: `--poke 0x0203528E:0x0100` (16-bit half-word) sets byte 0x0203528F to 0x01 cleanly — the 8-bit `--poke` rounds to half-word 0x0203528E so the high byte lands at 0x0203528F and gets masked. T105 also mapped: BNEM header at assets/emotion.bin +0x00..+0x0c, face data at header-named offset; face bank off_801CD08 (reference/bn6f/asm/asm00_2.s:25567, 23 entries, stride 0x180, shared 0x80-byte right half dword_872D914); draw routine drawEmotionWindow_801CDEC (asm00_2.s:27554-27583) skips face when val==5||6; LZ77/BitUnPack of eStruct2035280 is per-init only (BIOS CpuSet at PC 0x00000240, 18 word-aligned writes at frame 0) so the poke persists through 40-frame windows. SCOPE M7 emotion reads 0/25. Multi-pass stays OPEN per T105. **New evidence:** T105 PARTIAL delivered a non-blind baseline (plain ROM sha256 0d3ea6d3, 584392 B; 7-row guard set isolated PASS; cursor 1/1/170/186279 within veto; --watch on 0x0203528F reads 0x00 for 40 frames on canon).

**Files.** `src/emotion.rs` (FACE_INDEX `[u8; 24]` table keyed by emotion value 0..23 with skip-5/6 mapping; draw call emits one of 23 face slots), `assets/emotion.bin` (extend from 1 to 23 face slots, total 0x1B30 bytes), `tools/emotion_export.py` (extend to 23 slots, header 0x00..+0x0c unchanged), `tools/states.py` (ONE scenario `emotion_syn_full` = `battlestart.state` + `--poke 0x0203528E:0x0100` at f60), `tools/harness.py` (ONE row `emotion_syn`: canon=HOLD-poke, negative=no-poke→calm-both-sides), `docs/coverage/emotion.md` (NEW), `docs/worklog/T108.md`. **NOT** src/battle.rs, src/objects.rs, src/ai.rs, src/chips.rs, src/fixture.rs, tools/trace.py, tools/oracle.py, tools/allowlist.py, tools/inventory.py, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. **Recon:** extend tools/emotion_export.py to dump all 23 face slots (assets/emotion.bin to 0x1B30 bytes); cite each face's data start against off_801CD08 offsets; document shared right-half dword_872D914 and drawEmotionWindow_801CDEC's skip-5/6 path. **measurement.**
2. Baseline, no edit: build ROM, sha256+size; verify_rows 8-row guard set + cursor → 0/0/N ×7, cursor 1/1/170/186279; capture `emotion_syn_full` no-poke → both sides render calm face at OBJ (0,18) and (32,18). **measurement.**
3. Add `emotion_syn_full` scenario + `emotion_syn` row (canon=poke-0x0100-at-f60, negative=no-poke→calm-both-sides match step 2). **code change + measurement.**
4. Port src/emotion.rs: read byte 0x0203528F (peeked, `// provenance: T105 worklog`); `const FACE_INDEX: [u8; 24]` table with skip-5/6 mapping from drawEmotionWindow_801CDEC; rebuild, sha256+size. **code change + measurement.**
5. Re-run: `emotion_syn` 0/0/40 with non-blind negative; trace `emotion_syn_full`: emotion byte + OBJ tile at (0,18)/(32,18) first divergence **none** over the row's frames; 8-row guard set + cursor identical to step 2; verify_rows PASS is the veto. **measurement.**

**Rules.** Art from canon bytes, not redrawn (BNEM export is data). FACE_INDEX table cited from off_801CD08 with `// provenance: off_801CD08 asm00_2.s:25567`. State/descriptor poke carries `peeked` provenance. Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, canon never changes. ≤6 captures, tool budget ≤80.

**Acceptance.** `emotion_syn` 0/0/40 with non-blind negative (no-poke→calm-both-sides; poke→value-1-face-both-sides); OBJ tiles at (0,18)/(32,18) match between canon and rust; trace `emotion_syn_full`: emotion byte + OBJ tile first divergence **none** over the scene's 40 frames; 8-row guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming canon's measured no-face-change behavior (frames + bytes + cite) closes it.

**Measure and report.** rows: emotion_syn + 8-row guard set + cursor; frames 40 each (cursor 170). Emotion byte cite + face offset per value, before/after pixel totals, OBJ tile counts at the emotion positions both sides, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (the other ~22 emotion values, not value 1).

**Coordinator:** owns src/emotion.rs + asset layout + the row; runs alone (owns tools/states.py + tools/harness.py + tools/emotion_export.py). Free tier: verify_rows from a clean checkout on the 9-row set; verifier for step 1's BNEM header + face-bank cites. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M7 (emotion window 0/25 → 1/25 first port).

---

# T109. M4 Recovery family (0x20) as data — port the 8 recov chips through AttackPower +0x1a  *(OPEN)*

**Why.** The 8 recov chips in tools/scoreboard.py (Recov 0x9A, Recov30 0x9B, Recov50 0x9C, Recov80 0x9D, Recov120 0x9E, Recov150 0x9F, Recov200 0xA0, Recov300 0xA1) all share attack_family 0x20 with attack_subfamily 0x00 and the same effect_flags/unk_0e/lock_on_type: the only behaviour-selecting bytes are AttackPower (recov amounts) and lockout_frames (0x1E for all 8). Verified at reference/bn6f/data/ChipDataArr.s:4786/4817/4848/4879/4910/4941/4972/5003 — the eight records carry the same family code, same subfamily, same chip_element (0x0A = Null) and differ only in attack_power. AS_DATA_FAMILIES = {0x13, 0x15, 0x21} (T57 audit, T61 re-admit for 0x15); expanding to {0x20} fits the precedent of T17/T46/T48/T52/T78 (one const table per family, dispatch on `chip.family`, behavior reads AttackPower +0x1a). T28 NEGATIVE closed a different "amounts from record" attempt — that one mixed families (Recov + Barrier + Vulcan) and the verifier caught a per-chip-id arm. **New evidence:** all 8 recov chips share ONE family, ONE subfamily, ONE chip_element, ONE effect_flags — a single record-driven dispatch is exactly the T17 sword pattern. The T57 43-row audit confirms the remaining 15 id-dispatched chips; none of the 8 recov ids is in AS_DATA_FAMILIES today. SCOPE M4 row at 43/411 → 51/411 (this ticket adds 8 verified-pixels→verified rows if landed).

**Files.** `src/chips.rs` (one const table `RECOV_FAMILY = 0x20`; `match chip.family` arm reads AttackPower +0x1a and applies HP heal), `src/battle.rs` (replace `match chip.id` arm for 0x9A..0xA1 with the chip.family dispatch at the recov apply site, currently `match chip.id` in `use_chip`), `tools/inventory.py` (AS_DATA_FAMILIES add 0x20), `tools/scoreboard.py` (8 rows `demo-recovery`..`demo-recov300` already present), `docs/coverage/chips.md` (NEW), `docs/worklog/T109.md`. **NOT** src/objects.rs, src/ai.rs, src/emotion.rs, src/charge_shot.rs, src/fixture.rs, tools/trace.py, tools/oracle.py, tools/allowlist.py, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. **Recon:** cite the 8 ChipDataArr.s struct lines (4786/4817/4848/4879/4910/4941/4972/5003); tabulate attack_family/subfamily/chip_element/attack_power per row; locate the current `match chip.id` arm for the 8 ids in src/battle.rs and src/chips.rs; confirm `chip.family` accessor reads AttackFamily +0xb. **measurement.**
2. Baseline, no edit: build ROM, sha256+size; verify_rows 8-row guard set (opening/mettaur/cannon/buster/chip-use/wave/windowclose/cursor) PASS; verify_rows the 8 recov rows → all 0/0/40 PASS; capture one (e.g., demo-recov150) with both sides' HP-after equal (canon trace). **measurement.**
3. Port src/battle.rs: replace the `match chip.id` arm with `if chip.family == RECOV_FAMILY { heal_amount = chip.attack_power; }`; rebuild, sha256+size. **code change + measurement.**
4. Update tools/inventory.py AS_DATA_FAMILIES += {0x20} (mirrors T48's 0x15 re-admit precedent); rerun inventory; confirm the 8 recov rows flip to `verified` in SCOPE. **code change + measurement.**
5. Re-run: 8 recov rows 0/0/40 (PASS) with non-blind negative (one row stripped to id-dispatch → pixel mismatch on the heal delta); 8-row guard set + cursor identical to step 2; verify_rows PASS is the veto; SCOPE M4 row reads 51/411. **measurement.**

**Rules.** Art/amount from canon bytes (attack_power per record), not redrawn. Const table cited from ChipDataArr.s with `// canon: ChipDataArr_8021DA8` and `// provenance: T57 audit + T109 worklog`. Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, canon never changes. ≤6 captures, tool budget ≤80.

**Acceptance.** All 8 recov rows (demo-recovery, demo-recov30, demo-recov50, demo-recov80, demo-recov120, demo-recov150, demo-recov200, demo-recov300) 0/0/40 with non-blind negative; SCOPE M4 row 43/411 → 51/411; AS_DATA_FAMILIES = {0x13, 0x15, 0x20, 0x21}; 8-row guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming canon's measured no-heal behavior (frames + bytes + cite) or a row that doesn't move closes it.

**Measure and report.** rows: 8 recov + 8-row guard set + cursor; frames 40 each (cursor 170). Per-row chip_data_struct cite + AttackPower, before/after pixel totals, HP-before/HP-after both sides, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (any other family-0x20 chip outside the scoreboard's 8, e.g., line 8940).

**Coordinator:** owns src/chips.rs + src/battle.rs + the family table; runs alone (owns tools/inventory.py). Free tier: verify_rows from a clean checkout on the 9-row set; verifier for step 1's 8 struct cites. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M4 (recovery family 0/8 → 8/8 verified-as-data; AS_DATA_FAMILIES expanded 3 → 4).

---

# T110. M1 navicust battle-effect handler scenarios — enumerate the 19 M7-row from T15 PARTIAL  *(OPEN)*

**Why.** SCOPE M1 navicust reads 0/19: the FOUND table at asm/asm37_0.s:2111 (navicust_jt_NCPs, 47 words stride 4: 45 navicust_NCP_* + navicust_GigFldr1 + a no-op stub) is identified by T15 PARTIAL (LANDED 152ce3c) as the per-program handler dispatch (NOT a program-id enumeration — the index comes from sub_813B9FC(id-1) record halfword >> 2, asm37_0.s:2012). Of the 47 handlers, T15 PARTIAL distinguishes 32x SetCurPETNaviStatsByte + 11x GetCurPETNaviStatsByte + 3 misc (the no-op stub, GigFldr1, and one tracker), of which 19 affect battle (the M7 line). Without per-handler scenarios the M1 row stays 0/19 forever. T15 PARTIAL landed only docs; no scenario file exists. **New evidence:** T15 PARTIAL enumerated the 47 handlers with cites; T15's verifier-hyper re-run cross-checked the dispatch (sub_813B9FC); tools/states.py supports per-state poke + scripted input. The cheapest first scenario is the no-op stub (one capture, no edit to handlers needed; only the row needs adding).

**Files.** `tools/states.py` (NEW scenarios: `navicust_noop_full` = `battlestart.state` + `--poke 0x0203XXXX:0x0001` per handler index; one row per handler, 19 rows total over 2 batches to fit the budget), `tools/harness.py` (NEW 19-row block `navicust_handlers`: canon=HOLD, rust=plain_rom, all 19 reading a single NAVICUST handler byte; negative = same scenario WITHOUT poke → bytes match step 2 baseline), `tools/inventory.py` (extend parse_navicust to enumerate the 19 battle-effect handlers, cite per-handler site in asm37_0.s:2111-2600), `docs/coverage/navicust.md` (NEW), `docs/worklog/T110.md`. **NOT** src/battle.rs, src/objects.rs, src/chips.rs, src/fixture.rs, src/emotion.rs, src/charge_shot.rs, tools/trace.py, tools/oracle.py, tools/allowlist.py, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. **Recon:** list the 19 battle-effect handlers (T15 PARTIAL table — 32 SetCurPETNaviStatsByte + 11 GetCurPETNaviStatsByte → filter by which NAVI stat slots affect battle: HP, Atk, Spd, Charge, etc. via include/rom_structs/NaviStats.inc). Cite per-handler: `// canon: navicust_NCP_<name>` line in asm37_0.s:2111-2600. **measurement.**
2. Baseline, no edit: build ROM, sha256+size; verify_rows 8-row guard set + cursor → 0/0/N ×7, cursor 1/1/170; capture `navicust_noop_full` no-poke → both sides' NAVI stats byte-for-byte equal (control scenario). **measurement.**
3. Add `navicust_noop_full` scenario + 1 row (`navicust_noop`: canon=poke-stub-byte, negative=no-poke→matches step 2 baseline) — first M1 row. **code change + measurement.**
4. Add the remaining 18 scenarios + 18 rows in 2 batches (9 each, ≤6 captures per ticket × 3 ticket cycle); each scenario pokes one handler's NAVI stat byte; each row canon=HOLD, rust=plain_rom, negative=no-poke. **code change + measurement.**
5. Re-run: 19 navicust rows 0/0/N with non-blind negative; SCOPE M1 navicust row 0/19 → 19/19; 8-row guard set + cursor identical to step 2; verify_rows PASS is the veto. **measurement.**

**Rules.** Scenarios are poke-driven, no hand-played input (T20 BLOCKED's hard stop is preserved). Each poke carries `peeked` provenance. NAVI stat slot indexes cited from include/rom_structs/NaviStats.inc. Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, canon never changes. ≤6 captures per ticket × 3 ticket cycle, tool budget ≤80 per cycle.

**Acceptance.** 19 navicust rows 0/0/N with non-blind negative; SCOPE M1 navicust row 0/19 → 19/19; tools/inventory.py parse_navicust enumerates the 19 battle-effect handlers with per-handler cites; 8-row guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming canon's measured no-stat-change behavior (frames + bytes + cite) closes it.

**Measure and report.** rows: 19 navicust + 8-row guard set + cursor; frames per row 40 (cursor 170). Per-handler cite + NAVI stat slot, before/after pixel totals, stat-byte trace, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (the 28 handlers that DON'T affect battle, listed as out-of-scope).

**Coordinator:** owns tools/states.py + tools/harness.py + tools/inventory.py + the rows; runs alone. Free tier: verify_rows from a clean checkout on the 9-row set per cycle; verifier for step 1's 19-handler cite list. ≤$0.60 expected over 3 cycles, ≤$1.20 cap.

**Milestone advanced:** M1 (navicust battle-effects 0/19 → 19/19 first enumerated).

---

# T111. M4 Vdoll (id 0x96, family 0x12) as data — single-chip port, fastest ship-able AS_DATA expansion  *(OPEN)*

**Why.** Vdoll is a single pixel-verified chip (demo-vdoll 0x96, scoreboard.py:30, 70 frames) with attack_family 0x12 (ChipDataArr.s:4662, attack_subfamily 0x08), standing alone among the 14 family-0x12 records (lines 1686-3081, 4662). The 0x12 family itself is closed (no other pixel-verified chip in this family on the scoreboard), so this is a single-chip port: dispatch on `chip.family == 0x12` for id 0x96 only. AS_DATA_FAMILIES = {0x13, 0x15, 0x21} (T57 audit); T109 expands it to {0x20}. Vdoll is the cheapest follow-up: one const, one chip-id-to-arm remap, one row, no subfamily branching. **New evidence:** T57 PARTIAL identified all 43 pixel-verified chips as record-or-id (Vdoll is in the 15 id-dispatched set); T61 PARTIAL/T78 DONE showed the cursor veto survives the family-based dispatch pattern when no other ids in the family are remapped. T54 NEGATIVE closed Vulcan (0x17), T51 NEGATIVE closed Cannon (0x14) — those were families with 3-4 chips. A SINGLE-chip family port has zero risk of cross-chip regression. SCOPE M4 row at 51/411 → 52/411 if landed.

**Files.** `src/chips.rs` (one const `VDOLL_FAMILY = 0x12`; `match chip.family` arm for id 0x96 reads AttackPower +0x1a + AttackSubFamily 0x08), `src/battle.rs` (replace `match chip.id` arm for 0x96 with the chip.family dispatch at the Vdoll apply site), `tools/inventory.py` (AS_DATA_FAMILIES add 0x12), `tools/scoreboard.py` (one row `demo-vdoll 0x96` already present), `docs/coverage/chips.md` (append), `docs/worklog/T111.md`. **NOT** src/objects.rs, src/ai.rs, src/emotion.rs, src/charge_shot.rs, src/fixture.rs, tools/trace.py, tools/oracle.py, tools/allowlist.py, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. **Recon:** cite ChipDataArr.s:4662 (Vdoll's record); tabulate attack_family/subfamily/chip_element/attack_power; locate the current `match chip.id == 0x96` arm in src/battle.rs and src/chips.rs; confirm `chip.family == 0x12` does not collide with any other pixel-verified id (the scoreboard has no other 0x12 chip). **measurement.**
2. Baseline, no edit: build ROM, sha256+size; verify_rows 9-row guard set (opening/mettaur/cannon/buster/chip-use/wave/windowclose/cursor/vdoll) → Vdoll 0/0/70 PASS, others 0/0/N; cursor 1/1/170/186279; capture demo-vdoll with both sides' damage-taken equal (canon trace). **measurement.**
3. Port src/battle.rs: replace the `match chip.id == 0x96` arm with `if chip.family == VDOLL_FAMILY { apply_vdoll(chip.attack_power); }`; rebuild, sha256+size. **code change + measurement.**
4. Update tools/inventory.py AS_DATA_FAMILIES += {0x12} (mirrors T48/T109 precedent); rerun inventory; confirm Vdoll row flips to `verified` in SCOPE. **code change + measurement.**
5. Re-run: demo-vdoll 0/0/70 with non-blind negative (one-shot stripped to id-dispatch → pixel mismatch on the apply); 9-row guard set + cursor identical to step 2; verify_rows PASS is the veto; SCOPE M4 row reads 52/411. **measurement.**

**Rules.** Art/amount from canon bytes (attack_power per record), not redrawn. Const table cited from ChipDataArr.s:4662 with `// canon: ChipDataArr_8021DA8` and `// provenance: T57 audit + T111 worklog`. Cursor veto ≤1/1/170/186300. Single-chip port — NO subfamily branching required (subfamily 0x08 is the only one in 0x12 on the scoreboard). No allowlist, no patch_sterile, canon never changes. ≤4 captures, tool budget ≤60.

**Acceptance.** demo-vdoll 0/0/70 with non-blind negative; SCOPE M4 row 51/411 → 52/411; AS_DATA_FAMILIES = {0x12, 0x13, 0x15, 0x20, 0x21}; 9-row guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming canon's measured no-apply behavior (frames + bytes + cite) closes it.

**Measure and report.** rows: demo-vdoll + 9-row guard set + cursor; frames 70 each (cursor 170). Vdoll chip_data_struct cite + AttackPower + AttackSubFamily, before/after pixel totals, OAM byte trace, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (the 13 other family-0x12 records at lines 1686-3081, out-of-scope until scored).

**Coordinator:** owns src/chips.rs + src/battle.rs + the family table; runs alone (owns tools/inventory.py). Free tier: verify_rows from a clean checkout on the 10-row set; verifier for step 1's single cite. ≤$0.15 expected, ≤$0.30 cap.

**Milestone advanced:** M4 (vdoll row 51/411 → 52/411 verified-as-data; AS_DATA_FAMILIES expanded to 5).
