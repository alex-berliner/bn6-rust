# Proposed tickets 20260914-180113 (minimax/MiniMax-M3, $0.1249)

# Three proposed tickets, ordered by $/0 existing-row close against SCOPE.md milestones

The ranking is M2 first (sequencer closes 5 integrated rows + battle_full), then M5 unblock (T9d enables the next ported-virus row under the interpreters), then M9 audio (T13b, no existing row). The dollar argument for each is the headline.

---

### F38c. Close the five integrated rows to 0 via the sequencer predicate fix (T7d residual)  *(OPEN -- 2026-09-14)*

**Files.** src/battle.rs (the three sequencer-edge traps only: byte_203C974 read, dword_20367F0 read, the 0x8000 mask read on dword_20352C0), tools/trace.py (only if a field must be watched to see an edge), docs/coverage/ (notes), reference/bn6f (read)

**Why.** M2 (the lowest unmet milestone in docs/SCOPE.md, and the user's standing rule "M2 before content") is gated on the sequencer reaching 540/540 on battle_full without re-alignment. The only mechanism that closes it is the residual T7d's verifier named: "replace the three counts with byte_203C974 / dword_20367F0 / mask 0x8000 reads and fix the three short cites". The arithmetic is already closed: T7c's stored battle_full record reads 0x1c 0..10, 0x08 11..41, 0x20 42..43, 0x24 44..143, 0x00 144..146, 0x04 147..206 (= 60 frames), 0x08 207..315, 0x0C 316..; our side stays at 0x08 on the same frames, and the 173/540 divergent = 165 window-state frames + 8 kill-timing frames — every divergent frame is named, no fixture gap. The five integrated rows waiting on this same fix — opening 72499 / 2691 / 40, field 158979 / 5609 / 40 (allowed single-frame row), warp 40628 / 11744 / 30, buster 54672, chip-use 275307 — are the biggest existing harness rows still failing (F38's decomposition at 07ad4e3 already proved all of opening's 72499 is OBJ, so no missed layer). cursor at 3/3/170 is layout-sensitive and reported, not chased; windowclose is 0/0/40 from F37g. The two-in-a-row rule blocks T7e, so this carries the sequencer objective under the F prefix and inherits F38's "integrated rows" framing — F38's decomposition + T7d's residual is what makes the five rows reach 0. A precise negative is acceptable: if the predicates bring the sequencer to 540/540 but the integrated rows still fail, the report names the new residue per row and frame; "alignment by measured event" means we do NOT widen alignment to hide it.

**Do.**
1. Port `sub_802D6C4` (the 0x20 -> idle handler, asm03_0.s:15124-15134, returns byte_203C974 = "both players' entry machines reported done", armed by `sub_802D6A0`, cleared only on the two done edges) as a named predicate and wire it into the 0x20 -> 0x24 transition in src/battle.rs → *report `tools/trace.py --align sequencer=0x0` on battle_full: 0x20 leaves on the byte_203C974==1 edge, with the writer PC cited from the disassembly, and the trace's first sequencer divergence moves to k=43 or later.*
2. Port the [r5+2] latch (the byte `str r0,[r5]` at asm00_1.s:11030 zeroes; sub_800840C's 0x00 settle-3 arm reads it as "ready for 0x04") and `sub_801483C` (call sites at 10958 in 0x00 and 11022 in 0x24) into the 0x00 -> 0x04 transition → *report trace: 0x00 settle matches canon's 3 frames and 0x04 enters on `sub_801483C` returning 0 AND [r5+2]==4, not on `age+1>=SEQ04_FRAMES`; sequencer 0x00+0x04 divergent count drops by 60 (= the SEQ04_FRAMES window we currently fit).*
3. Port `sub_801E754` (the banner-idle check at asm00_1.s:10498-10502: `dword_20352C0 & 0x8000`, armed by `sub_8008064`'s timer setup 0x1e / 0x293 at entry) as the 0x04 -> 0x08 predicate → *report trace: 0x04 leaves on `sub_801E754 == 0` after timers 0x1e and 0x293 are armed; the k=147..206 0x04 range stays 60 frames long only because the predicate takes the same time the fitted count took — a residual timing gap is named, not hidden.*
4. Fix the three short cites T7c's verifier found: battle.rs cites asm00_1.s:10527-10537 for the 0x08 -> 0x20 write (actual 10609-10611); :10841 for the 0x24 leave (actual 10958 in 0x00 and 11022 in 0x24, not 10841 inside sub_800834A's jump table); the 0x04 test cited at 10508-10512 (covers the 0x08 write, not the bl/cmp/bne at 10500-10502) → *report `grep -n` of corrected line ranges in src/battle.rs comments and that no stale 10527-10537 / 10841 / 10508-10512 cite survives; each new citation within 2 lines of the instruction it names.*
5. Re-measure all five integrated rows + battle_full + result + windowclose + cursor → *report per row total/worst/frames before, same after; battle_full sequencer 540/540 or the remainder named per frame; result's 0x0C at k=0 still 0/0/40; windowclose still 0/0/40; cursor's 3/3/170 tear reported, not chased. A row that gets worse is reported with its numbers.*

**Rules.** No captures outside `tools/harness.py`'s existing rows (this adds **no row** — the integrated rows already exist). No edit to `tools/mgba_capture.c`, `tools/states.py`, `tools/trace.py`'s judge/align (T7b's territory), `FIXTURE.md`, `tools/harness.py` (any harness change is a separate ticket), `reference/bn6f` (read), `assets/`. No allowlist widening. No alignment change except by measured event (F2's rule). No fitted constants: byte_203C974 / dword_20367F0 / dword_20352C0 reads each carry a `// provenance:` tag with ROM address; SEQ04_FRAMES = 60 is either promoted to a ROM-sourced constant with cite or narrowed to "fit to battle_full's stored record". `docs/coverage/` notes go in this ticket, not the plan file.

**Measure and report.** rows: five integrated variants + battle_full + result + windowclose + cursor, frame by frame, total/worst/frames before and after;59-row rollup otherwise identical. frames: 540 battle_full + 40 result + 40 windowclose + 170 cursor + 30 warp + 32 buster + 40 chip-use + 40 opening + 40 field. total: per-row before/after. worst: per-row before/after. region: per-row, and per-edge per-row when the predicate's ported region matches the row's failure. commit: src/battle.rs only (the three sequencer-edge traps + three cites). Mechanism: three canon-predicate ports replace three fitted frame-counts; the stored battle_full record's 0x20/0x24/0x00/0x04 ranges now match because the predicates take the same time the counts took, not because the predicates are fudged. Unverified: that the 0x04 arm's 60-frame stay is mechanism (the two timers) not coincidence (the same fit under a predicate); that all five integrated rows close at the same fix — a residual per row is named in the report, not chased.

**Coordinator:** ~$0.20-0.25, no new row, one worktree + a measurement re-run. Dispatch first. The other two M2 candidates are weaker: T7d's residual IS this ticket (T7d's branch is the base, two-in-a-row blocks T7e), F35's BLOCKED note ("fast copy 11->1 scanline works as mechanism but fixed placement fails both ways") is independent and not on this path. Pair with T9d on disjoint files (T9d is docs/ + tools/states.py read; this is src/battle.rs) per the user's 2026-09-13 rule; verify_rows on the branch must PASS the 59-row rollup before landing. The "$0.04 per row across 5 integrated rows + battle_full = 6 rows" metric is the headline; if a row does not move, the report names why per frame.

---

### T9d. Why the poked Gunner battle never goes live: write-watch the sequencer on the working scenario and name the difference  *(OPEN -- 2026-09-14)*

**Files.** tools/states.py (only if a poke fixes it), docs/coverage/battlestart_gunner.md (corrected), docs/trace/t9d/ (new)

**Why.** T9c landed (`1f7efc9`) the `battlestart_gunner` recipe and per-slot `enemy_kind`, and the verifier CONFIRMED both: record 6 (`0x080b4bd8`) holds for all 900 frames, slot0 Mettaur panel `0x0205`/NameID `0x0001`/HP `0x0028`, slot1 NameID `0x0085`/HP `0x003C`, slot2 empty, MegaMan (2,2) HP 100, rng low half `0x0f46`. What it could not do is produce the row: the banner sequencer `dword_203CA70` (ewram.s:3040) **reads 0 for all 900 frames with no logged write at all**, the gauge is frozen at `0x00200000`, and the viruses park at state/action `0x0104`. The verifier REFUTED the causal chain this branch's coverage doc asserts (state 0 gated by `sub_801483C`, state 4 gated by `sub_801E754` ⇔ `dword_20352C0` bit `0x8000`): that bit is **already clear** in the poked battle, and the proposed unstick (sequencer=4 plus `0x02036848:4`/`0x02036840:4`) makes 4 stick for 338 frames, never writes 8, and produces no damage across 5 button presses. So "no attack event is reachable" is NOT established — what is established is that nobody has found who writes `dword_203CA70`. The cheap experiment nobody has run is the comparison against a scenario that DOES go live: the `mettaur` row's canon side is a live battle off `battlestart`, 70 frames, its negatives non-blind. Same base state, same pokes, one different roll outcome. Per SCOPE.md this is M5 (the next milestone after M2) and a port under the interpreters, never a re-creation.

**Do.**
1. `--watch-write 0x0203ca70` (and the gauge `0x020352A0`, and `0x02036848`) on the WORKING `battlestart` scenario for ~200 frames → *report the writer PC(s) that first put non-zero into the sequencer, the values and the frame, and what state the scenario was in when it happened.*
2. The same watch on `battlestart_gunner` for the same frame count → *report the diff: which of those writes is absent, and at which PC the chain stops (a write that never fires, or a branch never taken — cite the instruction line for whichever it is).*
3. Name the one thing the poked battle is missing → *report whether it is a value the roll poke left wrong (then add the poke in `tools/states.py`, with the address/width from ewram.s or the ROM label, and re-run 1 capture to show the sequencer moving and an attack event happening), or a structural property of state 6's setup bytes (`byte_80B5347`: 00 22 00 00 | 11 25 01 00 | 11 36 85 00 | F0), in which case say which byte and cite the ROM routine that reads it.*
4. Correct `docs/coverage/battlestart_gunner.md` → *the verifier said its gate-chain narrative must be fixed before it is copied forward: replace the `sub_801483C`/`sub_801E754` causal claim with what was actually measured (bit already clear, poke sticks 4 for 338 frames, no 8, no damage), and state the finding of step 3 in its place.*

**Rules.** No `src/*`, no `tools/harness.py` (this ticket adds NO row — the row is the next one if the lever is found), no `tools/mgba_capture.c`, no `tools/trace.py`, no `FIXTURE.md`, no `assets/`, `reference/bn6f` read-only. ≤5 capture runs. Every address named with its `ewram.s` line or ROM label. A precise negative is the good outcome: if the sequencer's writer cannot be localized within the budget, report which watch produced which empty log rather than inventing a gate.

**Acceptance.** `docs/trace/t9d/` holds both watch logs; the coverage doc no longer asserts the refuted chain; and either `tools/states.py` gains a poke that makes `dword_203CA70` advance (with a capture showing it, plus the frame of the first attack event, which is what the row will align on), or the report names the exact PC/byte where the poked battle's chain stops. Either way a follow-up row ticket becomes writable.

**Coordinator:** ~$0.05-0.10, 2-5 capture runs, no src change. Dispatch second. Pairs with F38c on disjoint files (F38c is src/battle.rs; T9d is docs/ + tools/states.py read). The dollar argument: this ticket does not bring an existing harness row to 0 — it enables T9 to add the Gunner row, which is M5 (the next milestone after M2) under the interpreters. The M2-first ordering of F38c ahead of this is per SCOPE.md; T9d runs in parallel with F38c on a separate worker.

---

### T13b. The two audio defects T13 measured: the ungated hit sample, and the 6.6 ms onset offset  *(OPEN -- 2026-09-14)*

**Files.** src/battle.rs (only the hit-sample gate at :3235 and any onset shift that lands), docs/audio/baseline-buster.md (three wording fixes), docs/coverage/audio-buster.md (new), reference/bn6f (read)

**Why.** T13 landed (`9d52135`) the first sample-exact audio comparison and its verifier confirmed, from the trees on disk and with zero capture runs, two defects that are mechanical facts about our side — not synthesis guesses: (a) `src/battle.rs:3235` sets `hit_in = BUSTER_HIT_DELAY` inside `Update::Strike { charged }`'s uncharged arm with **no enemy-overlap and no HP gate**, so `assets/buster_hit.wav` plays through the agb mixer on FIFO A (channel id 4) at press+10 (frames 118..130, RMS 2175.9, peak 11221 — which is our *whole-tree* peak) in a scenario that has no enemy to hit; canon's run of the same scenario shows no such onset (its frame 140 = 2181.5 sits *below* 136-139 = 2466/2519/2593/2782). It is the only `play_sound` call in all of `src`. (b) the buster blip's onset is **one frame later than canon's at frame granularity, and the verifier's mandatory qualifier is that the measured onset-to-onset distance is ~635 interleaved pairs = 6.6 ms, NOT one frame**: canon's first differing sample is pair 1119/1605 into its frame 135 (press+5, a partial frame — 486 of 3210 samples, all in L) while ours starts at pair 149/1605 of frame 114 (press+6, a full frame), so a fix that shifts ours a whole frame earlier **overshoots by ~10 ms and fails a sample-exact check it should pass**. The frame-level attribution is now measured, not inferred: the no-press canon control subtracts sample-exactly at all 130 pre-press frames plus the four poke frames, cancelling the frames 12-27 music (RMS 4234.6 / peak 8460 → difference 0.0). M9 is the next milestone after M2/M5 per SCOPE.md, and this is its first implementation ticket (T13 was measurement-only).

**Do.**
1. Gate the hit sample the way the ROM gates it → *find canon's own condition for the `SOUND_BUSTER_*` hit sound in the disassembly (the fire phase's enemy-overlap/HP test, cited `symbol:file:line`), implement that predicate on the `hit_in` write, and re-measure: our channel-4 tree in a zero-enemy fixture must go silent, and with an enemy present the event must appear where the predicate says. Report both trees' peaks, not one.*
2. Close the onset offset as a **time** difference, not a frame difference → *report the onset pair index on both sides after the change (canon 1119/1605 of its onset frame is the target's phase); state the residual in pairs and in ms at the measured 95999.1 / 95589.4 Hz, and do not claim parity at frame granularity alone.*
3. Three wording fixes in `docs/audio/baseline-buster.md`, none of which changes a number: label the 9179 as a **peak of the subtraction** on a channel where the blip *replaced* the music (canon's own peak at 136 is 8366); note that "decay to 2123 by 142" is measured against the ~3000 unattributed divergence floor the doc already disclaims; and change "now reports every mismatched frame" to what the tool does (counts all 106, lists the first 10).
4. Leave the frame-0 ramp ALONE but say what it is → *our frame 0 is short (234 pairs vs canon's 1605) AND silent (max |sample| 0 over its 234 pairs), which is why the first cross-side difference is global s16 index 2; report whether that is our boot-time audio ramp or the capture harness's own priming, with a cite, and stop there.*

**Rules.** No edit to `tools/harness.py` (this adds **no row** — audio is still not a harness verdict), `tools/mgba_capture.c`, `tools/states.py`, `tools/trace.py`, `FIXTURE.md`, `assets/`, `docs/coverage/` except the new `audio-buster.md`, `reference/bn6f` read-only. ≤8 capture runs, one at a time inside the 3-slot semaphore, and state the count honestly (T13 disclosed 13 runs against an 8 budget; a disclosed overage is fine, a quiet one is not). No fitted numbers: `BUSTER_BLIP_ENVELOPE`/`_FREQ`/`_FRAMES` may only change against a cited ROM value or a measured residue, and the `provenance:` tag must be updated in the same commit. `docs/inventory/enemies.{json,md}` are T12's generated output — regenerate with its tool, never hand-edit.

**Measure and report.** row: none — no harness row is added or changed; report the 59-row rollup unchanged. frames: the buster scenario's compared frame count, both sides. total: differing samples over total samples, and the first differing sample index + frame, after each step. worst: max |Δ| per channel, and the peak of the channel-4 tree in the zero-enemy fixture (must be 0 after step 1). region: per-channel, by channel id (0..5). commit: `src/battle.rs` (the hit-sample gate + any onset shift) + `docs/audio/baseline-buster.md` + `docs/coverage/audio-buster.md`. Mechanism: the channel-4 tree goes silent on the gated predicate (cited), and the onset residual becomes a measured ms number, not a frame number. Unverified: that the onset fix preserves every pixel in the harness (a src/ change must not move a pixel; verify_rows on the buster isolated row is the gate), and whether the frame-0 ramp is ours or the capture harness's.

**Coordinator:** ~$0.15-0.25, 4-8 capture runs, src/battle.rs change. Dispatch third. The dollar argument: this ticket does not bring an existing harness row to 0 either — audio is not yet a harness verdict (T13 explicitly adds no row) — but it advances M9 (the user's 2026-09-14 14:00 call put audio in scope) and addresses two defects the previous measurement pass named. Pair files are disjoint with F38c (F38c is src/battle.rs sequencer edges, T13b is src/battle.rs:3235 + the onset shift — same file but disjoint hunks; the pair works on a single worktree with `git add -u <path>` discipline per AGENTS.md). If F38c's verifier pulls T13b's src change into its branch by mistake, the bisect blame is the F38c worker's; T13b runs separately to keep the M2 sequencer fix and the M9 audio fix isolated.
