# Proposed tickets 20260914-183426 (minimax/MiniMax-M3, $0.1557)

Now I have enough context. Here are the three tickets I propose, ordered by how many existing harness rows each brings to 0 per dollar, all advancing M2 (the lowest-numbered unmet milestone per SCOPE.md):

---

### F38c. Close the integrated zero-enemy rows: lock the result-window slide start to the sequencer event and drive F34's driver chain  *(OPEN -- 2026-09-14)*

**Why.** With every BG layer 0 until the ramp (F33b) and inputs/objects running after the countdown (F33d), warp integrated 40628/11744/30, buster integrated 54672/12977/28, chip-use integrated 275307/18091/30 = canon's RESULT window sliding in on frames where ours has none or a different one (warp k=24..29, buster k=22..27, chip-use k=4..29 ramping at 25328/frame). F34/F34b took the result row itself to 0/0/40 with canon's driver chain (sub_802BD60 -> sub_802BE36 at 16 px/frame -> sub_802BF0C wait), so the window's content is right and what differs is WHEN and FROM WHICH state it starts on these rows. F38b NEGATIVE refuted the resolve-flag approach (warp 40628 -> 45751 with isolates regressing; sequencer 0x0C@47 unchanged on both sides; one BANNER_TO_RESULTS cannot sit at canon k=24..29 with the OBJ banner tail parked over k=0..9). The verifier's explicit next plan: "lock our slide's first frame to the same sequencer event (dword_203CA70 -> 0x0C, then the window driver's first state, watched on both sides), and compare the window's slide frame by frame against F34's driver chain (sub_802BD60 -> sub_802BE36 at 16 px/frame). chip-use's honest lock is 101 (remainder 281885 there)." field integrated 158949/5609/40 has the same slide plus a3-frame byte-identical stall (captures 118/119/120, lag 14 -> lag 11) that belongs to the post-0x0C simulation and must be re-attributed after the lock. ~600 k px of harness residue on M2's end-of-battle + every-entry-type, four rows (warp/buster/chip-use/field).
**Do.**
1. Watch both sides' dword_203CA70 (4-byte) and the window-driver state on warp integrated from the countdown to the first slide frame, identifying where ours enters the slide relative to canon's → *report the gap in frames and the exact sequencer event that gates canon's slide.*
2. Gate our slide's first frame on the same sequencer event (dword_203CA70 -> 0x0C -> window-driver first state, watched; never by score) and re-measure warp integrated per-k → *report k=0..23 unchanged 0, k=24..29 ramp drops to canon's 1917..11744 exactly, total 40628 -> 0; negative not blind.*
3. Apply the same lock to buster and chip-use integrated (chip-use's honest lock is 101; the281885 remainder after101 lives in the same driver chain) → *report each row's integrated before/after with the per-k ramp on the slide frames.*
4. On field integrated, apply the lock and re-attribute the 118-120 stall (lag 14 -> lag 11; the filler BG dropped at capture 121; results_delay 3,2,1 coincidence) → *report field's region sums (top/mid/bot/rest) and lag numbers before/after; the slide portion drops to canon's, the stall stays or is reduced.*
5. Re-measure the four rows' isolated variants, result isolated, and the full table → *report full-table deltas; nothing worse.*
**Rules.** src/results.rs and src/battle.rs (the end-sequence / results hand-off hunks only), src/banner.rs (only if the OBJ banner tail is the next blocker), tools/harness.py (the four rows' Align notes only). No alignment / allowlist / seed / scroll / hand change except by the watches above; offsets stay event-locked (warp 51, buster 103, chip-use 100, field 108), never by score.
**Measure and report.** warp / buster / chip-use / field integrated before/after (total/worst/frames) on identical event-locked windows with per-k shapes, watch traces both sides naming which post-0x0C class was locked, field stall region/lag table before/after, full-table deltas.
**Acceptance.** warp / buster / chip-use / field integrated 0 (negatives not blind) or their per-frame remainder with both sides' sequencer + window-state traces; every isolated row and the result row unchanged at 0; an allowlist entry removed only for a row that reads 0; nothing worse.
**Coordinator:** verify_rows on warp / buster / chip-use / field integrated + their isolated variants + every row the report names; the verifier on the sub_802BD60 / sub_802BE36 / sub_802BF0C citations and the watch traces.

---

### T7e. Land the sequencer's window states with a fixture that drives 0x24 -> 0x00 -> 0x04 -> 0x08 (the verifier's NEXT TICKET from T7c)  *(OPEN -- 2026-09-14)*

**Why.** T7c (PARTIAL, kept UNMERGED at wt/t7c @ 510b5c6, 2 commits 3def49e + 510b5c6) ports the chip-select window's own sequencer states from sub_800801C / off_8008038 (0x08 writes 0x20 on the window-open update; 0x20 sub_8008452 -> 0x24 on its second handler run; 0x24 sub_8008492 holds while the window is up and leaves on the Done edge; 0x00 sub_800840C settles 3 frames; 0x04 sub_8008064 waits out the banner record [SEQ04_FRAMES = 60] then writes 0x08). The branch is pixel-neutral and verify_rows PASSES on it (59 rows 0/0 with live negatives; cursor isolated shrank from main's 26/17 to 3/2; result row's sequencer is now 40/40 canon 0x0C at k=0), but the verifier REFUTED the exoneration of src/battle.rs because the mettaur/popup rows contain NO window states on either side (canon-met {0x1c,0x08} over 214 rows, canon-pop {0x1c,0x08} over 127), and the three window-OPENING fixtures (window / cursor / card, gauge 16384 + FLAG_OPEN_WINDOW) drive 0x08 k=8..132, 0x20 for 1 frame, 0x24 to the end and NEVER reach 0x00 or 0x04 -- those two arms have zero measured coverage, and the edges are frame-count fits where canon tests a subroutine (sub_800840C gates on sub_801483C + the [r5+2] latch, asm00_1.s:10947-10976; sub_8008064 writes 0x08 only when sub_801E754's banner-idle check returns 0 after arming timers 0x1e and 0x293, :10478-10514). The verifier's explicit NEXT TICKET: a window-CLOSING fixture that actually drives 0x24 -> 0x00 -> 0x04 -> 0x08 ("fixture-only is currently unfalsifiable"). Also carries two T7c citation defects to fix on the same pass: battle.rs cites asm00_1.s:10527-10537 for the 0x08 -> 0x20 write (actual 10609-10611) and :10841 for the 0x24 leave (actual sub_801483C call sites 10958 in 0x00 and 11022 in 0x24). Headline acceptance for M2 (battle_full sequencer 540/540 without re-alignment) is NOT met today: 173/540 still divergent (k=31..32 0x20, k=33..132 0x24, k=133..135 0x00, k=136..195 0x04 on canon; ours stays in 0x08 throughout). 0 px directly, but the sequencer port that M2 requires cannot land until the 0x00 / 0x04 arms have measured coverage.
**Do.**
1. Start in `bash tools/worktree.sh t7e-window-close` then `git merge wt/t7c`. Fix the two citation defects on the same pass (battle.rs:10527-10537 -> 10609-10611 for the 0x08 -> 0x20 write; battle.rs:10841 -> sub_801483C call sites 10958 in 0x00 and 11022 in 0x24). Re-run verify_rows → *report the row totals unchanged and the two cite lines now correct.*
2. Build a window-CLOSING fixture in tools/states.py (a `windowclose_full` recipe that starts in SEQ_0C and runs the Done edge) and verify on canon it drives 0x24 -> 0x00 (sub_800840C, settle 3 frames per sub_801483C + the [r5+2] latch, asm00_1.s:10947-10976) -> 0x04 (sub_8008064, SEQ04_FRAMES = 60 wait gated by sub_801E754's banner-idle check + timers 0x1e and 0x293, :10478-10514) -> 0x08 → *report each transition's frame number and the handler exit condition, both sides, with the watch on dword_203CA70 + the [r5+2] latch byte + the banner-idle return.*
3. Watch ours on the same fixture on each of the four transitions and cite which arm diverges → *report per-handler the first differing frame and the byte/word that drives it, with a citation to the corresponding arm of sub_800801C.*
4. Port only the missing predicate (gating0x00's leave on sub_801483C's latch, gating 0x04's 0x08 write on sub_801E754's return and the two timer arms) with each arm measured alone → *report per-arm deltas (the 0x00 settle length, the 0x04 banner-record wait length, the 0x08 re-entry timing) and the combined delta on battle_full sequencer.*
5. Re-run battle_full and the integrated warp / buster / chip-use / field traces (F38c's setup) → *report battle_full sequencer 540/540 or per-k remainder named frame by frame; the four integrated traces' 0x0C transition and the slide-start frame unchanged or improved; cursor isolated, windowclose isolated, every isolated row 0; nothing worse.*
**Rules.** src/battle.rs (the sequencer state hunk only), tools/states.py (new recipe), tools/trace.py (only if the trace needs a new field for sub_801483C's latch or sub_801E754's return), tools/harness.py (the integrated rows' notes). No alignment / allowlist / seed / scroll change except by the watches above; the fixture is a NEW state, not a flag on an existing row; no F38c content (this lands the sequencer, F38c drives the slide).
**Measure and report.** verify_rows on the branch (every row unchanged), the new fixture's sequencer trace on canon / ours with each transition's frame number and citation, per-arm deltas, battle_full sequencer frame-by-frame k-range table, the four integrated traces' 0x0C transition and slide-start frame, full-table deltas.
**Acceptance.** the four window-state transitions measured end-to-end on the new fixture (0x24 -> 0x00 -> 0x04 -> 0x08 all reached on canon AND ours with frame numbers and citations); battle_full sequencer 540/540 or per-k remainder with frame numbers; cursor / windowclose isolated unchanged (no regression in this pass); the integrated rows' sequencer trace unchanged on this branch (F38c drives the slide); every isolated row 0; nothing worse.
**Coordinator:** verify_rows on the full table; the verifier on the sub_800801C / sub_800840C / sub_8008064 / sub_801483C / sub_801E754 citations and the watch traces; the cite-line corrections from T7c checked.

---

### F38d. Close the opening integrated row (72499/2691/40): decompose per layer and region first, then port canon's mechanism *(OPEN -- 2026-09-14)*

**Why.** Opening integrated 72499/2691/40 has never been decomposed on its own merit; every other integrated row has at least one mechanism attributed (F33b for warp/buster/chip-use/field's BG1 seed, F34b for result, F38 for the ramp). It is the only integrated row on the table that landed an unchanged 72499 through F33d's post-0x0C landing (human session measured branch wt/f33d @ 208c158: opening 72499, warp 40628, buster 54672, chip-use 275307, field 159061 all identical to baseline, chip-use's drop from 567780 came from F12's chip-use re-cut 1a3ca80 not from F33d). F38's flagged sub-task ("opening integrated 72499/2691/40 has never been decomposed: do it per layer and region first") was deferred while F38 / F38b chased the warp / buster / chip-use / field side, and F38b's NEGATIVE ruled out a one-knob fix across the integrated set. The ticket's scope is just opening integrated. M2's "engine core on a full battle at zero divergence" — opening is one of the four battle-entry routes (with F33d's 0x0C path), and its integrated row is the only one still at full residue without a measurement pass. ~72 k px on one row.
**Do.**
1. Baseline on the current clean tree: `python3 tools/harness.py --only opening_integrated` and `tools/diffmask.py --row opening_integrated --per-layer` → *report total/worst/frames, BG0/BG1/BG2/BG3 and OBJ layer totals at every frame, and the per-region sums (top y0..63, mid y64..127, bot y128..159, rest = full-frame minus those three).*
2. Per-frame shape on the residue: BG1 alone, OBJ alone, then by region, on the event-locked window the opening row uses (its Align note in tools/harness.py) → *report the frame-by-frame layer contribution so the mechanism is pinned to a layer and a region, not "the row".*
3. Cross-check against the four decomposed integrated rows (F33b / F38): does opening integrated share any per-frame layer signature with warp / buster / chip-use / field, or is it a distinct mechanism → *report the comparison; either shared (then the fix is the same F38c lock) or distinct (then the fix is its own port, cited).*
4. Port only what the comparison and citations support; if shared, gate by the same sequencer event as F38c (with the cite); if distinct, find canon's routine for the distinct layer / region residue in reference/bn6f and cite it → *report per-arm deltas with the citations, the residue before/after by layer and region, and the full-table delta.*
5. Re-measure opening integrated, opening isolated, the four other integrated rows, and the full table → *report full-table deltas; nothing worse.*
**Rules.** src/battle.rs and src/banner.rs (only the hunks the comparison and citations name), src/results.rs (only if the distinct residue is in the result-window layer); tools/harness.py (the opening_integrated row's Align note only). No alignment / allowlist / seed / scroll / hand change except by the measured attribution in steps 2-4; offsets stay event-locked, never by score.
**Measure and report.** opening integrated before/after with per-frame layer contribution, the BG1 vs OBJ and top/mid/bot/rest splits, the cross-check table against warp / buster / chip-use / field, per-arm deltas with citations, full-table deltas.
**Acceptance.** opening integrated 0 (negative not blind) or its per-frame remainder with both sides' layer / region traces; opening isolated, the four other integrated rows and every isolated row unchanged; an allowlist entry removed only for a row that reads 0; nothing worse.
**Coordinator:** verify_rows on opening integrated + opening isolated + the four other integrated rows + every row the report names; the verifier on the cross-check table and any canon citation the distinct-mechanism branch names.
