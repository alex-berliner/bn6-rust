# Proposed tickets 20260915-121133 (hyper/qwen3.8-flash, $0.0445)

### F41a. Zero-enemy end of battle: find what edges the sequencer 0x08→0x0C on the warp/chip-use route, delete RESULTS_DELAY, open three integrated rows

**Why.** T7w NEGATIVE measured on HEAD: field integrated 158926/5597/40, warp 40628/11744/30, chip-use 275307/18091/30, buster 54672/12977/28; layer four-tuples (full/BG3-only/BG1-only/OBJ-off) field 158926/54556/842344/159187, warp 40628/40628/0/40628, chip-use 275307/44306/443520/250417 — the band is on **BG3**, not BG1 (refutes F36c's y=24..143 geometry): one new 16-px column per frame from x=0, warp 1917→11744 (+1920/f), chip-use 2090→12797 (+2090/f), field only +176/f (field's slide content is already in sync). 16 px/frame is `results::SLIDE_STEP` (src/results.rs:63 ← sub_802BE36's +2 columns, asm03_0.s:11666-11670): canon is sliding the results window in and we never draw it. T7w step 3 read the sequencer word `eBattleSequencerState_203CA70` (0x0203CA70) on each row's own capture: canon edges 0x08→0x0C at capture 47 **on every row**, slide's first tick at edge+107; ours is 0x0C from capture 8 on field (tick at entry+132, in sync under the fit) and **never leaves 0x08 on warp and chip-use**. Our show frame is `const RESULTS_DELAY: u16 = 110; // provenance: fitted` (src/battle.rs:1197, countdown :1513/:2007, read :2851-2880). tools/harness.py already ships a *resolving* zero-enemy fixture (`ZERO_ENEMY_RESOLVED = dict(ZERO_ENEMY, flags=0x31)  # 0x11 | FLAG_RESOLVE_OVER`, :1481), so the missing edge may be one fixture bit, not a state machine — and the band then needs no fitting at all.

**Files.** tools/harness.py (only the warp/chip-use/field rows' `rust=` **flags** byte and their Align notes), src/battle.rs (Sequencer's 0x08→0x0C arm; RESULTS_DELAY :1197/:1513/:2007; the show block :2851-2880), src/results.rs (the driver's start entry only), tools/allowlist.py (deleting AUDIT-6 keys only), tools/probe.py (peek/watch only), docs/coverage/integrated_end.md (notes)

**Do.**
1. `tools/harness.py --only warp --ui integrated`, then `chip-use`, then `field` → *report each row's total/worst/frames (expected 40628/11744/30, 275307/18091/30, 158926/5597/40) and the `flags` byte each row's `rust=` side carries.*
2. Read `FLAG_RESOLVE_OVER` (bit5) in src/fixture.rs and what consumes it in src/battle.rs, and compare the three rows' flags with `ZERO_ENEMY_RESOLVED`'s 0x31 → *report which rows carry the resolve bit, and the file:line of the `over` arm that consumes it.*
3. One capture per failing row, harness's own canon path, `--watch-write 0x0203CA70:4` (never a direct no-poke probe run — F40a's white-spin warning) → *report the store PC/LR that writes 0x0C and its frame, per row.*
4. Read that LR in asm00_1.s (sub_800801C :10422 + table off_8008038; sub_80081A4 :10617) and port the arm so our sequencer edges 0x08→0x0C on the same event, RESULTS_DELAY deleted not retuned → *report the diff, the cites, the fitted-constant count before/after (HEAD 19).*
5. Re-run the three rows on identical windows → *report per row total/worst/frames before/after and the first six band frames' per-frame totals.*
6. `tools/verify_rows.py` in the `row:ui=` expect form from a clean detached checkout → *report the 67-row table, cursor 1/1/170, mettaur 0/0/70, the 43 chip rows 0/0/30, and which AUDIT-6 keys were deleted.*

**Rules.** Only the named files. No change to canon_ref/frames/search/pokes/enemy data/descriptor; FIXTURE_SIZE stays 67 (F38f's descriptor growth cost a cursor regression). Every frame count comes from a step-3 reading plus an asm cite. If arming the resolve bit shifts a row's aligned window, report it — do not re-search. If the band is not the results window, report NEGATIVE with the readings and change no src/. Fitted count must not rise; canon never changes; ≤4 capture runs, one at a time; tool budget ≤100.

**Acceptance.** warp:integrated and chip-use:integrated read 0/0/30 (field 0/0/40) with negatives non-blind and their AUDIT-6 keys deleted; RESULTS_DELAY absent from src/battle.rs (fitted ≤18); the two sides' 0x08→0x0C edge and first-tick frames equal per row; nothing worse on cursor (1/1/170), mettaur 0/0/70, opening:integrated 25829/931/40, buster:integrated 0/0/28, the 43 chip rows 0/0/30. A NEGATIVE naming the store PC and the routine that draws the band also closes the ticket.

**Measure and report.** rows: warp, chip-use, field, buster (integrated), cursor, mettaur, opening, the chip rows. frames: 30/30/40/28/170/70/40/30. total/worst before/after per row plus the layer four-tuple; per row the edge frame and edge+107. region: band x=0..15 y=24..143 on BG3. commit. one line of mechanism (which routine edges the sequencer to 0x0C on the enemy-less route). one line of what is unverified.

**Coordinator:** dispatch first — three rows, three allowlist keys, one fitted constant, and steps 1-2 are free and decide whether this is a flag or a state machine (T7w's own leftover question). Worker = the run profile's resolved worker (F36c's class ran $0.134); verifier = the other family, on the step-3 store readings. ≤$0.40 expected, ≤$0.80 cap, tool budget ≤100, verify_rows on the full table plus the allowlist count. Advances **M2** (integrated pixel parity, the sequencer's end states) and the no-allowlist invariant.

---

### F42a. gunner 2105613: canon's battle-HUD element mask — the bar and enemy HP boxes are hidden while the banner owns the screen

**Why.** F40a BLOCKED (no src change) but its step 1, read off T9l's existing capture pair at zero capture cost, narrowed gunner 2105613/38237/130 (negative not blind 2284867; four-tuple full/BG3-only/BG1-only/OBJ-off 2105613/1426551/230400/1949547): canon 0..70 white; 71..85 the BG3 HUD is drawn (gauge bar + both enemy HP boxes); compared k=6..75 = canon 86..155 canon draws **neither** while rust draws both — "a DRAW-HIDE difference, not a tile choice"; canon 156..165 the chip window slides in at 1440 px/frame, complete at 165, which ours already matches (src/battle.rs:1995-2006). F40a's blocked step 2 used a direct no-poke probe capture that white-spun all 201 frames, so its counter zeros were an artifact; its retry note says read counters from the **harness's** canon capture of this row — whose bins do show 0x0203ca70 → 0x04000000 at frame 166, i.e. the fight only starts once the window is up, so canon 86..165 is banner-wait (that word: 0 = banner-wait, 4 = fight). And canon's HUD is per-element: `HudElementMask` = eStruct2035280+0x40 with setBattleHudElements_801BECC (asm00_2.s:25566), clearBattleHudElements_801BED6 (:25575), updateBattleHudElements_801BEE0 (:25584, table off_801BF04), drawBattleHudElements_801BF64 (:25634, table BattleHudDrawHandlers_801BF88 :25656 — element 4 = drawCustGauge_801C4E4 :26395, element 6 = drawQueuedChipName_801C6EE). Ours has no mask: `hud_live: bool` (src/battle.rs:1426, armed :2122, cleared :2579) exporting two peeked constants at :1762 (`0x4497` live / `0x0084` torn down). F39a found the same hide on opening's OBJ enemy-HP digits ("canon draws no HP digits through its whole intro", asm00_2.s:16120-16156) and put the real gate in src/battle.rs, outside its own Files.

**Files.** src/battle.rs (the mask field replacing `hud_live`, its set/clear sites, the :1762 export, the gates at :2382/:4562/:4607), src/hudtiles.rs (only the gauge-bar and enemy-HP-box draw gate), src/hud.rs (only the enemy HP digit emission at :133-135), tools/harness.py (GUNNER_ROW's Align note only — no flags/canon_ref/search/frames/seeds), docs/coverage/gunner.md, docs/coverage/opening_integrated.md (notes)

**Do.**
1. `tools/harness.py --only gunner` on HEAD → *report both variants (expected 2105613/38237/130, negative 2284867) and the layer four-tuple.*
2. Free: read mask `0x020352C0` per frame from the harness's own canon capture of this row (T9l's bins, else one harness capture with `--watch 0x20352C0:4`) → *report canon's mask series at frames 71..90 and 150..170 with the frame each bit changes, against ours (constant 0x4497) at the same frames.*
3. Match `BattleHudDrawHandlers_801BF88`'s element list (asm00_2.s:25656+) to the two pixel rectangles (bar y152 x12..60, HP boxes y0..15 x100..190) → *report element number + updater/draw routine file:line per rectangle.*
4. Port the mask: named bits, set/cleared at the sites step 2 names, both rectangles' draws gated on their bit → *report the diff, the cite count, the fitted-constant delta (HEAD 19).*
5. Re-run the row plus the four-tuple, then `--only opening --ui integrated` and `--only field --ui integrated` → *report all three before/after.*
6. `tools/verify_rows.py` (`row:ui=` form) → *report the 67-row table: cursor 1/1/170, mettaur 0/0/70, windowclose 0/0/40, the 43 chip rows 0/0/30, gunner's negative still non-blind.*

**Rules.** Only the named files. A rectangle's absence must follow a bit set/cleared at a cited call site — no per-frame, per-x or frame-number special case. No change to gunner's canon_ref=80/search/frames/ui="both"/enemy_kind/enemy_hp/flags or T9l's four derived backdrop seeds. Readings come from the harness's canon capture path, never a direct probe run. Fitted count ≤19; every new literal tagged `// provenance:`; canon never changes; ≤3 capture runs; tool budget ≤100.

**Acceptance.** gunner's BG3-only drops from 1426551 by ≥50% and the row's total to ≤700 000 on both variants, with both element bits and their set/clear sites cited; opening:integrated and field:integrated unchanged or better; cursor 1/1/170; mettaur 0/0/70; chip rows 0/0/30; no allowlist change; fitted ≤19. Or a NEGATIVE carrying canon's full 71..170 mask series and the element numbers, which retires the gauge-seed and tile-choice levers with data.

**Measure and report.** rows: gunner (both), opening (integrated), field (integrated), cursor, mettaur, windowclose, the chip rows. frames: 130/40/40/170/70/40/30. total/worst before/after per row, the four-tuple, canon's mask series with bit-change frames. region: y152 x12..60 and y0..15 x100..190. commit. one line of mechanism. one line of what is unverified.

**Coordinator:** dispatch second. Worker = the run profile's resolved worker (T9l's class landed at $0.58; F40a burned $0.28 on the wrong capture path); verifier = the other family, on the step-2 mask series and step-3 element numbers. ≤$0.45 expected, ≤$0.90 cap, tool budget ≤100. Steps 2-3 cost one existing capture plus a table read. Advances **M5** (the Gunner is T9j's ported per-type routine and this row is its gate) and **M2**'s every-entry-type clause; **M8**'s HP displays inherit the mask.

---

### T7x. battle_full SEQ_04's leave predicate is a bit test: port isBannerBusy_801E754, delete SEQ04_FRAMES and BANNER_FRAMES

**Why.** battle_full's trace diverges on 173/540 sequencer frames — 165 at k=31..195 (window setup) + 8 at k=297..304 (kill timing) (tools/harness.py:1484). T7u BLOCKED ($0.395): it replaced the fitted count with a `banner_idle(&BannerComposite)` check, got 273/540 → 256/540 and regressed cursor 1/1/170 → 38/37/170; not landed. Its diagnosis — "banner_at 30-frame countdown defeats banner_idle before the Banner struct spawns" — says the predicate was wrong in *shape*, not in kind: canon's is not about a banner object, it is one bit test. src/battle.rs:4550-4560 already holds the measured pattern from the real ROM (PAUSED, enemy HP forced 0, Start@10): teardown store at=0x0801BEDC lr=0x080081B9, mask 0x4497→0x0084 at frame 48, "bit 15 is set back a moment later by the ENEMY DELETED banner going up (sub_801E792's sub_801BECC(1<<15), asm00_2.s:31055-31112)", the mask reads 0x8084 (bit 15 set) across 48..105 and 0x0084 from 106. bn6f renamed the reader for exactly this: `isBannerBusy_801E754` = `HudElementMask & 0x8000` (asm00_2.s:31071-31097), with T7d's note that sequencer state 0x04 "writes 0x08 only when it returns 0" (reference/bn6f/docs/renames.md:136). Ours carries two fitted counts instead: `SEQ04_FRAMES: u16 = 60` (src/battle.rs:1186, leave at :3301) and `BANNER_FRAMES: u16 = 58 // provenance: peeked` (:1161).

**Files.** src/battle.rs (bit 15 of the HUD mask, SEQ_04's leave :3301, SEQ04_FRAMES :1186, BANNER_FRAMES :1161, the banner arm :2556/:2579 — nothing else), src/banner.rs (only the record's own completion that clears the bit), tools/trace.py (watch only), tools/probe.py (watch only), docs/coverage/battle_full.md (notes)

**Do.**
1. `tools/trace.py record/diff --align row:battle_full` on HEAD → *report the sequencer divergence (expected 173/540) and our k-spans for SEQ_08/20/24/00/04/08.*
2. Read isBannerBusy_801E754 (asm00_2.s:31071-31097), spawnBannerRecord_801E792 (:31055-31112) and clearBattleHudElements_801BED6 (:25575) and their callers → *report the site that sets bit 15, the one that clears it, and the counter/timer the clear waits on (T7d's 0x1e / 0x293 arms named).*
3. After the change, first the cheap gate: `tools/harness.py --only cursor --ui isolated` → *report cursor before/after; if it moves off 1/1/170 stop, report NEGATIVE with both sides' mask series (T7u's failure mode).*
4. Port: set bit 15 at the banner-spawn site, clear it at step 2's cited site, make SEQ_04's leave `!mask & BANNER_BUSY`, delete SEQ04_FRAMES and BANNER_FRAMES → *report the diff, the cite count, fitted-constant count before/after (HEAD 19, target ≤17).*
5. Re-run step 1 → *report the new divergence count and SEQ_04's enter/leave k on both sides.*
6. `tools/verify_rows.py` (`row:ui=` form) → *report the 67-row table, that the mask export at :1762 now comes from the ported bitset, and that no other row's mask series changed.*

**Rules.** Only the named files. The leave predicate is a bit test on a ported mask — never a frame count, never `banner_age >= N`, never a per-row case; SEQ04_FRAMES and BANNER_FRAMES are deleted, not retuned. If bit 15's clear site is not a ported routine, report the reading and keep the constant (NEGATIVE). No new harness row, no alignment/descriptor/flag change (FIXTURE_SIZE 67); fitted count must not rise; canon never changes; ≤3 capture runs; tool budget ≤100.

**Acceptance.** battle_full sequencer divergence ≤20/540 (from 173) with SEQ_04 entering and leaving on canon's k; both frame constants gone; isBannerBusy_801E754 cited at asm00_2.s:31071 in src/battle.rs; cursor still 1/1/170; mettaur 0/0/70; windowclose 0/0/40; every isolated pixel row 0/0; no allowlist change; fitted ≤17. A NEGATIVE with both sides' mask series and the sequencer edges also closes it.

**Measure and report.** rows: battle_full (trace), cursor, windowclose, mettaur, result, field, wave, popup, the chip rows. frames: 540/170/40/70/40/40/90/80/30. total/worst: divergence before/after, SEQ_04's span, both sides' mask words at 40..115. region: self.seq.state k=31..195. commit. one line of mechanism. one line of what is unverified (whether the 8 kill-timing frames at k=297..304 also shift, and whether bit 15's clear is the same event that ends opening's ENEMY DELETED banner).

**Coordinator:** dispatch third — it builds on F42a's mask field; if F42a has not landed, T7x adds the bit itself and the two must land in dispatch order with verify_rows re-run on main after the second merge. Worker = the run profile's resolved worker (T7u's class $0.395); verifier = the other family, on the step-2 clear-site cite. ≤$0.35 expected, ≤$0.70 cap, tool budget ≤100. Advances **M2** (165 of battle_full's 173 diverging frames) and the no-fitted-constants invariant.
