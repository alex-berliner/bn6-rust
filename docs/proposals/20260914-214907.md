# Proposed tickets 20260914-214907 (minimax/MiniMax-M3, $0.3145)

I have enough context. Here are the three tickets, ordered by the user's priority (M2 first, fresh chains so the "two-in-a-row" pause rules aren't violated):

---

### T14. M2 PA recognition: first Program Advance under the ported dispatcher as data  *(OPEN -- 2026-09-14)*

**Why.** SCOPE.md M2 lists "chip selection rules (codes, Regular, Tag, **PA recognition**, folder draw/reshuffle)" as required and zero of the 63 PA records (off_802BCB0 = 43 pointers, off_802BC60 = 20 pointers, with `.word NULL` terminator at asm/asm03_0.s:11546, name `off_802BCB0`/`off_802BC60` per `docs/inventory/program_advances.json`) are ported or recorded. The interpreter work that this gates on is already in place: T8 (script VMs) landed `752146e`, T6 (Mettaur per-type entry) replaced the hand-written brain with `MettaurEntry ForMettaur_8109EF4`, so a chip/PA recipe can become data the way the Mettaur is. The dispatch is canon's sub_8029520 at `reference/bn6f/asm/asm03_0.s:6640` (it picks `off_802BC60` when `[r7,#0x1c]` is non-zero, otherwise `off_802BCB0`; the loop at `:6656` walks the recipe list, jumps via `off_80295C0[matcher]` to `sub_80295C8` for strict code match or `sub_802961A` for a different matcher, then calls `sub_8029652` on hit and `sub_802963C` for one of the post-hit checks). Picking the smallest non-empty record closes the round trip: `off_802BC60[0]` = `[count=3][matcher=0][result=0x140][01/00][01/00][0c/00]` is Cannon + Cannon + Cannon -> HiCannon (chip-id 0x140, the third record of `off_802BC60` at `asm03_0.s:11497`). 0 harness rows for PAs exist; this ticket adds one (call it `pa_cannon3`), wires the dispatch into the chip-window's submission path, and proves the HiCannon recipe fires against the existing PA gate.

**Files.** src/chip_select.rs (new — the table-driven dispatcher), src/chips.rs (chip-id -> name; HiCannon = 0x140 already covered by F12's 43-chip corpus), src/deck.rs (only where the deck exposes the three slots the PA matcher walks), src/battle.rs (the chip-window's "fire chip" submission path's hook for PA detection), tools/states.py (one new scenario: a chip-window scenario with three Cannons in slots 0/1/2 and the PA gate primed), tools/harness.py (one new Check `pa_cannon3` aligned by the HiCannon's first differing pixel), docs/coverage/pa_recognition.md (new), `reference/bn6f` read-only

**Do, in order.**
1. Baseline `python3 tools/harness.py --list` and confirm `pa_cannon3` does not exist yet; confirm `cannon` isolated is `0/0/40` (F7's gate, the regression baseline); read `sub_8029520`/`sub_80295C8`/`sub_802961A`/`sub_802963C`/`sub_8029652` and write the dispatch shape (recipe table pointers, matcher dispatch table `off_80295C0`, per-record `[count][matcher][result][chip,code]*n` layout) into `docs/coverage/pa_recognition.md` with `file:line` cites for each routine — *report the citation table and confirm no recipe is referenced outside the two tables.*
2. Implement `src/chip_select.rs` as a data table mirroring `off_802BCB0` and `off_802BC60` (start with only `off_802BC60[0]`; tag the rest `// unrecorded: pending later ticket`); the dispatcher takes a deck slice and returns the matched recipe's result chip id or None; the match function is the strict matcher (`sub_80295C8`'s loop on `[r7,r6] cmp r1` with `r2` iterations) — *report the function signature, the data table's first record with its `provenance:` tag pointing at `asm03_0.s:11497 byte_802BB98`, and a unit-style test on three Cannon codes -> 0x140.*
3. Add `tools/states.py`'s `pa_cannon3_canon` scenario (chip-window opens, deck has three Cannons, the window is submitted with the PA recipe primed via the existing chip-window pokes) and the matching `pa_cannon3_rust` Side; add `tools/harness.py` Check `pa_cannon3` aligned by the first differing pixel of HiCannon's sprite — *report the new row's canon and ours first-frame HiCannon values with the alignment offset and the diffmask region (HiCannon sprite is 32x32 in `assets/`, HiCannon's tile starts at row 8/column 10 of the chip window's custom layer per F5b's coords).*
4. Run the full table from a clean detached checkout and report — *report `pa_cannon3` total/worst/frames, every existing isolated row still `0` (43 chips, wave, window, opening, chip-cannon, field, banner, warp, card, tiles, gauge, mettaur, popup, buster, result, windowclose), `cursor`'s `3/3/170` tear reported as it moves, and `battle_full`'s sequencer frame count unchanged from the `540/540` of T7e.*

**Rules.** Only the named files; no allowlist, fixture, descriptor, alignment or fixture-scope change outside the new scenario and its row; the data table starts with one record (off_802BC60[0]); every literal gets a `// provenance:` tag with `asm03_0.s:file:line` or `// canon:` for an address; `fitted constants in src/` (HEAD: 19) may not increase; assets/ read-only; cannon never changes; ≤6 capture runs, one at a time inside the 3-slot semaphore; the existing chip rows must not move (every chip family at 0/0 as F12 reports).

**Acceptance.** `pa_cannon3` row reads `0/0` with a negative not blind (no other chip scenario fires HiCannon); the full `tools/harness.py --list` table printed is identical to HEAD except `cursor`'s reported tear and the new `pa_cannon3` row; the dispatcher's data table has one record with its `provenance:` tag; `docs/coverage/pa_recognition.md` lists the five cited routines with `file:line`; `fitted constants` unchanged; `battle_full` sequencer still 540/540 unaligned.

**Measure and report.** row: `pa_cannon3` (new) + the existing 67-row regression set (43 chips + 24 isolated/integrated). frames: 40 pa_cannon3 (canon HiCannon's first frame through its 4-frame hold), 40 chip-cannon, 70 mettaur, 170 cursor. total/worst: `pa_cannon3` total pixels of HiCannon sprite (32x32 = 1024 max), worst case all on a single frame; the regression set's 0-rows report `0/0` verbatim, any movement reported as a number with its diffmask region. region: HiCannon's sprite bbox on the first frame it differs and the alignment offset. commit: `src/chip_select.rs` + `src/deck.rs` + `src/battle.rs` (chip-window submission hook) + `tools/states.py` + `tools/harness.py` + `docs/coverage/pa_recognition.md`. One line of mechanism (data-table dispatcher walking off_802BC60[0]'s `[01/00][01/00][0c/00]`); one line of what is unverified (the second matcher `sub_802961A`'s call sites — only `off_802BC60[0]` uses strict match so the second matcher is not exercised by this ticket).

**Coordinator:** dispatch first. M2's chip-selection sub-area has zero tickets; this opens it with the cheapest PA and proves the table-driven shape under the interpreters T8 landed. Worker muse-spark-1.3-contributor (T6's child class — the Mettaur-as-data port set the pattern), verifier GLM-5.3-flash cross-family; ≤$0.20 expected, ≤$0.50 cap; verify_rows on the full table plus the new row. Advances **M2** (and enables M4 Program Advances).

---

### T15. M2 end-of-battle rank and rewards: port the rank calculation as data under canon's result dispatcher  *(OPEN -- 2026-09-14)*

**Why.** SCOPE.md M2's clause "end of battle with rank and rewards" is the only M2 sub-area besides chip selection rules and every entry type that has zero tickets in the queue. The result screen UI is closed — `result` isolated reads `0/0/40` (F34b landed `bb8e8f0`, the intro-fade removal; the earlier F21d landed `90dd655`, the block-copy tile writes; F21b slide rework on `wt/f21b-result-tail`) — but the rank the player sees on that screen is computed by the engine and is currently whatever Rust emits by default. F32 landed the end-sequence timing (killing-blow → 0x0C in 35 updates, the 47-frame cadence) but did not touch the rank payload, and no `**Do**` in the closed F-tickets names it. The rank routine lives in the same dispatch family as `sub_802BD60` (referenced as the result-screen driver in F21d's write-up, `reference/bn6f/asm/asm00_2.s:11549`), and the rank/reward records sit beside the result-screen data the same way PA recipes sit beside the chip dispatcher (compare SCOPE.md's M1 PA row "FOUND: asm/asm03_0.s off_802BCB0 + off_802BC60" with the rank table, which the existing `tools/inventory.py` regeneration did not enumerate — T10 ran a measured ROM-table pass and the rank/reward table would be the next MISSING line in M1). F34's verifier verdict already confirmed the result-side driver chain (`sub_802BD60` family); what this ticket adds is the engine-side computation that runs before the result screen renders.

**Files.** src/results.rs (only the rank calc payload; the UI path stays as F34b left it), src/battle.rs (the engine-side end hook that fires once at `over` -> `0x0C` k=47 and writes the rank record), tools/inventory.py (the rank-record enumeration; regen `docs/inventory/ranks.json` and `docs/SCOPE.md`'s M1 ranks row from the ROM), tools/states.py (the `rank_full` scenario: a battle that wins in k=200 with full HP and the rank held at a known value), tools/harness.py (one new Check `rank_full` aligned by the rank letter's first differing pixel on the result screen), docs/inventory/ranks.json (new), docs/coverage/rank_rewards.md (new), `reference/bn6f` read-only

**Do, in order.**
1. Baseline `python3 tools/harness.py --list` and confirm `rank_full` does not exist yet; confirm `result` isolated is `0/0/40`; walk `reference/bn6f/asm/asm00_2.s:11549` and the call sites of `sub_802BD60` to find the rank-and-rewards calc — *report the routine's `file:line`, its inputs (HP delta, hits taken, custom counter at end, time-elapsed), its outputs (the rank letter `S/A/B/C/...` and the reward triple `zenny, bugfrag, bonus`), and the data table the rank letter reads from (the rank table's name and bytes).*
2. Extend `tools/inventory.py` to enumerate the rank record table and regenerate `docs/inventory/ranks.json` and `docs/SCOPE.md`'s M1 line; do not hand-edit — *report the rank count, the table pointer `file:line`, and the per-record layout (rank-letter, zenny, bugfrag, bonus threshold).*
3. Port the rank calc under the engine: a function that takes the four inputs above and returns the rank record from the inventory table; wire it into `src/battle.rs`'s end hook at the same point F32's `over` -> `0x0C` cadence arms — *report the function's `provenance:` tag with the canonical `file:line`, the dispatch cite from step 1, and a unit-style test on the rank table's boundary records (the threshold cases that flip the letter).*
4. Add `tools/states.py`'s `rank_full` scenario (battle wins at k=200 with HP 100% so the rank is the highest letter) and a new `tools/harness.py` Check `rank_full` aligned by the rank letter's first differing pixel on the result screen (the rank is drawn at a fixed tile in the result window per F21d's coords) — *report the row's canon and ours first-frame rank letter, the alignment offset, and the diffmask region.*

**Rules.** Only the named files; no allowlist, fixture (other than `rank_full`'s recipe), descriptor, alignment or fixture-scope change; `tools/inventory.py` changes are the M1 regen of `docs/inventory/ranks.json` + `docs/SCOPE.md`'s M1 line (T10's protocol); every literal gets a `// provenance:` tag with the canonical `file:line`; `fitted constants in src/` (HEAD: 19) may not increase; assets/ read-only; canon never changes; ≤6 capture runs, one at a time inside the 3-slot semaphore; the existing `result` row must stay `0/0/40`.

**Acceptance.** `rank_full` row reads `0/0` with a negative not blind; the full table is identical to HEAD except the new `rank_full` row and any movement of `cursor`'s reported tear; `tools/inventory.py` regenerates `docs/inventory/ranks.json` + `docs/SCOPE.md`'s M1 ranks row byte-stable against the check-in; `docs/coverage/rank_rewards.md` lists the routine, its inputs, and the rank table with citations; `fitted constants` unchanged.

**Measure and report.** row: `rank_full` (new) + the 67-row regression set. frames: 40 rank_full (the 40-frame result window), 40 result (regression), 170 cursor. total/worst: `rank_full` total differing pixels of the rank-letter sprite (rank letter is 1 tile at a fixed position; max ~64 px) and the regression set's `0`-rows verbatim. region: the rank-letter tile's bbox on the first differing frame, with the alignment offset. commit: `src/results.rs` + `src/battle.rs` + `tools/inventory.py` + `tools/states.py` + `tools/harness.py` + `docs/inventory/ranks.json` + `docs/coverage/rank_rewards.md`. One line of mechanism (data-table rank calc dispatched off `sub_802BD60`'s call at the end-hook); one line of what is unverified (the bonus-threshold sub-table — only the rank letter and zenny are exercised by `rank_full`'s single full-HP win).

**Coordinator:** dispatch second, after T14 lands (so the rank row sits on top of T14's PA mechanism if the reward path crosses it, but the `**Files.**` are disjoint — T14 is `src/chip_select.rs + src/chips.rs + src/deck.rs + src/battle.rs + tools/states.py + tools/harness.py + docs/coverage/pa_recognition.md`, this ticket is `src/results.rs + src/battle.rs + tools/inventory.py + tools/states.py + tools/harness.py + docs/inventory/ranks.json + docs/coverage/rank_rewards.md`; `src/battle.rs` and `tools/states.py` overlap, so T15 runs alone after T14, never paired). Worker muse-spark-1.3-contributor (F34b's child class — the result-row closure set the pattern), verifier GLM-5.3-flash cross-family; ≤$0.25 expected, ≤$0.60 cap; verify_rows on the full table plus the new row. Advances **M2** (and back-fills the M1 rank/reward row of `tools/inventory.py`).

---

### T16. M2 every entry type: the buster-only battle start (no chips, no PA gate)  *(OPEN -- 2026-09-14)*

**Why.** SCOPE.md M2's clause "every entry type" covers the way the battle is *entered*: a warp encounter, a chip-use encounter, a buster-only encounter, the post-results-screen resume, etc. — all of which start in different states (battle 47..battle 109 of `tools/harness.py`'s scenario family) and feed different sequencer arms. F33d PARTIAL covered the post-0x0C split (verifier CONFIRMED citations for `sub_800801C:10422`, `off_8008038`, `sub_80081A4:10617`, `0x0C@0x0800811E`; warp 40628/11744/30, buster 54672/12977/28, chip-use 275307/18091/30 — none of which moved because the work was a docs-only split on `wt/f33d 7cd1f5c`). The buster-only entry type is the one where MegaMan enters the battle without a chip selection window at all (the controller parks at the buster pose, AIData has no chip data queued, the sequencer skips 0x08 and goes 0x04 → 0x0C through a different `sub_8008064` predicate than the chip-fire one). It is the only M2 entry-type route where the cannon and chip-use residual patterns are absent by construction, so it isolates the engine's "pure engine, no UI" entry from the chip window's interference — and it is what F31b's row re-cut was implicitly reaching for when it changed the buster row's canon side from "idle" to "fires" (`src/battle.rs:3235`'s `BUSTER_HIT_DELAY`, T13b's gated hit sample). No ticket has targeted this entry type yet, and the buster scenario in the harness is currently integrated (the F33/F33b backdrop+gate work hit `warp/buster/chip-use integrated` together). A clean buster-only fixture is the cheapest way to prove the sequencer's non-chip-window arm.

**Files.** src/battle.rs (the sequencer's non-chip-window branch only, leaving T7's chip-window arms alone), src/fixture.rs (the buster-only fixture's `enemy_kind` byte and the `start_state` for the buster route), src/actor.rs (only if MegaMan's buster pose predicate at sequencer entry needs an arm), tools/states.py (the new `buster_only` scenario: a battle with no chip window, AIData parked at A-only, sequencer expected to skip 0x08), tools/harness.py (one new Check `buster_only` aligned by the buster-hit sprite's first differing pixel, replacing the integrated buster row's buster-only arm), docs/coverage/every_entry_type.md (new), `reference/bn6f` read-only

**Do, in order.**
1. Baseline `python3 tools/harness.py --list` and confirm `buster_only` does not exist yet; confirm `buster` integrated is non-zero (F33b PARTIAL `642770` on HEAD); confirm `battle_full` sequencer is still `540/540` unaligned (T7e); confirm `cursor`'s `3/3/170` tear — *report all four numbers and the cannon-side scenario id of the current `buster` row (F31b's re-cut).*
2. Walk canon's buster-only entry on a clean `buster_only` capture (no chips in deck, AIData primed for buster): `dword_203CA70` (`ewram.s:3040`) per-frame, `eBattleState.Index_01` (`0x02034881`, `ewram.s:2572`), MegaMan's BattleObject `0x0203a9b8` CurAction + timer, and the sequencer's path through `sub_8008064` to confirm it skips 0x08 and goes `0x04 → 0x0C` directly — *report the sequencer trace and the predicate `file:line` that gates the buster-only arm.*
3. Implement `tools/states.py`'s `buster_only` scenario (the buster-only fixture + the sequencer's expected path) and add `tools/harness.py` Check `buster_only` aligned by the buster-hit sprite's first differing pixel; on the rust side, wire `src/battle.rs` so the sequencer's non-chip-window branch matches canon's path (the predicate that gates it must be cited, not fitted) — *report the row's canon and ours first-frame buster-hit values, the alignment offset, and the diffmask region; the predicate citation `file:line`; the integrated `buster` row's measured movement (if any).*
4. Re-run `verify_rows.py` from a clean detached checkout and report — *report `buster_only` total/worst/frames, the regression set's `0`-rows verbatim, `cursor`'s tear reported as it moves, `battle_full` sequencer still `540/540`, and the integrated `buster` row's delta from the F33b `642770` baseline.*

**Rules.** Only the named files; no allowlist, fixture (other than `buster_only`'s recipe), descriptor, alignment or fixture-scope change outside `buster_only` and its row; the chip-window sequencer arms stay as T7e/T7f/T7j left them (do not touch SEQ04_FRAMES=60 or the fitted edges T7j is modelling); every literal gets a `// provenance:` tag with the canonical `file:line`; `fitted constants in src/` (HEAD: 19) may not increase; assets/ read-only; canon never changes; ≤6 capture runs, one at a time inside the 3-slot semaphore; `result`, `mettaur`, `wave`, `popup`, `field`, `windowclose`, the 43 chip rows, and `cannon` must not move.

**Acceptance.** `buster_only` row reads `0/0` with a negative not blind (no cannon/chip scenario fires it); the full `tools/harness.py --list` table is identical to HEAD except the new `buster_only` row and any movement of `cursor`'s reported tear; the regression set (43 chips + 23 isolated rows + `battle_full` sequencer) all stay at their HEAD numbers; the predicate gating the buster-only arm is cited with `file:line` and a `provenance:` tag in the same commit; `fitted constants` unchanged; `battle_full` sequencer still `540/540` unaligned; the integrated `buster` row does not move (≤0, with the move reported if it does).

**Measure and report.** row: `buster_only` (new) + the 67-row regression set. frames: 32 buster_only (canon buster-hit sprite's 4 frames held across F31b's measured 32-frame window), 540 battle_full sequencer, 170 cursor. total/worst: `buster_only` total differing pixels of the buster-hit sprite (32x32 ≈ 1024 px max, 4 frames), the regression set's `0`-rows verbatim; integrated `buster` total compared to F33b's `642770` baseline. region: buster-hit sprite bbox on the first differing frame, with the alignment offset. commit: `src/battle.rs` (sequencer non-chip-window arm only) + `src/fixture.rs` + `tools/states.py` + `tools/harness.py` + `docs/coverage/every_entry_type.md`. One line of mechanism (the predicate `file:line` that gates the buster-only entry to `0x04 → 0x0C` without passing through `0x08`); one line of what is unverified (the chip-use entry type — `buster_only` exercises the buster-only arm but the chip-use arm is what `chip-use integrated` measures and that one is F33d's).

**Coordinator:** dispatch third, after T14 and T15 land. M2 entry types are the second-least-explored sub-area; this ticket opens the buster-only arm so future entry-type tickets (warp-first, chip-use, post-0x0C continuation past F33d) can each target a single arm rather than re-measuring the integrated blob. Files are disjoint from T15's results/inventory set (`src/battle.rs` overlaps T15's `src/battle.rs` end-hook but the touched ranges are different — T15 is `over`/`0x0C`, this is the sequencer's pre-`0x04` arm — so T16 runs alone after T15). Worker muse-spark-1.3-contributor (T6's child class — Mettaur-as-data set the per-arm pattern that buster-only mirrors), verifier GLM-5.3-flash cross-family; ≤$0.25 expected, ≤$0.60 cap; verify_rows on the full table plus the new row. Advances **M2**.
