# Proposed tickets 20260917-060906 (minimax/MiniMax-M3, $0.1815)

Now I have enough context to write three fresh tickets. Let me draft them.

### T104. M7 NaviCust battle effects: first NCP port — pick an `SetCurPETNaviStatsByte` handler and verify its stat delta on a recorded scenario *(OPEN -- 2026-09-17)*

**Why.** SCOPE M7 reads 0/19 NaviCust battle effects; M1 NaviCust row confirms the handler table `navicust_jt_NCPs` at asm/asm37_0.s:2111 (47 words stride 4, 45 `navicust_NCP_*` + `navicust_GigFldr1` + a no-op stub; dispatched by `applyNavicustPrograms_813C684` at asm/asm37_0.s:2012). 32 of the 47 are `SetCurPETNaviStatsByte` writes, 11 are `GetCurPETNaviStatsByte` reads (asm37_0.s:2161-2600); the give/take chain is `GiveNaviCustPrograms` (asm03_1_1.s:8794) → `GiveItem 803cd98` → `reloadCurNaviStatBoosts_813c3ac` → `applyNaviStatsMaybe_813C458`. **No ticket has touched this surface** (grep TODO.md + TODO_ARCHIVE.md for "navicust" / "NCP" returns only T10/T15 inventory cites). The first port is a +Atk NCP: pick one of the `SetCurPETNaviStatsByte` arms, give the NCP pre-battle in a state recipe, fire Cannon, and compare the enemy's taken damage on both sides (the stat delta travels through the chip's damage path). The scenario machinery exists: T20's scripted-input framework + T80's State.patches + the mettaur row's fixture; only NCP state needs a poke.

**Files.** `src/battle.rs` (the chip-damage calc that reads the +Atk byte, T57 step-name it; ONE arm only), `src/fixture.rs` (descriptor NCP field, `// provenance: peeked` + file:line for the NCP id + its handler), `tools/states.py` (ONE new scenario `ncp_atk_full` = `battlestart.state` + poke `60:<NCP_addr>:0xNN` + Cannon in the picked slot), `tools/harness.py` (ONE new row `ncp_atk_dmg`: canon_ref=40, negative = same scenario WITHOUT the NCP poke), `docs/coverage/navicust.md` (NEW), `docs/worklog/T104.md`. **NOT** src/objects.rs, src/ai.rs, src/chips.rs, src/emotion.rs, tools/trace.py, tools/oracle.py, tools/allowlist.py, tools/inventory.py (re-run for the M7 line only), tools/mgba_capture.c, reference/bn6f.

**Do.**
1. **Recon:** pick one `SetCurPETNaviStatsByte` NCP from `navicust_jt_NCPs` (asm37_0.s:2161-2600); cite file:line for the handler and the stat byte it writes (likely `oCurPETNaviStats_Atk` +0x??, NaviStats.inc field); name the NCP id + its give-site (asm03_1_1.s:8794 call chain). **measurement.**
2. Baseline, no edit: build ROM, sha256 + size; verify_rows `wave,window,opening,chip-cannon,mettaur,windowclose,cursor` → 0/0/N ×6, cursor 1/1/170; capture the existing chip-cannon scenario for the +Atk damage baseline (read the enemy's HP both sides at the hit frame). **measurement.**
3. Add `ncp_atk_full` + the `ncp_atk_dmg` row (canon = REAL + NCP poke; rust = plain_rom; negative = no NCP poke). **code change + measurement.**
4. Port the +Atk read in src/battle.rs's chip-damage calc from step 1's cite; plumb the descriptor NCP field in src/fixture.rs; build ROM, sha256. **code change + measurement.**
5. Re-run: `ncp_atk_dmg` 0/0/40 with non-blind negative (no NCP → same damage as step 2, both sides; NCP poke → +Atk damage visible on both sides); trace `ncp_atk_full`: the stat byte AND the damage field first divergence **none** over the row's frames; guard set + cursor identical to step 2. **measurement.**

**Rules.** The NCP handler and stat byte cite file:line; the damage path reads the stat, not a fitted constant. State/descriptor poke carries `peeked` provenance (F5b's rule). Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, canon never changes. ≤6 captures, tool budget ≤80, one harness command at a time.

**Acceptance.** `ncp_atk_dmg` 0/0/40 with non-blind negative (no NCP → damage matches step 2; NCP → damage higher by exactly the +Atk delta on both sides); trace `ncp_atk_full`: stat byte AND damage field first divergence **none** over the scene's frames; 7-row guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming canon's measured no-stat-delta behavior (frames + bytes + cite) closes it.

**Measure and report.** rows: ncp_atk_dmg + chip-cannon + 7-row guard set; frames 40 each (cursor 170). NCP handler cite, stat byte address + cite, damage before/after NCP both sides, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (the other 18 NaviCust effects, by id).

**Coordinator:** owns src/battle.rs's damage arm + src/fixture.rs + the row; runs alone (owns tools/states.py + tools/harness.py). Free tier: verify_rows from a clean checkout on the 9-row set; verifier for step 1's two cites. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M7 (NaviCust 0/19 → 1/19 first port) and M1 (NaviCust count).

---

### T105. M7 emotion window: pick one reachable emotion state (Full Synchro via Counter Hit, or any pre-battle-set state) and port its face-selection gate *(OPEN -- 2026-09-17)*

**Why.** src/emotion.rs:11 explicitly says **"Only the calm face is drawn. The ROM's bank continues past it with one window per emotion, which this build has no state to choose between yet."** The draw routine is `sub_801CDEC` (asm00_2.s:27554-27583, HUD element 14) handing `sub_802FE28` packed pairs `0x80004012 / 0xCBB4` and `0x40200012 / 0xCBBC` — two OBJ tiles, palette bank 12, priority 2, positions `(0,18)` and `(32,18)`; the art+palette export is `tools/emotion_export.py` writing `assets/emotion.bin` (BNEM header at +0x00..+0x0c, face data at offset named by header). SCOPE M7 emotion reads 0/25 (the form row is 0/25 separately; emotion is its own sub-row in M7 prose). The emotion value byte lives near the navi's stat block (cite-able through `GetCurPETNaviStatsByte` callers in asm37_0.s). **No ticket has ported a non-calm face** (grep TODO/TODO_ARCHIVE for "emotion" returns only the T102 prose cite and existing row notes). T102 (proposed) targets the counter-hit grant; this ticket is the render gate's complement: a +1NCP-style poke to the emotion byte is enough to flip the face in a scenario with no hand-played input (the poke is the fixture, canon never changes — F5b's rule).

**Files.** `src/emotion.rs` (the face-selection gate, choose face index from emotion value; one const table per reachable state), `assets/emotion.bin` (verify face offsets per emotion value; no redraw, only re-layout), `src/fixture.rs` (descriptor emotion field), `tools/states.py` (ONE scenario `emotion_syn_full` = `battlestart.state` + poke `60:<emotion_addr>:0xNN` + scripted none), `tools/harness.py` (ONE row `emotion_syn`: canon_ref=40, negative = same scenario WITHOUT poke), `docs/coverage/emotion.md` (NEW), `docs/worklog/T105.md`. **NOT** src/battle.rs, src/objects.rs, src/ai.rs, src/chips.rs, tools/trace.py, tools/oracle.py, tools/allowlist.py, tools/inventory.py, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. **Recon:** read the BNEM header at `assets/emotion.bin` + every face's offset/width/height (currently 1 face, calm); cite each face's data start; find the emotion-state byte (cite file:line — likely one of the NCP-settable stats or a separate byte near the navi struct); confirm the emotion byte's reachable value list from canon's NCP set or from sub_801CADC (asm00_2.s:25577 updater). **measurement.**
2. Baseline, no edit: build ROM, sha256 + size; verify_rows 7-row guard set + cursor → 0/0/N ×6, cursor 1/1/170; capture the existing chip-cannon scenario, read OBJ tiles at (0,18) and (32,18) — both sides should be the calm face. **measurement.**
3. Add `emotion_syn_full` + the `emotion_syn` row (canon = REAL + emotion poke; rust = plain_rom; negative = same scenario WITHOUT poke → both sides stay calm, pixels match step 2). **code change + measurement.**
4. Port the face-selection gate in src/emotion.rs: read emotion-state byte (peeked address), index into a `const FACE_INDEX: [u8; N]` table keyed by emotion value; rebuild, sha256. **code change + measurement.**
5. Re-run: `emotion_syn` 0/0/40 with non-blind negative (no poke → calm face, both sides match step 2; poke → non-calm face, both sides render it); trace `emotion_syn_full`: the emotion byte AND the OBJ tile at (0,18)/(32,18) first divergence **none** over the row's frames; guard set + cursor identical to step 2. **measurement.**

**Rules.** Art from canon bytes, not redrawn (the BNEM export is already data). The FACE_INDEX table is cited from canon's per-emotion draw site. State/descriptor poke carries `peeked` provenance. Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, canon never changes. ≤6 captures, tool budget ≤80.

**Acceptance.** `emotion_syn` 0/0/40 with non-blind negative; OBJ tiles at (0,18)/(32,18) match between canon and rust; trace `emotion_syn_full`: emotion byte + OBJ tile first divergence **none** over the scene's frames; 7-row guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming canon's measured no-face-change behavior (frames + bytes + cite) closes it.

**Measure and report.** rows: emotion_syn + 7-row guard set; frames 40 each (cursor 170). Emotion byte cite + face offset per value, before/after pixel totals, OBJ tile counts at the emotion positions both sides, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (the other ~24 emotion states, by value).

**Coordinator:** owns src/emotion.rs + asset layout + the row; runs alone (owns tools/states.py + tools/harness.py). Free tier: verify_rows from a clean checkout on the 8-row set; verifier for step 1's BNEM header + emotion-byte cites. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M7 (emotion window 0/25 → 1/25 first port).

---

### T106. M3 element/weakness: identify canon's weakness lookup, port the chip-element→enemy-weakness gate, verify on a recorded Cannon-vs-Gunner scenario *(OPEN -- 2026-09-17)*

**Why.** SCOPE M3 names "elements and weakness" — **no ticket has touched it** (grep TODO.md + TODO_ARCHIVE.md for "weakness" / "element" returns only the M3 prose line). Our side has no element gate: grep `src/battle.rs` for "element" hits only the `CHIP_ELEM_*` constants; no per-enemy weakness table is read. Canon's mechanic is real and measurable: the Gunner's struct (off_8109150[0x17], elem_hp 0x3C, asm32.s:9538) carries an element byte, and the chip damage calc reads (chip.element == enemy.element) to pick the multiplier. The scenario exists end-to-end: T87 landed `battlestart_ai4_rank0` for the Gunner recording; the chip-cannon row (F7 DONE) covers the chip; only the element gate is missing. F25b's Mettaur attack-phase correction moved the chip window's hit to true phase, so a scripted A press lands a Cannon hit on the Gunner at a measured frame. The element delta is a *damage number* (one or two HP off the post-hit enemy HP), not a pixel position — fully measurable on the existing trace.

**Files.** `src/battle.rs` (ONE element-gate arm in the chip-damage calc, T57 step-name the damage call; cite the per-enemy struct field), `src/objects.rs` (the Gunner's element byte writer — already in the per-type routine from T9j PARTIAL — cite the field address), `tools/states.py` (ONE scenario `gunner_elem_cannon` = `battlestart_ai4_rank0` + Cannon in the picked slot + scripted A at the measured windup frame), `tools/harness.py` (ONE row `chip-elem-gun`: canon_ref=40, negative = same scenario WITHOUT Cannon in the picked slot), `docs/coverage/damage.md` (NEW), `docs/worklog/T106.md`. **NOT** src/chips.rs, src/ai.rs, src/emotion.rs, src/shot.rs (unless step 1 names it), tools/trace.py, tools/oracle.py, tools/allowlist.py, tools/inventory.py, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. **Recon:** read `byte_80182C4` row 0x17 (Gunner, asm00_2.s:19975+0x17*struct_size) and the per-actor struct at off_8109150[0x17] for the element byte; cite the Gunner's element value; read `ChipDataArr` (data/ChipDataArr.s) row 1 (Cannon, id=1) for its element byte; cite the chip-damage calc's element-gate site (likely sub_801BEE0 or a successor in asm00_2.s, file:line). Report: chip.element, enemy.element, expected multiplier (1.0/2.0/0.5/0.0 per the four cases). **measurement.**
2. Baseline, no edit: build ROM, sha256 + size; verify_rows 7-row guard set + chip-cannon + mettaur → 0/0/N ×7; capture the Gunner scenario on both sides over 40 frames past the Cannon hit frame; report total/worst/first-frame. **measurement.**
3. Add `gunner_elem_cannon` + the `chip-elem-gun` row (canon = REAL; rust = plain_rom + Cannon in picked slot; negative = same scenario WITHOUT Cannon → no damage event, both sides idle). **code change + measurement.**
4. Port the element-gate arm in src/battle.rs from step 1's cite; ensure the Gunner's element byte is plumbed from src/objects.rs (T9j already writes the per-type struct; the field is at a fixed offset — cite it); rebuild, sha256. **code change + measurement.**
5. Re-run: `chip-elem-gun` 0/0/40 with non-blind negative; trace `gunner_elem_cannon`: the post-hit enemy HP AND the chip element byte AND the enemy element byte first divergence **none** over the row's frames; 7-row guard set + cursor identical to step 2. **measurement.**

**Rules.** The element gate cites the per-enemy struct field and the chip element byte (both file:line) — no fitted multiplier, no `match enemy.kind`. The damage is the ported value, not a re-derivation. Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, canon never changes. ≤6 captures, tool budget ≤80, one harness command at a time.

**Acceptance.** `chip-elem-gun` 0/0/40 with non-blind negative (no Cannon → no damage event, both sides idle); the post-hit enemy HP matches between canon and rust to the exact element-multiplied value (cite step 1's expected damage = base × multiplier); trace `gunner_elem_cannon`: enemy HP + chip element + enemy element first divergence **none** over the scene's frames; 7-row guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming the missing cite, the wrong multiplier, or canon's measured no-element-gate behavior closes it.

**Measure and report.** rows: chip-elem-gun + chip-cannon + mettaur + 7-row guard set; frames 40 each (cursor 170). Element byte cite per side + expected multiplier + actual damage both sides, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (the other 186 viruses' element gates).

**Coordinator:** owns src/battle.rs's element arm + src/objects.rs's element plumb + the row; runs alone (owns tools/states.py + tools/harness.py). Free tier: verify_rows from a clean checkout on the 10-row set; verifier for step 1's three cites (Gunner element byte, Cannon element byte, element-gate site). ≤$0.25 expected, ≤$0.50 cap.

**Milestone advanced:** M3 (element/weakness: 0 covered → 1 covered with one Gunner-Cannon scenario).
