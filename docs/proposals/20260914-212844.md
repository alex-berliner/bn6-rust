# Proposed tickets 20260914-212844 (minimax/MiniMax-M3, $0.0841)

I have enough facts. Three tickets, ordered by M2-before-content and $/rows-closed:

---

### T7k. Enemy opening action follow-up: the Hidden/Appearing completion arm that frees CurAction 0x00  *(NEW -- 2026-09-14, follow-up to T7h's NEGATIVE: structural PAUSED-vs-Hidden/Appearing gap, cursor 10→741 regress forces a different arm than T7h's SPAWN port)*

**Files.** src/objects.rs (the per-type entry and the enemy's exported CurState/CurAction), src/actor.rs (the 0x0A/0x0B executors' entry condition), src/battle.rs (only where the enemy is created for a fixture), tools/trace.py (only if a field must be watched to see an edge), docs/coverage/battle_full.md (notes)

**Why.** T7h NEGATIVE proved that porting `RunSpawnAnimationMaybe_8016380` (asm00_2.s:16009) as the SPAWN arm of `MettaurEntry`'s `think[]` is the wrong fix: trace344/540 + 409/540 UNCHANGED, cursor REGRESSED 10/9/170 → 741/728/170 → forced revert. The mechanism is structural — canon's PAUSED state captures the Mettaur already at CurAction 0x0A (mid-HOP) at k=0, while ours has him in a 62-frame Hidden/Appearing fade preceding any entry arm. T7h's result explicitly: "the spawn-arm port is not the right fix because the divergence lives in the PAUSED-vs-Hidden/Appearing gap, not in the entry sequence". M2's gate is "battle_full trace: first divergence none" and `enemy_state_action` 344/540 + `enemy_anim` 409/540 are still on the board; one follow-up to T7h is allowed before BLOCKED per "a PARTIAL from a wrong guess about the cause gets one follow-up ticket". The right arm is the one that frees the gate holding CurAction at 0x00 during Hidden/Appearing — canon's CurAction becomes 0x0A the frame Hidden/Appearing completes, not the frame SPAWN runs.

**Do.**
1. Baseline `tools/trace.py record/diff --align row:battle_full` with no edit and read the window around the gap: `tools/probe.py` on enemy slot `0x0203ab68:+0x08..+0x26` plus MegaMan's `0x0203a9b8:+0x08..+0x26` for k=−10..40 on both sides → *report, per side per frame, the (CurState, CurAction, timer, anim) tuple; the first frame canon's CurAction becomes non-0x00 (the Hidden→Appearing→HOP chain); the first frame ours becomes non-0x00; and the timer's countdown on our side during the 62-frame fade (does our Hidden/Appearing even arm?).*
2. Find canon's writer of the post-Hidden/Appearing CurAction — `ForMettaur_8109EF4`'s sub-action 0x01→0x02 transition in asm31.s:170982, plus the `sub_8016380` SPAWN-exit arm that frees CurAction 0x00 → *cite the exact `file:line` per arm; report the timer's countdown canon holds during the fade (arms and decrement) versus ours.*
3. Port the Hidden/Appearing completion arm under the already-landed `enemy_think`/`enemy_act` entries (NOT as a new hand-written pose and NOT as a per-scenario branch) — the arm that frees the gate holding CurAction at 0x00 — and re-run the same diff → *report the trace's new first divergence (field + k + canon/rust values), enemy_state_action count after (baseline 344/540), enemy_anim count after (baseline 409/540), AND cursor pixel count (must NOT exceed 10/9/170 /186276).*
4. Re-run `tools/verify_rows.py` from a clean detached checkout → *report the table identical to HEAD except where the report says, with every chip row and the mettaur row still 0/0 and cursor's tear reported as it moves (≤10/9/170).*

**Rules.** Only the named files; no new harness row and no change to tools/harness.py, tools/states.py, FIXTURE.md or the descriptor contract; no allowlist change of any kind; no alignment change except by measured event; the fix is a port under the interpreters (T5/T6's `enemy_think`/`enemy_act`/`MettaurEntry`) — no new hand-written pose and no per-scenario branch; every new literal in an edited line gets a `// provenance:` tag or the canon symbol; `fitted constants in src/` (HEAD: 19) may not increase; canon never changes; ≤6 capture runs, one at a time inside the 3-slot semaphore.

**Measure and report.** row: battle_full, mettaur, popup, wave, cursor, result (trace rows plus the pixel rows this touches). frames: 540 for battle_full, 70 mettaur, 170 cursor. total/worst: per pixel row before and after. region: for the trace, the (CurState, CurAction) pairs frame by frame for k=−10..40 both sides; for any pixel regression, the diffmask region and frame. commit: src/objects.rs + src/actor.rs. One line of mechanism (the Hidden/Appearing completion arm cited file:line); one line of what is unverified (canon's Navi-side spawn arm, if only the virus arm was measured).

**Acceptance.** the `battle_full` first divergence is no longer `enemy_state_action` at k=0 — the pair is equal at k=0 on both sides, with the ported arms cited — and the two counts move off their baselines (344/540 and 409/540) by a measured amount, reported whatever it is; cursor pixel count NOT exceeding 10/9/170 / 186276; every isolated pixel row reads 0 as it does today (43 chips included); `fitted constants` not increased.

**Coordinator:** dispatch first. M2 trace gate is the priority; this is T7h's one allowed follow-up before BLOCKED. Worker muse-spark-1.3-contributor (T6's child class — the Mettaur as data), verifier GLM-5.3-flash cross-family; ≤$0.20 expected, ≤$0.50 cap; verify_rows on mettaur, wave, popup, cursor and the chip rows. Advances **M2**.

---

### T7l. Banner composite model: implement the swap the verifier REFUTED was unsafe but actually is *(NEW -- 2026-09-14, follow-up to T7j's PARTIAL: branch wt/t7j-banner-composite @ aa3486d docs-only, verifier confirmed swap is safe in this build)*

**Files.** src/banner.rs (the banner composite struct and the HUD bit 0x8000 watch), src/battle.rs (the 0x04 leave edge only — the `SEQ04_FRAMES=60` constant), tools/trace.py (only if a watch field must be added), tools/harness.py (the sequencer notes only), docs/coverage/windowclose.md (the measured predicate)

**Why.** T7j PARTIAL left the banner composite branch UNMERGED at wt/t7j-banner-composite @ aa3486d because the worker claimed "the swap is unsafe" — and the verifier-minimax REFUTED that claim: "banner_at (line 2608) is armed on 0x04→0x00 transition and counts down independently of SEQ_04 age, so banner timing is orthogonal to the SEQ_04 leave edge — the swap is safe in this build". The verifier's refutation is the go-ahead the worker never read. T7j only rewrote the `// provenance:` tag on `SEQ04_FRAMES=60` (src/battle.rs:1186 — currently tagged `derived`, the renamer's word, not a `// canon:` symbol) and updated docs; the constant itself still sits in the0x04 leave edge. `SEQ04_FRAMES=60` is one of the 19 fitted constants counted in `docs/reviews/2026-09-14.md` hygiene — the project's "no fitted constants" cross-cutting invariant is still violated by it (a literal in src/ with no ROM citation, matched to the picture, mechanism unknown). M2's sequencer acceptance is "battle_full trace: first divergence none" and the fitted constant sits in the path that T7e's sequencer arms set up; closing it advances M2 by removing one fitted constant AND closes the last predicate T7f named but couldn't implement (`sub_801E754`'s banner-idle check on HUD bit 0x8000, asm00_1.s:10478-10514, with timer arms 0x1e and 0x293). One follow-up to T7j is allowed before BLOCKED.

**Do.**
1. Re-record `tools/states.py`'s `windowclose_full` scenario on the current build and trace `0x020368C0` (the banner composite field, ewram.s lookup) plus HUD bit 0x8000 (the AIData bit `sub_801E754` reads) plus the `0x02036500` blink counter (asm03_0.s:4801-4804) across the 0x04 hold window (canon frames 147..206, k=25..84 per T7f) → *report, per side, the byte/halfword values for the banner composite on every frame of the 0x04 hold, the HUD bit's set/clear per frame, and the first frame `sub_801E754`'s banner-idle check would return 0.*
2. Port the banner composite model: a struct that mirrors `0x020368C0` (whatever fields it holds — read with `tools/probe.py --read` and parse `reference/bn6f/asm/asm03_0.s` for the layout) and the HUD bit 0x8000 reader; on the 0x04 leave edge at src/battle.rs:2536, replace `SEQ04_FRAMES=60` with the predicate `banner_composite.is_idle() && (hud & 0x8000) == 0` evaluated against the just-traced state → *cite the `sub_801E754` call site (asm00_1.s:10478-10514) with a `// provenance:` tag in the same commit and report no `SEQ04_FRAMES=60` literal remains in `src/` (`grep -n SEQ04_FRAMES src/` proves it).*
3. Re-run `tools/verify_rows.py` and the `windowclose_full` sequencer trace → *report the table identical to HEAD except any pixel movement the new predicate causes (must be ≤ the cursor tear 10/9/170 / 186276), the 0x04 leave frame on both sides (canon frame 206 vs ours — must match), and the fitted-constant count (must drop by exactly 1, 19→18).*

**Rules.** Only the named files; no allowlist change; no new harness row; no descriptor or alignment change; the fix is a port of `sub_801E754`'s check — no fitted countdown; every new literal gets a `// provenance:` tag with the cite `asm00_1.s:10478-10514` or the canon symbol; `fitted constants in src/` (HEAD: 19) MUST drop by exactly 1; canon never changes; ≤4 capture runs, one at a time inside the 3-slot semaphore.

**Measure and report.** row: windowclose, cursor, result, mettaur, wave, opening, chip-cannon (regression set), plus the fitted-constant count. frames: 40 windowclose, 170 cursor, 540 battle_full sequencer. total: fitted-constant count before and after (19 → 18). worst: any pixel row that moves. region: the 0x04 leave frame's diffmask region. commit: src/banner.rs + src/battle.rs. One line of mechanism (`sub_801E754`'s banner-idle check on the modeled composite, cited asm00_1.s:10478-10514); one line of what is unverified (whether the banner composite has fields not exercised by `windowclose_full` that the cannon/buster paths would see).

**Acceptance.** `SEQ04_FRAMES=60` no longer in `src/` (`grep` proves it); the 0x04 leave frame on `windowclose_full` matches canon's frame 206 on both sides (the sequencer trace says so); the new predicate is cited `asm00_1.s:10478-10514` with a `// provenance:` tag in the same commit; `fitted constants` count drops 19→18; the full harness table identical to HEAD except where the predicate moves cursor's tear (≤10/9/170 / 186276); battle_full sequencer remains 540/540 unaligned.

**Coordinator:** dispatch second, after T7k lands (their Files lines are disjoint — T7k: src/objects.rs + src/actor.rs + src/battle.rs; T7l: src/banner.rs + src/battle.rs — src/battle.rs is shared, so they run sequentially, not paired). Worker muse-spark-1.3-contributor (T7j's child class), verifier GLM-5.3-flash cross-family; ≤$0.15 expected, ≤$0.40 cap; verify_rows on every row named plus the fitted-constant count. Advances **M2**.

---

### T13d. Audio probe on the cannon route: what canon fires, what ours doesn't, then gate *(NEW -- 2026-09-14, follow-up to T13c's BLOCKED: option (a) re-scope — no chip-fire/damage play_sound calls exist in src/, audit only)*

**Files.** src/battle.rs (the chip-fire and chip-damage play_sound calls and their gate predicates only — IF the dump shows they fire), tools/probe.py + the audio dump path (the dump on the cannon route), docs/audio/baseline-cannon.md (new), docs/coverage/audio-cannon.md (new)

**Why.** T13c BLOCKED on a premise error the worker's audit caught: no chip-fire or chip-damage `play_sound` calls exist in `src/battle.rs` (only the T13b-gated buster hit at line 3130), no chip-fire/chip-damage wav ships with the build (assets/ has 47 bins + 1 wav), and the citation fragments were wrong (`sub_801A308` body reads `oBattleObject_Alliance XOR 1 + ORs oAIData_Unk_10`, contains no `PlaySoundEffect`; `sub_801B720` line number points inside `sub_801ABB8`). Per T13c's option (a), this ticket re-scopes to probe what canon fires on the cannon route first, then gate after the dump tells us what fires — adding arms before the dump is what the original ticket got wrong. M9 is "not started" per docs/SCOPE.md after T13/T13b (audio is "still not a harness verdict" per T13b's rules); this advances M9 closure without changing the harness. The cannon route is `tools/harness.py`'s `_chip_canon_0c("01")` (harness.py:868), which is at 0 px isolated — a clean baseline for the audio pass. M2 is closed at 540/540 unaligned (T7e); M9 is the next milestone behind the integrated variants' drift.

**Do.**
1. Run the audio dump on the existing cannon scenario (`tools/harness.py`'s `_chip_canon_0c("01")` route, harness.py:868) with a sterile ROM, and tree-solo each of the four audio channels (ch0/1/2/3, FIFO A; ch4/5/6/7, FIFO B per mgba_capture.c:631-641) → *report both trees' peak |sample| per channel, the onset frame and onset-pair index, and the RMS in the onset frame's window (canon AND ours); the buster fixture's per-channel peaks (T13: ch4 peak 0 zero-enemy, 11273 enemy-present at frames 118..130) are the control.*
2. From the dump, identify the canon-fired SFX samples (channels, time windows, frequency envelope) on the chip-fire and chip-damage paths → *report the sample IDs and the windows where they fire, with both sides' (canon AND ours) peak |sample|; an empty canon-fire list (i.e. canon is silent on this route) is a result, reported as one with the measured proof.*
3. If canon fires SFX samples that ours doesn't: locate the corresponding arms in `src/battle.rs` (or add them if missing per T13c's audit), gate each on its cited predicate (chip-fire on the sequencer's chip-fire event; chip-damage on `take_damage[enemy_idx] != 0`), re-run the dump → *report both trees' peaks again, the onset residual in pairs and in ms at the measured 95999.1 / 95589.4 Hz, and the per-channel envelope shape; if both trees are flat, that is a result, reported as one with the doc stating the absence.*
4. Write `docs/audio/baseline-cannon.md` (the same shape as `docs/audio/baseline-buster.md`) and `docs/coverage/audio-cannon.md` recording the per-channel peaks, the onset residual, and what is now parity and what is still residue → *report the doc's new finding section and the log file list.*

**Rules.** Only the named files; `tools/harness.py` notes only (no row added — audio is still not a harness verdict); `tools/mgba_capture.c`, `tools/states.py`, `tools/trace.py`, `FIXTURE.md`, `assets/`, `reference/bn6f` read-only; ≤8 capture runs, one at a time inside the 3-slot semaphore, count disclosed honestly; every `play_sound` change gets its `// provenance:` tag updated in the same commit; `fitted constants in src/` (HEAD: 19) may not increase; canon never changes.

**Measure and report.** row: cannon + full table (regression set). frames: 40 cannon, 540 battle_full. total: per-channel peak |sample| before and after. worst: any pixel row that moves. region: the onset-frame's diffmask region if a pixel regresses. commit: src/battle.rs (if SFX arms are added) + docs/. One line of mechanism (which SFX samples canon fires on chip-fire and chip-damage and where ours diverges — or that canon is silent on this route); one line of what is unverified (whether the audio dump captures all SFX arms, or whether some fire outside the compared window).

**Acceptance.** `docs/audio/baseline-cannon.md` and `docs/coverage/audio-cannon.md` exist with the measured numbers; the chip-fire and chip-damage SFX paths in `src/battle.rs` are gated by the cited predicates (if they exist) or correctly absent (if they don't, with the doc stating the absence); our per-channel trees match canon's onset frame and onset-pair index to within the buster-residual 635 pairs / 6.62-6.64ms class (or the report names the larger residual with its cause); the full harness table is identical to HEAD (`verify_rows.py` clean run; cursor's 10/9/170 / 186276 tear reported as it moves; nothing worse); `fitted constants` unchanged or lower.

**Coordinator:** dispatch third, after T7l lands. M9 is lower priority than M2 trace gate but the cannon route is at 0 px isolated so the audio pass is purely additive on a clean baseline; this is T13c's one allowed follow-up (option a). Worker muse-spark-1.3-contributor (T13b's child class), verifier GLM-5.3-flash cross-family; ≤$0.20 expected, ≤$0.50 cap; verify_rows on the full table (audio is still not a harness verdict, but no other row may move). Advances **M9**.
