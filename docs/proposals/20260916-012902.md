# Proposed tickets 20260916-012902 (minimax/MiniMax-M3, $0.1250)

### T35. M3's first panel-effect port: cracked panel breaks on MegaMan's step, from object_crackPanel at object.s:2218-2222 *(OPEN -- 2026-09-16)*

**Why.** T33 (landed NEGATIVE ea5d284) closed the panel-writer walk: 8/13 panel types verified, 5 (0x0 hole, 0x5 holy, 0x8, 0x9, 0xA) bounded GAPs with no writers in the ROM. M3 is not started, and the cheapest port is the cracked-panel break-on-step rule: `object_crackPanel` at object.s:2218-2222 sets `oPanelData_Type=0` (broken) when MegaMan steps on a panel of type 3 (cracked), with the reader at asm38.s:4318-4326 checking `Unk_12=0x708`. The rule's writer and reader are both cited, and the field fixture (T9l/T25/T31 work) already loads cracked panels — same stage data. The 8 verified panel writes were all closed the same mechanical way — a site with an immediate — and crack-on-step fits that pattern. Milestone **M3**, and M1's panels line.

**Files.** src/panel.rs (the per-type effect dispatch, if it exists; otherwise src/battle.rs's panel update path), src/fixture.rs (a panel-state descriptor if needed), tools/harness.py (a new scenario row), docs/worklog/T35.md.

**Do.** 1. Baseline on current main: run a panel-state fixture that loads cracked panels, dump the per-frame panel data via `tools/probe.py watch`, and report the panel-type transitions for MegaMan's row. 2. Read object.s:2218-2222 and asm38.s:4318-4326 on disk → report the exact writer of type 3 → type 0 and the test that arms it. 3. Port the rule: when MegaMan's panel is type 3 and he steps, write type 0 to the source cell. 4. Re-run step 1 → report the transition timing before/after. 5. Regression: `python3 tools/verify_rows.py <branch> field,mettaur,cursor,opening --expect field=0/0/40/1139 --expect mettaur=0/0/70/41734 --expect cursor=1/1/170/186279 --expect opening=0/0/40/86591`; report all lines.

**Rules.** No fixture byte not derived from the stage data we already load. No allowlist edit, no `patch_sterile.py`. The new scenario row's negative fixture must stay non-blind. fitted count HEAD 19 must not rise; any new literal gets `// canon:` or `// provenance:`. Re-check every file:line against disk (the 357da2a re-cut moved bn6f's numbers). ≤4 captures, tool budget ≤60; /tmp's four canon roots untouched.

**Acceptance.** `crack-step 0/0/N` with the panel-state transitions matching canon's, every isolated row unchanged from main, and the panel-writer cite carried in the diff. Or a NEGATIVE naming the cell-set code path that `object_crackPanel` reaches — that names the per-cell write mechanism the next ticket must port.

**Measure and report.** rows: the new scenario row plus field/isolated, mettaur, cursor, opening. total/worst/negatives before and after; the panel-type transition k before/after; the cite; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch first; no other open ticket touches src/panel.rs or src/fixture.rs. Free tier: verify_rows on the isolated set with the landed expects; a verifier for step 2's writer cite. ≤$0.20 expected, ≤$0.40 cap. Advances **M3**.

---

### T36. M4's third chip-data arm: Cannon, HiCannon and M-Cannon read AttackPower +0x1a and Element, removing their `match chip.id` arms *(OPEN -- 2026-09-16)*

**Why.** T17 (landed 8c1a62d) made the sword/blade family (0x13) data-driven; T19 (2baad23) made the contract loud; T28/T34 NEGATIVE-LANDED for Recov/Barrier/Vulcan (need subfamily/params[0] indexing). The next family is the Cannons: Cannon id 1, HiCannon 2, M-Cannon 3, three chips sharing one mechanism (AttackPower +0x1a + Element from the record). All three are pixel-verified through our own code (43/411 verified-pixels per docs/SCOPE.md), and the record bytes are already in our v2 asset (T17's append). docs/SCOPE.md's M4 line reads "10 of 411 as data, 33 through our own code"; porting the Cannon family moves three more to data, the cheapest clean family left because the lookup is a single record read with no subfamily indexing. Milestone **M4**, and M1's chips line.

**Files.** src/chips.rs (no change — accessors exist from T17/T19), src/battle.rs (**only** the Cannon arm in chip_strike :4277-4338 and any Cannon-specific amount lookup; never the landed sword/blade arm, never fight_latch/hp_readout_live), tools/inventory.py (the as-data category data), docs/SCOPE.md (regenerated only), docs/worklog/T36.md.

**Do.** 1. Baseline on current main: `python3 tools/harness.py --only chip-cannon,chip-hipcannon,chip-mcannon --no-gallery` (expect 0/0/40 with negatives) and quote SCOPE's M4 line. 2. Parity table before any edit: for Cannon/HiCannon/M-Cannon print AttackPower (+0x1a) and Element from `ChipDataArr_8021DA8` in reference/bn6f/bn6f.gba, beside the src literal → every agreement and mismatch is the finding. 3. Convert only what step 2 proves equal: replace the `match chip.id` arm with a single record read; delete the id-keyed constant → report the count of `CHIP_*` ids remaining in the Cannon arm before/after. 4. Re-run step 1 → every Cannon row must read 0/0 with the negative non-blind. 5. Regression: `verify_rows <branch> chip-cannon,chip-hipcannon,chip-mcannon,chip-use,cursor,mettaur --expect ...` plus the chip rows; report all lines. 6. If the generator can derive an as-data state for these chips, edit that data and regenerate; quote SCOPE's M4 line and M1 chips count before/after.

**Rules.** The v2 asset is the file. No `match` surviving a value step 2 proved equal — a mismatch is the finding, per chip with both numbers (T17's rule). No fix-up table. fitted count ≤19, derived count falls by the deleted tables. Re-check every cite against disk. ≤4 captures, tool budget ≤60.

**Acceptance.** The 3-chip parity table with zero unexplained mismatches, no `CHIP_*` id indexing an amount in the Cannon arm (before/after counts quoted), all 43 chip rows still 0/0 with their negatives, and SCOPE's M4 line moved through the generator (before/after quoted) — or a NEGATIVE that names the byte which contradicts the measured literal.

**Measure and report.** rows: chip-cannon, chip-hipcannon, chip-mcannon, plus the 40 non-Cannon chip rows for regression, chip-use, cursor, mettaur. frames 40/30/170/70. totals/worst/negatives before/after per row, the `CHIP_*` id count in the Cannon arm before/after, the parity table, SCOPE's M4 line and M1 chips count before/after, fitted count, commit, one line of mechanism, one line of what is unverified.

**Coordinator:** dispatch after T30 lands or closes (it edits these same comment lines). Free tier: verify_rows across the chip set with the landed negatives; a chip row that moves is a revert. A verifier for step 2's parity claim (the same ROM read T17's verifier did). ≤$0.20 expected, ≤$0.40 cap. Advances **M4**.

---

### T37. M5's first virus-port completion: settle Gunner's live attack event frame and bring gunner integrated below 1,500,000 *(OPEN -- 2026-09-16)*

**Why.** `gunner` integrated reads 2105613/38237/130 (neg 2284867, non-blind) — the largest row on the board — and T9c PARTIAL/T9e NEGATIVE/T9j PARTIAL landed pieces of the port (`ForGunner_8113078` routine at asm32.s:10123-10142 + 4-arm state machine sub_8112F70/sub_8112FBA/sub_8112FC2). T9g NEGATIVE measured the 234-frame settle window (sub_8112D9C), T9l PARTIAL split the residue by layer (k=0..5 fade tail, k=6..75 BG3 HP boxes + custom gauge bar, k≥77 chip window auto-open), and T32 DONE seeded the gauge descriptor (`GUNNER_ROW.gauge=0x4000` with `isCustGaugeFullAndBattleLive_800A21C` cite at tools/harness.py:1759). The unresolved lever is the Gunner's first attack event frame: once measured, the harness row can be aligned by the event (currently aligned by canon_ref=80), and T9g's settle window predicts the trigger frame inside the k=6..75 band. A measured first-attack event frame and an aligned capture bring the k=6..75 BG3 plateau from ~2047/frame to a measured single-frame residue — the largest single-row advance available for any virus. Milestone **M5** (every virus family and rank) and **M8** (HP displays).

**Files.** src/objects.rs (the gunner_update arm and the first-attack frame derivation), src/battle.rs **only** if the attack-event reads a battle-level field, tools/harness.py (the GUNNER_ROW descriptor's align, NOT its other fields), docs/worklog/T37.md.

**Do.** 1. Baseline, one session: `python3 tools/harness.py --only gunner --no-gallery` → 2105613/38237/130 (neg 2284867) + sha256sum of the built ROM. 2. From T9g's kept dumps (`/tmp/bn-t9g-gunner-extended/`), name the Gunner's first-attack event frame relative to `canon_ref=80`: read sub_8112D9C's settle window and the first attack object's spawn frame; report both with the kept capture dir. 3. Verify the gate is the right one: `tools/probe.py watch <canon rom> 0x02034XXX:<len> --loadstate <gunner row's canon state>` and report the per-frame series that confirms the attack-event frame. 4. If step 3 names a frame: move GUNNER_ROW's alignment to that frame, re-run step 1 → report the k=6..75 and k≥77 per-band totals before/after. 5. Regression: `verify_rows <branch> cursor,windowclose,field,mettaur --expect cursor=1/1/170/186279 --expect windowclose=0/0/40/207166 --expect field=0/0/40/1139 --expect mettaur=0/0/70/41734`; report every line.

**Rules.** No fixture byte outside GUNNER_ROW's align; no src/ change outside the gunner_update arm; no allowlist edit; `patch_sterile.py` forbidden. fitted count HEAD 19 must not rise (per T29: provenance_counts scans only src/, so report what the new literal is). Re-check every cite against disk. ≤5 captures, tool budget ≤80; /tmp's four canon roots and T9g's kept dumps untouched.

**Acceptance.** `gunner integrated` strictly below 1,500,000 with the four guard rows identical, the k=6..75 band's per-frame totals reduced to a measured single-frame residue, the attack-event frame named with the kept dir, and the gunner_update arm cite carried in the diff — or a NEGATIVE that names the field the settle window reads and shows it is not the attack event, which retires the lever.

**Measure and report.** rows: gunner (integrated + isolated), plus cursor, windowclose, field, mettaur. frames 130/170/40/70. total/worst/negatives before and after per row with both ROM shas, the k=6..75 and k≥77 per-band totals before/after, the attack-event frame, the kept dump dir, fitted count, commit, one line of mechanism, one line of what is unverified.

**Coordinator:** dispatch after T32 lands (it owns tools/harness.py this cycle). Free tier: verify_rows on the four guards plus direct --only gunner, gated on the integrated line with F48's matcher. A verifier for step 3's watch series — a memory finding every later virus ticket builds on. ≤$0.40 expected, ≤$0.80 cap. Advances **M5**.
