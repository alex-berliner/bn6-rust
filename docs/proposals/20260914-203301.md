# Proposed tickets 20260914-203301 (minimax/MiniMax-M3, $0.0967)

I have enough to write the three tickets. Let me draft them now.

---

### T14. End-sequence state machine port: the 5 integrated variants' results-window slide *(OPEN -- 2026-09-14, M2 gate on the integrated rows)*

**Files.** src/battle.rs (the end-sequence state machine and its0x0C hand-off), src/results.rs (the slide-in path and its entry condition), src/banner.rs (the banner composite that gates the 0x04 leave), tools/trace.py (only if a watch field must be added), tools/harness.py (the integrated rows' notes only)

**Why.** HANDOFF §1 names the gap: "Not at 0: the integrated opening/field/warp/buster/chip-use variants (canon's results window slides in on a frame our end sequence reaches differently per fixture; four passes say it needs the end sequence ported as one state machine, which the porting phase does)." The measured numbers today: **opening integrated 72499 / 2691 / 40**, **field integrated 305253** (cap 28000; that row is at11x its allowlist so the slide is the budget, not the residue), **warp integrated 45751** (F38b revert; canon's 4-frame-longer tail sits outside the window — `tools/harness.py:2037-2039`), **chip-use integrated 269172** (F38b revert; "honest lock stays 101; the printed search minimum sits at the band's left edge and is score-ridden, not event-locked"), **buster integrated** unmeasured on this build but its isolated residue is the 3-frame drift the same end sequence causes. The cause is named: canon's results window slides in on a frame our end sequence reaches differently per fixture; the F38b retry (resolve retried after F33d's `over` no longer freezes inputs/objects) confirms the slide is the mechanism, not a search window. T7e's sequencer is540/540 unaligned on battle_full (the end sequence reaches 0x0C at k=297 with the +35 dissolve seed); T7f's closing-window fixture validates the window states; the residual is the end sequence itself: how it counts from the killing blow, arms the banner composite, and hands off to the results state. Closing this ticket lands 5 rows in one go and is the highest-leverage M2 work the open queue names.

**Do.**
1. Baseline the five integrated rows with no edit: `python3 tools/harness.py --only opening,field,warp,buster,chip-use` (each at its integrated variant) and `tools/diffmask.py` for the regions → *report the five (total, worst, region) tuples, the five windows, and the sequencer trace per row at the killing-blow frame (canon frame 0x0C entry vs ours).*
2. Trace the end sequence frame-by-frame on battle_full with `tools/probe.py` from the killing blow to the results window's first slide-in frame: watch `dword_203CA70` (ewram.s:3040), `eBattleState.Index_01` (0x02034881, ewram.s:2572), the banner composite at0x020368C0, the HUD bit 0x8000 (the one T7f cites for sub_801E754's banner-idle check at asm00_1.s:10478-10514), and `0x02036500` (window-relative blink counter, asm03_0.s:4801-4804) → *report, per side, the frame each named field reaches its end-sequence value, the writer PC for each write, and the difference between the two sides' sequences on a single timeline (killing blow to first slide-in).*
3. Port the end sequence as one state machine: model the count from the killing blow, the banner composite's `sub_801E754` banner-idle check against HUD bit 0x8000 (asm00_1.s:10478-10514), the timer arms `0x1e` and `0x293`, and the `sub_802D6C4` return that gates the 0x20 leave (T7f's cited lines) → *cite each ported arm with `file:line`, report no fitted frame-count constants survive on strict-provenance reading.*
4. Re-run the five integrated rows plus the sequencer trace → *report the new (total, worst, region) for each of the 5 rows, the new sequencer k-count (must remain 540/540), and whether the four integrated rows' canon-vs-rust frames now align on the same killing-blow frame.*
5. Re-run `tools/verify_rows.py` from a clean detached checkout → *report the full table (61 of 67 rows at 0 today) with the5 integrated rows moved off their baselines; cursor's tear reported as it moves.*

**Rules.** Only the named files; no allowlist change; no new harness row; no alignment or descriptor change except by measured event; the fix is a port under the interpreters — no new hand-written pose, no per-fixture branch; every new literal in an edited line gets a `// provenance:` tag or the canon symbol; `fitted constants in src/` (HEAD: 19) may not increase; the 43 chip rows, wave, window, windowclose, mettaur, popup and buster isolated must not move; canon never changes; ≤8 capture runs, one at a time inside the 3-slot semaphore.

**Acceptance.** the five integrated rows are at or below 50% of their current baselines (opening 72499→36249, field 305253→152626, warp 45751→22875, chip-use 269172→134586, buster unmeasured → named with its number) with the slide-in event-locked (not score-aligned); the end sequence is one state machine under the named citations (no per-fixture branch, no fitted frame counts); battle_full's sequencer remains 540/540 unaligned; every other isolated row reads 0 with cursor's tear reported as it moves; `fitted constants` unchanged or lower.

**Measure and report.** row: opening/field/warp/buster/chip-use integrated, plus mettaur/popup/result/cursor and the chip rows. frames: 40 each for the 5 integrated, 70 mettaur, 170 cursor. total: the5 row totals before and after. worst: each row's worst frame. region: the diffmask region for the slide-in frames. commit: src/battle.rs + src/results.rs + src/banner.rs. One line of mechanism (the end-sequence state machine's named citations); one line of what is unverified (whether the buster integrated row's residue is the same mechanism or a separate one).
**Coordinator:** dispatch first. M2 is the lowest unmet milestone and this closes5 integrated rows the user named as the only blocker before M2 can be called "battle_full trace: first divergence none; pixels 0 on all its frames". Worker muse-spark-1.3-contributor, verifier GLM-5.3-flash cross-family; ≤$0.30 expected, ≤$0.60 cap; verify_rows on every row named (the 5 integrated plus mettaur/wave/window/windowclose/cursor/result and the 43 chip rows). Advances **M2**.

---

### T7j. Model the banner composite for sub_801E754's leave predicate: replace SEQ04_FRAMES=60 with the canonical banner-idle check *(OPEN -- 2026-09-14, T7f's peeked residual)***Files.** src/banner.rs (the banner composite and the HUD bit 0x8000 watch), src/battle.rs (the 0x04 leave edge only — the fitted `SEQ04_FRAMES=60` constant), tools/trace.py (only if a watch field must be added), tools/harness.py (the sequencer notes only), docs/coverage/windowclose.md (the measured predicate)

**Why.** T7f PARTIAL left one residual: the canonical leave predicate `sub_801E754`'s banner-idle check against HUD bit 0x8000 (asm00_1.s:10478-10514, with timer arms `0x1e` and `0x293`) needs **banner composite not modelled**, so `SEQ04_FRAMES=60` (src/battle.rs) stays peeked. T7f's result: "100/100 sequencer frame match on re-record; edges named in code as sub_8008452/sub_800840C/sub_8008064 with timer arms 0x1e/0x293 in comments; SEQ04_FRAMES=60 stays peeked because the asm timer arms are display parameters not countdown timers and the canonical leave predicate (sub_801E754 banner-idle check against HUD bit 0x8000) needs banner composite not modelled; verify_rows did not get a clean run before budget; worktree clean (no diff to land); no fitted frame-count constants survive on strict-provenance reading". The fitted constant violates the project's "no fitted constants" invariant (cross-cutting, daily review) — it is a literal in src/ with no `// provenance:` tag and no ROM citation. M2's sequencer acceptance is "battle_full trace: first divergence none" and `SEQ04_FRAMES=60` is one of 19 fitted constants still in src/; closing it advances M2 by removing a fitted constant AND closes the last predicate T7f named but couldn't implement. The verifier's note: "the canonical leave predicate needs banner composite not modelled" names exactly the missing piece.

**Do.**
1. Re-record `tools/states.py`'s `windowclose_full` scenario on the current build and trace `0x020368C0` (the banner composite field, ewram.s lookup) plus HUD bit 0x8000 (the AIData bit sub_801E754 reads) plus the `0x02036500` blink counter (asm03_0.s:4801-4804) across the 0x04 hold window (canon frames 147..206, k=25..84 per T7f) → *report, per side, the byte/halfword values for the banner composite on every frame of the 0x04 hold, the HUD bit's set/clear per frame, and the first frame sub_801E754's banner-idle check would return 0.*
2. Port the banner composite model: a struct that mirrors `0x020368C0` (whatever fields it holds — read with `tools/probe.py --read` and parse the disassembly reference/bn6f asm03_0.s for the layout) and the HUD bit 0x8000 reader; on the 0x04 leave edge, replace `SEQ04_FRAMES=60` with the predicate `banner_composite.is_idle() && (hud & 0x8000) == 0` evaluated against the just-traced state → *cite the sub_801E754 call site (asm00_1.s:10478-10514) and report no `SEQ04_FRAMES=60` literal remains in src/.*
3. Re-run `tools/verify_rows.py` and the windowclose_full sequencer trace → *report the table identical to HEAD except any pixel movement the new predicate causes (must be ≤ the cursor tear), the 0x04 leave frame on both sides (canon frame 206 vs ours — must match), and the fitted-constant count (must drop by 1,19→18).*

**Rules.** Only the named files; no allowlist change; no new harness row; no descriptor or alignment change; the fix is a port of sub_801E754's check — no fitted countdown; every new literal gets a `// provenance:` tag with the cite `asm00_1.s:10478-10514` or the canon symbol; `fitted constants in src/` (HEAD: 19) MUST drop by exactly 1; canon never changes; ≤4 capture runs, one at a time inside the 3-slot semaphore.

**Acceptance.** `SEQ04_FRAMES=60` no longer in src/ (grep proves it); the 0x04 leave frame on `windowclose_full` matches canon's frame 206 on both sides (the sequencer trace says so); the new predicate is cited `asm00_1.s:10478-10514` with a `// provenance:` tag in the same commit; `fitted constants` count drops 19→18; the full harness table identical to HEAD except where the predicate moves cursor's tear (≤10 px); battle_full sequencer remains 540/540 unaligned.

**Measure and report.** row: windowclose, cursor, result, mettaur, wave, opening, chip-cannon (regression set), plus the fitted-constant count. frames: 40 windowclose, 170 cursor, 540 battle_full sequencer. total: fitted-constant count before and after (19 → 18). worst: any pixel row that moves. region: the 0x04 leave frame's diffmask region. commit: src/banner.rs + src/battle.rs. One line of mechanism (sub_801E754's banner-idle check on the modeled composite); one line of what is unverified (whether the banner composite has fields not exercised by `windowclose_full` that the cannon/buster paths would see).

**Coordinator:** dispatch second (after T14 lands, so the banner composite is built on top of T14's end-sequence state machine, not against the peeked SEQ04_FRAMES=60). Worker muse-spark-1.3-contributor, verifier GLM-5.3-flash cross-family; ≤$0.15 expected, ≤$0.40 cap; verify_rows on every row named plus the fitted-constant count. Advances **M2**.

---

### T9h. Gunner port upstream of JumpOffset00 in sub_8026840: the AI handshake byte inside is_MegaMan_in_range? *(OPEN -- 2026-09-14, T9g's named next stop)*

**Files.** tools/states.py (only if a poke fixes the AI handshake), tools/probe.py (the AI handshake watch), src/fixture.rs (enemy_kind = 1 = Gunner per T9c, and panel-row-aware construction), src/objects.rs (the per-type `ForGunner_8113078` entry under enemy_think/enemy_act), src/ai.rs (the per-action tables), assets/ (Gunner art extracted at comp_825BFC4 per T9b), tools/trace.py (only if a watch field must be added), tools/harness.py (one new gunner row aligned by the first attack event), FIXTURE.md (the kind-1 fixture), docs/coverage/battlestart_gunner.md (corrected with the measured handshake)

**Why.** T9g NEGATIVE wrote "the AI handshake byte inside is_MegaMan_in_range? is the next stop". T9g's measured result: "Step 1: 700-frame settle with `--poke-at 165:0x020364C0:0x08`; Gunner slots at f69 CurState=0x04/CurAction=0x00/HP 0x3c, CurAction 0x01 at f151 parks through f699 (549 frames), HP 0x3c and gauge 0x00200000 unchanged, sequencer 0x00→0x04000000 at f166 then held. Step 2: `--poke-at panel-row-3 cheat0x0203a9c3:3` + trigger poke; identical trajectory; is_MegaMan_in_range? does not unstick on row3 alone. Step 3: write-watch 0x020013f0 over 700 frames; 637 unique seed values (~1 draw/frame), AI loop alive at RNG cadence." So the AI loop is alive (637 draws), the trigger unblocks the sequencer (T9e), and the panel row is not the gate (T9g step 2). What remains is the byte `is_MegaMan_in_range?` reads inside `sub_8026840` (asm03_0.s:525-704; called from `sub_8026A28` at asm03_0.s:761, the state-0 self-advance to state 4) — that byte is the AI handshake that decides whether the per-virus AI registers MegaMan as in attack range. T9g's verifier-minimax CONFIRMED the637-draw measurement. Closing this ticket is M5's only remaining step: Mettaur port landed (T6), Gunner art and recipe verified (T9b at comp_825BFC4 byte-for-byte), T9e proved the trigger poke, T9g proved the panel-row assumption wrong — the AI handshake is the named gate. A precise negative here is still a result (it closes M5 with the AI handshake named as the blocker and not the recipe).

**Do.**
1. Write-watch `sub_8026840`'s read set on `battlestart_gunner` from f0 to f700 with `tools/probe.py --watch-write` — the AI handshake byte is whichever read inside `sub_8026840` (asm03_0.s:525-704) flips between f151 (when CurAction becomes 0x01) and f699 (when the trace ends); the candidate set is the panel-coord bytes `0x0203a9c2..0x0203a9c5` (MegaMan's panel) plus the per-virus AI flags at `0x0203ab68..0x0203ab6d` (slot1) and `0x0203ab70..0x0203ab75` (slot0) → *report the byte that flips, the writer PC, and the values on both sides at f151 and f699.*
2. If the byte is a panel-coord: extend the panel-row poke to set the full coordinate canon reads (column included), re-run the same watch → *report whether the Gunner reaches an attack event (HP at 0x0203ab68+0x24 changes from 0x003c or gauge changes from 0x00200000) within 700 frames and at which frame; report an empty log if no movement, that is a result.*
3. If the byte is an AI flag: port `ForGunner_8113078` under enemy_think/enemy_act (`off_8109050[0x17]` / `off_81091D0[0x17]`, asm32.s:10060-10290; CurAction table 0x00-0x0B plus the six per-version parameter tables from byte_80182C4), wire assets/gunner.bin at kind1, add one harness row aligned by the first attack event → *report the row = 0/0 with negative not blind (kind 1 fixture, slot1 = Gunner, slot0 = Mettaur; the mettaur row unchanged at 0/0/70/41734).*
4. Re-run `tools/verify_rows.py` from a clean detached checkout → *report the table identical to HEAD except the new gunner row; mettaur/wave/cursor/opening/chip-cannon unchanged; cursor's tear reported.*

**Rules.** Only the named files; tools/harness.py for the new gunner row only; assets/ for art extracted byte-for-byte at comp_825BFC4 per T9b's measurement; no allowlist change; canon never changes; the kind-1 fixture must be backward-compatible (every existing row unchanged, kind 0 = Mettaur unchanged, T6's `MettaurEntry` path preserved); `fitted constants in src/` (HEAD: 19) may not increase; ≤8 capture runs total, one at a time inside the 3-slot semaphore; every new literal gets a `// provenance:` tag with the cite.

**Acceptance.** EITHER the gunner row = 0/0 with negative not blind (kind 1 fixture, slot1 = Gunner, slot0 = Mettaur) and the trace on the Gunner scenario shows the slot's CurState/CurAction/timers matching canon from spawn through the second attack, OR the report names the AI handshake byte that did not flip and what that rules out — a precise negative closes this ticket the same way T9e/T9f/T9g did; every existing isolated row unchanged; docs/coverage/battlestart_gunner.md corrected with the measured handshake byte.

**Measure and report.** row: gunner (new, aligned by first attack event), mettaur, wave, cursor, opening, chip-cannon (regression set). frames: 700 battlestart_gunner, 70 mettaur, 170 cursor. total: gunner before/after, fitted-constant count (must stay 19). worst: any pixel row that moves. region: gunner's diffmask region. commit: src/fixture.rs + src/objects.rs + src/ai.rs + assets/ + tools/harness.py (the new row) + tools/states.py (the recipe if step 2 passes). One line of mechanism (the AI handshake byte named with its writer PC); one line of what is unverified (whether the byte is per-slot or shared across both viruses).

**Coordinator:** dispatch third (after T14 and T7j land). M2 is the priority over M5; this is the M5 ticket whose measurement prep T9e/T9f/T9g already paid for ($0.04 + $0.10 + $0.15). Worker muse-spark-1.3-contributor (T6/T9c's child class — the Mettaur port and the Gunner recipe are already landed), verifier GLM-5.3-flash cross-family; ≤$0.30 expected, ≤$0.60 cap; verify_rows on every row named (mettaur, wave, cursor, opening, chip-cannon, the new gunner row). Advances **M5**.
