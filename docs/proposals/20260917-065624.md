# Proposed tickets 20260917-065624 (zai/glm-5.3, $0.1396)

### T106. M3 counter hit → Full Synchro: one scripted Cannon hit inside the Mettaur's attack window, the grant ported at canon's damage-path check  *(OPEN -- 2026-09-17)*

**Why.** M3's rule list names "counter hit and Full Synchro" (docs/SCOPE.md M3) and no ticket has ever been dispatched on it — the only mention in TODO/TODO_ARCHIVE is the T102 prose cite inside T105's Why. Measured facts to build on: the emotion byte is 0x0203528F = eStruct2035280+0xf (asm00_2.s:27649), value 1 = Full Synchro, drawn by drawEmotionWindow_801CDEC (asm00_2.s:27554-27583, 23-entry face bank off_801CD08, T105 recon); the Mettaur's attack phase is mapped and the mettaur row reads 0/0/70 (F17/F2/F25, F37h PASS); scripted A presses drive both sides (T7r's battle_full fixture, A@170); chip-cannon fires at 0 (F7's `_chip_canon_0c` route). The counter grant is a damage-path predicate reading the target's attack state — the one site this ticket ports.

**Files.** `src/battle.rs` (the damage-path counter predicate + grant write), `src/objects.rs` (only the enemy attack-state read the predicate cites), `tools/states.py` (ONE scenario `counter_hit` = the mettaur battlestart + ONE scripted A at the measured frame), `tools/harness.py` (ONE row `counter_hit`: canon_ref=N; negative = the same log with the press moved outside the window), `docs/coverage/statuses.md` (append the Full Synchro grant), `docs/worklog/T106.md`. NOT src/emotion.rs (T105's), tools/trace.py, tools/oracle.py, tools/inventory.py, reference/bn6f.

**Do.**
1. Recon: sweep the scripted press across the Mettaur's swing frames on canon with --watch on 0x0203528F; name the frames where the 0→1 write lands, the writer (backtrace cite) and the target field it reads. **measurement.**
2. Baseline, no edit: build ROM sha256+size; verify_rows 7-row guard set (wave, window, mettaur, popup, buster, result, field) + cursor; capture the press-outside-window scenario both sides: emotion byte 0 on both, row total/worst/frames, trace first divergent field. **measurement.**
3. Add `counter_hit` + the row; the SAME button log drives both sides, only the press frame differs between row and negative. **code change + measurement.**
4. Port the predicate + grant at the cited site; the window is canon's own check, no fitted frame range. **code change + measurement.**
5. Re-run: row 0/0/N; trace `counter_hit`: emotion byte first divergence none (same 0→1 frame both sides); negative stays 0 both sides; guard set + cursor identical to step 2. **measurement.**

**Rules.** One button log drives both sides; the poke only seeds the battle (peeked provenance), never the grant; no fitted constants; no allowlist; cursor veto ≤1/1/170/186300; ≤8 captures; tool budget ≤80; one harness command at a time.

**Acceptance.** `counter_hit` 0/0/N with non-blind negative (outside-window press grants nothing on either side); trace: emotion byte (+ the counter latch, if exported) first divergent frame none over the scene; guard set + cursor identical to step 2; verify_rows PASS is the veto. A NEGATIVE naming canon's measured window (frames, writer cite, the field it reads) closes it.

**Measure and report.** rows: counter_hit + 7-row guard set + cursor (cursor 170 frames); the grant cite; totals and first-divergent field before/after; ROM sha256+size; fitted count; commit; one line of mechanism; one line unverified (Full Synchro's damage doubling, next ticket).

**Coordinator:** owns the row; runs alone, dispatched after T105 (shares tools/states.py + tools/harness.py). Free tier: verify_rows from a clean checkout; the verifier for step 1's writer cite. ≤$0.25 expected, ≤$0.50 cap.

**Milestone advanced:** M3 (counter hit + Full Synchro: 0 recorded → 1 recorded + ported; feeds M7's emotion value 1).

---

### T107. M4 chips as data, next family: Bomb onto the record's AttackFamily/AttackSubFamily, with T78's pure-layout pad  *(OPEN -- 2026-09-17)*

**Why.** AS_DATA_FAMILIES = {0x13, 0x15, 0x21} (tools/inventory.py:237, T57's 43-row audit): 14 of the 43 pixel-verified chips dispatch through the record, 29 through our own code. The Bomb family's damage already reads the record (T46 DONE, merged 85f021c: AttackPower +0x1a) but its dispatch is still `match chip.id` — the same gap T48 closed for family 0x15. The cursor veto that closed the Cannon (T51 NEGATIVE), Vulcan (T54 NEGATIVE), StepSwrd (T85 NEGATIVE) and TrnArrw (T91/T94 NEGATIVE) ports is now a landed mechanism: T78 DONE re-landed AreaGrab+Invisibl with cursor 1/1/170 preserved.

**New evidence.** T78 DONE (wt/t78 @ e7ec05d): "Re-land T61's AreaGrab (163) + Invisibl (177) port with F12's pure-layout pad to close the cursor regression" — the mitigation the NEGATIVE family ports lacked.

**Files.** `src/battle.rs` (the Bomb dispatch arm: family + subfamily consts replacing the id match), `src/fixture.rs` (only if a row descriptor must carry the family byte — cite), `tools/harness.py` (row-note updates only; NO new row — the bomb rows exist), `docs/worklog/T107.md`. NOT tools/inventory.py (AS_DATA_FAMILIES is the coordinator's stamp), tools/states.py, reference/bn6f.

**Do.**
1. List T46's Bomb ids (docs/worklog/T46.md, else T46's TODO stamp), their AttackFamily/AttackSubFamily bytes (ChipDataArr.s file:line cites), and every src/battle.rs site that keys them by id. **measurement.**
2. Baseline: build ROM sha256+size; verify_rows 7-row guard set + cursor; the bomb rows' totals (expected 0/0/N each). **measurement.**
3. Port: dispatch on the family const; per-id behaviour from the record's AttackSubFamily and any other record byte canon's own tables use (cite each; name any the record does not carry as `// unnamed:`); apply T78's pad if the build moves cursor. **code change + measurement.**
4. Re-run: every bomb row 0/0/N unchanged; grep the ids' match arms → 0 hits; trace on one bomb scenario: first divergent field none; guard set + cursor identical to step 2. **measurement.**

**Rules.** No fitted constants: every per-id value comes from the record or is named; no allowlist; cursor veto ≤1/1/170/186300; ≤4 captures; tool budget ≤60.

**Acceptance.** All bomb rows 0/0/N unchanged with non-blind negatives; the id-keyed arms gone (grep 0); trace no divergence on one bomb scenario; guard set + cursor identical; verify_rows PASS is the veto. A NEGATIVE naming the record byte that is missing (cite) closes only that sub-part.

**Measure and report.** rows: the bomb rows + 7-row guard set + cursor; before/after totals (both 0/0/N expected — the change is dispatch, not pixels); ROM sha256+size; fitted count; the pad applied or not; commit; one line of mechanism; one line unverified (the family's unprobed ids, T75's K=0 list).

**Coordinator:** stamps AS_DATA_FAMILIES {0x13,0x15,0x21}+Bomb in tools/inventory.py after landing and re-runs `python3 tools/inventory.py`; runs alone, after T105 (shares tools/harness.py). Free tier: verify_rows from a clean checkout. ≤$0.15 expected, ≤$0.30 cap.

**Milestone advanced:** M4 (as-data families 3 → 4; verified-as-data 14 → 14+k of 43).

---

### T108. M5 second virus family as a ported routine: the ai_index-4 rank v0 that battlestart_ai4_rank0 already fields (enemy_idx 0x13) — think+act from T12's roster, row + trace at 0  *(OPEN -- 2026-09-17)*

**Why.** M5 reads 1/187 (only the Mettaur, enemy_idx 1: is_mettaur_row tools/inventory.py:416-421). T9's blocker ("a file-set widening I cannot grant") is gone: T87 DONE landed State battlestart_ai4_rank0 (tools/states.py:261-296, VERIFIED: EVENT_681 flag 60:0x02001d58:0x0240 + iCurrFrame lever 0x37a → list 0x080b50b0 rec3 0x080b50e0, ai_index-4 rank v0, enemy_idx 0x13, slots populate at capture frame 69 of 79). T12 landed the roster (byte_80182C4 → AIThinkTables_8109050[ai_index]: every enemy_idx's think and act entry); Struct2 off_8109150 carries the rank's hp (low 12 bits) and element (high nibble, asm00_2.s:683). T6's MettaurEntry (ForMettaur_8109EF4) and T9j's GunnerEntry are the port shape.

**Files.** `src/objects.rs` (the family's per-type entry: spawn/init + idle + first attack, consts from the roster), `src/battle.rs` (only the spawn/version read the entry cites), `src/fixture.rs` (per-enemy bytes via F38h's descriptor-preserving encoding if the rank needs them), `tools/states.py` (extend battlestart_ai4_rank0's frames if the first attack falls outside 79 — pokes stay byte-identical), `tools/harness.py` (ONE row `ai4_rank0`: canon_ref=N; negative = same scenario without the lever pokes → the default encounter), `docs/coverage/<family>.md` (NEW), `docs/worklog/T108.md`. NOT tools/inventory.py, reference/bn6f.

**Do.**
1. Name the family: the identity row of enemy_idx 0x13 (byte_80182C4 cite), its think/act entries from T12's walk, its Struct2 row (hp + element cites) and sprite category. If the chain is not one act state machine of the shape T6/T9j ported, STOP and report the cites — the port is bigger than this ticket. **measurement.**
2. Baseline: build ROM sha256+size; verify_rows 7-row guard set + cursor; capture battlestart_ai4_rank0 both sides: row total/worst/frames; trace first divergent field + frame (expect the spawn/init group at or after frame 69). **measurement.**
3. Port the entry: spawn/init, idle, and the first attack arm the capture reaches; every const named with `// canon:` cites. **code change + measurement.**
4. Re-run: ai4_rank0 0/0/N; trace battlestart_ai4_rank0: first divergent field none over the scene's frames; negative non-blind; guard set + cursor identical to step 2. **measurement.**

**Rules.** A virus is a port under the interpreters, never a re-creation: behaviour from the disassembly's routine, stats from Struct2, art from canon bytes; pokes keep peeked provenance, canon never changes; no allowlist; cursor veto ≤1/1/170/186300; ≤8 captures; tool budget ≤80.

**Acceptance.** `ai4_rank0` 0/0/N with non-blind negative; trace: first divergent field none over the scene's frames; guard set + cursor identical; verify_rows PASS is the veto. NEGATIVE closes it: step 1's chain-shape stop (with cites), or a measured wedge (the frame the poked battle stops progressing, watch cite).

**Measure and report.** rows: ai4_rank0 + 7-row guard set + cursor; frames N; family name + hp + entry cites; before/after totals and first-divergent field; ROM sha256+size; fitted count; commit; one line of mechanism; one line unverified (the family's other ranks; arms past the first attack).

**Coordinator:** owns the row; runs alone, dispatched after T105-T107 (shares tools/states.py + tools/harness.py). Free tier: verify_rows from a clean checkout; the verifier for step 1's roster cites. ≤$0.30 expected, ≤$0.60 cap.

**Milestone advanced:** M5 1/187 → 2/187 (and M1's viruses count).

---

### T109. M7 first NaviCust battle effect: FirstBarrier granted from the NCP stats byte at canon's battle-init reader (fallback SuperArmor), one poked row  *(OPEN -- 2026-09-17)*

**Why.** navicust battle effects read 0/19 (docs/SCOPE.md M1/M7 row; slot rows DERIVED-FROM-HEADERS). The handler table is FOUND: navicust_jt_NCPs asm/asm37_0.s:2111 (47 words, 45 navicust_NCP_* handlers) dispatched by applyNavicustPrograms_813C684 (asm37_0.s:2012); 32 handlers are SetCurPETNaviStatsByte writes (asm37_0.s:2161-2600). No ticket has touched a battle effect (TODO/TODO_ARCHIVE grep 'navicust' → inventory work only). The Barrier object is already data (family 0x15, T48/T57), so the grant reuses landed behaviour; the mechanism is a pre-battle stats byte the battle reads at init — T105's poke-as-fixture pattern applies unchanged.

**Files.** `src/battle.rs` (the battle-init reader + grant), `src/fixture.rs` (the stats-byte poke field), `tools/states.py` (ONE scenario `ncp_firstbarrier` = the mettaur battlestart + ONE poke of the stats byte), `tools/harness.py` (ONE row `ncp_firstbarrier`: canon_ref=N; negative = no poke), `docs/coverage/navicust.md` (NEW), `docs/worklog/T109.md`. NOT src/emotion.rs, tools/inventory.py, reference/bn6f.

**Do.**
1. Recon: name the FirstBarrier handler in navicust_jt_NCPs, the NaviStats byte it writes, and canon's battle-init reader of that byte (watch-write the byte, backtrace the reader). If no reader is citable in ≤4 commands, switch to SuperArmor (the flinch gate: T67's flinch-exit cite asm00_2.s:18365) and say so in the report. **measurement.**
2. Baseline: build ROM sha256+size; verify_rows 7-row guard set + cursor; capture the mettaur battlestart both sides: no barrier OBJ, mettaur row 0/0/70 re-confirmed. **measurement.**
3. Scenario + row: poke the stats byte on BOTH sides at the same pre-battle frame; capture canon: the barrier OBJ's first frame and the granted amount (cite). **code change + measurement.**
4. Port the reader: our battle init reads the same byte and spawns the Barrier family's object with canon's granted amount. **code change + measurement.**
5. Re-run: ncp_firstbarrier 0/0/N; negative (no poke) identical to step 2 on both sides; trace: the stats byte + the barrier-object field first divergence none; guard set + cursor identical. **measurement.**

**Rules.** Amounts and art from canon bytes/cites, none fitted; the poke is the fixture (peeked provenance), canon never changes; no allowlist; cursor veto ≤1/1/170/186300; ≤6 captures; tool budget ≤80.

**Acceptance.** `ncp_firstbarrier` 0/0/N with non-blind negative; trace: the stats byte and the barrier object first divergent frame none over the scene; guard set + cursor identical to step 2; verify_rows PASS is the veto. A NEGATIVE naming the byte's unmappable reader (search trail: watch results + sites walked) closes it.

**Measure and report.** rows: ncp_firstbarrier + 7-row guard set + cursor; frames N; handler + byte + reader cites; before/after totals; ROM sha256+size; fitted count; commit; one line of mechanism; one line unverified (the other 18 effects, by handler).

**Coordinator:** owns the row; runs alone, dispatched after T105-T108 (shares tools/states.py + tools/harness.py). Free tier: verify_rows from a clean checkout; the verifier for step 1's handler/reader cites. ≤$0.25 expected, ≤$0.50 cap.

**Milestone advanced:** M7 navicust battle effects 0/19 → 1/19 (and M1's navicust row).

---

### T110. M8 results variants, second: the Escape chip's end — the no-rank results window on the landed chip-fire route  *(OPEN -- 2026-09-17)*

**Why.** M8 names "results variants (win/lose/escape/time-out)" and SCOPE records "win results only": the result row reads 0/0/40 (F21d/F21e; F37h PASS). The chip-fire route is landed and at 0 (F7: cannon re-pointed onto F5b's `_chip_canon_0c` route; every chip row 0, F12) and the end-state sequencer table is landed (T7/T7b, TRC2 v3 export). Escape's chip id comes from the name table tools/inventory.py:190-200 reads; its use-site and the sequencer's escape hand-off are this ticket's recon. No ticket has touched escape (TODO/TODO_ARCHIVE grep).

**Files.** `src/battle.rs` (the escape hand-off: chip-use → end state → results window), the results renderer file F21d's block copy lives in (cite it in step 1; only the escape variant's suppressed elements), `tools/states.py` (ONE scenario `escape_end` = the chip-fire route with Escape selected instead of Cannon), `tools/harness.py` (ONE row `escape_end`: canon_ref=40; negative = the same route with Cannon → the win result, unchanged), `docs/worklog/T110.md`. NOT tools/inventory.py, reference/bn6f.

**Do.**
1. Recon: Escape's id from the name table + ChipDataArr.s cite; canon's escape use-site and the sequencer branch it arms (watch the sequencer byte across a canon capture of the route); the result-window elements canon suppresses on escape (rank banner, chip reward). **measurement.**
2. Baseline: build ROM sha256+size; verify_rows 7-row guard set + cursor; result row 0/0/40 re-confirmed; canon-only capture of escape_end: the state walk + the frames where the variant departs from win. **measurement.**
3. Add `escape_end` + the row. **code change + measurement.**
4. Port the hand-off and the suppressed elements (reuse the win window's tiles; skip exactly what canon skips — cite each; no fitted delays, the timing comes from the ported hand-off). **code change + measurement.**
5. Re-run: escape_end 0/0/40; the win result row unchanged 0/0/40 as the non-blind negative; trace: sequencer + results fields first divergence none over the scene; guard set + cursor identical to step 2. **measurement.**

**Rules.** No redraw of result art — canon's own tiles with suppressed elements per cites; no allowlist; cursor veto ≤1/1/170/186300; ≤6 captures; tool budget ≤80; one harness command at a time.

**Acceptance.** `escape_end` 0/0/40; the win result row unchanged 0/0/40 (non-blind negative); trace: sequencer + results fields first divergent frame none; guard set + cursor identical; verify_rows PASS is the veto. A NEGATIVE naming canon's escape refusal predicate on this route (cite + frames) closes it.

**Measure and report.** rows: escape_end + result + 7-row guard set + cursor; frames 40; id + use-site + branch cites; before/after totals; ROM sha256+size; fitted count; commit; one line of mechanism; one line unverified (lose/time-out variants; time-out is netbattle-timer only).

**Coordinator:** owns the row; runs alone, dispatched after T105-T109 (shares tools/states.py + tools/harness.py). Free tier: verify_rows from a clean checkout; the verifier for step 1's cites. ≤$0.25 expected, ≤$0.50 cap.

**Milestone advanced:** M8 results variants 1 → 2 of 4.
