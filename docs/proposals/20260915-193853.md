# Proposed tickets 20260915-193853 (hyper/qwen3.8-flash, $0.0256)

I could not call the pruner (no such tool is exposed to me here) and I have read-only tools, so the tickets are below for you to drop into `docs/proposals/`. **Note on the count:** the run instruction caps this cycle at **2** tickets while the standing template asks for 3 — I honored the cap, so the third seed is named (not admitted) at the bottom.

---

### T22. Settle F47's open (c): rebuild the backdrop art-content test on the ROM's OWN per-step upload list, not the s\*37+k grid  *(OPEN -- 2026-09-15)*

**Why.** F47 (DONE, landed bc2fca7) closed (b) as a signature but left **(c) ART CONTENT: not settled**, because its own test assumed the wrong layout: "best-step matching … against `assets/backdrop.bin` at s\*37+k layout matched only 2-3/37 tiles … the ROM's per-step tile layout in the asset is NOT the s\*37+k grid my check assumed." The true list is already in the repo and needs no new disassembly hunt: `tools/backdrop_export.py:47` `FRAMES` = "blob indices for each animation step, one row of 37 VRAM tiles each" (written by :70-103 into the BNBD v2 header whose layout is at :28-32), and `src/backdrop.rs:71-79` names the seven ROM tables it was matched against -- `docs/renames.md:335`: FRAMES[0]=`byte_807FE40`, [1]=`CD8`, [2]=`C90`, [3]=`D20`, [4]=`D68`, [5]=`DB0`, [6]=`DF8` (`BattleBackdropTiles0_807FE40`…`Tiles6_807FDF8`), scheduled by `BattleBackdropGFXAnimScript_807FB98` (`reference/bn6f/data/dat20.s:140-172`, VERIFIED EXACT: 29 entries, `STEP_ORDER`/`STEP_HOLD` at src/backdrop.rs:83-90). So 2-3/37 is a **wrong-layout artifact candidate**, and the question "does the ROM hold tiles our asset does not?" is answerable from kept bytes plus ≤2 captures on the existing `field` fixture -- no hand-played input, no new state. This is M8's first backdrop item, and M1's `backdrops` line still reads `DERIVED-FROM-RECORDS … byte->art/palette mapping a GAP … 0/3`. Milestone **M8** (and M1's backdrops line).

**Files.** `tools/backdrop_export.py` (read; do NOT regenerate the asset -- if you find a content mismatch, report it), `tools/inventory.py` (the backdrops section only, generator data), `docs/SCOPE.md` (regenerated only, never hand-edited below the marker), `docs/coverage/field_integrated.md`, `docs/worklog/T22.md` (mandatory). **NOT** `src/` (nothing at all), **NOT** `tools/harness.py`, **NOT** `tools/mgba_capture.c`, **NOT** `tools/allowlist.py`, **NOT** `web/`, **NOT** `reference/bn6f` (read-only, never committed), no state or row deleted or created, /tmp's four roots untouched.

**Dependencies.** Follows F47 and its kept edges. `bash tools/worktree.sh t22-backdrop-art-content`.

**Measured from.** Today's main, one commit.

**Do.** 1. Verify the transcription before trusting it: for each of the 7 tables, compare its rows against `FRAMES[step]` and report matched slots **x/37** with the `data/dat20.s` (or neighbouring) line for each table -- 7 numbers; any row that disagrees is the finding, not a typo to smooth over. 2. Re-run F47's step test under the TRUE layout: canon's resident 37 slots (ids 1..37 @ `0x0600E800`, 32 B each, `0x06000000:0x800` window) against `asset[FRAMES[step]]`, at F47's named frames (canon edges k=5,13,21,29,37 = captures 140,148,156,164,172; rust k=8,16,24,32 = 129,137,145,153); reuse `/tmp/bn-f47/{canon,rust}_tiles.bin` if they survive, else re-dump both sides with `tools/probe.py watch … 0x06000000:0x800 --loadstate <the field row's own state>` → report matched/37 per step for both sides. 3. Reproduce the artifact: the same comparison under the naive `s*37+k` index → report both numbers side by side (F47 got 2-3/37) -- this is the control that makes step 2 mean something. 4. Write the chain (Background byte → anim script → 7 tile tables → palette bank 0) into `tools/inventory.py`'s backdrops row with cites (writer `battleSettings_setBackground` asm/asm03_0.s:14592, stage pairs `byte_203CA50`, `battleSettings_802D2B2` :14599), regenerate, and quote SCOPE's line before and after (0/3 → N/3, or the GAP kept with its widened trail). 5. Prove the no-op: `cargo build --release` twice, `cmp -l` the packed `.gba` against main's → **0** differing bytes.

**Rules.** No src/ edit: the byte-identical ROM in step 5 is the proof, and it also means no row can move. No harness row added or changed, no `Align`/frames/region/`extra` touched, no allowlist edit of any kind, canon and canon (sterile) unchanged, `patch_sterile.py` forbidden, negative fixtures stay non-blind. ≤3 capture runs, one at a time. ≤45 tool calls. Any literal you write gets `// canon:` or `// provenance:`; report the fitted-constant count (main: 19 derived 431 peeked 147).

**Acceptance.** One of: **(a) ART FAITHFUL** -- every step matches `FRAMES[step]` on canon's resident set (report all 7 x/37 counts, no threshold invented), AND the naive grid reproduces F47's ~2-3/37 → then F47's (c) is closed and `field`'s integrated residue is **timing-only**, which is what F42/F45/F46/F47 have been circling; **(b) ART DEFECT** -- a named (step, slot, 32-byte hash both sides) triple where the ROM differs from the asset, with the exporter line, asset NOT changed; **(c) NEGATIVE** -- the resident content cannot be attributed to a step at the named frames, with the frame and the reason. Plus: SCOPE's backdrops line moved through the generator with before/after quoted, and ROM cmp = 0.

**Measure and report.** rows: none added; `field` isolated **0/0/40/1139** and `opening` isolated **0/0/40/86591** (name the ui line -- both totals zero, so per docs/measurement-drift.md they need no band and cannot mask a mispair), the built ROM's sha256 and every /tmp canon input's sha256 quoted next to the non-zero `field` integrated 158930/5601/40/261029 (that number is a REPORT QUOTE from docs/measurement-drift.md §1-3, not your gate), the 7×2 per-step counts under both layouts, the SCOPE lines, the fitted count, commit, one line of mechanism, one line of what is unverified.

**Coordinator:** free tier = `verify_rows <branch> field,opening --expect field=0/0/40/1139 --expect opening=0/0/40/86591` plus `cmp` of the two built ROMs (must be 0 bytes; a non-identical ROM means the ticket broke its own rule and does not land). Verifier only for (a)/(b): open one kept dump and re-hash one step's 37 tiles independently. ≤$0.10 expected.

---

### T23. Locate F47's 3-frame art-clock offset: read canon's OWN anim-state record at one paired frame and say SEED or FREE-RUN  *(OPEN -- 2026-09-15)*

**Why.** F47 proved the edges and then said the mechanism is open: "WHAT IS NOT MEASURED: WHERE the phase difference lives -- seed vs free-run is UNMEASURED, and unreachable from this fixture," after **retracting** its own earlier "the 3 frames live in the art-timer SEED" (that cite, src/battle.rs:2385-2403, is the results-window tail plus `prime_backdrop`'s signature; its gate at :2416 is unreachable under `FIELD_ZERO`, tools/harness.py:1564/:2034). It named the smallest step that WOULD measure it: "one `--peek`/`--watch` of canon's art-step counter (adjacent to `eBGScrollCBCounters` at 0x02009690) at one known battle-relative frame, showing its step index runs 3 ahead of ours." The addresses are located and citable without new research: `eGFXAnimStates` at **0x020094c0** (ewram.s:596; 24-byte records -- `&eGFXAnimStates[24*v1]` docs/decomp/asm00_0.c:2325, reader/writer of record+0 :2346-2354, `ZeroFillByWord(eGFXAnimStates, &loc_1B0)` :2304), `eBGScrollCBCounters` at **0x02009690..0x02009698** (ewram.s:619-621; `bg_scroll_cb_counters_struct` = two words, "zeroed once, at battle init" asm/asm00_0.s:3282, the drain writers at :3506-3621). Two captures on the existing `field` fixture. This settles a mechanism claim; it adds no row. Milestone **M8**, and it is the gate on any later backdrop-timing src/ ticket.

**Files.** `docs/coverage/field_integrated.md` (append the phase verdict next to F47's edge table), `docs/worklog/T23.md` (mandatory). **NOTHING ELSE** -- not `src/` (the fix site is named, never edited), not `tools/harness.py`, not `tools/probe.py`, not `tools/mgba_capture.c`, not `tools/allowlist.py`, not `assets/`, not `docs/SCOPE.md`, not `reference/bn6f` (read-only), not `web/`, no state or row created or deleted, /tmp's four roots untouched.

**Dependencies.** Follows F47; run **after T22** if T22 is dispatched (T22's table is what lets you name a step by its resident art). `bash tools/worktree.sh t23-art-clock-phase`.

**Measured from.** Today's main, one commit, zero tree change.

**Do.** 1. Identify the index before making any claim: `tools/probe.py watch <canon rom> <frames> 0x020094c0:0x18 0x02009690:0x8 --loadstate <the field row's canon state> --every 1` across the row's window → print per frame slot 0's six halfwords and both scroll counters, and name which halfword is the **entry index** and which the **frame countdown**, citing the decompiled reader/writer; a halfword that never changes, or changes at non-edge frames, is not the index -- say so. 2. Anchor to F47: the index halfword must change at captures **140,148,156,164,172** and nowhere else in 135..174 → report the five capture numbers you actually measured. If they differ from F47's, **that is the result** (its edges came from tile hashes, not the counter) and the coverage file's table gets the correction. 3. Our side, no src/ change: pair canon's index sequence against F47's rust tile-set edges (captures 129,137,145,153) on one k-axis; if we expose no readable index, say plainly that our step is identified by resident art, not by a counter. 4. Verdict, one line, with the numbers above it: canon's index at the paired k=0 is (i) already ahead by the amount that predicts a 3-frame edge → **SEED**; (ii) equal at k=0 and 3 apart by the first edge → **FREE-RUN**, with the accumulating frame named; (iii) not attributable → **NEGATIVE** with the byte trail. Then name the src/ site each verdict would require, with line numbers, and touch nothing. 5. Prove the no-op: `cargo build --release` twice, `cmp -l` against main's packed `.gba` → **0**; re-run step 2's rows (§Measure).

**Rules.** Tools/docs only; byte-identical ROM is the acceptance, not a nicety. No harness row added or changed, no `Align`/frames/region edited, no allowlist edit, no `--with-main` band (docs/measurement-drift.md §5: it is diagnostic-only and may never gate a merge), canon and canon (sterile) unchanged, no `patch_sterile.py`, never delete or regenerate a /tmp state (`tools/check_inputs.sh` refuses you). ≤2 capture runs total, one at a time, ≤30 tool calls. Every address you quote carries its `// canon: eGFXAnimStates`-style symbol.

**Acceptance.** The identified index halfword with its reader/writer cites; the 5 measured canon edge captures (matching or correcting F47's); a one-line **SEED / FREE-RUN / NEGATIVE** verdict with the paired-frame numbers printed; the src/ fix site named with lines and NOT edited; `cmp` 0 differing bytes; `field` isolated **0/0/40/1139** and `opening` isolated **0/0/40/86591** unchanged (zero totals, so no build identity needed to gate them -- name the ui line you gated). NEGATIVE closes the ticket too: it retires the peek route and redirects the phase question to the tile-content route.

**Measure and report.** rows: field, opening (isolated lines, both zero, before = after, because the ROM is byte-identical); the ROM sha256 you built and the /tmp canon input sha256s; the per-frame halfword table for the 40-frame window (or a bounded excerpt with the full file kept under `/tmp/bn-<runid>/`, dir named next to the numbers); the two edge sets; the verdict; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** ROM-cmp 0 plus `verify_rows <branch> field,opening --expect field=0/0/40/1139 --expect opening=0/0/40/86591`. A verifier IS warranted here (a memory finding, the hand-off item for T22+/any src/ ticket): it must open the kept watch file and confirm the halfword it is told is the index really changes only at the named captures. ≤$0.05 expected.

---

**Deferred, not admitted this cycle (cap of 2):** the verify_rows legibility hazard from seed (c) -- the verdict names which ui line it matched but still prints the isolated line's numbers, so a MATCH on `opening` integrated read as `0/0/40/1139`-shaped text; it is a one-line print change plus a `--selftest` probe, but it advances no SCOPE milestone, so under this cycle's ordering it waits. Also untouched this cycle: F46's salvageable `field-bg1-regs` row and 193 worklog lines on the unmerged `wt/f46-backdrop-registers` (its permanently-FAILED `PORTED_CHECKS` entry and its false register comment stay out); T19's asset widening (5 ROM arc rows + 3 shapes) is the next M4 candidate but re-gates every chip row, so it needs a ROM-version event budget of its own.
