# Proposed tickets 20260917-041154 (zai/glm-5.3, $0.1165)

### T86. M2: scripted input that registers — hold the tap past canon's 2-frame key pipeline  *(OPEN -- 2026-09-17)*

**Why.** M2's chip-selection rules (PA recognition, Regular, Tag) and T20 all need one button log driving both sides, and the synthetic route is broken in the tool, not in canon: T71 PARTIAL step 4 spent 5/5 captures on `--script` with the chipselect selection words STATIC (keys never registered); T80 PARTIAL (landed e7738ea) got only step 3 done and names the blocker "mgba_capture.c's scripted-input". Mechanism: tools/mgba_capture.c:413-439 parses `Key@frame` taps, and :896-898 applies each tap for exactly ONE frame (`taps[t].frame == i`), but canon's input pipeline reads the pad on a 2-frame cadence and merges taps closer than ~6 frames (docs/worklog/T71.md:121) — a 1-frame tap lands in the unread frame and is lost. Fixing this unblocks T80 steps 4-6 (M2 PA recognition, M4 0/63), Regular/Tag, and T20's shared-log premise. Milestone **M2** (the interaction-rule measurement surface).

**New evidence.** T80 PARTIAL (2026-09-17, wt/t80 @ e7738ea): steps 4-9 NOT landed, "blocker remains mgba_capture.c's scripted-input (7ag owns it, off-limits to this ticket)" — this ticket owns it. T71 PARTIAL step 4: 5/5 captures, selection words static; bisect suspects named in order (a) `--script` Left@30 hold-vs-tap, (b) the 2-frame pipeline gap, (c) Start+A ordering. The `--a-ticks` hold path (mgba_capture.c:893-895) already works — chip rows fire chips through it — so the defect is the tap path alone.

**Files.** `tools/mgba_capture.c` (tap application :896-898 only), `tools/covstep.c` (sync the same named const, :202-208 uses the same syntax), `docs/coverage/input_script.md` (NEW — the working script + hold + watch addresses), `docs/worklog/T86.md`. **NOT** `src/` (ROM byte-identical), `tools/harness.py`, `tools/states.py` (chipselect + pa_chipselect_state exist), `tools/allowlist.py`, `tools/trace.py`, `reference/bn6f`.

**Do.**
1. Baseline, no edit: grep tools/harness.py for rows passing a script → report the list (expect none landed); capture REAL + chipselect.state with `--script "Left@30"` + per-frame peek of the cursor word (docs/worklog/T71.md step-4 address; hand list byte_20349C0) → words static frames 25..40; guard rows wave,window,opening,chip-cannon,mettaur,windowclose,cursor → expect 0/0/N and cursor 1/1/170. **measurement.**
2. Fix: named `TAP_HOLD_FRAMES` const applied as `i >= frame && i < frame + hold` at :896-898, `// provenance:` the 2-frame pipeline (T71.md:121); bisect hold ∈ {1,2,3} → the minimum that moves the cursor word exactly one slot; report it. **code change + measurement.**
3. Multi-key: `Left@30,Left@40,A@70,A@90,A@110` (gaps ≥ the measured merge window) → 3 picks, OK collapse; PA result word at 0x02036660 (written via sub_802B6F2 asm03_0.s:10881) changes; report the exact working script line + the word. **measurement.**
4. Both sides: same script on canon (REAL + pa_chipselect.state) and rust (plain_rom + descriptor) → the watched words move identically on both; report both sequences. **measurement.**
5. verify_rows on the step-1 guard set from a clean checkout → identical; ROM sha256+size unchanged. **measurement.**

**Rules.** Tooling only; no src/, ROM byte-identical. The `--a-ticks` path (:893-895) untouched. Minimum hold wins — a larger hold is a fitted constant. No row config change; if step 1 finds a landed row using `--script`, its numbers must be identical after or the branch stays unmerged. Negative fixture: hold=1 replay shows static words (non-blind). ≤6 captures, tool budget ≤60.

**Acceptance.** A named script (keys, frames, hold) that (a) moves the chipselect cursor word one slot, (b) completes 3 picks + OK, (c) writes 0x02036660, on BOTH sides through the same tool; guard set identical; hold=1 negative still static; covstep.c carries the same const. NEGATIVE naming which key/frame still fails (with the watch word static at that frame) closes it.

**Measure and report.** rows: the 7-row guard set; frames 90/16/40/40/70/40/170. Watch-word sequences before/after, hold value, script line, guard totals/worst identical, ROM sha256+size, commit; one line of mechanism; one line unverified.

**Coordinator:** owns tools/mgba_capture.c + tools/covstep.c; pair with T88 (src/battle.rs, disjoint); never beside T87 (needs this landed first). Free tier: verify_rows on the guard set. ≤$0.25 expected, ≤$0.50 cap. Advances **M2** (unblocks T20, T87, Regular/Tag).

---

### T87. M2: PA recognition as data — PA_RECIPES walk in src/custom.rs + the pa_recog row (finish T80 PARTIAL)  *(OPEN -- 2026-09-17)*

**Why.** T80 PARTIAL (landed e7738ea) completed only step 3: tools/states.py `State.patches` + `pa_chipselect_state` (base=CHIPSELECT, patches=((0x5DDC0,0x01),), /tmp sha f9935ded…f79f). Steps 4-9 (script bisect, PA_RECIPES port, pa_recog row, pad ladder, verify_rows, coverage doc) are unlanded and were blocked on scripted input — removed by T86. T71 decoded all 63 recipes: `PARecipePtrsA` 43 ptrs at off_802BCB0 + 20 at off_802BC60 (B dead in this build); record `[count u8][matcher u8][result u16][ingredient u16]×count`, 30 m=0 + 33 m=4; reader `custMenuPressOK_8028D3A` asm03_0.s:5437 → sub_8029110 :6029 → `matchPARecipes_8029520` :6643; result stored at **0x02036660** via sub_802B6F2 :10881. SCOPE M4 program advances **0/63**; M2's "PA recognition" rule unmet.

**New evidence.** T86 (must be landed; quote its script line + `TAP_HOLD_FRAMES` here) removes T80's named blocker. T80's step-5 plan stands: `PA_RECIPES` const in src/custom.rs, walk on the OK-press branch of `navigate()` BEFORE `Phase::Closing`, sliding window over `self.picks` (id=word&0x1ff, code=word>>9; m=0 id-exact + ascending-code run ≤1 wildcard, m=4 id-exact), rust-side watch mirror at **0x0203FF00**. eBattleFolder=0x0203CDC0, state-file patch site 0x5DDC0.

**Files.** `src/custom.rs` (PA_RECIPES const + walk + mirror), `src/deck.rs` (id/code exposure only if missing), `tools/states.py` (extend pa_chipselect_state patches to the full recipe folder: folder[0]=0x0001 Cannon-A, folder[1]=0x000C TankCan1-A at file 0x5DDC0, `peeked`), `tools/harness.py` (ONE new row `pa_recog`), `docs/coverage/chips.md`, `docs/worklog/T87.md`. **NOT** tools/mgba_capture.c (T86's), tools/allowlist.py, tools/trace.py, src/battle.rs (T88's), src/objects.rs, src/fixture.rs, tools/inventory.py, reference/bn6f.

**Do.**
1. Baseline, no edit: build, sha256; verify_rows guard set (wave,window,opening,chip-cannon,mettaur,windowclose,cursor) → 0/0/N, cursor 1/1/170; `python3 tools/inventory.py` → quote PA line 0/63. **measurement.**
2. Extend the state patches to the two-word folder; peek both words from the built state (byte-diff vs chipselect.state at 0x5DDC0). **measurement.**
3. Port: `PA_RECIPES` generated from T71's parser output (cite the byte_802Bxxxx sites asm03_0.s:114xx), walk on the OK press before Phase::Closing; on match rewrite picks to the collapsed hand and write_volatile the result word to 0x0203FF00. **code change.**
4. Row `pa_recog`: canon = REAL + patched state + T86's script + watch 0x02036660; rust = plain_rom + descriptor (deck=[1,12,71,54,1], codes, window pick fields) + same script + watch 0x0203FF00; align on the marker; negative = same route WITHOUT the A presses (watch stays 0xFFFF, cursor-walk pixels still move = non-blind). Run it → 0/0/N. **measurement.**
5. verify_rows on the full row table + pa_recog from a clean checkout → all identical except pa_recog new at 0; inventory PA line 0/63 → 63/63. **measurement.**

**Rules.** Recipe table generated from the parser, no per-PA `match result_id`. Picked-word view reads Deck::id/code, no fitted count. No allowlist, no patch_sterile, canon never changes; state patch `peeked`; mirror is rust-only EWRAM top page. Cursor ≤1/1/170. ≤8 captures, tool budget ≤80.

**Acceptance.** Trace target: field = the PA-result word (canon 0x02036660 vs rust mirror 0x0203FF00), frames = the row's window, **first divergent frame none**; pa_recog 0/0/N with non-blind negative; inventory PA 0/63 → 63/63; guard set + cursor identical. NEGATIVE naming the recipe-site mismatch (record bytes vs walk arm) closes it.

**Measure and report.** rows: pa_recog + 7-row guard set. Both watches' word sequences at the collapse frame, before/after totals/worst/region, recipe count 63, both ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified.

**Coordinator:** dispatch ONLY after T86 lands (its script line is this ticket's input); owns src/custom.rs + tools/harness.py:pa_recog + tools/states.py — run alone, never beside T88 (both touch tools/harness.py). Free tier: verify_rows + inventory rerun. Verifier for step 3's table-driven claim. ≤$0.30 expected, ≤$0.70 cap. Advances **M2**, then **M4**.

---

### T88. M4: Spreadr1-3 (ids 9-11) as data — the next unrecorded family off the record's own attack bytes  *(OPEN -- 2026-09-17)*

**Why.** SCOPE chips 43/411: ids 1-8 are verified (Cannon family + Vulcan1-3/SuprVulc verified-pixels, AirShot verified), and ids 9-11 Spreadr1/2/3 (data/ChipDataArr.s:282/313/344, 30/60/90 dmg, LMN*/ABC*/QRS*) are the first unrecorded standard block. T75 NEGATIVE measured K=0 unprobed ids left in the record-driven families {0x13,0x15,0x21} — M4 only moves by porting a NEW family, and T17/T48/T52 fixed the shape: dispatch on the record's AttackFamily/AttackSubFamily bytes through the ROM's own tables, never `match chip.id`. StepSwrd (T85 NEGATIVE) closed the 0x13 tail; this is a fresh family. T51's lesson is a hard rule: a family port must not regress cursor (1/1/170; F12's pure-layout pad, T78, is the known fix). Milestone **M4**.

**New evidence.** T85 NEGATIVE (2026-09-17): the 10 family-0x13 rows PASS 0/0/40 with T17 negatives — HEAD's chip surface is green, so any movement here is this ticket's. T57 DONE census: 43/411 verified; AS_DATA_FAMILIES={0x13,0x15,0x21} in tools/inventory.py. T52 DONE is the template row (chip-airshot 0/0/40, non-blind negative 4602).

**Files.** `src/battle.rs` (use_chip + chip_strike Spreadr arms → record dispatch), `src/chips.rs` (accessors only if missing), `tools/inventory.py` (AS_DATA_FAMILIES += the measured family byte; no parser rewrite), `tools/harness.py` (rows chip-spreadr1/2/3 + negatives), `tools/states.py` (only if the existing chip fixture cannot field id 9 — check first), `docs/coverage/chips.md`, `docs/worklog/T88.md`. **NOT** tools/allowlist.py, tools/trace.py, tools/mgba_capture.c (T86's), src/custom.rs (T87's), src/objects.rs, src/fixture.rs, reference/bn6f.

**Do.**
1. Baseline, no edit: verify_rows on the guard set + the 10 sword/blade rows + chip-vulcan1..3 + chip-airshot → expect all 0/0/N, cursor 1/1/170; quote inventory M4 line 43/411. **measurement.**
2. Read ChipDataArr.s:282/313/344 → name family/subfamily bytes; find the ROM's chip-object routine that byte dispatches to (the table the record's attack bytes feed); cite file:line for the routine and its per-rank damage pickup. **measurement.**
3. Port the family arm as record dispatch (no `match id { 9..11 }`); every literal named `// canon:` or `// provenance:`. **code change.**
4. Add the three rows on the chip-cannon fixture pattern (hand poke / descriptor deck with id 9/10/11); run → expect 0/0/40 each, negatives non-blind (different-chip or no-chip fire). **measurement.**
5. verify_rows full chip set + cursor from a clean checkout → all prior rows identical, cursor ≤1/1/170, fitted count not increased; inventory M4 43/411 → 46/411. **measurement.**

**Rules.** Record-driven only; a subfamily byte that cannot be expressed from the record is reported, not tuned. No fix-up table, no allowlist, no patch_sterile, canon never changes. Any prior chip row non-zero or cursor above 1/1/170 → branch unmerged and reported. ≤6 captures, tool budget ≤80.

**Acceptance.** chip-spreadr1/2/3 0/0/40 with non-blind negatives; guard set + all 43 prior chip rows identical; AS_DATA_FAMILIES grows by the measured byte; SCOPE M4 **43/411 → 46/411**; the dispatch cite carries file:line. NEGATIVE naming the family byte / routine gap (which arm diverges, at which frame) closes it.

**Measure and report.** rows: chip-spreadr1/2/3 + guard set + 10 sword/blade + 3 vulcan; frames 40 each. Totals/worst/region before/after, family+subfamily bytes, both ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified.

**Coordinator:** owns src/battle.rs + tools/inventory.py; pair with T86 (tools/mgba_capture.c, disjoint); never beside T87 (tools/harness.py). Free tier: verify_rows on the chip set + cursor. Verifier for step 2's dispatch cite. ≤$0.40 expected, ≤$0.80 cap. Advances **M4**.
