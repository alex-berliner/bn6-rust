# Proposed tickets 20260917-072323 (zai/glm-5.3, $0.1620)

### T106. M7 emotion window, pass 2 — the `emotion_syn` row and the FACE_INDEX gate (continue T105 OPEN)  *(OPEN)*

**Why.** T105 is OPEN with everything but the port measured (docs/worklog/T105.md): plain ROM sha256 0d3ea6d3, 584392 B; 7-row guard set isolated PASS (opening 0/0/40, mettaur 0/0/70, cannon 0/0/40, buster 0/0/28, chip-use 0/0/30, wave 0/0/90, windowclose 0/0/40); cursor 1/1/170/186279 within the ≤1/1/170/186300 veto; `--watch 0x0203528F` reads 0x00 across 40 frames from battlestart.state on canon (calm baseline confirmed). Recon complete and stamped: emotion byte = eStruct2035280+0xf = 0x0203528F (asm00_2.s:27649); drawEmotionWindow_801CDEC (asm00_2.s:27554-27583, HUD element 14) skips the face when val==5||6; face bank off_801CD08 = 23 entries, stride 0x180, shared 0x80-byte right half dword_872D914; vals 15..19 alias 5..9; src/emotion.rs:11 still draws only the calm face. SCOPE M7 emotion reads 0/25. Test value 1 (T105's pick). This is the second and last proposal on this objective today.

**Files.** `src/emotion.rs` (FACE_INDEX gate), `assets/emotion.bin` (23 slots), `tools/emotion_export.py` (extend to 0x1B30 bytes), `src/fixture.rs` (descriptor emotion field; buffer 64→66 or overlay — decide in step 1), `tools/states.py` (ONE scenario `emotion_syn_full` = battlestart.state + poke 60:0x0203528F:0x01), `tools/harness.py` (ONE row `emotion_syn`, canon_ref=40), `docs/coverage/emotion.md`, `docs/worklog/T106.md`. **NOT** src/battle.rs, src/objects.rs, tools/trace.py, tools/oracle.py, tools/allowlist.py, reference/bn6f.

**Do.**
1. Extend tools/emotion_export.py to all 23 slots; rebuild assets/emotion.bin; report every slot's offset/length + the BNEM header; decide descriptor 64→66 vs overlay (F38g's 67-byte lesson) and name it. **measurement.**
2. Baseline, no src/ edit: guard set + cursor at T105's numbers; capture chip-cannon, read the OBJ tiles at (0,18)/(32,18) both sides — calm. **measurement.**
3. Add `emotion_syn_full` + row `emotion_syn` (canon = REAL + poke, rust = plain ROM): poked canon must show a non-calm face at (0,18)/(32,18) — name differing tile ids and frames. **measurement.**
4. Port the gate in src/emotion.rs: read 0x0203528F (peeked), index the bank through a cited const FACE_INDEX, honor skip values 5/6 and the 15..19 alias; rebuild, sha256+size. **measurement.**
5. Re-run: `emotion_syn` 0/0/40; negative (same scenario WITHOUT poke) 0/0/40 both sides calm AND poked-canon vs plain-rust NON-zero (not blind); guard set + cursor identical to step 2. **measurement.**

**Rules.** Art from canon bytes only; FACE_INDEX cited from drawEmotionWindow_801CDEC's per-value sites; the poke carries peeked provenance; cursor veto ≤1/1/170/186300; no allowlist, no patch_sterile, canon never changes. ≤6 captures, tool budget ≤80.

**Acceptance.** `emotion_syn` 0/0/40 with the non-blind negative; OBJ tiles at (0,18)/(32,18) match canon for value 1; guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming canon's measured no-face-change (frames + bytes + cite) closes it.

**Measure and report.** rows: emotion_syn + 7-row guard set (40 frames each, cursor 170); per-slot offsets, before/after pixel totals, differing OBJ tiles, ROM sha256+size, fitted count, commit; one line mechanism; one line unverified (the other ~22 values).

**Coordinator:** owns src/emotion.rs + the asset + states/harness; runs alone (T105's file set). Free tier: verify_rows clean checkout on the 8-row set; verifier for step 1's offsets. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M7 (emotion window 0/25 → 1/25, first port).

---

### T107. M1 navicust battle effects — the first effect end to end: SuperArmor's flinch gate read off the PET stats byte  *(OPEN)*

**Why.** SCOPE M1's navicust battle-effects row is 0/19 with the table FOUND and measured: navicust_jt_NCPs asm/asm37_0.s:2111 (47 words stride 4: 45 navicust_NCP_* + GigFldr1 + a no-op stub), dispatched by applyNavicustPrograms_813C684 (asm37_0.s:2012, index = sub_813B9FC(id-1) record halfword >> 2), handlers 32× SetCurPETNaviStatsByte + 11× GetCurPETNaviStatsByte (asm37_0.s:2161-2600), give/take chain asm03_1_1.s:8794 → GiveItem 803cd98 → reloadCurNaviStatBoosts_813c3ac → applyNaviStatsMaybe_813C458; fields per include/rom_structs/NaviStats.inc. No ticket has ported a battle effect (grep TODO/TODO_ARCHIVE "navicust": T15's table walk only). The missing half is the battle-side reader; the most isolated measurable one is SuperArmor (no flinch on hit): the player hit path is already at 0 (mettaur 0/0/70; F1/T67 landed the flinch behaviour), so a stats-byte poke that suppresses the flinch is a clean delta on both sides, no scripted input.

**Files.** `src/battle.rs` (the player hit/flinch gate only; if the arm lives in another src file, name it in step 1 and touch only that), `tools/states.py` (ONE scenario `mettaur_armor` = the mettaur recipe + one-shot poke of the SuperArmor stats byte at the frame --watch names), `tools/harness.py` (ONE row `armor`), `docs/coverage/navicust.md` (NEW: the 47-word table + chosen byte + every reader), `docs/worklog/T107.md`. **NOT** src/objects.rs, src/emotion.rs, tools/inventory.py, tools/allowlist.py, reference/bn6f.

**Do.**
1. Walk navicust_jt_NCPs + NaviStats.inc: name the SuperArmor handler, the stats-byte index it SetCurPETNaviStatsByte's, and EVERY battle-side GetCurPETNaviStatsByte caller that reads it (file:line); if no per-byte battle reader exists, the full caller census closes the ticket NEGATIVE. **measurement.**
2. Baseline, no edit: guard set + cursor 1/1/170/186279; --watch the stats byte over the mettaur scenario; name the poke frame. **measurement.**
3. Add `mettaur_armor` + row `armor` (canon = REAL + poke, rust = plain ROM): canon's MegaMan must NOT flinch where the mettaur row's does (frames + mm action/anim values named). **measurement.**
4. Port the gate: our flinch arm reads the same peeked byte at canon's reader site; rebuild, sha256+size. **measurement.**
5. Re-run: `armor` 0/0/70; negative poked-canon vs plain-rust NON-zero (flinch frames differ — not blind); mettaur 0/0/70 unchanged; guard set + cursor identical to step 2. **measurement.**

**Rules.** The stats byte gets a named const with provenance (canon: its NaviStats field); the poke carries peeked provenance; cursor veto ≤1/1/170/186300; no allowlist, no patch_sterile, canon never changes. ≤6 captures, tool budget ≤80.

**Acceptance.** `armor` 0/0/70 non-blind; mettaur + 7-row guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE with the full GetCurPETNaviStatsByte caller census (every caller cited) closes it honestly.

**Measure and report.** rows: armor, mettaur, guard set, cursor (70/170 frames); byte address+name, handler cite, reader cite, flinch frames before/after both sides, ROM sha256+size, fitted count, commit; one line mechanism; one line unverified (the other 18 effects).

**Coordinator:** owns the flinch-gate site + states/harness — NOT pairable with T105/T106 (states/harness overlap) or T108 (battle.rs overlap); runs alone or after them. Free tier: verify_rows on the 9-row set. Verifier: step 1 cites. ≤$0.15 expected, ≤$0.35 cap.

**Milestone advanced:** M1 (navicust battle effects 0/19 → 1/19; M7's NaviCust clause).

---

### T108. M4 Bomb family 0x12, second arm — behaviour from AttackSubFamily, twelve id-keyed rows as data  *(OPEN)*

**Why.** T57's audit (docs/worklog/T57.md) measured the split: 14 of 43 pixel rows dispatch through the record, 29 by id; twelve of the 29 are family 0x12 — MiniBomb 54/0x00, EnergBom 55/0x01, MegEnBom 56/0x01, LilBolr1 98/0x03, BlkBomb 60/0x06, BugBomb 67/0x07, VDoll 150/0x08, IceSeed 69/0x09, PoisSeed 70/0x0C, GrasSeed 68/0x0D, FlshBom1 57/0x0E, BigBomb 202/0x0F — consumed by use_chip arm src/battle.rs:4175, strike arm :4483, per-id physics :4550-4580, ener/flash/seed selectors by id (:4596-4604); the `wide` byte :4541 already reads the record. T46 DONE (85f021c) landed the family's damage arm (AttackPower +0x1a) and T57 named the follow-up ("widening the rule to subfamily pairs"). Twelve rows flip from verified-pixels to verified-as-data with no new captures — every row is already 0 — the largest single M4 count move available (43/411 → 55/411). The family's last ticket (T46) was DONE.

**Files.** `src/battle.rs` (the family-0x12 sites only: :4175, :4483, :4550-4580, :4596-4604), `tools/inventory.py` (the subfamily-axis vocabulary T57 named), `docs/coverage/chips.md`, `docs/worklog/T108.md`. **NOT** tools/states.py, tools/harness.py (no new rows — the twelve existing rows are the test), tools/allowlist.py, reference/bn6f.

**Do.**
1. Table: every ChipDataArr_8021DA8 id with attack_family (+0xb) == 0x12 — id, AttackSubFamily (+0xc), the id-keyed byte it currently feeds, and whether a subfamily is shared by ids that must differ (name the record byte carrying the difference); any id that must stay id-keyed is named with its record-byte evidence. **measurement.**
2. Baseline: the twelve rows + 7-row guard set + cursor at HEAD (ROM sha256+size). **measurement.**
3. Rewrite the arms: dispatch on family 0x12 + AttackSubFamily (+0xc) — flight/gravity/vx, ener/flash/seed, rest/anim — so no `match chip.id`/CHIP_* arm selects behaviour for a 0x12 id; rebuild, sha256+size. **measurement.**
4. Mutation negative, then restore: point one subfamily at a neighbour's arm and show a named row goes non-zero (frames + region); restore. **measurement.**
5. Re-run the twelve rows + guard + cursor: every row 0/0/N unchanged; extend tools/inventory.py's rule to family+subfamily pairs, rerun `python3 tools/inventory.py`; report the new M4 count. **measurement.**

**Rules.** A row that leaves 0 is a defect, not a trade; the vocabulary change cites T57 step 3; cursor veto ≤1/1/170/186300; no allowlist, no patch_sterile, canon never changes. ≤4 captures, tool budget ≤80.

**Acceptance.** All twelve family-0x12 rows 0/0/N unchanged with the step-4 mutation negative; no chip.id behaviour arm remains for family 0x12; inventory reports 55/411; guard set + cursor identical; verify_rows PASS is the veto. NEGATIVE naming the ids that must stay id-keyed (record bytes cited) closes it.

**Measure and report.** twelve rows + guard + cursor, before/after (totals identical); ROM sha256+size; the id→subfamily table; fitted count; commit; one line mechanism; one line unverified (the family-0x12 ids with no pixel row).

**Coordinator:** owns src/battle.rs's 0x12 sites + inventory; serializes behind any battle.rs ticket (T107); pairs with tools/states.py-only tickets. Free tier: verify_rows on the 20-row set. Verifier: the subfamily table. ≤$0.10 expected, ≤$0.25 cap.

**Milestone advanced:** M4 (43/411 → 55/411 as data; T46's family finished).

---

### T109. M5's second virus on the scoreboard — the ai=4 rank T87 already fields, as a per-type port or a zero-port ride  *(OPEN)*

**Why.** T87 DONE (6e493bc) landed State battlestart_ai4_rank0: EVENT_681 flag poke 60:0x02001d58:0x0240 swaps the encounter root to 0x080b50b0 (12 ungated records), lever 60:0x0200a210:0x37a → 891 mod 12 = 3 → rec3 0x080b50e0 fields ai_index-4 rank v0 (NameID 0x0013) at frame 69, slot1 HP 0x5a byte-matching off_8109150[4] elem_hp (0x0810ae4c, stride 6); 40-frame determinism 0 px across two builds; guard 7/7 PASS. T58 PARTIAL promised "every virus rank whose identity row carries ai_index 4 needs no port, only a recording, a trace and pixels" — but T87's Why corrects the model ("the ported Mettaur routine rides ai_index 1; the live trio is ai 1/7/17 — NOT 4"), so whether off_8109050[4] is ForMettaur_8109EF4 (zero port) or its own routine (a T6-pattern port) is unmeasured. M5 is 1/187; this is the cheapest second row: recording, lever and byte-match already exist on main. Not the closed Gunner objective — different family, different route.

**Files.** `src/objects.rs` (the ai=4 per-type entry, T6's MettaurEntry pattern; zero diff if step 1 proves the routine already ported), `tools/states.py` (extend battlestart_ai4_rank0's frames only if the row needs more; no other recipe changes), `tools/harness.py` (ONE row `ai4` aligned at the frame-69 populate event), `docs/coverage/viruses.md` (NEW: the ai=4 family row), `docs/worklog/T109.md`. **NOT** src/battle.rs, src/ai.rs, tools/rom_enemy_tables.py, tools/allowlist.py, reference/bn6f.

**Do.**
1. Resolve the contradiction: print tools/rom_enemy_tables.py's ai=4 row (think/act symbols + cites) and state which it is — off_8109050[4] == ForMettaur_8109EF4 (asm31.s:170982, zero port) or a distinct routine (name it); plus the family's element and NameID 0x0013's name. **measurement.**
2. Baseline, no edit: capture battlestart_ai4_rank0 both sides from HEAD; guard set + cursor; report the populating frame, slots' NameID/HP, and our side's first divergent enemy field over 60 frames. **measurement.**
3. Add row `ai4` (canon = REAL state, rust = plain ROM; negative = the base state WITHOUT the EVENT_681 poke — it fields the default trio and must differ). **measurement.**
4. If step 1 says port: port the routine into src/objects.rs following MettaurEntry (cite per arm); if zero-port: no src change, and say so. Rebuild, sha256+size. **measurement.**
5. Re-run: `ai4` 0/0/60 non-blind; trace over the scene's frames: enemy_state_action/enemy_anim/enemy_hp first divergence **none**; guard set + cursor identical to step 2. **measurement.**

**Rules.** The virus is a port under the interpreters, never a re-creation; every behaviour cites reference/bn6f file:line; cursor veto ≤1/1/170/186300; no allowlist, no patch_sterile, canon never changes; only the named State's frames may change. ≤6 captures, tool budget ≤80.

**Acceptance.** `ai4` 0/0/60 with the no-poke negative reading non-zero; the trace's enemy fields first divergence **none** over the scene's frames; guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming the measured blocker (e.g., the ai=4 routine's spawn art or a lever beyond the row's frames) closes it.

**Measure and report.** rows: ai4 + 7-row guard + cursor (60/170 frames); step 1's resolution with cites; populating frame; before/after totals; ROM sha256+size; fitted count; commit; one line mechanism; one line unverified (the ai=4 family's other five ranks).

**Coordinator:** owns src/objects.rs + the row; pairs with T108 (battle.rs+inventory, disjoint); NOT with T105/T106/T107 (states/harness). Free tier: verify_rows on the 9-row set. Verifier: step 1's resolution. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M5 (1/187 → 2/187).

---

### T110. M8's second arena — one non-default Background byte as a pixel row (byte→art already measured)  *(OPEN)*

**Why.** M8 reads "one arena; win results only". T70 DONE closed M1's mapping gap with measurement: BattleSettings.Background (+0x4; writer battleSettings_setBackground asm03_0.s:14591-14593) is read at battle init by sub_8081308 (asm21.s:469-523, ldrb at :473-475) and used as a TABLE INDEX by sub_8080DA0 — off_8080F98[r] (asm21.s:32) → LoadBGAnimData (asm03_0.s:21209), off_8081220[r] (:36) — census 0xff ×1047 (map default), 0x07 ×192, 0x08 ×1 (singleton at 0x080aee70), id 6 = RobotControlComp art. T22 verified our current arena slotwise 37/37 ×7 steps against assets/backdrop.bin; src/backdrop.rs has no second art. One poked Background byte is the cheapest second arena: 0x08 if it is art 7's palette variant (step 1 measures), else id 6.

**Files.** `src/backdrop.rs` (art selection by the poked byte), `src/fixture.rs` (descriptor backdrop field; buffer-width decision named in step 1), `tools/backdrop_export.py` (extend to the chosen id's anim), `assets/backdrop.bin` (append the second anim, layout documented), `tools/states.py` (ONE scenario `bg_alt` = the battlestart recipe + one-shot poke of BattleSettings+0x4 = 0x0200AF64 at the frame --watch names, value = chosen id), `tools/harness.py` (ONE row `bg_alt`), `docs/coverage/backdrops.md`, `docs/worklog/T110.md`. **NOT** tools/allowlist.py, tools/trace.py, reference/bn6f.

**Do.**
1. Read off_8080F98/off_8081220 rows for ids 6/7/8 from the ROM; diff each LoadBGAnimData tile/map/palette pointer set against the current arena's (T22's cites); report which of 8 or 6 is the cheapest DISTINCT art and its pointer set; extend tools/backdrop_export.py, rebuild the asset, decide the descriptor field vs overlay. **measurement.**
2. Baseline, no edit: guard set + cursor at HEAD (ROM sha256+size). **measurement.**
3. Add `bg_alt` + row `bg_alt` (canon = REAL + poke, rust = plain ROM under the same state): canon must show the second arena's tiles — name the first differing frame and the BG1 cells that change; negative without the poke = the default art, identical to today's rows. **measurement.**
4. Port the selection in src/backdrop.rs: honor the poked byte at canon's read chain (cite :473-475, :32), load the appended anim; rebuild, sha256+size. **measurement.**
5. Re-run: `bg_alt` 0/0/60 non-blind (no-poke side stays the default art AND poked-canon vs plain-rust reads non-zero); T22's slotwise art-content test rerun on the new id; guard set + cursor identical to step 2. **measurement.**

**Rules.** Art from canon bytes only; the poke carries peeked provenance; no allowlist, no patch_sterile, canon never changes; cursor veto ≤1/1/170/186300 — backdrop edits regressed cursor before (F37i/F37j), so any movement beyond the veto is a reported defect. ≤6 captures, tool budget ≤80.

**Acceptance.** `bg_alt` 0/0/60 non-blind; the slotwise art-content test PASS for the new id; guard set + cursor identical to step 2; verify_rows PASS is the veto. NEGATIVE naming the measured blocker (e.g., the id's anim needs a loader path our engine cannot schedule) closes it.

**Measure and report.** rows: bg_alt + 7-row guard + cursor (60/170 frames); chosen id + pointer set with cites; slotwise counts; before/after totals; ROM sha256+size; fitted count; commit; one line mechanism; one line unverified (the remaining art ids).

**Coordinator:** owns src/backdrop.rs + the exporter + states/harness; pairs with T108 (disjoint); NOT with T105/T106/T107/T109. Free tier: verify_rows on the 9-row set. Verifier: step 1 pointer set. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M8 (arenas 1 → 2; M1 backdrops gains its first pixel row beyond the default art).
