# Proposed tickets 20260914-201644 (minimax/MiniMax-M3, $0.1184)

```
### T7g. The battle's tail as canon's sequencer arms: the five integrated rows from allowlisted to 0, and the AUDIT-6 entries from 4 to 0  *(OPEN -- 2026-09-14, follows T7f, advances M2)*

**Files.** src/battle.rs (the end sequence: `over`, BANNER_TO_RESULTS, the results hand-off), src/results.rs (the slide driver), src/banner.rs (what the 0x0C arm tears down), tools/harness.py (the integrated rows' Align notes only), tools/allowlist.py (deletions only), docs/coverage/battle_full.md (notes)

**Why.** docs/SCOPE.md's M2 is the lowest unmet milestone and its pixel gate is the five integrated rows: at HEAD (`docs/reviews/2026-09-14.md` scoreboard, cursor tear 10/9/170/186276 after T13b) 62 of 67 rows read 0 and the five non-zero are all integrated variants — opening (allowlist 28000), field integrated 158993/5621/40 (allowlist 28000; T13b moved it +59 from 158934), warp integrated (allowlist 19500), buster integrated (allowlist 28000), chip-use integrated (allowlist 28000) — four of them held open only by the last four allowlist entries (`tools/allowlist.py:54-57`, all `AUDIT-6: HUD vs a zero-enemy arena, not compared before`, dated 2026-09-08). F38's measured them as "canon RESULT slide w/o rust counterpart" (verifier-CONFIRMED for warp). F38b (NEGATIVE) measured that no single constant can serve them: with the resolve flag on, warp 40628 → 45751 (isolated 0 → 5123), buster 54672 → 81083 (isolated 0 → 2286), chip-use 275307 → 269172 (isolated 0 → 4026) — because one BANNER_TO_RESULTS cannot sit at canon k=24..29 with the OBJ banner tail parked over k=0..9. Canon's mechanism is a state table (sub_800801C asm00_1.s:10422 dispatching through off_8008038; the 0x0C handler sub_80081A4 :10617 does the HUD teardown; the results driver `sub_802BD60` asm03_0.s:11550 is already cited at src/results.rs:713 for the per-tick redraw), and T7e/T7f put canon's predicates under the *window* edges — the 0x08 → 0x0C → results tail is still hand-ordered. The same tail owns three of `battle_full`'s perma-divergences in the 0x0C window: `mm_timer` canon=65535 rust=0 on 239/239 frames, `enemy_state_action` canon=(8,0) rust=(4,2) on 239/239, `enemy_anim` canon=0 rust=2 on 239/239 — plus the trace's own NOTE that the residual 3-frame drift is ours ("4 repeated row(s), 130 jumped row(s) … the 3-frame drift is OURS, not a canon divergence"). Five pixel rows + four allowlist entries + three trace fields on one mechanism — the highest rows-per-dollar in the queue.

**Do.**
1. Baseline, no edits → *report per row total/worst/frames at HEAD (expected: opening ≤28000, field ≤158993, warp ≤40628, buster ≤54672, chip-use ≤275307) and ten integers — the capture index at which canon's results window first moves and ours does, per row; canon's zero-enemy band is F38b's k=24..29.*
2. Port the tail as canon's arms: the 0x08 handler's leave, the 0x0C handler's teardown (sub_80081A4, asm00_1.s:10617) and the results hand-off driven by the per-tick redraw already cited at src/results.rs:713 (sub_802BD60, asm03_0.s:11550), so the slide's first frame follows from a state entering rather than from a frame count → *report, per row, the two sides' slide-start capture index equal (five pairs of integers), and `battle_full`'s sequencer still 540/540 as T7e left it.*
3. Re-measure the 0x0C window of `battle_full` on the same recording → *report the three perma-divergent fields (`mm_timer` 239/239, `enemy_state_action` 239/239, `enemy_anim` 239/239) closed to 0 or restated with their canon values and the arm that writes each; and the trace NOTE's 3-frame drift either resolved or renumbered (currently: last compared row's counter 536 where a stall-free clock reads 539).*
4. Delete an allowlist entry only for a row that now reads 0; re-run the whole table from a clean detached checkout → *report rows at 0 (HEAD: 62 of 67), `allowlist entries:` (HEAD: 4 → expected 0), and any row still non-zero with its total, worst and frame.*

**Rules.** src/ changes only in the three named files; tools/allowlist.py by deletion only, never an addition and never a raised bound; no new harness row; no alignment change except by measured event (F2's rule); no per-row/per-fixture branch — one mechanism serves all five; every integrated row's isolated twin must stay 0/0 (warp, buster, field, opening, result, windowclose); BANNER_TO_RESULTS is either deleted or carries a `// provenance:` tag naming the ROM value it comes from, updated in the same commit; `fitted constants in src/` (HEAD: 19) may not increase; canon never changes; ≤8 capture runs, one at a time inside the 3-slot semaphore, count disclosed.

**Acceptance.** the five integrated rows read 0/0 with their negatives not blind, and the four `tools/allowlist.py:54-57` AUDIT-6 entries are gone (the cross-cutting invariant "no allowlist entries" then holds by measurement); or each surviving row is named frame by frame with both sides' sequencer trace and no allowlist entry kept for an unnamed residue. `battle_full`'s sequencer stays 540/540, the three 0x0C-window fields are closed or restated with their writers, every isolated row still 0 with cursor's tear reported as it moves from 10/9/170, and `fitted constants` unchanged or lower.

**Measure and report.** row: opening/field/warp/buster/chip-use integrated, plus battle_full, result, windowclose, cursor. frames: 540 (battle_full) + 40 per integrated row + 40 result + 40 windowclose + 170 cursor. total: per row before/after. worst: per row before/after. region: per row, the capture index where the slide starts on each side, and the diffmask region of any frame still non-zero. commit: src/battle.rs + src/results.rs + src/banner.rs + the allowlist deletions. One line of mechanism; one line of what is unverified.

**Coordinator:** dispatch first, only after T7f lands (it consumes T7f's window predicates and its acceptance restates the table T7f left). Worker muse-spark-1.3-contributor (T7c/T7d's child class), verifier GLM-5.3-flash cross-family; ≤$0.30 expected, ≤$0.70 cap; verify_rows on every row named, plus the full table twice on the same commit (before/after) since allowlist deletions are the evidence. Advances **M2**.

---

### T9h. The Gunner row: write the per-type routine under enemy_think/enemy_act using T9g's measured first-attack-event frame as the row's Align  *(OPEN -- 2026-09-14, follows T9g, advances M5)*

**Files.** src/objects.rs (the per-type entry ForGunner_8113078 wired as the kind=1 branch), src/actor.rs (the 0x0A/0x0B hop executors ref-checked against T9b's slot probe), src/battle.rs (only the fixture's enemy construction by kind — T9c's `battlestart_gunner` recipe stays), src/ai.rs (ForGunner_8113078 at off_8109050[0x17] and off_81091D0[0x17], asm32.s:10060-10290), assets/ (gunner.bin verbatim at comp_825BFC4, T9b's measurement), src/fixture.rs (kind=1 honored, backward-compatible), tools/states.py (only if a poke fixes the trigger — T9g's docs commit sets the path), tools/harness.py (the new row aligned by the Gunner's first attack event), FIXTURE.md (kind=1 row notes), docs/coverage/battlestart_gunner.md (rewritten with the port and the row's frame)

**Why.** T9g landed docs-only (d43e4c0) with three measured negatives: 700-frame settle, panel row 3, RNG — none fires an attack event on slot1 (0x0203ab68) inside 700 frames; sequencer reaches 0x0C at f166 and parks; 0x020013f0 draws 637 unique values (~1/frame), AI loop alive at RNG cadence but neither sub_8112D9C's 234-frame heartbeat nor the row-3 panel probe unsticks slot1. The remaining unmeasured lead is *canon's own per-type routine*: ForGunner_8113078 lives at off_8109050[0x17] / off_81091D0[0x17] (asm32.s:10060-10290) with the CurAction 0x00..0x0B dispatch and six per-version parameter tables from byte_80182C4 — T9b measured the art reproduces `assets/gunner.bin` byte-for-byte at comp_825BFC4 (4588 B / 2 gfx blobs / 1 palette / 4 anims / 16 frames / 154 OAM), T6 landed the Mettaur as canon's per-type entry (`ForMettaur_8109EF4` at objects.rs, identical mechanism), and the Gunner scenario is fixed (`battlestart_gunner` at T9c, kind=1 in fixture). M5 is "Mettaur done; Gunner in progress (T9c)" per SCOPE.md: this is the Gunner-as-data port the interpreters and T6 enable, completing M5's two entries. The mettaur harness row at 0/0/70/41734 stays the regression gate.

**Do.**
1. Baseline, no edits: capture Gunner scenario `battlestart_gunner` (record 6, setup byte_80B5347 = 00 22 00 00 | 11 25 01 00 | 11 36 85 00 | F0; slot1 = 0x85 Gunner, slot0 Mettaur) on both sides, watch slot1 CurState/CurAction/timer at +0x08/+0x09/+0x20 and HP at +0x24 with `tools/probe.py --watch-write` over 700 frames → *report the first frame ANY of them moves from its parked value (or stays parked, reported as one); the new row's Align should target the same k on both sides after this step.*
2. Port `ForGunner_8113078` per T6's MettaurEntry pattern: add the constant, wire the think/act dispatch on off_8109050[0x17] / off_81091D0[0x17], wire assets/gunner.bin as the kind-1 art, port the CurAction 0x00-0x0B arms under the already-landed `enemy_think`/`enemy_act` entries rather than as a new hand-written pose → *report, per arm actually ported, the `file:line` citation (T12's `tools/rom_enemy_tables.py` is the cross-check; the table idx 0x17 must resolve to `ForGunner_8113078`).*
3. Add one harness row `gunner` aligned by the first attack event measured in step 1 (event = first frame slot1 HP drops OR 0x020352A0/0x020352A2 changes from frozen 0x00200000; canonical-align by the canon frame, search band confirmed unique) → *report the row's first compared frame at 0 px and the negative fixture (kind=1 fixture, no Mettaur in slot0) reading the negative residue as non-blind.*
4. Re-run `tools/verify_rows.py` from a clean detached checkout → *report the 67-row table with mettaur 0/0/70/41734, wave 0/0/90/3840, chip-cannon 0/0/40/9505, cursor 10/9/170/186276 (the known tear) all unchanged; gunner row at 0; mettaur/wave/popup traces unchanged.*

**Rules.** Only the named files; tools/harness.py for the new row only (no edit to existing rows); docs/coverage/battlestart_gunner.md rewritten before this ticket copies it forward with the measured first-attack-event frame; no allowlist change of any kind; no alignment change except by measured event (F2's rule); the fix is a port under the interpreters — no new hand-written pose and no per-scenario branch; every new literal in an edited line gets a `// provenance:` tag or the canon symbol; `fitted constants in src/` (HEAD: 19) may not increase; the kind-1 fixture must be backward-compatible (every existing row unchanged; mettaur in particular at 0/0; kind 0 unchanged); canon never changes; ≤8 capture runs total, one at a time inside the 3-slot semaphore.

**Acceptance.** gunner row 0/0 with negative fixture not blind (kind=1, slot1 = Gunner, slot0 = Mettaur); the trace on the Gunner scenario shows the slot's CurState/CurAction/timers matching canon from spawn through the second attack; every existing isolated row unchanged (mettaur 0/0/70/41734 in particular; cursor's tear reported as it moves); docs/coverage/battlestart_gunner.md rewritten with the port's citations and the row's measured alignment frame; `fitted constants` not increased.

**Measure and report.** row: gunner (new), mettaur, wave, chip-cannon, cursor (regression rows). frames: 700 Gunner scenario (alignment-band), 70 mettaur, 90 wave, 40 chip-cannon, 170 cursor. total/worst: per pixel row before/after, with gunner row's start-frame reported as the alignment event. region: for gunner, the slot1 (CurState, CurAction, timer, HP) pairs frame by frame for the alignment window both sides; for any pixel regression, the diffmask region and frame. commit: src/objects.rs + src/ai.rs + src/actor.rs + src/fixture.rs + assets/gunner.bin + tools/harness.py (row only) + the docs. One line of mechanism; one line of what is unverified (whether the second attack's element/effect appears on the second-vs-third beats in the row's window — that's M3, not M5, and is out of scope here).

**Coordinator:** dispatch second, after T9g's docs commit and after T7g lands. Files are disjoint from T7g's (T7g is src/battle.rs + src/results.rs + src/banner.rs; this is src/objects.rs + src/actor.rs + src/ai.rs + src/fixture.rs + assets/ + tools/harness.py new-row + docs/coverage/) so the two can run in parallel per the two-worker rule once T7g is underway. Worker muse-spark-1.3-contributor (T6's child class — the Mettaur port was a T6 ticket), verifier GLM-5.3-flash cross-family; ≤$0.30 expected, ≤$0.60 cap; verify_rows on every row named. Advances **M5**.

---

### T13d. Audio on the cannon route: probe canon's per-channel play_sound arms first, then gate ours to match  *(OPEN -- 2026-09-14, re-scoped from T13c BLOCKED, advances M9)*

**Files.** tools/audio_probe.py (T13's instrument; extend with cannon-route coverage), tools/states.py (only the cannon route fixture, no new poke), docs/audio/baseline-cannon.md (new), docs/coverage/audio-cannon.md (new), src/battle.rs (only after step 2's predicate citations land — the actual gating; the gate predicate's address is a `// provenance:` tag in the same commit)

**Why.** T13c (`BLOCKED`, worker-minimax) was correctly rejected: there is **only one `play_sound` call in `src/` today** — the T13b-gated buster hit at `src/battle.rs:3235` — and the canon-side dumps show neither a chip-fire nor chip-damage SFX firing on the cannon row (chip-cannon `_chip_canon_0c("01")` is at 0 px isolated). The "add arms, then gate" scope of T13c was backwards: no source arms exist to mis-gate. M9 (audio: "the sound driver's battle side… measured by dumping both sides' audio on a scenario and comparing sample-exact" per SCOPE.md) still has "not started" as its status — T13/T13b were buster-only by the user's scoping. Re-scoped as a *probe-first* ticket: use T13's `tools/audio_probe.py` on the existing 0/0 cannon route (no harness change, no fixture change), tree-solo each of the six PSG/FIFO channels (mgba_capture.c:631-641), and read what canon actually fires frame-by-frame. If canon fires an SFX that ours doesn't, this ticket gates it; if canon fires nothing, this ticket *measures the absence* (the per-channel tree stays at the music-RMS floor T13 measured on the buster fixture) — a precise negative that closes M9's "battle side SFX on a chip-using scenario" entry. Either way, the cross-cutting invariant "no fitted constants" moves by one measurement: every gate we don't add is fitted-by-omission until this run.

**Do.**
1. Probe canon first, no source edit: dump both sides on the cannon scenario (`tools/harness.py:868`, `_chip_canon_0c("01")` route, identical to T7/F7's row), `--dump-audio` only, `--audio-channel 0..5` soloed in turn (one run per channel per side = 12 runs), with T13's per-channel trees (ch0/1/2/3 FIFO A; ch4/5/6/7 FIFO B) → *report, per channel, both trees' peak |sample|, RMS, onset frame and onset-pair index for any SFX onset, and the buster fixture's per-channel peaks (T13: ch4 peak 0 zero-enemy, 11273 enemy-present at frames 118..130) as the control; report the absence of any SFX onset on the cannon route as a precise negative, with the music-floor RMS as the number.*
2. If a canon SFX onset exists that ours doesn't: find the predicate in the disassembly for that arm (the chip-fire dispatcher sub_801A308 at asm00_2.s:21830; the chip-damage arm sub_801B720 family at asm00_2.s:23030 fired when `take_damage[enemy_idx] != 0`; cite each with `file:line`) and add a single `play_sound` call to `src/battle.rs` gated by that predicate; re-run step 1 → *report the per-channel peaks again, the onset residual in pairs and in ms at the measured 95999.1 / 95589.4 Hz.*
3. Write `docs/audio/baseline-cannon.md` (the same shape as `docs/audio/baseline-buster.md`) and `docs/coverage/audio-cannon.md` recording the per-channel peaks, the absence-or-presence decision from step 1, the gate citation if step 2 ran, and what is now parity and what is still residue → *report the doc's new finding section.*

**Rules.** Only the named files; `tools/mgba_capture.c` (shared `/tmp/mgba_capture` rebuild-on-newer-mtime would swap the binary under any running capture), `tools/harness.py` notes only (no row added — audio is still not a harness verdict), `tools/states.py` (only the cannon route fixture, no new poke), `tools/trace.py`, `FIXTURE.md`, `assets/`, `reference/bn6f` read-only; ≤8 capture runs total, one at a time inside the 3-slot semaphore, count disclosed honestly; every gate added gets its `// provenance:` tag updated in the same commit; `fitted constants in src/` (HEAD: 19) may not increase; canon never changes.

**Acceptance.** `docs/audio/baseline-cannon.md` and `docs/coverage/audio-cannon.md` exist with the measured numbers; the cannon route's per-channel trees are reported for both sides with onset-or-absence decisions named; the full harness table is identical to HEAD (`verify_rows.py` clean run; cursor's 10/9/170 tear reported as it moves; nothing worse); `fitted constants` unchanged or lower; and either a cited gate was added and matches canon's onset within the buster-residual 635 pairs / 6.62-6.64ms class, OR the absence is the result and `docs/audio/baseline-cannon.md` records it as the per-channel music-floor RMS (named, measured, not "inherent").

**Measure and report.** row: none — audio is still not a harness verdict (T13b's rule); report the 67-row rollup unchanged from HEAD. frames: the cannon scenario's compared frame count, both sides, per channel. total/worst: differing samples / max |Δ| per channel (T13's tree comparison) — present only if an onset exists. region: per channel id, the onset frame and onset-pair index, or the music-floor RMS per frame if absent. commit: `tools/audio_probe.py` (if extended) + `docs/audio/baseline-cannon.md` + `docs/coverage/audio-cannon.md` + `src/battle.rs` (only if step 2 fires). One line of mechanism; one line of what is unverified (whether the cannon route is representative of the 43 chip-family routes — a single-scenario M9 measurement by the user's scoping, broader coverage is a follow-up ticket).

**Coordinator:** dispatch third, after T7g lands (so any gate sits on the T7g + T7f sequencer arms, not against the fitted SEQ04_FRAMES=60). Disjoint from T7g (T7g is src/battle.rs end-sequence + src/results.rs + src/banner.rs; this is tools/audio_probe.py + docs/* + src/battle.rs SFX-gate lines only) so it can run alongside T9h per the two-worker rule. Worker muse-spark-1.3-contributor (T13/T13b's child class), verifier GLM-5.3-flash cross-family; ≤$0.20 expected, ≤$0.50 cap; verify_rows on the full table. Advances **M9**.
```
