# Proposed tickets 20260917-054823 (zai/glm-5.3, $0.1301)

### T101. M2 — The Regular chip: canon's auto-add edge measured, then the port *(OPEN -- 2026-09-17)*

**Why.** SCOPE M2 names "chip selection rules (codes, Regular, Tag, PA recognition, folder draw/reshuffle)". Two are closed: PA recognition (T86 NEGATIVE), folder draw/reshuffle (T59 NEGATIVE). **Regular has no ticket** (grep TODO.md + TODO_ARCHIVE.md for "Regular": only T97's netbattle prose). Our side has zero Regular logic: grep `src/*.rs` for "regular" hits only agb's `RegularBackground` type (src/backdrop.rs:61, src/field.rs:13, src/results.rs:14) — src/custom.rs never reads a regular slot. Canon's mechanic is real: UI strings "Choose Regular Chip" / "as a Regular Chip!" at data/textscript/compressed/CompText86CEE84.s:194/:247; the custom window's OK/close path is custMenuPressOK_8028D3A (asm03_0.s:5437) → sub_8029110 (:6029). The scenario machinery needs no hand-played input: windowclose_full (T7f) validated both sides' window edges (0x00 enter+leave 144..146, 0x04 enter+leave 147..206), T7r's fixture scripting seeds gauge/window state, and a close edge is passive.

**Files.** `src/custom.rs` (the close arm that reads the regular slot; cite from step 2), `src/fixture.rs` (descriptor regular field, `// provenance: peeked` + file:line), `tools/states.py` (ONE scenario `window_reg_full` = windowclose_full base + regular-slot poke, NO OK press), `tools/harness.py` (ONE row `window_reg`), `docs/coverage/custom.md`, `docs/worklog/T101.md`. **NOT** tools/trace.py, tools/oracle.py, tools/allowlist.py, tools/inventory.py, src/battle.rs, src/objects.rs, src/chips.rs, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. Baseline, no edit: build ROM, sha256; verify_rows `wave,window,opening,chip-cannon,mettaur,windowclose,cursor` → 0/0/N ×6, cursor 1/1/170; trace `windowclose_full` and quote the seq.state edges as reference. **measurement.**
2. Recon: walk the "Choose Regular Chip" handler to the save-RAM regular-slot address (file:line); poke it in a state recipe and watch the hand words through a no-OK window close — report whether canon auto-adds the regular, at which frame, and the word written. **measurement.**
3. Add `window_reg_full` + the `window_reg` row (canon = REAL + poke; rust = plain_rom + descriptor regular field; negative = same scenario WITHOUT poke/field). **code change + measurement.**
4. Port the auto-add arm in src/custom.rs from step 2's cite; plumb the descriptor field in src/fixture.rs; rebuild, sha256. **code change + measurement.**
5. Re-run: `window_reg` 0/0/N with non-blind negative (unset regular → hand word AND pixels differ); trace `window_reg_full`: seq.state and the hand word first divergence **none** over the scenario's frames; guard set identical to step 1. **measurement.**

**Rules.** The auto-add is ported from the cited routine — no fitted hand word, no fitted frame. State/descriptor pokes carry peeked provenance (F5b's rule: the poke is the fixture, canon never changes). Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile. ≤6 captures, tool budget ≤70, one harness command at a time.

**Acceptance.** `window_reg` 0/0/N with non-blind negative; `window_reg_full` trace: `seq.state` AND the hand word first divergence **none** over the scene's frames (both sides' close edges at the same frames); 7-row guard set + cursor identical to step 1; verify_rows PASS is the veto. NEGATIVE naming canon's measured no-auto-pick behavior (frames + words + cite) closes it.

**Measure and report.** rows: window_reg + 7-row guard set; frames as run; regular-slot address + cite, auto-add frame + word; before/after pixel totals, worst, region, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified.

**Coordinator:** owns src/custom.rs + src/fixture.rs + the row; runs alone (owns tools/states.py + tools/harness.py). Free tier: verify_rows from a clean checkout on the 8-row set. ≤$0.20 expected, ≤$0.40 cap. Advances **M2**.

---

### T102. M3 — Counter hit and its emotion grant: one recorded scenario, the hit-time predicate ported *(OPEN -- 2026-09-17)*

**Why.** SCOPE M3 names "counter hit and Full Synchro" and M3's status is "not started"; no ticket has touched it (grep TODO.md + TODO_ARCHIVE.md: the only Synchro hits are tutorial-text cites). The grant is measurable and already half-built on our side: TRANSFER.md:2585 records the early observation ("the hit puts the navi into FULL SYNCHRO, which rewrites the navi's own palette bank"), corrected at TRANSFER.md:2738-2751 — the lit palette is the emotion state and a disasm search for a "Full Synchro flag" found none, so the grant rides the emotion-window state machine, which our side already renders and compares (tools/chip_compare.py:24 names the navi's Full Synchro in the diff; src/emotion.rs exists). The counter window is enemy-driven and already measured: the Mettaur's attack phase is pixel-perfect at its true phase (F25b), and scripted fixture input exists (T7r's seeded A press) — no hand-played input needed.

**Files.** `src/shot.rs` or `src/battle.rs` — whichever holds the hit→damage call (step 2 names it; edit only that one) for the counter check, `src/emotion.rs` (the grant + window state), `src/fixture.rs` (scenario + scripted A), `tools/states.py` (ONE scenario `counter_full` = mettaur base + Cannon in the picked slot + scripted A at the measured windup frame), `tools/harness.py` (ONE row `counter_synchro`), `docs/coverage/damage.md` (NEW), `docs/worklog/T102.md`. **NOT** tools/trace.py, tools/oracle.py, tools/allowlist.py, src/objects.rs, src/ai.rs, reference/bn6f.

**Do.**
1. Baseline, no edit: build ROM, sha256; verify_rows `wave,window,opening,chip-cannon,mettaur,cursor,windowclose` → 0/0/N ×6, cursor 1/1/170. **measurement.**
2. Recon: cite canon's counter predicate (the hit routine's test that the victim is mid-attack; file:line) and the grant site (the emotion state byte + its writer; file:line); read the Mettaur's windup frames off the mettaur row's trace. **measurement.**
3. Add `counter_full` + the `counter_synchro` row: fire Cannon at the windup frame; negative = the same fire at +10 frames (outside the window), which must read non-zero (no counter popup, no grant). **code change + measurement.**
4. Port the predicate and the grant from step 2's cites; rebuild, sha256. **code change + measurement.**
5. `counter_synchro` 0/0/N with the negative non-blind; trace the scenario: the emotion-state field AND the counter flag first divergence **none** over the row's frames; guard set identical to step 1. **measurement.**

**Rules.** Predicate and grant ported from cited routines — no fitted window frames: the window comes from canon's own test, and the +10-frame negative proves the port is not a frame match. Emotion art/anim from canon bytes. Cursor veto ≤1/1/170/186300. No allowlist, no patch_sterile, canon never changes. ≤6 captures, tool budget ≤80, one harness command at a time.

**Acceptance.** `counter_synchro` 0/0/N with a non-zero negative; the scenario's trace: emotion-state field AND counter flag first divergence **none** over the row's frames; guard set + cursor identical to step 1; verify_rows PASS is the veto. NEGATIVE naming the predicate's or grant's missing cite (or canon's measured no-grant behavior with frames) closes it.

**Measure and report.** rows: counter_synchro + chip-cannon + mettaur + 7-row guard set; frames as run; predicate cite, grant cite + state byte, windup frames used; before/after pixel totals, worst, region, ROM sha256+size, fitted count, commit; one line of mechanism; one line unverified (Full Synchro's double-damage half, not exercised by this scene).

**Coordinator:** owns the damage-path edit + src/emotion.rs + the row; runs alone (owns tools/states.py + tools/harness.py). Free tier: verify_rows from a clean checkout on the 9-row set; verifier for step 2's two cites. ≤$0.25 expected, ≤$0.50 cap. Advances **M3**.

---

### T103. M4 — FireBrn1-3 as data: a fresh record family through the chip dispatch *(OPEN -- 2026-09-17)*

**Why.** M4 reads 43/411 with as-data families {0x13,0x15,0x21} (`AS_DATA_FAMILIES`, tools/inventory.py); the id-ordered unrecorded frontier starts at Spreadr1 (id 9) and **FireBrn1-3 has no prior ticket** (names at data/textscript/TextScriptChipNames0.s:65-71). Closed families stay closed: Cannon (T36/T38/T51 NEGATIVE), Vulcan (T54 NEGATIVE), TrnArrw (T91/T94 NEGATIVE — id 26 carries family 0x2D, the over-trigger trap), StepSwrd (T85 NEGATIVE), Recov 0x20 (T73 NEGATIVE: every id record-clean but the code-size shift moved cursor 1/1/170/186279 → 7/6/170/186276). Port shapes are proven: T17 swords (hit shape indexed by AttackSubFamily, src/battle.rs:4391), T46 Bomb (+0x1a AttackPower, 85f021c), T48 Barrier, T52 AirShot — and T78 (e7ec05d) landed a chip port through the cursor veto using F12's pure-layout pad, the standing answer to T73's revert. Landing moves 43/411 → 46/411.

**Files.** `src/battle.rs` (ONE family arm keyed on `chip.family` at the use_chip dispatch, T17/T46 shape), `tools/inventory.py` (AS_DATA_FAMILIES + its provenance comment ONLY), `tools/states.py` (3 NEW `firebrn_select_<id>` scenarios, chip-cannon fixture base), `tools/harness.py` (3 NEW rows `chip-firebrn1/2/3`), `docs/coverage/chips.md`, `docs/worklog/T103.md`. **NOT** src/chips.rs, src/fixture.rs, tools/trace.py, tools/allowlist.py, reference/bn6f; no layout/pad file unless cursor forces T78's pattern (then name it in the report).

**Do.**
1. Baseline, no edit: build ROM, sha256 **and size**; verify_rows 7-row guard set + chip-cannon + chip-bomb + chip-sword → 0/0/N. **measurement.**
2. Read the three FireBrn records (attack_family, attack_sub_family, element, attack_power per id, data/ChipDataArr.s file:line each); census EVERY other id whose attack_family byte matches — T91's over-trigger check — and report the sharer list. **measurement.**
3. If every sharer is dispatchable inside one arm: port it citing canon's attack-spawn routine per arm; if a sharer over-triggers, STOP — NEGATIVE naming the sharer ids + bytes. **code change + measurement.**
4. Add the 3 scenarios + rows; run each → 0/0/40, negative = no-chip-fire path (non-blind). **measurement.**
5. verify_rows guard set + 3 new rows; cursor ≤1/1/170 (apply T78's F12 pure-layout pad if code size moves it; report ROM size before/after per T56); `python3 tools/inventory.py` M4 line 43/411 → 46/411. **measurement.**

**Rules.** No `match chip.id` arm for these ids — dispatch keys on the record; every literal in an edited line gets a `// provenance:` or `// canon:` tag. A family joins AS_DATA_FAMILIES only when ALL its ids read the record. Cursor ≤1/1/170/186300: code-size movement is absorbed by T78's pad, never by reverting record-driven behavior; ROM size delta is part of the report. No allowlist, no patch_sterile, canon never changes. ≤6 captures, tool budget ≤80, one harness command at a time.

**Acceptance.** 3 chip-firebrn rows 0/0/40 with non-blind negatives; the dispatch keys on `chip.family` with no per-id arm (verifier-checked); guard set + cursor identical to step 1; SCOPE M4 chip count 43/411 → 46/411; verify_rows PASS is the veto. NEGATIVE naming the sharer ids (record bytes + cites) or the first divergent row/frame/region closes it.

**Measure and report.** rows: 3 chip-firebrn + chip-cannon + chip-bomb + chip-sword + 7-row guard set + cursor; frames 40 each (cursor 170); family/subfamily/element/power per id, sharer census, ROM sha256+size before/after, inventory chips line before/after, fitted count, commit; one line of mechanism; one line unverified.

**Coordinator:** owns src/battle.rs's family arm + the rows; runs alone (owns tools/states.py + tools/harness.py + tools/inventory.py). Free tier: verify_rows from a clean checkout on the 14-row set + re-run tools/inventory.py; verifier for the record-keyed dispatch claim. ≤$0.15 expected, ≤$0.35 cap. Advances **M4**.
