# Proposed tickets 20260916-012659 (minimax/MiniMax-M3, $0.1642)

I'll review the relevant context and propose three fresh tickets.

Based on my read of HANDOFF.md §3, TODO.md, TODO_ARCHIVE.md, and docs/SCOPE.md:

**Open state of milestones:**
- M2 sequencer timeline is closed for this phase (T7y/T7z NEGATIVE per T27's own no-fifth-ticket rule)
- M3 is "not started" — the lowest truly unmet milestone (T18/T33 closed docs/M1 only)
- M4 partial (10 chips AS DATA + 33 verified-pixels; T34 NEGATIVE on third pass)
- M5 in progress (Gunner row 2105613/38237/130 = largest single row on board; T32 just landed GUNNER_ROW gauge=0x4000 descriptor 5a5fcd9, preparing F40a's deferred lever)

**Ordering by leverage and milestone priority (M2→M3 before content):**

---

### T35. M3's first scenario: panel ownership with AreaGrab, one recorded trace + pixel row per rule

**Why.** docs/SCOPE.md's M3 line reads "not started (the Mettaur/chip rows cover a few incidentally)" and is the lowest truly unmet milestone; M2 sequencer is closed for this phase (T7y NEGATIVE, T7z BLOCKED). The cheapest M3 advance is a single recorded scenario for one rule, panel ownership/area steal, fired through AreaGrab (chip 163, `verified-pixels` per docs/SCOPE.md, codes BFS\*, element NONE) — the smallest chip that exercises an M3 rule without an element-typed enemy (Mettaur is element NONE). The chip's own harness row already reads 0/0/30 (chip-area PASSes per F12's landing), so the chip is correct; M3 needs a NEW scenario where one panel's owner changes and the trace + pixels record the transition. T33 established the panel flag word: `word_3007924` (IWRAM copy asm38.s:4242-4249 = ROM 0x081D7E24) carries the 13 panel flags including the alliance bit, OR-ed into `oPanelData_Flags` by `_object_updatePanelParameters` (asm38.s:4213-4219); 8/13 verified, 5 GAPs (0x0/0x5/0x8/0x9/0xA). Milestone **M3**, and M1's panels line.

**Files.** tools/states.py (new root state), tools/harness.py (new `Check` row only — no existing row touched), src/fixture.rs (descriptor only, no behavior change), docs/worklog/T35.md.

**Do.** 1. Baseline: `python3 tools/harness.py --only chip-area,cannon,field,tiles,gauge,mettaur,cursor,result,windowclose,chip-use --no-gallery` → report every line (chip-area 0/0/30, cannon 0/0/40/9505, field 0/0/40/1139, tiles/gauge 0/0/8/12197, mettaur 0/0/70/41734, cursor 1/1/170/186279, result 0/0/40/111839, windowclose 0/0/40/207166, chip-use 0/0/30/7768). 2. Find the ROM routine: `grep -n "AreaGrab\|off_80.*Area" reference/bn6f/asm/asm*.s` → report the routine's file:line, the bit in `oPanelData_Flags` it changes, and the `word_3007924` entry it walks; cite the existing reader at asm38.s:4315-4317 for type 0x0 if relevant. 3. Build a scenario: from `pausedwithcannon.state`, place MegaMan adjacent to a Mettaur, fire AreaGrab, save state, run 30 frames; record the named capture where the owner bit transitions. 4. Add a new harness row `panel-own` (isolated + integrated, frames=30, event-locked to the owner-change capture, with a non-blind negative fixture that fires AreaGrab on a panel already owned). 5. Run the new row → expect 0/0/30 with a cited ownership-change trace field name from step 2. 6. Regression: `verify_rows` on the 12-row guard list → no row moves; `provenance_counts()` must stay 19.

**Rules.** Only the named files; no src/ behavior change; no allowlist edit; no row's `align`/`canon_ref`/`search` band changed; canon never changes; ≤4 captures, tool budget ≤70; any new literal gets `// canon:` or `// provenance:`.

**Acceptance.** `panel-own 0/0/30` with a non-blind negative and a cited ownership-change trace field name, the 12 isolated rows unchanged, `provenance_counts()` = 19, and docs/SCOPE.md's M3 line moves from "not started" to "in progress" with `panel-own` named. Or a NEGATIVE naming the ROM routine that writes the owner bit and why it is unreachable from this scenario.

**Measure and report.** rows: panel-own (new), the 12 isolated guards. frames: 30. totals/worst/negatives before and after per row, the ROM routine cite, the trace field name with file:line, fitted count, commit, one line of mechanism, one line of what is unverified.

**Coordinator:** dispatch alone (it owns tools/harness.py + tools/states.py + src/fixture.rs descriptor). Free tier: `verify_rows` across the 12-row guard list. A verifier for step 2's ROM cite only. ≤$0.30 expected, ≤$0.60 cap. Advances **M3**.

---

### T36. M5: port the gunner's gauge and chip-window auto-open to honor GUNNER_ROW's gauge=0x4000 descriptor that T32 just landed

**Why.** The gunner row at 2105613/38237/130 (neg 2284867, non-blind) is the largest single row on the board; T9l's split (424814d, docs/harness.py:2392-2430) names three bands — (1) k=0..5 fade tail BG1-only 230400 exactly six frames, BG1 0 for k≥6; (2) **k=6..75 on BG3** "the two enemy HP boxes (y0..15, x100..190) and the bottom-left custom gauge bar (y152, x12..60): canon's bar reads full from k=0 while GUNNER_ROW's borrowed gauge=0 never fills"; (3) k≥77 canon's gauge-full pause opens the chip window on BG3 (slide from canon ~156; "comes up on its own at frame 165", src/battle.rs:1998) plus a ~5.8k/frame OBJ plateau downstream. T32 (5a5fcd9) just added GUNNER_ROW's `gauge=0x4000` with `isCustGaugeFullAndBattleLive_800A21C` (asm00_1.s:15305) cite and an `& 0xFF` mask on the fixture_cheats gauge write — the lever is now actionable, but T32's gate "fires descriptor-side only (rust-side gauge register still reads 0x00 until src/-side seed work that F40a deferred)" remains. F40a was BLOCKED here. T26 NEGATIVE (9963a00) closed the SEQ_20/24/00/04 ladder predicate this would consume. Milestone **M5**.

**Files.** src/battle.rs (the `Some(f) => if f.gauge != 0 { GAUGE_FULL } else { 0 }` arm at :2150, the auto-open arm at :2906, the existing slide-in note at :1998 — T26's ladder cites only, in those ranges), tools/harness.py (GUNNER_ROW note text only, not values), docs/worklog/T36.md.

**Do.** 1. Baseline, one session, with the built ROM's sha256: `python3 tools/harness.py --only gunner --no-gallery` → 2105613/38237/130 neg 2284867; plus `verify_rows` on cursor 1/1/170/186279, windowclose 0/0/40/207166, field 0/0/40/1139, mettaur 0/0/70/41734. 2. Honor the descriptor: at src/battle.rs:2150 replace `f.gauge != 0 → GAUGE_FULL` with a cite-armed read matching `isCustGaugeFullAndBattleLive_800A21C`'s `gauge == 0x4000 && live` test (asm00_1.s:15305); keep the byte mask T32 added at tools/harness.py:156. 3. Add the auto-open: at canon's capture ~157, port the BG3 RESULT-slide-in trigger already noted at src/battle.rs:1998 ("comes up on its own at frame 165") onto the gauge-full predicate from step 2; T26's SEQ_20/24/00/04 ladder stays untouched — quote one T26 cite in the diff. 4. Per-band report: BG3 totals at k=6..75 and k≥77 before/after, the slide-in frame before/after. 5. Regression: `verify_rows` on the 4-row guard list with the landed negatives; gunner's per-band totals must show the BG3 plateau dropping while integrated's overall count strictly decreases.

**Rules.** Only the named ranges. No new fitted constant. No row's `align`/`canon_ref`/`search` band changed. No allowlist edit. No `patch_sterile.py`. Canon never changes. ≤6 gunner captures, tool budget ≤80. Any new literal gets `// canon:` and re-checked against disk. /tmp's four canon roots untouched; artifacts under `/tmp/bn-t36-*/`.

**Acceptance.** `gunner` integrated strictly below 2105613 with the BG3 plateau (k=6..75) clearly reduced and the chip-window auto-open landing within ±2 frames of canon's capture ~157, the 4-row guard list byte-identical, both ROM sha256s reported, fitted ≤19. Or a NEGATIVE naming which of (a) the gauge arm or (b) the auto-open blocks the landing.

**Measure and report.** rows: gunner (integrated+isolated), the 4-row guard. frames: 130/170/40/70. totals/worst/negatives before/after per row, the per-band totals, the slide-in frame, both ROM sha256s, fitted count, commit, one line of mechanism, one line of what is unverified.

**Coordinator:** dispatch first and alone (no other open ticket touches GUNNER_ROW or src/battle.rs's :2150/:2906). Free tier: `verify_rows` on the 4-row guard plus the direct `--only gunner --ui integrated`. A verifier for step 2's cite claim and step 3's slide-in cite. ≤$0.50 expected, ≤$1.00 cap. Advances **M5**.

---

### T37. M4: convert Cannon/HiCannon/M-Cannon (chips 1, 2, 3) to data-driven from ChipDataArr's AttackPower

**Why.** docs/SCOPE.md M4 reads "10 chips pixel-verified AS DATA -- the sword/blade family-0x13 rows … T17 -- plus 33 pixel-verified through our own code". After T17 (8c1a62d, swords) and T19 (2baad23, contract), the Cannon family (chips 1/2/3: Cannon 40 damage ABC\*, HiCannon 100 LMN\*, M-Cannon 180 RST\*, all `CHIP_ELEM_NONE`) is the simplest next family: element NONE means no element-typed enemy is required (Mettaur is element NONE, the cannon scenario already exists), three records with the same family and growing `AttackPower` (+0x1a in `chip_data_struct` per reference/bn6f/include/rom_structs/ChipData.inc). T28 (fd1be73) confirmed Cannon's record bytes match the src literals (chip-cannon row 0/0/30/??? and chip-use 0/0/30/7768 both PASS); the 43 chip rows are zero rows, which F48 (4df0c52, docs/measurement-drift.md) proved structurally safe as gates — a drift cannot make a mispaired frame read 0. Asset stays v2: tools/chip_export.py already ships AttackPower via T17's tail. Milestone **M4** and M1's chips line.

**Files.** tools/chip_export.py (no change if Cannon bytes already exported; else append-only), assets/chips.bin (regenerate, first 36 bytes of every record byte-identical), src/chips.rs (Cannon-family accessor), src/battle.rs (the Cannon arm only — never `chip_strike`'s landed sword/blade arm, never `fight_latch`/`hp_readout_live`), tools/inventory.py (Cannon family added to AS_DATA_FAMILIES, generator data only), docs/SCOPE.md (regenerated), docs/worklog/T37.md.

**Do.** 1. Baseline, one session, with the ROM sha256: `python3 tools/harness.py --only chip-cannon,chip-hicannon,chip-mcannon,chip-use,cannon,field,tiles,gauge,mettaur,cursor --no-gallery` → report every line (chip-cannon/hicannon/mcannon 0/0/30/???; chip-use 0/0/30/7768; cannon 0/0/40/9505; field 0/0/40/1139; tiles/gauge 0/0/8/12197; mettaur 0/0/70/41734; cursor 1/1/170/186279). 2. Parity table, zero captures: read `ChipDataArr_8021DA8+id*44` for ids 1/2/3 out of `reference/bn6f/bn6f.gba` → report `AttackFamily` (+0xb), `AttackSubFamily` (+0xc), `AttackPower` (+0x1a), `AttackParam1..4` (+0x10..0x13), `LockoutFrames` (+0x14) side by side with the src literals; every agreement and every mismatch is a finding. 3. Only for values step 2 proves equal: add Cannon's family to `AS_DATA_FAMILIES` in tools/inventory.py, regenerate, rewrite the Cannon arm to dispatch on AttackFamily/AttackPower from the record, delete the three `CHIP_CANNON/HICANNON/MCANNON` ids in src/battle.rs. 4. `cmp` first 36 bytes of every record against pre-change asset → 0 diffs, quote new stride. 5. Re-run step 1 → every row identical to baseline. 6. `python3 tools/inventory.py` → quote SCOPE's M4 line and M1 chips count before/after.

**Rules.** Asset is append-only; v2 stays v2 unless step 2 forces v3 (then first 36 bytes byte-identical). No per-chip id survives as a special case in the Cannon arm. No allowlist edit. No row's `align`/`canon_ref`/`search` band changed. Fitted ≤19 (report it). Canon never changes. ≤6 captures, tool budget ≤80. Any new literal gets `// canon:` or `// provenance:`. /tmp's four canon roots untouched; artifacts under `/tmp/bn-t37-*/`.

**Acceptance.** The three Cannon rows read 0/0/30 with their landed negatives reproduced, all three ids gone from src/battle.rs's Cannon arm, asset cmp diff = 0, fitted ≤19, and SCOPE's M4 line moves through the generator's own data with the new chip count named (10 → 13). A NEGATIVE that a record byte contradicts a measured literal closes the ticket per chip with both numbers.

**Measure and report.** rows: chip-cannon, chip-hicannon, chip-mcannon, chip-use, cannon, field, tiles, gauge, mettaur, cursor. frames: 30/30/30/30/40/40/8/70/170. totals/worst/negatives before/after per row, the parity table, the asset cmp diff count, fitted count, ROM sha256, SCOPE M4/M1 lines before/after, commit, one line of mechanism, one line of what is unverified.

**Coordinator:** dispatch alone (it owns src/battle.rs's Cannon arm and the asset v2 file). Free tier: `verify_rows` over the chip set with the landed negatives as expects; any non-zero is a revert. A verifier for step 2's parity table (the same ROM-read T17/T19 verifiers did). ≤$0.30 expected, ≤$0.60 cap. Advances **M4**.
