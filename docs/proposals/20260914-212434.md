# Proposed tickets 20260914-212434 (minimax/MiniMax-M3, $0.1542)

I have what I need. Let me write the three tickets. The completion ladder puts M2 first (engine core at zero divergence); T7e landed battle_full sequencer 540/540 but T7h NEGATIVE left the first divergence on the trace still unclosed (enemy_state_action 344/540 at k=0), T7j PARTIAL left SEQ04_FRAMES=60 (`src/battle.rs:1186`, one of 19 fitted constants per `docs/reviews/2026-09-14.md:174`) still in src/ despite the verifier REFUTING the worker's "swap unsafe" claim, and T7f PARTIAL is essentially DONE but lacks a clean re-record with the canonical predicate once T7l lands. All three are M2 trace-gate work; leverage ranking puts T7k first (closes the trace gate that gates5+ integrated variants: opening 72499, warp 40628, field 1589, buster 546, chip-use 2), then T7l (closes a fitted constant and unblocks T7m), then T7m (lands the chip-window sequencer acceptance cleanly).

### T7k. battle_full first divergence — measure the PAUSED-vs-Hidden/Appearing gap and port the bridge under `enemy_think`/`enemy_act` (no hand-written pose)

**Files.** src/objects.rs (the per-type entry and the enemy's exported CurState/CurAction), src/actor.rs (the 0x0A/0x0B executors' entry condition), src/battle.rs (only where the enemy is created for a fixture), tools/probe.py (the per-frame slot watch), tools/trace.py (only if a field must be added), docs/coverage/battle_full.md (notes), docs/trace/t7k/ (the per-frame logs, new)
**Why.** T7h NEGATIVE (branch wt/t7h-opening-action reset to d511109, no commit landed) proved the SPAWN-arm port is **not** the right fix: trace counts UNCHANGED (enemy_state_action 344/540 and enemy_anim 409/540, both baselines `docs/trace/t7b/battle_full.diff.txt`) and the divergence lives in the **PAUSED-vs-Hidden/Appearing gap**, not the entry sequence. canon's recording captures the Mettaur mid-HOP at CurAction 0x0A from frame 11 (`docs/trace/t7b/battle_full.diff.txt`: `FIRST DIVERGENCE: enemy_state_action at k=0 (canon frame 11) canon=(4, 10) rust=(4, 0)`) while ours has a 62-frame Hidden/Appearing fade that precedes any entry arm and ends with CurAction still 0x00 at the same k=0 — `BattlePaused` (GameState+0x0A, `GameState.inc:34`) reads1, `PauseBattle` `asm00_1.s:14878`, unflagged objects skip their update (`asm00_1.s:90-110`), so the entry sequence never runs on the mettaur scenario and the spawn arm is dead code in the only scenario that exposes the gap. Cursor regressed 10/9/170 → 741/728/170 on the spawn-arm port and was forced revert; the regression is the structural "we entered the pose before canon" symptom, not the spawn arm's fault. T6 already landed `MettaurEntry` (`ForMettaur_8109EF4`, `objects.rs`) and T5/T6's interpreters (`enemy_think`/`enemy_act`); this ticket ports the **bridge** between Hidden/Appearing end and HOP start under those interpreters — the arm canon uses to fast-forward from the captured Hidden/Appearing state to a mid-HOP CurAction 0x0A — never as a hand-written pose. Closing the trace gate is the highest-leverage M2 unclosed item: HANDOFF names the integrated variants (opening 72499, field integrated 1589, warp integrated 40628, buster integrated 546, chip-use integrated 2) as blocked on the end-sequence ported as one state machine, and T7e's540/540 sequencer is that state machine; once the first divergence moves off `enemy_state_action` at k=0, the integrated variants can be re-measured against the new trace.
**Do.**
1. Baseline (no edit), probe the enemy slot `0x0203ab68` (+0x24 HP, +0x08 CurState, +0x09 CurAction, +0x20 timer, +0x10 anim) and `GameState+0x0A` (BattlePaused) and `GameState+0x04` (BattlePaused source byte) for k=-20 to k=40 with `tools/probe.py` on both sides → *report, per side, the frame the slot first appears, the BattlePaused value per frame, the frame each side first reaches CurAction 0x0A, the timer/arm values across the gap, and the writer PCs for every transition; report an empty log per watch as one.*
2. Find canon's bridge between Hidden/Appearing end and the HOP executor — `ForMettaur_8109EF4`'s CurAction 0x00 → `RunSpawnAnimationMaybe_8016380` (`asm00_2.s:16009`), the `BattlePaused` arm that fast-forwards `CurAction` (`asm00_1.s:90-110` plus the `sub_8008064` 0x04 enter at `asm00_1.s:10478-10514`), and any other arm canon uses to land at CurAction 0x0A from a PAUSED state — and port that arm under `enemy_think`/`enemy_act` (T5/T6's interpreters, NOT a new hand-written pose in src/objects.rs) → *report the citation `file:line` per arm actually ported, the new trace's first divergence (field + k + canon/rust values), and the new `enemy_state_action` count (baseline 344/540) and `enemy_anim` count (409/540).*
3. Re-run the `battle_full` trace diff and `tools/verify_rows.py` from a clean detached checkout → *report the table identical to HEAD except where the report says, every chip row and the mettaur row still 0/0, cursor's tear reported (HEAD 10/9/170/186276), the regression on T7h (741/728/170) NOT repeated, and the integrated variants re-measured: opening 72499, field integrated 1589, warp integrated 40628, buster integrated 546, chip-use integrated 2 (the numbers either fall toward0 or are named as the new remainder).*
**Rules.** Only the named files; no new harness row and no change to `tools/harness.py`, `tools/states.py`, `FIXTURE.md` or the descriptor contract; no allowlist change of any kind; no alignment change except by measured event; the fix is a port under the interpreters (T5/T6's `enemy_think`/`enemy_act`/`MettaurEntry`) — no new hand-written pose and no per-scenario branch; every new literal in an edited line gets a `// provenance:` tag or the canon symbol; `fitted constants in src/` (HEAD: 19) may not increase; canon never changes; ≤6 capture runs, one at a time inside the 3-slot semaphore.
**Acceptance.** `battle_full` first divergence is **no longer** `enemy_state_action` at k=0 — the pair is equal at k=0 on both sides with the ported arm(s) cited — and the two counts move off their baselines (344/540 and 409/540) by a measured amount, reported whatever it is; mettaur trace70/70 unchanged; every isolated pixel row 0 with cursor's 10/9/170 tear reported; `fitted constants` not increased; the integrated variants named with their new numbers and at least one named mechanism for any new remainder.
**Measure and report.** row: battle_full, mettaur, popup, wave, cursor, result, opening integrated, field integrated, warp integrated, buster integrated, chip-use integrated (the trace rows plus the pixel rows this touches). frames: 540 for battle_full, 70 mettaur, 170 cursor, 40 each for the integrated variants. total/worst: per pixel row before and after (the integrated numbers being the leverage). region: for the trace, the (CurState, CurAction, timer, anim) per frame for k=-20..40 both sides; for any pixel regression, the diffmask region and frame. commit: src/objects.rs + src/actor.rs (the ported bridge arm). One line of mechanism (the cited arm canon uses to fast-forward from Hidden/Appearing to mid-HOP); one line of what is unverified (canon's Navi-side spawn arm, if only the virus arm was measured).
**Coordinator:** dispatch first. M2 trace gate is the highest-leverage unclosed M2 item (T7e landed the sequencer 540/540; the first divergence on the trace is the only thing left before M2's "first divergence none" can be claimed, and 5 integrated variants are gated on it). Worker muse-spark-1.3-contributor (T6's child class — this is the continuation of the Mettaur as data), verifier GLM-5.3-flash cross-family; ≤$0.15 expected, ≤$0.40 cap; verify_rows on battle_full, mettaur, popup, cursor, result, wave and the chip rows. Advances **M2**.

---

### T7l. Replace SEQ04_FRAMES=60 with `sub_801E754`'s canonical banner-idle check (T7j retry — verifier REFUTED the worker's "swap unsafe" claim)

**Files.** src/banner.rs (the banner composite and the HUD bit 0x8000 watch), src/battle.rs (the 0x04 leave edge only — `SEQ04_FRAMES=60` at `src/battle.rs:1186`), tools/trace.py (only if a watch field must be added), tools/harness.py (sequencer notes only), docs/coverage/windowclose.md (the measured predicate)
**Why.** T7j PARTIAL (`aa3486d`) kept branch UNMERGED because the worker claimed the swap was unsafe ("banner_at (line 2608) is armed on 0x04→0x00 transition"), and the verifier-minimax **REFUTED** that claim: "banner_at (line 2608) is armed on 0x04→0x00 transition and counts down independently of SEQ_04 age, so banner timing is orthogonal to the SEQ_04 leave edge — the swap is safe in this build." The branch is clean (verify_rows PASS on the seven named rows, fitted-constant count delta 0) and the verifier did the analysis that proves the swap safe. **Citation correction**: original ticket cited `sub_801E754` at `asm00_1.s:10478-10514` — that range is the **caller** `sub_8008064`; the function itself is at **`asm00_2.s:31011`** and the cite must move. `SEQ04_FRAMES=60` is one of 19 fitted constants in `src/` (per `docs/reviews/2026-09-14.md:174`) and violates the cross-cutting invariant "no fitted constants" (`AGENT_GUIDE.md:32`: "matched to the picture, mechanism unknown"). The fitted constant sits in the path T7e's sequencer arms set up: the 0x04 hold at `src/battle.rs:2536` (`SEQ_04 if self.seq.age >= SEQ04_FRAMES - 1 => self.seq.transition(SEQ_08)`) is one of the four edges T7f PARTIAL named in code as `sub_8008452`/`sub_800840C`/`sub_8008064` with timer arms 0x1e/0x293 in comments. T7f PARTIAL already validated the 0x04 hold window (canon frames 147..206, k=25..84) at 100/100 sequencer match on `windowclose_full`, so the work to do is small (build the banner composite model + replace the literal) and the verifier's analysis is in hand.
**Do.**
1. Re-record `tools/states.py`'s `windowclose_full` scenario on the current build and trace `0x020368C0` (the banner composite field, `ewram.s` lookup) plus HUD bit 0x8000 (the AIData bit `sub_801E754` reads) plus the `0x02036500` blink counter (`asm03_0.s:4801-4804`) across the 0x04 hold window (canon frames 147..206, k=25..84 per T7f) → *report, per side, the byte/halfword values for the banner composite on every frame of the 0x04 hold, the HUD bit's set/clear per frame, and the first frame `sub_801E754`'s banner-idle check would return 0.*
2. Port the banner composite model: a struct that mirrors `0x020368C0` (whatever fields it holds — read with `tools/probe.py --read` and parse the disassembly `reference/bn6f/asm/asm03_0.s` for the layout) and the HUD bit 0x8000 reader; on the 0x04 leave edge, replace `SEQ04_FRAMES=60` with the predicate `banner_composite.is_idle() && (hud & 0x8000) == 0` evaluated against the just-traced state → *cite `sub_801E754` at **`asm00_2.s:31011`** (NOT `asm00_1.s:10478-10514` which is the caller `sub_8008064`), report no `SEQ04_FRAMES=60` literal remains in `src/` (grep proves it), and `SEQ04_FRAMES` itself removed from `src/battle.rs:1186`.*
3. Re-run `tools/verify_rows.py` and the `windowclose_full` sequencer trace from a clean detached checkout → *report the table identical to HEAD except any pixel movement the new predicate causes (must be ≤ the cursor tear ≤10 px), the 0x04 leave frame on both sides (canon frame 206 vs ours — must match), and the fitted-constant count (must drop by exactly 1, 19→18).*
**Rules.** Only the named files; no allowlist change; no new harness row; no descriptor or alignment change; the fix is a port of `sub_801E754`'s check — no fitted countdown; every new literal gets a `// provenance:` tag with cite `asm00_2.s:31011` or the canon symbol; `fitted constants in src/` (HEAD: 19) MUST drop by exactly 1; canon never changes; ≤4 capture runs, one at a time inside the 3-slot semaphore.
**Acceptance.** `SEQ04_FRAMES=60` no longer in `src/` (grep proves it); the 0x04 leave frame on `windowclose_full` matches canon's frame 206 on both sides (the sequencer trace says so); the new predicate is cited `asm00_2.s:31011` with a `// provenance:` tag in the same commit; `fitted constants` count drops 19→18; the full harness table identical to HEAD except where the predicate moves cursor's tear (≤10 px); battle_full sequencer remains 540/540 unaligned; windowclose 0/0/40, result 0/0/40, mettaur 0/0/70, wave 0/0/90, opening 0/0/40, chip-cannon 0/0/40 all unchanged (T7j's clean verify_rows PASS baseline).
**Measure and report.** row: windowclose, cursor, result, mettaur, wave, opening, chip-cannon (regression set), plus the fitted-constant count. frames: 40 windowclose, 170 cursor, 540 battle_full sequencer. total: fitted-constant count before and after (19 → 18). worst: any pixel row that moves. region: the 0x04 leave frame's diffmask region. commit: src/banner.rs + src/battle.rs (only the SEQ04_FRAMES removal + the predicate arm). One line of mechanism (`sub_801E754`'s banner-idle check on the modeled composite, with the timer arms 0x1e/0x293 honoured); one line of what is unverified (whether the banner composite has fields not exercised by `windowclose_full` that the cannon/buster paths would see — those rows at 0/0 is the control).
**Coordinator:** dispatch second, after T7k lands (so the banner composite is built on top of the closed first divergence, not against an open structural gap). Worker muse-spark-1.3-contributor (T7j's child class), verifier GLM-5.3-flash cross-family; ≤$0.15 expected, ≤$0.40 cap; verify_rows on every row named plus the fitted-constant count. Advances **M2**.

---

### T7m. windowclose sequencer acceptance close-out — re-run T7f with T7l's canonical predicate in place, clean verify_rows PASS to land the four edges

**Files.** src/battle.rs (the four sequencer edges only — no new fitted frame-count constants), tools/states.py (only if the `windowclose_full` scenario needs a re-record), tools/trace.py (only if a watch field must be added), tools/harness.py (sequencer notes only), docs/coverage/windowclose.md (the named predicates)
**Why.** T7f PARTIAL left the worktree clean with "100/100 sequencer frame match on re-record" and "no fitted frame-count constants survive on strict-provenance reading", but **verify_rows did not get a clean run before budget** — the diff to land was empty because `SEQ04_FRAMES=60` (T7j's fitted constant) had to be replaced first. The four edges were named in code as `sub_8008452` (0x20 leave, on `sub_802D6C4`'s return ~11003-11008) / `sub_800840C` (0x00 → 0x04, gated on `sub_801483C` + `[r5+2]` latch at 10947-10976) / `sub_8008064` (0x04 → 0x08, on `sub_801E754`'s banner-idle check at 10478-10514 with timer arms 0x1e and 0x293). With T7l's banner composite in place — `SEQ04_FRAMES=60` replaced by the canonical `sub_801E754` banner-idle check at `asm00_2.s:31011` — the 0x04 leave edge now uses the canonical predicate, the `windowclose_full` scenario's 0x00 entered+left (144..146, 22..24) and 0x04 entered+left (147..206, 25..84) ranges from T7f hold, and a clean verify_rows run lands the chip-window sequencer acceptance with no fitted frame-count constants surviving. `windowclose` row is already 0/0/40/0 isolated (T7j baseline) — this ticket does not chase pixels; it lands the acceptance `battle_full sequencer 540/540` (T7e) and `windowclose_full sequencer 100/100` (T7f) together as the M2 sequencer gate, and removes the last fitted frame-count constant in `src/` (T7l drops the banner-wait one; this ticket removes the residue in the other three edges if any survived T7f).
**Do.**
1. Merge T7l's branch into a fresh worktree (`bash tools/worktree.sh t7m-windowclose-acceptance; git merge --ff-only wt/t7l`) and re-record `windowclose_full` scenario on the post-T7l build → *report the four edge frame ranges (0x24, 0x00, 0x04, 0x08) on both sides; confirm 0x00 entered AND left, 0x04 entered AND left per T7f acceptance; the 0x04 leave frame must be canon frame 206 (the T7l predicate's frame).*
2. Replace any remaining fitted frame-count constant in `src/battle.rs` with the canonical predicate → *name each ported line with `file:line` (e.g. `sub_8008452` for0x20 leave at `asm00_1.s:11003-11008`, `sub_800840C` for 0x00→0x04 at `asm00_1.s:10947-10976`, `sub_8008064` for 0x04→0x08 at `asm00_1.s:10478-10514`) and report no fitted frame-count constant survives in `src/` (grep proves it; `SEQ04_FRAMES=60` already gone after T7l).*
3. Re-run `tools/verify_rows.py` and the `windowclose_full` sequencer trace from a clean detached checkout → *report the table identical to T7l's result (battle_full sequencer 540/540 unaligned, every isolated row 0, cursor's10/9/170 tear reported); and the fitted-constant count (must drop by ≥1 from T7l's 18 baseline).*
**Rules.** Only the named files; no allowlist change; no harness row added; no descriptor or alignment change; no fitted frame-count constants survive (each replaced with the canonical predicate that names it); citations updated in the same commit; canon never changes; ≤8 capture runs total, one at a time inside the 3-slot semaphore.
**Acceptance.** `windowclose_full` scenario records 0x00 entered and left and 0x04 entered and left, with frame ranges named (both sides); the 0x04 leave frame matches canon's frame 206 (T7l's predicate); battle_full sequencer remains 540/540 unaligned (T7e's result); result's sequencer still 0x0C at k=0; every isolated row 0 with cursor's 10/9/170 tear reported; the four sequencer edges each named with the predicate each stands for (no fitted frame counts); `fitted constants in src/` count does not increase.
**Measure and report.** row: windowclose, cursor, result, mettaur, wave, opening, chip-cannon (regression set). frames: 40 windowclose, 170 cursor, 540 battle_full sequencer. total: fitted-constant count before and after (T7l: 18 baseline). worst: any pixel row that moves. region: the 0x04 leave frame's diffmask region. commit: src/battle.rs (only the four edge predicates; the SEQ04_FRAMES removal is T7l's commit). One line of mechanism (the four edges, each cited, each replacing a fitted count with the canonical predicate); one line of what is unverified (whether the 0x04 hold window's length is bounded by the banner composite's idle predicate alone, or whether the timer arms 0x1e/0x293 contribute a second gate — T7l's predicate tests idle only).
**Coordinator:** dispatch third, after T7l lands. The ticket depends on T7l's banner composite being in `src/` (`SEQ04_FRAMES=60` gone, predicate live); the worker merges T7l into a fresh worktree (`bash tools/worktree.sh t7m-windowclose-acceptance; git merge --ff-only wt/t7l`) and continues from T7f's branch. Worker muse-spark-1.3-contributor (T7f's child class), verifier GLM-5.3-flash cross-family; ≤$0.10 expected, ≤$0.30 cap; verify_rows on every row named. Advances **M2**.
