# Proposed tickets 20260913-054409 (Muse Spark 1.3 contributor, $0.0052)

### F12. The chip rows' own residues, smallest family first *(OPEN -- 2026-09-13)*
**Why.** After F5b the 43 chip-scoreboard rows share one route (`afterdissolve_0x0c` + `ALIGN_CHIP_0C`, harness.py:485/891) with 27 of 43 chip rows already at 0; the residue left is family by family: SuprVulc 2464 / 177 / 113, the bomb, seed and VDoll rows 10 to 8338, EnergBom and MegEnBom 19958 / 1871, the chip-family-0x15 rows 14740 to 73481. This pass takes only the smallest family (bomb/seed/VDoll, 10-8338 px), the most rows to 0 per dollar on the board.
**Do, in order.**
1. Baseline each row of this family with its harness line plus `tools/diffmask.py` region over the row's window, and report all three numbers.
2. Localize the residue to frames and an element per row (effect object, tiles, palette), and report the frame window and region per row.
3. Cite canon's routine for that element in `reference/bn6f` (file:line) and name the fix, and report the predicted frame window the fix moves.
4. Fix in `src/` (or fixture/descriptor with `peeked` provenance if the residue is the fixture), then re-run the family rows plus wave, window, opening, chip-cannon and the full table, and report every harness line.
**Rules.** `src/` only unless the residue is proven fixture; no allowlist change; no alignment or region change except by measured event (F2's rule); captures one at a time.
**Measure and report.** Row by row: row · frames · total · worst · region · commit, each with its before/after harness lines over the same window.
**Coordinator:** verify_rows on every row the report names plus the full table; the verifier only for claims beyond harness lines; mark PARTIAL with which family is done and leave OPEN for the next family.
### F15. tiles/gauge: the one vblank frame, 208 px at k=7 *(OPEN -- 2026-09-13)*
**Why.** F6 left `tiles`/`gauge` (one capture, two rows, harness.py:1180-1181) at k=0..6 exactly 0 and 208 px on k=7, the vblank-race residue HANDOFF §9 records; same merge window `cursor` went 620802 -> 620914 (+112) although F3's code cannot run in that row (no OK press), the custmatch/cursor VRAM write HANDOFF §9 says lands a frame early between differently sized binaries. Two rows to 0 for one single-frame attribution.
**Do, in order.**
1. Baseline `tiles` and `gauge` with harness lines plus `tools/diffmask.py` region at k=7, and report both lines with the frame window.
2. Run `--watch-write` on both sides over k=6..7, name the writer and the frame each side lands on, and report the writer address and frame for canon and ours.
3. Make ours land where canon's does, re-run `tiles`/`gauge` plus wave, window, opening, chip-cannon, and report all five harness lines.
4. Measure `cursor` separately with `--watch-write` on both sides (no fix on this ticket unless the same writer), and report its harness line before/after to prove no silent code-size move.
**Rules.** `src/` only; no allowlist change; no alignment change; captures one at a time; a row that gets worse is reported, not hidden.
**Measure and report.** tiles · frames · total · worst · region · commit · one line of mechanism (which write, which frame) · one line of what is unverified; same shape for gauge and cursor.
**Coordinator:** verify_rows on tiles, gauge, cursor plus wave/window/opening/chip-cannon; the verifier only for the writer/frame claim beyond harness lines; one wrong guess gets one follow-up, then BLOCKED.
### F13. `card` isolated: 18486 px over 16 frames *(OPEN -- 2026-09-13)*
**Why.** `card` (harness.py:1398, `demo-cardname` check, `window_cursor=0`) reads 18486 px over 16 frames; HANDOFF §10 names the chip window's cursor-move timing as the cause; the sibling `window` row is already at 0, so the residue is the card-name picture path, not the deck/window fields. One row to 0 in a 16-frame window, cheaper attribution than `mettaur` (31075 px over 70 frames).
**Do, in order.**
1. Baseline `card` with its harness line plus `tools/diffmask.py` region and the first non-zero pixel frame, and report the line with the frame window.
2. Localize the residue to frames and the cursor/picture element against the at-0 `window` row on the same `CUSTMATCH_ROW`, and report both rows' numbers over the same window.
3. Cite canon's cursor-move routine in `reference/bn6f` (file:line) for that element and name the mistimed write, and report the predicted frames the fix moves.
4. Fix in `src/` (or fixture/descriptor with `peeked` provenance), then re-run `card`, wave, window, opening, chip-cannon and the full table, and report every harness line.
**Rules.** `src/` only unless proven fixture; no allowlist change; no alignment or region change except by measured event (F2's rule); captures one at a time.
**Measure and report.** card · frames · total · worst · region · commit · one line of mechanism · one line of what is unverified, each with before/after numbers over the same window.
**Coordinator:** verify_rows on card plus every row the report names; the verifier only for claims beyond harness lines; a PARTIAL from a wrong guess gets one follow-up ticket, then BLOCKED and move on.
