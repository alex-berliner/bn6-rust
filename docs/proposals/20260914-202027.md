# Proposed tickets 20260914-202027 (minimax/MiniMax-M3, $0.0842)

### T7j. Banner-idle predicate replaces the SEQ04_FRAMES=60 fitted constant

**Files.** src/battle.rs (the SEQ_04 transition at line 2536 + the banner struct fields used by the new predicate), tools/trace.py (only if a field must be watched to verify the predicate), docs/coverage/battle_full.md (notes)

**Why.** T7f PARTIAL left one fitted constant on a strict-provenance reading: `const SEQ04_FRAMES: u16 = 60` at src/battle.rs:1186 drives the SEQ_04 → SEQ_08 edge at line 2536, and the verifier refused to promote it because the canonical leave predicate is **named, cited, and not modelled**: `sub_801E754`'s banner-idle check at asm00_1.s:10478-10514, which returns 0 only when the HUD bit `0x8000` clears AND the two timer arms `0x1e` (effect ID 30, the spawn banner) and `0x293` (effect ID 659, the close banner) have both reached their final tick. The current code counts frames because it has no banner composite: the `banner` struct (battle.rs:1318 `banner_done`, :1339 `banner_at`) only knows about the chip window's BATTLE START! banner, not the per-frame HUD bit. `sub_801E792` (asm00_2.s:31055-31112) is the routine that arms the timer pair via `sub_801BECC(1<<15)` — its return value is the bit `sub_801E754` reads. Replacing the constant with the predicate is the one move that closes the T7f PARTIAL cleanly and removes a fitted constant without touching any other state edge.

**Do.**
1. Baseline the trace on `tools/states.py`'s `windowclose_full` (T7d's scenario) with the current build → *report the SEQ_04 frame range (canon k=136..195, 60 frames; ours k=136..195, 60 frames — currently 100/100 sequencer match per T7f) and the HUD field's per-frame `0x8000` bit and the two timer arms' remaining-tick fields (named in the trace export).*
2. Port sub_801E754's predicate into the SEQ_04 → SEQ_08 edge: arm both timer fields (`banner_timer_30` and `banner_timer_659`) inside the `sub_801E792` equivalent (the `banner_at` arm at battle.rs:3133-3140) and transition only when the HUD bit is clear AND both timers have reached their final tick → *report the cited `file:line` ranges (sub_801E754 asm00_1.s:10478-10514, sub_801E792 asm00_2.s:31055-31112, sub_801BECC asm00_2.s:31055) and the resulting trace frame range on `windowclose_full` and `battle_full` (must equal canon's 60 frames; report the new first-divergence k if any).*
3. Delete `SEQ04_FRAMES` from src/battle.rs and verify no other reference survives (`grep -n SEQ04_FRAMES src/`) → *report the grep result (zero hits outside the comment line being removed) and `fitted constants in src/` count unchanged (HEAD: 19).*
4. Re-run `tools/verify_rows.py` from a clean detached checkout → *report the table against T7e's result (battle_full sequencer 540/540, every isolated row 0, cursor's tear reported as it moves from its T7e value), and the windowclose_full sequencer still 100/100 with SEQ_04 frame range named on both sides.*

**Rules.** Only the named files; no allowlist change; no harness row added; no descriptor or fixture change; no fitted frame-count constant survives (the deleted `SEQ04_FRAMES` is replaced by the cited predicate); citations updated in the same commit; the 43 chip rows, wave, window, opening, cannon, field, banner, warp, card, tiles, gauge, mettaur, popup, buster, result and windowclose must not move; canon never changes; ≤6 capture runs, one at a time inside the 3-slot semaphore.

**Measure and report.** row: windowclose_full sequencer, battle_full sequencer, cursor, windowclose, the 43 chip rows, wave, mettaur, popup, buster, result, field. frames: 100 (windowclose_full), 540 (battle_full), 170 (cursor), 40 each for the others. total/worst: per pixel row before and after. region: for the SEQ_04 frame range, the two sides' `k` enter and leave frames; for any pixel regression, the diffmask frame and region. commit: src/battle.rs (SEQ_04 transition + banner struct fields), tools/trace.py only if a watch field was added. One line of mechanism; one line of what is unverified (whether the HUD `0x8000` bit's clearing is the SAME field both routes arm — `battle_full` uses an opening banner with effect 0x1e, `windowclose_full` uses the close path with effect 0x293, and the predicate's two-arm check has to hold across both).

**Acceptance.** `SEQ04_FRAMES` deleted from src/; the SEQ_04 → SEQ_08 edge is `banner.is_idle()` on the cited predicate (HUD bit `0x8000` clear AND timer 0x1e at final tick AND timer 0x293 at final tick), named in a `// canon: sub_801E754 (asm00_1.s:10478-10514) via sub_801E792 (asm00_2.s:31055-31112)` comment; windowclose_full's SEQ_04 frame range on the ported side is canon's 60 frames (k=136..195 ±0); battle_full's sequencer remains 540/540 unaligned (T7e); every isolated pixel row reads 0 with cursor's tear reported as it moves; `fitted constants` count not increased.

**Coordinator:** dispatch first. M2 sequencer is the lowest unmet milestone and T7f's PARTIAL is precisely diagnosed with a named predicate; this is the work that closes the fitted-constant residue. Worker muse-spark-1.3-contributor (T7f's child class), verifier GLM-5.3-flash cross-family; ≤$0.10 expected, ≤$0.30 cap; verify_rows on every row named. Advances **M2**.

---

### T7k. battle_full's first divergence via canon's PAUSED-to-HOP transition (NOT T7h's SPAWN arm)

**Files.** src/objects.rs (the `MettaurEntry` per-type entry, only the PAUSED-init arm — no SPAWN arm added), src/actor.rs (the 0x0A HOP executor's entry condition, only if canon's PAUSED path differs), tools/trace.py (only if a field must be watched to verify the init), tools/oracle.py (only if a new field is required to see the snapshot), docs/coverage/battle_full.md (notes)

**Why.** T7h NEGATIVE proved the SPAWN-arm port is the wrong mechanism: it regressed cursor 10/9/170 → 741/728/170, the trace counts `enemy_state_action 344/540` and `enemy_anim 409/540` were UNCHANGED, and the diagnosis it left was that the divergence is structural — canon's PAUSED-state ROM snapshot captures the Mettaur already at `(4, 0x0A)` (HOP), while ours creates the enemy fresh at fixture load and runs the 62-frame Hidden/Appearing fade before any entry arm fires. The same diagnosis (T7i's option (a) "widen scope to allow upstream per-scenario ALIVE/PAUSED choice change") is one of three blockers a human-session note listed, but the **in-scope** mechanism is different: cannon's per-type entry must, on a PAUSED scenario, set the enemy's `CurAction` directly to `0x0A` and skip the spawn arm — i.e., the entry has a "from-saved-state" branch that the current `MettaurEntry` does not model. The candidate citation is `playerObject_main_80EA460` (asm31.s:107131, T7i's coverage table) and `ForMettaur_8109EF4` (asm31.s:170982, T6): the per-type entry's caller checks the save state's `BattlePaused` byte before entering `RunSpawnAnimationMaybe_8016380` (asm00_2.s:16009), and the PAUSED-side path emits the (4, 0x0A) pair without running the spawn sequence. This is the **port-under-the-interpreters** mechanism T7h's rules require; it does NOT change the fixture and does NOT change the descriptor contract.

**Do.**
1. Baseline the trace with no edit (`tools/trace.py record/diff --align row:battle_full`) and read the Mettaur's PAUSED-side init path on canon with `tools/probe.py` — read `eBattleState.IsBattlePaused` (`0x02034840`, `ewram.s:2556`), the Mettaur's `BattleObject` at `0x0203ab68` (+0x04 state, +0x09 action, +0x20 timer, +0x0E anim), and the `BattlePaused` byte's writer PC across the save → *report (per side, per frame k=0..40): the Mettaur's `(CurState, CurAction, timer, anim)` quadruple, the `IsBattlePaused` value, and the frame canon's CurAction becomes `0x0A` (expected k=0).*
2. Port canon's PAUSED-side init: in `MettaurEntry`'s setup arm, read `IsBattlePaused` and, when it is non-zero, set `CurAction = 0x0A` and `CurState = 4` directly without invoking `RunSpawnAnimationMaybe_8016380`'s spawn-wait — cite `playerObject_main_80EA460` (asm31.s:107131) for the entry condition and `ForMettaur_8109EF4` (asm31.s:170982) for the per-type caller → *report the cited `file:line` ranges, the resulting `(CurState, CurAction, timer, anim)` at k=0 on both sides, and the new first divergence on battle_full (field + k + canon/rust values; expected: no longer `enemy_state_action` at k=0).*
3. Re-run the same diff and the mettaur/wave/popup traces on the same scenario set → *report `enemy_state_action` and `enemy_anim` frame counts (baseline 344/540 and 409/540), the new first divergence, and mettaur `70/70` unchanged.*
4. Re-run `tools/verify_rows.py` from a clean detached checkout → *report the table against HEAD (every isolated row 0, cursor's `10/9/170` tear reported as it moves), with no descriptor/allowlist change.*

**Rules.** Only the named files; no new harness row and no change to `tools/harness.py`, `tools/states.py`, `FIXTURE.md` or the descriptor contract; no allowlist change of any kind; no alignment change except by measured event; the fix is a port under the already-landed `MettaurEntry` interpreter (T6) — no new hand-written pose and no per-scenario branch; every new literal in an edited line gets a `// provenance:` tag or the canon symbol; `fitted constants in src/` (HEAD: 19) may not increase; the 43 chip rows, mettaur isolated, wave, window, windowclose, popup, buster, result and field must not move; canon never changes; ≤6 capture runs, one at a time inside the 3-slot semaphore.

**Measure and report.** row: battle_full (trace), mettaur, popup, wave, cursor, result, field, the 43 chip rows. frames: 540 for battle_full, 70 mettaur, 170 cursor, 40 each for the others. total/worst: per pixel row before and after; for the trace, the two counts (baseline 344/540 and 409/540) and the new first divergence's field and k. region: for the trace, the (CurState, CurAction, timer, anim) quadruple frame by frame for k=0..39 both sides; for any pixel regression, the diffmask frame and region. commit: src/objects.rs (MettaurEntry PAUSED-init arm) + src/actor.rs (only if the HOP executor's entry condition differs). One line of mechanism; one line of what is unverified (whether the `IsBattlePaused` reader byte is the only field the entry checks, or whether the per-type caller also reads the save state's `CurBattleDataPtr` for the slot count before deciding).

**Acceptance.** battle_full's first divergence is no longer `enemy_state_action` at k=0 — the `(CurState, CurAction, timer, anim)` quadruple is equal at k=0 on both sides, with the ported arms cited — and the two counts move off their baselines (344/540 and 409/540) by a measured amount, reported whatever it is; mettaur's trace stays divergence-free on 70/70; every isolated pixel row reads 0 as it does today (43 chips included), cursor's tear reported; `fitted constants` count not increased.

**Coordinator:** dispatch second, after T7j lands (so the SEQ_04 transition is canonical and the trace's window-setup frames stay at 540/540). Worker muse-spark-1.3-contributor (T6's child class — the Mettaur as data), verifier GLM-5.3-flash cross-family; ≤$0.20 expected, ≤$0.45 cap; verify_rows on mettaur, wave, popup, cursor and the chip rows. Advances **M2**.

---

### T9h. The AI handshake byte inside `is_MegaMan_in_range?`: write-watch it on battlestart_gunner over 700 frames

**Files.** tools/probe.py (the targeted write-watch harness), tools/states.py (the `battlestart_gunner` recipe's poke list — only if a poke is found in step 1 that makes the sequencer advance), docs/trace/t9h/ (new — the watch logs), docs/coverage/battlestart_gunner.md (the corrected measurement, replacing T9g's "AI handshake byte inside is_MegaMan_in_range? is the next stop" sentence with the named byte)

**Why.** T9g NEGATIVE closed the two earlier Gunner measurements: the 700-frame settle (Gunner parks at CurState=0x04/CurAction=0x01 at f151, HP `0x3c` and gauge `0x00200000` unchanged through f699; sequencer 0x00→0x04000000 at f166 then held) and the panel-row-3 probe (identical trajectory; `is_MegaMan_in_range?` does not unstick on row 3 alone). The verifier-minimax CONFIRMED that the RNG loop is alive (637 unique seed values over 701 frames, ~1 draw/frame), so the AI scheduler is running — but the AI handshake inside `is_MegaMan_in_range?` never trips. The next stop the verifier named is precise: the byte the handshake reads. The most likely candidate is the per-virus "act now" latch that `sub_8112D9C` (the 234-frame heartbeat, the per-virus AI scheduler) writes into the Gunner's BattleObject to release the `is_MegaMan_in_range?` check — likely `+0x42` `oAI_ActFlag` or `+0x46` `oAI_TargetPanelX` (naming candidates the verifier flagged). The measurement: write-watch `0x0203ab68+0x00..0x0203ab68+0x60` for 700 frames on both sides, on the T9e trigger-poked scenario, with the slot1 fields frozen at the f151 park reported in T9g — the byte that DOES get written canon-side but NOT ours is the gate.

**Do.**
1. Write-watch the full Mettaur slot1 BattleObject field range `0x0203ab68+0x00..+0x60` (74 bytes — every `oBattleObject_*` field up to the per-object tail) on `battlestart_gunner` with the T9e trigger poke (`--poke-at 165:0x020364C0:0x08`), 700 frames, both sides → *report every writer PC + byte value, per field, with the freeze frame (T9g: f151) flagged on both sides; a per-field table of "writer PC, byte value, frame, canon-side yes/no, ours-side yes/no" is the deliverable, with one column highlighted as "this is the gate".*
2. If a single field differs (most likely): pin that field with `tools/probe.py --watch-write` on JUST that byte for 700 frames and run `--poke-at <frame>:<address>:<value>` on ours-side to reproduce the writer → *report the byte, the value, the frame to poke, and the resulting Gunner trajectory (whether `CurAction` advances off `0x01` within the next 234 frames, whether HP or gauge moves from `0x3c` / `0x00200000`, and the writer PC the poke emulates).*
3. If a single field differs (alternate): write-watch the ROM-side address canon reads to populate that field — likely `sub_8112D9C`'s per-virus slot at `0x0203ab68 + oAI_*` via the AS-mid hook the heartbeat writes (the verifier listed `sub_8112D9C asm32.s:10343` as the candidate caller) → *report the disassembly citation, the byte that writer targets, and the byte our side never writes (a struct init or a per-frame call we do not make).*
4. Write the result into `docs/coverage/battlestart_gunner.md` (replacing T9g's "AI handshake byte inside is_MegaMan_in_range? is the next stop" with the measured byte name + frame + value) and drop raw logs in `docs/trace/t9h/` → *report the doc's new finding section and the log file list. If step 2 produced a one-shot poke that releases the Gunner, name the poke line in the ticket's acceptance — that becomes the gate the row ticket keys off.*

**Rules.** No `src/*`, no `tools/harness.py` (this ticket adds NO row), no `tools/mgba_capture.c`, no `tools/trace.py`, no `FIXTURE.md`, no `assets/`, no other `docs/coverage/`; `reference/bn6f` read-only. `tools/states.py` may gain the poke ONLY if step 2 produces it, and the poke line carries its address/width with the `eS20364C0` symbol and `ewram.s` line. ≤4 capture runs total (one per step + a control). Every address named with its `ewram.s` line or ROM label. If the recipe changes, `python3 tools/states.py build all` must still build what it builds today, and report that.

**Measure and report.** row: none (trace ticket). frames: 700 per route, both sides. total/worst/region: n/a, no pixel comparison; the numbers are the per-field writer table from step 1, the named byte from step 2/3, and the Gunner's trajectory (CurState, CurAction, HP, gauge) frame by frame after the named-byte poke if any. commit: `tools/states.py` only if step 2 passes, plus the docs. One line of mechanism; one line of what is unverified (whether the named byte is the AI handshake OR a downstream consumer that needs the upstream handshake to also fire — if the named-byte poke alone does not release the Gunner, the next stop is the byte that writes the named byte, named in the same `ewram.s` line).

**Acceptance.** `docs/trace/t9h/` holds the watch logs; `docs/coverage/battlestart_gunner.md` names the byte with its frame and value on both sides; and EITHER the byte's writer is identified canon-side AND ours-side's frozen-frame path is named (so the row ticket knows what to fix), OR a one-shot poke is found that releases the Gunner (HP or gauge moves off `0x3c` / `0x00200000` within 700 frames, with the byte, frame, and value reported). A precise negative ("no byte in `+0x00..+0x60` is written canon-side either; the handshake is upstream of the slot") is the desired output and closes this ticket.

**Coordinator:** dispatch third, after T7j lands (independent file set; `tools/probe.py + tools/states.py + docs/*` are disjoint from T7j's `src/battle.rs + tools/trace.py`). Worker muse-spark-1.3-contributor (T9g's child class), verifier GLM-5.3-flash cross-family; ≤$0.15 expected, ≤$0.40 cap. Advances **M5**.
