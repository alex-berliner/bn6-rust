# Proposed tickets 20260917-064039 (zai/glm-5.3, $0.1833)

### T106. M3 counter hit: canon's Full-Synchro grant on the damage path, from the writer of 0x0203528F *(OPEN -- 2026-09-17)*

**Why.** SCOPE M3 names "counter hit and Full Synchro"; no ticket in TODO.md/TODO_ARCHIVE.md has ever touched it (grep "counter" hits nothing in the queues) — a fresh objective, and the complement of the OPEN ticket T105, which is the emotion window's *render* gate only. T105's landed recon: emotion byte = eStruct2035280+0xf = **0x0203528F** (asm00_2.s:27649), updater sub_801CADC (asm00_2.s:25577), face bank off_801CD08 = 23 entries. Our port has **no counter machinery at all**: grep src/ for counter/Counter returns only ai.rs planner pacing and script.rs script counters — the damage path never tests the enemy's attack phase. The swing window is already measured on a live row: mettaur 0/0/70, canon's 106-frame attack cycle (F25 PARTIAL), shockwave flight 44 frames ours vs 45 canon (F25c PARTIAL), spray closed (F25d DONE); a chip press is deliverable at a chosen tick through AIData (F5b DONE, chip-cannon 0/0/40).

**Files.** `src/battle.rs` (the damage path: enemy attack-phase test + the grant + the popup), `src/banner.rs` (the counter popup if canon draws it through the banner/popup path), `src/fixture.rs` (press-tick + poke fields), `tools/states.py` (ONE scenario `counter_hit_full`), `tools/harness.py` (ONE row `counter_hit` + its negative), `docs/coverage/counter.md` (NEW), `docs/worklog/T106.md`. **NOT** src/emotion.rs (T105's), NOT tools/trace.py, tools/oracle.py, tools/inventory.py, tools/mgba_capture.c, reference/bn6f.

**Do.**
1. Recon: on the mettaur scenario, `--watch` 0x0203528F while delivering one Cannon press inside and one outside the swing window; name canon's writer (file:line), the value it stores for Full Synchro, the enemy-side predicate the damage path tests, and the counter popup's art/id with cites. **measurement.**
2. Record `counter_hit_full` = mettaur base + scripted A press at the in-window tick; canon capture; measure the frame the emotion byte flips, the popup's first/last frame and pixel region, both sides' HP. **measurement.**
3. Port the grant: the attack-phase test, the emotion-byte write, the popup; rebuild ROM, sha256+size. **code change + measurement.**
4. Row `counter_hit` canon vs rust over the scene; negative = the same scenario with the press moved outside the window (no counter on either side, equals the chip-fire baseline). **measurement.**
5. Trace `tools/trace.py` on `counter_hit_full`: fields {emotion byte, enemy HP, enemy CurAction, popup phase} first divergence **none** over the scene; guard set + cursor. **measurement.**

**Rules.** Canon never changes; press/poke provenance `peeked`; every frame number from a watch or a cited line, no fitted constants; no allowlist. Cursor veto ≤1/1/170/186300. ≤6 captures, tool budget ≤80, one harness command at a time.

**Acceptance.** `counter_hit` 0/0/N with a non-blind negative; trace first divergence **none** on the four fields over the scene's frames; guard set + cursor identical to baseline; verify_rows PASS is the veto. If T105's face gate has not landed when this dispatches, the two OBJ tiles at (0,18)/(32,18) are T105's named item: report that region separately and close on the trace plus every other region at 0. NEGATIVE naming canon's measured no-grant behaviour (frames + bytes + cite) closes it.

**Measure and report.** row · frames · total · worst · region · commit · mechanism · unverified, plus the writer cite, the value written, and the in/out-window frame pair. **Coordinator:** owns src/battle.rs/src/banner.rs + the row; runs alone (states.py/harness.py overlap T105's). verify_rows from a clean checkout on the guard set + the new row. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M3 (counter hit / Full Synchro: unrecorded → ported+verified).

---

### T107. M4 Cannon family as data: finish the record dispatch with the measured layout pad, ids 1-3 onto the AS_DATA census *(OPEN -- 2026-09-17)*

**Why.** Cannon/HiCannon/M-Cannon (ids 1-3, data/ChipDataArr.s:34/:65/:96) read 0/0 on their rows but dispatch through our own id arm; T36 PARTIAL landed the AttackPower+0x1a read (be6e706) and deferred the family rewrite; T38 then T51 closed on the cursor veto, the code-size/layout class T56 named. The census gate is `AS_DATA_FAMILIES = {0x13, 0x15, 0x21}` (tools/inventory.py:237): reading the three rows' own AttackFamily byte and dispatching on it moves them `verified-pixels → verified` (43 → 46 as data).
**New evidence.** T78 (merged e7ec05d, after T51's close) re-landed exactly this class of cursor-vetoed chip port (AreaGrab/Invisibl) using F12's pure-layout pad with cursor preserved — the cure is measured, not hypothesised.

**Files.** `src/battle.rs` (the Cannon arm → family const), `tools/inventory.py` (AS_DATA_FAMILIES + census), `docs/SCOPE.md` (generated rows only), `docs/worklog/T107.md`. **NOT** src/chips.rs, src/objects.rs, tools/states.py, tools/harness.py, reference/bn6f.

**Do.**
1. Read ids 1-3 family/subfamily bytes from ChipDataArr (cite the three lines); census via tools/inventory.py which other ids carry the same family — no over-match, or name the subfamily split that keeps the arm exact. **measurement.**
2. Baseline: the three chips' rows + guard set + cursor, ROM sha256/size. **measurement.**
3. Port: replace the id arm with the family const, cite the ROM's own attack-family dispatch for that family (include/rom_structs/ChipData.inc offsets); rebuild. **code change + measurement.**
4. If cursor leaves 1/1/170, apply T78's pure-layout pad (cite e7ec05d's mechanism) until the veto holds; report size before/after. **measurement.**
5. Extend AS_DATA_FAMILIES, rerun `python3 tools/inventory.py`, regenerate SCOPE; the three rows 0/0/N unchanged; census 43 → 46. **measurement.**

**Rules.** No new rows, no scenario edits — the rows exist and stay the gate. Provenance tags on the new consts; no fitted constants; no allowlist. Cursor veto ≤1/1/170/186300. ≤4 captures, tool budget ≤60.

**Acceptance.** The three rows 0/0/N identical before/after the port; cursor ≤1/1/170/186300; inventory flips ids 1-3 to `verified`; verify_rows PASS is the veto. NEGATIVE with the pad's measured failure (cursor numbers + ROM-size spread, both directions) closes it.

**Measure and report.** row · frames · total · worst · region · commit · mechanism · unverified, per chip; the family byte + census table; ROM sha256/size before/after the pad. **Coordinator:** owns src/battle.rs; pairs with nothing that touches battle.rs or inventory. verify_rows clean-checkout on the guard set + the three rows. ≤$0.15 expected, ≤$0.30 cap.

**Milestone advanced:** M4 (chips as data 43/411 → 46/411).

---

### T108. M8 results variants, first: the LOSER window end to end — MEGAMAN DELETED banner to LOSER slide at 0 *(OPEN -- 2026-09-17)*

**Why.** SCOPE M8 reads "win results only". The port already carries the LOSE half, unexercised: results.rs:4 cites sub_802C34E (asm03_0.s:12353) "or the LOSER window when the player is", VARIANT_N=2 with LOSE=1 (results.rs:20-23) and LOSE-only phases (Waiting→Dismissing, no reward, results.rs:772-775); battle.rs:3077-3079 already picks banner::MEGAMAN_DELETED (banner.rs:49) on `megaman.is_defeated()`. The WIN row is 0/0/40 (F34b DONE). No ticket has recorded a defeat. The mettaur scenario's shockwave reaches MegaMan (F1 DONE; 44/45-frame flight, F25c PARTIAL) — a defeat is one HP poke away on an existing live scenario.

**Files.** `src/battle.rs` (the defeat route → Results LOSE), `src/results.rs` (variant selection/slide if it diverges), `src/banner.rs` (banner timing if it diverges), `src/fixture.rs` (HP poke field), `tools/states.py` (ONE scenario `result_lose`), `tools/harness.py` (ONE row + negative), `docs/coverage/results.md` (NEW), `docs/worklog/T108.md`. **NOT** src/objects.rs, tools/trace.py, tools/inventory.py, reference/bn6f.

**Do.**
1. Recon canon: mettaur base + MegaMan HP poked to a value the shockwave kills; watch the HP field, the death edge, the MEGAMAN DELETED banner frames, the LOSER window's first slide frame; cite the asm caller that selects the LOSER variant. **measurement.**
2. Scenario + row `result_lose`; negative = HP poked to survive (fight continues; row equals the mettaur baseline's region table). **code change + measurement.**
3. Port the divergent edge (defeat detection → banner → LOSER slide/dismiss per results.rs's own phases); rebuild, sha256. **code change + measurement.**
4. Row 0/0/N end to end; trace fields {megaman HP, seq state, results phase, banner id} first divergence **none**; guard set + cursor. **measurement.**

**Rules.** Canon never changes; the HP poke carries `peeked` provenance; no fitted frame counts — the slide/dismiss numbers come from the capture or cited lines; no allowlist. Cursor veto ≤1/1/170/186300. ≤6 captures, tool budget ≤80.

**Acceptance.** `result_lose` 0/0/N with a non-blind negative; trace first divergence **none** on the four fields; guard set + cursor identical to baseline; verify_rows PASS is the veto. NEGATIVE naming canon's actual route from this base with cites (e.g. it leaves the battle scene for a game-over screen — frames + the branch's file:line) closes it.

**Measure and report.** row · frames · total · worst · region · commit · mechanism · unverified, plus the death-edge frame, banner span, first-slide frame, and the LOSER-caller cite. **Coordinator:** owns src/battle.rs/src/results.rs + the row; runs alone (states.py/harness.py). verify_rows clean-checkout on the guard set + result + result_lose. ≤$0.20 expected, ≤$0.40 cap.

**Milestone advanced:** M8 (results variants 1/4 → 2/4; also M2's "end of battle" row coverage).

---

### T109. M8's second arena: export the id-6 backdrop art as data, select it on the Background byte, row at 0 *(OPEN -- 2026-09-17)*

**Why.** T70 (DONE) measured the byte→art map: BattleSettings+0x4 read at battle init by sub_8081308 (asm21.s:469-523, ldrb :473-475, called from sub_8080DA0 :15-45) indexes off_8080F98[r] → LoadBGAnimData (asm03_0.s:21209); id 6 = the RobotControlComp art; scripted battles source the byte from byte_203CA50 (battleSettings_802D2B2, asm03_0.s:14599); T70 verified the mapping by poke. The port renders exactly one arena: assets/backdrop.bin (BNBD, src/backdrop.rs:115-118) with one schedule STEP_ORDER/STEP_HOLD read off off_807FB98 (src/backdrop.rs:69-75). SCOPE M8 reads "one arena"; no row exists for any second backdrop.

**Files.** `tools/backdrop_export.py` (second art set), `assets/` (new .bin), `src/backdrop.rs` (second BNBD + schedule + selection), `src/fixture.rs` (background field), `tools/states.py` (ONE scenario `bg_arena2`), `tools/harness.py` (ONE row + negative), `tools/inventory.py` (census row), `docs/SCOPE.md` (generated), `docs/coverage/backdrops.md` (NEW), `docs/worklog/T109.md`. **NOT** src/battle.rs, reference/bn6f, tools/mgba_capture.c.

**Do.**
1. Export id 6's tile/map/palette tables from its LoadBGAnimData pointers; byte-exact check against reference/bn6f, citing each table's file:line; measure its step schedule (steps × frames/step). **measurement.**
2. Scenario `bg_arena2`: scripted battle with the Background byte poked (T70's route) on both sides; canon capture; measure the anim's step boundaries from the dump. **measurement.**
3. Port selection: second BNBD + its own schedule const, chosen by the fixture's background byte (`peeked`); rebuild, sha256+size. **code change + measurement.**
4. Row `bg_arena2` 0/0/N over ≥ one full anim cycle; negative = same scenario without the poke (first arena, equals the field row's BG1 region). **measurement.**
5. Slotwise table (slot k = FRAMES[step][k-1], T22's convention) for the new arena, both sides; inventory regen adds the census row. **measurement.**

**Rules.** Art from canon bytes, never redrawn; no permutation guesswork — the slotwise table is the proof; no allowlist. Cursor veto ≤1/1/170/186300. ≤6 captures, tool budget ≤80.

**Acceptance.** `bg_arena2` 0/0/N with a non-blind negative; slotwise table all-zeros for every step/slot; guard set + cursor identical; verify_rows PASS is the veto. NEGATIVE naming the measured blocker (e.g. the poke route does not reach sub_8081308 on this base — frames + watch + cite) closes it.

**Measure and report.** row · frames · total · worst · region · commit · mechanism · unverified, plus the export's byte-exact table cites and the step schedule. **Coordinator:** owns src/backdrop.rs + the exporter; pairs with nothing touching backdrop/states/harness. verify_rows clean-checkout on the guard set + the new row. ≤$0.25 expected, ≤$0.50 cap.

**Milestone advanced:** M8 (arenas: second backdrop, unrecorded → verified) and M1's backdrops census.

---

### T110. M10's first surface: two linked cores in the capture tool — a paired, reproducible canon-vs-canon link dump *(OPEN -- 2026-09-17)*

**Why.** T97 (DONE, d63a3c7 on main) landed the netbattle recon — handshake + chip-trade routines with cites, the link UI state field, and a two-instance scenario design (docs/coverage/netbattle.md, netbattle_recon.md). SCOPE M10 stays "not started (needs link emulation in the capture tool)": tools/mgba_capture.c drives exactly one headless core today, and no M10 measurement of any kind can exist until two cores link. Zero src/ changes, so no row veto applies.

**Files.** `tools/mgba_capture.c` (a `--link` two-core mode: both sides' frame dumps + state exports), `tools/harness.py` (the capture flag only, no row), `docs/coverage/netbattle.md` (design + captures), `docs/worklog/T110.md`. **NOT** src/, tools/states.py, tools/inventory.py, reference/bn6f, vendor/ (read-only: cite what it offers, never patch it; a needed vendor change is the coordinator's decision).

**Do.**
1. Recon the vendored libmgba (vendor/) for its link/SIO-multiplayer connector between two cores in one process (cite file:line) against T97's design; name the wiring chosen. **measurement.**
2. Extend tools/mgba_capture.c: two cores, link wired, per-side frame dump + state export, `--link` flag; build. **code change + measurement.**
3. Paired capture: canon ROM ×2 from T97's scenario start (link-wait → handshake) for N ≥ 120 frames; watch the UI state field T97 named on both sides; name its per-side transitions with frames. **measurement.**
4. Determinism: two independent runs, 0 differing pixels per side (T87's convention). **measurement.**
5. Land the design, captures and the transition table in docs/coverage/netbattle.md; no harness row yet — our side cannot handshake, the row arrives with the first port. **measurement.**

**Rules.** Canon never changes; the ROM runs unmodified on both sides; every transition frame from a watch, cites for every routine named; no allowlist, no fitted constants. Guard set untouched by construction (no src/). ≤8 captures, tool budget ≤80.

**Acceptance.** A paired two-instance dump exists, reproducible 0-diff across two runs; the handshake field's per-side transitions named with frames and cites; the capture tool rebuilt and used for both runs. NEGATIVE with the vendored-API cites (which connector is missing for headless linking) closes it.

**Measure and report.** per side: frames · total · worst · region · commit · mechanism · unverified, plus the transition table and the vendor cites. **Coordinator:** owns the capture tool; may pair with any src/-only ticket. verify_rows still run (must be untouched). ≤$0.30 expected, ≤$0.60 cap.

**Milestone advanced:** M10 (netbattle: recon → first measured two-instance surface).
