# T134 RECON MAP — M6's first Navi: the ActorType fork, the 25-slot tables, one slot on screen

READ-ONLY recon. Every causal claim below is UNVERIFIED and carries the cheap re-check that would
kill it. Line numbers: exact where a symbol was found by grep (labels, off_ tables); for asm31.s
blocks inside `sub_80F2354`/`naviObject_dispatch_80F2330` they are anchored on
`off_80F239C:` = asm31.s:123363 (grep-exact) and counted backwards → treat as ±4.

## 0. Premise corrections (the ticket's own cites are wrong)

- `docs/worklog/T120b.md` DOES NOT EXIST. `wc -c docs/worklog/T120b.md` → No such file. The landed
  navi/identity decode the fork sits on is **docs/worklog/T76.md** (176 lines) — plus T121b is a
  panel-flags worklog, unrelated.
- `docs/coverage/navis.md` DOES NOT EXIST. No file under docs/ is named navis*. The "25-slot /
  family-A join" prose lives in **docs/worklog/T76.md:6, :17-25, :96-176** and in the generated
  `docs/SCOPE.md` "navis + cybeasts (M6)" section.
- `docs/tickets/T134.md` DOES NOT EXIST (`ls docs/tickets/` = Q3, T48, T50, T57, T7E, T9L only).
- `docs/worklog/T76.md:96-100` already says it: `navis + cybeasts (M6): FOUND: asm/asm31.s
  off_80F24D8/off_80F253C/off_80F25A0 -- verified 0/25`, and its Step 3 concludes the inventory is
  "reading the WRONG three tables". The ticket was written off prose; §2 below counts the real tables.

## 1. WHERE the actor-type fork is

### 1a. The struct offset (name, not number)
`reference/bn6f/include/structs/AIData.inc`
```
:2   enum8 ActorType // loc=0x0
:6   	struct_const ACTOR_TYPE_VIRUS, 0x00
:7   	struct_const ACTOR_TYPE_NAVI, 0x01
:8   	struct_const ACTOR_TYPE_PLAYER, 0x02
:9   u8 AIIndex // loc=0x1
```
So the byte tested is **`oAIData_ActorType` = AIData+0x0**, reached through the owning object's
**`oBattleObject_AIDataPtr`**; the slot index is **`oAIData_AIIndex` = AIData+0x1**.
(NaviStats.inc is a *different* struct — the player's stat block, `oNaviStats_NaviIndex` at +0x29;
not the navi roster.)

### 1b. The one fork site: `t1_0x0_80B81EC` (asm31.s:15-35)
```
asm31.s:15 t1_0x0_80B81EC:
asm31.s:16 	push {r4,lr}
asm31.s:17 	ldr r4, [r5,#oBattleObject_AIDataPtr]
asm31.s:18 	ldr r1, off_80B8200 // =T1ActorTypeHandlers_80B8204
asm31.s:19 	ldrb r0, [r4,#oAIData_ActorType]
asm31.s:20 	lsl r0, r0, #2
asm31.s:21 	ldr r1, [r1,r0]
asm31.s:22 	mov lr, pc
asm31.s:23 	bx r1
asm31.s:24 	pop {r4,pc}
asm31.s:28 T1ActorTypeHandlers_80B8204:
asm31.s:29   // virus
asm31.s:30 	.word virusObject_dispatch_8108F50+1
asm31.s:31   // navi
asm31.s:32 	.word naviObject_dispatch_80F2330+1
asm31.s:33   // player
asm31.s:34 	.word playerObject_main_80EA460+1
```
The test is a **table lookup, not `cmp #1`**: `ldrb ActorType; lsls #2; ldr [T1ActorTypeHandlers+r0]; bx`.
In-repo comment asm31.s:4-14 records a live `--trace-pc` of this entry (AUDIT wave 3c): a PAUSED
capture's MegaMan object (`r5==0x0203a9b0`) and its Mettaur (`r5==0x0203ab60`) both land here, so
**this is the shared T1 entry for every T1 category object**.
UNVERIFIED (mechanism): that ActorType is the *only* lever selecting the navi funnel — an object
could also be routed by its T1 sub-table (asm00_1.s ~8592 `T1BattleObjectJumptable`). Cheap check:
`--trace-pc` 0x080b81ed on a Mettaur capture, then `--poke-at <frame>:<AIData+0>:1` the same route
and diff the traced PC set (expect the virus funnel to disappear and naviObject_dispatch_80F2330 to
appear). Address to watch for "which funnel": `0x080b81ed` (fork) and `0x080f2331`
(naviObject_dispatch_80F2330+1).

### 1c. The navi funnel: `naviObject_dispatch_80F2330` — spawn/AI/art hook
```
asm31.s:~123310 naviObject_dispatch_80F2330:
asm31.s:	push {lr}
asm31.s:	ldr r1, off_80F2344 // =off_80F2348
asm31.s:	ldrb r0, [r5,#oBattleObject_CurState]
asm31.s:	ldr r1, [r1,r0]
asm31.s:	mov lr, pc
asm31.s:	bx r1
asm31.s:	bl object_updateSprite
asm31.s:	pop {pc}
asm31.s:~123322 off_80F2344: .word off_80F2348
asm31.s:~123324 off_80F2348: .word sub_8016F56+1   // CurState 0
                              .word sub_80F2354+1   // CurState 1  <-- the navi AI leg
                              .word sub_8016C4E+1   // CurState 2
```
`sub_80F2354` is the per-navislot dispatch (asm31.s:~123327-123362):
```
sub_80F2354:
	push {r4,lr}
	ldr r0, [r5,#oBattleObject_AIDataPtr]
	ldrb r0, [r0,#oAIData_AIIndex]
	lsl r4, r0, #2
	ldr r0, off_80F239C // =off_80F2410
	ldr r0, [r0,r4]
	mov lr, pc
	bx r0
	ldr r0, off_80F23A4 // =off_80F2474
	ldr r0, [r0,r4]
	mov lr, pc
	bx r0
	bl GetBattleMode
	cmp r0, #10
	bne loc_80F2378
	bl sub_800E9FA
loc_80F2378:
	ldr r1, off_80F23A0 // =off_80F23AC
	ldr r0, [r1,r4]
	bl ai_eventuallyRunsAIAttack_801AF44
	ldr r0, off_80F23A8 // =NaviActHandlers_80F25A0
	ldr r0, [r0,r4]
	mov lr, pc
	bx r0
	ldr r1, [r5,#oBattleObject_AIDataPtr]
	ldrb r0, [r1,#oAIData_ChipLockoutTimer]
	tst r0, r0
	beq loc_80F2394
	sub r0, #1
	strb r0, [r1,#oAIData_ChipLockoutTimer]
loc_80F2394:
	bl object_presentCollisionData
	pop {r4,pc}
```
Pointer words: asm31.s:123363-123370 (`off_80F239C→off_80F2410`, `off_80F23A0→off_80F23AC`,
`off_80F23A4→off_80F2474`, `off_80F23A8→NaviActHandlers_80F25A0`).
UNVERIFIED: that a type-1 object is legally driven by this CurState table (CurState 0 lands on
`sub_8016F56`, a different leg than the virus funnel's). Cheap check: run the navi pokerow (§3) with
`--watch 0x0203ab60:0x30` (CurState/CurAction/CurAnim) and `--trace-pc` and confirm the traced PCs
include `0x080f2355` and no `virusObject_*` PCs.

### 1d. The second fork family — art and stats (actor_type FIRST, then ai_index)
`enemy_getStruct1` (asm00_2.s:647-670) and `enemy_getStruct2` (asm00_2.s:674-712) both do
`GetVerActorTyAndAIIdx_80182B4` → `ldrb r2,[r0,#1]; lsls #2; ldr r1,[off_800F230/off_800F260+r2]`
then `AIIndex*4`. Columns (asm00_2.s:667-670, :710-712):
```
off_800F230: virus AIEnemyStruct1Ptrs_81090D0 / navi NaviEnemyStruct1Ptrs_80F24D8 / player PlayerEnemyStruct1Ptrs_80EA814
off_800F260: virus AIEnemyStruct2Ptrs_8109150 / navi NaviEnemyStruct2Ptrs_80F253C / player PlayerEnemyStruct2Ptrs_80EA8D8
```
Struct1 record fields (asm00_2.s:632-639): `spriteIndex0 +0x00`, `spriteIndex1 +0x01`,
`Unk_02 +0x02`, `Unk_03 +0x03`, `Unk_04 +0x04`, `Element +0x05`,
`SecondaryElementWeakness +0x06`, `HasShadow +0x07` → **Struct1 is the art/element record**.
Struct2 = HP: `elem_hp +0x00` (low 12 = figure, high nibble = element), `ElemDamage +0x04`, one row
per *version*, stride 6 (asm00_2.s:672-694).

### 1e. Spawn: there is NO fork at spawn
`sub_80076A0` (asm00_1.s:9143-9260) is the spawner:
```
asm00_1.s:9155 	bl GetVerActorTyAndAIIdx_80182B4   // -> *const (version, actor_type, ai_index)
asm00_1.s:9163 	ldrb r4, [r7, #0]                  // version -> params
asm00_1.s:9164 	bl object_spawnType1               // (obj_idx=0, x, y, z, params) -> *BattleObject $r5
asm00_1.s:9177 	bl object_createAIData             // -> stored at [r5,#oBattleObject_AIDataPtr]
asm00_1.s:9218 	bl GetVerActorTyAndAIIdx_80182B4
asm00_1.s:9221 	strb r1, [r4,#oAIData_Version_16]
asm00_1.s:9222 	strb r1, [r4,#oAIData_Version_17]
asm00_1.s:9225 	strb r1, [r4,#oAIData_ActorType]    // <-- just STORED (from table byte 1)
asm00_1.s:9228 	strb r1, [r4,#oAIData_AIIndex]      // <-- just STORED (table byte 2)
asm00_1.s:9231 	bl enemy_getStruct1 / :9234 strb [r4,#oAIData_Unk_03]
asm00_1.s:9238 	bl enemy_getStruct2                 // HP / Element / Damage from the navi column
```
So **spawn path and table indexing are type-independent**; the difficulty (navi funnel, navi art,
navi stats) all appears *after* the object exists, driven by `oAIData_ActorType` and the two
`enemy_getStruct*` columns. `GetVerActorTyAndAIIdx_80182B4` itself (asm00_2.s:20022-20030) is
`mov r1,#3; mul r1,r0; ldr r0,=VerActorTyAIIdxTable_80182C4; add r0,r0,r1; mov pc,lr` — 3-byte rows,
452 rows (T76; asm00_2.s:20031-20040 comment block; navi rows = enemy_idx 0x100..0x1C3).
UNVERIFIED: that `sub_80076A0` is the only spawner that matters (battle.rs:1721 cites
`spawnEnemy_80073E2` asm00_1.s:8695 as the panel-cell reader). Cheap check: `grep -n "object_spawnType1"
reference/bn6f/asm/*.s` and `--trace-pc 0x080076a1` on the battlestart capture.

### 1f. ActorType reads that are NOT navi forks (bounded negative)
`asm00_2.s:2252` (`sub_800FD0A`, `cmp r0,#2` player-buster tier), `asm00_2.s:2349`
(`setChipsForPlayerObjects_800FDC0`, `cmp r1,#2`), `asm00_2.s:2720`, `asm00_2.s:16887`, `:16941`,
`asm00_2.s:21879`, `asm31.s:12768-12795` (writes), `asm31.s:85943`, `:108770`, `:109362-109441`,
`:109692`, `:110745`, `:110910`, `asm00_2.s:5303`, `:11115`. Grep pattern used:
`grep -rn "oAIData_ActorType|ActorType" reference/bn6f/asm/*.s`. There is **no `ACTOR_TYPE_NAVI`
symbol name anywhere in the asm** — the constant is named only in AIData.inc; the asm expresses the
fork as the table lookup above.

## 2. WHICH are "the four 25-slot tables" — there are SIX (and seven more)

Entry stride for all of them: **4 bytes (`.word`)**; index = `oAIData_AIIndex * 4`.
Depth evidence: each block is 25 `.word` lines, bounded by the next label (label-to-label spacing
26 lines), **and** T76's 159-row identity walk gives exactly 25 distinct AIIndex values 0x00..0x18.

### The four read by `sub_80F2354` (pure `[AIIndex]`, no actor-type column)
| # | label | asm31.s | 25 words? | what it is |
|---|---|---|---|---|
| 1 | `off_80F2410` (ROM 0x080F2410) | :123397 | yes (counted) | first call; shared hit/damage legs: `sub_801A9B8` ×19, `sub_801AA48` ×1 (slot 5), `sub_801AAC0` ×6 (slots 12-17), `sub_801AB40` ×2 (slots 20,21) — 4 distinct symbols, NOT per-navi |
| 2 | `off_80F2474` | :123423 | yes | second call; per-ai pattern routine: 22 `nullsub_106` + slot 5 `sub_80F651C`, slot 10 `sub_80FAE5C`, slot 16 `sub_81003A8` |
| 3 | `off_80F23AC` | :123371 | yes | passed to `ai_eventuallyRunsAIAttack_801AF44`; 25 pointers to per-ai CurAction/attack state tables (`off_80F29B0` slot 0/17/22, `off_80F2B14`, …) — **this is the AI** |
| 4 | `NaviActHandlers_80F25A0` | :123551 | yes | last call; per-ai act handler: 22 `nullsub_106` + slot 0 `sub_80F2A1E`, slot 5 `sub_80F650A`, slot 13 `sub_80FDEFC` |

**None of the four indexes art.** Art and stats are the two `[actor_type][ai_index]` tables (§1d):
| label | asm31.s | 25 words? | role |
|---|---|---|---|
| `NaviEnemyStruct1Ptrs_80F24D8` | :123449 | yes (+24 `// 0xNN` comment lines) | **ART** — 8-byte records, spriteIndex0/spriteIndex1/Element/HasShadow; slot0 `byte_80F28C4` (asm31.s:123765) = `00 00 01 01 00 00 00 01` |
| `NaviEnemyStruct2Ptrs_80F253C` | :123500 | yes | **STATS** — slot0 `byte_80F28CC` (asm31.s:123767), 6-byte version rows, elem_hp low12/elem high nibble (T76's per-ai HP table) |
So the ticket's "four 25-slot tables" is right only for the `sub_80F2354` four; the roster is
**six** 25-deep tables in the navi cluster (T76's own Step-3 header names three of them:
`off_80F24D8/off_80F253C/off_80F25A0`).

### Seven more 25-deep tables (CurAction-handler tables for the navi column)
`off_80F2604` :123577 (verified 25 words), `off_80F2668`, `off_80F26CC`, `off_80F2730`,
`off_80F2794`, `off_80F27F8`, `off_80F285C` :123733. Reached by seven sibling routines
`sub_800F35C/378/394/3B0/3CC/3E8/404` (asm00_2.s:894-1000) through
`indexByActorTypeAndAIIdxThenDispatch_800F420` (asm00_2.s:1008-1023:
`ldrb r1,[r3,#oAIData_ActorType]; lsls #2; ldr r2,[r0,r1]; ldrb r1,[r3,#oAIData_AIIndex]; lsls #2;
ldr r0,[r2,r1]; bx r0`) whose actor-type column is `{virus off_8109250/…, navi off_80F2604/…,
player off_80EA9A0/…}` (asm00_2.s:902-996). Only `off_80F2604`'s 25 was re-counted here; the other
six are 26-line label spacings → cheap check: `sed -n 'A,Bp' file | grep -c '\.word'` per label.
UNVERIFIED (mechanism): which of the seven each caller uses (the caller set is the seven arms of the
navi/virus state machine). Cheap check: `--watch-write` each table entry address while a navi row runs.

### The virus-side 32-entry chain (for contrast)
`AIThinkTables_8109050` (asm31.s:169448), `AIEnemyStruct1Ptrs_81090D0` (:169513),
`AIEnemyStruct2Ptrs_8109150` (:169578), `AIActHandlers_81091D0`, pointed at by `off_8109048/4C`
and read at asm31.s:169407-169417 (T12/T76 chain). 32 entries — navi AIIndex values 0..0x18 fit
inside it, which is exactly T76's "navi rows reuse the virus arms".

## 3. HOW a navi slot is selected at field time (the index lever)

**What exists today (grep, tools/states.py):** `grep -n "navi" tools/states.py` → **ZERO hits**.
The manifest (`STATES` at tools/states.py:155) has 14 states; the enemy-bearing ones are:
- `battlestart` (states.py:207-229): pokes `60:0x02001c16:0x2000` + `60:0x02001c18:0` (one roll
  attempt) → 3 Mettaurs, NameID 0x0001.
- `battlestart_gunner` (states.py:231-259): the same plus **`60:0x0200a210:0x371`** — a one-shot
  write of `iCurrFrame` (0x0200a210) one tick before the roll, so the roll picks BattleSettings
  record 6 = 0x080b4bd8 instead of 7; chosen record ptr is watched at **0x02001b9c**
  (`oGameState_CurBattleDataPtr`); slot1 NameID 0x0085 (Gunner), HP 0x003c.
- `battlestart_ai4_rank0` (states.py:261-330): adds `60:0x02001d58:0x0240` (EVENT_681 in
  eEventFlags, table-root swap) + `60:0x0200a210:0x37a` → record 0x080b50e0, enemy_idx 0x13.
  Note the recorded failure: poking map bytes (0x02001b84 `oGameState_MapId`) WEDGED the overworld.
So the existing lever family is: **`iCurrFrame` poke → `rollRandomEncounter_80AA4C0` picks the
record** (index = `iCurrFrame mod filtered_count`, asm/asm29.s:10432-10436).

**The family-A (scripted) route — T131's question, owned by another run** (worktree
`/tmp/bnwt/t131-scriptedentry`, docs/worklog/T131.md, branch `wt/t131-scriptedentry`, which also
edits tools/states.py +65 and tools/trace.py +17): the roll's **OPT path**, asm/asm29.s:10216-10234:
if `oS2001c04_Unk_28` (u32 @0x02001c2c) != 0 AND `GetPositiveSignedRNG()&1 == 0` AND
`oS2001c04_OptCurBattleDataPtr` (u32 @0x02001c30) has `(ptr>>0x18)==8`, the roll does
`str r0,[r7,#oGameState_CurBattleDataPtr]` and returns it — **any 0x08-prefixed record is adopted
wholesale**, no root walk, no rec7 gate, no count. T131's own ROM walk found **zero family-A lists
inside any of the four roots**, i.e. the OPT path is the only way to a scripted record.
→ **DEPENDENCY**: T134's canonical route to a *scripted* navi encounter is T131's `battlestart_scripted`
state (states.py in that worktree); do not build a second one.

**Cheapest route to ONE navi on screen that the disassembly already supports**: a RAM poke of the
live enemy's **`oAIData_ActorType` (AIData+0x0, AIData reached via BattleObject+0x04
`oBattleObject_AIDataPtr`) from 0x00 → 0x01**, on any existing enemy state (`battlestart`,
`battlestart_ai4_rank0`, `/tmp/battlestart.state`). One poke; it touches no table index, so
`oAIData_AIIndex` and the object's panel/NameID stay put, but from the next frame:
`t1_0x0_80B81EC` dispatches the **navi** funnel (asm31.s:32) instead of the virus one, and
`enemy_getStruct1/2` switch to `NaviEnemyStruct1Ptrs_80F24D8[ai]` / `NaviEnemyStruct2Ptrs_80F253C[ai]`
for art/stats. What it needs beyond the poke: knowing the live AIData address (peek BattleObject+0x04
first — the object slots on this route are 0x0203aa88/0x0203ab60/0x0203ac38, states.py:222-224,
0x0203aab0/0x0203ab88 for the AI4 row).
UNVERIFIED, with the check that kills each:
1. "the poke changes the funnel" → `--trace-pc` 0x080b81ed; expect a `0x080f2331` visit and no
   `virusObject_*` visit after the poke frame.
2. "art follows the new column" → `--watch` the Struct1-derived sprite fields on that object (its
   `spriteIndex`/sheet base; the spawn-time read is asm00_1.s:9231 `enemy_getStruct1`) and compare a
   poked vs unpoked capture pixel-wise (`tools/compare_frames.py`).
3. "the navi CurState table is legal for an object mid-virus-leg" → `--watch 0x0203ab60:0x30`
   (CurState/CurAction) and `tools/states.py`'s own determinism recipe (40 frames, two builds).
4. "the poke survives the object's own writers" → `grep -n "oAIData_ActorType" ...:12768-12795`
   shows asm31.s:12776/12795 DO write it; `--watch-write <AIData+0>:1` for the whole capture.
Fallback (canon-faithful, needs T131): OPT-path pokes + a BattleSettings record whose formation
names an enemy id in 0x100..0x1C3. **Existence UNVERIFIED**; cheapest check is static, no capture:
scan the formation arrays for ids 0x100..0x1C3 (`tools/inventory.py`'s `parse_formations` keying /
`tools/rom_enemy_tables.py`). If none exists, the poke route above is the only one and the ticket's
"family-A index route" is a dead end.

## 4. OUR side: exact sites, and what collides

- `src/fixture.rs:355-370` — `KIND_METTAUR = 0`, `KIND_GUNNER = 1`, `Fixture::kind_of(slot)` =
  `(self.enemy_kind >> (slot*2)) & 0b11`. Descriptor fields: FIXTURE.md:13 `+4 u8 enemies`,
  FIXTURE.md:14 `+5 u8 enemy_kind` (2 bits per slot, 3 slots), FIXTURE.md:30 `+32 u16 enemy_hp`;
  parser at `src/fixture.rs:421 read()`, `:189` (the +5 byte's doc).
  **BOUNDED NEGATIVE**: `grep -rn "kind_of\|KIND_GUNNER\|KIND_METTAUR" src/` → only fixture.rs:360,
  :361, :367 (definitions). `kind_of` has **no caller** in src/; the comment at src/fixture.rs:357-359
  claims battle.rs's construction keys off it, but no site does. A navi kind would go in the free
  value 2 and only *stay* inert until the spawn site reads it.
- `src/battle.rs` **`Battle::new`** (fn starts src/battle.rs:2091) — the spawn site. Flag by function
  name, not line: enemy `Profile` closure (~:2184-2189), fixture branch (`if let Some(f) = fixture`)
  `for i in 0..f.enemies` → panel cell (F38h `panel_override_mask` vs diagonal), then
  `es.push(Actor::new(spr::Assets::new(METTAUR), col, row, true, enemy(hp)))` and
  `ai_list.push(ai::Ai::new(ai::Style::Mettaur))` (~:2246-2253); the no-fixture arm (~:2259-2271)
  builds exactly one Mettaur. **METTAUR art + `Style::Mettaur` are hardcoded on both arms** — this is
  the single site where a navi art/AI choice has to appear.
- `src/actor.rs:231-243` `Profile { hp, mercy, death_frames }` + `ENEMY_DEATH_FRAMES` :243,
  `Actor::new` :446-476 (takes `spr::Assets` + `Profile`), `sprite_key()` :585, `oracle_fields` :695.
- `src/ai.rs:143` `pub enum Style`, `Ai::new(style)` :198-204, and the **only** style branch in
  `Ai::update` :227-229 (`if matches!(self.style, Style::Mettaur)`). Per-navi planners are *named
  but unported*: src/ai.rs:72-74 (ProtoMan `sub_80FBA24` asm31.s:141641, Colonel placeholder),
  src/objects.rs:291-430 (`MettaurEntry`).
- `src/battle.rs` oracle surface that names the enemy (second collision risk in the same file):
  field table ~:1723-1729 (`enemy_state_action`, `enemy_anim`, `enemy_panel_x/y`, `enemy_gap`,
  `enemy_timer`, `enemy_hp`), doc table ~:1825-1830, writes ~:1917-1928 / :1981-1995,
  `battle_rank` :2043-2061 (reads canon's `byte_80182C4[3*enemy_idx]`, and says so: our Actor
  carries **no enemy_idx/ai_index** — FIXTURE.md's `enemy_kind` is two bits only).
- Collisions: `wt/t128` (unmerged thrown-chip rewrite) — `git -C /tmp/bnwt/t128 diff --stat main`
  = `src/battle.rs 230 ++++---`, `src/field.rs -79`, `src/main.rs`, `tools/inventory.py`,
  `tools/trace.py -31`, `tools/panel_flags_export.py` (plus worklog deletions from its older base).
  → **any src/battle.rs spawn edit collides with t128** (same file, same region family).
  `wt/t133` — `git -C /tmp/bnwt/t133 diff --stat main` = EMPTY and `git status --short` = EMPTY at
  recon time, HEAD == main 1afb985. `src/backdrop.rs` does exist (`ls src/`), so the ticket's
  "t133 edits src/backdrop.rs" is **not visible**; cheap re-check:
  `git -C /tmp/bnwt/t133 status --short; git -C /tmp/bnwt/t133 log --oneline -3`.
  A backdrop-only change would not collide with any site above.
  `wt/t131-scriptedentry` also edits `tools/states.py` (+65) → if T134 adds a state recipe, it
  collides there; prefer the RAM-poke route, which needs `--poke-at` only.

## 5. Bounded negatives (what I searched and found EMPTY)

1. `docs/worklog/T120b.md` — absent (`wc -c` → No such file).
2. `docs/coverage/navis.md` — absent; no `navis*` file under docs/coverage/ (48 files listed).
3. `docs/tickets/T134.md` — absent.
4. `tools/states.py` — `/navi/` zero hits; no state fields a navi (14 states, STATES:155).
5. `src/` — `kind_of` zero callers; `KIND_GUNNER`/`KIND_METTAUR` appear only at their definitions.
6. `reference/bn6f/asm/*.s` — no `ACTOR_TYPE_NAVI`/`ACTOR_TYPE_*` token; the fork is a table lookup.
7. `reference/bn6f/asm/*.s` — no `cmp`-style "is this a navi" test; all `oAIData_ActorType` compares
   found are against `#2` (player). Navi selection is always a **table column**.
8. `wt/t133` — no diff vs main, no dirty files (see §4), so its claimed `src/backdrop.rs` collision
   is unproven.
9. `tools/states.py` state list holds no family-A (scripted) record; the only in-flight one is
   `battlestart_scripted` in the unmerged T131 worktree.
