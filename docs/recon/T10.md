# Recon map for T10 (hyper/deepseek-v4.1-flash, $0.8540 nominal; every causal link unverified)

MAP — where the T10 inventory material lives (reference/bn6f read-only: paths relative to `reference/bn6f/`; no edits, no captures run).

## 1. Battle chips
- `data/ChipDataArr.s:2` `ChipDataArr_8021DA8::` — 411 `chip_data_struct` records (counted by `grep -c`). Layout `include/rom_structs/ChipData.inc:3-38`: `Codes u32 +0`, `Rarity +5`, `ChipElement +6`, `LibraryType +7`, `MB +8`, `EffectFlags +9`, `AttackFamily +0xb`, `AttackSubFamily +0xc`, `AlphabetSortPos +0x18`, `AttackPower +0x1a`, `IDSortPos +0x1c`, icon/image/palette ptrs `+0x20/+0x24/+0x28`, `Size 0x2c`.
- Accessor: `asm/asm02.s:5-16` `getChip8021DA8: ... ldr r1, off_8021AB0 // =ChipDataArr_8021DA8` (index × 0x2c).
- Names: `data/textscript/TextScriptChipNames0.s` (411 names, `@`-terminated); selector-by-code helpers `getOffsetToQuantityOfChipCodeMaybe_8021c7c` `asm02.s:305`, `GetChipCountOfCode`.
- src: `src/chips.rs:1-100` (reads `assets/chips.bin`, `tools/chip_export.py`); `src/deck.rs:1-105` folder pack `id | code<<9`.
- RAM: none (const ROM). Count check: `--peek` cannot read ROM; a control dump of `0x08021DA8` len `411*0x2c` from `tools/bnasm.read_symbol` vs `ChipDataArr_8021DA8`. Runtime link: `--watch-write 0x020366C0` (selected-chip code array, below). unverified: the struct `Size 0x2c` → 411 records is read from `.inc`, not asserted by the ROM; cheapest confirmation is a byte-distance from `ChipDataArr_8021DA8` to the next symbol in the link map.

## 2. Program Advances
- Recognition: `asm/asm03_0.s:6029` `sub_8029110` fills scratch from selected chips (`bl getChip8021DA8`, `getChipID_802A54E`), then `asm03_0.s:6631` `sub_8029520` walks recipe lists.
- Recipe lists: `asm/asm03_0.s:11502 off_802BCB0` (43 pointers `byte_802BA60..byte_802BB92`, terminated `.word NULL`) and `asm/asm03_0.s:11481 off_802BC60` (20 pointers `byte_802BB98..byte_802BC56`). Sample record `asm/asm03_0.s: byte_802BA60: .byte 0x3,0x4,0x53,0x1,0x47,0x0,0x48,0x0,0x49,0x0` = `[count=3][matcher=4][result=0x153][chip,code]×3`.
- Matchers: `asm/asm03_0.s:6703 off_80295C0: .word sub_80295C8+1, sub_802961A+1`; code-id extraction `sub_80295C8` `asm03_0.s:6719`, exact-match `sub_802961A` `asm03_0.s:6753`.
- Names: `data/textscript/TextScriptChipNames1.s` (117 strings, included at `data/dat38_85.s:4388`).
- src: not ported (`docs/SCOPE.md` M4; `docs/HANDOFF_2026-09-12.md:397` lists PAs as out of the current scope).
- RAM: scratch `dword_2033000` (`0x02033000`, zeroed 0x48 bytes `asm03_0.s:6036`), code scratch `byte_20366C0` (`0x020366C0`), `byte_20366FE` (`0x020366FE`), menu state `eS20364C0`/`S20364C0`. Cheapest check: `--poke-at F:0x02033000:...` is impractical; instead `--watch-write 0x02033000:0x48` and `--trace-pc 0x08029520 --trace-steps 200` on the `popup`/`battle_full` rows (selection must hit `sub_8029520`). unverified: that `byte_802BA60`'s `[2]` is the result chip id and `[1]` an index into `off_80295C0`; confirm by a control run selecting a known 3-chip PA and watching `byte_20366C0`/the resulting chip.

## 3. Viruses (family, rank, HP, element, entry routine)
- id → (version, actor_type, ai_index): `asm/asm00_2.s:19965 GetVerActorTyAndAIIdx_80182B4` (`mul r1,r0,#3; ldr r0,=byte_80182C4; add`), table `asm/asm00_2.s:19975 byte_80182C4` (3-byte records `version, ACTOR_TYPE_*, ai_index`; virus entries 0x1.. follow 6-version families: Mettaur `0x00,ACTOR_TYPE_VIRUS,0x01`, etc.).
- Element/sprite struct: `asm/asm00_2.s:646 enemy_getStruct1` → `off_800F230 // virus/navi/player` (`asm00_2.s:661-673`), virus sub-table `asm/asm31.s:169485 off_81090D0` (32 ptrs; `MettaurEnemyStruct1_8109BD0` at idx 1, `GunnerEnemyStruct1_8112B94` at 0x5C). Fields `.equiv` `asm00_2.s:637-644`: spriteIndex0/1, `Element +5`, `SecondaryElementWeakness +6`, `HasShadow +7`.
- HP/damage struct: `asm/asm00_2.s:684 enemy_getStruct2` → virus sub-table `asm/asm31.s:169550 off_8109150` (`MettaurEnemyStruct2_8109BD8`); `.equiv` `asm00_2.s:678-682`: `ElemHP u16 +0`, `ElemDamage u16 +4`, stride 6 keyed by `version`.
- Per-type entry routines: `asm/asm31.s:169291 battleObject_dispatch_8108F50` → `battle_8108F74` (`asm31.s:169314`); object jumptables `asm/asm00_1.s:1891 T1BattleObjectJumptable`, `asm/asm00_1.s:2087 T3BattleObjectJumptable`; virus sub-jumptables `asm/asm31.s:~169606 off_81091D0`, `off_8109250`, `off_81092D0`, `off_8109350` (Mettaur arm = `nullsub_13+1` in the T1 table, `sub_810ABD0+1` at 0x0C).
- Names/display: `LoadVirusNameIDsForUpToFourSlots_800A998` `asm/asm00_1.s:16361` (reads `oBattleObject_NameID`), `RetrieveAndRenderEnemyVirusNamesInBattle_801E574`, `selectVirusOrNaviNamesAndWhich_800EC56`, text `data/textscript/TextScriptVirusChipNames.s`.
- src: `src/ai.rs:1-50` (`Style::Mettaur`, `ForMettaur_8109EF4`, `sub_8109FD6`), `src/actor.rs:634-677`, `src/objects.rs:1-15`; Gunner partial `src/gunner.rs`.
- RAM: enemy BattleObjects `0x0203AA88 / 0x0203AB60 / 0x0203AC38` (NameID `+0x28`, HP `+0x24`). Cheapest check: `--watch-write 0x0203ab60:0x40` on a `--poke-at` enemy-setup change; control = same recipe, different `ai_index` byte at `byte_80182C4`. unverified: that `off_81090D0[ai_index]` (not the family id) is what `enemy_getStruct1` returns; confirm by tracing `--trace-pc 0x0800F230` while swapping one enemy.

## 4. Navi bosses and Cybeasts
- Same two accessors; navi sub-tables `asm/asm31.s:123439 off_80F24D8` (EnemyStruct1, 0x00..0x60; `// 0x2C protoman` `byte_80FB8B4`) and `asm/asm31.s:123490 off_80F253C` (EnemyStruct2). Entry routines `asm/asm31.s:123540+ off_80F25A0` (`sub_80F650A`, `sub_80FDEFC`, …).
- Sprites: `data/SpritePointersList.s:61 naviBattleSpritePtrs`. Names: `data/textscript/TextScriptNaviNames0.s` / `TextScriptNaviNames1.s`. Navi ids: `constants/constants.inc:57-69` (`NAVI_MEGAMAN..NAVI_PROTOMAN`).
- Cybeasts = navi table entries + `TF_GREGARBEAST/FALZARBEAST` (below); sprite categories `constants/enums/sprite_categories.inc:17-18 G_BEAST_0B/F_BEAST_0C`.
- src: colonel/protoman AI stubs `src/ai.rs:4-6`; not ported.
- RAM: navi stats block `eNaviStats203CCE0` (`0x0203CCE0`), `eBattleNaviStats203CB10` (`0x0203CB10`), `eBattleNaviStats2034A60` (`0x02034A60`). Cheapest check: `--peek 0x0203a9b0` vs enemy slot on a navi fixture; control-run one boss vs `ai.rs`. unverified: which of `off_80F24D8` index values maps to which boss name id (the table has `protoman` only at 0x2C); confirm with `--watch-write 0x0203ab60:0x30`.

## 5. Player forms (Crosses, Beast Out/Over, Cross Beast)
- Enums: `constants/constants.inc:91-114` `TF_HEATCROSS 0x1 … TF_FALZARBEASTOVER 0x18`; `NaviStats.Transformation loc 0x2c` `include/structs/NaviStats.inc:38`.
- Per-form stat structs: player tables `asm/asm31.s:107438 off_80EA814` (EnemyStruct1; `MegaManEnemyStruct1_80F02C0`, then form entries at 0x8C-0xC0 `byte_80F02C5..byte_80F02FC`) and `asm/asm31.s:107537 off_80EA8D8` (EnemyStruct2). Update jumptable `asm/asm31.s playerObjectUpdateJumptable_80EA93C` (`sub_80F0608+1`).
- Behaviours: `battle_beastJumpLockOn_8026554` `asm/asm03_0.s:143`; `SetBeastOutCounterTo3`; `NaviStats.BeastOutCounter loc 0x21`.
- src: `src/emotion.rs:1-8` (emotion window; forms not modelled), `src/battle.rs`, `src/actor.rs`.
- RAM: `Transformation` byte in NaviStats (`eNaviStats203CCE0+0x2c`), `BeastOutCounter` (+0x21). Cheapest check: `--poke-at F:0x0203CCE0+0x2c:0x01` then `--watch-write 0x0203a9b0:0x28`; control-run with 0x00. unverified: the 0x8C-0xC0 entries are exactly the Cross/Beast forms; confirm by comparing struct bytes against `TF_*` order.

## 6. Panel types / effects
- Struct: `include/structs/PanelData.inc` (`Type loc 0x2`, `Alliance 0x3`, `Flags 0x14` with the documented bit list, `ReserverObjectPtr 0x1c`, `Size 0x20`).
- Array: `ewram.s:2927 panel_data_struct ePanelData1_1 // 0x2039c00 // start of on-screen panels`.
- Ticker: `asm/object.s:1018 panel_800BFC4` (walks `ePanelData1_1`, `Type==3` → `object_getPanelParameters`, stores `Unk_18`), counter `byte_203CB04` (`0x0203CB04`, reload 0x8c).
- Mutators: `asm/object.s:2201 object_crackPanel`, `asm/object.s:2305 object_breakPanel` (+`object_breakPanelLoud`, `object_breakPanel_dup1..3`), `asm/object.s:2600 object_setPanelType`, `asm/object.s:2651 object_setPanelTypeBlink`; area helpers `object_setPanelAlliance`, `object_reservePanel/removePanelReserve`, `object_panel_setPoison`, filter helpers `object_getPanels*`.
- Neighbour geometry: `asm/asm00_2.s:21079 PanelOffsetListsPointerTable::` plus `byte_80198E8..` offset lists.
- src: `src/field.rs:1-405`; `docs/coverage/plan-interpreters.md` panel sections.
- RAM: `0x02039C00` panel array, `0x0203CB04` tick. Cheapest check: `--watch-write 0x02039800:0x20` (panel 0) on `field` row + a `--poke-at` panel type; control = same capture un-poked. unverified: which panel `Type` value selects which effect routine — the code has no type→routine table; confirm by `--trace-pc 0x0800BFC4` with each value.

## 7. Statuses
- Flag struct: `include/structs/CollisionData.inc:14-34` (`OBJECT_FLAGS_PARALYZED 0x800`, `OBJECT_FLAGS_BLIND 0x2000`, `OBJECT_FLAGS_CONFUSED 0x8000`, `OBJECT_FLAGS_UNAFFECTED_BY_POISON 0x08000000`, `OBJECT_FLAGS_BUBBLED 0x80000000`), timers `PoisonPanelTimer +0x8`, `BlindTimer +0x20`.
- Damage flags: `include/structs/BattleObject.inc:118-121` (`DAMAGE_DOUBLE 0x8000`, `DAMAGE_PARALYZE 0x4000`, `DAMAGE_UNINSTALL 0x2000`, `DAMAGE_ERASECROSS_SKULL_HIT 0x1000`, `DAMAGE_NOTHING 0x800`).
- Routines: `asm/asm00_2.s:21720 object_setCollisionStatusEffect1`; `object_checkPanelParameters`; damage application in `asm00_2.s` around `object_breakPanel`/`calcFinalDamage...`.
- src: no dedicated file; statuses partly in `src/objects.rs`, `src/actor.rs`.
- RAM: CollisionData reached via `oBattleObject_CollisionDataPtr` (`+0x54`); enemy slots above. Cheapest check: `--watch-write 0x0203aa88:0x80` on a shockwave/popup row; control = no-hit run. unverified: that `CollisionData.inc` offsets match the live layout (the `.inc` has no address); confirm by dumping `[obj+0x54]` with `--dump`.

## 8. Encounter formation tables / BattleSettings
- Struct: `include/rom_structs/BattleSettings.inc` (`Battlefield 0x0`, `Music 0x2`, `BattleType 0x3`, `Background 0x4`, `BattleNumber 0x5`, `FieldSidesModifier 0x6`, `UnkOptSettings 0x8`, `EnemySetupArrPtr 0xc`, `Size 0x10`).
- Tables: `data/BattleSettings.s:2 battleSettingsList0::` and `data/BattleSettings.s:1505 BattleSettingsList1::` (0x10-byte records); getters `asm/asm00_1.s:15948 getBattleSettingsFromList0`, `asm/asm00_1.s:15956 getBattleSettingsFromList1`.
- Map → settings: `asm/asm01.s:519 off_8020170`, `:522 off_8020178`, `:525 off_8020180` (`[*const [*const [*const FFStop32<[BattleSettings]>;16]];2]`), `:530 off_8020188`, groups `asm/asm01.s:543 off_8020190` → `off_80B1B7C`, `off_80B1ED8`, `off_80B23C8`, `off_80B29CC`; selection `asm/asm29.s:10338 sub_80AA5F4` (map-group/map-number level, 0x10-stride records, `0xff` terminator, `sub_80AA824`, `sub_80AA6A4`), entry `asm/asm29.s:10327 chooseRandomEncounterMaybe_80aa5e4` (writes `oGameState_CurBattleDataPtr`).
- Enemy setup arrays referenced by `EnemySetupArrPtr`: `byte_80AFF44`, `byte_80AFF4D`, … (BattleSettings.s), consumed by `SpawnBattleObjectUsingBattleEntityConfig_8007368` (`0xF0`-terminated; comment in BattleSettings.inc).
- src: `src/battle.rs` (rebuilt per battle), `src/fixture.rs` descriptor.
- RAM: `GameState.CurBattleDataPtr 0x02001B9C` written at `asm29.s:10286` (`at=0x080AA59E` per HANDOFF §… known anchor), live `BattleSettings_200AF60` (`0x0200AF60`). Cheapest check: `--watch-write 0x02001b9c` + `--peek 0x0200af60` on the `battle_full` row; control = `emptyfield_start` recipe. unverified: that `off_8020190` indexing is `[group][map]`; confirm by poking `GameState.MapGroup/MapNumber`.

## 9. Arena backdrops
- `include/rom_structs/BattleSettings.inc:8 Background loc 0x4`; setter `asm/asm03_0.s:14556 battleSettings_setBackground` (`strb r0,[r1,#0x4]` into `BattleSettings_200AF60`); flags `asm/asm03_0.s:14520 battleSettings_setFlags_unkSettings`; getters `asm/asm03_0.s:14503-14510`.
- Loaders: `asm/asm00_0.s:3096 CopyBackgroundTiles`, `CopyBackgroundTiles812EAAC`, `iCopyBackgroundTiles`; `BWAnimData`/`BGScrollCallbacks` structs in `include/rom_structs/`.
- src: `src/backdrop.rs:1-36` (reads `assets/backdrop` built by `tools/backdrop_export.py`); `docs/HANDOFF_2026-09-12.md:113`.
- RAM: `0x0200AF60+4`; BG scroll counters `0x02009690/94` (HANDOFF). Cheapest check: `--peek 0x0200af64` after `--poke-at` of the settings record; control = same state without poke. unverified: which `Background` byte value → which tile blob; confirm by poking `0x0200af64` and diffing `backdrop` row frames.

## 10. Navi Customizer programs that alter battle
- Grant/take: `asm/asm03_1_1.s:8794 GiveNaviCustPrograms`, `:8814 TakeNaviCustPrograms`, helper `sub_803D148` (all call `GiveItem`/`TakeItem`/`CheckKeyItem`); no dedicated battle-effect table found in asm — effects are the `NaviStats` fields.
- `include/structs/NaviStats.inc`: `Attack 0x1`, `Speed 0x2`, `Charge 0x3`, `BButton 0x4`, `BPwrAtk 0x5`, `FstBarr 0x6`, `BLeftAbility 0x7`, `RegUP 0x9`, `CustomLevel 0xa`, `MegaLevel 0xb`, `GigaLevel 0xc`, `Mood 0xe`, `CustHPBug 0x19`, `FloatShoes 0x1b`, `AirShoes 0x1c`, `UnderShirt 0x1d`, `SuperArmor 0x23`, `EmotionBug 0x24`, `ProcessingBug 0x31`, `SlipRun 0x35`, `MaxBaseHP 0x3e`, `CurHP 0x40`, `MaxHP 0x42`, `ChipRecovery 0x50`, `Poem 0x5f`, `TurnsUntilCustBugActivates 0x63`.
- Recompute: `asm/asm32.s:37063 reloadCurNaviBaseStats_8120df0`, `asm/asm37_0.s:1719 reloadCurNaviStatBoosts_813c3ac`, `asm/asm00_2.s:10473 initNaviStats203CCE0_8013846`, `asm/asm00_2.s:9820 initNaviStats_WithDefaultStatsMaybe_8013438`; XML dialog `data/textscript/TextScriptNaviCustDialog.s`, `CompTextScriptNaviCustAndFolders86D82C4` `data/dat38_75.s:14`.
- src: `src/custom.rs:1-20` (chip-select window; NaviCust programs not modelled), `src/deck.rs` Regular/Tag absent (`src/deck.rs:12`).
- RAM: `0x0203CCE0` (NaviStats), `0x0203CB10`/`0x02034A60` (battle copies). Cheapest check: `--watch-write 0x0203cce0:0x64` at battle init on the `battle_full` row; control = stock navi. unverified: how installed programs become the `NaviStats` bytes (there is no program→field table); confirm by installing one program in a save and diffing the block.

## Existing verified anchors / tooling to mirror
- Verified items: Mettaur — `docs/coverage/mettaur.md`, `src/ai.rs:8-50`, `src/actor.rs:634-677`; 43 chip rows — `tools/chip_export.py` header, `src/chips.rs`. Both are pixel-verified through our code, not yet "as data" (matches T10 acceptance).
- Exporters that `tools/inventory.py` should sit beside: `tools/chip_export.py`, `tools/custom_export.py`, `tools/backdrop_export.py`, `tools/emotion_export.py`, `tools/banner_export.py`, `tools/hud_tiles_export.py`, `tools/results_export.py`, `tools/spr_export.py`, `tools/font_export.py`; symbol reader `tools/bnasm.py` (`read_symbol`).
- Counting entry point for the status column: `docs/SCOPE.md:36-40` ("Per-item tables", currently empty); `tools/daily_review.sh` is the counter.

## Cheapest checks, condensed (all unverified causal links)
| link | cheapest check |
|---|---|
| struct `Size 0x2c` → 411 chips | byte distance `ChipDataArr_8021DA8` to next symbol (static, no capture) |
| PA list → recognition | `--watch-write 0x02033000:0x48` + `--trace-pc 0x08029520` on `popup` |
| `byte_80182C4` idx → enemy struct | `--watch-write 0x0203ab60:0x40`, swap one `ai_index` byte (control) |
| panel `Type` → effect routine | `--trace-pc 0x0800BFC4`, one `Type` per run; `--dump` panel array `0x02039C00` |
| status offsets | `--dump [obj+0x54]` len 0x28; `--watch-write 0x0203aa88:0x80` |
| map → BattleSettings | `--watch-write 0x02001b9c` (expect `at=0x080AA59E`), `--peek 0x0200af60` |
| `Background` byte → blob | poke `0x0200af64`, diff `backdrop` row frames (control) |
| NaviCust program → NaviStats | `--watch-write 0x0203cce0:0x64`, install one program vs stock (control) |

The worker can confirm the static parts (tables/counts) with `tools/bnasm.py` against `reference/bn6f`; every runtime link above needs a capture, and none of them is established by this read.
