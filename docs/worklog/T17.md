# T17 — Chips as data, first family: sword/blade arm driven from ChipDataArr bytes

Worktree: /tmp/bnwt/t17-chips-as-data, branch wt/t17-chips-as-data, HEAD 2478ff1.

## Step 1 — baseline (2026-09-16)

Build: `cargo build --release` (CARGO_TARGET_DIR=/tmp/ct_t17-chips-as-data), gbafix → /tmp/t17_base.gba.

`python3 tools/harness.py --no-gallery --ui isolated`:

- All 43 `chip-*` rows **PASS total 0 worst 0** (frames 40 each for the sword/blade
  eleven, 40 for cannons/airshot/recoveries/barriers, 60-113 for vulcans/bombs/seeds,
  77/80 areagrab/invisibl — every one 0/0). The ticket's "0/0/30 each" reads as
  total 0 / worst 0; the isolated chip rows actually run 40+ frames, chip-use is the
  30-frame row. All PASS, none blind.
- distinct `CHIP_*` ids inside `chip_strike`'s sword/blade arm: **11**
  (SWORD WIDESWRD LONGSWRD WIDEBLDE LONGBLDE MURAMASA STEPSWRD FIRESWRD AQUASWRD
  ELECSWRD BAMBSWRD).
- `fitted constants: 19 (derived 430, peeked 147)`.
- Pre-existing on this main HEAD, not touched by this ticket: `gunner` FAILED
  (total 2105613), `cursor` FAILED total 1 worst 1 (the ticket's step 6 expects
  cursor ≤1/1/170, so 1/1 is the unchanged value).

## Step 2 — ROM bytes vs the literals in chip_strike

Sword/blade rows of ChipDataArr_8021DA8 (attack_family 0x13 for all eleven;
EffectFlags 0x4A except Muramasa 0xDA; LockoutFrames 0 for all):

| chip | id | subfamily | hit-shape byte_80EBA18[sub] | arc row byte_80EBAD8[sub] | code panels | code (anim,pal) | code palette-add |
|---|---|---|---|---|---|---|---|
| Sword | 71 | 0x00 | 0x1 | 0x18 | panel ahead | (2,0) | — |
| WideSwrd | 72 | 0x01 | 0x4 | 0x16 | column | (0,0) | — |
| LongSwrd | 73 | 0x02 | 0x2 | 0x17 | two ahead | (1,0) | — |
| WideBlde | 74 | 0x03 | 0x4 | 0x19 | column | (0,5) | — |
| LongBlde | 75 | 0x04 | 0x2 | 0x1A | two ahead | (1,5) | — |
| FireSwrd | 76 | 0x0C | 0x4 | 0x16 | column | (0,0) | 1 |
| AquaSwrd | 77 | 0x0D | 0x4 | 0x16 | column | (0,0) | 2 |
| ElecSwrd | 78 | 0x0E | 0x4 | 0x16 | column | (0,0) | 3 |
| BambSwrd | 79 | 0x0F | 0x4 | 0x16 | column | (0,0) | 4 |
| StepSwrd | 81 | 0x01 | 0x4 | 0x16 | column | (0,0) | — |
| Muramasa | 85 | 0x08 | 0x2 | 0x2D | two ahead | (1,6) | — |

byte_80EBA18 is word-indexed in the asm (`lsl r2,#2; ldr r3,[r3,r2]`), so the
per-subfamily byte is the first of each 4-byte group:
`1,4,2,4,2,0x11,0x11,0x11,2,6,0xB,0x11,4,4,4,4`. byte_80EBAD8 is byte-indexed:
`18 16 17 19 1A 1B 1C 28 2D 25 5F 1B 16 16 16 16`.

Row→(animation, palette) from byte_80E0398's rows (asm31.s:109180-109200):
0x16→(0,0) 0x17→(1,0) 0x18→(2,0) 0x19→(0,5) 0x1A→(1,5) 0x2D→(1,6).
Elemental palette-add is the asm's own `sub - 0xb` for sub 0xC..0xF
(asm31.s:109268-109276), which the record's AttackSubFamily reproduces directly.

**Parity: 11/11 agree, zero mismatches.** Every literal in the arm is reproduced
by the record bytes + the two ROM tables. No contradiction to report.

Note: StepSwrd's AttackSubFamily is 0x01, the same as WideSwrd's — the ROM gives
it WideSwrd's column shape and 0x16 arc, which is exactly what the code's
per-id lists did. The record bytes carry that for free; no id needed.

## Steps 3-4 — the append-only asset and the data-driven arm (2026-09-16)

First tried the tail inline (record stride 36 -> 44, 8 behaviour bytes packed
after the palette pointer). That regenerates every blob pointer u32 in the
first 36 bytes (all 48 records diffed) and shifts every blob by +384, which
breaks the ticket's own rule "existing offsets never move". Dropped it.

Landed layout: chips.bin v2 is byte-append-only at the file level —
- the v1 table (48 x 36) and every blob are untouched;
- version field 1 -> 2 (the only non-append byte in the file);
- the behavioural tail (48 x 8: effect_flags, attack_family, attack_subfamily,
  attack_param_1..4, lockout_frames) is appended after the blobs, in record
  order; src/chips.rs locates it as data.len() - count*8.

cmp of every record's first 36 bytes, old vs new asset: **diff count 0**.
Record stride 36 unchanged; the tail's stride is 8. Whole old file is a
byte-prefix of the new one except the version u32 at offset 4.

src/chips.rs: `TAIL` const (provenance: derived), tail offset in `Chips::new`
with a sanity assert, new pub fields effect_flags/family/subfamily/params/
lockout on `Chip` (effect_flags/params/lockout `#[allow(dead_code)]` —
exported for later tickets, no arm reads them yet).

src/battle.rs: `chip_strike` now dispatches the sword family on the record's
attack_family (SWORD_FAMILY 0x13, canon) BEFORE the `match chip.id`, and the
eleven-id arm is deleted. Subfamily -> behaviour comes from two canon tables
copied from the asm data:
- SWORD_HIT_SHAPE (canon: SwordHitShapeBySubfamily_80EBA18, asm31.s:109334,
  word-indexed so the shape is each entry's first byte) with shape codes
  SHAPE_COLUMN 4 / SHAPE_TWO_AHEAD 2, default = the panel ahead;
- SWORD_ARC_ROW (canon: SwordArcBySubfamily_80EBAD8, asm31.s:109352), rows
  mapped to (animation, palette) via byte_80E0398: 0x16->(0,0) 0x17->(1,0)
  0x18->(2,0) 0x19->(0,5) 0x1a->(1,5) 0x2d->(1,6);
- elemental palette add = subfamily - 0xb for subfamily 0xc..0xf
  (asm31.s:109268-109276), straight from the record byte.
New top-level `SWORD_ARC_UP` use: the block reuses the existing fitted 0x10.

Counts after: distinct CHIP_* ids inside chip_strike **24** (bombs/seeds/
airshot/vulcans/cannons — the remaining arms), sword ids **0** (all eleven
gone). `fitted constants: 19 (derived 431, peeked 147)` — HEAD 19, delta 0
(derived +1 from the TAIL layout const).

## Step 5 — re-run (2026-09-16)

`python3 tools/harness.py --no-gallery --ui isolated` after the change:
all 43 chip-* rows PASS total 0 worst 0, row-for-row identical to baseline
(same totals, same negatives: e.g. chip-sword 6397, chip-muramasa 8824,
chip-areagrab 28276). gunner FAILED 2105613 and cursor FAILED 1/1/170 are
byte-identical to baseline (pre-existing on main; the ticket expects cursor
<=1/1/170). `fitted constants: 19`.

## Step 6 (2026-09-16)

`python3 tools/harness.py --only chip-use,cannon,field,tiles,gauge
--no-gallery`:
- chip-use  isolated PASS 0/0, frames 30 | negative not blind (7768)
- cannon    isolated PASS 0/0, frames 40 | negative not blind (9505)
- tiles     isolated/integrated PASS 0/0 frames 8
- gauge     isolated/integrated PASS 0/0 frames 8
- field     isolated PASS 0/0 frames 40; integrated FAILED (allowed:
  AUDIT-6) total 158928 — verified pre-existing on the baseline build
  (158940 there; my change does not move it)
- chip-use  integrated FAILED (allowed: F41) total 275307 — identical to the
  baseline build's 275307.
mettaur 0/0/70 and cursor 1/1/170 unchanged (from the full runs above;
cursor <= 1/1/170 as the ticket expects). Harness verdict: "all checks
pass: every compared frame reads 0".

## Unverified

- Only the 48 exported chips carry the tail; the other 363 ChipDataArr rows
  are not in the asset, so family-driven dispatch is proven on the sword
  eleven only.
- byte_80E0398's row -> (animation, palette) mapping is still a canon-tagged
  match on the row byte, not a read of the effect table itself; rows outside
  {0x16..0x1a, 0x2d} fall back to (0,0).
- AttackParam1..4 / EffectFlags / LockoutFrames are exported but unread
  (StepSwrd's Param1=1, Muramasa's EffectFlags=0xda are unexplained).

## Next ideas

- Follow-up ticket: drive the bomb/seed family (attack_family 0x12) from
  AttackParam1..4 the same way — GrasSeed's params [2,2,0,0] look like they
  encode the grass sheet.
- byte_80E0398's entry layout would let the arc (anim, palette) mapping be
  read from the ROM data instead of the six-row match.

## verify_rows (2026-09-16)

`python3 tools/verify_rows.py wt/t17-chips-as-data` over the eleven
sword/blade rows + chip-use at 638bcc3: every expectation MATCH
(0/0/40/<neg> each, chip-use 0/0/30/7768), verify_rows PASS.
