# T129 — M3's ownership rule: panels get the Alliance byte, and AreaGrab's steal stops being a stand-in

Worktree `/tmp/bnwt/t129`, branch `wt/t129`, started from main `a6d027e`. **STATUS: steps 1-3
landed with measurements; step 4's port is ON THE BRANCH but the new `panel_steal` row does NOT
read 0 (3190516/37634/90) and cursor's tear moved off class with no pad restoring it — branch
left UNMERGED on purpose. Read "Step 4 (blocked)" below before continuing.**

## Step 1 — the write list, from the asm, zero captures

**The only write to `oPanelData_Alliance` (+0x3) in the whole ROM** (grep over every
`strb r?, [r?, #oPanelData_Alliance]` in reference/bn6f/asm): one instruction,
`object_setPanelAlliance` (object.s:2609-2627):

- `bl object_getPanelDataOffset`; if the panel's `Type` (+0x2) is 0 (hole), return without
  writing (`beq locret_800CC34`, object.s:2616-2618).
- `strb r6, [r0, #oPanelData_Alliance]` — Alliance = caller's third arg (object.s:2619).
- clears `Unk_10` (+0x10, u16) to 0 (object.s:2620-2621).
- `bl object_updatePanelParameters` — recomposes Flags (below).

Callers of `object_setPanelAlliance` (grep over `bl`): exactly three.
1. `sub_80C64A0` (asm31.s:30616) — the AreaGrab steal orb's landing action.
2. `t4_0x51_80E655C` (asm31.s:44044, then `object_setPanelAllianceTimerLong`).
3. `sub_80E65E0` (asm31.s:98548, then `object_setPanelAllianceTimerLong`).

**The 0x04000000/0x08000000 alliance flag bits are never stored by anyone.** They are
DERIVED every `object_updatePanelParameters`: `sub_3007978` (asm38.s) walks the panel's
occupancy word (`unk_2034F60` indexed col/row), and for every object standing there ORs
`oCollisionData_SelfCollisionTypeFlags >> 16` into the panel's Flags (`lsr r3,#0x10; lsl
r3,#0x10; orr r0,r3`), recomposed at asm38.s:4234 — the same call that ORs `Alliance << 5`
(0x20 enemy-panel bit) and the type table word `word_3007924` (asm38.s:4213-4219, ROM
0x081D7E24).

**Which panels AreaGrab takes, in what order.** `areaGrabMoveBoundary_80E0754`
(asm31.s:85444) — first frame: Param1==0 spawns ONE orb at `object_getEdgePanelMatchingRow`;
Param1!=0 calls `sub_800D58C` and spawns one orb per row (loop r1=1..3, asm31.s:85492-85500).
Each orb = `spawnAreaGrabOrb_80C6548` → type-3 object kind 0xf (asm31.s:30657-30681),
inheriting the spawner's Alliance (asm31.s:30669-30670). Orb: setup Z=0x1000000 /
ZVel=0x80000 (32-frame fall, sub_80C6438 asm31.s:30516-30544); landing action sub_80C64A0
(asm31.s:30542-30598): gate `object_checkPanelParameters(col,row,0x20,0x0F88)` from
`byte_80C6514[Alliance*8]` (want_set 0x20 for alliance 0, want_clear 0x0F88), then
`sub_800D668(col,row,Alliance^1)` (object.s:4162-4217 — the boundary walk, no reserve
write), then `object_setPanelAlliance(col,row,orb Param1)` +
`object_setPanelAllianceTimerLong` (asm31.s:30594-30618) — one panel per orb per frame;
orbs land together = the column in one frame.
`object_setPanelAllianceTimerLong` (object.s:2630-2638) writes 0x708 to
`unk_2034010+col*8+6` — the per-column revert timer, the source of the live `Unk_12=0x0708`
(`_object_setPanelType` writes the same for types 9..12, asm38.s). TimerShort writes 1.

**The reserve pointer.** `object_reservePanel` (asm00_2.s:25036-25056) sets
ReserverObjectPtr + Flags|=0x80; `object_removePanelReserve` clears both; the AreaGrab chain
never writes it.

## Step 2 — the steal's true frame (canon watch, 1 capture)

Canon side of the chip-areagrab recipe (`_chip_canon_0c("a3")`, STERILE +
afterdissolve_0x0c + the frame-3 AIData/mirror press pokes), 110 frames, watches on
panel (4,1) = panel[3] = 0x02039C00+3*0x20: `0x02039C62:2` (Type/Alliance) and
`0x02039C74:4` (Flags) → /tmp/t129_ta.bin, /tmp/t129_fl.bin.

**The flip is ONE event in 110 frames: canon frame 79 — Alliance 0x01 → 0x00, Flags
0x00010032 → 0x00010012, Type stays 0x02.** Nothing else changes through frame 109. The
77-frame chip-areagrab window (canon_ref 5 .. 81) CONTAINS the steal at 79 — the cut was
hiding it behind `--disable-bg`, not ending before it. scoreboard.py's "stops one frame
short of its steal" note is retire-able in this form: the steal is at window-relative frame
74 of 77, invisible because bg is off, not because the window ends early.

## Step 3 — baseline (before any edit), main a6d027e

ROM ours (plain): sha256 13a674e4bb8a76668cce903d01b2369685d6cc4ff0498ef448fdfb7c7717072f,
592748 B. `--ui isolated --no-gallery`, all identical to T130's landed numbers:
chip-areagrab 0/0/77 (neg 28276), mettaur 0/0/70 (41734), wave 0/0/90 (3840), window 0/0/16
(81056), opening 0/0/40 (86591), chip-cannon 0/0/40 (9505), windowclose 0/0/40 (207166),
popup 0/0/80 (1288), result 0/0/40 (111839), cursor FAILED 1/1/170 (186279 — the documented
landed tear; main.rs is at SEAM_PHASE_PAD_ITERS=25 on main, not 17). fitted 17 / derived
436 / peeked 146. T121b's `panel_poison` row did NOT land (no panel scenario in states.py).

## Step 4 — the port (ON THE BRANCH, row NOT green)

- `src/field.rs`: `Panels.enemy_owned: [bool]` REPLACED by the canon container,
  `alliance: [u8; PANEL_COUNT]` (0 player / 1 enemy, PanelData.inc +0x3); init cols 4-6 = 1;
  `enemy_owned()` reads the byte; `steal_column` writes the byte to 0 (cite object.s:2619)
  and still clears `ENEMY_PANEL` (asm38.s:4220-4223 recomposition).
- `panel_flags::ENEMY_ALLIANCE` 0x04000000 / `ALLY_ALLIANCE` 0x08000000 named with
  PanelData.inc cites + the derivation gap documented (occupancy mask carries no side; the
  bits stay clear except where writers imply them — canon's panel (4,1) word matches ours
  byte-for-byte at 0x00010012/0x00010032 either way on this route).
- `src/battle.rs`: the AreaGrab dispatch arm's "The stand-in is nothing" comment retired,
  replaced by the measured mechanism (flip at canon 79, object.s:2619).
- `tools/harness.py`: new `panel_steal` row — chip-areagrab's recipe with the window at 90
  frames (canon 5..94, 16 past the steal) and bg back on both sides; fixture flags 0x1B
  (BLANK_BACKDROP cleared, bit2) and no `--disable-bg`.
- `tools/states.py`: `panel_steal` trace scenario (canon_ref 5, rust_base 122, frames 90).

**RESULT: panel_steal isolated FAILED total 3190516 worst 37634 frames 90, negative not
blind (3241748).** Diffmask at capture frame 90: 98.01% of the screen (37634/38400), rows
0..159, cols 0..239 — a WHOLE-SCREEN difference, not the stolen column, not the orb. So the
bg-on afterdissolve pair does not render the same backdrop at all; the row as designed
compares the full backdrop, and something wholesale differs (candidates for the next
worker, in order of suspicion: (a) our plain-ROM fixture path with BLANK_BACKDROP cleared
has never been exercised against the afterdissolve state — the `field` row proves bg-on
parity on a DIFFERENT route; (b) the sterile canon state's backdrop scroll/palette state at
load vs ours; (c) the harness's isolated-ui handling of a row whose sides pass no
--disable-bg/--disable-obj at all). Do NOT chase by re-disabling bg — that deletes the
row's subject; find the backdrop delta first with `tools/probe.py diff` or a --only-bg
pair, and check what the `field` row's rust Side passes that mine does not.

## Step 5 — controls with the port in place (isolated, --no-gallery)

chip-areagrab 0/0/77 (neg 28276) — the 77-frame control holds, the field change altered
nothing before the steal. mettaur 0/0/70, wave 0/0/90, chip-cannon 0/0/40, window 0/0/16,
windowclose 0/0/40, popup 0/0/80, result 0/0/40, opening 0/0/40 — ALL byte-identical to
step 3. **cursor FAILED 38/37/170** (neg 186276) — the footprint moved the seam, as
cursor.md predicts for any code move. Pad sweep authorised by the coordinator (one-integer
SEAM_PHASE_PAD_ITERS): 22→29/28, 25(landed)→38/37, 26→32/19, 28→22/19, 30→22/19. **No pad
in 22..30 restores ≤1/1.** src/main.rs RESTORED to the landed 25 and left clean; per the
coordinator rule the branch stays UNMERGED rather than widening anything.

fitted 17 / derived 436 / peeked 146 before AND after — no new fitted constants; every new
number carries an asm or PanelData.inc cite. 3 probe captures spent total (canon watch,
both-side panel_steal pair), well under the 6 budget.

## Mechanism (one line) and unverified (one line)

- Mechanism: AreaGrab's steal is `object_setPanelAlliance`'s single `strb` to
  `oPanelData_Alliance` (object.s:2619) on each orb's landing — the drawn tile and the
  Flags word follow through `_object_updatePanelParameters`' recomposition (Alliance<<5 →
  the 0x20 bit drops); the 0x04000000/0x08000000 bits are per-frame derivatives of the
  occupants' collision-type flags (sub_3007978, asm38.s), never stored.
- Unverified: the other two `object_setPanelAlliance` callers (t4_0x51_80E655C asm31.s:44044,
  sub_80E65E0 asm31.s:98548) — no scenario reaches them; the occupant-side derivation of
  the two alliance flag bits (our occupancy mask carries no side); and WHY the bg-on
  afterdissolve pair differs whole-screen (panel_steal's blocker, above).

## Next-ticket list

1. panel_steal's whole-screen bg-on delta (the row exists, the recipe exists, the flip
   frame is pinned at canon 79 — only the backdrop parity on this route is missing).
2. The occupant-side derivation of Flags 0x04000000/0x08000000 (needs battle.rs to pass
   ally/enemy occupancy separately — one call-site line outside this ticket's allowed arm).
3. cursor's seam at the new footprint: sweep SEAM_PHASE_PAD_ITERS beyond 30 (values 22..30
   all ≥22/19 here; a V-bottom may exist below 22 or above 30).
