# T130 — M3's first live rule: `object_panel_setPoison`'s masked template, and the flip frame settled from the capture already on disk

Worktree `/tmp/bnwt/t130`, branch `wt/t130`, started from main `4519b2a`.
Baseline (before any edit): `chip-poisseed isolated PASS total 0 worst 0 frames 70 | negative: not blind (total 50708)`; fitted 17 (derived 436, peeked 146).

## Step 0 — the verdict on `/tmp/t121b_wide.txt`: it is USABLE, and the flip is IN it

**The ticket's premise is wrong in our favour: the file contains values.** The verifier's
"rc=0 with an empty value column" does not describe what is on disk today. Measured with
python over the raw file:

- Line 1 is the header `frame 0x02039c00  ` (with two trailing spaces — this is
  `tools/probe.py cmd_watch`'s own header format: `print("frame " + " ".join(...))` with ONE
  watched address, so one column label and no `HDR:` prefix). 263 lines total: header +
  260 frame rows (ids 0..259) + a `#E#` footer.
- Each frame row is `<frame id> <1600 hex chars>` = the full 800 bytes (0x320) watched.
  That is `--watch 0x02039C00:0x320:<file>` — the **file-target watch form**, not the stdout
  table form. The verifier's `HDR:`-shaped output is a different read path (a stdout/harness
  table form) whose value column came up empty; the file on disk is the worker's own
  file-target capture and it is complete. So: the worker's numbers came from a *different
  read path than the verifier's*, exactly the question the ticket asked.
- Why the verifier's per-panel forms died: `mgba_capture --watch` takes **max 16 watches**
  (`--watch: too many watches (max 16)`, tools/mgba_capture.c) — 25- and 50-address forms
  cannot work; rc=1 there is the tool working as designed. A single wide watch is the
  supported form and is what this file is.

**The flip, reproduced from the file (zero captures spent):**

- Layout: the watched window is the panel array, stride **0x20** bytes per panel (panel[0]
  at +0x00: Type +0x2 = 02, Animation +0x6 = 02, Unk_12 +0x12 = 0x0708, Flags +0x14 =
  0x00010012 own side / 0x00010032 enemy side), 8 panels per row = 0x100 (6 visible + 2
  padding, matching T121b's own note), 25 entries inside the 0x320 window.
- **Capture frame 52**: exactly one word changes: panel[12] (= row 2, col 5 — the 3x3's
  centre) Flags `0x00010032 -> 0x80010032` (the ally-attack occupancy bit, PanelData.inc
  0x80000000).
- **Capture frame 53**: **NINE panels flip, not six** — indices {3,4,5,11,12,13,19,20,21}
  = rows 1-3 × cols 4-6 (1-based), a 3x3, the enemy half. Per panel: Flags
  `0x00010032 -> 0x00010134`, and words +0x0/+0x4 `0x01020001 -> 0x01040001` (Type byte +0x2
  02→04, Animation byte +0x6 02→04). T121b's "six panels, cols 4-6 × rows 1-2" is refuted by
  its own capture: the third row is there (`0x01040001` at panels 19/20/21), and our own
  sheet loop (`src/battle.rs` poison_pending block: `field::half(true)` = cols 4-6, rows
  1..=field::ROWS) already covers exactly those nine.
- Frame numbers are capture frames from the `afterdissolve_0x0c` loadstate, not
  battle-aligned frames; both the worker's "52/53" and mine read the same column, so
  worker-attested = reproduced, same numbers, now from a file either side can read.
- Algebra check on panel[12]: `0x80010032 & ~0x3f5f | 0x114 = 0x80010134`, but the file
  reads `0x00010134` — the occupancy bit was cleared the same frame the poison landed, so
  the observed word is template-after-pod-teardown, sampled after the whole frame. The other
  eight panels go `0x10032 -> 0x10134` = the template exactly.

## Step 1 — the writer pinned, with the guard polarity CORRECTED (the ticket had it backwards)

`object_panel_setPoison` (asm/object.s:2540-2563), instruction stream:

```
bl object_getPanelDataOffset      ; r0 = panel data ptr (or 0)
tst r0, r0 / beq -> return 0      ; no panel -> no poison
ldr r1, [r0,#0x14]                ; Flags
mov r2, #0x10
tst r1, r2                        ; Z = (Flags & 0x10 == 0)
beq -> return 0                   ; taken when the bit is CLEAR
ldr r2, =0x3f5f (dword_800CBD0) / bic r1, r2
ldr r2, =0x114  (off_800CBD4)   / orr r1, r2 / str r1, [r0,#0x14]
mov r2, #4 / strb [Type +0x2] / strb [Animation +0x6]
mov r0, #0x90 / bl PlaySoundEffect / return 1
```

**The guard's meaning: `tst`+`beq` branches to the return-0 tail when `Flags & 0x10` is
CLEAR, so a panel is poisoned only when bit 0x10 IS SET.** The ticket (and its restatement
of the verifier's reading — "only a panel whose Flags & 0x10 is zero is poisoned", "a panel
with the bit set is skipped") states the opposite polarity. Normal panels carry 0x10
(0x00010032/0x00010012), the table words for hole (0x18000) and broken (0x14000) do NOT —
so the guard's visible effect is: poison cannot land on a hole or a broken panel. The same
guard shape guards every writer of the crack/break family (object.s:2215-2563: `tst #0x10;
beq` at each of object_crackPanel/object_breakPanel/object_breakPanelLoud/dups).

Caller chain pinned: `byte_80E2588` (asm31.s:89651-89667) is 3 entries × 0x18 bytes
(Param1 0/1/2); entry +0x14 holds the writer pointer (`object_crackPanelDup1+1`,
`object_breakPanel_dup3+1`, `object_panel_setPoison+1` — the ONLY reference to setPoison in
the whole ROM: no `bl` site anywhere, grep-checked). `t4_0x1e_80E25D0` (asm31.s:89665-89681)
dispatches on CurState through `off_80E25E4` → {sub_80E25F0 (setup: ExtraVars =
entry base, then collector by entry+1 byte via off_80E2620), sub_80E2628 (per-frame drip:
Timer 8 → one panel per 8 frames off the shuffled list, writer at entry+0x14), sub_80E268C
(teardown)}.

**Two honest gaps, reported not papered over** (both next-ticket material):
1. The live flip is 9 panels in ONE frame; `sub_80E2628`'s drip is one panel per 8 frames.
   Nine one-panel objects spawned together (Timer=8 → all write the same frame) would fit,
   as would a writer path not through this object; unverified either way. What the capture
   *does* pin: the flip lands the frame after the pod's occupancy bit appears/clears
   (52→53), so the +8-drip read of the sheet route is not what happened live.
2. The poison entry's collector words (sub_80E269A passes [entry+4] as want-SET and
   [entry+8] as want-CLEAR to `object_checkPanelParameters`, object.s:2688-2713) decode to
   want_set=0, want_clear=0x10 for entry 2 — the collector would pre-filter OUT the very
   panels setPoison's guard requires. My byte alignment of the entries may be off by the
   tail word (the listing's 3 entries are 28+24+20 bytes against a 0x18 stride); the
   alignment of +4/+8 per entry is the part to re-derive before leaning on it.

Our corresponding object today: the PoisSeed sheet arm in `src/battle.rs`'s
`poison_pending` block (sheet loop over cols 4-6 × rows 1-3, one frame after the pod lands)
and the VDoll `rest_poisons` arm (single panel, object_setPanelType 4 at the landing,
sub_80D47C0 loc_80D481E) — plus `src/field.rs`'s `Panels`, which until this ticket had no
flag word at all.

## Step 2 — the two citation defects fixed (plus the area rule re-stated)

- `bn6f.map:34342` is `0x081d6000 IWRAMRoutinesROMLocation` (read the line) — not the
  table. `word_3007924` is in no map line (grep). Correct provenance, from the copy
  routine itself (reference/bn6f/asm/start.s, `start_copyMemory` call):
  `IWRAMRoutinesROMLocation -> 0x3005B00, size 0x1ed4`; the table's IWRAM address gives
  `0x3007924 - 0x3005B00 = 0x1e24`, so ROM `0x081d6000 + 0x1e24 = 0x081d7e24`, and
  `0x1e24 < 0x1ed4` is inside the copy. Fixed in `docs/coverage/panels.md` and
  `tools/panel_flags_export.py` (docstring + comment only).
- The "each entry gated by the 0x10 guard byte" aside is gone: the `0x10`s inside
  `byte_80E2588` are per-entry check words fed to `object_checkPanelParameters`
  (want-set/want-clear), while setPoison's guard is an immediate `#0x10` tested against the
  panel's own Flags. Two unrelated 0x10s. Also corrected: "per-CurState writer table" →
  CurState picks the routine (off_80E25E4), Param1 picks the entry (Param1*0x18).
- Area rule, from `sub_80E269A`'s own loop (asm31.s:89775-89810): the collector enumerates
  **cols 6..1 × rows 3..1 = all 18 panels** and keeps those passing
  `object_checkPanelParameters(col, row, [entry+4], [entry+8])`, then shuffles the survivors.
  The listing pins the enumeration and the filter, not the final shape; the LIVE shape is
  measured: nine panels, cols 4-6 × rows 1-3 (the enemy half, 3x3). "3x2" is refuted and no
  "3x2" claim remains anywhere.

## Step 3 — the port (src/field.rs only; src/battle.rs untouched, wt/t128 honoured)

`Panels` gains `flags: [u32; PANEL_COUNT]` and the type-4 write routes through
`object_panel_setPoison`'s sequence **inside field.rs**, so both existing call sites in
`src/battle.rs` (the PoisSeed sheet loop's `panels.set(c, r, PANEL_POISON)` and VDoll's
`rest_poisons` arm — both already plain `set(.., PANEL_POISON)` calls, and the VDoll arm
lives in the block wt/t128 converted) get the template with zero battle.rs edits:

- guard: `Flags & 0x10` must be SET, else no write at all (return false, like the
  routine's return-0 tail) — `panel_flags::GUARD_10`, canon: object.s:2544-2546 immediate;
  unnamed (no .inc names the bit).
- `Flags = (Flags & ~0x3f5f) | 0x114` — `PANEL_POISON_MASK` = dword_800CBD0
  (object.s:2561), `PANEL_POISON_TEMPLATE` = off_800CBD4 (object.s:2563). The mask clears
  0x2 (blocks movement) and re-sets 0x10 through the OR; 0x4 is unnamed, set by this
  routine itself; 0x114 = the landed type-4 table word 0x110 + that 0x4.
- `Type = 4` written directly (the `strb`s at :2552-2554); `Animation = 4` arrives through
  the existing per-frame tick (`update()` derives animation from types — same-frame, the
  same path every other type change uses).
- init flags = `PANEL_FLAG_WORDS[type] | alliance<<5 | 0x2` — composition, not literals:
  `PANEL_FLAG_WORDS` = the exported table's own words for types 0-4 (canon: ROM
  0x081D7E24+k*4, assets/panels.bin), `ENEMY_PANEL` 0x20 OR'd from `Alliance << 5`
  (asm38.s:4220-4223, kept in step by `steal_column`), `BLOCKS_MOVEMENT` 0x2 named in
  PanelData.inc. The composition reproduces BOTH measured frame-0 words exactly
  (own 0x00010012, enemy 0x00010032); which routine ORs the 0x2 at init is unpinned
  (measured present in /tmp/t121b_wide.txt frame 0).
- `steal_column` clears `ENEMY_PANEL` when the player takes the column (same asm38.s cite).

## Step 4 — the comparison, labelled honestly: row bytes + a ONE-POINT STATE INVENTORY against canon's watched values, not a compared row

- `chip-poisseed` (the route's existing pixel row): **byte-identical** — PASS total 0
  worst 0 frames 70, negative not blind (total 50708), identical to the pre-change
  baseline. On this row BOTH sides are sterile/blank-backdrop, so neither side paints
  panel tiles and the row cannot see Flags/Type — it pins only the sheet timing.
- Our side has no watch static (forbidden) and no trace scenario (states.py is T127's
  file this pass), so the flag word was read by a **--peek report**: one probe capture of
  our own plain ROM under the chip-poisseed fixture variant with BLANK_BACKDROP cleared
  (so the field paints and the write happens), `--dump 0x03000000:0x8000` at frame 200.
  The `Panels.flags` block was located at IWRAM **0x03007528** (pattern search: nine
  0x10012 / nine 0x10134 in the 18-panel array, regen 0x258 alongside). A second capture
  `--watch 0x03007534:4 0x0300759f:1` (flags[3] and types[3], panel row 1 col 4):
  **capture frame 178: Flags 0x00010032 -> 0x00010134, Type 0x02 -> 0x04** — one frame,
  no intermediate, the exact values canon shows at ITS frames 52/53 in
  /tmp/t121b_wide.txt. This is a one-point state inventory against canon's watched
  values, NOT a first-divergence-none row comparison: M3's pixel-visible poison row needs
  a non-sterile route in tools/states.py, which is T127's file this pass — that is the
  next ticket, not an edit under this one.
- 2 captures spent total (budget <=5); canon cost 0 (the preserved file).

## Step 5 — what the flag word unlocks, and the unnamed-bit census

- The movement rule and the poison tick can now key on real state: bit 0x2
  (blocks movement, PanelData.inc) is per-panel readable and the template clears it, and
  Type 4 / flag 0x100 identify the poisoned panel. The tick's owning routine is NOT
  implemented here and stays unnamed by measurement — tickPanels_800C380
  (asm/object.s:1421-1559) only handles regen (T121b's finding); the reader of
  Flags 0x100 / Type 4 (the standing-on-panel damage tick) is the next ticket.
- Unnamed-bit census: 0x4 reached live, no .inc name (setPoison's own); 0x10000 set in
  all 13 table words, unnamed; 0x80000000 (ally attack) seen live at frame 52 — of the
  high nine bits 0x40000000..0x80000000 no live route has been seen touching any bit
  except 0x80000000 on this capture. T129's problem.

## Step 6 — guard set, cursor (pad sweep), ROM, counts

- 8-row guard set at the landed pad (17): mettaur 0/0/70/41734, wave 0/0/90/3840,
  window 0/0/16/81056, opening isolated 0/0/40/86591, chip-cannon 0/0/40/9505,
  windowclose 0/0/40/207166, popup 0/0/80/1288 — all byte-identical to the T90/T122
  baseline; opening integrated 18740/647/40/98649 = the documented landed failure
  (F43), unchanged. **cursor 31/28/170, negative 186277 — OUT of class** (1/1/170),
  as cursor.md predicts for any code move.
- Pad sweep (`SEAM_PHASE_PAD_ITERS`, src/main.rs edited in place, NEVER committed):

| pad | cursor total/worst/170 | negative |
|-----|------------------------|----------|
| 0   | 34/19 | 186285 |
| 4   | 18/17 | 186275 |
| 8   | 23/22 | 186277 |
| 12  | 37/34 | 186277 |
| 16  | 32/31 | 186276 |
| 17 (landed) | 31/28 | 186277 |
| 20  | 26/25 | 186276 |
| 22  | 7/6  | 186276 |
| 23  | 26/25 | 186276 |
| 24  | 3/3  | 186279 |
| **25** | **1/1** | **186279** |
| 26  | 16/15 | 186275 |
| 28  | 29/28 | 186277 |

  **Pad 25 restores the class exactly: cursor 1/1/170, negative 186279 — main's own
  numbers.** The branch CAN land, but landing requires the one-integer
  `SEAM_PHASE_PAD_ITERS` 17 -> 25 commit in src/main.rs, which this ticket's standing
  constraint ("never commit src/main.rs") forbids — the parent decides; src/main.rs is
  left at 17 and clean. A landing pass should re-run the full guard set at pad 25 (only
  cursor was re-measured there).
- ROM: ours sha256 5a09e6d342c2c215843cb6a137cdb3809d2264869b42833de5f9278a7682bf35,
  592716 B; main's plain build c1ead4cdb7e154508ac9c7611ea1ae72ad4131aa3dfbf81092709a28d0679915,
  592752 B — **delta -36 B** (non-monotonic, as warned).
- fitted/derived/peeked: 17 / 436 / 146 before AND after — no new fitted, every new
  constant carries a canon cite.
- inventory.py / SCOPE: the M1-panels map cite corrected in tools/inventory.py (comment +
  row) and docs/SCOPE.md (table + section header) to the copy-arithmetic derivation; the
  M3 line went from "not started (the Mettaur/chip rows cover a few incidentally)" to the
  same line plus T130's dated increment (per-panel Type/Animation/Flags live with the
  guard + masked template; flip settled at frames 52/53; state peekable, not yet
  pixel-compared outside the sterile sheet route).
- trace.py: `panel_type` (0x02039C62:1) and `panel_flags` (0x02039C74:4) added to
  CANON_WATCHES (15 of mgba_capture's 16 used) — panel[3], the first flipped panel.

## Mechanism (one line) and unverified (one line)

- Mechanism: the poison flip is setPoison's masked template — guard Flags&0x10 SET, then
  `(Flags & ~0x3f5f) | 0x114` — which clears blocks-movement 0x2, keeps enemy-panel 0x20
  and unnamed 0x10/0x10000, and sets poison 0x100 plus setPoison's own unnamed 0x4, with
  Type=4/Animation=4 alongside.
- Unverified: the crack and break templates' live behaviour (shared writer table, never
  measured), what bit 0x4 is, which object instance delivers the nine one-frame writes
  (sub_80E2628's drip is one per 8 frames), which routine ORs init 0x2, and the poison
  damage tick's owner.

## Veto — verify_rows from a clean checkout — PASS

`python3 tools/verify_rows.py wt/t130 (03d77d3) mettaur,wave,window,opening,chip-cannon,
windowclose,popup,cursor,chip-poisseed` — **PASS, all 9 MATCH**: the seven zero rows and
chip-poisseed at their exact baseline numbers, opening at its documented integrated
failure (18740/647/40/98649), cursor at the committed pad-17 value (31/28/170/186277,
out of class — the pad-25 sweep table above is the landing path).

## Next-ticket list (for whoever picks up M3's panels)

1. The poison damage tick's owner: the reader of Flags 0x100 / Type 4 on the
   standing-on-panel tick (tickPanels_800C380 is regen-only). Needs a states.py route —
   T127's file this pass, so it waits.
2. The pixel-visible poison row: a non-sterile route so panel tiles are comparable on
   both sides (same states.py dependency).
3. Which object instance delivers the nine one-frame writes, and whether crack/break's
   live templates match their listing arms.
4. The high-nine-bits census (0x40000000..0x80000000) — T129.
5. If the parent authorises it: `SEAM_PHASE_PAD_ITERS` 17 -> 25 in src/main.rs lands
   this branch's cursor in class (1/1/170/186279, byte-identical to main's numbers).
