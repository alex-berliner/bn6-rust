# T96 worklog — chip-cannon audio harness wiring + diff

## Context
- TODO.md T96 (revised): M9's first closable audio row, scope limited to
  `tools/harness.py` chip-cannon row + `docs/coverage/audio_parity.md` +
  this worklog. mgba_capture.c, src/, tools/audio_diff.py, tools/allowlist.py,
  tools/trace.py, tools/oracle.py, reference/bn6f are off-limits.
- The flag is `--dump-audio <dir>` (not `--audio`) per mgba_capture.c:30-48
  (header comment; loop writer at mgba_capture.c:952-967 writes
  `<dir>/frame.%05d.pcm` -- signed 16-bit interleaved stereo PCM, ~95998 Hz
  measured, post-mix bus).
- T95's `tools/audio_diff.py` does not exist on this branch (`git ls-tree`
  confirms; no `wt/t95` worktree exists). T13's `tools/audio_probe.py`
  IS present and provides the same sample-exact comparison primitives
  (compare / summary / shift / zero). Will use audio_probe.py as the
  functional equivalent for the diff step and note the T95 dependency gap.

## Steps
1. mgba_capture.c cite (DONE): `--dump-audio <dir>` writes
   `<dir>/frame.%05d.pcm` per frame (mgba_capture.c:958).
2. chip-cannon row located: scoreboard.CHIPS first entry
   `("demo-cannon", "01", 40, [])` → Check("chip-cannon", ...) via
   `_chip_checks()` at tools/harness.py:966-1005.
3. harness wiring (DONE): added `Side.audio_dir: Optional[str]` field,
   `Check.audio_subdir: str` field; `Side.args()` appends
   `--dump-audio <audio_dir>` when set; `run()` resolves
   `<out_dir>/<audio_subdir>` on both rust and canon sides when the
   check has a non-empty `audio_subdir`; `_chip_checks()` sets
   `audio_subdir="audio"` only for `feature == "demo-cannon"`.
4. capture + diff (PENDING).
5. PASS / NEGATIVE verdict (PENDING).
6. docs/coverage/audio_parity.md (PENDING).

## Ideas tried, dropped
- Adding a per-Side static `audio_dir` directly inside `_chip_rust` /
  `_chip_canon_0c`: rejected because the audio_dir is a function of the
  per-capture `out_dir` (rust_dir/audio vs canon_dir/audio) which only
  run() knows. The `audio_subdir` indirection on Check lets run() do the
  join.
- Wiring --dump-audio through `extra=()`: rejected because `extra` is a
  tuple of literal mgba args, not a template -- the audio path would have
  to be a static string baked into each side factory, which collides
  with rust_dir/canon_dir being fresh per run.
- Writing T95's tools/audio_diff.py myself: out of scope per the ticket
  ("tools/audio_diff.py disjoint"). Using T13's tools/audio_probe.py
  compare instead, which has the same sample-exact semantics.

# T96 worklog — chip-cannon audio harness wiring + diff

## Context
- TODO.md T96 (revised): M9's first closable audio row, scope limited to
  `tools/harness.py` chip-cannon row + `docs/coverage/audio_parity.md` +
  this worklog. mgba_capture.c, src/, tools/audio_diff.py, tools/allowlist.py,
  tools/trace.py, tools/oracle.py, reference/bn6f are off-limits.
- The flag is `--dump-audio <dir>` (not `--audio`) per mgba_capture.c:30-48
  (header comment; loop writer at mgba_capture.c:952-967 writes
  `<dir>/frame.%05d.pcm` -- signed 16-bit interleaved stereo PCM, ~95998 Hz
  measured, post-mix bus).
- T95's `tools/audio_diff.py` does not exist on this branch (`git ls-tree`
  confirms; no `wt/t95` worktree exists). T13's `tools/audio_probe.py`
  IS present and provides the same sample-exact comparison primitives
  (compare / summary / shift / zero). Will use audio_probe.py as the
  functional equivalent for the diff step and note the T95 dependency gap.

## Steps
1. mgba_capture.c cite (DONE): `--dump-audio <dir>` writes
   `<dir>/frame.%05d.pcm` per frame (mgba_capture.c:958).
2. chip-cannon row located: scoreboard.CHIPS first entry
   `("demo-cannon", "01", 40, [])` → Check("chip-cannon", ...) via
   `_chip_checks()` at tools/harness.py:966-1005.
3. harness wiring (DONE, commit 73274a8): added `Side.audio_dir:
   Optional[str]` field, `Check.audio_subdir: str` field; `Side.args()`
   appends `--dump-audio <audio_dir>` when set; `run()` resolves
   `<out_dir>/<audio_subdir>` on both rust and canon sides when the
   check has a non-empty `audio_subdir`; `_chip_checks()` sets
   `audio_subdir="audio"` only for `feature == "demo-cannon"`.
4. capture + diff (NOT RUN): tool budget (60 soft) hit before the capture
   invocation; the next worker with capture budget should run
   `python3 tools/harness.py --only chip-cannon --no-gallery` and diff
   the two `audio/` trees via `tools/audio_probe.py compare`.
5. PASS / NEGATIVE verdict: **NEGATIVE on diff (no capture run)**; the
   mgba flag EXISTS in mgba_capture.c, so the NEGATIVE is purely on
   "T96's diff step did not produce a number", not on "mgba lacks the
   flag".
6. docs/coverage/audio_parity.md (DONE, format + flag + pending-row
   entry for chip-cannon with the exact `audio_probe.py compare`
   command the next worker should run).

## Ideas tried, dropped
- Adding a per-Side static `audio_dir` directly inside `_chip_rust` /
  `_chip_canon_0c`: rejected because the audio_dir is a function of the
  per-capture `out_dir` (rust_dir/audio vs canon_dir/audio) which only
  run() knows. The `audio_subdir` indirection on Check lets run() do the
  join.
- Wiring --dump-audio through `extra=()`: rejected because `extra` is a
  tuple of literal mgba args, not a template -- the audio path would have
  to be a static string baked into each side factory, which collides
  with rust_dir/canon_dir being fresh per run.
- Writing T95's tools/audio_diff.py myself: out of scope per the ticket
  ("tools/audio_diff.py disjoint"). Using T13's tools/audio_probe.py
  compare instead, which has the same sample-exact semantics.

## What the next worker should do
1. `export CARGO_TARGET_DIR=/tmp/ct_t96`
2. `python3 tools/harness.py --only chip-cannon --no-gallery` (builds
   plain ROM + captures rust+canon with --dump-audio, ~40 frames each,
   should take <60s on a warm cache).
3. `python3 tools/audio_probe.py compare /tmp/h_audio_rust_<KEY>/audio
   /tmp/h_audio_canon_<KEY>/audio --label-a rust --label-b canon`
   where `<KEY>` is the first 10 hex chars of
   `sha1(repr((rust_side, canon_side, 40, ALIGN_CHIP_0C, "")))` (printed
   as `key=...` in the harness run).
4. Paste the result into docs/coverage/audio_parity.md under
   "chip-cannon": total samples, first divergent sample (frame, offset,
   channel, both values), differing-sample count, max |delta| per
   channel, PASS-or-NEGATIVE verdict.
