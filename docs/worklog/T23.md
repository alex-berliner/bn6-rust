# T23 — locate F47's 3-frame art-clock offset: read canon's OWN anim-state record, verdict SEED / FREE-RUN / NEGATIVE

Ticket T23 (2026-09-15). Branch `wt/t23-art-clock-phase`, worktree /tmp/bnwt/t23-art-clock-phase.
Analysis artifacts (kept for the auditor): **`/tmp/bn-t23-art-clock-phase/`** — `canon_animstates.bin`
(175 frames x 0x1d0 B, `eGFXAnimStates` `0x020094c0`), `canon_scrollcnt_run1.bin` (175 x 8 B,
`eBGScrollCBCounters` `0x02009690`), `t23_analyze.py`, `t23_align.py`, the ROM builds
(`t23_rom_build1/2.gba`, `t23_rom_main2.gba`). The four /tmp canon roots were not touched
(sha256s re-verified at the end). **Capture runs used: 1 of ≤2.**

Dependency context read: `docs/worklog/T22.md` (landed 55549f2 — slotwise table, slot k =
`FRAMES[step][k-1]`, canon map ids +1 / port +512) and `docs/worklog/F47.md` (phase headline:
canon tile-set edges at k=5,13,21,29,37 = captures 140,148,156,164,172; ours at k=8,16,24,32 =
captures 129,137,145,153; "seed vs free-run UNMEASURED"). This ticket does NOT re-prove the
edge headline — it locates the clock and reads its phase.

## Gates (baseline, before any edit — worktree @ 21d7ba7, zero tree change)

`python3 tools/verify_rows.py wt/t23-art-clock-phase field,opening --expect field=0/0/40/1139
--expect opening=0/0/40/86591` → **PASS**, both rows **MATCH (isolated line)**:
`field PASS 0/0/40/1139`, `opening PASS 0/0/40/86591`, exit 0.

## Step 1 — the watch (ONE capture run): record identification

Planned per ticket: watch `0x020094c0:0x18` + `0x02009690:0x8`. Executed as ONE run watching
the WHOLE anim-state region `0x020094c0:0x1d0` (19 records, up to `eBGScrollCBCounters`) plus
`0x02009690:0x8` — same single run, but if record 0 had not been the backdrop anim, the other
18 records were already in the file and no second run would be needed. Replicated the field
row's canon Side exactly (`tools/harness.py` `_zero_enemy_canon("Start@10")`: sterile ROM,
`/tmp/pausedwithcannon.state`, DELETE_ENEMY `0x0203ab84:0 0x0203ab86:0`, script `Start@10`,
`--disable-bg`), 175 frames (F47's count, window k=0..39 = captures 135..174).

**Tooling note (reported finding, nothing edited):** `tools/probe.py watch` deletes its watch
files after printing (`shutil.rmtree(out, ...)` + `os.remove(f)`, probe.py cmd_watch), so it
cannot leave the kept dump the auditor needs — I invoked `/tmp/mgba_capture` directly with the
same `--watch ADDR:LEN:FILE` arguments probe.py builds (probe.py run_capture:60-63). If probe
should keep dumps, the change is in probe.py's cmd_watch (do not delete the `*_w_*.bin` files,
or add `--keep`); probe.py is outside this ticket's file list, so untouched.

**Run identity:** the scroll-counter stream is **175/175 word-identical** to F47's kept
`/tmp/bn-f47/canon_scrollcnt.bin` at shift 0 (`t23_align.py`, shifts -10..+10, only shift 0
matches anything) — this run IS F47's capture indexing; no cross-run correction factor applies.

**Record 0** (`// canon: eGFXAnimStates[0]`), six halfwords; **no other record (1..18) ever
changes** — record 0 is the backdrop anim:

| hw | field | behaviour | cite |
|---|---|---|---|
| hw0 | `0x0001` constant | active flag + anim id (`*v2 = 1; v2[1] = v1`) | `LoadGFXAnim` 0x8001b1c, reference/bn6f/docs/decomp/asm00_0.c:2316-2339 |
| hw1 | counts `0008`→`0001`, 1/frame | **frame countdown** — `ProcessGFXAnims` decrements and reloads from the entry's hold field (`*(v0+1) -= 1; if <= 0 ... *(v0+1) = v3[1]`) | `ProcessGFXAnims` 0x8001b94, asm00_0.c:2359-2394 |
| hw2:3 | `0x0807fba4` constant | script LoopAddress (base; the same base src/backdrop.rs's seed doc cites) | LoadGFXAnim writes `*(v2+1) = v4; *(v2+2) = v4` (words +4/+8) |
| hw4:5 | pointer `0x0807fc5c..fc84→fba4` | **entry index** — current script-entry pointer, `+8` per entry ("eight bytes an entry"); `ProcessGFXAnims` advances it (`*(v0+2) = v3`) | asm00_0.c:2374-2385 |

The halfword that never changes (hw0) is the active flag, not the index; hw1 changes EVERY
frame (countdown), so it is not the entry index either — the entry index is hw4:hw5, which
changes ONLY at step edges. Ticket step 1's "a halfword that never changes, or changes at
non-edge frames, is not the index" — satisfied: hw4:5 changes only at edges.

## Step 2 — canon's edge captures (measured)

Entry-pointer advances at captures **139, 147, 155, 163, 171** within 135..174 — every capture
≡ 3 (mod 8), nowhere else. The countdown reloads to **8** at 139..163, but to **4** at **171**:
171 is the supercycle WRAP to entry 0, whose hold is 4 (the printed pointer there is the script
base `0x0807fba4` and the printed value is `0004`, not 8 — my first gloss said "reloads to 8"
at all five edges; verifier-hyper caught it against my own printed table). Full-scan over
0..174: ramp-up
edges at 3,7,11,15,19 (hold-4 entries), then steady every-8 spacing beginning at **27** (the
first 8-gap is 19→27; 27,35,…,171).

**These differ from F47's tile edges (140,148,156,164,172) by exactly ONE capture — the ticket
anticipated this ("if they differ from F47's, that is the result"):** F47's edges came from
tile hashes; the CLOCK ticks one capture earlier. Canon's schedule on the clock: **k=4+8m**
(pointer) → k=5+8m (resident tiles; the pointer→VRAM copy lands the next capture). This
matches `Backdrop::seed`'s own note that canon's copy goes through
`QueueEightWordAlignedGFXTransfer` (sub_8001C94, asm00_0.s:3752) while the port writes
same-frame. **F47's coverage-table edge list gets this correction** (a pure append at the END
of docs/coverage/field_integrated.md, after T22's section — not beside F47's table).

## Step 3 — pairing against our side

Our side exposes no readable counter (the port's anim state is `Backdrop.step/timer`,
src/backdrop.rs:98-100, not at any watched address; F47 proved our scroll regs unreadable) —
**our step is identified by resident art, not a counter**, as the ticket anticipated. F47's
rust tile edges 129,137,145,153 (k=8,16,24,32; T22: resident steps 5,6,0,1) are the only
signal. Paired k=0 (F47's axis, canon 135+k ↔ rust 121+k):

- canon at cap 135: countdown hw1 = `0004`, entry CommandPos `0x0807fc64`, resident step 4 (T22);
- ours at cap 121: resident step 4 (T22) — **step indices equal**;
- our TIMER at paired k=0 = **8**: `FIELD_ZERO art_timer=7` + `Backdrop::seed`'s
  `self.timer = timer + 1` construction lead (tools/harness.py:1564, src/backdrop.rs:233) —
  our first tick at k=8 vs canon's k=4.

**Pairing-robustness check (done because the row's own Align offset 22 ≠ F47's 14):** the two
pairings differ by exactly one art period (8), so the countdown comparison is invariant —
canon cap 143 (the row's pairing) reads the SAME `0004`. The verdict below does not depend on
which 8-multiple pairing is right.

## Step 4 — VERDICT: SEED

> Canon's art timer is ALREADY 4 frames ahead of ours at paired k=0 (countdown `0004` vs our
> seeded 8), CONSTANT across all five measured clock edges, with no accumulating frame in the
> window (both timers decrement 1/frame, period 8 on both sides). 4 − canon's 1-frame
> pointer→VRAM pipeline = F47's 3-frame tile-edge offset (k=5 vs k=8) — this RECONCILES F47's
> number; it is NOT a prediction (F47's 3 was in hand before the equation was written). The
> verifier checked whether the `1` was fitted to close the gap and concluded it is not: the 1
> is MEASURED as tile edge 140 − clock edge 139 (canon's own clock against canon's own VRAM,
> across two dumps whose run identity was verified), and our 8 on our side is corroborated by
> F47's measured rust edge at k=8 — exactly what a timer of 8 at k=0 predicts.

Not FREE-RUN: the offsets do not accumulate and the first edge is not where equality ends —
the timer phases are unequal at k=0 itself. Not NEGATIVE: the record is at the address F47
inferred (0x020094c0, record 0), the index halfword is identified with reader/writer cites,
and the byte trail is in the kept files. **A step-index-only comparison at k=0 would have
misread FREE-RUN (both sides show step 4); the countdown phase is the discriminator.**

**src/ fix site (named, NOT edited):** the seed path is `prime_backdrop`'s `match self.fixture`
(src/battle.rs:2416) → `Backdrop::seed`'s `self.timer = timer + 1` (src/backdrop.rs:231-233),
fed by `FIELD_ZERO art_entry=10, art_timer=7` (tools/harness.py:1564); the fresh-battle default
is `timer: STEP_HOLD[0] + 1` (src/backdrop.rs:179). A timer-phase fix is a one-constant change
at those lines (art_timer 7 → 3 would put our k=0 timer at canon's 4). **Correction to F47's
prose:** its "seed gate ... is unreachable under FIELD_ZERO" is itself a miscite —
`FIELD_ZERO` has `art_entry=10 ≠ FIXTURE_UNSET (0xFFFF)` and `prime_backdrop` is called
unconditionally (src/main.rs:372), so the gate fires.

## Step 4b — corroboration I under-cited: our own landed table already carries the answer

The strongest evidence in this ticket is not the paired countdown alone — it is that canon's
dump matches **our own already-landed table** `src/backdrop.rs:90-92`
(`STEP_HOLD = [4 × 10, 8 × 19]`) **entry-for-entry**. The countdown field I watched is
`ProcessGFXAnims`'s per-entry hold reload (`reference/bn6f/docs/decomp/asm00_0.c:2359-2394`,
`v2 = *(v0+1) - 1; … *(v0+1) = v3[1]`; `+2` is the advanced pointer), so every reload value
directly reads the hold table:

- capture 0 sits at **entry 5** (`0x0807fbcc` = base + 5×8) holding **4**, and the pointer
  advances every 4 through entries 6..9 (edges 3,7,11,15);
- it switches to **8** at **entry 10** (cap 19) and advances every 8 through entries 10..28
  (edges 27,…,163);
- it wraps to **entry 0** (`0x0807fba4`, hold 4) at **cap 171**.

That is a **192-frame supercycle (10×4 + 19×8) reproduced by an independent 175-frame dump** of
canon — the ROM's own hold schedule IS our `STEP_HOLD`. The consequence for the verdict: our
side already carries the right hold data; **the defect is the SEED, not the table.** This is
precisely what **T24** is now testing (sibling branch `wt/t24-art-clock-seed`, worktree
/tmp/bnwt/t24-art-clock-seed): put our art-clock countdown at canon's measured 4 and judge on
the only lines that contain backdrop pixels (field integrated, field-bg1) — read T24's log for
its result, and stay out of its worktree.

## Step 4c — ANSWERED while in flight: the field-row 0/0/40 reconciliation (was: unverified)

The `field` row reads 0/0/40 despite the 4-frame timer offset because **the passing `field`
line is the ISOLATED `--disable-bg` variant**, which switches off all four BG layers
(`tools/mgba_capture.c:386-395` sets `disableBG[0..3] = true`) — **the backdrop is not in
those compared pixels at all.** The backdrop lives on `field` integrated and `field-bg1`.
There is no hidden hiding mechanism in the row's pairing/compare; my step-4 "tension" above is
answered, not open.

This landed on main while this branch was in flight: **F47's worklog now carries a
`CORRECTED BY T23` section (commit 3a96422)** retracting its own "seed path unreachable under
FIELD_ZERO" limitation on this branch's evidence, with the disk proof (`harness.py:1564-1565`
`art_entry=10`, `src/battle.rs:1576` `FIXTURE_UNSET = 0xFFFF`, gate `src/battle.rs:2417`,
`src/main.rs:372` unconditional call, `src/fixture.rs:132-134` `0x31 & 0x04 == 0`). If you are
reading from F47's log, that section is the chain link back here. Record also: **the
refutation of that landed claim is the reason T24 exists** (its ticket landed as main
80246a5).

## Step 5 — the no-op proof

`cargo build --release` twice in the worktree (second after `cargo clean -p bn`):
build1 == build2, both sha256 **1997be3b4e8f463ac328aede2d5a027d7f73fcb8adcab5834592d71a7a419fed**
(= today's expected tree build). Main checkout rebuilt fresh (its checked-in build artifact
was STALE — ELF dated 09:15 while HEAD 21d7ba7 landed 20:17; I deleted the untracked
`target/thumbv4t-none-eabi/release/bn` and rebuilt, source tree untouched) → `cmp -l` worktree
build1 vs main's fresh build: **0 differing bytes**
(/tmp/bn-t23-art-clock-phase/t23_rom_{build1,build2,main2}.gba).

**Tooling note:** my first cmp against main's build compared against the STALE artifact
(~478k differing bytes) — the lesson: verify the main checkout's build artifact mtime against
HEAD's commit time before trusting a cmp; a stale target dir can masquerade as a source delta.

## Numbers & gates (after)

- verify_rows re-run after the docs edit (§Measure's "re-run step 2's rows"): same command →
  PASS, both MATCH (isolated line), `field 0/0/40/1139`, `opening 0/0/40/86591` — before = after
  (byte-identical ROM, docs-only change).
- Fitted constants: `python3 -c '...provenance_counts()'` → **19** (derived 431, peeked 147) —
  unchanged (no src/ edit).
- `bash tools/check_inputs.sh` → exit 0 (run at the end; no /tmp root touched).
- /tmp canon input sha256s (for any non-zero quote; both gates here are zero-total so no
  drift band applies): /tmp/bn6f_real.gba
  a37c1028adb72082b51e142321fa437967bc54b6f46730a53f6581ad455ad670, /tmp/bn6f_sterile.gba
  af13c2061eb38fe753a0dd9d8e5d12d98af565f7b49b92a484528143e4154cf3, /tmp/chipselect.state
  64c1fd163e373b66d19d071dff337f83f926c6c972f9ed0c4132ccaba43274d3,
  /tmp/pausedwithcannon.state
  9b1e89538ad67256dcb81d847e9434b420db3c9c3916d65acd89a8022ce34aba.

## Report line (AGENTS.md shape)

rows: field, opening (isolated ui lines, both zero-total, before = after — docs-only change,
ROM byte-identical to main, cmp 0, sha256 1997be3b…419fed) · frames 40 · total 0 · worst 0 ·
region: canon eGFXAnimStates 0x020094c0 (record 0, 24 B) + eBGScrollCBCounters 0x02009690,
field row canon window captures 135..174 · commit (this branch, per landed step) · mechanism:
canon's art CLOCK ticks at k=4+8m (entry pointer hw4:5, `ProcessGFXAnims` asm00_0.c:2359-2394;
edges 139,147,155,163,171 — one capture ahead of F47's tile edges 140,148,156,164,172) and its
countdown phase at paired k=0 is `0004` vs our seeded 8 → **SEED**, constant 4-frame timer
offset (= 3-frame tile offset + canon's 1-frame upload pipeline); fix site
battle.rs:2416 / backdrop.rs:231-233 / harness.py:1564, named not edited · unverified: the
cap-171 wrap reload — first written as 8 in prose, corrected to the measured 4 (entry 0's
hold), so the steady-8 rhythm inside this window is attested only at 139..163 and the reading
concession stands; under the row's offset-22 pairing canon at 143 sits on a DIFFERENT pointer
(`0x0807fc6c` vs `0x0807fc64` on F47's offset-14 axis), so the "step indices equal at k=0" leg
holds only on F47's axis while the countdown leg (`0004` under both) holds under both
pairings; our side has no readable counter (step identified by resident art). ANSWERED during
flight (step 4c): the field row's 0/0/40 is the `--disable-bg` ISOLATED variant, backdrop not
in those pixels (mgba_capture.c:386-395); F47's retraction landed as main 3a96422 and T24
(main 80246a5, sibling wt/t24-art-clock-seed) now tests the seed.

Run stats: ~34 tool calls — OVER the ticket's ≤30 budget (the overage went to the stale-main-build cmp: main's checked-in ELF predated HEAD, so the first cmp burned ~5 calls re-building and diagnosing; see the tooling note in step 5); 1 of ≤2 capture runs; model not exposed to this agent.

## Wording-and-evidence pass (post-verifier audit, 2026-09-15)

verifier-hyper returned CONFIRM on the substance; this pass fixes prose only, ZERO captures,
ZERO re-measurement, allowed files `docs/worklog/T23.md` + `docs/coverage/field_integrated.md`
only: (1) "predicted" → "reconciles" — the 4 = 3 + 1 decomposition is not a prediction (F47's 3
was in hand); the 1 is measured (tile edge 140 − clock edge 139), not fitted; (2) gloss errors
corrected — at 171 the countdown reloads to 4 (supercycle wrap to entry 0), and steady every-8
spacing begins at 27, not 11; (3) ADDED the STEP_HOLD entry-for-entry corroboration (§Step 4b)
— the strongest evidence, previously uncited; (4) `unverified` rewritten — field-row
reconciliation ANSWERED (§Step 4c: the passing `field` line is the `--disable-bg` ISOLATED
variant, backdrop not in those pixels); remaining: the cap-171 reading concession and the
offset-22 pairing concession (pointer `fc6c` vs `fc64`, so step-index equality holds only on
F47's axis); (5) F47's `CORRECTED BY T23` retraction (main 3a96422) cross-referenced, and the
T24 link (main 80246a5, `wt/t24-art-clock-seed`) made explicit. Gates unchanged: fitted 19
(derived 431, peeked 147), check_inputs exit 0, /tmp roots intact, ROM untouched (docs-only
diff against 92dde82).
