# T65 -- the 297 formations question: ROM-side record-stream census (M1 -> M5)

## Baseline (step 1)

- `python3 tools/inventory.py` (at 9e2e8dc):
  `formations (M8): FOUND: data/BattleSettings.s battleSettingsList0:2 / BattleSettingsList1:1505, 461 records, 297 0xF0-terminated formation arrays -- verified 0/297`
- generated rows: formations.json 297 rows, lists {battleSettingsList0: 269, BattleSettingsList1: 192}; backdrops.json 3 rows (0x07: 192, 0x08: 1, 0xff: 268).
- ROM build (src/ untouched): `cargo build --release` produces only the ELF
  (no .gba); the hashed ROM comes from
  `python3 tools/gbafix.py $CARGO_TARGET_DIR/thumbv4t-none-eabi/release/bn <out>.gba`
  -> sha256 `5b46337aa27da9ca881f2321fe1e210f54717285b4dfa5256c1680bac4b985ef`
  (/tmp/t65_base.gba, 584296 bytes). Re-confirmed after the verifier fixes:
  after-ROM sha256 identical.

## The walk (steps 2-3, probe /tmp/t65_walk.py + /tmp/t65_census.py)

Pointer chain (one line): off_8020170 (asm29.s:10371, selected by selectEncounterTableForMap_80AA5F4;
EVENT_67F/680/681 swap only the internet table via 0x08020178/80/88) -> group table
(21 real-world words 0x08020190..0x080201E4 / 23 internet words, INTERNET_NUM_GROUPS GameAreas.inc:10)
-> [table + group*4] map array (16 words) -> [arr + map*4] record list; records 16 bytes
(BattleSettings.inc), byte[0]==0xff terminates (T58's self-check).

**TWO families, and the old .s parse only ever saw one:**
- family A (scripted battles): battleSettingsList0 @0x080aee70, BattleSettingsList1 @0x080b0d88
  (bn6f.map:28302/28573), consumed by getBattleSettingsFromList0/List1 (asm00_1.s:16046-16062,
  index*0x10, no roll). ROM walk: **269 + 192 = 461 records, 297 formation arrays -- the .s
  numbers were exactly right here.**
- family B (random encounters): the off_8020170 tree -- **82 distinct lists, 779 records,
  779 distinct formation arrays, ALL missed by the .s parse** (they live in unlabeled ROM after
  0x080b1bbc; BattleSettings.s has no labels for them).
- Totals: 84 lists, 1240 records, 1076 distinct formation arrays. Terminator/formation
  mismatches: **0** (every list ends on a 0xff byte[0]; every EnemySetupArrPtr reaches a 0xF0).
- Old parse missed: 779 records and 779 formation arrays (the whole encounter tree). Its 461/297
  stand for family A.

## Known answers (step 3) -- all reproduced byte-for-byte

CentralArea1 = list 0x080b4b78, owner default-variant/internet mapgroup 0x90 (g 0x10 after the
-0x80) map 0, **14 records**, terminator ok:
rec6 0x080b4bd8 rec7=0 ids 00,01,85; rec10 0x080b4c18 rec7=0 ids 00,01,85,01 (formation
0x080b5387 = T58's byte_80B5387); rec12 0x080b4c38 rec7=1 ids 00,01,01,05; rec13 0x080b4c48
rec7=1 ids 00,01,01,06. rec12/rec13 are the only rec7!=0 records in the list.

Also re-derived T58's roll arithmetic: the mod count is the **filtered** count (12 ungated
records in this list, not 14): 0x371+1=882 mod 12 = 6 (rec6), 0x375+1=886 mod 12 = 10 (rec10),
0x37D+1=894 mod 12 = 6, and 894 mod 14 = 12 = "count of 14 would have picked rec12". Lever
value = position among the rec7==0 records, as the census script computes it.

## M5 answer (step 5)

- **88 ungated (rec7==0) family-B records name enemy_idx 2..6, over 24 distinct encounter
  lists; enemy_idx 2 (92 slots) and 3 (42 slots) only. NO list read this way has an UNGATED
  record naming enemy_idx 4, 5 or 6** (NEGATIVE for ranks v3/v4/v5 via random encounters; idx 5/6
  appear only gated, rec12/13; idx 4 not at all in family B).
- Family A (scripted, indexed -- no roll lever): 50 records name idx 2 (26), 3 (45), **4 (25)**.
  So rank-4 (Mettaur v3) is reachable only through scripted-battle settings.
- Lever values per record written to docs/coverage/formations.md; range 0..13. Caveats: lever
  assumes only rec7==0 records pass (other JumpTable80AA6B8 handlers may pass with events set,
  shifting the count) and the element-mask test (sub_80AA824, ORs element bits over all quads)
  passing under the 0x1f fallback select arg; T58's three verified (poke, record) pairs fit
  count=12 exactly.

## Tools change (step 4)

parse_formations/parse_backdrops in tools/inventory.py now walk the ROM (both families) instead
of parsing .s lines; rows keep their shape (formation/entries/enemy_ids/cite/status), family-B
arrays get `rom_XXXXXXXX` names + ROM-address cites (no labels exist); backdrops census now
covers all 1240 records (0xff 1047 / 0x07 192 / 0x08 1). AS_DATA_FAMILIES untouched (T62's).

## Unverified

No scenario, no port, no src/ change (ROM sha256 gate), no harness row. The gate handlers
beyond rec7==0/1 (JumpTable80AA6B8 entries 2..11) are mapped, not audited; the element-mask
pass rate per select arg (0x40/0x20/PET byte/0x1f) is inferred, not traced.

## Step 4-5 landed

- tools/inventory.py parse_formations/parse_backdrops now ROM-walk both families (row shape
  unchanged; family-B arrays get rom_XXXXXXXX names); formations M1 line now:
  `ROM-WALK (T65): ... 1240 records over 3 lists, 1076 0xF0-terminated formation arrays -- old .s
  parse held 461 records / 297 arrays and missed the whole encounter tree (779 records, 779
  arrays); mismatches 0`. Backdrops census now over all 1240 records: 0xff 1047 / 0x07 192 /
  0x08 1. docs/SCOPE.md + docs/inventory regenerated. NOTE for the next worker: the edit tool
  resolves relative paths against the SESSION cwd, not the bash cwd -- my first edits landed in
  the main checkout's tools/inventory.py and had to be copied over and reverted there.
- docs/coverage/formations.md written: per-list record table, known-answer table, and the 88
  ungated enemy_idx 2..6 records with lever values.

## Verifier round (c4d03425) -- seven mechanical fixes applied on wt/t65

1. M1 line now prints the computed list count (84 = 2 scripted + 82 encounter-tree, from
   `sum(v.get("lists", 1))`, not the literal 3) and leads with the ROM-walk framing; the
   0/1076 denominator is annotated as arrays-all-unrecorded (0 not inflated).
2. Cite erratum (orchestrator, post-verifier): item 2 as originally executed was mislabelled
   -- NOTHING was dead. Both .inc cites are LIVE; the verifier run that reported them
   nonexistent sat in a checkout whose reference/bn6f submodule was UNPOPULATED, which is
   why they were briefly called dead and replaced. What the fix actually did was ADD
   disassembly corroboration -- asm/asm01.s:555-580 showing 0x080201E4 + 23*4 = 0x08020240
   (pt_8020240) for the 23 internet groups, and battleSettings_setBackground at
   asm/asm03_0.s:14591-14593 (strb r0,[BattleSettings_200AF60+0x4]) for the Background
   byte -- while the original cites remain correct as written:
   include/rom_structs/BattleSettings.inc:10 (`u8 Background, \background // loc=0x4`) and
   constants/enums/GameAreas.inc:10 (`.equiv INTERNET_NUM_GROUPS, 23`; line 9 is
   REAL_WORLD_NUM_GROUPS, 7). The 0x08 singleton's record-address line
   (battleSettingsList0 rec0, 0x080aee70) is unchanged.
3. Step-5 NEGATIVE rescoped per family: family B has ZERO ungated 4/5/6 records (5/6 only
   gated rec12/13; 4 absent); family A HAS 15 ungated rec7==0 records naming 4/5/6
   (0x080af430/0x080af480/0x080af4e0 name 4) but is index-fetched, no lever.
4. formations.md gained a notation legend (columns, q = 0-based quad index, pairs shape).
5. byte_* labels now carried from data/BattleSettings.s into the walk, so family-A arrays
   keep their T50 label names in formations.json (rom_* only for unlabeled family B).
6. Zero-id gap documented: 1045/1076 arrays contain a 0x00 id (e.g. rom_080aff44
   ['00','7f']); 0x00 unverified, likely player slot (T9c), not an enemy_idx.
7. Hash attribution fixed: cargo emits only the ELF; the ROM is gbafix output; before ==
   after re-confirmed.

## PASS 3 (worker-hyper, wt/t65c) -- arrays accounted + backdrop art chain

Both PASS-3 end conditions closed; zero src/ changes (ROM sha256 below identical before/after by
construction -- the build ran once on this branch and no src/ byte changed).

### 1) The 1076 arrays

- Every array now carries `referenced_by` (record addresses + owning list) in docs/inventory/
  formations.json: 1076/1076 referenced, 1240 referrer entries. Residue: **4 orphan quad runs**
  (0x080b199b/0x080b19a4, 2 quads each; 0x080b1b57/0x080b1b68, 4 quads each; 52 bytes total), zero
  references from any census record and zero ROM words equal to their addresses (aligned AND unaligned
  search). Everything else in 0x080aee70..0x080b81eb is interval-accounted: record lists (terminator+3
  align-pad bytes), 16-word map arrays, referenced arrays.
- The 297-label puzzle: `byte_80B083F` is an interior continuation label of array 0x080b083b (labeled
  dword_80B083B at its first quad), referenced by battleSettingsList0 record 0x080afa20 -- the old .s
  parse counted it as a separate array. Generator now also parses dword_ labels in BattleSettings.s.
- Method note: my first scan pass had a containment off-by-one (b<y vs b<=y) and initially reported the
  map arrays' own bytes as orphan candidates; grouping group-table words pointing at them identified them
  as 16-word map arrays (each group table slot g2 -> 0x080b23c8 etc.). Second pass explains them.

### 2) Backdrop byte -> art

Chain measured end-to-end: sub_8080DA0 (asm21.s:15-45, from initBattleStructsAndVram_80071D4
asm00_1.s:8435) -> sub_8081308 (asm21.s:469-523; reads [BattleSettings+0x4] at :473-475; 0xff -> map
default) -> id indexes off_8080E34[id*0x10] scroll/LCD, off_8080F98[id] -> LoadBGAnimData
(asm03_0.s:21209), off_8081220[id] -> LoadGFXAnims.
- **0x07** (192 recs, all BattleSettingsList1) = off_8081098: tiles off_8616598, tilemap byte_8616634,
  palette byte_8616760 = the Comps1/Comps2 map bg art asset (maps/Comps1/loader.s:236-245 off_806DBD4).
- **0x08** (battleSettingsList0 rec 0 @0x080aee70) = off_80810B4: same tiles/tilemap, palette byte_8616EC4
  = the art's alt-palette variant (maps/Comps1/loader.s:246-250 off_806DBF0). Id 6 = RobotControlComp art.
- **0xff** = NOT unset: sub_8081308 resolves real-world -> 7, net -> pt_808139C[group-0x80][map]
  (23-word table asm21.s:548-564), weather-puzzle maps -> 0x15 flag-clear else 0x10. Family-B 0xff
  defaults measured: 755 net records -> ids {0,1,3..15,17..20} + 55 weather-puzzle 0x10-or-0x15; 24 real
  records -> 7.
- Off-by-one lesson: off_8080F98's entry k is at line 214+k (label at 213) -- my first read mapped entry 6
  to 7; the generator's ROM lookup (w(0x08080F98+4v)) caught it, and the printout disagreed with my
  hand-cite, which is what surfaced it.

### 3) Bonus: 0x00 quad id = player-spawn slot

Dispatch = quad[0]>>4 into off_80073A0 (SpawnBattleObjectUsingBattleEntityConfig_8007368 asm00_1.s:8569-
8599): 0=spawnMegaMan_80073CC (asm00_1.s:8635-8648), 1=spawnEnemy_80073E2 (id = quad[2], :8727),
2=spawnMysteryData_8007424, 3=spawnRock_8007450, 8=spawnRockCube_80074FA, 9=spawnGuardian_800751C,
10=spawnMetalCube_800748A. 1045 arrays carry quad[2]==0: 991 have every such quad in the dispatch-0
player slot (unused byte), the other 54 only in mystery-data/rock/guardian quads (no enemy-id read).
ZERO dispatch-1 quads have id 0 -- enemy_idx 0 never spawns from a formation. Identity-table row 0
(ai_index 0, enemies.json) exists but is formation-unreachable.

### Dropped ideas
- Rendering the 0x07/0x08 art to name it visually: unnecessary -- both asset triples are byte-identical
  to map loaders' own LoadBGAnimData arguments, which is a stronger name.
- Chasing the unaligned word refs at 0x080be41a/0x82c8f36 for array 0x080b0000: misaligned coincidences,
  excluded from the generator's accounting (it records word_refs but they are all outside the region).

### Next (unverified)
- Whether bg id 7 == "Comps art" is visually right is UNVERIFIED (structural asset identity only).
- The scripted-battle override paths (byte_203CA50, event background word) decide the FINAL bg for
  family-A battles when they fire; which of the 192 List1 0x07 battles actually render id 7 depends on
  those overrides, not measured per-battle.
- The 4 orphan runs' origin (dev leftovers?) unknown.
