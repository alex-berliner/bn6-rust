# T119 — M2 end of battle, second pass: land T112's rank/zenny calcs on T118's main, settle battle_full zenny k=406

Branch `wt/t119-endofbattle`, from main d0d70c2. Plan: cherry-pick wt/T112's 422d5fa (src/battle.rs +
tools/trace.py) + 8dbdc72 (docs) onto T118's footprint-stable main, re-measure everything.

## Step 0 — baseline BEFORE the cherry-pick (measurement, main state d0d70c2, this tree)

`python3 tools/harness.py --only opening,mettaur,cannon,buster,chip-use,wave,windowclose,popup,result,cursor --no-gallery --ui isolated`:

| row | total/worst/frames | negative |
|---|---|---|
| opening | 0/0/40 | 86591 |
| mettaur | 0/0/70 | 41734 |
| cannon | 0/0/40 | 9505 |
| buster | 0/0/28 | 3167 |
| chip-use | 0/0/30 | 7768 |
| wave | 0/0/90 | 3840 |
| cursor | **1/1/170** | 186279 |
| windowclose | 0/0/40 | 207166 |
| result | 0/0/40 | 111839 |
| popup | 0/0/80 | 1288 |

fitted 17, derived 434, peeked 146. T118's cursor class (1/1/170/186279) reproduced on this tree.

Merge-base main..wt/T112 = 4a1d4ada; main since touched src/battle.rs (+12/-1) and src/fixture.rs (+16)
— cherry-pick conflict expected in battle.rs only; tools/trace.py unchanged on main since the base.

## Step 1 — cherry-pick (below)

## Step 1 — cherry-pick (done)

`git cherry-pick 422d5fa 8dbdc72` applied clean; src/battle.rs auto-merged (main's +11/-1 since
merge-base 4a1d4ada touches the fixture/result_elapsed area, no overlap with the two fns or the
three feed sites; `git diff wt/T112 HEAD -- src/battle.rs` = exactly main's +11/-1).
Commits a5808df (code) + ecc1338 (docs). Build OK.
ROM: **592752 B, sha256 c1ead4cdb7e154508ac9c7611ea1ae72ad4131aa3dfbf81092709a28d0679915**
(main plain was 592604 B; +148 B from the ported calcs).

## Step 2 — canon watch re-run (1 capture, 64 frames, loadstate /tmp/result_arrival.state)

All three addresses + values survive the merge, frame-for-frame identical to T112's watch:
stats record 0x0203F4A4: reward halfword +8 0xFFFF→**0x4064** at f9, clear time +4
0xFFFFFFFF→**1760** at f10, level +1→**2** at f11; results slot 0x02035260 copied at f12
(lvl 2, reward 0x4064); window struct eS20364C0 filled at f13 (+8 lvl 2, +0x14 reward 0x4064,
+0x1c time 1760); rank byte eS20364C0+0x0e = **0** every frame (T112's watch-write frame 13,
old 0→new 0).

## Step 3 — traces (4 recordings)

- `result` 40 frames: **rank 40/40, zenny 40/40, FIRST DIVERGENCE none** (sequencer, mm_*,
  rng_cadence also all-match). Same as T112.
- `battle_full` 540 compared frames: **rank 540/540**; **zenny first divergence k=406 (canon
  frame 417) canon=100 rust=0, 113/540 frames** — T112's numbers exactly. FIRST DIVERGENCE
  overall is enemy_state_action k=17 (known recon-scenario divergence, outside this pair).
- **k=406 settled — value byte-equal at window-up, mechanism named.** Both sides' zenny value
  sets are {0,100}; each side flips exactly once, 0→100, at its own results-window-up row:
  canon table row 417 (=capture frame 428, canon_ref 11), our rust table row 519 — the same
  one-row event shape (results-level + zenny appear together, mm_state_action (4,8) already
  up). From each side's window-up the stored word is **100 on both sides** (rank 0 on both).
  The 113-frame divergence window is the capture-timeline offset between the two windows'
  openings, owned by the battle itself: the sequencer enters the result phase 0xc at canon
  row 316 vs our row 405 (89 capture frames later) because the scripted battle diverges from
  k=17 (enemy_state_action, 458/540 frames — pre-existing, separately ticketed on this
  timeline). The F33d export-counter stall contributes only the 3-frame pairing drift the
  diff NOTE reports (6 repeated/21 jumped rows, counter 536 vs 539) — T112 attributed the
  whole 113 to the stall; this re-measurement splits it: 3 frames pairing drift (F33d),
  ~110 frames the scenario's own battle-timeline offset. NOT a value mismatch.
- Level (INFO, unjudged): our window exports results_level **8** at window-up where canon's
  reads **2** — T112's INFO note confirmed on the merged tree. Outside the ticket's pair.

## Step 4 — guard set + result + cursor after (identical command)

All ten rows **byte-identical to step 0** (same totals AND same negative totals):
opening 0/0/40/86591, mettaur 0/0/70/41734, cannon 0/0/40/9505, buster 0/0/28/3167,
chip-use 0/0/30/7768, wave 0/0/90/3840, windowclose 0/0/40/207166, popup 0/0/80/1288,
**result 0/0/40/111839**, **cursor 1/1/170/186279** — T118's cursor class SURVIVED the
footprint change; **no pad re-fit** (fitted stays 17; derived 434→436, +2 from the port;
peeked 146).

## Step 5 — hygiene

- Fitted 17 → 17 (unchanged), derived 434 → 436, peeked 146 → 146.
- No rank or zenny value is a bare literal: rank is `battle_rank()`'s computed
  BATTLE_RANK_NO_RECORD (const, derived cite); zenny is the sub_802C54C decode of
  `reward_word` or, where the roll is unported, the named consts RESULTMATCH_ZENNY=100
  (peeked) / descriptor +44 on fixture rows that already compared it (the `result` row).
- Path of each traced value: `result` row zenny = descriptor +44=100 (fixture path kept
  byte-for-byte by the port's FIXTURE_UNSET guard; the row compared this value before the
  ticket — the Rules' carve-out); `battle_full` zenny = real-play site's
  `battle_zenny().unwrap_or(RESULTMATCH_ZENNY)`. The decode itself (sub_802C54C structure)
  is ported and cited; the ROLL that fills reward_word is unported and named
  (sub_802C8FA→sub_80AA8E0/sub_80AAC8C), so the decode returns None in this build.
