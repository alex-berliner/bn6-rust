# T140 — attribute the 1389 px of `super_armor` frame by frame, then take the row to 0 so wt/t138's port can land

Branch: `wt/t138` (continued from T138's tip 9329017). Note: `git merge --ff-only main` was
impossible (branch has 355c77f/17daf22/9329017, main moved to 12d63a3) — merged main into the
branch instead (merge commit 8541893), keeping T138's commits.

## Step 1 (DONE): the row reproduces from the clean checkout of the tip

`python3 tools/harness.py --only super_armor --no-gallery` at 8541893 (tip = merge of main 12d63a3):

```
super_armor isolated    FAILED                           total 1389     worst 463    frames 70   | negative: not blind (total 30566)
fitted constants: 17 (derived 437, peeked 146)
```

**Byte-identical to T138's line: 1389/463/70 | neg 30566.** Step 1 reproduction confirmed.

## Step 2 (in progress): per-frame attribution with captures KEPT

Mechanism found for keeping captures: the harness has NO `--keep` flag (checked `--help` and the
argparse block; the only capture flag is `--no-gallery`). T137's route (docs/worklog/T137.md:60)
was to drive `tools/harness.py`'s own `Side.do_capture` from a small script and write frames to a
kept dir (`/tmp/t137_kept`). I will do the same for the `super_armor` row's exact Sides.

Captures kept at `/tmp/t140_kept/{canon,rust}/frame.#####.rgb`.

TRAP already known (T137): mgba's 4th byte per px is per-capture ALPHA — diff BGR only (bytes 0..2).

(continued below as measured)

### Step 2 result (DONE): the 1389 px are NOT spray residue -- they are the hit visual itself

Kept-capture run (`/tmp/t140_keep.py`, driving `harness`'s own `Side.do_capture`, serial): rust 283
frames, canon 175 frames, both kept under `/tmp/t140_kept/{rust,canon}` with the watch file. Marker
origin 8; alignment search range(153,174) reproduced the row's own choice:

- offset 165 total **1389** (sharp single minimum: neighbours 164:30566, 166:30103, everything else
  50k-114k) -- the row's own found offset, byte-identical to the harness's 1389/463/70.
- negative (frame+1 at the found offset): 30566 -- byte-identical to the harness line.

Per-frame table (the ONLY differing frames; every other k in 0..69 reads 0):

| k | canon frame | rust frame | px | region | band |
|---|---|---|---|---|---|
| 15 | 115 | 188 | 463 | x[43..77] y[70..113] (player sprite box) | **flinch window 114..136** |
| 16 | 116 | 189 | 463 | same box | **flinch window 114..136** |
| 17 | 117 | 190 | 463 | same box | **flinch window 114..136** |

463 x 3 = 1389 exactly; the spray band (canon ~140..162) contributes ZERO. T138's spray-residue
explanation is REFUTED; the flinch window is NOT clean.

What the pixels are (box histograms, kept frames):

- canon 114/117/118 (and rust 187/191): player idle palette (dark blues 8,57,123 x125,
  16,33,107 x114, 16,16,16 x99, 24,198,255 x79, 8,99,189 x72) + the Mettaur wave sprite (light
  blues 173,222,239 x86 / 222,239,247 x85 / 198,231,247 x74 / 132,198,231 x72 / 156,214,239 x33)
  + the wave's white core (247,255,255 x128).
- canon 115/116 (armored AND unpoked /tmp/t138_ctl, byte-identical 0 px): the PLAYER contributes
  ZERO pixels -- only the wave sprite (same light-blue counts) + its white core. Canon HIDES the
  player for exactly 2 frames, then back on the idle palette at 117.
- rust 188/189/190: wave sprite + white 128 + **591 white** -- the player drawn as a WHITE
  silhouette (idle shape + the flinch pose's extra 175 px), 3 frames.

Armored canon 115/116 is byte-identical to the UNPOKED flinch 115/116 (0 px) and to /tmp/t138_poke
115/116; they diverge at 117 (743 px -- the flinch pose vs the armored idle), which is the armor
gate doing its job. The hide at 115/116 is common to every hit (armored or not).

### Step 3 (DONE): the port WAS missing something -- the fitted white flash, not a second gate

The armor gate itself is correct (no flinch action on either side, verified again by the 117
divergence). The defect: `take_damage` unconditionally did `flash = FLASH_FRAMES (=4, provenance
fitted -- "a stand-in chosen to look right", actor.rs:267) + player.set_white(true)`, painting the
player white for 3 frames where canon blinks it INVISIBLE for 2. Canon's hit path does call
sprite_forceWhitePalette (sub_801B394, asm00_2.s:24062-24067) but the forced white never renders on
this route (idle palette at 114 and 117, sprite hidden 115/116) -- the mercy blink
(hidden iff (invulnerable>>1)&1, asm00_2.s:16796-16806) carries the visual. With mercy 0x78
(sub_801A5EE, asm00_2.s:22261) the blink reads hidden/hidden/shown/shown at 115/116/117/118 --
exactly canon's measured pattern (and our blink was already byte-exact from 118 on, under the
white's shadow it could never engage earlier).

Fix: removed `FLASH_FRAMES`, the `flash` field, its countdown, and the white from `take_damage`;
the blink gate loses its `flash == 0` condition. No new constant. Fitted count 17 -> 16.

Re-run, identical command:

```
super_armor isolated    PASS                             total 0        worst 0      frames 70   | negative: not blind (total 29640)
fitted constants: 16 (derived 437, peeked 146)
```

**0/0/70. The row went to 0 with the window UNCHANGED (canon_ref 100, search 153..174, offset 165).**
Negative 29640 non-blind (canon shifted +1 at offset 165).

## Step 4 (DONE): the scenario state -- one rejected build, then byte-identical

`tools/states.py` gains `paused_superarmor`: rom=STERILE, base=PAUSED, script=Start@10,
`poke_at=("40:0x0203852e:0x0202",)`, frames=41 (lever fires during frame 40, state saved after it;
loading reproduces the old run's frames 41 onward, the documented off-by-one). The row's canon side
now loads it -- no per-run poke; the two sides differ only in how the flag arrives (rust's fixture
byte at 0x020000c0 vs canon's CollisionData baked in the state), and canon_ref moved 100 -> 59 with
the EVENT window identical (flinch hit old 114 = new 73, blink 74/75, idle 76).

REJECTED FIRST BUILD (kept for the next worker): the recipe initially lacked the row's per-frame
ALIVE cheats (0x0203ab84/86:0xffff, holding the Mettaur's HP), so the baked state's enemy behaved
differently and the row read **FAILED 2296/177/70**. Attribution from kept frames
(/tmp/t140_kept/canon2, new-state capture vs the old kept canon shifted 41): the divergence was
confined to the enemy base strip x[168..199] y[111..120], ~75 px/frame, intermittent (0 at new 100
and 130), NOT a phase shift -- no whole-frame shift in 40/41/42 repaired it (159 px at new 60 against
all three). With `cheats=ALIVE` added to the recipe and rebuilt:

```
super_armor isolated    PASS                             total 0        worst 0      frames 70   | negative: not blind (total 29640)
```

**byte-identical to the poke route's line** -- same total, same negative. The scenario is the row's
canon side now; the poke tuple is gone from harness.py (SUPERARMOR_LEVER keeps the lever string for
the record).

## Step 5 (DONE): guard set, ROM identity, statuses.md -- cursor MOVED, branch stays unmerged for the merge decision

Behavior negative (lever flip, rust fixture byte absent vs armored canon, 1 kept capture
/tmp/t140_kept/rust_unarmored): total 7900 px over 25 nonzero frames, worst 743, non-blind -- with
the expected flinch signal (k=30..34 = canon 89..93: 614 px at the player box x[40..77] y[66..113],
rust showing the flinch/idle-color split) BUT an UNATTRIBUTED pre-hit divergence: k=0..14 (canon
59..73) differ ~149 px at the enemy strip x[169..199] y[111..120] with rust drawing white
(247,255,247) where the armored row read 0 against the same canon frames. NOT resolved within
budget -- leading suspicions, for the next worker: (a) the kept armored rust capture is the PRE-fix
binary (white flash still in), so the pair is not the landed row's pair; (b) the absent fixture byte
reads power-on EWRAM (agb crt0 zeroing not verified), so "unarmored" may not be unarmored; (c) the
cannon the PAUSED state carries fires early and the enemy-strip white is its hit/muzzle on ONE side.
The landed negative is therefore the harness's own frame shift (29640, non-blind). The lever-flip
negative must NOT be cited as landed until (a)-(c) are settled.

Guard set (`--only mettaur,result,cursor,wave,chip-use,tiles,gauge,window,windowclose,opening,popup
--no-gallery`), vs main's line:

```
mettaur 0/0/70 neg 41734 (identical) | result 0/0/40 neg 111839 (identical)
wave 0/0/90 neg 3840 | chip-use isolated 0/0/30 neg 7768 (integrated FAILED 275307 = its documented allowed failure)
tiles 0/0/8 neg 12197, integrated 0/0/8 neg 12843 | gauge same | window 0/0/16 neg 81056
windowclose 0/0/40 neg 207166 | opening isolated 0/0/40 neg 86591 (integrated FAILED 18740/647/40 -- main's opening-integrated line not re-measured this session, budget)
popup 0/0/80 neg 1288
```

**CURSOR MOVED: 26/25/170, negative 186276 (main: 1/1/170, neg 186279).** My white-flash removal
shifted the binary layout -- exactly the tear the coordinator warned about. Per instruction: NOT
fixed (src/main.rs's pad untouched), the number is reported, and the branch is left UNMERGED for the
coordinator's merge decision.

ROM: sha256 760f2029654d11f6670a72bb15940029db1690bbda8ca09e31f3748dac2d2d1c, 593352 B
(T138's pre-fix build: 8ceb99ec..., 593272 B -> delta +80 B). The row's own landed line, final:

```
super_armor isolated    PASS                             total 0        worst 0      frames 70   | negative: not blind (total 29640)
fitted constants: 16 (derived 437, peeked 146)
```

`docs/coverage/statuses.md` written fresh with the gate as landed (0x220000 pair, ANGER unported,
damage unconditional, hit visual = mercy blink).

## Unverified (said plainly)

- ANGER (bit 21 of the 0x220000 mask): unported, unmeasured; nothing sets it on this route.
- The Transformation route (SetObjectSuperArmorFlag family, asm00_2.s:12396-12671): untouched.
- Whether `sub_80139F6`'s AIData+0xa4 path (asm00_2.s:10739-10846) ever re-arms the stat copy
  mid-battle: unread; if it fires, canon re-copies the stat where our port copies once at setup.
- Why canon's forced white palette never renders (what clears FinalPalette under the blink):
  untraced; the observable is what is ported.
- The lever-flip behavior negative's pre-hit enemy-strip divergence (above): unattributed.

## Would I call it land-ready?

The row is 0/0/70 with the guard table clean EXCEPT cursor's tear moved 1/1/170 -> 26/25/170. That
is the coordinator's merge call, not mine: either the tear is accepted as this run's price for the
first live NaviCust rule (the whole wt/t138 branch lands), or a pad fix is ticketed separately. I
want the former; the port is real, the row is event-paired, the scenario makes it re-runnable, and
the negative is non-blind.
