# T114 — buster_charge 2498/188/32 → 0: the y158 gauge attribution is REFUTED

Branch `wt/t114-charge-gauge` (worktree /tmp/bnwt/t114-charge-gauge, base 261ee30).
**No src change landed: the ticket's premise did not survive measurement.** This log is the
ticket's "bounded NEGATIVE" branch — attribution + seed table + the unported mechanism, so the
next ticket can land the fix with the right files in scope.

## Baseline (before anything)

`python3 tools/harness.py --only buster_charge --no-gallery`:

```
buster_charge isolated    FAILED  total 2498  worst 188  frames 32 | negative: not blind (total 5113)
buster_charge integrated  FAILED  total 170998 worst 6315 frames 32 | negative: not blind (total 172378)
fitted constants: 16 (derived 433, peeked 146)
```

Alignment reproduced by hand (harness `run()`, same inputs): rust origin 8, offset 207,
canon_ref 130 → canon frame `130+k` vs rust capture frame `215+k`. Counts:
`[188, 0,0,0,0,0, 92,96,96,93,93, 92×20, 0]` = 2498. So the row has exactly two residues:
a k=0 transient (188 px) and a steady-state ~92 px/frame from k=6 to k=30, gone at k=31.

## Step 1 — what actually differs (measured, not the ticket's story)

**The gauge strip renders NOWHERE in this row.** The isolated variant passes `--disable-bg`
to both sides, which sets `disableBG[0..3]` on mGBA's renderer (tools/mgba_capture.c:380-397):
every BG layer off, OBJ only. The CUSTOM gauge lives on the shared BG3 (`battle.rs` `hud_bg`,
`src/hudtiles.rs::set_gauge`). Measured: **zero differing pixels at y≥140 in all 32 frames**
(scan of every frame pair). The ticket's "22 px dithered sliver on y158, canon (12,24,8)/(16,5,29)
vs our red-only" does not exist on this baseline; no `hudtiles.rs` change — palette, BAR tag,
not-full arm, exporter bytes — can move a single pixel of this row.

**The steady residue is one missing OBJ.** OAM watched every frame on both sides
(`--watch 0x07000000:1024`), plus OBJ VRAM (`--watch 0x06010000:0x8000`):

| frame k (canon f) | canon | ours |
|---|---|---|
| k=6 (136) … k=30 (160) | 16×8 OBJ, raw (72..74, 78), shape wide size 0, tile 0x20/0x21 pal 2 → 0x27/0x28 pal 2, pri 2 — **present** | **absent** (our OAM i=1..5 = canon's i=2..6 exactly; nothing at (73,78)) |
| all k | navi parts (49,70 32×32 t9; 43,96 16×16 t25; 66,102 16×8; 42,100 32×16), bolt 32×16 at (91,75) from f139, result mark (37,21) 16×16 t0x200 pal 11 | identical, 0 px of residue |

The diff patch (73..87, 79..85), ~92 px/frame: canon's OBJ draws bright cyan/white
(RGB15 (3,24,31)/(0,16,27)/(1,12,23)/(28,28,28)) over the barrel area; our side shows the
32×32 navi body part's own dark-navy pixels ((2,4,13)/(4,6,16)) and black where that part is
transparent. Same x-range every frame → static overlay, not the bolt (our bolt is at the same
(91,75) as canon's, pixel-identical).

**Identity and lifecycle of canon's OBJ** (canon frames, OAM watch):
- f136: 16×8 @ (74,78), tile 0x1f **pal 1**, tile 0x1f carries art this frame only
- f137–138: 16×8 @ (72,78), tiles 0x20+0x21 pal 2 (art A)
- f139–142: 16×8 @ (73/74,78), tiles 0x27+0x28 pal 2 (art B), and tile 0x21 flips to art A' at f141
- f143–160: 16×8 @ (74,78), tiles 0x20+0x21 pal 2, art stable ("50444453 37222244…" / "4454000 033131100…")
- gone at f161 (the attack state exits at 160). 25 frames = the tail of the 30-frame charged attack.

The art tiles are **not** in `assets/buster_fx.bin` (checked: none of the 0x20/0x21/0x27/0x28
tile byte strings appear in it) — a new export from canon OBJ VRAM is required.
The 8×8 at (96,78) in canon's OAM (the plain-buster muzzle fixture) holds blank tile 0x1f from
f137–160 → draws nothing → costs 0 px, consistent with the buster row's own note.

**The k=0 transient (188 px, bbox y58–121 x33–94 — the navi region, release frame only):**
canon129-vs-rust215 reads 436, canon130-vs-rust215 reads 188, canon130-vs-rust216 reads 188 —
so the steady alignment at offset 207 is optimal and the k=0 diff is the *documented* one-frame
state-watch ambiguity of this row (harness buster_charge note, T106: canon writes CurAction
0x10 ON the released frame 130 while our export block flips one frame late). 1 frame, main.rs/
oracle territory, unchanged from baseline.

## Step 2 — seed sweep (bounded negative, both knobs)

Rust captures only (canon capture reused), same alignment/scoring:

| seed | total | worst |
|---|---|---|
| baseline (gauge 0 / gauge_tick unset) | 2498 | 188 |
| `gauge=1` (not-full strip, INTERIOR arm) | 2498 | 188 |
| `gauge_tick=58` (bar step +1, marker phase flipped) | 2498 | 188 |

**Phase refuted and then some:** the residue is invariant under both gauge knobs because the
strip never renders in this variant. Not fixture phase — a mechanism, on the OBJ layer.

## Step 3 — what the fix actually needs (NOT landed, files out of scope)

Canon's charged shot spawns a muzzle/flash temp object that our charged arm never does:
`src/battle.rs:3576` `Update::Strike { charged }` pushes the bolt and skips the fx spawn (the
`BUSTER_FX` spawn lives in the plain-only `else`). The fix = (a) export the charged flash cells
(16×8, OBJ VRAM tiles 0x20/0x21 and 0x27/0x28, the pal-1 f136 cell, and the OBJ palette
entries it uses — all canon bytes, e.g. tools/spr_export.py-style) and (b) spawn it in
`battle.rs`'s charged arm from the pose's ~6th frame to the attack's exit. `battle.rs` is
forbidden here (ticket "NOT" list; coordinator note) and `charge_shot.rs` stays undeclared
until T111's pad — so this stops here by rule, not by choice.

Canon-side call site not chased to its `bl`: the dispatch cell for this state's BPwrAtk 0x01 is
`sub_8011CB4` (cell 0x11, `ChargeShotHandlersByTransformation_80117D4`, asm00_2.s:5827, body
asm00_2.s:6339-6360) which only seeds the attack vars; the flash spawn will be in the charged
attack's per-frame arm, same family as the plain buster's `sub_800FAAC → sub_80C4FFE`
(asm00_2.s:1903-1916). Chase it with `python3 tools/csrc.py sub_8011CB4` and the pose timer
when picking this up.

## What would move this row to 0

1. Charged muzzle fx (≈92 px/frame × 25 frames, the steady residue) — battle.rs + new asset export.
2. The k=0 one-frame release skew (188 px once) — the row's own documented export-block question, main.rs/oracle.

## Ideas tried and dropped

- Believing the ticket's y158/gauge attribution: refuted by direct scan (no diffs y≥140) and by
  the sweep above. No hudtiles edit made — any such edit would be cosmetic here and could only
  add fitted constants.
- Retiring `BAR`'s `fitted -- NOT VERIFIED` tag: **left in place.** This row never shows the
  gauge strip (BG disabled in isolated; integrated's gauge pixels are dominated by the window
  and were never part of the row semantics), so nothing measured here can verify BAR. The tag
  stands; a `tiles`/`gauge` row pass is the place to retire it.

## Next worker

Take the OBJ table above as the spec; the art is sitting in the OBJ VRAM watch files
(/tmp/t114_canon_objvram.bin, frames 136–160) but re-dump them — /tmp is not durable. Get
battle.rs + the exporter into scope (pair with T111's pad, which already touches the cursor
footprint this row shares).
