# T29 — scroll pair record repair + the 4800-band attribution

**Outcome: comment/citation-only landing.** No `src/` change, no value change:
ROM byte-identical (`1997be3b…`, 583852 B), all four field lines unchanged by
construction. Superseded the brief's original premises after supervisor
correction: the mirror address claim (0x02000008/0x0200000C) was FALSE — the
landed `0x020000B2/B6` (= TRC2 block at 0x02000080 + 50/+54, src/battle.rs
trace table) is correct and was kept; the "(12,16) px displacement" was never
measured and was dropped.

## What I measured (this session)

**Baseline (main c3d3b95, 4 harness row runs):** field isolated `0/0/40/1139`,
field integrated `32623/5806` (neg 173631), field-bg1 `4800/4800` (neg
175545), field-bg2 `4800`, field-bg3 `380263/24906`; ROM sha256
`1997be3b4e8f463ac328aede2d5a027d7f73fcb8adcab5834592d71a7a419fed`, 583852 B.
All match the brief's expects.

**The solve (A) — re-derived, not trusted.** Exhaustive solve over all 1024
quarter-values against `/tmp/bn-f47/canon_scrollcnt.bin` (canon caps 135..174)
+ `/tmp/bn-t25-watch/{rust_xq,rust_yq}.bin`, model = landed T25 model (plain
`>>4` register, visible register at rust cap 121+k = mirror at 120+k, equality
mod the 256-px content period):
- k=1..39 solution sets: **H = {471, 472}** (2 of 1024), **V = {748}** (1 of
  1024). k=39 spot-checked = 0 for both.
- k=0-only sets: **H = {473..476}**, V = {747..750}; H's intersection with
  k=1..39 is **empty** → k=0 is not seed-fixable.
- At the old 466/745 the model predicts register-residual series H [2,1]
  (period 2), V [1,0,1,1] (period 4) — exactly the negation of F47's measured
  best-shift cycle [(-2,-1),(-1,0),(-2,-1),(-1,-1)]. The old comment's
  "466/745 predicts exactly the measured cycle" is confirmed.
- F33b's closed form (`scroll_xq=(2f-2(R0-7))%1024`) reproduces 466/745 but
  reaches 471/748 for **no whole-frame lag** (+5/+3 quarters is a ceil-edge
  phase correction, not a lag).

**The convention conflict (B) — measured.** Under the asm cite's own
`(counter-8)>>4` / `(counter-4)>>4` convention (asm00_0.s:3287-3303), the
solve's sets move to **H {473,474}, V {749}**. The landed mechanism is fitted
under the plain `>>4` convention; the -8/-4 reading is settled empirically by
the capture, not derived from the asm.

**The 4800 band (D) — decisive, 3 harness-row-equivalent capture pairs via
`harness.run()` itself (harness-identical alignment, dirs kept under
/tmp/bn-f5e1c735/):**
- field-bg1 k=0: **4800 px, rows 0..19 × cols 0..239**; k=1..39 all 0.
- field-bg2 k=0: **4800 px, rows 0..19 × cols 0..239**; k=1..39 all 0.
- → **The same band.** bg1's and bg2's 4800 are ONE shared k=0 region on two
  layer pairs — one shared F45-transition artifact, not a scroll defect, not a
  coincidence. Confirms the verifier's content-not-phase verdict (its
  translation sweep minimum at (0,0)=4800, next best 10362 at (0,+1)).

**Step 5 — what field integrated's 32623 is (per-frame, offset 108):**
- k=0..2: 5806+5319+4698 = **15823 px**, full-height, cols 0..227 — the
  F45 pre-boundary transition/stall frames.
- k=5: **945 px, rows 0..19** — the same top band the bg1/bg2 rows isolate
  (partially occluded here by correct content on other layers).
- k=24..39: **15855 px**, rows 49..154, left-anchored band growing cols
  0..15→0..190 — canon's own RESULT-window slide-in (canon ~154..167) vs our
  `shown` layer; the bg3 row's note predicts exactly this shown-vs-HUD
  mismatch. Stabilizes at 1190 for k=37..39.
- k=3..4 and k=6..23: **0**.
- The (12,16)/257-px counter question cannot reach ANY of this: the frames
  where the scroll ramp is actually compared (k=6..23) read 0, and bg1's own
  isolated residue is its k=0 band only, which the translation sweep pins as
  content at (0,0). The displacement is invisible in every judged frame —
  consistent with any constant ≡ small mod the 256-px period.

## Tried and dropped
- Re-deriving the original brief's "(12,16) px, mirror at 0x02000008/0x0200000C"
  claim: 0x02000008..0x02000030 is the ORCL block (src/main.rs `ORACLE_OFFSET`,
  magic+frame), not x_q/y_q — the address claim is impossible against our own
  source; supervisor confirmed the brief was wrong, not the source.
- Register-space mod-512 equality solve: zero solutions at any (lag, offset) —
  the correct comparison is mod the 256-px content period (the map repeats),
  which is what the landed comment already said.
- Kept-dump pixel re-analysis of /tmp/bn-t25-bg1 at naive index offsets:
  full-screen diffs (those dirs lack marker watches; --only-bg rendering also
  blanks pre-boundary rust frames). Dropped in favour of `harness.run()`
  itself, which guarantees the row's own event-locked alignment.

## Standing limitations noted
- `provenance_counts()` (harness.py:3219-3233) scans `src/*.rs` only: "fitted
  19" carries ZERO information about literals in tools/harness.py — it would
  have stayed 19 even if 471/748 had been pixel-fitted. The assurance for the
  pair is the exhaustive-solve solution sets, not the census.
- tools/trace.py still TypeErrors at :56 on main HEAD (since 0701f09); used
  the /tmp/bn-t26/run_trace.py shim pattern only if needed (not needed here).

## Next
- The shared 240×20 k=0 band is fully attributed as F45 transition content
  shared across bg1/bg2; if a later ticket wants the 15823 (k=0..2 transition)
  and 15855 (RESULT window) groups broken down further, the kept captures are
  under /tmp/bn-f5e1c735/h_{rust,canon}_{df9368f9eb,d03beff8fb,90de86ef9f}.
