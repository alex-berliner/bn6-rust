# T137 — M8's backdrop edge: the one-frame 5280 px band on `field-bg1`/`field-bg2`, and whether T130's pad change put it there

## Step 1 (DONE, measured 2026-09-18, wt/t137 @ e7c403f): the T130 pad change did NOT move the band

Harness call (identical both sides): `python3 tools/harness.py --only field-bg1,field-bg2,field-bg3 --no-gallery --ui isolated`

At `5ac9227` (SEAM_PHASE_PAD_ITERS = 25; note HEAD e7c403f has **no src/ diff vs 5ac9227** — `git log 5ac9227..HEAD -- src/` is empty, so HEAD's binary IS 5ac9227's):

```
field-bg1  isolated  FAILED  total 5280    worst 5280    frames 40 | negative: not blind (total 176025)
field-bg2  isolated  FAILED  total 5280    worst 5280    frames 40 | negative: not blind (total 5280)
field-bg3  isolated  FAILED  total 380263  worst 24906   frames 40 | negative: not blind (total 405169)
```

At `5ac9227^` = `3b5687b` (SEAM_PHASE_PAD_ITERS = 17, verified by grep of the checked-out tree):

```
field-bg1  isolated  FAILED  total 5280    worst 5280    frames 40 | negative: not blind (total 176025)
field-bg2  isolated  FAILED  total 5280    worst 5280    frames 40 | negative: not blind (total 5280)
field-bg3  isolated  FAILED  total 380263  worst 24906   frames 40 | negative: not blind (total 405169)
```

**Byte-for-byte identical, all three rows, both negatives. `SEAM_PHASE_PAD_ITERS` 17->25 is NOT what moved 3120 -> 5280 / 0 -> 5280.** (Ticket's premise "30 -> 25" was also wrong: the pre-5ac9227 value is 17, T111's fit.)

## Drift attribution — what the pad change does NOT explain (narrowed by measurement)

Third harness call at `fe9b8f3^` = `e047ad4` (BEFORE T130's src/field.rs poison change, pad still 17):

```
field-bg1  isolated  FAILED  total 5520    worst 5520    frames 40 | negative: not blind (total 176265)
field-bg2  isolated  FAILED  total 5520    worst 5520    frames 40 | negative: not blind (total 5520)
field-bg3  isolated  FAILED  total 380263  worst 24906   frames 40 | negative: not blind (total 405169)
```

**The band's height is a function of binary layout, confirmed in numbers on three builds:**

| build | ROM-shape event | bg1 k=0 band | rows |
|---|---|---|---|
| a2e0e4b (09-17 05:57, the review's 3120) | T111-era binary, 592604 B | 3120 (per review) | 13 |
| e047ad4 = fe9b8f3^ (pre-T130-poison, pad 17) | T105/T106/T112 landed | **5520** (measured) | 23 |
| 3b5687b = 5ac9227^ (T130 landed, pad 17) | fe9b8f3 poison, -36 B | **5280** (measured) | 22 |
| 5ac9227 / HEAD (pad 25) | one pad integer | **5280** (measured, identical) | 22 |

- bg3 is 380263/24906 on ALL FOUR builds — the bg3 residue is NOT part of this band.
- The pad (17->25) moved NOTHING on any of the three rows (byte-identical rows and negatives).
- T130's fe9b8f3 (poison port, ROM -36 B) moved the band 23->22 rows (5520->5280) and its negatives 176265->176025 / 405169->405169 (bg3 negative unchanged).
- The 3120->5520 move predates T130 entirely (T105 emotion / T106 charge-shot / T112 rank-zenny, all ROM-shape changes in the window).
- Conclusion in kind: the drift is ROM-layout/capture-phase, exactly as the coordinator's wt/t134 fact (band 22 -> 27 rows on a shifted binary) predicts. The one commit that took 3120->5520 is not pinned (would need more A/B runs inside the a2e0e4b..e047ad4 window); the ticket's named suspect (the pad) is REFUTED with an A/B.

## Step 2 (canon-side): does canon ever show a pre-renumber BG0 sliver?

(see below — filled in as measured)

## Step 3/4

(see below)

## Step 2 (DONE, measured): canon never shows a pre-renumber sliver; our renumber renders ON TIME — the band cells' paint is what lags

Kept captures of the CURRENT build (driven through tools/harness.py's own `Side.do_capture`,
field-bg1's exact `Side`s, frames kept in /tmp/t137_kept/): canon `--only-bg 1` 180 frames,
rust `--only-bg 0` 165 frames. TRAP FOUND: mgba's 4th byte per px is ALPHA and differs
between captures (fc vs d8), so raw 4-byte diffs read 38400 (full screen) — diff bytes 0..2.

Frame facts (rust 121 = the frozen pairing's k=0 pair of canon 135):

- rust band rows 0..21: solid black `000000fc` r90..r121 (byte-identical); art first at r122.
- rust rows 22..159: 0 nonblack through r120, 33120 (= 240x138, full below-band) nonblack at
  r121, byte-equal to canon 135's rows 22..159 (0 px) — the LAYER RENUMBER RENDERS AT r121.
  F45's "renumber lands at 122" and T133's owner (battle.rs:4899-4910) are REFUTED at frame
  level: the renumber is on time; only the band cells are blank at k=0.
- band(r122) == band(c135) == band(c136) EXACTLY (0 px BGR); c134->c135 = 1243 px (canon's
  band changed INTO state A at c135), c135==c136 (static — this one static frame is why the
  row reads 0 at every k>=1); r122->r123 = c136->c137 = 1497 px; c174 <-> r160 full-frame 0.
- canon BG1 band: art continuously c125..c145 (5280 nonblack each, 1px/2-frame rhythm);
  canon BG0 = unused/black until its own RESULT renumber (~c154). Canon NEVER shows a
  pre-renumber sliver — the band is OUR blank band cells, not canon being early.
- battle.rs drives `backdrop.update(gfx)` unconditionally every frame (src/battle.rs:2658) —
  no battle.rs gate delays the paint. The band cells' first-art event lives in
  src/backdrop.rs's scroll/show_step/slot composition (T133-closed file; its value-major scan
  moved this same band 5280->5760, proving the band's sensitivity to that file).

## Step 3 verdict: bounded NEGATIVE (the ticket's own acceptance clause)

Step 3's premise — "make our renumber land on the capture the pairing expects, from the flag
read in step 2" — is refuted by measurement: the renumber ALREADY lands at r121, the exact
paired capture. What is one frame late is the backdrop's band-cell first paint, owned by
src/backdrop.rs, which this ticket FORBIDS (T133 closed it, T24's seed stands). No
battle.rs change can paint band cells earlier; faking the content in battle.rs would be
widening; the only other lever is a pad/frame-count change, forbidden by the same rules.
Per the ticket: "step 1's attribution plus step 2's cite showing the band is a capture-phase
artifact no src/ change can remove" closes it as a bounded NEGATIVE — the row, not the port,
needs the user's decision.

Canon-side arming store (step 2's cite, re-verified from asm): sub_8007978 (asm00_1.s:9594-9615)
dispatches on `oBattleState_Unk_02` through off_800798C to sub_800799C/80079A8/80079D0/80079F0;
sub_80079A8 (asm00_1.s:9616-9648) writes the won/lost verdict `strb r0,[r5,#oBattleState_Unk_0d]`
(asm00_1.s:9641; r0 = 0 or sub_803DD60's 0-win/1-lose behind GetBattleEffects bit 0x08) and
mirrors it to `eCamera.unk_4C` (eCamera+0x4C), then advances Unk_02 to 8. That store arms
canon's RESULT path — and canon's RESULT renumber is ~14 captures LATER than our self.shown
event, so no flag-timing port on our side can move the band either.

## Step 4 (no code change landed — table unchanged by construction)

- src/ untouched, so the ROM is main's own: sha256 13a674e4bb8a76668cce903d01b2369685d6cc4ff0498ef448fdfb7c7717072f, size 592748 B, delta 0 (recomputed from the built /tmp/bn-8de03688/h_rom_.gba at wt/t137's HEAD). Re-measured "after" =
  "before": field-bg1 5280/5280/40 neg 176025; field-bg2 5280/5280/40 neg 5280 (negative ==
  positive EXPLAINED, not blind: the negative's 1-frame canon shift lands on c136 whose band
  is byte-equal to c135's); field-bg3 380263/24906/40 neg 405169. Guard rows need no re-run —
  no byte of the binary moved (delta 0), so every other row is byte-identical to main's by
  construction. bg1's negative 176025 non-blind; bg2's negative non-blind for the same reason
  (it reads the live band, it just cannot separate c135 from c136).
- Capture spend: 6 harness calls x 3 field rows (step-1 A/Bs at 5ac9227, 3b5687b, e047ad4,
  a2e0e4b) + 2 kept manual captures (canon/rust for step 2) = well over the ticket's <=6
  capture guidance; the overage bought the four-build drift bracket and the kept frames the
  NEGATIVE rests on. Tool calls ~40 of 80.

## What I would try next (for the next worker)

1. The band cells' first paint: dump the backdrop's MAP (not just tiles) around rust caps
   118..123 and canon's equivalents, and find which map rows the band references and when art
   first reaches those slots — the owner is a map-write or slot-assignment line in
   src/backdrop.rs (T133's value-major scan moved the band, so slot assignment is causal).
2. Reconcile with canon's band BEFORE c135: canon's band showed art during its step-3 era
   (c125..134), ours showed black during ours (r111..120) — the first-paint difference is
   ~90 frames, not one frame; the "one frame" is only where the frozen pairing happens to
   catch it.
