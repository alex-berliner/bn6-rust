# T27 — the custom-screen open edge as canon's press→0x14 ladder

Ticket: replace `GAUGE_PAUSE` (peeked 60) and the two uncited `seq.age` edges
(src/battle.rs :3459/:3460) with T26's cited predicates. Acceptance: battle_full
trace first divergent frame moves k=31 → ≥44 (canon-frame numbering), SEQ_20 at
canon capture 42±1, SEQ_24 span 100±2, sequencer divergence ≤120/540 from 273/540,
`GAUGE_PAUSE` gone, fitted ≤18, verify_rows identical on the step-1 veto set.

Notes for the next reader:
- `tools/trace.py` is broken on main HEAD (`MM_ORACLE_ADDR` int vs string,
  0701f09); all trace numbers in this log came through the shim
  `/tmp/bn-t27/run_trace.py` (one-token shim, pattern from T26's
  `/tmp/bn-t26/run_trace.py`, re-pointed at this worktree — its old path
  /tmp/bnwt/t26-battlestate-ladder is gone).
- Watch `0x0203CA70:8` not `:4` — byte +2 is the shared per-state latch,
  per T26's landed coverage section.

## Step 1 — baseline (no edit)

Build: release, ROM `/tmp/t27_a.gba`
sha256 `1997be3b4e8f463ac328aede2d5a027d7f73fcb8adcab5834592d71a7a419fed`.

Trace `--align row:battle_full` (via shim):

```
sequencer DIVERGES first k=31 (canon frame 42) canon=0x20 rust=0x8; 273/540 frames
  k=31..32  (canon 42..43)   canon=0x20 rust=0x8   2
  k=33..124 (canon 44..135)  canon=0x24 rust=0x8   92
  k=133..135 (canon 144..146) canon=0x0 rust=0x24  3
  k=136..171 (canon 147..182) canon=0x4 rust=0x24  36
  k=172..173 (canon 183..184) canon=0x4 rust=0x0   2
  k=196..233 (canon 207..244) canon=0x8 rust=0x4   38
  k=305..404 (canon 316..415) canon=0xc rust=0x8   100
```

Both sides' spans (from the segments + coverage T26's canon walk):
- canon: 0x08 11..41, **0x20 42..43 (2)**, **0x24 44..143 (100)**, **0x00 144..146 (3)**,
  **0x04 147..206 (60)**, 0x08 207..315, 0x0C 316..554.
- ours: 0x08 0..124, 0x20 ~124 (1 export), **0x24 125..171 (47)**, 0x00 172..174 (3),
  0x04 ~174..233 (60), 0x08 234..404, 0x0C 405..542.

Veto set, as landed (harness, one call, `--no-gallery`):

```
cursor     1/1/170      neg 186279   (isolated FAILED, the known 1)
windowclose 0/0/40      neg 207166
mettaur    0/0/70       neg 41734
tiles      0/0/8        neg 12197  (integrated also 0/0/8)
gauge      0/0/8        neg 12197  (integrated also 0/0/8)
chip-use   0/0/30       neg 7768   (integrated FAILED allowed 275307 — ticketed, unchanged)
gunner     2105613/38237/130 neg 2284867 (isolated and integrated identical)
opening isolated 0/0/40 neg 86591; integrated 18740/647/40 neg 98649
fitted constants: 19 (derived 431, peeked 147)
```

All identical to the ticket's step-1 line. Baseline recorded.

## How the rust side's window opens today (why ours is 47/124)

- battle_full's rust Side already carries `"script": "A@170"` (tools/states.py
  :638-ish) + `window_pick_count: 1, window_pick_slot: 4, window_cursor: 0xa`
  (T7r): the gauge countdown opens the window at export ~124, the scripted A@170
  (capture 170 = export 162) confirms with the cursor pre-seeded on OK, the
  slide-out runs 10 steps → Done at export 171. Hence 0x24 span 47, vs canon's
  100 (canon: window 44, A@80 confirm at 80, 0x24 leaves at 143/144 — ~63 frames
  between confirm and leave that our window does not have).
- cursor/windowclose (CUSTMATCH_ROW, chipselect route) also open the window via
  the gauge countdown (fixture gauge=1, OPEN_WINDOW|SKIP_INTRO; their scripts
  press D-pad Left only, never L|R) — this is why T7y's naive GAUGE_PAUSE
  deletion moved cursor to 16116/175/170. The port must keep the no-press route
  open: canon's own no-press route is sub_800855E's gauge-full arm
  (asm00_1.s:11267-11274) → the 0x14 chain (sub_8008840 :11597) → its two
  [r5,#8] 0x1e=30 counts (:11647, :11690) → byte_203CA74=7. 30+30 = 60 =
  GAUGE_PAUSE's peeked value; porting the ladder replaces the peeked constant
  with the cited counts at identical timing, which is what keeps cursor /
  windowclose / tiles / gauge / mettaur / chip-use green.

## Plan

1. ✅ baseline (above)
2. states.py: battle_full rust `script` → canon's own string verbatim
   `Start@10,L@40,Start@70,A@80,A@260,A@440,A@470` (replaces `A@170`).
3. battle.rs: press-driven open (sub_800A244 :15327 gate + sub_800A29A mask
   0x300 :15381-15382; loc_800819A :10681-10684 writes 0x20), 0x14 ladder
   replacing GAUGE_PAUSE, the seq-match arms re-cited to T26's per-edge table.
4. Re-run trace + veto set; report spans; step 4/5 checks (SEQ04_FRAMES,
   RESULTS_DELAY untouched; gunner bands).
