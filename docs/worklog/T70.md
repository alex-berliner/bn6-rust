# T70 -- the Background byte's art mapping: close the backdrops GAP out of the ROM's own record stream (M1 -> M8)

## Baseline (step 1)

- `python3 tools/inventory.py` (at 175acb0 + T65 pass 3 already in):
  `formations (M8): ROM-WALK (T65): ... 1076 0xF0-terminated formation arrays ... mismatches 0 -- verified 0/1076`
  `backdrops (M8): DERIVED-FROM-RECORDS, byte->art CLOSED (T65 pass 3): ... -- verified 0/3`
  NOTE for the next worker: the ticket was written against pass-1+2 SCOPE (a GAP with
  "no table or arithmetic offset found"); T65 pass 3 (79da4ed) landed BEFORE this ticket
  ran and had already rewritten the prose headers to CLOSED -- but left the generated
  count at `0 / 3` (rows still `status: unrecorded`) and left the T15 GAP trail in the
  section note ("Still a GAP for a table", limit (ii) "mapping stays 0/3"). What was
  missing was verification + the count move, not the prose.
- ROM build (src/ untouched): `cargo build --release` emits only the ELF; the hashed ROM
  is `python3 tools/gbafix.py $CARGO_TARGET_DIR/thumbv4t-none-eabi/release/bn <out>.gba`
  -> sha256 `5b46337aa27da9ca881f2321fe1e210f54717285b4dfa5256c1680bac4b985ef`,
  584296 bytes. Identical after (gate met).

## Reader table (step 2)

Every reader of the Background byte and of `battleSettings_setBackground`'s argument:

| reader | file:line | what it does with 0xff vs 0x07 vs 0x08 |
|---|---|---|
| writer | asm/asm03_0.s:14592 `strb r0,[BattleSettings_200AF60+0x4]` | stores the argument |
| stage-pair source | asm/asm03_0.s:14615 `ldrb r0,[r6,#1]` -> :14616 bl setBackground (battleSettings_802D2B2) | argument = `byte_203CA50[2*(stage-1)+1]`; the stage-pair word is copied from `dword_203F568` by battle_copyStructsIncludingBattleStats_800b2d8 (asm/asm00_1.s:17871-17874, word copy) |
| 2nd caller | asm/asm33.s:16529 (sub_812B768) | passes a caller-supplied byte (story-battle path with getBattleSettingsFromList1) |
| init read | asm/asm21.s:474 `ldrb r0,[r1,#4]` (r1 = [r10->BattleState.BattleSettings]), :475 `cmp r0,#0xff`, :476 `bne locret` (passthrough) | non-0xff (incl. 0x07/0x08) returns unchanged; 0xff resolves: net `pt_808139C[group-0x80][map]` ldrb :489, weather override `0x15` :508, real-world `7` :516 |
| **art SELECT** | **asm/asm21.s:32 `ldr r0,[r6,r1]` = off_8080F98[r] -> bl LoadBGAnimData :33; :36 off_8081220[r] -> bl LoadGFXAnims :37** (r = sub_8081308's return, sub_8080DA0 called from initBattleStructsAndVram_80071D4 asm/asm00_1.s:8435) | a TABLE index, not arithmetic, not a compare chain |

byte_203CA50 readers in asm/: asm03_0.s:14609 (above) and asm00_1.s:17871 (the copier;
no other). The asm33.s:4168-4195 per-scene compare chain from T15's trail is a
DIFFERENT loader (CopyBackgroundTiles' callers, scene tile arrays) -- not this path.

## Art per byte (step 3, all read from reference/bn6f/bn6f.gba)

BGAnimData layout per LoadBGAnimData (asm/asm03_0.s:21209-21268): +0 gfx WRAPPER
{u32 size_words; u32 lz77_off; LZ77 blob at wrapper+lz77_off}, +4 dest 0x06000020,
+8 tilemap WRAPPER {u8 w; u8 h; LZ77 @+0xc}, +0xc tilemap dest offset,
+0x10 palette WRAPPER (data @+4; 0 = skip), +0x14 staging 0x03001960, +0x18 size 0x20.

- **0x07 and 0x08** (off_8080F98[7] = 0x08081098, [8] = 0x080810B4): SAME gfx wrapper
  0x08616598 (0x88 words = 544 B = 17 tiles, LZ77 @0x086165b0) and SAME tilemap
  0x08616634 (32x32); only the palette differs (0x08616760 vs 0x08616EC4) -- the
  Comps1/Comps2 map bg art in two palette variants. Naming link: the Comps maps' own
  BGAnimData structs off_806DBD4/off_806DBF0 = {gfx 0x08616598, dest 0x06008020,
  tmap 0x08616634, +0x1800} (same art at the overworld's VRAM spot), cited by
  reference/bn6f/maps/Comps1/loader.s:224-227; id 6's gfx 0x08610b04 (33 tiles) is
  cited by reference/bn6f/maps/RobotControlComp/loader.s:133.
- **0xff on the battlestart route**: CentralArea1 net group 0x90 map 0 ->
  pt_808139C[0x10] = 0x08081438, first byte = **9**. off_8080F98[9] gfx 0x08617068
  (init copy is transient -- the anim overwrites slots 2..37), and off_8081220[9]'s
  first script = **BattleBackdropGFXAnimScript_807FB98** = T22's byte-exact
  default-arena anim. The id-5 guess in the pass-3 docstring lineage is wrong for
  THIS route; id 9 is the measured one (byte_80813F8's id 6 = maps 0/1 of the
  first net group, not CentralArea1).

## Poked fielding (step 4 -- one canon capture, plus one aborted mis-poke)

Route: /tmp/overworld_net.state + the battlestart recipe (79-frame direction cycle,
roll-open pokes at frame 60: 0x02001c16:0x2000 / 0x02001c18:0), 140 frames.
Poke: `--cheat 0x02001b9c:0xee70 --cheat 0x02001b9e:0x080a` (pins the chosen-record
pointer to battleSettingsList0 rec0 = 0x080aee70, the 0x08 singleton, every frame
boundary). Watch on 0x02001b9c, VRAM dump 0x06000000:0x800 and palette dump
0x05000000:0x20 at frame 139.

- **First attempt INVALID, reported for honesty**: high half poked as 0x0800 ->
  pointer 0x0800ee70 (garbage ROM data at offset 0xEE70), watch showed it; that
  capture proves nothing and its dumps were discarded. No budget was left for a
  second attempt after this, hence the re-run with the corrected halfword.
- **Corrected capture**: watch shows 0x080aee70 on every frame except frame 60
  (the game's own write of the natural 0x080b4be8 mid-frame; re-pinned at 61).
- **read #1**: [0x02001b9c] = 0x080aee70 -> byte at [ptr]+0x4 = **0x08**.
- **read #2**: BG1 tile base slot k=5 (0x060000A0, window offset 5*32) =
  `ff ff ff ff ff ff ff ff ec ff ff ff c3 fe ff ff 31 ec ff ff 12 c3 fe ff 2c 31 ec ff cc 12 c3 ff`
  = tile 3 of the id-8 anim blob **0x08616904** (script 0x0807FAC0's header src;
  num_tiles 0x10/step, dest 0x6000040); 16/36 VRAM slots hold that blob's tiles --
  the anim schedule of off_8081220[8] demonstrably ran with the poked byte.
- UNVERIFIED: palette bank 0 read 0x7FFF-white at the dump frame (the poked
  scripted record's battle repaints palettes / fades; the id7-vs-id8 palette
  distinction rests on the static table read: wrappers 0x08616760 vs 0x08616EC4,
  tiles+tilemaps identical). No live poke of 0x07 itself; 0x07's fielding route is
  getBattleSettingsFromList1 (asm00_1.s:16046-16062, index fetch), art identical to
  the captured 0x08 modulo palette wrapper.

## Formations gap's new size (step 5 report)

Generator census re-run: records 1240 / arrays 1076 (mismatches 0), arrays containing
a 0x00 quad id = **1045 -- exactly T65 pass 1+2's figure**. All 1076 arrays are now
interpreted (T65 pass 3): the 0x00 quad id is the dispatch-0 PLAYER-SPAWN slot
(off_80073A0 entry 0 = spawnMegaMan_80073CC); per-array zero-id dispatch sets
{0}:991, {0,3}:38, {0,10}:12, {3}:2, {0,9}:2; dispatch nibble 2 never appears; ZERO
dispatch-1 quads carry enemy_idx 0. The formations count stays `0 / 1076` by the
rows' own semantics (no scenario records a formation row; T65's verifier chose not
to inflate) -- not touched by this ticket.

## Tools change (step 5) + re-run (step 6)

- parse_backdrops: the three census rows' status moved `unrecorded` -> per-byte
  `verified (...)` with the measured cites (row shape unchanged);
  SECTION_NOTES["backdrops (M8)"]: stale T15 GAP trail + limit (ii) replaced by the
  T70 measured mapping (reader table, per-byte blobs, capture), sentinel numbers
  updated 268/461 -> 1047/1240; M1 row header gains "(mapping verified + poked T70)".
- Re-run: `backdrops (M8): ... -- verified 3 / 3` (was 0/3); formations line
  unchanged 0/1076; ROM sha256 identical before/after.
- git diff --name-only: docs/SCOPE.md, docs/inventory/backdrops.json,
  tools/inventory.py (+ this worklog). AS_DATA_FAMILIES untouched; no src/, no
  harness/states/trace/oracle/allowlist change.

## Ideas tried and dropped

- Poking [0x02001b9c] at a mid-init frame: dropped -- the write window is unknown;
  an every-frame cheat at frame boundaries pins the pointer through init and is
  self-diagnosing via the watch (adopted).
- Matching id-5's init tiles against T22's FRAMES[0] (the pass-3-era assumption that
  id 5 = the default arena): 1/15 slots + palette mismatch -> dropped; the route
  resolves to id 9, whose ANIM (not init copy) is T22's script.
- Matching VRAM slots against id-8's init LZ77 tiles: 0/36 -> the anim overwrites
  slots 2..17 every step (16-tile steps from blob 0x08616904), so the anim blob is
  the evidence, not the init copy.

## Unverified

No scenario, no port, no src/ change, no row; palette leg of the 0x08 poke (bank 0
white at dump); no live poke of 0x07; anim scripts other than 0x0807FB98/0x0807FAC0
not decoded; per-frame timing of the init read inside frames 61..75 not traced
(the watch + VRAM identity bound it from outside).
