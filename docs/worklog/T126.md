# T126 — M3's first damage rule: elements and weakness as a ported multiplier

Worktree `/tmp/bnwt/t126`, branch `wt/t126`. Every number below came from a
command run in this session.

## Step 1 — decode the tables (measurement only, no build)

- ROM: `/tmp/bn6f_real.gba` 8388608 bytes, sha256
  `a37c1028adb72082b51e142321fa437967bc54b6f46730a53f6581ad455ad670`.
- Primary table `byte_3007444`: routine reads `defender*5 + attacker`
  (asm38.s:3534-3536); 25 bytes found at ROM 0x081d7944 (unique match), rows
  quoted in docs/coverage/elements.md §2. One extra hit per weakness,
  additive (`mul r0,r4` over `1+primary+secondary+bubble+elecBubble`,
  asm38.s:3483-3520).
- Secondary: no table — a bitfield rule (asm38.s:3634-3692), §3 of
  elements.md. The 0x80 special case keys on the attacker's
  `SelfCollisionTypeFlags & 0x2000`; that flag has no name in the .inc.
- Defender bytes: Struct2's first halfword is **element<<12 | hp&0xfff**
  (asm00_1.s:9240-9249 masks 12 bits for HP, shifts 12 for Element) — the
  `elem_hp` question is settled: element AND HP ride together. Struct1's
  Element (+5) ORs into BattleObject.Element (asm00_2.s:17637), its
  SecondaryElementWeakness (+6) goes to collision+0x18 (asm00_2.s:21358).
- Walked all 32 entries of both tables: **Mettaur (ai 0x01) and Gunner
  (ai 0x17) both read element 0, weakness 0**; only ai 0x19 (cursor w=0x40)
  and ai 0x1a (wind w=0x20) have nonzero weakness; 13 viruses have nonzero
  element nibbles (list in elements.md §5). `off_8109150[4]` row 0
  (0x0810ae4c) = 0x005a → HP 90, element 0.
- Attacker element: `BattleObject.Element` splits into collision
  PrimaryElement (low nibble) / SecondaryElement (high nibble)
  (asm00_2.s:21347-21351). Chip records carry `ChipElement` at +0x6.
  CHIP_ELEM→ELEM mapping (+1 for fire/aqua/elec/wood, flag bits for
  sword/cursor/wind/break) is **derived from the table structure + enums,
  not yet from a found conversion site** — I searched for an 11-byte
  CHIP_ELEM→element-byte table in the ROM (pattern `01 02 03 04 ...` with
  0x80/0x40/0x20/0x10 in the sword/cursor/wind/break positions) and found
  none; canon most likely converts per-chip in the chip scripts. The
  multiplier tables themselves are measured; the mapping is the one weak
  link and is named as such.

Ideas tried and dropped:
- Looking for the CHIP_ELEM→element-byte conversion table in ROM: not found
  (5 candidate hits, none matched the secondary bit positions). Next worker:
  the conversion happens per attack object at chip-script spawn time; trace
  `--trace-pc` on `object_setupCollisionData` during a sword row would catch
  the caller chain.
- Reading the IWRAM table bytes from the live game: unnecessary — the
  disassembly's `.byte` rows are the ROM's load image and the 25 bytes match
  exactly one ROM location.

## Step 2-5 — see below (updated as landed)

### Plan notes for the next worker

- The ×≠1 pair is measured with a RAM poke: PAUSED state's Mettaur lives with
  its collision data already created, so the poke must go to the collision
  data's PrimaryElement (+0x2 via [obj+0x54]), not BattleObject.Element —
  spawn already ran. The fixture kinds carry no element (§5), so without the
  poke no ×≠1 pair exists on any green row; that is the ticket's bounded
  negative.
- Enemy HP is invisible in pixels (viruses have no HP box), so the row's
  multiplier evidence is the trace's e1_hp step, and the pixel row rides the
  death-timing difference if the enemy is killable inside the window.

## SESSION END — where this ticket stopped (handoff)

This session landed **step 1 only** (commit 9820945). The tool/capture budget
ran out before steps 2-5. State for the next worker:

- **Baseline launched, NOT completed.** `nohup python3 tools/verify_rows.py
  wt/t126 <43 chip rows>,opening,mettaur,cannon,buster,chip-use,wave,windowclose,
  popup,result,cursor > /tmp/t126_baseline.log 2>&1 &` (pid 2594409, launched
  at commit 9820945, pre-change tree). The log was still empty at session end
  (verify_rows builds its own detached checkout + target dir first — the
  checkout is pinned to the step-1 tree, so later commits on wt/t126 do NOT
  corrupt it). **Check `/tmp/t126_baseline.log` first**: if it finished, those
  54 lines are the step-2 baseline; if the process is dead, re-run it before
  any code change.
- **Step 3 (the port) not written.** Design settled, from the measurements:
  1. `src/actor.rs`: add `element: u8` + `secondary_weakness: u8` to `Actor`,
     defaulted to 0 (ELEM_NULL), set at construction — `Actor::new` gets them
     from the `Profile` (fixture path) or from the Mettaur struct1 row
     constants (element 0, weakness 0 — measured, so the default IS correct
     for every enemy our fixtures can build).
  2. `src/fixture.rs`: ONE descriptor byte `enemy_element` (+ a free offset in
     the reserved region, same precedent as `enemy_hp`), read into `Profile`.
  3. `src/battle.rs` damage apply path ONLY: wrap the `enemy.take_damage(...)`
     call sites for chip hits (battle.rs:4648/4665 sword+seed family,
     :4257, and the Shot/bomb path if the chip carries element into the
     projectile) with `power * (1 + primary_mul + secondary_mul)` where
     `primary_mul = PRIMARY_TABLE[defender.element*5 + atk_elem]`,
     `atk_elem = chip.element+1` for chip.element 0..3, 0 for 0xa (NONE);
     `secondary_mul = 1 if defender.weakness!=0 && (defender.weakness & atk_bits)!=0`
     with `atk_bits` = {5:0x80, 6:0x40, 8:0x20, 9:0x10} for sword/cursor/wind/
     break chips, else 0. The 0x80-vs-attacker-flag 0x2000 special case has no
     attacker-side port to read — leave it unported and named (no fixture
     carries weakness anyway). PLUS(4)/OBSTACLE(7) unported, named. Constants
     get `// provenance: derived -- docs/coverage/elements.md §2/§3, ROM
     0x081d7944 / asm38.s:3634-3692`.
  4. The 43 chip rows must stay 0/0: their route has enemies=0 (no defender)
     → the multiplier must key on a DEFENDER being present, not on the chip
     firing; the buster/wave paths (BUSTER_DAMAGE, gunner::DAMAGE, enemy
     shockwave) must NOT gain an element (their attackers are not chips).
- **Step 2's canon damage pair, one recipe that worked on paper but was never
  captured:** canon side STERILE+PAUSED, `--poke 0x0203ab6e:0x0001` does NOT
  work (spawn already ran; the collision data exists) — poke the collision
  struct instead: read the pointer once with `--peek 0x0203abb4`
  ([obj+0x54]), then `--cheat <ptr+2>:0x0001` (every frame; collision+0x2 is
  PrimaryElement, CollisionData.inc:89). Enemy HP: `--poke-at F:0x0203ab84:...`
  before the strike; watch 0x0203ab84:2 on canon, e1_hp (oracle export) on
  ours. The Mettaur sits away from sword range on PAUSED — either poke its
  PanelXY (object +0x12 halfword AND collision +0xa halfword; whether the
  sprite follows the poke is UNMEASURED) or accept the bounded-negative
  outcome the ticket itself anticipates: with Mettaur/Gunner both
  element-0/weakness-0 (elements.md §5), **no ×≠1 pair is reachable on a
  green row without RAM pokes** — that, plus the §5 table walk, is a complete
  bounded negative if the poked measurement proves too fragile.
- Step 4/5: elem_weak row (negative = same recipe, ×1, must not be blind),
  then re-run the baseline set + `cursor ≤1/1/170` with the pad sweep
  reported, per docs/coverage/cursor.md.
