# T141 — is SEAM_PHASE_PAD_ITERS still a knob on wt/t138's layout?

Worker: T141, 2026-09-18. Tip at start: 3e50994 (T138 port + T140 row), pad = 25.

## Step 1 — endpoints (measured, before any edit)

`git merge main` first (ticket instruction): main HAD moved — it carried T135's
LANDED c5cab90 (src/actor.rs +28, src/battle.rs +33, assets/megaman.bin
26662→26854B, tools/harness.py form_cross row, states.py +12). Merged clean as
9535ea5. So this tip's layout is NOT the layout T140 measured: it is
T138+T140+T135.

Baseline `python3 tools/harness.py --only cursor --no-gallery` (twice, identical):

```
cursor isolated FAILED total 16241 worst 3385 frames 170 | negative: not blind (total 170163)
```

NOT the ticket's expected `26/25/170/186276` (that was 3e50994 without T135)
and NOT main's `1/1/170/186279` either. ROM:
sha256 2138710f9d413af4b73667c8ba22982353491173936e9e57343319aa77454dbf,
594044 B (+692 vs T140's 593352 — T135's asset+code).

## Shape (before sweeping — do not spend captures blind)

`/tmp/cursor_detail.py` (T105's leftover harness-internals probe, capture pair 3):
only 5 divergent frames of 170, evenly spaced k=6,36,66,96,126 (~3300–3385 px
each, period 30 = the cursor blink period). Not a 1 px seam cut and not palette
corruption:

- rust capture frame 252 is **pixel-identical (0 px, full frame)** to canon 22;
- canon's own sequence around the blink: … 19 (6020 vs c22), 20 (5517), **21
  (3385)**, 22 (0), 23 (1938) …; rust: … 250 (6020), 251 (5517), **252 (0),
  253 (0)**, 254 (1938) …
- i.e. rust shows the post-blink content for **two** frames (252 AND 253) where
  canon shows it for one (22). Frame contents and order are otherwise identical;
  no rust frame equals canon 21 (the transient).

So the merged layout's divergence is a **duplicated display frame** at every
tile-content-change (blink) frame — the overshoot side of T111's V-bottom
(too few iters → seam tear, too many → duplicated frame), not a new render
divergence. Pad 25 overshoots on THIS layout; T132 only swept upward (29..35),
which is consistent with it getting worse upward. The V-bottom for this layout
should sit at or below ~21, so the sweep goes DOWN from 25.

## Step 2 — sweep (pad → total/worst/frames/negative)

Running as pad 25 (done above) then 21, 17, … downward, 1 capture pair each.
| pad | total/worst/frames | negative |
|-----|--------------------|----------|
| 25 (current) | 16241/3385/170 | 170163 |

(remaining rows filled as measured)

## Step 2 — sweep results (pad → total/worst/frames | negative)

| pad | cursor | negative | field-bg1=bg2 | field-bg3 |
|-----|--------|----------|---------------|-----------|
| 11 | not run | — | 4320/4320 | — |
| 13 | **1/1/170 — main's exact line** | 186279 | 4800/4800 | 380263/24906 |
| 15 | 55/49/170 | 186277 | — | — |
| 17 (T111's) | 77/56/170 | 186350 | 1920/1920 | — |
| 19 | 68/67/170 | 186276 | 6720/6720 | 380263/24906 |
| 21 (CHOSEN) | **1/1/170 — main's exact line** | 186279 | 5040/5040 | 380263/24906 |
| 23 | 321/280/170 | 186557 | — | — |
| 25 (T130's) | 16241/3385/170 | 170163 | 5520/5520 | 380263/24906 |
| main | 1/1/170 | 186279 | 3600/3600 | 380219/24906 |

The class plateau is NOT contiguous (15/17/19 read 49–77 px); 25 duplicates blink
frames. bg3 is pad-invariant (380263 at 13/19/21/25) vs main's 380219: +44,
outside the ±25 drift envelope. field-bg1/2 hop 1000–3000 px per pad step, never
hit main's 3600 in 11..25.

## Step 3 — guard set at the chosen pad 21

mettaur 0/0/70/41734, result 0/0/40/111839, super_armor **0/0/70/29640** (the
branch's headline row — still 0, negative unchanged), wave 0/0/90/3840, chip-use
isolated 0/0/30/7768 (integrated = documented allowed failure, unchanged), tiles
0/0/8/12197, gauge 0/0/8/12197, window 0/0/16/81056, windowclose 0/0/40/207166,
opening isolated 0/0/40/86591, popup 0/0/80/1288 — all identical to main.
field-bg1/2/3 at 21: 5040/5040, 5040/5040, 380263/24906 vs main 3600/3600,
3600/3600, 380219/24906 — NOT byte-identical, and no pad fixes it.

Cross-checks: `tools/verify_rows.py HEAD` reproduced all my lines exactly;
`verify_rows.py main` (main had moved by a TODO-only commit, 0947461→703eb0c,
code identical) measured main same-session: field-bg1 3600/3600/40/174345, bg2
3600/3600/40/3600, bg3 380219/24906/40/405125, opening 0/0/40/86591.

## Verdict / for the next worker

1. The pad is ALIVE: 13 and 21 restore main's exact cursor line on this layout.
   The recurring "pad exhausted" hypothesis is dead — T132's 29..35 sweep just
   looked on the wrong (overshoot) side.
2. The strict land-ready bar (guard byte-identical to main INCLUDING the
   field-bg rows this ticket added) is NOT reachable by the pad: bg3's +44 is
   layout-driven (pad-invariant), bg1/2 never rejoin 3600. The merge of T135
   (already landed on main — unavoidable) re-rolled the field-bg artifact.
   Whether "field-bg reroll within its documented non-zero row" is an acceptable
   landing is the coordinator's call; the cursor objection that blocked six
   landings is gone at pad 21.
3. Dropped ideas: pad 19 co-fit (measured, both rows worse); extrapolating the
   field-bg slope linearly (it is step-like — 1920@17 vs 5040@21).
4. Next: if the strict bar stands, the fix is not a pad — it is T118's missing
   measurement (canon's mid-frame write scanline) or a tolerant policy for the
   field-bg reroll class. If not, land wt/t138 at pad 21 as-is.

## Unverified

- field-bg at pads <11 and {12,14,16,18,20,22,24}: unmeasured; an exact 3600
  co-fit there while cursor stays 1/1 is unlikely (hop sizes) but not excluded.
- T140's lever-flip behavior negative remains unattributed (their log's (a)-(c)).
- Mechanism line: the pad delays agb's screenblock copy past vblank by a
  constant; too little = stale-tile tear, too much = the copy misses the frame
  and the display repeats the previous frame at every content-change frame.

Final ROM (pad 21): sha256
c5178b3a8fcd7c281e82edc40f661bbf51c9ab731c81be9b117c2233f8426820, 594028 B
(vs T140's 760f2029… 593352 B: +676; vs the merged pad-25 build 2138710f…
594044 B: −16). fitted 16, derived 438, peeked 146.
