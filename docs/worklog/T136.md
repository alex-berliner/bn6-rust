# T136 — M8's LOSER window, steps 3-5: the row on the lever T127 measured, and the fixture trap it exposed

Worktree /tmp/bnwt/t136, branch wt/t136, base e047ad4. Ticket budget ran out at the
soft limit (60 tool calls) mid-ticket: steps 1-4 are MEASURED, step 5 is NOT reached
(no `src/results.rs` edit, nothing localised to edit), and the row's unmodified read
is a full-screen number the ticket's step 5 does not cover. Everything measured is
below so the next worker starts from numbers, not guesses.

## Step 1 (no capture) — the LOSE arm's claimed suppressions and what can see each

From `src/results.rs` (all cites read this session, unmodified src):

| suppression | cite | visible via |
|---|---|---|
| coin picture + amount line (draw_reward no-ops unless `variant == WIN`) | src/results.rs:601 (`if shown.variant != WIN { return; }`), doc at :594-596 | pixels (reward region); trace TRC2 +62 zenny (no reward drawn) |
| no-reward dismiss (LOSE waits in `Phase::Waiting` and dismisses on confirm; WIN must go through Revealing/Cooldown/RewardWait first) | src/results.rs:772-776 | pixels (dismiss fade timing); no trace field |
| window ignores time, level and rank entirely | src/results.rs:464-465 (doc on `show`) | trace TRC2 +60 rank, +61 level (INFO); pixels (time/level lines blank) |

Assertion list for the row: over the lose frames, zenny must read 0/absent, rank and
level must not appear, and the window must hold stable until confirm.

## Step 2 (canon capture 1, 400 frames) — the LOSER screen's composition timeline

`/tmp/mgba_capture /tmp/bn6f_real.gba /tmp/t136_lose 400 --loadstate /tmp/pausedwithcannon.state --script "Start@10" --poke-at 40:0x0203a9b8:0x0008 --watch 0x0203CA70:4 --watch 0x020364C4:4`

- Sequencer 0x0203CA70: 0x1c -> 0x08 @11 -> 0x10 @41 -> **0x4000010 @42** (lose end
  state; matches T127 exactly, poke -> state within 5 frames).
- **T127's cited slide-in marker is refuted: the record dword 0x020364C4 NEVER moves**
  (stays 0x0a051d01 garbage through 400). showResultWindow's variant record write is
  not observable on the lose route.
- Screen timeline (per-frame diffs + brightness): battle live to ~136; fade-out
  137-152 (fully black 152-168); fade-in 169-176 revealing the LOSER screen ALREADY
  composed (no visible slide-in — it happens inside the black); stable 177-348;
  dismiss fade 349-379 (fully black by 379). No input was ever pressed after
  Start@10, so the lose route dismisses on its own pacing.
- The stable screen carries a **period-64-frame full-screen animation** (~330 px/frame
  moving; every x-band 1700-1900 px per cycle; frame N == frame N+64 pixel-identical,
  32-shift differs by 3001 px). Dominant colours: dark magenta [132,24,99] over most
  of the screen, orange [255,181,49] left-of-centre — flame/glow-style art, not the
  win window's static arena.

## Step 4 (canon capture 2, same session, 400 frames) — both-dead: canon does NOT show WIN

`/tmp/t136_bothdead`: same recipe PLUS `--cheat 0x0203ab84:0 --cheat 0x0203ab86:0`
(states.py:135's DELETE_ENEMY) and `--watch 0x02009A18:4` (the eLinkState+0x48 gate).

- Sequencer identical: 0x4000010 @42. **The defeat gate 0x02009A18 never fires**
  (stays 0) — the CurState poke reaches the lose state WITHOUT the eLinkState gate;
  sub_803DD60's cited input path is not the whole story on this route.
- **The ticket's negative premise is refuted by measurement**: the both-dead capture
  is pixel-IDENTICAL to the live-enemy one on every frame 180..344 (165 frames, 0
  differing px, checked all). Canon's lose screen shows no Mettaur either way, and
  under both-dead canon shows the SAME LOSER screen — not WIN. There is no
  enemy-deleted-(WIN) canon side to build T127's negative on. The row's negative
  stays the default 1-frame canon shift (non-blind as measured below), and the row's
  canon side carries DELETE_ENEMY only as belt-and-braces (measured no-op).

## Step 3 (measured, unmodified src) — baseline, guard set, the row's first read

- Baseline ROM (plain build): sha256 `c1ead4cdb7e154508ac9c7611ea1ae72ad4131aa3dfbf81092709a28d0679915`,
  **592752 B** — byte-identical to main's T124-baseline ROM (same sha there).
- Guard set isolated, main's lines reproduced exactly: opening 0/0/40, mettaur 0/0/70,
  chip-cannon 0/0/40, wave 0/0/90, window 0/0/16, **cursor 1/1/170 (pre-existing tear,
  unchanged)**, windowclose 0/0/40, **result 0/0/40 unchanged**, popup 0/0/80.
  fitted 17 / derived 436 / peeked 146 — unchanged (no const added).
- **`result_lose` isolated: FAILED total 1536000 worst 38400 frames 40 | negative: not
  blind (1536000).** 38400 = the WHOLE screen differs on EVERY compared frame.

### Why this is a stop, not a fix

Step 5 says "fix only what step 3 localises, inside the LOSE arm". A full-screen
every-frame diff localises nothing: the compared 40 canon frames (canon_ref 185,
inside the stable cycle) cannot be matched at ANY rust offset 0..199, which means one
of — unverified, each is the next worker's first measurement, cheapest first:

1. **Our defeat path may never fire with `megaman_hp=0` at spawn.** LOSE_ROW relies on
   `decided` (src/battle.rs:2856-2866) seeing `megaman.is_defeated()` from frame 0 of
   the fight. I could not confirm this session what `is_defeated` does for an object
   that spawns at HP 0 (definition not located before budget out; battle_full only
   ever exercised megaman_hp=60). One `--watch 0x0203CA70:4` capture on the rust side
   answers it: if the sequencer never leaves the fight/intro for 0x10, the fixture
   cannot seed a loss and this is the `src/battle.rs` NEGATIVE the ticket's Rules
   name (one-word change at the defeat line, coordinator-owned file).
2. **Our window may arrive later than offset 199.** Our port's lose pacing (banner +
   RESULTS_DELAY + slide) was never measured; if it lands past rust frame ~240 the
   search band misses it. Widen `search` (row config, mine to set) only after 1 is
   ruled out — never to make a number move.
3. **Even aligned, the period-64 full-screen animation is outside today's LOSE arm.**
   Canon's lose screen animates everywhere; `src/results.rs` draws a static window and
   there is no backdrop animation mechanism in it. If 1-2 are fixed and the residual
   is the animation, that residual is a NEW mechanism (animated lose backdrop), not
   one of the three suppressions step 1 lists — a follow-up ticket's scope, not a
   widening of this one.

The negative is NOT blind (1536000) — trivially, since every frame differs — so the
row can fail; it just has nothing to pass against yet.

## What landed

- `tools/harness.py`: LOSE_ROW fixture (normal defeat path, NOT start_state=1 — the
  :3193 WIN-hardcode trap honoured) + the `result_lose` Check (canon side = PAUSED +
  Start@10 + DELETE_ENEMY cheats + poke-at 40:0x0203a9b8:0x0008, canon_ref 185,
  search 0..199, frames 40, negative default frame-shift).
- `tools/states.py`: NOT touched — the `result_lose` TRACE_SCENARIOS entry needs a
  measured rust_base for its trace pairing and no offset was ever found (full-screen
  diff, no unique minimum). Add it after 1-3 above resolve; `result`'s entry at
  states.py:730-736 is the template, mercy_addr 0x02038514 same family.
- No `src/` edit of any kind; ROM sha and every guard row byte-identical to main; no
  allowlist change; no existing row's config touched.

## Next worker's plan

1. Rust-side `--watch 0x0203CA70:4` capture of the LOSE_ROW fixture (cheapest: ONE
   capture) — does SEQ_10 fire? If no: report the src/battle.rs NEGATIVE (the ticket's
   named shape: the defeat/`decided` line, one-word change) and stop.
2. If SEQ_10 fires, find the rust window's arrival frame, widen/keep the search
   honestly, re-run the row. If the residual localises to the period-64 animation,
   open the animated-lose-backdrop as its own ticket (inventory: which routine drives
   it — the lose wrapper sub_8009916's resultWindowDriver_802BD60 per T127, unverified).
3. Then the trace scenario + rank/zenny-over-lose-frames report (step 5's tail), and
   `docs/coverage/results.md` with the variant table: the win/lose store
   oBattleState_Unk_0d (asm00_1.s:9641, GetBattleEffects bit 0x08, sub_803DD60
   asm03_1_1.s:9414), with the THIS-TICKET correction that the CurState poke reaches
   the lose state without eLinkState+0x48 ever firing, and 0x020364C4 never moves on
   the lose route.

Unverified (this ticket's one line of unverified): escape and time-out endings need
their own scenarios; the period-64 animation's owning routine; `is_defeated` at HP 0
spawn; whether our port's banner/RESULTS_DELAY pacing can even reach the window inside
offset 199.
