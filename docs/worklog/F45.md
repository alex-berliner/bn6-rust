# F45 worklog — attributing `field`'s integrated residue (158935/5606/40) to layers

Branch: wt/f45-field-layer-attribution. Ticket: F45. Recon: docs/recon/F45.md (read first;
every causal link in it UNVERIFIED until a capture says otherwise).

## Baselines (step 1, on this worktree, before any code change)

```
python3 tools/harness.py --only field --ui integrated --no-gallery
field      integrated  FAILED (allowed: AUDIT-6 ..., <=28000)  total 158935  worst 5606  frames 40  | negative: not blind (total 261034)
python3 tools/harness.py --only field --ui isolated --no-gallery
field      isolated    PASS   total 0        worst 0     frames 40  | negative: not blind (total 1139)
fitted constants: 19 (derived 430, peeked 147)   # grep -rn "provenance: fitted" src/ | wc -l = 19
```

Matches the ticket's expectation exactly. Not touched since.

## Capture budget plan

Ticket allows at most 6 new captures. Recon §5's first pass = the three post-boundary
masks (each Check row = 1 rust + 1 canon capture = 2, so 3 rows = 6):

1. `field-bg1`: canon `--only-bg 1` (backdrop) vs rust `--only-bg 0` — post-boundary pairing
2. `field-bg2`: canon `--only-bg 2` (field panels) vs rust `--only-bg 1`
3. `field-bg3`: canon `--only-bg 3` (HUD) vs rust `--only-bg 2`

That spends the whole budget, so:

* The pre-boundary window (k=0..4, rust 116..120 <-> canon 130..134, SAME N both sides) is
  NOT re-captured. F42's kept capture dirs still exist under /tmp/bn-57f5663a/ and contain a
  `--only-bg 1`-on-BOTH-sides pair at exactly the pre-boundary alignment (base 116,
  canon_ref 130); the k=0..4 numbers are re-derived offline from those dirs instead. Caveat
  checked: those dirs' integrated per-k reproduces main's own row exactly
  (0 0 3566 5319 5319 5606 ... = 158935/5606/40), so the rust ROM they captured renders
  identically to main on these frames.
* BG2-only and BG0-only full composites (ticket step 5): NOT captured — no budget left after
  the three post rows. Documented as unmeasured in docs/coverage/field_integrated.md.
* Rows are executed via a driver that calls harness.run()/negative_counts() directly on the
  landed Check objects (identical Sides, Align, frames) because run_check() deletes both
  capture directories before returning, and the per-frame coverage table needs the frame
  files. The harness rows themselves are landed unchanged and will reproduce when run
  normally.

## Log

### Step 2 (2026-09-16): the three post rows captured and read

6 captures, one row at a time, via a driver that calls `harness.run()` / `negative_counts()`
on the landed Check objects (run_check deletes the capture dirs; the per-frame table needs
the frame files). Dirs kept under /tmp/bn-cd4ee804/. rust origin 8, offset 113, canon_ref
135 in all three rows, as landed.

```
field-bg1  total 367776  worst 14559  frames 40 | negative: not blind (total 402740)
field-bg2  total    4560  worst  4560  frames 40 | negative: not blind (total 4560)
field-bg3  total 380263  worst 24906  frames 40 | negative: not blind (total 405169)
```

Per-frame counts (post k; rust capture = 121+k, canon = 135+k):

```
bg1: 14559 8642 11608 9923 11679 10419 11665 11385 10751 8263 10830 9263 10900 9879 10786
     10057 9492 7362 9536 7893 9570 7338 9430 7822 9170 6990 9141 7521 9104 6810 8896 7261
     8543 6422 8477 6786 8407 7645 9584 7967          (coverage 16.7-37.9%, none >=75%)
bg2: 4560 0 0 0 ... (0 on k=1..39)                  (coverage 11.9% then 0.0%)
bg3: 704 0 x19, then 1917 3837 5757 7677 9696 11744 13792 15840 17973 20197 22421 24642
     24818 24906 24906 24906 24906 24906 24906 24906 (coverage 64.9% max, none >=75%)
```

Pre-boundary, same-N BG1-only, re-derived offline from F42's kept dirs
(/tmp/bn-57f5663a/h_rust+canon_54bfe1c415, `--only-bg 1` on BOTH sides, base 116 / canon_ref
130 — verified: that pair's integrated twin 69e75a445d reproduces main's 158935/5606/40
per-k exactly, and the pair 93c9548c99 reads k2=0/k10=1126, some other isolation, unused):

```
k=0..4 pre-bg1: 0 0 7362 11117 11117   (coverage 0.0 0.0 19.2 29.0 29.0 — all usable)
```

### Findings

1. THE KILL-CHECK PASSED: F42's BG1-only saturation (100% on 34 frames) collapsed to <=37.9%
   under the canon-N / rust-(N-1) pairing. The compensation is same-content as far as any
   capture can show. `field-bg2` reading byte-identical on 39 of 40 frames is the strongest
   single piece of evidence (a wrong pairing compares panels against backdrop and saturates).
2. The k>=5 bulk (139125 px over field k=6..39, 87.5% of the row) is carried by the BACKDROP
   layer: panels 0 everywhere, HUD 0 until canon's own RESULT slide-in starts at canon 154
   (= post k=19, exact 1917 px/frame ramp saturating at 24906 = the RESULT window area,
   matching the `warp` row's measured slide rate) — that part is the canon RESULT-over-HUD
   vs rust HUD-on-BG2/shown-on-BG3 stack difference, invisible in the integrated composite
   (no integ spike at k>=19), not a HUD defect.
3. Accounting closes with no gap: 14204 (k=2..4, backdrop, pre-boundary) + 5606 (k=5,
   boundary transition frame; backdrop 14559 / panels 4560 / HUD 704 all usable there) +
   139125 (k=6..39, backdrop) = 158935. Rejected frames: 0. Uncovered: 0.
4. Windows: `--disable-bg` and `--only-bg N` both force disableWIN[0]/[1]=true
   (tools/mgba_capture.c:396-398, :549-550) — re-read and confirmed this session. A window
   claim cannot come from any isolation mechanism. BLEND/BLDCNT/BLDALPHA/BLDY/mosaic: grep
   across tools/ empty, no flag touches them, untested — could in principle carry
   composite-only residue; noted in the coverage doc with why.

### Rejected frames

None (0 across every mask used). The only saturated masks seen this session were F42's own
kept capture at k>=5 (92.5%/100%) — from F42's run, not re-captured; excluded per the rule.

### Ideas tried and dropped

* Landing a pre-boundary same-N Check row (k=0..4, frames=5, Align(130,108,None)): dropped
  — the 6-capture budget is exactly the three post rows, and the pre-boundary numbers are
  already derivable from F42's kept capture. Recipe is in docs/coverage/field_integrated.md
  for a follow-up.
* BG2-only / BG0-only full composites (ticket step 5): no budget left after the three post
  rows. Unmeasured, recorded as such.
* Deriving the pre-boundary window from the POST rows' own capture dirs (they contain rust
  116..120 / canon 130..134): useless — those captures have rust N-1 vs canon N, which
  pre-boundary is a MISMATCHED layer pair, not the same-N pairing the pre window needs.

### What I would try next

`--watch` eGFXAnimStates[0] (0x020094c0) and eBGScrollCBCounters (0x02009690) on both sides
across rust 116..125 / canon 130..139 to pin the backdrop divergence's first cause (the F32
end-sequence offset firing early), and a BLEND-off capture path (a new renderer flag) if
anyone needs the last few percent of the attribution closed by measurement.

### Budget spent

New captures: 6 of 6 (3 rows x rust+canon). Baseline existing-row runs (field integrated +
isolated, step 1): 4 captures, not new-row budget. Tool calls well under 70.
