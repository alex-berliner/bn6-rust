# F44 — Gate the HP readout on canon's battle-state latch, not the intro ramp

Branch `wt/f44-hud-state-latch` (worktree /tmp/bnwt/f44-hud-state-latch).
HEAD at start: 575450d (F44's own ticket commit; F43's `1542df0`
cherry-picked as the starting point, then its arms replaced).

## Step 1 — baseline (main, before any edit)

```
python3 tools/harness.py --only opening --ui integrated --no-gallery
opening    integrated  FAILED  total 25829  worst 931  frames 40 | negative: not blind (total 105749)
python3 tools/harness.py --only cursor --no-gallery
cursor     isolated    FAILED  total 1      worst 1    frames 170 | negative: not blind (total 186279)
fitted constants: 19 (derived 430, peeked 147)
```

Both match the ticket's quotes exactly. F43 branch HEAD re-checked first:
`wt/f43-hp-readout-gate` = 5bf7ff6 (worklog) on 1542df0 (the emission gate),
the same `hp_readout_live` this ticket replaces.

## Step 2 — the latch (implementation)

`git cherry-pick 1542df0`, then in src/battle.rs:

- NEW FIELD `fight_latch: bool` — canon's `oBattleState_Index_00 == 4`
  handover latch. Set at `Battle::new` when the descriptor's SKIP_INTRO
  declares the pairing against a mid-fight canon state (a state-loaded
  battle's state word IS 4), and in update()'s intro machine when
  `intro_next` walks past the last enemy (the handover site:
  sub_8007A0C's `mov r0,#4 / str r0,[r5,#oBattleState_Index_00]`,
  asm00_1.s:9716-9721, after the last enemy materialises).
- `hp_readout_live()` = `self.fight_latch && !matches!(self.seq.state,
  SEQ_0C | SEQ_10)`. The `SEQ_0C | SEQ_10` arm is canon's terminate arm
  (eStruct2038160_getBattleTerminate01() != 0 leaves to state 0x10 before
  the chain, asm00_1.s:9757-9776). The `intro_fade`/`intro_next` arms of
  F43's first cut are REPLACED by the latch; the `banner` arm is DROPPED
  (the only banner it could see is the closing ENEMY-DELETED one, armed
  only when `over` — vacuous). No paused term: `oGameState_BattlePaused`
  is tested AFTER the chain (asm00_1.s:9795).
- A latch-probe byte at 0x020000C0 (BATTLE_MARKER's own tail, one volatile
  store per frame, written by nothing else): bit0 live, bit1 fade, bit2
  next, bit3 banner, bit4 over, bit5 latch. Read with `--watch`.

No new numeric constants; fitted-constant count unchanged (19).

## Step 3 — the latch separates the rows (per-frame trace, BEFORE any pixel capture)

Probe driver (tools/trace-free): rust-side captures with the row's own
Side + `--watch 0x020000C0:1`, marker origin 8 both rows, documented
offsets (opening 119, cursor 237 — both rows' own unique search minima).

```
cursor   (170 compared frames, rust frames 245..414): bit pattern CONSTANT 0x21
         [live latch] on every k=0..169 — latch open from battle frame 0
         (SKIP_INTRO: the battle starts handed over), digits drawn on every
         compared frame, exactly what canon's state-4 row shows.
opening  (40 compared frames, rust frames 127..166): bit pattern CONSTANT 0x04
         [next] on every k=0..39 — latch CLOSED all 40 (the intro walk is
         still materialising enemies; fade already 0, so F43's fade arm was
         vacuous here too). Extended capture: the latch OPENS at battle
         frame 182 (capture frame 190, origin 8) — 16 frames after the
         compared window ends.
```

So the single latch is closed across opening's whole compared intro AND
open across cursor's whole compared row. POSITIVE: the distinction lives at
the emission site. The trace also nails F43's cursor regression to the
`intro_next` arm exactly as the ticket suspected: on cursor's compared
window `intro_next < enemies.len()` for the WHOLE row (the F37b frozen-pose
enemy parks the walk), while `intro_fade == 0` was already true — the
43-frame hide was the walk arm, not the fade.

## Step 4 — after (pixel measurements)

(quoted below as they land)
