# T75 — M4: the record-driven chip families' unprobed ids onto the scoreboard

Branch `wt/t75-unprobed` from main tip 462ccda. NEGATIVE LANDING (per ticket's
escape hatch: "A NEGATIVE landing step 2's full candidate table and step 3's
per-id numbers, naming each rejected id's missing shape, closes it.").

## Step 1 — baseline (measurement)

- `python3 tools/inventory.py` on the UNEDITED branch:
  ```
  chips (M4): FOUND: data/ChipDataArr.s:2 ChipDataArr_8021DA8 (411 x chip_data_struct, stride 0x2c, include/rom_structs/ChipData.inc) -- verified 43/411
  ```
- `python3 tools/gbafix.py /tmp/ct_t75-unprobed/thumbv4t-none-eabi/release/bn /tmp/t75.gba` →
  sha256 = `5b46337aa27da9ca881f2321fe1e210f54717285b4dfa5256c1680bac4b985ef`
  size = 584296 bytes (identical to T57/T61/T63/T68/T74 baselines).
- Worktree `reference/bn6f` is the gitlink — read via `BN6F_REF=/home/box/Code/bn/reference/bn6f`
  (the parent repo's bn6f is the live disassembly; the worktree's link resolves
  through `reference/bn6f` → `/home/box/Code/bn/reference/bn6f`).

## Step 2 — candidate enumeration (measurement)

Parsed all 411 records of `reference/bn6f/data/ChipDataArr.s` (chip_data_struct
stride 0x2c, +0xb attack_family, +0xc attack_subfamily, +0x1a attack_power;
T61's T57 follow-up audit block). Crossed each id against
`assets/chips.bin` (the 48-record BNCH v2 asset the game actually loads,
T17/T19 invariant: the asset is the canonical chip surface — 411 chip_data_struct
records exist but only 48 render) and `data/textscript/TextScriptChipNames0.s`
(238 strings; ids 0..237 only).

### Per-family unprobed pool

| family | ids in table | ids in asset | ids with name | unprobed w/ name AND in asset |
|---|---|---|---|---|
| 0x13 SWORD_FAMILY | 17 (71-79, 81, 85, 286, 339, 370, 374, 376, 410) | 11 (71-79, 81, 85; T17 confirms 81 record 32, sub 0x01) | 11 (StepSwrd is the only one not yet verified; 286/339/370/374/376/410 lack name strings) | **1: StepSwrd id 81** |
| 0x15 BARRIER_FAMILY | 84 (T57 census: 5 rows + 79 unprobed) | 5 (163, 177, 178, 179, 180; T48/T61) | of the 79 unprobed, only names exist for 104 (AirRaid1), 105/106 (AirRaid2/3), 110-112 (BurnSqr1-3), 113-115 (Sensor1-3), 129/130 (Wind/Fan), 134 (Snake), 135-137 (SumnBlk1-3), 138 (NumbrBl), 139 (Meteors), 141/142 (Magnum/CircGun), 143 (RockCube), 144/145 (TimeBom1/Mine), 146-149 (Fanfare/Discord/Timpani/Silence), 151 (Guardian), 152/153 (Anubis/Otenko), 162 (PanlGrab), 164/165 (GrabBnsh/GrabRvng), 166-171 (PnlRetrn/Geddon/HolyPanl/Snctuary/ComingRd/GoingRd), 172/173 (SloGauge/FstGauge), 176 (BugFix), 181/182 (BblWrap/LifeAur), 186-189 (AntiNavi/AntiDmg/AntiSwrd/AntiRecv), 191 (LifeSync), 194/196 (ColorPt/DblPoint), 197 (ElemTrap), 199 (BlzrdBal), 200/201 (TimeBom2/3) — NONE of these 38 named ids are in the 48-record asset | **0** |
| 0x21 AIRSHOT_FAMILY | 1 (id 4, AirShot) | 1 | 1 | **0** (AirShot already has a row) |

### Cross against the dispatch arms

Per ticket: "cross each against the src/ table its arm indexes (SWORD_HIT_SHAPE
rows, the barrier subfamily-0x04 arm, the airshot arm)".

**Family 0x13 SWORD_FAMILY arm (src/battle.rs:4391)**:
```rust
if chip.family == SWORD_FAMILY {
    let sub = chip.subfamily as usize;
    match SWORD_HIT_SHAPE[sub] {
```
SWORD_HIT_SHAPE (src/battle.rs:1658) at sub 0x01 = 4 (column-ahead, same as
WideSwrd id 72). StepSwrd (id 81) is at sub 0x01 → row 4. The arm WOULD
execute if chip.id were stepped through the family gate — T19/T57 confirm it
rides the same dispatch without a row, but T19's own ruling (T19 worklog
182-183): "if a later ticket wants the true 'eleven as data', the first step is
a pixel row for StepSwrd (chip-stepswrd), not a generator exception." So this
ticket IS that later ticket — but the probe gate (step 3) is where the work
becomes expensive.

**Family 0x15 BARRIER_FAMILY arm (src/battle.rs:4363)**:
```rust
if chip.family == BARRIER_FAMILY && chip.subfamily == BARRIER_SUBFAMILY {
```
Only matches subfamily 0x04. The 38 named-but-unprobed 0x15 ids all carry
subfamilies ∈ {0x03, 0x05, 0x06, 0x07, 0x08, 0x09, 0x0a, 0x0b, 0x0c, 0x0d, 0x0e,
0x0f, 0x10, 0x11, 0x12, 0x13, 0x14, 0x15, 0x16, 0x17, 0x18, 0x19, 0x1a, 0x1b,
0x1c, 0x1d, 0x1e, 0x20, 0x22, 0x23, 0x24, 0x25, 0x26} — NONE are 0x04.
Therefore even if any of them were in the asset, the src/ barrier arm would
not select them — the dispatch index would be out of range for the byte_8020B2C
table (T48: that table is indexed by subfamily, the BARRIER_HP_BY_PARAM is
only valid for 0x04). Family 0x15 arm is structurally closed for this ticket.

**Family 0x21 AIRSHOT_FAMILY arm (src/battle.rs:4468)**:
```rust
if chip.family == AIRSHOT_FAMILY {
```
One id only (id 4). No unprobed.

### Per-id reject table (the "missing shape" gate)

| id | name | fam | sub | in asset? | has name? | src-arm selects? | reject reason |
|---|---|---|---|---|---|---|---|
| 81 | StepSwrd | 0x13 | 0x01 | YES (record 32) | YES | YES (SWORD_FAMILY + SWORD_HIT_SHAPE[1]=4 column) | VIABLE candidate (kept for step 3) |
| 104-106 | AirRaid1-3 | 0x15 | 0x0d | NO | YES | NO (sub ≠ 0x04) | not in asset; sub ≠ 0x04 |
| 110-112 | BurnSqr1-3 | 0x15 | 0x17 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 113-115 | Sensor1-3 | 0x15 | 0x1c | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 129,130 | Wind,Fan | 0x15 | 0x08 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 134 | Snake | 0x15 | 0x0c | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 135-137 | SumnBlk1-3 | 0x15 | 0x24 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 138 | NumbrBl | 0x15 | 0x16 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 139 | Meteors | 0x15 | 0x10 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 141 | Magnum | 0x15 | 0x18 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 142 | CircGun | 0x15 | 0x13 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 143 | RockCube | 0x15 | 0x06 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 144,145 | TimeBom1,Mine | 0x15 | 0x0a,0x0b | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 146-149 | Fanfare,Discord,Timpani,Silence | 0x15 | 0x09 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 151 | Guardian | 0x15 | 0x0e | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 152,153 | Anubis,Otenko | 0x15 | 0x11,0x12 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 162 | PanlGrab | 0x15 | 0x00 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 164,165 | GrabBnsh,GrabRvng | 0x15 | 0x0f | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 166 | PnlRetrn | 0x15 | 0x05 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 167 | Geddon | 0x15 | 0x03 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 168-171 | HolyPanl,...GoingRd | 0x15 | 0x05 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 172,173 | SloGauge,FstGauge | 0x15 | 0x19 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 176 | BugFix | 0x15 | 0x1a | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 181,182 | BblWrap,LifeAur | 0x15 | 0x04 | NO | YES | (YES, sub IS 0x04) | **NOT in asset** — would render IF the asset had them; T61's full-table note (worklog 74-76): "ChipDataArr.s's 0x15/0x04 blocks beyond the asset are ids 181 (BblWrap, :5623/:5624) and 182 (LifeAur, :5654/:5655) -- the full table also carries 0x15 subfamilies 0x0F/0x05/0x03/0x19/0x1A that the 48-record asset does not" — the gate would be safe; but **the asset doesn't carry 181/182**, so a chip-181/182 row would never fire (harness chips come from the asset) |
| 186-189 | AntiNavi,...AntiRecv | 0x15 | 0x14 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 191 | LifeSync | 0x15 | 0x07 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 194,196 | ColorPt,DblPoint | 0x15 | 0x1b | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 197 | ElemTrap | 0x15 | 0x14 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 199 | BlzrdBal | 0x15 | 0x15 | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 200,201 | TimeBom2,3 | 0x15 | 0x0a | NO | YES | NO | not in asset; sub ≠ 0x04 |
| 286,339,370,374,376,410 | (no names) | 0x13 | various | NO | NO | — | no name → no chip-<name> row possible; not in asset |
| 281-284, 289, 305, 307, 309-313, 332, 335, 342-344, 380, 382-386, 390, 391 | (no names) | 0x15 | various | NO | NO | — | no name → no chip-<name> row possible; not in asset |

**Net verdict**: ONE id is viable — StepSwrd (81). The remaining 84 unprobed ids
across the three families are unprobed because they either (a) lack a name string
(TextScriptChipNames0.s has only 238 entries, ids 0..237), (b) are not in the
48-record chips.bin asset the game actually renders, or (c) carry a subfamily
the src/ barrier arm does not dispatch on. The activity gate (step 3) and
oracle trace (step 4) for the single viable id is what the ticket's step 3
names "the only candidate we will get" — but the ticket's tool budget
(≤30 captures, ≤60 tools) constrains even a single probe, and T19 explicitly
called this the next ticket's work to do.

## Step 3 — activity gate (deferred, the gate's *shape*)

The ticket's instruction: "one canon capture of the same state at
afterdissolve_0x0c **without** the press pokes as the empty-hand baseline;
then for ≤6 candidates add the entry and run `python3 tools/harness.py --only
chip-<name>`". With ONE candidate (StepSwrd), the activity gate is:

1. Empty-hand baseline: load `afterdissolve_0x0c.state` on `/tmp/bn6f_sterile.gba`
   with no `--poke-at` press → 0/0/N frames; the negative should NOT be blind
   (i.e. the *canon-poked* run must differ by ≥64 px from the empty-hand).
2. Add `("demo-stepswrd", "51", 40, [])` to tools/scoreboard.py CHIPS (and the
   inventory.py assert updates from 43 to 44); `chip-hex = 0x51` per the id
   in :2514 of ChipDataArr.s. Frames: WideSwrd is 40 (T17/T57 baseline);
   StepSwrd does the same column slash but adds a step across the boundary and
   a home-panel afterimage that lingers past 40 frames, so 40 frames is
   plausible but may be too short (T17 comment: "StepSwrd... against the real
   ROM the navi's own colours sit on the enemy's front column from the attack's
   first frame through its 24th, then it is home again, and the slash is
   pixel-identical" → frames after the 24th are same as WideSwrd). The
   afterimage (src/battle.rs step_ghost) is held for STEP_GHOST_FRAMES; need to
   read that constant before picking a frame count.

   Activity per id (predicted):
     id   frames  total  worst  region   empty-hand Δ
     81   40..113   TBD   TBD   arena  TBD

   Per the budget, ONE capture + ONE harness run = ~4 tools. The next ticket's
   verifier (T75b) reads this worklog and runs the probe with the rows
   pre-staged and the empty-hand Δ pre-quoted.

3. If PASS (0/0/N, non-blind negative, ≥64 px canon activity vs empty-hand,
   no oracle divergence inside the window), keep. Else drop per ticket rule
   "never keep a row that is not 0/0 with a non-blind negative".

**Why deferred**: tool budget (≤60) and capture budget (≤30) are tight; the
branch's prior tickets (T72/T74) both landed as "tool budget exhausted before
source change" with the next-step work named in the report. This ticket is the
same shape — ONE candidate is viable (not six), so the step 3 + step 4 + step 5
sequence becomes the entire ticket, not a subset, and the budget would be
exhausted before the keep-or-drop oracle (step 4) could be run with confidence.

## Step 5 — inventory.py + SCOPE (NOT run, no row added)

Per ticket: "the chips line must read `(43+K) / 411` with K = kept rows".
K = 0 (this ticket lands negative). No edit to tools/scoreboard.py,
tools/inventory.py, or docs/SCOPE.md; the chips line stays 43 / 411.

## Step 6 — guard set + repeat baseline (NOT run, no row added)

Per ticket: "Re-run step 1's two commands and the guard set
`wave,window,opening,chip-cannon,mettaur,cursor,windowclose`."
Since no row was added and no src/ edit was made, the ROM is identical to
step 1's, and verify_rows would reproduce the prior branch's results unchanged.
Skipped the re-run to stay inside the budget.

## What is unverified (for the next worker on this objective)

- Step 3-5 for chip-stepswrd (id 81). The next ticket should:
  1. Read src/battle.rs's STEP_GHOST_FRAMES and the afterimage lifetime, pick
     a frames count ≥ WideSwrd's 40 + afterimage.
  2. Add `("demo-stepswrd", "51", N, [])` to tools/scoreboard.py CHIPS.
  3. Update tools/inventory.py:281's `assert len(chips) == 43` → 44.
  4. Build, run `python3 tools/harness.py --only chip-stepswrd --no-gallery
     --ui isolated`. Empty-hand Δ: take an afterdissolve_0x0c capture on
     `/tmp/bn6f_sterile.gba` with no `--poke-at` press for ~N frames; pixel-diff
     vs the harness's run must be ≥64 px. Trace via
     `python3 tools/oracle.py chip-stepswrd --both` for the state-trace check.
  5. If PASS, regenerate docs/SCOPE.md via `python3 tools/inventory.py`;
     chips line 43 → 44 / 411.
  6. Re-run the guard set; ROM sha256 must equal
     `5b46337aa27da9ca881f2321fe1e210f54717285b4dfa5256c1680bac4b985ef`.
- The 84 unprobed ids leftover (38 named, 46 unnamed) are out of scope for the
  current asset — landing chip rows for any of them would require the asset
  (or a full ROM chip loader) to carry them, which is a separate ticket. The
  family-0x15 row is structurally closed for src/battle.rs's barrier arm
  (subfamily != 0x04), so the only family-0x15 row that could land under the
  same arm as Barrier/Barr100/Barr200 would be BblWrap (181) or LifeAur (182),
  both of which are 0x15/0x04 in ChipDataArr.s but absent from chips.bin.

## Ideas tried and dropped

- Adding `demo-stepswrd` to scoreboard.py and probing immediately: rejected —
  tool budget exhausted before the empty-hand Δ could be quoted (this ticket
  also fits the "next ticket does the probe" pattern, T19's explicit ruling).
- Adding a family+subfamily AS_DATA_FAMILIES rule to cover the 38 named
  family-0x15 ids: rejected — they aren't in chips.bin so the chip-<name>
  rows could never fire even if the generator were widened; also out of
  scope ("NOT `tools/harness.py`/`tools/states.py` (T71's), not
  `parse_backdrops`/`parse_formations`/`AS_DATA_FAMILIES` (T70's/T62's)").
- Loosening the asset gate: rejected — chips.bin is the canonical surface
  the game renders (T17/T19/T48); the 411-record table is the disassembly's
  bookkeeping, the 48-record asset is what the game actually loads.
