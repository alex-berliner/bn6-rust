# T30 — repair the stale file:line cites in `src/` provenance tags + the SPRITE_LOADER prose

Branch `wt/t30-src-cites` (worktree `/tmp/bnwt/t30-src-cites`,
`CARGO_TARGET_DIR=/tmp/ct_t30-src-cites`). Comment-text only; every rewritten site keeps
its original line count (see "line-count discipline" below for why this is load-bearing).

## Baseline (before any edit)

- `cargo build --release` + `tools/gbafix.py` on HEAD `79d3c17`: ROM sha256
  `435daeaf6eccd15e0dbef6f2a081d13d445fcebc7b2f7b2b063bdde7650c433e`, 583852 B.
- **The ticket's pinned sha `1997be3b4e8f463a…` does NOT match current main.** Forensics:
  - main (`79d3c17`) and this worktree build byte-identical ROMs (fresh target dirs,
    `cmp` clean) → build is deterministic.
  - `git diff bfd65b3..HEAD` (the commit that opened T30) touches only `TODO.md`; the
    buildable tree is unchanged since the ticket was written.
  - Building `546fc6e` (T29, the last commit whose log records `1997be3b`) **today
    reproduces `1997be3b…` exactly** → toolchain/environment unchanged and reproducible.
  - The ROM moved in **`fd1be73`** (T28 PARTIAL, "minimal first step"): it added 57 lines
    to `src/chips.rs` (the dead `read_chip_state` reader). The reader is dead-code
    eliminated (`nm` finds neither ELF containing it; both ELFs have identical symbol
    tables), but the raw binaries differ in **exactly 6 bytes** (binary offsets
    0x49c24/34/44/54/64/74, inside `.rodata`), each a `Location`-style line-number
    constant shifted by exactly **+57** — the lines fd1be73 inserted. So T28's landed
    claim "ROM byte-identical to T27 NEGATIVE landing" is FALSE, and T28's verifier's
    "ROM sha256 1997be3b on both sides" cannot have been a fresh post-merge build.
  - **Consequence for this ticket:** comment line-count changes are NOT safe — they shift
    `.rodata` panic-`Location` line numbers and move the ROM. Every T30 edit is therefore
    a same-line-count rewrite. (Found by burning one +2/+1 line round-trip before this
    was obvious; the ROM sha flipped and came back only when counts matched.)
- So the acceptance's real bar — "identical to main's" — is checked against
  `435daeaf…` (what main builds to); the literal `1997be3b…` in the ticket is stale.

## Verified replacement cites (read on disk before writing; grep -n is authoritative)

- `asm/asm31.s:111044` = `mov r0, #0xe` in the AI-attack routine — confirmed wrong.
  - Label `RecovHealBySubfamily_80EC870:` **asm31.s:111132**, body `.byte`s :111133-111134,
    reader block `sub_80EC844` at :111109ff (`ldr r4, off_80EC86C` :111115),
    `off_80EC86C:` :111130-111131, decomp `docs/decomp/asm31.c:86133`, ROM file offset
    `0x0EC870`. (Ticket's numbers verified as-is.)
  - ROM bytes `xxd -s 0x0EC870`: `0a00 1e00 3200 5000 7800 9600 c800 2c01 e803 0000`
    = [10,30,50,80,120,150,200,300,1000] + 2 pad — matches `RECOV_HP` byte-for-byte.
- `data/dat01.s:189` = `byte_8020AFC::` (the previous table) — confirmed wrong.
  - `BarrierHpByType_8020B2C::` **data/dat01.s:194**, labelled extent 0x020B2C..0x020B8C
    = 96 bytes = 16 stride-6 rows (the next label `byte_8020B8C::` starts at :203). The
    (hp u16, 0000, ffff) row shape covers rows 0..7 (asm lines :195-202); rows >= 8 change
    shape, so "8 rows" is where the HP pattern stops, NOT a label boundary.
    NOTE: T28's landing commit message says the label is at :195 ("194 is its .space") —
    that is off by one; `sed -n '194p'` is the label line. Stride 6 corroborated by
    `docs/decomp/asm00_2.c:16089` (`BarrierHpByType_8020B2C[6 * a1]`) and
    `asm/asm00_2.s:22591`/`:22602`. ROM file offset `0x020B2C`: bytes
    `0000 0000 ffff | 0a00 …` — row 1 = 0x0A, row 5 = 0x64, row 7 = 0xC8 — matches
    `BARRIER_HP`/`BARR100_HP`/`BARR200_HP`.
- `asm31.s:109878-109892` = tail of `loc_80EBED6`'s routine — confirmed wrong.
  - `VulcanShotsBySubfamily_80EBFEC:` **asm31.s:110022**, `.word 0xA050403` :110023,
    `off_80EBFE4:` :110018-110019, reader `ldr r2, off_80EBFE4` :109967, decomp
    `docs/decomp/asm31.c:85326`, ROM file offset `0x0EBFEC`. Bytes
    `0304 050a` = 3,4,5,10 — matches VULCAN1/2/3/SUPRVULC_SHOTS; the fan-offset word
    `08101820` sits at `0x0EBFF0` (`VulcanShotFanOffsets_80EBFF0` -- src/shot.rs cites it
    at :74, not :89 as this log first said, and that line still uses the dead name
    `dword_80EBFF0`).

## Repairs (old -> new), all comment text, all same-line-count

src/battle.rs:
- :498 `asm31.s:109878-109892` -> `asm31.s:110022` (doc prose)
- :502 tag `dword_80EBFEC = 0xA050403, asm31.s:109878-109892` ->
  `VulcanShotsBySubfamily_80EBFEC = 0xA050403, asm31.s:110022-110023 (label + .word;
  off_80EBFE4 at :110018-110019, reader ldr at :109967, decomp asm31.c:85326, ROM 0x0EBFEC)`
- :503-:506 tags (VULCAN1/2/3/SUPRVULC_SHOTS) -> `VulcanShotsBySubfamily_80EBFEC =
  0xA050403, asm31.s:110022-110023, ROM 0x0EBFEC`
- :4556 `asm31.s:109878-109892` -> `asm31.s:110022-110023` (in-battle doc prose)
- :644 `(asm31.s:111044)` -> `(RecovHealBySubfamily_80EC870, asm31.s:111132-111134, ROM 0x0EC870)`
- :645 tag `byte_80EC870, asm31.s:111044` -> `RecovHealBySubfamily_80EC870 (9 u16 + 2 pad),
  asm31.s:111132-111134, ROM 0x0EC870, decomp asm31.c:86133`
- :650 `byte_8020B2C, dat01.s:189` -> `BarrierHpByType_8020B2C, data/dat01.s:194, ROM 0x020B2C`
- :651 tag -> `BarrierHpByType_8020B2C row 1, data/dat01.s:194, ROM 0x020B2C, decomp asm00_2.c:16089`
- :654 `byte_8020B2C (data/dat01.s:189)` -> `BarrierHpByType_8020B2C (data/dat01.s:194)`
- :672 tag -> `BarrierHpByType_8020B2C, data/dat01.s:194 (rows :195-202), ROM 0x020B2C, decomp asm00_2.c:16089`
- :673/:674 tags -> `BarrierHpByType_8020B2C row 5 / row 7, data/dat01.s:194, ROM 0x020B2C`

## SPRITE_LOADER prose rewrite — nm evidence (current release build, HEAD tree)

```
02000000 00000100 D BATTLE_MARKER
02000100 000000a8 d _RNvNtCsfOjFFig40ml_3agb9interrupt15INTERRUPT_TABLE
020001a8 00000048 d _RNvNtNtNtNtCsfOjFFig40ml_3agb7display6object7sprites16sprite_allocator13SPRITE_LOADER
020001f0 000000b8 d _RNvNtNtNtCsfOjFFig40ml_3agb7display5tiled12vram_manager12VRAM_MANAGER
```
- 0x02000080..0x020000C0 is **BATTLE_MARKER's own tail** (bytes 128..192 of its 0x100
  reservation: marker+padding, fixture descriptor, TRC2 block, spare). agb's EWRAM data
  starts at 0x02000100 (INTERRUPT_TABLE); SPRITE_LOADER is at **0x020001a8**, not
  0x02000080. (Q3's worklog already recorded the 0x020001a8 address.)
- Rewritten sites (facts now true, conclusion kept — the block must be linker-placed
  *inside* the array; an absolute 0x02000080 write owns memory outside the array, and
  growing the array pushes agb's data forward instead of colliding):
  - main.rs array doc (:140-148): loader-begins-at-0x02000080 claim replaced with the nm
    ownership facts; kept the R6 clobber history (minus the false partner attribution)
    and the array-layout-guarantee argument.
  - main.rs :260 pointer and :264 ORACLE_OFFSET provenance tag: nm facts updated to
    BATTLE_MARKER owns 0x02000000..0x02000100 / agb's data starts at 0x02000100.
  - main.rs :274 and battle.rs :1782-1783: "collided with the sprite loader" ->
    "collided with memory it did not own (today's nm puts SPRITE_LOADER at 0x020001a8)".
  - battle.rs :1847-1848: "overlap agb's SPRITE_LOADER/interrupt table the way an
    absolute 0x02000080 address did in R6" -> names the real neighbours with addresses.

## After measure

- `cargo build --release` + gbafix: `435daeaf…` — `cmp` vs main's ROM: identical.
  (Not `1997be3b…`; see baseline section — that hash died with fd1be73.)
- `python3 tools/verify_rows.py wt/t30-src-cites chip-recovery,chip-barrier,chip-use,cursor,field,tiles`:
  **PASS 6/6 --** chip-recovery 0/0/40/1731, chip-barrier 0/0/80/41067, chip-use 0/0/30/7768,
  cursor FAILED 1/1/170/186279 (the known tear, ticketed), field 0/0/40/1139, tiles 0/0/8/12197
  -- every number identical to the ticket's main expectations.

## Unverified / would try next

- The exact clobber partner of R6's absolute 0x02000080 write is not re-derivable from
  today's build (the prose's "sprite loader" attribution was the stale part; I did not
  dig the R6-era ELF). Everything I could not re-attest, I did not assert in the prose.
- All three tables' values re-attested from the pinned ROM bytes at the named offsets —
  no unattested value remains in the touched tags.
