# T124 — delete the fitted seam pad (pad 0 was in class on three footprints)

Worktree: /tmp/bnwt/t124, branch wt/t124 (from main 9df624f).
Method per ticket: delete SEAM_PHASE_PAD_ITERS and its loop; measure the full
row set before/after; then read cursor on wt/T117 and wt/T112 with the deletion
applied on top, plus a deliberate +-1 KiB inert delta on main.

## Step 0 — baseline (no edit, clean checkout at 9df624f)

ROM sha256 c1ead4cdb7e154508ac9c7611ea1ae72ad4131aa3dfbf81092709a28d0679915,
592752 B. fitted 17 / derived 436 / peeked 146.

```
opening        isolated PASS  total 0    worst 0    frames 40  | neg not blind (86591)
mettaur        isolated PASS  total 0    worst 0    frames 70  | neg not blind (41734)
cannon         isolated PASS  total 0    worst 0    frames 40  | neg not blind (9505)
buster         isolated PASS  total 0    worst 0    frames 28  | neg not blind (3167)
buster_charge  isolated FAILED total 2498 worst 188  frames 32  | neg not blind (5113)   <- known main residue, T117's branch fixes it
chip-use       isolated PASS  total 0    worst 0    frames 30  | neg not blind (7768)
wave           isolated PASS  total 0    worst 0    frames 90  | neg not blind (3840)
cursor         isolated FAILED total 1   worst 1    frames 170 | neg not blind (186279) <- in class (k=97 1 px)
windowclose    isolated PASS  total 0    worst 0    frames 40  | neg not blind (207166)
popup          isolated PASS  total 0    worst 0    frames 80  | neg not blind (1288)
emotion_syn    isolated PASS  total 0    worst 0    frames 40  | neg not blind (644)
emotion_face_b isolated PASS  total 0    worst 0    frames 40  | neg not blind (644)
emotion_skip   isolated PASS  total 0    worst 0    frames 40  | neg not blind (644)
```

## Step 2 — the pad deleted (uncommitted src delta, measured 2026-09-19)

SEAM_PHASE_PAD_ITERS (src/main.rs:322) and its VBlank closure deleted
entirely: no user interrupt handler registered, `commit()` runs at the
interrupt's natural phase. Rebuilt, full 13-row set:

```
ROM sha256 b5b59a0876b407afc541dba035cb85ab878da8f503e5f100b6b349821a9bdd7d, 592544 B
fitted 16 / derived 436 / peeked 146            (17 -> 16, derived/peeked unchanged)

opening 0/0/40 | mettaur 0/0/70 | cannon 0/0/40 | buster 0/0/28
buster_charge 2498/188/32 (byte-identical to baseline) | chip-use 0/0/30
wave 0/0/90 | windowclose 0/0/40 | popup 0/0/80
emotion_syn 0/0/40 | emotion_face_b 0/0/40 | emotion_skip 0/0/40
cursor  31/25/170 | negative 186277 (was 186279)   <-- OUT of class
```

The pad-0 premise dies on today's main. T118b measured pad 0 = 0/1 on three
footprints, but at base a8bd3d3 — before T119's rank/zenny code re-rolled the
footprint. On 9df624f, pad 0 reads 31/25 while pad 17 reads 1/1: the
in-class integer moves with the binary, and there is no integer (0 included)
that holds across a footprint change. T118b's 0/1 column was its base's
coincidence, not a mechanism.

## Step 4 (four-footprint table) — skipped on the coordinator's call

The table's premise ("pad 0 is in class on all three") is already refuted on
main itself; the remaining three reads cannot rescue a premise that fails on
footprint one. Not spent.

## Step 3 of the close-out — pad restored

src/main.rs restored byte-identical to main 9df624f (the deletion was never
committed, so the restore is `git checkout` — branch tip carries only docs).
Confirmation read, ONE capture: cursor 1/1/170, negative 186279, fitted
17/436/146, ROM sha c1ead4cdb7e154508ac9c7611ea1ae72ad4131aa3dfbf81092709a28d0679915
592752 B — identical to main's. The 31/25 was the deletion, not machine
drift; the pad is load-bearing on this footprint and stays.

## Next ticket's spec (the one measurement the fix needs)

The seam is a PLACEMENT problem, not a volume problem (T118: the
changed-chunk copy fits inside vblank on every one of 170 frames) and not an
integer-pad problem (T118b's LY anchor, T124's deletion). The missing number
is canon's own mid-frame write scanline, obtained with `--watch-write` on the
screenblock during the cursor scenario (canon side). Only after that number
exists is a timed writer worth writing.

Prior art to read first — failed mid-frame placement attempts, do not repeat
them blind:
- F35b / T21: the earlier placement attempts the timed writer was blocked on
  (T118's worklog names them; the delta to place is now much smaller).
- F37j (NEGATIVE, kept, 2026-09-15 review): a mid-frame placement attempt.
- T118's VRAM hazard: the screenblock copy path must write u16/u32 only
  (GBATEK: an 8-bit store lands in both halves of the halfword; the first
  census build wrote bytewise and exploded cursor to 5.8M total).

## Unverified

- Whether ANY setting survives a footprint delta beyond ~4 KiB — untestable
  while the in-class value itself moves with the binary.
- What sets the surviving k=97 1 px tear at pad 17 (the historical F3
  sub-frame seam): canon's own mid-frame write scanline has still never been
  timestamped; `--watch-write` on the screenblock is that job, and it is the
  next ticket's first capture.
