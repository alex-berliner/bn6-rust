# T121 — M5: wire the ai-index-4 spawn and port its think arm off_810B2D0

Branch `wt/t121-ai4`, worktree /tmp/bnwt/t121-ai4. Carries T113's 477686-px row.

## Step 1 — cherry-pick + baseline row (measurement only)

Cherry-picked T113's row+inventory commit 2e3c619 -> a569948 (docs/coverage/ai4.md,
tools/harness.py ai4 row, tools/inventory.py identity keying; its worklog commit 447f403
NOT picked — docs/worklog/T113.md already on main).

Baseline run against today's src (expect 477686/38237/40, negative 522293):

477686/38237/40, negative non-blind 522293 — byte-exact match to T113's
numbers on the first run (determinism re-confirmed x1; T113 measured x3).

## ROM re-reads (this ticket, direct from /tmp/bn6f_real.gba)

- byte_80182C4+3*0x13 = `00 00 04` (Version 0, ActorType 0 virus, AIIndex 4);
  controls idx 0x01 = `00 00 01`, idx 0x85 = `00 00 17` — all three re-verified.
- `off_8109150[4]` row-0 elem_hp at 0x0810ae4c = `5a 00` = 0x005a (stride-6 row
  `5a 00 | 00 04 1e 00`) — the real default the 0xffff sentinel must become.
- `AIThinkTables_8109050`+0x10 = `d0 b2 10 08` = off_810B2D0 ✓; +0x04 =
  `f4 9e 10 08` = ForMettaur_8109EF4 ✓ (dispatch is ai_index*4).

## Construction-path audit (Do step 2, as the code stands)

- `src/battle.rs` fixture loop (the :2142/:2148 call site): EVERY fixture
  enemy gets `spr::Assets::new(METTAUR)` art + `ai::Style::Mettaur`, and
  `fixture.rs`'s `kind_of()` is read by nothing. AI4_ROW's enemy_kind=0x09
  (slot0 kind 1 Gunner, slot1 kind 2 ai-4) decodes but is ignored.
- HP: harness packs `enemy_hp` default 0 (harness.py:164-168), but AI4_ROW and
  GUNNER_ROW write 0xFFFF explicitly; battle.rs maps 0 -> METTAUR_HP(40) and
  passes 0xFFFF RAW -> the trace exports 0xffff where canon holds the kind's
  own Struct2 default (ai-4: 0x005a; gunner: 0x003c per its live record).
- Export slot mismatch found and fixed by descriptor: the trace watches canon
  0x0203ab60/ab68/ab84 = the AI-4 (canon's SECOND real enemy; 0x0203aa88 is
  the Gunner, T87's slot0), while our export is `enemies.first()`. So the
  ai-4 kind must sit in slot 0 of AI4_ROW: enemy_kind 0x09 -> 0x06 (slot0
  kind 2 at panel (6,3), slot1 kind 1 at (6,1)) — same two cells, ai-4 first.
- Export action byte: `Actor::oracle_fields` hardwires the enemy idle to 0x0a
  (canon's METTAUR idle = the hop executor's Unk_00=0 step). Canon's ai-4
  idles at CurAction 0x00 (RunSpawnAnimationMaybe_8016380): T87 measured
  0x0004@69 -> 0x0104@151, the whole row window canon 80..119 sits at 0x0004.
  Fix: parameterize the two enemy idle arms with a per-style byte
  (`Ai::oracle_idle_action()`: Mettaur/Gunner 0x0a unchanged, Ai4 0x00).

## Think arm read (Do step 4 map, off_810B2D0)

All 15 slots are phase/stage-indexed dispatchers. Only slot 0 is reachable in
the row's window (canon CurAction 0 throughout):

| idx | handler | cite | status |
|-----|---------|------|--------|
|0x00| RunSpawnAnimationMaybe_8016380 | asm00_2.s:16035-16052 (+ phase tables off_80163A8: sub_80163B4/materializeObject_801641A/sub_8016460; off_80164B4: sub_80164C0/sub_8016520/sub_801657E) | ported: the spawn/idle hold — our Actor's own spawn timeline (Hidden/Appearing/Idle) stands in for the phase machinery; the entry holds CurAction 0 |
|0x01| sub_80165B8 | asm00_2.s:16373-16380 (CurAction=8, CurPhaseAndPhaseInitialized=0) | ported as the exit mapping, NOT armed: canon fires it at capture 151, outside every compared window; the arming timer is unported |
|0x02| sub_810B338 | asm31.s:173663-173676 | named unported |
|0x03| sub_80166AE | off_810B2D0[3] | named unported (flinch family arm; shared 0x03 export kept) |
|0x04| sub_8016B02 | off_810B2D0[4] | named unported |
|0x05| sub_8016CE8 | off_810B2D0[5] | named unported |
|0x06| sub_8016B36 | off_810B2D0[6] | named unported |
|0x07| sub_8016B72 | off_810B2D0[7] | named unported |
|0x08| sub_810B350 | asm31.s:173678-173693 (oAIState_DecideState 2-way off_810B364: sub_810B36C/sub_810B3CC) | named unported — the family decision loop; the window never reaches it |
|0x09| genericAI_exitAttackStateAfterDelay_81097BA | off_810B2D0[9] | named unported |
|0x0A| sub_8109804 | asm31.s:170226-170241 (AttackStage 2-way off_8109818: sub_8109820/sub_810986E) | named unported |
|0x0B| sub_810B042 | asm31.s:173274-173287 (1-arm stage off_810B054: sub_810B058) | named unported |
|0x0C| sub_810B06A | asm31.s:173301-173318 (5-arm stage off_810B07C) | named unported |
|0x0D| genericAI_movementRelated_8109952 | off_810B2D0[13] | named unported |
|0x0E| sub_810B172 | asm31.s:173443-173460 (5-arm stage off_810B184: sub_810B198/1F4/244/270/2AE) | named unported |

## Ideas tried and dropped

- Mapping kind 1 (Gunner) slots to the existing `gunner` port: NOT done —
  GUNNER_ROW (guard set) packs kinds 1,1; the gunner-style dispatch would
  change that row's recorded 2105613 px, breaking "guard set byte-identical".
  Kind 1 keeps Style::Mettaur this ticket; the re-baseline is its own ticket.
- Porting the 0x00 phase tables as fresh machinery: dropped as re-creation —
  our Actor's shared spawn timeline already reproduces the materialize
  (mettaur-row trace zero); the entry documents the mapping instead.
- Per-kind HP defaults for kinds 0/1: dropped — FIELD_ROW pins 0xffff
  literally (demo-field's FIELDMATCH_HP) and GUNNER_ROW's 0xffff is frozen
  with its row; ONLY the ai-4 sentinel becomes its Struct2 default.

## Step 2 — the 0xffff sentinel becomes the kind's Struct2 default (commit 2)

`AI4_HP = 0x005a` (off_8109150[4] elem_hp) substituted when the descriptor
leaves the 0xFFFF unset halfword AND the slot decodes to KIND_AI4; kinds 0/1
keep the raw sentinel (FIELD_ROW's 0xffff = demo-field's FIELDMATCH_HP
literal; GUNNER_ROW's residue frozen with its number).

Measurement: the ai4 row is **byte-identical** to baseline
(477686/38237/40, neg 522293) — mechanism: the current descriptor puts the
ai-4 kind in slot 1 and only `enemies.first()` (slot 0, kind 1) is rendered
into the enemy HP box and exported to the trace, so the substituted value
has no surface yet. It gains one in step 3's slot reorder. Derived count
436 -> 438 (AI4_HP + KIND_AI4).

## Step 3 — the kind wired through the construction site (commit 3)

- `fixture.rs`: KIND_AI4 = 2 (identity bytes cited); `battle.rs` fixture
  loop: per-slot `kind_of` -> HP (step 2) + `Style::Ai4` for kind 2; kind 1
  deliberately STAYS Style::Mettaur (gunner dispatch would move GUNNER_ROW's
  frozen 2105613). Art stays METTAUR for every kind (only the Mettaur's
  sprite is ported; canon picks each NameID's sprite at spawn) — the field
  band owns that residue. `ai.rs`: Style::Ai4 + Ai4Entry (slot-0 hold) +
  `Ai::oracle_idle_action` (Mettaur/Gunner 0x0a unchanged, Ai4 0x00);
  `actor.rs`: the two enemy idle arms of `oracle_fields` take the byte as a
  parameter (Mettaur rows' export unchanged). `harness.py`: AI4_ROW slot
  order swapped (enemy_kind 0x06, panel_row [3,1]) so enemies.first() = the
  ai-4 = canon's traced slot 0x0203ab60; compiler-forced Ai4 arms in
  battle.rs's two style matches + objects.rs:enemy_think (one arm each,
  cited).
- MEASURED: ai4 477686/38237/40 neg 522293 -> **488098/38237/40 neg
  533114** (worst unchanged = the canon fade tail k=0..5; the style change
  moved the mid-band behaviour). gunner **2105613 byte-identical** (isolated
  AND integrated) — the kind-1 control holds. mettaur/cannon/buster/
  chip-use/wave isolated 0; windowclose/result/popup 0; chip-use integrated
  EXACTLY its allowlisted 275307.
- **TEAR NAMED**: cursor 1/1/170/186279 -> 50/31/170/186289 and
  opening-integrated 0 -> 18740, buster-integrated over its 28000 cap —
  the F12/T112 binary-shift class. NO pad re-fit is allowed by this
  ticket; the true pre-change guard numbers on THIS footprint are being
  measured next (baseline run at the cherry-pick commit), then the code
  shape gets reworked until cursor returns byte-identical WITHOUT the pad.

## Baseline guard run (the true before, at cherry-pick a569948, own worktree)

opening-integrated **18740/647/40** and buster-integrated **54672/12977/28**
are the PRE-EXISTING state of this footprint (byte-identical before AND after
my change — NOT my regressions; T119's guard list didn't include them).
Every row and every negative is byte-identical before/after EXCEPT ai4 (the
subject) and cursor. chip-use integrated lands EXACTLY on its allowlisted
275307 both sides.

## The cursor tear (named, not cured)

baseline 1/1/170/186279 -> step-2 binary 10/9/170/186276 -> step-3 binary
50/31/170/186289 -> step-4 binary 227/168/170/186444. This is T118's
mechanism exactly ("genuinely phase-fragile — any footprint re-roll of the
whole-copy phase breaks it"; "no src/ landing is safe from cursor/windowclose
phase re-rolls"): the ≤1/170 class was a write-phase coincidence of the
screenblock copy, and code-size changes re-roll it. The documented cure is
F12's pad ladder (128 -> 160, the one-line next rung) — this ticket's step 5
says "no pad re-fit", and my rules say don't widen a change to make a number
move, so the tear is reported, not papered over. Shapes tried: HP-only
(tore 10/9), full wiring (50/31), +indexed-port doc (227/168) — each
re-roll moves it; no zero-delta shape exists for a change that adds
behaviour. The coordinator owns the pad decision.

## Step 4 — the think arm ported by handler index (commit 4)

Ai4Entry is now the indexed dispatch: 0x00 ported (the hold; phase tables
cited), 0x01 mapped-never-armed (CurAction=8/phase=0, cited), 0x02..0x0E
named unported with their sub-table cites (see the struct doc + worklog
table at step 1). MEASURED: ai4 488098/38237/40 neg 533114 — byte-identical
to step 3 (the port's observable behaviour in-window IS the hold).

## Step 5 — trace + close-out (NOT fully run: tool budget exhausted)

Step 5's trace (enemy_state_action/enemy_anim/enemy_hp, canon watches
0x0203ab68:2 / 0x0203ab70:1 / 0x0203ab84:4 vs our TRC2 block, --align
row:ai4) did NOT run: tools/trace.py's record is keyed on
states.TRACE_SCENARIOS and "ai4" is not registered (tools/states.py is
outside this ticket's allowed files), and the hand-rolled capture+diff did
not fit the remaining tool budget (hard stop at 80). T113's "before" is
recorded in its coverage doc: state_action k=0 (canon 0x0004 vs ours
0x0a04), enemy_hp k=0 (canon 0x005a vs ours 0xffff), anim unwatched. The
AFTER numbers are the follow-up's first command — the port's in-window
observable (slot-0 Ai4 exports 0x0004 idle, HP 0x005a from AI4_HP) predicts
state_action/hp first divergence NONE; verify_rows from a clean checkout
also NOT run (same budget stop) and is the veto.

## Close-out

- Steps 1-4 measured and committed (43f5fad, bbbdd8e, 4a341ea, eac74fd +
  the cherry-pick a569948).
- Predicted-vs-actual: the ticket predicted cursor byte-identical with no
  pad re-fit; cursor re-rolled (T118's phase mechanism — quoted in the
  tear section). The pad ladder's next rung (128 -> 160) is the documented
  one-line cure and belongs to the coordinator.
- Would try next: (1) the trace after/verify_rows; (2) cursor after the
  pad rung; (3) kind 1 -> Style::Gunner wiring once GUNNER_ROW's residue
  is re-baselined by its own ticket; (4) the family decision loop
  (off_810B364 arms) if M6 wants the Navi ranks to fight.

## Pad sweep (supervisor-authorized, 2026-09-18) — option (b), none restores the class

POPUP_GATE_PAD tried at 160, 144, 176, 192 (≤2 captures each; cursor row only):

| pad | cursor (isolated) | negative |
|-----|-------------------|----------|
| 128 (landed) | 227/168/170 | 186444 |
| 160 | 18/12/170 | 186278 |
| 144 | 46/43/170 | 186276 |
| 176 | 43/37/170 | 186277 |
| 192 | 56/37/170 | 186289 |

None reads the exact main class (1/1/170/186279). Per the supervisor's hard
fallback the pad is REVERTED to the landed 128-byte form (final binary =
step-4's, sha256 5457c8fad76948425a8393afded838f72c717cd97a2e507089e6c41d65563e04,
5,658,004 B) and the pad re-fit is routed to a follow-up. The five tuples show
the phase moving continuously with pad size (the T118 mechanism is real); the
1/1 basin is narrower than the 16-byte grid or sits off this ladder's axis —
a follow-up with a wider sweep (or the timed-writer cure T118 names) owns it.

## Residue split at close (PARTIAL)

Per the ticket's close path: step-1's split stands as the residue split —
top (y0-31, HUD/HP) 100174, mid (y32-127, field sprites) 317084, bot
(y128-159) 60428, out of 477686 at the pre-port binary. After the port the
row total is 488098 (worst still 38237 = the canon fade tail k=0..5); the
per-region AFTER split was not re-measured (capture budget) — mid-band still
owns it by construction (art family gap: no ai-4/Gunner sprite port). First
divergent trace field named: enemy_state_action k=0 (canon 0x0004 vs our
pre-port 0x0a04; the port's export maps our ai-4 to 0x0004 — the AFTER trace
did not run, unverified). Unverified, per the ticket: the other five ai-4
ranks v1..v5 (enemy_idx 0x14..0x18) and whether 0x113..0x118 really fields
as a Navi — that is M6's own scenario.
