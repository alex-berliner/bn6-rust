# T122b worklog — buster_charge's last frame: the k=0 release edge

Ticket: turn buster_charge isolated 188/188/32 (with wt/T117's flash cherry-picked) into 0/0/32 by
landing the charged release on canon's frame (CurAction 0x10 written ON canon capture f130; ours one
frame later). Files allowed: src/battle.rs (charge-release + Update::Strike arms only),
docs/coverage/charge-gauge.md, this log. NOT: tools/harness.py, src/charge_shot.rs, src/main.rs
(T118's; the coordinator authorises taking T117's flash base f3c76d6, which touches src/main.rs +
assets — noted in report).

## Facts distilled from the inherited context (all from commands run on main/worktree 5cccf09)

- Row def: tools/harness.py:2389 `buster_charge`, ui=both, frames=32, Align(canon_ref=130
  (CHARGE_RELEASE_FRAME), search 198..212). T106 measured alignment offset 207.
- Canon side: sterile + AFTER_DISSOLVE state (/tmp/buster_charge.state), B held via per-frame cheat
  on oAIData_JoypadHeld 0x020340a2, release = one-shot poke of released word 0x020340a6 at frame
  130. Measured (T106): CurAction 0x0203a9b9 goes 0x08 -> 0x10 ON capture frame 130, holds
  130..159, back to 0x08 at 160 (30-frame charged attack).
- Rust side: B held from battle-frame 100 for 105 frames, release at 205 (predicted event lock 205,
  origin 8 -> capture 213).
- T117 (wt/T117 @ f3c76d6, PARTIAL, unmerged) ported the flash: buster_charge 2498/188/32 ->
  188/188/32, negative 5113 -> 3167. Flash = one wide 16x8 OBJ, pri 2, pal 2 (pal 1 first frame),
  canon f136..160 = k=6..30; CHARGE_FX_DELAY=8 armed at attack_charged() (NOT at Strike — Strike
  fires at k=7, one tick after canon's flash first frame k=6). delay 2 -> flash first OAM at k=0,
  delay 8 -> k=6..30. So our release-to-OAM pipeline is 2 ticks longer than canon's.
- T117 measured positions (74,78)/(72,78)/(73,78)/(74,78); T117's pending doc corrections to
  charge-gauge.md: measured x-range 74..88 (doc says 73..87 / 66..82), pal/pri attribution line,
  hygiene counts dated to base 261ee30.
- k=0 transient 188 px, navi sprite area y58..121 x33..94, OBJ only (both sides --disable-bg).
- T118: NEGATIVE (content-driven VRAM copy reverted on branch); cursor 1/1/170 neg 186279 is the
  veto class on main (pad SEAM_PHASE_PAD_ITERS=25 since T130). T124: no stable integer exists for
  the seam; per cursor.md the tear's frame moves with ROM layout and is reported, not chased; T130
  authorises a one-integer pad re-fit in src/main.rs per landing if cursor moves off 1/1.
- T117's pad was 16 on ITS branch; main is now at pad 25 (T130) -> cherry-pick of f3c76d6 will
  conflict in src/main.rs; keep main's pad 25.
- fitted count must not rise: main prints 17 (derived 434, peeked 146).

## Plan
1. Cherry-pick f3c76d6 onto wt/t122b, resolve src/main.rs keeping pad 25. Build. Baseline:
   buster_charge isolated (expect 188/188/32, neg ~3167) + buster 0/0/28. Record ROM sha256+size.
2. Step 1 measurement: k=0 188-px inventory (layer/position/OBJ entries, owning frame pair).
3. Step 2: canon order reading (sub_8011CB4 asm00_2.s:5827, 6339-6360 + caller; csrc.py).
4. Step 3: port the edge in src/battle.rs.
5. Steps 4-5: re-run buster_charge (0/0/32, neg non-zero), buster 0/0/28, 8-row guard set, cursor.
6. verify_rows PASS from clean checkout; docs corrections; commits per step.

## Step 0 — cherry-pick + baseline (MEASURED)

- Cherry-picked f3c76d6 (wt/T117 flash) onto wt/t122b as a7470ec; resolved src/main.rs conflict by
  KEEPING main's SEAM_PHASE_PAD_ITERS=25 (T130's re-fit; T117's 16 was for T117's own binary).
- Baseline buster_charge isolated **FAILED total 188 worst 188 frames 32 | negative: not blind (total 3167)**;
  fitted constants 17 (derived 436, peeked 148).
- ROM /tmp/bn-8ceff0e6/h_rom_.gba sha256 2ea554ba7f93ce19d6c9b460cbaa84544316fe6735da8135705c9fda64fa95fb,
  590848 B.

## Step 1 — the k=0 188-px inventory (MEASURED, ad-hoc captures 1-2: rust+canon, /tmp/t122b/)

Row alignment: canon_ref=130 (CHARGE_RELEASE_FRAME), search band 198..212; totals per offset
{198:10325 ... 206:3099, **207:188**, 208:3099 ...}: unique minimum at offset 207, rust origin 8
→ pairing canon f130+k ↔ rust f215+k. Only k=0 differs (188 px; k=1..31 all 0).

- 188 px are OBJ-only (both sides --disable-bg), bbox **x33..94, y58..121** (the navi sprite).
- OAM at the pair: canon OAM0 vs rust OAM0 — SAME attr0/attr1 (0x0015, 0x4025: same position and
  shape), attr2 differs **0xb200 vs 0x60a6** (canon: tile bank 0/pal 3 = the charged-attack pose's
  first art frame; ours: tile 166/pal 6 = the held art). So one OBJ entry (the navi body) differs
  in tile+palette only.
- Art timeline (consecutive-frame pixel events): canon **f129 +252 (aim art), f130 +510 (attack
  frame 0, ONE frame), f131 +188 (held art)**, flash at f136. Ours: **f213 +252 (aim art, same
  event), f214 +434 (held art directly)**, flash at f221. Our held art == canon's held art
  pixel-exactly (canon f131 vs rust f214..220 = 0 px; canon f131 vs rust f216 = 0).
- So the residue is NOT a global one-frame lag of the whole pipeline: our tail events (flash 221,
  bolt 224, recoil 225..228) pair with canon's (136, 139, 140..143) exactly under offset 207 —
  i.e. canon's k timeline maps onto our release+2. The ONE missing thing is canon's one-frame
  attack-pose art frame 0 at canon f130: canon draws [aim@rel-1(+252), frame0@rel(+510),
  held@rel+1(+188)]; ours draws [aim@rel(+252), held@rel+1(+434)] and never renders frame 0.
- CurAction watch (0x0203a9b8:2 on canon): byte+9 (CurAction) reads 0x08 → **0x16** (not T106's
  0x10!) from the release frame, 30 frames, 0x08, then 0x11 at f171 (a plain-buster re-fire: the
  one-shot released poke leaves the released word set). Our oracle mm_state_action (field 12)
  exports 0x0804 → 0x0B04 (Attacking=0x0b per actor.rs:699) at battle 212.
- Rust-side direct watch of 0x0203a9b8 reads all zeros — our port keeps no fake object struct
  there; the oracle block (0x02000008) is the only export.
- Canon release frame: the poke lands at capture f130 and the action+pose change is visible from
  dump 129/130 (±1 frame of dump-point ambiguity); T106's "ON frame 130" stands for the event,
  and the row's canon_ref=130 is that event.

Implication for the fix: inserting frame 0 at OUR held-art start (release+1) while keeping every
tail event fixed makes offset 207 pair canon f130 (frame0) ↔ rust f215 (frame0) = 0 with the
flash still at k=6 — CHARGE_FX_DELAY can stay 8. Widening/moving anything else would break k1..31.

## Step 1 continued — the mechanism, pinned by OAM + pixel components (MEASURED, same 2 captures)

OAM watches (0x07000000:0x400, both sides) and component analysis of the consecutive-frame diffs:

- The navi BODY (OAM entry 0, attrs 0x0015/0x4025, tile 0xb200 canon / 0x60a6 rust) never changes
  art in this scenario on either side — canon f131's body == canon's idle art; ours too (rust f214
  == rust f100 (our idle) == canon f131, all 0 px). The body is a red herring in the old notes.
- The moving parts are the ARM/GUN object and the GLOW:
  - canon f129: aim-arm parts (attrs 8052/0047, 405e/2037, tiles 18xx) + glow.
  - canon f130 (release, row k=0): glow GONE, ATTACK-arm parts appear (attrs 4066/0041, 0066/4029,
    0046/8031, 4064/802a, tiles 08xx) — AND the aim-arm parts LINGER one frame (reverted to their
    f128 cell, attrs 005a/b040/182f etc., entries 1-4, while the attack arm is entries 5-8). One
    frame of BOTH arms.
  - canon f131: aim arm gone; attack arm only. The 188-px k=0 diff is EXACTLY this one frame of
    lingering aim arm (component lists of canon130→131 and canon130↔rust215 are identical:
    15px@(74,81,66,76), 15px@(71,85,86,88), 15px@(46,53,103,113), 15px@(42,56,91,93), 13px@(82,90,103,110)).
  - ours: capture 214 = glow GONE + attack arm ON + aim arm REMOVED, all in the SAME frame
    (+434: 318px@(50,77,68,111) = glow art, plus the arm swap). Capture 213 = our release frame
    (charging/glow cell +252 at (54,73,80,99) — the same +252 event canon has at f129).
- So the port's defect is NOT "our export block one frame later" (our CurAction/anim export 0x0B04/
  anim 0x0e lands at battle 212, five frames after our release — that oracle skew is real but
  pixel-invisible because the body art is idle-equal on both sides). The pixel-visible defect is:
  **canon keeps the aim-pose arm object for ONE frame after the attack arm appears; our port drops
  it on the same frame.** Ours also merges glow-death + arm-swap into one frame where canon splits
  them (glow dies at canon f130 = release, arm lingers to f131) — the glow death timing is
  pixel-invisible in the window (both glowless by k=0).
- Frame bookkeeping (all measured): our release is processed at battle 206 (capture 214) — the
  script B@108..B@212 events run one capture frame behind the note's "release at 205"; oracle
  begin() (action 0x0B, anim 0x0e) at capture 220 = battle 212; flash (CHARGE_FX_DELAY=8 armed at
  release) at capture 221 = k=6 ✓; bolt at capture 224 = k=9 ✓; recoil events 225/226/228 = canon
  140/141/143 ✓. Tail alignment is exact; ONLY the aim-arm linger is missing.
- T106 correction (measured on this row's canon capture, watch 0x0203a9b8:2): canon's charged-attack
  CurAction (0x0203a9b9) goes 0x08 → **0x16** (not 0x10), holds ~30 frames, then 0x08, and 0x11 at
  f171 (a plain-buster re-fire: the one-shot released poke leaves the released word set). The
  0x0203a9b8 halfword is (CurState=0x04, CurAction) — and our port keeps NO fake object at
  0x0203a9b0 (reads all zeros; the oracle block 0x02000008 is our only export).
- Row's alignment: canon_ref 130, search 198..212, unique minimum at offset 207 (totals
  206:3099, 207:188, 208:3099), rust origin 8 → canon f130+k ↔ rust f215+k.

## THE FIX (for the next worker — not landed; tool budget ran out first)

In src/battle.rs, the charged release path must keep the aim-arm/gun object visible for exactly one
frame after the attack arm appears (canon f130 ↔ our aligned k=0), then drop it — the same-frame
swap is correct for the PLAIN buster (its row passes 0/0/28 with it), so the linger must be
charged-only (gate on the charged release site / charge_fx_in's arm, not on the shared arm code).
Everything else in the row is already exact; a correct linger should make offset 207 read 0 with
k=1..31 untouched and the flash still at k=6 (CHARGE_FX_DELAY stays 8). Do NOT touch the
glow-death timing (invisible in the window) and do NOT shift the release (our release frame is
already aligned to canon's — the art events coincide at k=0 except the linger).
Candidates read but not edited: the arm is `spr::Player(BUSTER_ARM)` (battle.rs:2552), plain-path
removal via BUSTER_ARM_DELAY/BUSTER_WINDUP; the charged path's arm state handling sits with the
`Update::PoseBegun | Update::Strike` gun.play(1) block (battle.rs:3690).

## Where this ticket stops

Steps 3-5 (the code change, row re-runs, guard set, verify_rows) NOT started: the session's tool
budget was consumed by the k=0 inventory. Baseline and step 1 + 2's first half are committed here;
coverage/charge-gauge.md corrections still owed (x-range 74..88, pal/pri line, hygiene counts).
