# T230 — rebase wt/t225 onto post-T216 main; re-find the pad plateau; land the blink

## Context
- main f5e6927 = T215 dca956a + T220 0033d2d + T216 f440247 (SEAM_PHASE_PAD_ITERS 25->21, blind_met row).
- wt/t225 = a2a4bdb + 0da47e6 (emotion blink port: countdown byte 0x0203528F gates the draw,
  descriptor +64 blink_countdown, emotion_skip negative; pad re-fit 20 on the OLD tree).
- T225's landing attempt 2026-09-18 conflicted with main in `src/main.rs` (pad iters) and
  `tools/harness.py` (T216 pokeoff/negative_canon vs T225 negative_rust machinery).

## Step 1 — merge (done, commit d7dc490)
- Conflicts resolved keeping BOTH sides' semantics:
  - tools/harness.py: Check gains both `negative_canon` (T216) and `negative_rust` (T225);
    run_check: `pokeoff` branch (T216) then `elif ui in negative_sides` (T225) then else.
  - src/main.rs: pad iters parked at 21 pending the sweep below.
- Auto-merged cleanly: src/battle.rs (the `if self.hud_live { self.emotion.update(); }` block
  + descriptor +64 arg), src/emotion.rs, src/fixture.rs (FIXTURE 66), docs/coverage/emotion.md.
- Cite-fix per ticket: sub_801CB38's eStruct+0xe stores are asm00_2.s:27442/:27478/:27485
  (verified in canon: `strb r1, [r5,#0xe]` at each), NOT :27343-27344 (that is an unrelated
  `strh` loop in loc_801CAAE). Fixed in tools/harness.py:3625 region (emotion_skip comment).

## Step 2 — pad sweep on the merged tree (candidates 19..25, cursor row each)
Target line (T216/T225 plateau): `cursor isolated FAILED total 1 worst 1 frames 170 | negative: not blind (total 186279)`

| pad | result |
|-----|--------|
| 21 (merge baseline) | cursor isolated FAILED total 1 worst 1 frames 170, negative 186279 — exact target line |

(sweep continues below)

Sweep result (merged tree, cursor row, `python3 tools/harness.py --only cursor --no-gallery`):
| pad | cursor isolated |
|-----|-----------------|
| 19  | FAILED total 766 worst 728 frames 170, neg 187004 |
| 20  | FAILED total 1 worst 1 frames 170, neg 186279 -- EXACT line |
| 21  | FAILED total 1 worst 1 frames 170, neg 186279 -- EXACT line (merge baseline) |
| 22  | FAILED total 1 worst 1 frames 170, neg 186279 -- EXACT line |
| 23  | FAILED total 16241 worst 3385 frames 170, neg 170163 (T225's old-tree 23/21 value reappears) |
| 24  | FAILED total 6 worst 6 frames 170, neg 186280 |
| 25  | FAILED total 1 worst 1 frames 170, neg 186279 -- EXACT line |

Plateau {20,21,22} (+25) of width >=3. Pinned **21** (commit 7c98583): mid-plateau and T216's
value, so the pad is unchanged against main -- the blink port does NOT move the plateau on the
merged tree, unlike on T225's old tree (which had 19/20). The plateau's left edge moved 19->20
between T225's tree and the merged tree, but 21 is inside both.

## Step 3 -- T225 acceptance set + T216 canaries on the merged tree

| row | merged tree | target |
|-----|-------------|--------|
| emotion_skip | PASS 0/0/40, neg 27760 | 0/0/40 neg 27760 -- EXACT |
| emotion_syn  | PASS 0/0/40, neg 644 | 0/0/40/644 -- EXACT |
| emotion_face_b | PASS 0/0/40, neg 644 | 0/0/40/644 -- EXACT |
| form_cross   | PASS 0/0/40, neg 644 | 0/0/40/644 -- EXACT |
| cannon       | PASS 0/0/40, neg 9505 (old-box 0) | 0/0/40/9505 -- EXACT |
| mettaur      | PASS 0/0/70, neg 41734 | 0/0/70/41734 -- EXACT |
| cursor       | FAILED 1/1/170, neg 186279 | 1/1/170/186279 -- EXACT (the known 1px plateau line) |
| blind_met    | was FAIL 60901/1219/70 neg BLIND -> fixed -> PASS 0/0/70, neg 60901 | 0/0/70 neg 60901 |
| windowclose  | FAIL 63962/3557/40, neg 266037 | 0/0/40 neg 207166 -- NOT YET (investigating) |

**blind_met root cause (the row's own design caught it):** the merge added T225's emotion code,
which moved `BLIND_POKE_MAILBOX` in .bss 0x020002f4 -> **0x020002f8** (+4, nm on the merged ELF
`nm /tmp/ct_t230/thumbv4t-none-eabi/release/bn`). harness.py hardcoded the old nm'd address, so
the rust side's pokes landed 4 bytes off -> positive showed the gate OFF (60901) and the negative
went BLIND. Fix: BLIND_MAILBOX_ADDR = 0x020002f8, provenance derived (nm on THIS tree's ELF) --
exactly the "fails loudly instead of lying" behaviour the row's comment promises. blind_met then
PASS 0/0/70 neg 60901, matching T216's landing.

windowclose investigation continues below.

### windowclose: the drift predates T230 -- it landed with T216 (f440247), and it is not the pad

- Merged tree: windowclose FAIL 63962/3557/40, neg 266037 (deterministic, x2: at pad 21 and pad 25).
- Plain main (t230base worktree, f5e6927): windowclose FAIL 63962/3557/40, neg 266037 -- byte-identical.
- 0420cab (T216's landing + TODO docs only, src == f5e6927's): same 63962/266037.
- T220's landing (0033d2d, BEFORE T216 landed -- git log 0033d2d..0420cab contains f440247)
  recorded windowclose 0/0/40/207166. So the drift 0/0/40/207166 -> 63962/266037 came in with
  T216's landing; its commit verifies cursor/blind_met/mettaur but never re-read windowclose.
- The 63962/3557/40 signature is EXACTLY T220's documented "Battle-field layout butterfly"
  (docs/worklog/T220.md:117): Battle-layout/boot-timing shift desyncs windowclose's script-timed
  Start@230,A@260 presses. Pad 25 vs 21 makes no difference (tested both on the merged tree), so
  the trigger is T216's objects.rs gate code / .bss mailbox, not SEAM_PHASE_PAD_ITERS.
- T230 verdict: **unchanged by the merge** (merged == main, byte-for-byte). NOT the ticket's cited
  0/0/40/207166 -- that number is stale for any post-T216 tree. Not chased further: objects.rs is
  outside T230's file scope, and the regression predates this ticket. RECOMMENDATION for the
  coordinator: T216's landing silently regressed windowclose (and see field-bg1/2 below); a
  follow-up ticket should re-fit the butterfly on post-T216 main before anything else lands there.

### field-bg1/2: neither tree reproduces the quoted 4080/4080 in this session; merged <= main, not chased

- Merged tree: field-bg1 6000/6000/40 neg 176745; field-bg2 6000/6000/40 neg 6000.
- Plain main (f5e6927, deterministic x2): field-bg1 206045/38400/40 neg 353464; field-bg2 42720/38400/40 neg 42720.
- field-bg3 merged: 380263/24906/40 neg 405169 -- EXACTLY T220's recorded line, so the /tmp canon
  inputs are unchanged since T220's landing and the fixed pairing (search=None) still works on the
  merged tree. Therefore main's field-bg1/2 explosion (worst 38400 = a full-frame diff on one
  frame) is a REAL tree-side regression from T216's landing, unnoticed (T216 never re-read the
  field rows); and the merged tree reads 6000/6000 -- much better than main's current state,
  though +1920 px/frame over T220's documented 4080/4080 (a flat residue, both rows, both total
  and negative exactly +1920 -- a real, small merge-side residue worth its own ticket).
- T230 verdict: the merge does NOT make field-bg1/2 worse (merged 6000 < main 206045/42720);
  neither tree reproduces the quoted 4080/4080; not chased per ticket instruction.

## Step 3 final -- full isolated table on the merged tree (pad 21, `--ui isolated --no-gallery`)

PASS (0 diffs, negatives not blind): warp, buster, chip-use, wave, window, card, result, banner,
popup, mettaur 0/0/70/41734, blind_met 0/0/70/60901, cannon 0/0/40/9505 (old-box 0),
emotion_syn 0/0/40/644, emotion_face_b 0/0/40/644, emotion_skip 0/0/40/27760, form_cross 0/0/40/644,
rollup no-crash (3 walks of 2600 frames).
KNOWN-FAIL pins matching main's own lines: cursor 1/1/170/186279 (the plateau line), windowclose
63962/3557/40/266037 (== main; see above), field-bg1/2 (see above), field-bg3 380263/24906/405169
(== T220's recorded line), buster_charge 2498/188/32/5113 (== T220's recorded line), gunner /
navi-gunner / navi-gunner-ai 2105613/2092176/2092176 (== T220's recorded lines). 9 FAILED total,
none introduced by this merge; every row whose line main documents as current read exactly that
line here except field-bg1/2 and windowclose, both of which main itself no longer reads at the
quoted values (both drifts landed with T216 f440247).

## Unverified / handed to the coordinator

- The butterfly's exact trigger inside T216's landing (objects.rs gate code vs .bss mailbox vs
  code-size boot shift) is NOT diagnosed here -- only that it is pad-independent and predates T230.
- The +1920 px/frame flat residue on merged field-bg1/2 vs T220's 4080 line is UNNAMED (suspect
  T225's emotion code touching a field-scene BG region); not chased per ticket.
- verify_rows on the named set is the coordinator's step (ticket); all harness lines above were
  measured in this session on wt/t230 at pad 21 (7c98583 + worklog commits).
