# T230 — rebase wt/t225 onto post-T216 main; re-find the pad plateau; land the blink

## Context
- main f5e6927 = T215 dca956a + T220 0033d2d + T216 f440247 (SEAM_PHASE_PAD_ITERS 25->21, blind_met row).
- wt/t225 = a2a4bdb + 0da47e6 (emotion blink port: countdown byte 0x0203528F gates the draw,
  descriptor +64 blink_countdown, emotion_skip negative; pad re-fit 20 on the OLD tree).
- T225's landing attempt 2026-09-18 conflicted with main in `src/main.rs` (pad iters) and
  `tools/harness.py` (T216 pokeoff/negative_canon vs T225 negative_rust machinery).

## Step 1 — merge (done, commit d7dc490)
- Conflicts resolved keeping BOTH sides' semantics:
  - tools/harness.py: Check gains both `negative_canon` (T216) and `negative_rust` (T225);
    run_check: `pokeoff` branch (T216) then `elif ui in negative_sides` (T225) then else.
  - src/main.rs: pad iters parked at 21 pending the sweep below.
- Auto-merged cleanly: src/battle.rs (the `if self.hud_live { self.emotion.update(); }` block
  + descriptor +64 arg), src/emotion.rs, src/fixture.rs (FIXTURE 66), docs/coverage/emotion.md.
- Cite-fix per ticket: sub_801CB38's eStruct+0xe stores are asm00_2.s:27442/:27478/:27485
  (verified in canon: `strb r1, [r5,#0xe]` at each), NOT :27343-27344 (that is an unrelated
  `strh` loop in loc_801CAAE). Fixed in tools/harness.py:3625 region (emotion_skip comment).

## Step 2 — pad sweep on the merged tree (candidates 19..25, cursor row each)
Target line (T216/T225 plateau): `cursor isolated FAILED total 1 worst 1 frames 170 | negative: not blind (total 186279)`

| pad | result |
|-----|--------|
| 21 (merge baseline) | cursor isolated FAILED total 1 worst 1 frames 170, negative 186279 — exact target line |

(sweep continues below)
