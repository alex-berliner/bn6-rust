# T217 — Spreadr AS_DATA (replacement worker, branch wt/t217r)

## What this ticket is
Spreadr1/2/3 (chip ids 9/10/11) should join the "as-data" families: behaviour
dispatched on the chip record (attack_family), not a `match chip.id` arm, with
damage read from AttackPower (+0x1a). The ticket text said family 0x09; the
dead worker's verified context (accepted by the coordinator) corrects this:

- ids 9/10/11 carry **attack_family 0x25** (data/ChipDataArr.s:291/:322/:353),
  attack_power 0x1E/0x3C/0x5A = 30/60/90 (data/ChipDataArr.s:305/:336/:367).
  attack_subfamily 0x00 for all three (:292/:323/:354).
- The "0x09" label is likely the index=id conflation: off_802CCB4[9] =
  sub_80E5EA8 (generic t4_0x4a spawner).
- Family 0x25 routes through the AIAttackJumptable (asm31.s:107674+) entry
  0x25 = bubbleShotAttack_80ECBB0 (asm31.s:111577).

## Assets
assets/spreadr_gun.bin recovered from the dead worker's untracked file in
/tmp/bnwt/t217 (352 bytes, BNSP: 1 gfx blob, 1 anim, 3 frames, 6 oam entries).
Copied into this worktree.

## Canon shape (bubbleShotAttack_80ECBB0, asm31.s:111576-111591)
A two-stage dispatcher on oAIAttackVars_AttackStage (off_80ECBC4):
- stage entry 0 = sub_80ECBCC (asm31.s:111594-111650): on first entry (latch 0)
  set navi animation 0xb, spawn the t1_0x5 gun barrel via
  spawn_t1_0x5_tempAttackObject_80B8E30 with sub_80ECC86()+dword_80ECC54[Unk_03]
  (dword_80ECC54 = 0x152F1515), set flag bit, latch=4, AttackWait=0. Each frame
  after: AttackWait++; when >1 increment barrel counter ([barrel+0x10]), play
  sound 0xaf, and spawn the projectile: spawn_t3_0x12_80C6ADA at
  PanelX+frontDir, PanelY, with dword_80ECC5C[Unk_03] (=0x10,0x10,0x00,0x10),
  Unk_0c, Damage+AttackBoost, Unk_02 (asm31.s:111646). Then AttackStage=4.
- stage entry 1 = sub_80ECC60 (asm31.s:111669-111690): countdown from 0x14,
  then clear RelatedObject1Ptr + BusterBarrelPtr, object_exitAttackState.

## Plan
1. Add demo-spreadr1/2/3 rows (ids 09/0a/0b) to tools/scoreboard.py CHIPS ->
   harness builds chip-spreadrN rows automatically (_chip_checks). Baseline
   first, before any src change.
2. Gate the attack in src/battle.rs on chip.family == 0x25 (SPREADR_FAMILY),
   read chip.power for damage. Port the minimum of bubbleShotAttack's
   behaviour the rows need (navi pose, gun barrel sprite, projectile flight).
3. tools/inventory.py: add 0x25 to AS_DATA_FAMILIES with the citation; the
   assert len(chips)==43 must become 46.
4. docs/coverage/chips.md: fill the family column for the three rows.
5. verify_rows on the three new rows + canaries (chip-cannon, chip-airshot,
   mettaur, cursor).

## Log
(work in progress)

## Measured (this session)

Baseline (before any src change, rows added first):
- chip-spreadr1/2/3 isolated FAILED total 17138 worst 1260 frames 70, neg not blind 16411 (identical
  totals across the three -- expected: the chips differ only in AttackPower, no enemies on the fixture).

Key discoveries, in order:
1. The 48-record assets/chips.bin does NOT contain ids 9/10/11 at all -- the shipped asset is the old
   hand list (chip_export.py CHIPS). Our side silently consumed chip 9 (unknown id) and fired nothing.
   So the ticket's "replace match chip.id for ids 9/10/11" had no arm to replace: the record gate had
   to be BUILT, and the asset extended.
2. chips.bin regenerated with Spreadr1/2/3 appended (tools/chip_export.py CHIPS). FIRST attempt placed
   them mid-list (asset idx 19-21) and moved the CURSOR row 1/1/170/186279 -> 16241/3385/170: Battle::new
   builds the stand-in folder BY ORDINAL (chips.get(i % chips.len()), src/battle.rs:2273). Re-appended at
   the END (idx 48-50); all 48 records and their ordinals verified byte-identical. Asset 48780 -> 50736.
3. With the record in the asset, the family gate landed: 17138 -> 3250 (all three rows, identical).
   Non-zero frames: k=0 (canon 5, 226 px), k=3,4 (canon 8,9, 783 each), k=5,6 (canon 10,11, 262 each),
   k=22 (canon 27, 727). ALL inside x46-122, y66-109 -- the gun/barrel zone and the pose tail.

## Residual (exact next steps for the next worker)
Canon OAM watch (0x07000000, frames 4-13, this row's own canon route):
- The gun barrel shows 2 parts (tiles 35/31, y75) from canon 5, gains a third (tile 37, y66 -- the
  muzzle) canon 8-11, and WOBBLES +-2 px per frame (x 88,88,86,85,85,87,87,88...). Our effect plays
  the asset's 3 anim frames by their own durations; canon advances the barrel object's oBattleObject
  CurAnim (+0x10) BY HAND every frame after the shot (sub_80ECBCC's `ldrb [barrel+0x10]; add #1`,
  asm31.s:111626-111630), so the frame timing is counter-driven, not duration-driven. Next step:
  drive our barrel's frame index off the pose counter the same way (frames 5..: counter 1,2,3...),
  and check the 6-OAM-entry/3-frame mapping in assets/spreadr_gun.bin against tiles 35/31/37.
- k=22 (canon 27, 727 px, x46-92 y70-109): the navi's return-to-idle -- action is back to 0x08 at
  canon 26, ours matches, but canon 27 still differs. Probably the anim 0xb -> idle handover frame
  (one frame of the last pose frame vs idle); probe canon 26-28 OAM vs ours.

## BLOCKER found (must be resolved before the rows can land at 0)
The 2 KB asset growth moves the CURSOR row off its landed line: 1/1/170/186279 -> 16241/3385/170.
Measured mechanism: the bigger ROM costs ONE MORE BOOT FRAME (rust marker origin 8 -> 9), and the
custmatch screen's periodic redraw (every 30 frames, k=6/36/66/96/126, x16-115 y12-132) then pairs
against the wrong phase. Widening the cursor Align search band (230..253, origin 9 measured live)
still returns offset 237 as the minimum with the 16241 residue, so this is NOT fixable by re-pinning
alone -- the custmatch side's own timing shifted. With the 48-record asset kept and only the src
changes in, cursor reads 18/17/170 (the F30/F12-cursor28 layout-jitter class, +-20s) -- close to
baseline but not byte-identical either. Next worker: find what in the demo-custmatch build depends
on the asset (suspect: a pack/window list that iterates the full record count), or shrink 2 KB
elsewhere, before re-landing the 51-record chips.bin.
