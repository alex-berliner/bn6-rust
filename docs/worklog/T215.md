# T215 — end-of-battle hand-off: the 0x0C entry predicate

Branch `wt/t215`, from main 874a533 (worktree /tmp/bnwt/t215). Target dir /tmp/ct_t215.

## What the ticket asks

Transcribe the 0x0C entry predicate (T7e/T26 cited, `sub_800801C` region) into
`src/battle.rs::sequencer_step`; cite the predicate and the rank-table reader.
Acceptance: sequencer first-div k>305 **OR** the 0x0C predicate matches canon
across k=305..530; mettaur + cursor byte-identical; verify_rows PASS on the full
isolated table.

## Line-number drift (read this before re-citing)

The ticket cites `sub_800801C` at asm00_1.s:10422-10465. On this tree
(2026-09-18) that range is sub_8007FD2's tail; the dispatcher
`stepBannerSequencer_800801C` sits at **asm00_1.s:10477-10520** and the fight
state `bannerSeqState08Fight_80080D2` (which owns the 0x0C write) at
**10613-10720**. T26 already warned: "bn6f re-cuts move line numbers — re-grep
the symbol, not the number". Every cite below is symbol-anchored.

## The canon predicate (read, not fitted)

The 0x0C entry is `bannerSeqState08Fight_80080D2`'s outcome dispatch
(asm00_1.s:10629-10663), not anything inside the dispatcher itself:

- `bl getBattleOutcome_800A152` (asm00_1.s:15213-15248): timestop → 0;
  ordinary branch (oBattleState_Unk_04 == 0) → Unk_0d != 0 ? 1 : 2.
- outcome==1 && oBattleState_Unk_3a == 0 (ldrh :10634) → loc_8008116
  :10637-10644: **oBattleState_Unk_18 += 1** then `mov r0,#0xc; str r0,[r5]`.
- outcome==1 && Unk_3a != 0 → sub_800AAD6 (asm00_1.s:16734-16745), NO state
  write. Unk_3a is set by sub_800AAE8 (:16745-16751) from the alive-actor
  removal path sub_810E386 (asm32.s:1354) — never set in this build.
- outcome==2 → Unk_19 += 1 + 0x10 (:10645-10653). outcome==7 → 0x18
  (:10654-10663, unported: Unk_0b never set).
- Unk_0d's single writer: battle-FSM step sub_80079A8 (asm00_1.s:9646-9668,
  T127's note): `GetBattleEffects & 8 ? sub_803DD60() : 0`. The port's variant
  decision is the WIN/LOSE pick at the show site (src/results.rs WIN/LOSE).

## Our side, before

`src/battle.rs` ~:3067: `if self.dissolve_in == Some(0) && self.seq.state ==
SEQ_08 { transition(if megaman.is_defeated() { SEQ_10 } else { SEQ_0C }) }` —
the same decisions, unfenced, with the outcome getter inlined at the edge.

## Step 1 — the transcription (this commit)

- `fn battle_outcome(&self) -> u32` — getBattleOutcome_800A152's ordinary
  branch; the Unk_0d latch is fused with the dissolve-done event (same-frame
  stand-in, cited; canon reads its latched byte every fight tick, we read
  `is_defeated` exactly where canon's latch event lands).
- `fn sequencer_step(&mut self)` — the outcome match: 1 → SEQ_0C, 2 → SEQ_10,
  other arms cited-unported. Call site = the old edge site (dissolve gate kept,
  it IS the latch event stand-in).
- Rank-table reader cited in the SEQ_0C arm: entering 0x0C arms
  bannerSeqState0CWinCount_80081A4 (asm00_1.s:10722) whose window is the
  results hand-off; the rank byte there is `battle_rank` (sub_802CA1E +
  sub_802C97E, asm03_0.s:13232-13257 — T119).
- Behavior on every traced path is decision-identical to before (same predicate
  inputs, same edge site); the point of the step is that the predicate now
  carries canon's shape and cites, so the next worker diffs against canon's
  bytes, not against a fitted inline.

## Baseline (before the edit, this tree, measured 2026-09-18)

- `python3 tools/oracle.py battle_full`: FIRST DIVERGENCE enemy_state_action
  k=17 (canon frame 28) canon=(4,8) rust=(4,10); negative NOT BLIND (6/10 fields
  move on the +1 shift).
- `tools/trace.py diff` (tr_c/tr_r, --align row:battle_full): sequencer 273/540
  divergent, first k=31; canon 0x0C from k=305, ours from k=405; rank 540/540
  match; zenny first k=406 (canon 100, rust 0 — the window-up offset T119
  settled); mm_state_action k=250, mm_anim k=251, mm_timer k=269, enemy fields
  k=17/21, rng_cadence k=124 — all pre-existing, separately ticketed.

## Why the first acceptance route (first-div k>305) is unreachable here

The sequencer word diverges at k=31 (canon 0x20, ours 0x08 — the gauge-full
window arm, T178 BLOCKED note at asm00_1.s:10695). No 0x0C-predicate change can
move first-div past k=305 while that stands. So this ticket closes on the OR
route: the predicate verified against canon across k=305..530.

## Step 2 — the predicate vs canon, k=305..530 (measurement, landed)

Method: canon capture of the battle_full recipe (REAL rom, PAUSED state, cheat
0x020349c2:0x01, script Start@10,L@40,Start@70,A@80,A@260,A@440,A@470), 560
frames, `--dump 0x020093c8:8` resolving eToolkit_BattleStatePtr = **0x02034880**
(ELF: eToolkit 0x020093b0 + oToolkit_BattleStatePtr 0x18; the first attempt
watched 0x02004880 — a halfword-truncated peek — and read static bytes; re-run
at the true base), then `--watch 0x02034880:0x40` (BattleState +0x00..+0x3f:
Unk_02/04/05/0b/0d/18/19/32/3a) + `--watch 0x0203CA70:4`.

Numbers (k = capture − 11, states.py battle_full canon_ref):

- Sequencer word spans reproduce T26 exactly: 0x1C −11..−1, 0x08 0..30, 0x20
  31..32, 0x24 33..132, 0x00 133..135, 0x04 136..195, 0x08 196..304, **0x0C
  305..548**.
- The transcribed predicate (state==0x08 ∧ outcome==1 ∧ Unk_3a==0 ∧
  ¬timestop) fires at exactly **k=304** and nowhere else; the word flips
  0x08→0x0C at capture 316 (k=305) with **Unk_18 0→1 the same frame**
  (asm00_1.s:10640-10644), Unk_3a == 0 and Unk_19 == 0 on every frame.
- **k=305..530: predicate-driven model vs traced word — 0 mismatches.**
- The winning read is the Unk_04 != 0 arm (Unk_04 == 1 the whole capture):
  Unk_05 (1 throughout the fight) clears at k=304, Unk_0d == 0, so
  :15229-15237 returns 1 (WIN) exactly there. The Unk_04 == 0 arm and the
  Unk_0b outcome-7 sibling are unmeasured on this scenario. Unk_0d stays 0 at
  the edge — sub_80079A8's latch write runs AFTER the 0x0C entry on this
  battle (its WIN/LOSE role is the result-window variant, T127).
- flags +0x32 timeline 0x3 → 0x13 (f30) → 0x1 (f37..): timestop bit 2 never
  set — the getter's timestop arm is dead on this scenario.

Rust side (own trace, tr_r2): 0x08 234..404, 0x0C 405..539 — the same
predicate shape decides our edge (sequencer_step is the only SEQ_0C writer);
the 100-frame entry offset is the fight's own kill-timing offset (our enemy
dies later; enemy model divergence from k=17, 458/540 frames, pre-existing,
separately ticketed). Re-measured after the step-1 commit: sequencer 273/540
first k=31, rank 540/540 match, zenny first k=406 — identical to baseline.
Canaries: mettaur 0/0/70 negative 41734, cursor 1/1/170 negative 186279 —
byte-identical to HEAD's class (874a533 touched only tools/; T119's step-0/4
fingerprints reproduce to the digit).

## What is unproven / next

- The 100-frame 0x0C entry offset (canon k=305 vs ours k=405) is the fight's
  own kill-timing offset: our enemy dies ~100 frames later (enemy model
  divergence from k=17, 458/540 frames — pre-existing, separately ticketed on
  that timeline, T119's note). The predicate is not what holds it.
- oBattleState_Unk_18/Unk_19 (the win/lose turn counters) and the Unk_3a
  arm are named-unported; porting them needs the alive-actor-removal path
  (sub_810E386) in the object model.
- The Unk_04 == 0 arm of getBattleOutcome and the Unk_0b outcome-7 sibling
  are transcribed-but-unmeasured: battle_full exercises only the
  Unk_04 != 0, Unk_05 == 0, Unk_0d == 0 read. A tag/net-battle scenario
  would measure them.
