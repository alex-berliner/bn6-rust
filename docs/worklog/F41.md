# F41 — chip-use integrated: "the last 2 px" — NEGATIVE, premise refuted by measurement

Branch `wt/f41-chip-use-2px` (worktree /tmp/bnwt/f41-chip-use-2px), HEAD 8d38d39.
No src/ change. tools/allowlist.py's `"chip-use:integrated"` ticket text re-titled only
(max_px 28000 and date untouched; entry NOT deleted — the row is not at 0).

## Baseline (step 1) — before any edit, commit 8d38d39

```
chip-use   isolated    PASS  total 0        worst 0     frames 30 | negative: not blind (total 7768)
chip-use   integrated  FAILED (allowed: AUDIT-6, <=28000) total 275307 worst 18091 frames 30 | negative: not blind (total 283978)
```

The ticket's premise ("the 2026-09-15 scoreboard prints chip-use integrated FAILED
total 2", "the smallest non-zero row on the board") is **wrong**. The row reads
**275307 px over 30 frames (worst 18091 on k=29)**, and this is not new: the row's own
Align note records F38b (2026-09-14) measuring `chip-use integrated 275307->269172`,
and T7w's layer split the same day logged `chip-use 275307/18091/30`. The "total 2"
line was a misread (most plausibly of `total 275307`); there are no 2 pixels to name.
~8000 px differ on **every** frame k=0..23, full-screen bboxes, growing to 18091 by
k=29 — five orders of magnitude from the ticket's defect size.

## Step 2 — per-k bbox (integrated, fresh captures, reproduced 275307 exactly)

Search minimum at offset 94 (the band's left edge, as the note says — score-ridden,
not event-locked); rust origin 8, canon_ref 130. Per-k counts:
7267, 7874, 7855, 8201, 8025, 8191, 8191, 8319, 8319, 8502, 8028, 7941, 7885, 7989,
7449, 7366, 8331, 8945, 8114, 8110, 8008, 8371, 8371, 7966, then 10017, 11191, 12171,
14006, 16213, 18091 (k=24..29, +~2100/frame). Every k's bbox is near-full-screen
(x 0..233..239, y 0..159) — no 2-px locus exists.

## Step 3 — the layer, by measurement (BG3-only, `--only-bg 3` on both sides)

```
chip-use integrated (--only-bg 3)  total 44306 worst 12797 frames 30 | negative: not blind (59749)
per-k: 0 for k=0..23, then k=24..29 = 2090, 4186, 6282, 8378, 10573, 12797
bbox k=24: x 0..15  y 24..154;  k=29: x 0..95 y 16..154  (one 16-px column per frame)
```

Named: **the k=24..29 band is BG3 — canon's RESULT window slide-in**, one
RESULT_SLIDE_STEP (16 px) column per frame, written on canon's side by
`resultWindowSlideTick_802BE36` (reference/bn6f/asm/asm03_0.s:11701, dispatched by
`resultWindowDriver_802BD60`, .word at asm03_0.s:11674; renames.md:305-306). Our
side's counterpart is src/results.rs (`SLIDE_STEP` src/results.rs:63, slide blit at
:767) — but our zero-enemy fixture battle **never resolves** (sequencer stays 0x08
through the whole window; canon enters 0x0C at canon capture 47 and first ticks the
window at edge+107), so rust never draws it. BG3 alone is otherwise EXACT (k=0..23
read 0).

The remaining ~8000/frame (k=0..23, and the base under the band) is not BG3 and not
OBJ/HUD (isolated = 0) — by T7w's same-day four-tuple it is **BG1** (BG1-only read
443520, i.e. ~15k/frame of backdrop-art mismatch under CHIPUSE_ZERO's
art_entry=13/art_timer=3/scroll_xq=522/scroll_yq=773 seeds, tools/harness.py:1570),
consistent with this row's own note ("the BG1 art mismatch (~17k/frame) dominates").
I did not re-measure the BG1-only split on this commit (capture budget; see below).

## Why the 2-px port does not exist (the NEGATIVE)

1. The defect is 275307 px, not 2 — there is nothing pixel-sized to port.
2. The BG3 band is structurally unfixable inside this row's frozen rules: matching
   canon's slide at k=24..29 needs our side to reach the results show ~107 frames
   after a sequencer edge our fixture never takes. Arming SceneFlags::RESOLVE_OVER
   was already measured (F38b, in the row's own note): integrated 275307->269172 but
   isolated 0->4026 — worse, reverted. T7w further showed no show timing lands the
   band under the event-locked offset (canon's edge maps outside the window).
3. Step 4's T9l seed lever could at best shrink the BG1 share; even a perfect BG1
   fix leaves the measured 44306-px BG3 band, so the `"chip-use:integrated"` entry
   can never be deleted at a measured 0 — the ticket's landing condition is
   unreachable. Deriving seeds to shrink an allowlisted number without reaching 0
   would also burn the remaining capture budget on a number the row is allowed to
   carry.

## Capture budget

8 mgba_capture runs used: baseline isolated+integrated pairs (4), the integrated
re-measure pair (2), the BG3-only pair (2). The 8-run cap is spent; a BG1-only
re-split and a step-4 seed attempt + re-measure would each need more.

## What is unverified

- The BG1 attribution of the k=0..23 ~8000/frame rests on T7w's BG1-only split
  (443520) at its own commit plus this row's note, not a fresh BG1-only run here.
- Whether T9l-style seed derivation for CHIPUSE_ZERO would shrink the BG1 share,
  and by how much — unmeasured (see budget). If a later ticket wants it, derive the
  seeds at canon_ref=130 the T9l way (probe.py watch of eBGScrollCBCounters /
  eGFXAnimStates on this row's own canon capture) and expect the ceiling to be the
  BG3 band's 44306, not 0.

## Next worker's note

Re-title verdict landed on the branch; entry count stays 4 -> 4. The honest
follow-up objective is F42's (field) plus a shared "zero-enemy fixtures never
resolve" mechanism ticket — the band mechanism is identical on warp/buster/chip-use,
and none of them can reach 0 without a fixture/alignment change, which their rules
forbid. The allowlist row for chip-use should stay until that decision is made.
