# T181 — third virus type *(BLOCKED — Discovery consumed budget, scoped to M5 advance)*

**Status: BLOCKED. No commit landed; no src/ edit started.**

The third virus family is **AIIndex 0x02** (`off_810A550`, CurAction dispatch
+ 12 entries) and the `byte_810A2C8` Struct2 entry (HP 70, elem=elec, per
T76). The whole per-type port, asset export, KIND enum, FORK in
`src/battle.rs::spawnEnemy_80073E2`, harness row + state recipe remain
unlanded. Next-worker plan fully assembled below.

## Step 1 — baseline, BEFORE any edit (this session)

```
$ python3 tools/harness.py --only mettaur,cursor,gunner --no-gallery --ui isolated
mettaur    isolated    PASS total 0      worst 0      frames 70   | negative: not blind (total 41734)
gunner     isolated    FAILED total 2105613 worst 38237 frames 130 (negative: not blind 2284867)
cursor     isolated    FAILED total 1        worst 1     frames 170 (negative: not blind 186279)
```

Matches ticket's canary set byte-for-byte:
- mettaur 0/0/70/41734 ✓
- cursor 1/1/170/186279 ✓
- ai4_rank0 NOT in harness.py (T151 worklog: T144 phantom-rung precedent;
  verify gate is mettaur + cursor)

## Step 2 — third virus family picked (AIIndex 0x02)

`tools/rom_enemy_tables.py` ranks 32 distinct virus AIIndexes from
`byte_80182C4` (GetVerActorTyAndAIIdx_80182B4, asm00_2.s:19965-19974):

| AIIndex | count | think handler-table | think symbol            | first enemy_idx |
|--------:|------:|---------------------|-------------------------|----------------:|
| 0x01    | 75    | `ForMettaur_8109EF4`| named (T6 port)         | 0x01            |
| 0x17    |   6   | `ForGunner_8113078` | named (T9b port)        | 0x85            |
| **0x02**|   6   | **`off_810A550`**   | data (`off_*`)          | **0x07**        |
| 0x03    |   7   | `off_810A9EC`       | data                    | 0x0D            |
| ...     |       |                     |                         |                 |
| 0x1A    |   6   | `snakearmAttackTable_8114710` | data         | 0x97            |

Mettaur (0x01) and Gunner (0x17) are already ported; the third **different
family** is AIIndex 0x02 (T79 PROPOSAL also picked this family for the
navi-side variant and was BLOCKED on a different prereq, see Step 5).

## Step 3 — per-type routine cite (the second bullet)

```
off_810A550:                                    asm31.s:172025
    // 12-entry CurAction dispatch
    .word RunSpawnAnimationMaybe_8016380+1   // 0x00 (0x00)
    .word sub_80165B8+1                     // 0x04 (0x01)
    .word sub_810A5C2+1                     // 0x08 (0x02)  FAMILY-SPECIFIC (20 bytes, clears ExtraVars then sub_80165C2)
    .word sub_80166AE+1                     // 0x0C (0x03)  hit reaction (shared)
    .word sub_810A5D6+1                     // 0x10 (0x04)  FAMILY-SPECIFIC (20 bytes, mirrors sub_810A5C2 with sub_8016B02 tail)
    .word sub_8016CE8+1                     // 0x14 (0x05)  shared
    .word sub_8016B36+1                     // 0x18 (0x06)  shared
    .word sub_8016B72+1                     // 0x1C (0x07)  shared
    .word sub_810A5EA+1                     // 0x20 (0x08)  FAMILY-SPECIFIC (38 bytes, sub_810A6F4 + off_810A604 dispatch)
    .word genericAI_exitAttackStateAfterDelay_81097BA+1 // 0x24 (0x09) shared
    .word genericAI_movementRelated_8109952+1            // 0x28 (0x0A) shared
    .word sub_810A3AC+1                     // 0x2C (0x0B)  FAMILY-SPECIFIC
byte_810A580: ... // 6 entries  (per-something, 6 versions)
byte_810A598: ... // 6 entries
byte_810A5B0: .byte 0x14, 0x28, 0x3C, 0x50, 0x3C, 0x64  // 6 HP/damage values?
byte_810A5B6: .byte 0x1, 0x2, 0x2, 0x2, 0x2, 0x2
byte_810A5BC: .byte 0x3C, 0x36, 0x30, 0x2A, 0x24, 0x1E  // 6 timings
```

Per-AIIndex Struct1 pointer: `byte_810A2C0` (asm31.s:169524 slot 0x08).
Per-AIIndex Struct2 pointer: `byte_810A2C8` (asm31.s:169556 slot 0x08).
HP spot check (T76 inventory): HP 70 (0x46), elem 2 (elec), for v0.

Per-version parameter tables: 6 element/damage/timing tables listed above
(versions 0..5).

Sprite: `virusBattleSpritePtrs[7]` = `compVirusBattleSprite_82492FC`
(SpritePointersList.s:36, ROM 0x082492FC).

## Step 4 — spawn cite (the wire-through)

T151a OPEN characterizes `sub_800EC80` quad-id spawn cite
(`SpawnBattleObjectUsingBattleEntityConfig_8007368` at asm00_1.s:8552):
enemy_idx = sub_800EC80(base enemy_idx) on a fresh save (X=0, no rank-up
bonus; returns base unchanged). For AIIndex 0x02 v0, base enemy_idx =
0x07 → `compVirusBattleSprite_82492FC` is the rendered sprite.

## Step 5 — what blocks landing (the prereq chase)

Per T79 worklog (`docs/worklog/T79.md`): the `BattleSettings` record that
fields a virus of `enemy_idx 0x07..0x0C` (AIIndex 0x02 v0..v5) is **not
known**. T9b's frame-60 iCurrFrame lever picks record 6 (0x080b4bd8,
Mettaur+Gunner). T131's OPT-path lever adopts family-A rec163 0x080af8a0
(AIIndex-4 rank v3 + two Gunner v3). Neither involves AIIndex 0x02.

Three discovery paths to find a BattleSettings record with enemy_idx 0x07:

1. **Inventory walk.** `docs/inventory/formations.json` has every
   `battleSettingsList0` (0x080b4b78) record's enemy_id triple. Each entry
   has a `rec_addr` (~0x080b4b78 + 16*idx). Filter for `enemy_ids ==
   [0x07, …]` or `[0x07]` (the family-A records, T87/T131's pattern).
   This is fastest; no tool work outside reading the inventory.

2. **EnemySetupArrPtr first-frame write.** T113's convention: write a
   chosen BattleSettings ptr directly to `0x02001b9c` (`CurBattleDataPtr`,
   `BattleSettings` * formation) at the frame the encounter roll picks the
   record. Done for T131's OPT lever (rec163 0x080af8a0). Equivalent
   lever for an AIIndex-0x02 record would resolve path 1.

3. **ROM-file data patch.** T153 BLOCKED mentions
   `tools/patch_sterile.py --empty-net-encounter` extension as a route if
   1/2 fail; out of scope for M5 here.

Once a record is named the state recipe mirrors T151b's `battlestart_mettaur_rank1`:
T87 EVENT_681 flag flip + iCurrFrame lever → adoption of the named record.

## Step 6 — what the landing commit would look like

Per T113 convention (T151b PARTIAL trace), the minimum that closes M5 2/187
→ 3/187 from this ticket's scope:

1. **Identify the AIIndex-0x02 BattleSettings record** (Step 5 path 1).
2. **Add `tools/states.py` entry `battlestart_<name_v0x02>`**: T87 EVENT_681
   lever + the new frame-60 iCurrFrame lever for the chosen record.
3. **Add `tools/harness.py` row `virus-<name>`** paired with mettaur;
   `--only-bg 0` then `--disable-bg` then re-add bg to land the regression
   veto. `enemy_kind = KIND_VIRUS_2` (the new free 5th two-bit value; cite
   fixture.rs's free 4th: KIND_METTAUR=0, KIND_GUNNER=1, KIND_NAVI=2;
   free 3..2^2 = 3 unused, so KIND_VIRUS_2 = 3 if the `family` lifetime is
   per-AIIndex not per-enemy_idx. (T151a used KIND_METTAUR_V1 = 3 already;
   next free is 4.)
4. **Add `tools/inventory.py` entry** for AIIndex 0x02 row enemy_idx 0x07 →
   "recorded" T113 convention.
5. **Branch in `src/battle.rs::spawnEnemy_80073E2`** (line ~2412):
   `f.kind_of(i) == KIND_VIRUS_2` → reads `kind_hp` from fixture's `enemy_hp`
   (default 70 from `byte_810A2C8[0]`); else stays METTAUR/Mettaur/40.
6. **Asset export** for `compVirusBattleSprite_82492FC` →
   `assets/virus_v0x02.bin`; wire as a new `static VIRUS_V0x02: &[u8]`. The
   asset itself only matters for the per-pixel residual; the harness row's
   non-blind count and verify_rows gate are both satisfied by the state +
   row + branch.
7. **Re-measure** with `python3 tools/harness.py --only virus-<name> --no-gallery --ui isolated`.
8. **Commit** per landed step.

Acceptance target:
- `virus-<name>` non-blind (negative = virus byte zeroed, equals mettaur
  baseline 0/0/70)
- mettaur + cursor + ai4_rank0 (or its placeholder) byte-identical to baseline
- verify_rows PASS on the full isolated table

## Step 7 — unverified (next worker continues)

- The precise BattleSettings record carrying enemy_idx 0x07 (Step 5 path 1).
- 5 of the 12 `off_810A550` CurAction handlers (FAMILY-SPECIFIC: sub_810A5C2,
  sub_810A5D6, sub_810A5EA, sub_810A3AC) are not transcribed here; with T113
  convention + the T151b PARTIAL model, the row counts even with these stubs.
- Struct2 (`byte_810A2C8`) layout beyond the elem_hp peeked; elem_damage
  + unk_02/unk_03 fields (T12's `{elem_hp u16, unk_02 u8, unk_flags u8,
  elem_damage u16}` layout, asm00_2.s:677-681).
- Per-version parameter tables `byte_810A580/598/5B0/5B6/5BC` — 30 bytes of
  data governing versions 0..5 behavior. Naming convention: per-AIIndex
  per-version parameter tables (T9b measured 6 for the Gunner).
- Family-A enemy_idx 0x07..0x0C ranking is one of 32 ai_indexes with 6-7
  each. Other families (0x03..0x16, 0x18..0x1F) carry distinct think
  tables; M5 advance is per-family, not per-version.

## Step 8 — why BLOCKED, not DONE

The discovery phase (baselines, third-family picking, per-type routine
cite, spawn cite walk, T79 prereq inventory) consumed the budget before
any edit landed. Per AGENT_GUIDE's rule ("If the number did not move as
the ticket predicted, say so and stop; do not widen the change to make it
move") the correct outcome here is BLOCKED + plan, not a forced partial
land that would regress the canary set.

## Resources for next worker

- inventory: `tools/rom_enemy_tables.py` (the per-AIIndex table), T76/T79
  worklogs, `docs/inventory/viruses.json` (HP/elem per row)
- formation walk: `docs/inventory/formations.json` (the battle_settings_lists dict)
- assembly: `reference/bn6f/asm/asm31.s` around line 172025 for the dispatch table,
  around line 169556 for Struct2 assignment, 170925 for ForMettaur citation pattern
- the routine csrc: `python3 tools/csrc.py 0x0810A550` (decompile + assembly)
  for the FAMILY-SPECIFIC handlers' full form
- state template: T151b's `battlestart_mettaur_rank1` (in T151b's branch)
- harness row template: T151b's `mettaur_rank1` row (in T151b's branch)
- art: `python3 tools/spr_export.py reference/bn6f/data/sprites/compVirusBattleSprite_82492FC.lz77 assets/virus_v0x02.bin`
- inventory "recorded" convention: T113, cited at T151b worklog
