# T26 — battle-state 0x14 ladder: what each custom-screen edge waits on

## Step 1 — baseline (no edit)
`tools/trace.py record canon|rust battle_full` + `diff --align row:battle_full`
(through a /tmp shim — see "Tooling defect" below): sequencer **273/540**, first divergence
**k=31** (canon 0x20, ours 0x8) — exactly the ticket's prediction. Canon spans on 0x0203CA70:
0x1C 0..10, 0x08 11..41, 0x20 42..43, 0x24 44..143, 0x00 144..146, 0x04 147..206, 0x08 207..315,
0x0C 316..554. Ours: 0x08 0..124, 0x24 125..171, 0x00 172..173, 0x04 174..233, 0x08 234..404,
0x0C 405..542. Artifacts: /tmp/bn-t26/tr_c, /tmp/bn-t26/tr_r.

## Tooling defect found (not mine, not fixed here)
`tools/trace.py:56` does `int(O.MM_ORACLE_ADDR, 0)` but `tools/oracle.py:149` defines
`MM_ORACLE_ADDR = 0x02000008` as an **int** → TypeError on import; every `trace.py record/diff`
on main HEAD dies at import (broken since 0701f09 "Q4 step 2"). The ticket forbids editing
tools/trace.py, so all measurements ran through /tmp/bn-t26/run_trace.py, which sets
`oracle.MM_ORACLE_ADDR = hex(...)` before runpy'ing trace.py. **Someone must fix trace.py:56**
(e.g. drop the int()) or the next trace ticket hits the same wall.

## Steps 2-3 — the asm walk (cites re-opened on disk 2026-09-16; eight corrected per verifier-hyper)
Line numbers are from the pinned worktree snapshot of asm00_1.s (2026-09-15 22:53); the original
publish had eight off cites, corrected here and in docs/coverage/battle_full.md after
verifier-hyper opened every cite on disk. Anchors are the symbols/labels — bn6f re-cuts move line
numbers.
Two dispatch tables over the same word 0x0203CA70 (stepBannerSequencer_800801C :10448, ten entries
:10484; sub_80084F0 :11150, seven entries). Fight body = second table's state-0x04 handler
sub_800855E :11206; gauge-full arm :11267-11274, store :11273-11274 (twin sub_80089CC :11870,
store :11876-11877):
isCustGaugeFullAndBattleLive_800A21C (:15305) == 1 → PauseBattle + 0x14, one frame.

The 0x14 chain's waits live in sub_8008840 :11597, sub-dispatched on the byte **0x0203CA72**
([r5,#2], :11600) — a SHARED per-state latch field (latched at :11034-11039, :11098-11103,
:10944), not a global chain position: 0 → sub_8008864 :11616 (waits isBannerBusy :11632; banner
0x54 spawned :11626);
4 → sub_8008894 :11642 (countdown 0x1e=30 :11647-11648); 8 → sub_80088B2 :11663 (isBannerBusy
:11675;
banner 0x1c :11670); 0xC → sub_80088D6 :11685 (countdown 0x1e=30 :11690-11691) → exits via
byte_203CA74=7 :11699-11700 + oGameState_Unk_14=7 :11703. The chain never writes 0x0203CA70.

Banner-table edges (what the trace shows): SEQ_08→SEQ_20 armed by sub_800A244 :15327 (gate
sub_800A8F8()==1 :15329-15331, then OR of both players' ready bytes [+0x50] :15340-15347) then
loc_800819A PauseBattle+0x20 :10681-10684; SEQ_20→SEQ_24 waits
sub_802D6C4==0 :11076-11081; SEQ_24→SEQ_00 waits sub_801483C==0 :11095, exits via
byte_203CA74=6 :11106-11107; SEQ_00→SEQ_04 waits sub_801483C==0 + [r5,#2] latch :11031-11041, writes 4
:11048-11049; SEQ_04→SEQ_08 arms 0x1e=30 :10530-10531 (+0x293=659 at [r5,#0xA]) but exits on
isBannerBusy==0 :10565-10567 with the store at :10578-10579; SEQ_08→SEQ_0C on getBattleOutcome==1
(cmp :10602), store :10615-10616 (loc_8008116 :10611).
The earlier "gauge-full arm reaches the window via the 0x14 chain" line is DISASSEMBLY-ONLY
INFERENCE (verifier-hyper): battle_full never shows 0x14 (observed low bytes exactly
0x1C,0x08,0x20,0x24,0x00,0x04,0x08,0x0C) — the measured route is loc_800819A.

**Arithmetic test: NEGATIVE.** Counts-from-asm vs measured spans: SEQ_20 0/2, SEQ_24 0/100,
SEQ_00 0/3, SEQ_04 0/60. No per-state count reproduces any span; every timer in the family is the
SHARED driver slot [r5,#8] = 0x0203CA78, armed/decremented at :10530-10531 (SEQ_04, 0x1e=30; aux
0x293=659 at [r5,#0xA] :10537), :10713-10714 + :10745/:10749 + :10767-10769
(bannerSeqState0CWinCount, 0x66=102), :10815/:10818/:10820 + :10832-10834
(bannerSeqState10LoseCount, 0x66 or 0x5e), :10863 (bannerSeqState14MessageCount :10854, 0x66=102),
:10939 (state 0x18's sub_8008364, counts UP to 0x3c=60), and the second dispatcher's copies
(:11188/:11647/:11690, 0x1e=30).
Every span is a banner-record lifetime (isBannerBusy_801E754, asm00_2.s:31073) or a busy/pad wait
owned outside the states. Per acceptance this closes T26 as the NEGATIVE and re-points T27 at the
driver countdown + isBannerBusy. Nothing patched (no fitted constants; provenance count stays 19).

## Step 4 — pad path
Ours: the L/R test-open arm is `cfg!(debug_assertions) && (L||R is_just_pressed) → gauge_pause=1`
at **src/battle.rs:2883-2886** — the ticket's :2729-2732 moved in the 357da2a re-cut (GAUGE_PAUSE
:1116, SEQ04_FRAMES :1186, arm :2906-2908, edges :3459-3460 unchanged). Harness builds release
(tools/harness.py:272 verified) → the player is not in any measured loop. Canon:
refreshAIDataFromJoypad_8012DFC (asm00_2.s:8972) writes AIData Held/Pressed from mirror 0x02036822
(:8989-8993), and sub_800A244 :15327 ORs the per-player ready bytes. The literal L|R mask was
subsequently walked by verifier-hyper: sub_800A29A asm00_1.s:15368-15398 tests **0x300 (L|R)**
(:15381 ldr r2, off_800A2CC // =0x300; :15382 tst r1,r2) after the HP<0x2900 gate (:15372-15375);
called from sub_800A244 :15350/:15357. Corrective: sub_800A244's gate is sub_800A8F8()==1
(:15329-15331), not an inline TestBattleFlag_0x40 — that flag gates the SEQ_20/SEQ_24 handlers
(:11060/:11092).

## Step 5 — state word
The 0x14 chain sub-dispatches on the byte **0x0203CA72**, not 0x0203CA70 — same 12-byte struct
(zeroed by sub_80084C0, :11113-11119; ewram.s:3040-3046 corroborates the layout), different byte.
But verifier-hyper corrected the interpretation: byte_203CA72 is a SHARED per-state latch field
(:11034-11039, :11098-11103, :10944), not a global chain position. T7y's gunner observation
(0x0203CA70=0x00 for all 260 frames while the window opens) means the ladder's position is simply
not visible in the traced word — watch **0x0203CA70:8** instead, and note bytes +2/+3 of the
4-byte banner watch are LIVE in /tmp/bn-t26/tr_c/banner.bin (0x0000 k=0..42, 0x0400 k=43, 0x0404
k=145..146, 0x0400 k=148..206 and k=317..554) while tools/trace.py masks the sequencer comparison
to the low half (its comment at :285-287). Gunner-side holder of the 0x00: the
second dispatcher's state-0x00 handler sub_8008528 (:11189, waits isBannerBusy :11196). Writer
cite is by table position; the gunner capture was not re-run (≤2 capture budget spent on the
baseline), T7y's measurement stands.

## Steps 6-7 — docs + no-op proof
Table written into docs/coverage/battle_full.md (T26 section). `cargo build --release` twice,
packed, cmp against main's packed ROM: 0 differing bytes; harness `--only mettaur,cursor
--no-gallery` unchanged (see commit message for the lines). No src/ change; no captures beyond the
two baseline records.

## What is unverified / next
- The L|R mask question is closed (verifier-hyper walked sub_800A29A, mask 0x300).
- Gunner was not re-captured: the byte_203CA72 latch's runtime values on gunner are inferred from
  the dispatcher structure, not watched. T27 should --watch 0x0203CA70:8 on gunner (NOT the 4-byte
  word alone — trace.py masks its comparison to the low half, :285-287; use
  /tmp/bn-t26/run_trace.py's shim until trace.py:56 is fixed).
- T27 should model the driver countdown [r5,#8] + isBannerBusy, not per-state ages.

## Verifier follow-up (2026-09-16, this commit)
verifier-hyper (35 turns) confirmed the four headline claims and returned GO on merging 949f0f0,
but found eight cites off on disk; all eight are corrected above and in
docs/coverage/battle_full.md. It also settled, from our own artifacts: (1) the L|R literal mask is
sub_800A29A :15368-15398, mask value 0x300 (:15381-15382); (2) the 0x14-route sentence in the
SEQ_08→SEQ_20 row is disassembly-only — battle_full never shows 0x14, the measured route is
loc_800819A; (3) bytes +2/+3 of the banner watch are live in tr_c/banner.bin (0x0000 k=0..42,
0x0400 k=43, 0x0404 k=145..146, 0x0400 k=148..206 and k=317..554) while trace.py masks the
comparison to the low half (:285-287), and byte_203CA72 is a shared per-state latch — so T27 must
watch 0x0203CA70:8, not 0x0203CA70. No src/ or tools/ changes were made in this follow-up; docs
only.
