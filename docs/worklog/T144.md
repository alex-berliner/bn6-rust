# T144 — M5 Gunner think arm row carry-down: T121 PARTIAL follow-up

Worktree `/tmp/bnwt/t144`, branch `wt/t144`, started from main `157d152`.
**Status at this commit: PREMISE AUDIT — the ticket's "New evidence: T121's port is in
place" is FALSE on main; decision requested from the coordinator before any src change.**

## Step 1 — the three baseline asks, measured on main `157d152`

1. **gunner isolated current px**: `python3 tools/harness.py --only gunner --no-gallery
   --ui isolated` on the untouched tree:
   `gunner isolated FAILED total 2105613 worst 38237 frames 130 | negative: not blind
   (total 2284867)` — byte-identical to T9l's recorded 2105613/38237/130 and to T121's
   own before/after control. **NOT 477686**: that number is T113's `ai4` row, which does
   not exist on main (see findings). fitted 17 / derived 437 / peeked 146.
2. **oracle enemy_state_action on the ai-index-4 scenario**: NOT RUNNABLE on main —
   there is no `ai4` harness row (not in `harness.py` CHECKS), no `ai4` entry in
   `states.TRACE_SCENARIOS`, and `harness.SCENARIO_ROWS = ("battle_full",)` only, so
   `tools/oracle.py ai4` exits "unknown row" — run first-hand at close:
   `unknown row 'ai4': harness.py --list names the rows, tools/harness.py
   SCENARIO_ROWS lists the non-comparison scenarios (battle_full)`. This is exactly
   the step-5 blocker T121
   recorded (states.py outside its allowed files); the follow-up inherited it unchanged.
3. **oracle enemy_kind**: NO SUCH ORACLE FIELD. `docs/oracle_layout.json` exports:
   magic, battle_frame, rng, mm_state_action, mm_anim, mm_panel_x/y, mm_gap, mm_timer,
   mm_hp, enemy_state_action, enemy_anim, enemy_panel_x/y, enemy_gap, enemy_timer,
   enemy_hp, gauge, padding. `enemy_kind` is a *descriptor input* (fixture.rs
   `kind_of`), not an oracle export, so there is nothing to oracle against.

## Findings — the premise chain, checked against main

- **T121's port never landed.** `Style::Ai4`, `Ai4Entry`, `AI4_ROW` and the `ai4`
  harness row exist ONLY on branch `wt/t121-ai4` (commits a569948, 43f5fad, bbbdd8e,
  4a341ea, eac74fd). `git branch --contains eac74fd` → wt/t121-ai4 only;
  `git merge-base --is-ancestor eac74fd main` → NOT in main. T121 closed PARTIAL and
  its TODO line records the close, but no landing commit reached main.
- **T113's row never landed either** (same branch, via T121's cherry-pick a569948).
- **`GunnerEntry::think` does not exist on main.** `src/gunner.rs`'s `GunnerEntry` has
  only `new()`; the per-type port `gunner_update` (ForGunner_8113078,
  asm32.s:10123-10142, T9j PARTIAL) is `#[allow(dead_code)]` and returns
  `Update::Nothing` — the gunner row's visible content is driven by `Battle::gunner_ctl`
  / `Battle::impacts`, not by any think arm. (Note: this is the GUNNER family's table;
  `off_810B2D0` is NOT the gunner's — see next.)
- **`off_810B2D0` is the AIIndex-4 family's think table, verified first-hand this
  ticket**: label at `reference/bn6f/asm/asm31.s:173621`, 15 `.word` entries at
  173622-173636 (`RunSpawnAnimationMaybe_8016380, sub_80165B8, sub_810B338,
  sub_80166AE, sub_8016B02, sub_8016CE8, sub_8016B36, sub_8016B72, sub_810B350,
  genericAI_exitAttackStateAfterDelay_81097BA, sub_8109804, sub_810B042, sub_810B06A,
  genericAI_movementRelated_8109952, sub_810B172`). The ticket's "from asm/asm32.s" is
  a wrong file — the table and its family-local arms (sub_810B338 asm31.s:173663-173676,
  sub_810B350 asm31.s:173678-173693, 2-way off_810B364) all sit in asm31.s.
  In the row's 40-frame window canon's ai-4 sits at CurAction 0x00 the whole time
  (T121's window analysis; T87 measured 0x0004@69 -> 0x0104@151), so the only reachable
  per-tick visible-content path is slot 0x00 = `RunSpawnAnimationMaybe_8016380`
  (`asm00_2.s:16035-16052`, phase tables off_80163A8 / off_80164B4).
- **The gunner's own think table is `ForGunner_8113078`** (AIThinkTables_8109050 slot
  0x17, asm31.s:169494; T76's table), ai_index 0x17 from `byte_80182C4[3*0x85]`.
  Wiring `off_810B2D0`'s arm into the Gunner dispatch would give the gunner the WRONG
  family's behaviour and would move the gunner guard row (2105613) — failing the
  ticket's own step 5 (`verify_rows gunner` must reproduce the recorded line).

## Merge feasibility (for the decision)

`git merge-tree --write-tree main wt/t121-ai4` → clean, 0 conflicts (merge-base
2c08cad). Per-file overlap is in disjoint regions (actor.rs, battle.rs, harness.py
changed on both sides since the base). T121's own precedent applies: it cherry-picked
T113's row commit before building on it.

## Options put to the coordinator

(a) Cherry-pick wt/t121-ai4's commits onto wt/t144 (T121's own precedent), then do the
    carry-down there: the ai4 row's per-tick content driven by `Ai4Entry`'s think arm
    (Files list extended with src/ai.rs, where the entry lives; the ticket's
    "GunnerEntry::think" read as the ai-4 entry's think). Expectation set honestly: the
    row's residue is the canon fade tail k=0..5 (worst 38237) + the art-family gap (our
    METTAUR-art stand-ins vs canon's Gunner/ai-4 sprites) + HP boxes — T121 measured
    the port byte-identical in-window (488098/38237/40 after, 477686 before its step 3),
    so "0/0/N" is NOT the expected outcome of think-arm wiring alone; a "named
    reduction" needs the phase-table/materialize path cited above to actually move
    pixels.
(b) Close as phantom-rung / queue-integrity (the T155/T157 convention): the ticket's
    stated premise is not on main.
(c) Literal gunner-row reading (wire the GUNNER's own arm — gunner_update/ForGunner —
    into GunnerEntry::think so the row's OBJ content is driven by the port): matches
    the ticket's file list and "gunner row", contradicts both off_810B2D0 mentions,
    and is really T9j's follow-up scope (T120b's TODO line assigned the ai-4 scope to
    T121's line, not to a gunner-row ticket).

## Coordinator decision (2026-09-18) — (b) BLOCKED, phantom-rung / queue-integrity

Verbatim rationale: the ticket as written is unsatisfiable on main — its premise branch
wt/t121-ai4 was never landed, its baseline number belongs to an unlanded row,
GunnerEntry::think does not exist, and off_810B2D0 is the wrong family's table (the
asm31.s first-hand check settles that). Cherry-picking the unverified branch into this
ticket (option a) is NOT authorized — no verified report rows are held for wt/t121-ai4,
and landing it inside T144 would smuggle an unvetted landing past the verify gates.
Option (c) contradicts the ticket's citations, refused.

Disposition: this worktree is EVIDENCE ONLY — no src/ change attempted, no wiring.
The prerequisite is named: **wt/t121-ai4 must land (or be superseded by a land-T121
ticket) before any T144 rewrite**; the queue owner can also rewrite T144's "New
evidence" against whatever lands.

## Close-out (BLOCKED)

- Branch `wt/t144` carries exactly one artifact: this worklog (commit fee39f3, then the
  decision record). Zero src/ diffs, zero tooling diffs, ROM untouched.
- Measured and reported, per the ticket's Measure-and-report shape (gunner · frames ·
  total · worst · region · commit · mechanism · unverified): gunner isolated
  **2105613 / 38237 / 130**, negative not blind 2284867, worst class = the canon fade
  tail k=0..5; region = T9l's recorded split (k0..5 full-screen fade-in; k6..75 BG3 —
  the two enemy HP boxes + gauge bar; k≥77 BG3 — the gauge-full chip-window plateau,
  OBJ plateau downstream); commit fee39f3; mechanism = none — this is a premise audit,
  the row was not modified; unverified = nothing beyond the ticket's own asks that
  cannot run on main (ai4 oracle scenario unregistered, enemy_kind not an oracle
  field).
- Would try next (for whoever holds the queue): land wt/t121-ai4 through the normal
  gate (its own step-5 trace + verify_rows were the reason it stopped PARTIAL), then
  re-issue the carry-down against the landed tip with the file list corrected
  (+src/ai.rs) and the progress number named as the ai4 row.

## Numbers ledger (this ticket so far)

| what | number |
|---|---|
| gunner isolated, main 157d152 (baseline) | 2105613 / 38237 / 130, neg 2284867 |
| T113's ai4 row (wt/t121-ai4 only) | 477686 / 38237 / 40, neg 522293 |
| ai4 after T121's port+kind wiring (branch only) | 488098 / 38237 / 40, neg 533114 |
| captures used | 0 beyond the harness's own baseline pair |
