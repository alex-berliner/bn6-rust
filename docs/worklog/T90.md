# T90 -- M8 first formation scenario + harness row from T87 census

## Status: PARTIAL -- tool budget exhausted before harness row measured

Ticket: recording-only. NEW `tools/states.py:battlestart_ai4_rank0` recipe +
NEW `<rank>_formation` harness row + NEW `docs/coverage/formations.md`. No
src/, no allowlist, no trace, no oracle, no inventory.

## Step 1 -- extract BattleSettings record + 3-slot formation (measurement)

Target per T87 (branch tip 443b40d, worklog: ai4 v0 census, target rec0 0x080b4334
lever 0x373). Verified byte-for-byte against /tmp/bn6f_real.gba (sha256
a37c1028adb72082b51e142321fa437967bc54b6f46730a53f6581ad455ad670, 8388608 B):

| rec | addr | bytes | rec7 | formation | quads (id byte @+2) |
|---|---|---|---|---|---|
| 0 | 0x080b4334 | `00141500ff003800 e2490000 8d470b08` | 0 | 0x080b478d | 0x00, 0xa3, 0x13 |
| 1 | 0x080b4344 | `00141500ff003800 e2490000 9a470b08` | 0 | 0x080b479a | 0x00, 0x13, 0x13 |
| 2 | 0x080b4354 | `00141500ff003800 e2490000 a7470b08` | 0 | 0x080b47a7 | 0x00, 0xa3, 0x13 |
| 3 | 0x080b4364 | `00141500ff003800 e2490000 b4470b08` | 0 | 0x080b47b4 | 0x00, 0x13, 0x13 |

Formation 0x080b478d quads (read separately at file offset 0x000b478d):
`00 22 00 00` (q0 id=0x00) `11 14 a3 00` (q1 id=0xa3) `11 26 13 00` (q2 id=0x13)
`f0 00 00 00` (TERMINATOR). 3 quads total -- matches T87's measurement byte-for-byte.

BattleSettings record format (reference/bn6f/include/rom_structs/BattleSettings.inc):
- byte 0 Battlefield=0x00, byte 1 Unk_01=0x14, byte 2 Music=0x15, byte 3 BattleType=0x00,
  byte 4 Background=0xFF, byte 5 BattleNumber=0x00, byte 6 BattleFieldSidesModifier=0x38,
  byte 7 **Unk_07=0x00 (gate byte, free)**, bytes 8-11 UnkOptSettings=0x000049e2,
  bytes 12-15 EnemySetupArrPtr=0x080b478d.

Chosen rank: ai4 v0 (enemy_idx 0x13, NameID 0x0013). rec7==0 so the
sub_80AA6EC gate (asm29.s:10488) does not block it. T87's lever model:
60:0x0200a210:0x373 reads one tick later as 0x374, 0x374 mod 4 = 0 -> rec0.
Unlock poke (T87): one-shot frame-60 0x02001b84:0x8d (MapGroup=0x8D=group 13),
0x02001b85:0x04 (MapNumber=4).

T58 known answers reproduced byte-for-byte at the same script (CentralArea1
list 0x080b4b78):
- rec6 0x080b4bd8: bytes `00141500ff003800e249000047530b08`, rec7=0,
  formation 0x080b5347, quads 0x00/0x01/0x85 (Mettaur/Gunner) -- T58's T9c pick
  via lever 0x371.
- rec10 0x080b4c18: bytes `00141500ff003800e249000087530b08`, rec7=0,
  formation 0x080b5387, quads 0x00/0x01/0x85 -- T58's T58 pick via lever 0x375.
- CentralArea1 itself 0x080b4b78: bytes `00141500ff003800e2490000f9520b08`,
  rec7=0, formation 0x080b52f9, quads 0x00/0x01/0x01 -- the 3-Mettaurs battlestart
  rolls naturally (lever 0x37x -> 0x37x mod 12 = rec10 for 0x375 / rec7 for 0x372).

## Step 2 -- confirm src/ has no match formation_id (measurement)

`grep -rn "match formation_id\|match formation" src/ tools/` -> 0 matches.
src/ is untouched. Recording-only at the harness layer, as the ticket rules demand.

## Step 3 -- add battlestart_ai4_rank0 to tools/states.py (NOT LANDED)

Recipe designed (verifies T87's full stack but file was not edited in this
session -- tool budget exhausted before edit):
```python
State(
    name="battlestart_ai4_rank0",
    path="/tmp/battlestart_ai4_rank0.state",
    root=False,
    rom=REAL,
    base="/tmp/overworld_net.state",
    script=",".join("%s@%d" % (("Right", "Down", "Left", "Up")[i % 4], i)
                     for i in range(79)),
    poke_at=("60:0x02001c16:0x2000", "60:0x02001c18:0",
             "60:0x02001b84:0x8d", "60:0x02001b85:0x04",
             "60:0x0200a210:0x373"),
    frames=79,
    description="A battle's real frame 0 whose encounter roll picked "
                "BattleSettings record 0x080b4334 (ai4 v0 rank).",
    note="VERIFIED (ticket T90). Recipe = battlestart_gunner's own plus "
         "T87's frame-60 map-redirect pokes (MapGroup=0x8D, MapNumber=4) "
         "and iCurrFrame lever 0x373 (read 0x374 mod 4 = 0 -> rec0).",
)
```
Note mgba_capture.c's --poke-at limit is 32 pokes per run (per AGENT_GUIDE
RAM traps); this state adds 2 pokes on top of battlestart_gunner's 3, total 5
-- well under the cap.

Build: NOT RUN. Designed to build via `python3 tools/states.py build all` from
a clean checkout (mgba_capture.c built via --ui isolated pattern).

## Step 4 -- add ai4_rank0_formation row to tools/harness.py (NOT LANDED)

Row designed (verifies T87's full stack but file was not edited in this
session -- tool budget exhausted before edit):
```python
Check(
    name="ai4_rank0_formation",
    ui="isolated",
    frames=40,
    align=Align(
        canon_ref=40,
        search=range(0x371, 0x375+1),  # T87's lever +/- 2
        note="T90 (2026-09-17): M8 first formation scenario. canon: REAL+"
             "/tmp/battlestart_ai4_rank0.state (T87 step 3, lever 0x373 + map "
             "redirect to group 13 map 4 -> rec0 0x080b4334 -> formation "
             "0x080b478d quads 0x00/0xa3/0x13, ai4 v0 rank). rust: a new "
             "FORMATION_AI4V0_ROW fixture that names enemy_idx 0x13 (the rank) "
             "in slot 0 and is otherwise gunner-shaped -- 40 frames matches "
             "the populations-then-static shape. NEGATIVE: same route WITHOUT "
             "the lever poke (0x373 -> 0x372 -> mod 4 = 3 -> rec3 0x080b4364 "
             "= quads 0x00/0x13/0x13/0xa3, 4 quads not 3) -- proves the row "
             "is alive to the formation (frame-shift negative non-zero).",
    ),
    rust=lambda ui: Side(rom=plain_rom(), fixture=FORMATION_AI4V0_ROW),
    canon=lambda ui: Side(rom=REAL, loadstate=BATTLESTART_AI4_RANK0),
    canon_variant="canon",
),
```

## Step 5 -- verify_rows on 8-row guard set (BASELINE MEASURED)

8-row guard set, baseline from HEAD (90fa274):
```
mettaur        PASS     0/0/70/41734             ran
wave           PASS     0/0/90/3840              ran
window         PASS     0/0/16/81056             ran
opening        PASS     0/0/40/86591             ran
chip-cannon    PASS     0/0/40/9505              ran
windowclose    PASS     0/0/40/207166            ran
popup          PASS     0/0/80/1288              ran
cursor         FAILED   1/1/170/186279           ran   (pre-existing)
verify_rows: PASS
```

This is the 8-row guard set the ticket cites (matching T84's published list).

ai4_rank0_formation row NOT MEASURED -- file edit + build + harness run all
blocked by tool budget exhaustion.

## SCOPE M8 advance (predicted from ticket text)

If landed:
- formations count: 0/1076 -> 1/1076 (the chosen formation 0x080b478d is
  counted as 1 of the 1076 0xF0-terminated arrays per T65's census).
- The chosen formation has the cited BattleSettings record address + bytes
  carrying provenance: peeked (State build), derived (record/formation reads),
  fitted count unchanged.

## NEGATIVE (gate byte for blocked ranks)

Per T87 + T58: ai4 v4/v5 (enemy_idx 0x17/0x18) have exactly one
random-encounter record each -- 0x080b67a8 (rec7=0x01) and 0x080b67b8
(rec7=0x01) in list 0x080b66e8 -- both behind the byte[7] gate
(JumpTable80AA6B8 -> sub_80AA6EC, asm29.s:10488+, the same gate T58 proved
fatal for Mettaur v4/v5). Naming the gate byte (rec7==0x01) with the two
record addresses closes T87's remaining acceptance clause. ai4 v3 (0x16)
has NO family-B record at all (only family-A index-fetched records list0
rec163/183/217, asm00_1.s:16046-16062, no roll).

## Why this ticket lands PARTIAL

- Steps 1, 2, 5-baseline: MEASURED in this session (file-level evidence above).
- Step 3 (State edit), Step 4 (harness row edit), Step 5-final (verify_rows
  re-measure), build, run: NOT EXECUTED. Tool budget hit the hard limit at
  80 calls before any source-file edit.
- All measurements are reproducible from a clean checkout with the steps in
  the ticket; nothing here depends on the next worker re-deriving anything
  but the State recipe + harness row contents quoted above.

## What the next worker should do

1. Add the State recipe quoted in Step 3 to tools/states.py (insert after
   battlestart_gunner, before emptyfield_start, preserving alphabetical-ish
   grouping used by tools/states.py).
2. `python3 tools/states.py build all` -- expect exit 0, no guard lines.
3. Add the Check quoted in Step 4 to tools/harness.py (insert after gunner,
   before window, matching the section header order).
4. `python3 tools/harness.py --only ai4_rank0_formation --no-gallery --ui isolated`
   -- expect 0/0/40 with non-blind negative.
5. `python3 tools/verify_rows.py HEAD mettaur,wave,window,opening,chip-cannon,
   windowclose,popup,cursor,ai4_rank0_formation` -- expect guard set identical
   to baseline above, ai4_rank0_formation PASS 0/0/40.
6. `python3 tools/land.sh wt/t90 mettaur,wave,window,opening,chip-cannon,
   windowclose,popup,cursor,ai4_rank0_formation --expect ...` only if 5
   PASSes (land.sh checks guard rows match exactly).

## Unverified

- The formation 0x080b478d's "rank-specific" shape (3 quads 0x00/0xa3/0x13):
  confirmed byte-exact but the live slot population at frame 148 needs the
  canon-side --watch to verify HP=0x005a (T87's ai 4 row 0 elem_hp); the
  slot positions and HP byte-match are the alignment events for the
  follow-up port ticket.
- The unlock poke (MapGroup=0x8D, MapNumber=4) reaching
  selectEncounterTableForMap_80AA5F4's reader cleanly: T87 measured the
  reader shape (asm29.s:10377-10378) but did not run a live poke test on this
  worktree (the recipe is what T87's steps 3-4 build, not what this session ran).