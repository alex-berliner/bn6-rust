# T154 — Second emotion face: extend FACE_INDEX with a non-cross chosen drawn face

Start: wt/t154 from main 940ffa7 (T152b/c/T146b/T151b/c judged). The ticket
inherits T122's emotion row already on the branch (emotion_syn + emotion_face_b
+ emotion_skip), T105's `src/emotion.rs::FACE_INDEX` with Full Synchro
cross-face swap (5..9→10..14, slot offsets in rows 10..14), and the 23-entry
slot table in `docs/coverage/emotion.md`.

Goal: pick one drawn face from `off_801CD08` (asm00_2.s:27580-27603, 23
entries, OBJ at (0,18)/(32,18), palette bank 12) that is NOT the Full Synchro
cross-face arm, add it to `FACE_INDEX`, prove it on the harness, and check the
canaries stay byte-identical (mettaur 0/0/70/41734, cursor 1/1/170/186279).

## Step 0 — read the ticket's chosen table

`off_801CD08` (asm00_2.s:27580-27603), 23 pointer words, deltas from
dword_872D814:
`[0x0000, 0x0180, 0x0300, 0x0480, 0x0600, 0x0780, 0x0880, 0x0980, 0x0a80,
0x0b80, 0x0c80, 0x0d80, 0x0e80, 0x0f80, 0x1080, 0x0780, 0x0880, 0x0980,
0x0a80, 0x0b80, 0x1700, 0x1700, 0x1800]`

Already byte-identical to the landed `FACE_INDEX`. The cross-face swap is
applied at slot-resolution time, not in this table.

## Step 1 — baseline (no edit)

(measured below)

## Next

Write the table into the worklog once built, then verify_rows.
