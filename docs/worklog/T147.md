# T147 — M2 battle_full re-measure on T131's family-A scripted scenario

Ticket: measurement + tools wiring. Capture canon + ours on `battlestart_scripted` (the ticket's
"battlestart_opt" shorthand; states.py has no recipe by that name -- T131's scenario is
`battlestart_scripted`), trace + oracle field map, top-three fields, docs/coverage/battle_full.md
update, verify_rows on the full isolated table. NO src/, NO allowlist. Base 292dca0 (main).

## Baseline (before any edit)

- `python3 tools/harness.py --only mettaur --no-gallery`:
  `mettaur isolated PASS total 0 worst 0 frames 70 | negative: not blind (total 41734)`.
- Canon trace on the scenario already worked (T131 left it canon-only):
  `python3 tools/trace.py record canon battlestart_scripted --out /tmp/t147/c_scr` -> 113 capture
  frames, /tmp/battlestart_scripted.state rebuilt by `states.py build all` (hygiene gate, exit 0).

## What canon reads on the scripted scenario (the capture that feeds the map)

Capture frame 69 = battle frame 0 (T131's peeked populate frame, re-confirmed: e1 name 0x0088
appears exactly at 69). Over the whole 113-frame capture:

- `settings` (0x02001b9c, oGameState_CurBattleDataPtr) = 0x080AF8A0 = rec163 -- the family-A
  record T131 fielded, stable through the window.
- Sequencer word 0x0203CA70 (`banner`) = 0x00000000 on EVERY captured frame. T87 measured 0 at
  capture 320 on the parent route; this window never leaves the stall. T58's stuck-sequencer
  blocker: canon's scripted-entry battle PARKS in entry.
- Every actor reads (state,action) = (4,0): mm (panel 2,2) HP 100; e1 0x0088 (4,3) HP 250;
  e2 0x0088 (5,1) HP 250; e3 0x0016 (6,2) HP 200 -- rec163's formation exactly (T131 step 3,
  re-read here). e1_timer bounces 2/1 from capture 86 (battle k=17); mercy pointer chase reads 0
  ([mm+0x54] is 0 on this route); gauge 0; rank/zenny 0 (no result).

## The rust side (this ticket's states.py edit)

`battlestart_scripted` gains a `rust` spec (was canon-only, T131): rec163's slots as the
descriptor names them -- enemies=3, enemy_kind 0x15 (slots 0/1/2 = kind 1 Gunner; the port's kind
table has no ai4, so canon e3's 0x0016 fields as a Gunner BY DESIGN -- that gap is the map's
content), panel_col [4,5,6] / panel_row [3,1,2] / mask 0x07 (rec163's quads), enemy_hp 250 (FIRST
slot only, FIXTURE.md +32), megaman (2,2) HP 100 (peeked off this capture), and battle_full's
standing T7r contract below that (gauge=1, deck, window pre-picks, script A@170 -- all outside
this 40-frame window). Two wiring edits so the tools can judge it:

- tools/trace.py `E1_SLOT["battlestart_scripted"] = "e1"` -- canon slots 1..3 are ALL live here
  (unlike the PAUSED rows whose slot 1 is a shell) and the rust export's enemy fields are
  enemies.first(), so the honest pair is canon e1 (0x0203aa88) <-> rust slot 0.
- tools/oracle.py: `ENEMY_SLOT["battlestart_scripted"] = 1` (table slot 0 is MegaMan itself;
  0x0203aa88 is index 1 -- the `opening` entry is the same three-enemies-in-slots-1..3 shape) and
  main() now routes any two-sided TRACE_SCENARIOS entry through `_scenario_check` (the T47 flow)
  without needing harness.py's SCENARIO_ROWS tuple. One-sided scenarios stay unroutable.

First attempt had ENEMY_SLOT=0 (would have watched MegaMan's struct as "the enemy"); caught by
reading the ENEMY_TABLE comment before running, never measured.

## The map (tools/trace.py diff, 40 compared frames, canon 69+k <-> rust export 0+k)

| field | divergent | first k (canon frame) | canon | rust |
|---|---|---|---|---|
| sequencer (0x0203CA70 low half) | **40/40** | k=0 (69) | 0x0 | 0x8 |
| mm_state_action | **40/40** | k=0 (69) | (4,0) | (4,8) |
| enemy_state_action (canon e1 <-> rust slot 0) | **40/40** | k=0 (69) | (4,0) | (4,10) |
| mm_anim / mm_panel_x / mm_panel_y / mm_timer | 0/40 | -- | match | match |
| enemy_anim / enemy_panel_x / enemy_panel_y | 0/40 | -- | match | match |
| rank / zenny | 0/40 | -- | match | match |
| rng_cadence | 0/40 | -- | match | match |

TOTAL 40/40 frames carry a judged divergence; first: enemy_state_action at k=0 (canon frame 69).
Rust export counter note (F33d stall): 1 repeated / 38 jumped rows, 1-frame drift by the last row
-- rows pair by capture index, the tool prints this before any number is read.

oracle.py battlestart_scripted (first run on a two-sided scenario) agrees field for field and adds:
INFO mm_hp 100=100, enemy_hp 250=250 (rec163's row-3 HPs reproduced by the fixture from the FIRST
slot; e2/e3 ride the kind default -- unjudged), gauge info-only (canon 0 vs ours GAUGE_FULL --
T7r contract, not judged), pixel total 1225257 worst 38400 first-nonzero k=0 (the phases differ
visually from the first frame). Negative controls, honestly weak here: trace `--shift 1` reads the
same 40/40 with the same first k (canon frame 70) and the oracle names its own field negative
BLIND -- the window is ONE frozen canon state (every field holds exactly one value pair), so there
is no state timing to shift. The pairing's real evidence is the byte-level statics: panels, anims,
timers, HPs and the 1-step rng cadence hold for 40 straight frames at the fixed (69, 0) offset,
and canon_ref 69 is T131's peeked populate frame re-confirmed at k=0 (e1 name 0x0088 appears
exactly there).

## Top three divergence fields (the ticket's ask), ranked

1. **sequencer** (dword_203CA70 low half), 40/40, k=0: canon 0x0 vs rust 0x8. Cite:
   stepBannerSequencer_800801C's dispatch table off_8008038 (asm00_1.s; renames.md:96). The phase
   carrier: canon's scripted route never advances the word (T58's stall; 0 through this capture's
   113 frames, T87's 0 at 320), ours is a descriptor battle at the fight state from export 0.
2. **mm_state_action**, 40/40, k=0: canon (4,0) vs rust (4,8). Cite: playerObject_main_80EA460
   (asm31.s:107131) / playerObject_update_80EA484 (asm31.s:107147) -- the player rides the same
   stalled entry (action 0) while ours is in the fight action.
3. **enemy_state_action**, 40/40, k=0: canon (4,0) vs rust (4,10). Cite: the Gunner exec family --
   sub_8113162's aim predicate (asm32.s:10248) and sub_8112F4E/sub_8113002/ai_8113038's volley
   (asm32.s:9958-10102) (src/gunner.rs's own cites): our slot-0 Gunner is already in action 10
   while canon's parked Gunner holds entry action 0.

All three are 40/40 with ZERO unique-frame separation -- one mechanism (canon parked in entry vs
our fight phase) drives all three, so the ranking is by role: the sequencer is the phase carrier
(1), then the actor fields (mm 2, enemy 3; enemy_state_action is the diff's literal first
divergence and the enemy entry machine was the battle_full map's #1 group).

## Unjudged / what this map cannot see

- canon e2/e3 read parked (4,0), HP 250/200; the rust export's e2/e3 columns are 0xFFFF sentinels
  by design ("No second/third enemy in this model", src/battle.rs trace_snapshot) -- never judged,
  same rule as oracle.py's has_enemy. The ai4 gap (canon e3 0x0016 vs our Gunner) lives ENTIRELY in
  those unjudged columns: this map cannot see it, and naming it is enemy-family work (M3), not a
  field re-fit.
- The settings word (0x080af8a0 on canon) has no rust counterpart (no adoption path in the port) --
  extra_watches carries it as info only.
- Whether canon's stall EVER breaks on this route beyond capture 320 (T87's last datum) is open;
  if it never does, the parked-entry window IS the scenario's comparison contract.

## Verify

verify_rows on the full isolated table (all rows from `--list`, no src/ change so the ROM hashes
identically to main) -- run and recorded at the bottom of this log; result appended below.

## Capture ledger

Baseline: harness mettaur (x2 -- first run's line was cut by my own tail; re-run warm, same
numbers), trace record canon 113f. After the tooling edit: states.py build all (gate), trace
record rust 41f, trace diff x2 (shift 0 / shift 1, offline), oracle battlestart_scripted (canon
113f + rust captures, one invocation). ~7 captures total.

## Verify result (full isolated table)

`python3 tools/verify_rows.py wt/t147 <all 69 rows from --list except rollup> --expect cursor=1/1/170/186279
--expect opening=0/0/40/86591 --expect mettaur=0/0/70/41734 --expect cannon=0/0/40/9505 --expect buster=0/0/28/3167
--expect chip-use=0/0/30/7768 --expect wave=0/0/90/3840 --expect windowclose=0/0/40/207166 --expect popup=0/0/80/1288
--expect result=0/0/40/111839` -> **verify_rows: PASS** (69/69 rows ran, no BLIND negative, all ten expectations
matched -- the nine guard rows at their T125/T131 zeros, cursor at main's known tear byte-for-byte, pinned fresh
in this session before the sweep).

Six rows print FAILED primaries; none is gated and none is this branch's: the branch's src/ is byte-identical to
main's (git diff 292dca0..a00bbca --stat = tools/{oracle,states,trace}.py + docs only), so the verify checkout at
a00bbca measured MAIN's own lines: cursor (isolated AND integrated, the long-documented 1/1/170/186279 tear --
expected, MATCH), and the unpinned field-bg1 3600/3600/40/174345, field-bg2 3600/3600/40/3600, field-bg3
380219/24906/40/405125, buster_charge 2498/188/32/5113, gunner 2105613/38237/130/2284867 (the ui=both rows'
primary = their integrated variants; their isolated lines were not pinned by this sweep and their main-side state
predates this ticket -- flagging for the next field/charge/gunner ticket, not a T147 regression).

Full log: /tmp/t147/verify.log (69 rows; the sweep waited on a concurrent verification's lock, then ran clean).

## Ticket numerics, for the next reader

- The ticket's "T125 ... 174/540 sequencer frames" does not match T125's own published map (273/540 sequencer,
  507/540 union; 174/540 is T49-base's t7b file, which T125's section already flags as a conflation). This ticket
  re-measures NEITHER number: it adds the NEW scenario's map. battle_full's own row numbers did not move (no src/).
- "tools/states.py battlestart_opt": no recipe/scenario of that name exists or ever existed; the scenario is
  `battlestart_scripted` (states.py TRACE_SCENARIOS, T131).
