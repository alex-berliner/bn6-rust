# T218 worklog — NaviCust second handler (M7)

**Status: IN PROGRESS.** Branch `wt/t218`, worktree /tmp/bnwt/t218, base 0a354b3.

## Baseline (before any edit; harness, this session, main = 0a354b3)

```
mettaur    isolated    PASS  total 0 worst 0 frames 70  | negative: not blind (total 41734)
chip-cannon isolated   PASS  total 0 worst 0 frames 40  | negative: not blind (total 9505)
cursor     isolated    FAILED total 1 worst 1 frames 170 | negative: not blind (total 186279)
```

All three match the canary lines in the ticket context (cursor 1/1/170 is the
pre-existing T90 failure). Capture path is up; proceeding.

## What is known (from T110 + T150 + T132 worklogs, read-only)

- `navicust_jt_NCPs` = 47 entries, asm/asm37_0.s:2111 (jt 2112-2158), bodies 2161-2780.
  Full enumeration with per-handler cites: docs/inventory/navicust_handlers.json.
- T110: the handler chain (`applyNavicustPrograms_813C684` → dispatch) only runs from
  boot/map/cutscene callers, NEVER battle init. Poking the equipped-NCP channel
  (byte_2006DD8) or the map NaviStats record (0x020047cc) mid-battle changes 0 pixels.
  Battle copies eBattleNaviStats203F4AC read 0x0 on the battlestart route and had no
  in-window writer found from that state.
- T150/T132 already covered the SuperArmor handler (Set(0x23,1), asm37_0.s:2168).
- T206 (proposed, not landed): "first non-SuperArmor handler" = Custom1 (index 2,
  Set(0xa,...), asm37_0.s:2180) by enumeration order. Its pick is not on main, so I
  treat its store site as slot 0xa (CustomLevel) and choose a DIFFERENT slot.

## Handler choice (to be confirmed against canon asm before transcribing)

Candidate: **navicust_NCP_FstBarr** (First Barrier), jt entry index 7,
`Set(6,1)`, cite asm/asm37_0.s:2265 (jt entry 2119). Store site = NaviStats
slot 0x6 (FstBarr) — distinct from T206's 0xa and from SuperArmor 0x23.
Battle-start effect (barrier at battle start) is the natural fit for
"wire BattleStart to call the give chain".
Fallback candidates if FstBarr's consumer is not measurable in the
chip-cannon window: UnderSht (0x1d, asm37_0.s:2331), Shield (0x7=0x3b,
asm37_0.s:2276).

## Plan

1. ✅ Baseline canaries (above).
2. Read canon FstBarr handler asm + its battle-init consumer; cite file:line.
3. Create src/navicust.rs with the transcribed store site; wire BattleStart.
4. Add ncp_row2 (positive = NCP given on both sides; negative = NCP not given on
   rust side = chip-cannon baseline) + needed state/fixture bits.
5. Re-measure row + canaries; verify_rows full isolated table; commit per step.

## Ideas tried and dropped

(none yet)

## Next worker

- If this log stops mid-way, the state is: baseline done, handler pick pending
  canon confirmation. Inputs live at /tmp/bn6f_real.gba etc.

## Step 2 — canon recon (measured + cited)

- Handler pick: **navicust_NCP_FstBarr** (jt entry 2119), asm37_0.s:2265-2272 =
  `SetCurPETNaviStatsByte(0, 6, 1)`; store site = NaviStats slot 0x6 via
  SetCurPETNaviStatsByte (asm00_2.s:10401-10414). Distinct from T206's presumed
  Custom1 (slot 0xa) and from the already-ported SuperArmor (0x23, T132/T150).
- Consumer FOUND: `sub_8013892` (asm00_2.s:10572-10597): at MegaMan battle-init,
  reads battle NaviStats slot 6 (`GetBattleNaviStatsByte_AllianceFromBattleObject`,
  struct = eBattleNaviStats0 0x0203ce00, GetBattleNaviStatsAddr asm00_2.s:10188);
  non-zero -> `barrierTakeDamage_801A7CC` (asm00_2.s:22695, sets CollisionData.Barrier
  + HP fields from BarrierHpByType_8020B2C, ROM@0x08020B2C: type1 -> Unk_16=10,
  Unk_17=0, Unk_1a_1b=0xffff) then `sub_80E0D98(AIData+0x60)` (asm31.s:86253-86275)
  which SPAWNS the bubble object (object_spawnType4 var7) linked to the player.
  Type 1 = the Barrier chip's own barrier (src/battle.rs BARRIER_HP_BY_PARAM[1]=10,
  palette teal 0) — the demo-barrier row already verifies our bubble render.
- PET->battle flow: init_8013B64 (asm00_2.s:10982-11050) copies ROM DEFAULTS
  (NaviBaseHpByRow_80210DD, ROM@0x080210DD) into the battle NaviStats — NOT the
  PET record; the equipped value flows PET record (0x020047d2) -> master
  eBattleNaviStats203F4AC -> (battle_copyStructsIncludingBattleStats_800b2d8,
  asm00_1.s:17812+) -> eBattleNaviStats0.
- TIMING (measured): battlestart.state is PAST the whole chain. --poke
  0x0203ce06:1 at load STICKS (peek frame 39 = 0x01) and changes 0 pixels over
  40 frames (consumer already ran pre-snapshot). Same for poking MegaMan's
  CollisionData barrier fields (0x020384f6/0x02038506/0x0203850a, CollisionDataPtr
  0x0203aa04 -> 0x020384f0, peeked): all target bytes read 0, poke -> 0 pixel diff
  — the bubble is an OBJECT, not collision-state-derived, so a derived-state poke
  cannot reproduce the visual either.
- CONSEQUENCE: canon side must run its own chain from a pre-init state
  (PET-record poke at load, before the transition copy). T151's
  battlestart_scripted recipe in states.py is the base to cut earlier.

## Ideas tried and dropped
- Poking battle NaviStats slot 6 / collision barrier fields on existing
  mid-battle states: measured dead (0 pixels, above). Barrier is an object.

## Step 3 — supervisor decision + the b3 route hunt (measured)

Supervisor decision (this run): b3 — the save-state route; time-box two state
recipes, then conclude; if the byte cannot arrive at capture frame 0, commit
the gated transcription + wiring and report PARTIAL.

Recipe #1 (`battlestart_ncp_pre` = battlestart_scripted cut to frames=77,
committed in tools/states.py): poke the DISPATCH INPUTS at load — program
record 0 halfword := 0x001C (FstBarr, jt entry 7; records = 8-byte stride at
0x02004190, sub_813B9FC asm37_0.s:421-428; halfword/4 = jt index,
applyNavicustPrograms asm37_0.s:2044-2056) + equipped-list slot
unk_2006CF8[0] := 0x01 (dispatch reads it via sub_813BD0C asm37_0.s:858-861;
list is EMPTY on this save: 0x02006CF8/0x02006C88 read all-00 on
overworld_net.state). MEASURED: 0 pixels over 48 frames; PET slot 6
(0x020047d2) = 0x00 at frame 30; battle slot 6 (0x0203ce06) = 0x00. The
record halfword SURVIVES (0x001c at frame 30) but the chain never re-runs at
the map->battle transition — T110's caller census stands (boot/navicust-exit/
cutscene only, NOT cbGameState-on-transition).

Recipe #2 (craft the .srm so the save itself carries FstBarr equipped, cold
boot the real ROM, walk into battle): NOT RUN — out of budget this session.
unk_2006CF8 reads all-00, so the .srm's equipped-program encoding cannot be
located by pattern match on this save; the morning pass would need the
NaviCust screen's save layout (sub_813BA08's record-writer callers are the
trail). If frame 0 of such a capture reads 0x0203ce06 = 0x01 and the barrier
draws, ncp_row2 is buildable exactly as the ticket prescribes.

## Step 4 — what landed (gated transcription, per supervisor fallback)

- src/navicust.rs (NEW): SetCurPETNaviStatsByte store site (asm00_2.s:10401-
  10414), navicust_NCP_FstBarr = Set(6,1) (asm37_0.s:2265-2272), give_chain —
  all behind const GIVE_FSTBARR = false. The consumer is NOT ported yet
  (sub_8013892 asm00_2.s:10572-10597 + barrierTakeDamage_801A7CC :22695 +
  bubble spawn sub_80E0D98 asm31.s:86253); enabling the gate without it
  diverges from canon's measured no-op.
- src/battle.rs Battle::new: one gated give_chain call at battle start
  (ticket's "wire BattleStart"), constant-gated because the descriptor's
  flags byte is full through bit 7 (fixture.rs:115) — a second flags byte is
  the next worker's descriptor-contract step.
- src/main.rs: mod navicust.
- Post-edit canaries (harness, this session): chip-cannon 0/0/40/9505,
  mettaur 0/0/70/41734, cursor 1/1/170/186279 — BYTE-IDENTICAL to baseline.
- NOT DONE: ncp_row2 (no live route), its harness row + negative, the full
  isolated verify_rows sweep, docs/coverage/navicust.md update. T218 stays
  OPEN per the supervisor's fallback (report PARTIAL).

## Next worker (in order)

1. Craft the .srm route (recipe #2 above) — the only measured-plausible path
   to canon running its own chain before capture frame 0.
2. If lit: add the NCP descriptor bit (second flags byte, FIXTURE.md +
   fixture.rs + harness), port the consumer (sub_8013892 + barrierTakeDamage
   + bubble spawn; our chip-barrier render is already verified 0-diff on
   demo-barrier), add ncp_row2 = chip-cannon scenario + NCP given on both
   sides, negative = not-given variant (must read 0/0/40/9505), canaries +
   full verify_rows.
3. T206's pick is NOT on main; re-check it before assuming Custom1 (slot 0xa).
