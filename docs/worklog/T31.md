# T31 — measure the k=0..2 group of `field`'s integrated residue (and the bg1/bg2 one-capture-later pairing question)

Worktree: /tmp/bnwt/t31-field-k012, branch wt/t31-field-k012,
CARGO_TARGET_DIR=/tmp/ct_t31-field-k012.

**Outcome: measurement-only landing.** No `src/` change, no value change
(FIELD_ZERO stays 471/748; step 5's precondition never fired). Comment-only
record appended to tools/harness.py's FIELD_ZERO block (items (4)/(5)) plus
this worklog.

## Step 1 — baseline (recorded before touching code)

Harness rows, worktree HEAD = main 79d3c17:

```
field      isolated    PASS  total 0     worst 0     frames 40 | negative: not blind (total 1139)
field      integrated  FAILED (allowed: AUDIT-6 ... <=28000) total 32623 worst 5806 frames 40 | negative: not blind (total 173631)
field-bg1  isolated    FAILED total 4800  worst 4800  frames 40 | negative: not blind (total 175545)
field-bg2  isolated    FAILED total 4800  worst 4800  frames 40 | negative: not blind (total 4800)
field-bg3  isolated    FAILED total 380263 worst 24906 frames 40 | negative: not blind (total 405169)
```

All five match the ticket's acceptance expects exactly. Field integrated's
per-frame (k, diff): 0..5 = 5806, 5319, 4698, 0, 0, 945; then 0 up to k=23;
24..39 = 119,297,454,583,756,932,1108,1284,1375,1375,1375,1373,1254,1190,
1190,1190 (the RESULT-window group, total 15855; 945+15855+15823 = 32623 —
T29's partition reproduced live).

## Step 2 — scroll-counter dumps

Both T25/T29 dump sets still present, **nothing re-captured**:
- `/tmp/bn-f47/canon_scrollcnt.bin` (2800 B = 175 caps x 16 B; eBGScrollCBCounters
  0x02009690; cap134 = -64208/-32104, cap135 = -64216/-32108).
- `/tmp/bn-t25-watch/{rust_xq,rust_yq,rust_battleframe,rust_marker}.bin`
  (210 u32 watches from load; TRC2 mirror 0x020000B2/+54; battle 0 = watch
  index 8, watch index = absolute capture frame).

**Convention finding the next worker needs:** the /tmp/bn-t25-watch dumps are
at the **OLD seed 466/745** (watch_scroll.py ran before the fixture move), and
that dir's kept `analyze.py` pairs canon cap `135+k` with mirror watch index
`113+k` folding mod 512 — a superseded convention. The landed model is
one-capture-later (visible register at compared capture c = mirror at c-1)
with equality mod the 256-px content period; under it, with dump seed 466/745:
- seed 466/745 gives the T29-recorded old-seed series [(2,1),(1,0),(2,1),(1,1)]
  period-4 (H [2,1] period-2, V [1,0,1,1] period-4) — artifact (3) reproduced
  exactly;
- seed 471/748 (dx=+5,dy=+3 on the dump) gives k'=0 = (+1,0) and k'=1..39 all
  (0,0) (canon caps 135..174 <-> rust caps 121..160) — artifact (1) reproduced
  exactly. Machinery validated before use.

Row indices: the `field` row compares canon cap 130+k <-> rust abs cap 116+k
(origin 8 + landed offset 108), k=0..39; **T29's "k=0..2" residue group is row
k=0..2 = canon caps 130..132.** The T25/T29 solve texts' "k=0" is bg1-band
indices (canon 135), 5 frames later — kept distinct throughout below.

## Step 3 — the k=0..2 verdict: NOT SCROLL (two independent measurements)

**(a) Exhaustive solve, all 1024 quarter-values per axis, both pairings, row
k=0..2** (model: canon counter->register same-capture `(counter>>4)&511`; ours
`-((x_q+3)//4)&511`; candidate seed s shifts the dumped mirror by
(s-466)/(s-745) mod 1024; equality mod 256):

| pairing | axis | seeds zeroing row k=0..2 | best total over k=0..2 | seeds zeroing ramp k=6..39 |
|---|---|---|---|---|
| one-capture-later (landed) | H | {465,466} | 0 px | {471,472} |
| one-capture-later (landed) | V | {744,745,746} | 0 px | {748} |
| same-capture | H | **NONE** | 1 px (at k=0) | {469,470} |
| same-capture | V | {743,744,745,746} | 0 px | {747} |

The k=0..2 zero-sets and the ramp zero-sets are **disjoint** (one-capture-later:
{465,466}∩{471,472}=∅, {744..746}∩{748}=∅). So: *no pairing under the landed
model beats 15823 without unfixing the ramp* — the best k=0..2 seed (466/745)
predicts H [1,2] period-2 / V [0,1,1,1] period-4 residual across the 34 ramp
frames whose pixels currently read 0. At the landed 471/748 the model itself
predicts only (-2,-1),(-1,-1),(-1,-1) px on row k=0..2 — sub-2px.

**(b) Pixel translation sweep, the row's own integrated pairing**
(harness.run serial, origin 8, canon 130+k <-> rust 116+k; the fresh capture
reproduced 32623/5806 exactly). Minimum over the whole dx,dy ∈ [-4..4]² grid:

- k=0: base 5806; **min at (0,0)**; best non-zero shift (dx=-1,dy=0) = 11123.
- k=1: base 5319; **min at (0,0)**; best non-zero (dx=-1,dy=0) = 10675.
- k=2: base 4698; **min at (0,0)**; best non-zero (dx=-1,dy=0) = 9187.

Every shift makes every frame ~2x WORSE: the 15823 is transition CONTENT in
place, not displaced content. Precise negative delivered: *no pairing under
the landed model beats 15823; best is 15823 px at shift (0,0) on all three
frames.*

**Mechanism named (peeked counters):** across canon caps 128..142 the canon H
register rounds to half-px steps (86,85,85,84,84,83,83,82,82,81,81,...) while
our x_q mirror holds 686 for caps 116..120 (STEP_HOLD, the 192-tick cycle) —
at the F45 pre-boundary transition the two quantizations diverge by 1-2 px AND
the resident art differs mid-transition. No seed fixes that without unfixing
the ramp; no pixel shift fixes it because the content itself differs.

## Step 4 — bg1/bg2 one-capture-later row pairing: BOTH k=0 bands vanish, only bg2's row survives the move

field-bg1/field-bg2 checks rebuilt in-script with rust_offset 113 → 114 (same
event-locked canon_ref 135; no harness.py row edit), driven through
harness.run serial:

- **field-bg2 rust_offset=114: total 0, worst 0, all 40 frames** — the 4800
  VANISHES (panels content is static, so the moved pairing holds the window).
- **field-bg1 rust_offset=114: total 170745, worst 9923** — its k=0 band ALSO
  vanishes (k=0 reads 0; nonzero ks start at k=1: 9923, then 8740, 7237, 9143,
  ... on odd-ish ks), but the moved pairing mispairs the scrolling backdrop on
  every k>=1: profiled k=1 = 9923 px over rows 0..159, cols 0..229 (full
  height — canon's register is stalled across the transition while ours has
  resumed, so one-capture-later pairs different scroll content).

Answer to the ticket's question: **neither 4800 persists as a k=0 band under
the rust-later pairing — both vanish.** The band is a ONE-CAPTURE TRANSITION
EDGE on both layers (rust reaches canon-135's appearance one capture later
than the landed pairing expects), not static content and not two coincidental
regions — bg1's and bg2's 4800 remain ONE shared F45 transition artifact, and
bg1's half of that sentence is now pairing-measured, not only
translation-swept. But no row-level pairing move fixes both rows (bg1's row
explodes at offset 114), so this is not a landable pairing change.

## Step 5 — not fired

No pairing improves both `field` lines (the solve sets are disjoint from the
ramp's; the pixels do not respond to any shift; the offset-114 pairing makes
bg1 35x worse), so **FIELD_ZERO stays 471/748 and the guard list was not
run**. ROM byte-identical to main: `git diff main --name-only` = tools/harness.py
(comment lines only) + docs/worklog/T31.md; no `src/`, no row definitions, no
scoreboard region touched; builds are deterministic within a session (two
builds of the worktree = 435daeaf… twice, 583852 B).

## Step 6 — what the next `field` ticket should attack

Of the three residue groups in field's 32623:
1. **k=0..2 (15823 px) — MEASURED, not scroll.** Transition content in place;
   the model has at most 2 px of phase to offer there and the seeds conflict
   with the ramp. Not worth a scroll ticket.
2. **k=5 (945 px, top band) — measured twice now** (T29 translation sweep, T31
   pairing test): one-capture transition edge shared with the bg1/bg2 rows.
3. **k=24..39 (15855 px, rows 49..154) — still argued** (canon's own
   RESULT-window slide-in vs our `shown` layer). This is the only group
   without a measurement of its own; the next `field` ticket should attack
   THIS one (e.g. a `--only-bg`/shown-layer attribution row for k=24..39, the
   bg3 note already predicts the shown-vs-HUD mismatch).

## Tried and dropped

- `analyze.py`'s convention (canon 135+k <-> mirror 113+k, mod 512): reproduces
  nothing; superseded, kept only as an archaeology warning above.
- First solve run assumed the dumps were at the landed seed 471/748: gave a
  contradictory picture (k=0..2 zero at the landed seed, which contradicts the
  pixel evidence). Caught by validating against T29's two recorded artifacts;
  re-derived dump seed = 466/745. Lesson recorded in Step 2.
- First pixel sweep used `(a != b).sum()` (counts differing CHANNELS, 2.87x
  inflated): must be `cc._count_diff` (3-plane OR = differing pixels). All
  numbers above are `_count_diff`.

## Standing limitations

- The k=0..2 verdict is measured against whole-pixel translations of the FULL
  frame; a per-layer (bg0/bg1/bg2/bg3) translation sweep could in principle
  hide a layer-local scroll fix behind other layers' content — but the
  register-level solve already caps the possible phase at 1-2 px on the layer
  the model speaks for (BG1), and the full-frame sweep at (±1,0) makes things
  ~2x worse, so a hidden sub-pixel BG1-only win cannot be large. bg2/bg3's
  panels/HUD do not scroll.
- tools/trace.py:56 still TypeErrors on main (int(O.MM_ORACLE_ADDR, 0) where
  oracle.py:149 defines an int); not touched (not this ticket's file), shim
  not needed — no state-trace subject here.
- The offset-114 captures were one-off runs through harness.run (scratch dirs
  /tmp/bn-a9257321/h_{rust,canon}_* keyed t31-cap114); they are not rows and
  nothing was re-regioned.

## Acceptance evidence

- `python3 tools/verify_rows.py wt/t31-field-k012 field,field-bg1,field-bg2,
  field-bg3,tiles,cursor --expect ...` (branch d1d84e2, clean checkout
  /tmp/bnwt/verify-d1d84e2): **verify_rows: PASS**, all six expects MATCH —
  field 0/0/40/1139, field-bg1 4800/4800/40/175545, field-bg2 4800/4800/40/4800,
  field-bg3 380263/24906/40/405169, tiles 0/0/8/12197, cursor 1/1/170/186279
  (identical to main). field integrated measured in-session this ticket:
  32623/5806/40, negative not blind (173631).
- ROM byte-identical to main: `git diff main HEAD -- src/ Cargo.toml
  Cargo.lock vendored` = 0 lines; the only branch changes are tools/harness.py
  comment lines (+49, zero deletions) and docs/worklog/T31.md. Builds are
  deterministic in-session (two builds = 435daeaf… twice, 583852 B).
- Negative-naming kept throughout: the solve reports the FULL zero-sets and
  the disjoint intersections, not just best values; the sweep names the best
  NON-zero shift per frame; the pairing test names where bg1's 170745 lives.
- Note: main advanced to d55ff2f (TODO-only, judge_append T32-34) after this
  worktree was cut at 79d3c17; merge-base is 79d3c17 and the TODO delta does
  not touch src/ or tools/, so the byte-identity argument above is unaffected.

## Postscript — 2026-09-16 (post-verify repair, verifier-hyper)

Verifier-hyper audited ef147c7 (GO, comment-only items (1)-(3)) and promoted
this worklog's step-2 seed warning ("the /tmp/bn-t25-watch dumps are at the OLD
seed 466/745") into the landed FIELD_ZERO block itself, replacing the stale
provenance cite line: the dump stream is proven the 466/745 capture three ways
(all-even x_q across 210 watches with SCROLL_X_Q = 2 at src/backdrop.rs:66;
anchor x_q[8]=470 = 466+2*2, y_q[8]=747 = 745+2 at battle frame 0; file
timestamps 2026-09-15 21:55:50, 31 min before 718fba5 moved the pair), so every
seed in the landed items (1)-(3) is a shift applied to a 466/745 mirror, never
a value read out of a 471/748 capture, and what the dump validates directly is
only the old-seed series in item (3). The verifier also re-derived item (1)'s
solution sets from the two dump files alone in row indices and reproduced
H {471,472} / V {748} ramp vs {465,466} / {744,745,746} k=0..2, disjoint --
confirming this worklog's step-3 tables, and confirming there is no model
conflict: T31's row indices are the landed one-capture-later model with k
relabelled by 5 (canon-rust gap 14 in both). The verifier's audit of this
worker's two self-reported errors (the first solve's dump-seed assumption; the
(a != b).sum() channel-vs-pixel inflation) found both correctly diagnosed and
disclosed in the step-"Tried and dropped" section above.
