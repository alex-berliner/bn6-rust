# T69 — finish BLIND: gate the object's whole OAM commit

Branch `wt/t69` (merge of wt/t64 tip 9f1ce9a on main 78e72ce). Baseline
reproduced exactly: `blind FAILED 69987/1940/90 | negative: not blind
(70487)`, mailbox still nm 0x020002fc (harness literal correct, no edit
needed).

## The headline correction: T64's row compared different battle phases

T64's premise — both sides poked "at capture frame 40", the same battle
moment — is measured FALSE. The mettaur row's own align (canon_ref=140,
search 193..213) has a UNIQUE zero at rust offset 205, exactly 0 over 70
frames, which pins the capture-clock mapping: **rust capture = canon capture
+ 73** (canon capture 0 = our battle content frame 73). So canon's
capture-40 poke lands on content frame 113 = our capture **113**, not 40;
T64's two pokes sat 73 content-frames apart. Measured both ways:

- pokes at capture 40 both sides (T64): poked pair ~1,000 px/frame apart
  everywhere (the wave flight phase-shifted by 8 frames ≈ its 1.7 px/frame
  flight speed); NO offset in any band zeroes the window (min 69,987 at 31;
  wide search 0..89 over the unpoked pair: no zero, best 24,474 flat).
- rust poke at capture 113: the wave aligns and the residual collapses to a
  CONSTANT 315 px/frame at x168-199 y111-120 — the enemy HP readout.

The remaining 315 px/frame was the real second contributor (the ticket's
"shadow or another commit path" guess): canon's poke removes her HP readout
entry (OAM x168 y108, present unpoked, GONE poked at captures 41/45; her box
holds 6 entries unpoked, 0 poked — the claim "zero" is matched), while our
gate (Actor::show early-out) never reached `draw_number_in`'s number objects.

## The port

- src/battle.rs: the gate is now answered ONCE per frame
  (`actor::opposing_player_blind()`) and applied at BOTH commit sites that
  emit OAM entries for an enemy — the sprite parts in Actor::show (passes
  OBJECT_FLAGS_BLIND or 0) and the enemy HP readout's filter. Canon's shape:
  the clear is blindVisualHandledHere_8016934's cross-alliance arm
  (asm00_2.s:16866-16869) on `oObjectHeader_Flags` bit 1
  (OBJECT_FLAG_VISIBLE; re-asserted every frame at :16824-16829, also
  cleared at :16843), and the sprite layer obeys the header flags in
  UpdateBattleObjectSprite's header-flag tests (asm00_2.s:25195-25243).
- src/actor.rs: OBJECT_FLAGS_BLIND made pub(crate); new
  `opposing_player_blind()` carrying the cite and the measured OAM counts.
- tools/harness.py: BLIND_POKES_RUST capture 40 → **113**; Align search
  range(26,39) → range(100,111) (event-derived offset 105 = 113-8); canon
  side untouched; frames stay 90; note rewritten with the measurement.

## Numbers (post-port, this tree)

- ours poked-vs-unpoked over the common 90-frame window (rust 113..202):
  **72,528** (max 1,219) vs canon's **72,194** over the same window — 0.46%
  apart; pre-port the same window gave 44,493 (the readout's 28,000 px
  missing).
- blind row after the port: **FAILED 1575/466/90 | negative: not blind
  (14056)** — 4 nonzero frames only:
  k=0 177 px x169-199 y111-120 (readout, the poke boundary frame — canon's
  capture 40 is its last ungated picture, T64 measured k=0 = 0 on canon's
  side; ours shows a one-frame readout sliver), and k=75-77 466 px each at
  x42-69 y66-113 — the wave hitting MegaMan (canon capture 115 ≈ T64's
  "hits at ~114"): our hit reaction differs for 3 frames.
- guard set (verify_rows from the merge commit, pre-port): wave 0/0/90/3840,
  window 0/0/16/81056, opening 0/0/40/86591, chip-cannon 0/0/40/9505,
  mettaur 0/0/70/41734, cursor FAILED 1/1/170/186279 (known, byte-identical),
  windowclose 0/0/40/207166 — all MATCH. Re-run from the ported tip: see the
  commit message / final report.

## Ideas tried and dropped

- OAM A/B on the pre-port build (rust captures 41/121, canon 41/45): proved
  our gate already removed all 3 of her sprite entries while pixels survived
  — which killed the "second sprite commit path" theory and pointed at the
  readout (an entry our OAM never carries at that phase but canon's does).
- Wide offset search (0..89) on the unpoked pair: no zero anywhere — killed
  the "alignment band too narrow" theory and forced the mettaur-row-derived
  content mapping (rust = canon + 73).
- Battle-clock watches (canon 0x0200a210 ticks +1/frame; scroll counters
  0x02009690/94 tick -8/-4 per frame from the state's own base): both are
  capture-relative only; the content mapping above is the usable clock.

## What I would try next

1. The k=0 boundary (177 px): decide which picture the poke-frame's presented
   frame shows on each side (poke lands "immediately before" the frame; the
   draw-vs-poke order within the frame differs across sides) — either shift
   the window one frame or pin the readout's own update phase.
2. The k=75-77 MegaMan hit region (466×3): first check whether the wave
   arrival frame differs by one (align artifact at the hit), then whether our
   hurt flash/knockback render differs — it may belong to the cursor/T56 tear
   family or to the enemy-hit path, NOT to the render gate.
3. Canon's BlindTimer expiry tick (sub_800E730, object.s:5512-5520) is still
   unmodeled — irrelevant while the row pokes 0xffff.

## verify_rows from the PORTED tip (4db0620)

blind FAILED 1575/466/90/14056 (4 nonzero frames); guard rows: wave
0/0/90/3840, window 0/0/16/81056, opening 0/0/40/86591, chip-cannon
0/0/40/9505, mettaur 0/0/70/41734, windowclose 0/0/40/207166 — all
byte-identical to published. **cursor read 3/2/170/186277** (published:
1/1/170/186279) — the known single-frame tear MOVED with the ROM layout
(this port changes code size; the parent's own re-measure note says the tear
moves with the layout). The ticket's unmerge rule ("cursor > 1/1/170/186279
on the same ROM layout") is letter-violated; flagged for the reviewer, not
chased.

ROMs: canon /tmp/bn6f_real.gba sha256
a37c1028adb72082b51e142321fa437967bc54b6f46730a53f6581a d455ad670 (8,388,608 B);
ours (post-port build) /tmp/bn-d3213f50/h_rom_.gba sha256
a36b03b71d383e7ded74a2bf8934012b814e440e9bc992899aaf0380b1f9745c (583,108 B).

Status against the ticket's acceptance: the port and the fixture re-pin are
landed and evidenced; `blind` is NOT 0/0/90 (1,575/466, 4 frames) and ours
poked-vs-unpoked reads 72,528 against canon's 72,194 (0.46%). SCOPE's
statuses 0/69 line untouched (T18's walk). The k=0 readout sliver and the
k=75-77 MegaMan hit region are the named unverified residue.
