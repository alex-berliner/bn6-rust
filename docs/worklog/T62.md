# T62 — M4/M1 bookkeeping: SCOPE M4 prose line + family-0x13 comment + T57 cite slips

Date: 2026-09-16. Branch `wt/t62` (worktree /tmp/bnwt/t62, base 1cac961).
Docs/comment-only ticket: zero src/ changes by construction.

## Step 1 — baseline (no edit)

- `python3 tools/inventory.py` regenerated SCOPE + docs/inventory/chips.json.
  Counts recomputed from the rule: **verified 11 / verified-pixels 32 / unrecorded 368**
  (411 rows) — same split verifier-hyper measured on wt/t57. Generated M1 chips line
  `| 43 / 411 |` unchanged.
- ROM build: `cargo build --release` then
  `python3 tools/gbafix.py /tmp/ct_t62/thumbv4t-none-eabi/release/bn /tmp/t62_before.gba`
  → sha256 `5b46337aa27da9ca881f2321fe1e210f54717285b4dfa5256c1680bac4b985ef`
  (byte-identical to the sha verifier-hyper quoted for HEAD).
- docs/SCOPE.md:21 verbatim (before):

> 14 chips pixel-verified AS DATA -- the sword/blade family-0x13 rows (Sword..BambSwrd,
> Muramasa, T17), the Barrier family-0x15 subfamily-0x04 rows (Barrier, Barr100, Barr200,
> T48), AND AirShot (id 4, family-0x21, T52) dispatch through the record's
> AttackFamily/AttackSubFamily and the ROM's own tables -- plus 29 pixel-verified through
> our own code; StepSwrd (81) rides the same record dispatch without a pixel row of its own
> (tools/inventory.py AS_DATA_FAMILIES={0x13,0x15,0x21}) |

So :21 said 14/{0x13,0x15,0x21} while the generated table above it said 11/{0x13,0x21} —
exactly the two-answers disagreement the ticket names.

## Step 2 — SCOPE.md:21 rewrite

New status cell read from step 1's generated output: **11** as-data rows (the ten
family-0x13 rows + AirShot), **32** verified-pixels (29 prior + the three Barrier
rows T57 demoted), `AS_DATA_FAMILIES={0x13,0x21}`. The demotion's reason is stated
in one clause with the src cite that shows it: the same family 0x15 also holds
id-keyed AreaGrab/Invisibl (src/battle.rs:3434/:3430, verified on disk — both arms
of the `match chip.id` at :3429), so family 0x15 alone selects nothing. Milestone
text (cols 1–3) and every other prose line byte-identical.

## Step 3 — family-0x13 comment (tools/inventory.py :212-217, comment only)

The T57 comment claimed "the one byte this family still keys by id is the use_chip
ENTRY arm src/battle.rs:4144". Verifier was right to refute: src/battle.rs:3288
`let (asset, palette) = match self.chip_in_use.map(|c| c.id)` with four arms
CHIP_FIRESWRD/AQUASWRD/ELECSWRD/BAMBSWRD (:3288-3294) is a second id key. Comment
now names BOTH keys and keeps the family in on this argument:

- :4144 entry arm — lists all 11 sword-family ids (incl. StepSwrd/Muramasa), shared,
  selects no behaviour;
- :3288-3294 sprite/palette match — picks only the per-id sprite asset + palette,
  cosmetic-per-id, not behaviour;
- the behaviour (hit shape) is read from the record at :4391 (`chip.family ==
  SWORD_FAMILY`, SWORD_HIT_SHAPE by record subfamily — verified on disk).

So the step-3 argument holds and 0x13 stays; no T61 blocker from this clause. The
`AS_DATA_FAMILIES = {0x13, 0x21}` literal (line 228+) untouched.

## Step 4 — three cite slips, corrected in place with dated notes (2026-09-16)

1. docs/worklog/T57.md:34-36 — barrel palette `(chip.id - CHIP_CANNON)` was cited
   :4266; on disk :4266 is `let mut barrel = spr::Player::new(...)` and the byte is
   **:4269**. Cites fixed in all three Cannon rows, note on row 1.
2. docs/worklog/T57.md:53 — `ener` "comment at :4596-4604"; on disk the
   `ener: chip.id == CHIP_ENERGBOM ...` line is **:4578** (:4596-4604 is the
   surrounding vx/vz match).
3. docs/worklog/T57.md:98 — status counter "inventory.py:1165"; on disk the
   `ver = sum(...)` counter is **tools/inventory.py:1184**.

## Step 5 — rebuild + diff check

- `cargo build --release` + gbafix again → sha256 identical to step 1:
  `5b46337aa27da9ca881f2321fe1e210f54717285b4dfa5256c1680bac4b985ef` (no src change,
  expected but quoted per T56's rule).
- `git diff --name-only` = docs/SCOPE.md, tools/inventory.py, docs/worklog/T57.md
  (+ docs/worklog/T62.md this log). Nothing else.
- `python3 tools/inventory.py` re-run after the edits: generated part of SCOPE.md
  unchanged (`git diff --stat docs/SCOPE.md` shows the :21 line only).

## What is unverified

- Nothing behavioural: the ticket changes no number and no code. The claim that the
  :3288-3294 sprite/palette key is "cosmetic-per-id" rests on reading the match's
  arms (asset + palette only) — no pixel run was taken to prove the sprite choice
  has no behavioural side effect; T61's owner should keep that in mind when it
  re-adds 0x15.
- T61's veto (cursor 20/19 on wt/t61b) is untouched by this ticket; main's
  generated counts (11/32/368) were current at work time and the prose was written
  from this run, not from T57's or T61b's numbers.
