# T47 — M2 battle_full trace tool fix (oracle/diffmask/trace)

## Step 1 — checkout

`bash tools/worktree.sh t47-trace-fix` → worktree `/tmp/bnwt/t47-trace-fix`,
branch `wt/t47-trace-fix`, HEAD `c2a4bc2`. T7r PARTIAL is already merged at
7358b0d; the "T7r branch tip" is HEAD. `export CARGO_TARGET_DIR=/tmp/ct_t47-trace-fix`.

## Step 2 — baseline (HEAD, no edits): four tool gaps reproduced verbatim

```
$ python3 tools/oracle.py battle_full
unknown row 'battle_full': harness.py --list names the rows (rollup is a no-crash walk, not a comparison)

$ python3 tools/oracle.py battle_full --both
usage: oracle.py [-h] [--shift SHIFT] row
oracle.py: error: unrecognized arguments: --both

$ python3 tools/diffmask.py battle_full --frames 170..200 /tmp/x.png
usage: diffmask.py [-h] [--align ALIGN] [--solid SOLID] [--diff DIFF]
                   real rust out
diffmask.py: error: unrecognized arguments: --frames

$ python3 tools/trace.py record canon battle_full --out /tmp/x
Traceback (most recent call last):
  File "/tmp/bnwt/t47-trace-fix/tools/trace.py", line 56, in <module>
    ORCL_ADDR = int(O.MM_ORACLE_ADDR, 0)
                ^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: int() can't convert non-string with explicit base
```

Cites: tools/oracle.py:332 (H.CHECKS filter), tools/oracle.py:495
(`ap.add_argument("row")` + `--shift`), tools/diffmask.py:54-62 (argparse,
no --frames), tools/trace.py:56 (`int(O.MM_ORACLE_ADDR, 0)` where
`O.MM_ORACLE_ADDR` is already an int = 0x02000008).

verify_rows baseline (HEAD, the veto-on-regression gate):
```
verify_rows: HEAD (c2a4bc2) in /tmp/bnwt/verify-c2a4bc2
  wave           PASS     0/0/90/3840              ran
  window         PASS     0/0/16/81056             ran
  opening        PASS     0/0/40/86591             ran
  chip-cannon    PASS     0/0/40/9505              ran
  mettaur        PASS     0/0/70/41734             ran
verify_rows: PASS
```

## Step 3 — edit + measure + commit per fix

(to be filled in as each lands)

## Step 4 — final run + acceptance

(to be filled in)