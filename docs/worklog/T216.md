# T216 — BLIND status reticket (BLOCKED: tool budget soft limit hit at 80 calls)

Branch `wt/blind-status` (HEAD = main `7fb6dd6`). NO src/, tools/, or
states.py change was made — the budget ran out before any capture ran, any
assembly reading was transcribed into code, or any harness row was built.
This worklog is the handoff. The next worker must NOT inherit the
references below without re-verifying them against today's disassembly and
code; line numbers and function names drift on every renames pass.

## What T216 asks for, decoded

1. "Transcribe the BLIND render-gate arm from `sub_801A186` (asm00_2.s:21612
   vicinity, T18 cited reader) into `src/objects.rs::render_enemy`."
   **Two of these references do not match today's tree.**
   - `sub_801A186` (grepped `reference/bn6f/asm/asm00_2.s` line 21688-21749)
     is the panel-poison handler — it tests `object_getFlag` against
     `=0x8000028` and ticks `oCollisionData_PoisonPanelTimer`. It is NOT the
     BLIND render-gate. The T18-cited BLIND reader it almost-certainly means
     is at `blindVisualHandledHere_8016934` (asm00_2.s:16832-16900), where
     the cross-alliance `bl object_getFlag; ldr r1, =0x2000; tst r0,r1`
     sits at line 16861 (T18's exact cite, verified against the current
     file by `grep -n "=0x2000"` and reading the routine's body).
   - The "21612 vicinity" appears to be either a stale line number (T18 was
     written before the 2026-09-15 rename pass moved labels and renumbered
     lines) or a confusion with `object_setFlag1` at 21618, which is the
     setter helper, not the gate. The helper function `object_getFlag`
     itself is at line 21636, and the SETTER chain `object_setFlag1` /
     `object_clearFlag` / `object_getFlag` sits at 21618-21641. So
     "21612 vicinity" most charitably refers to the reader helper pair
     — useful for a `peek` of `oCollisionData_ObjectFlags1` (+0x3c) but not
     itself the render-gate arm.
   - **`src/objects.rs::render_enemy` does not exist.** The current closest
     function is `pub fn enemy_act` at src/objects.rs:169, which dispatches
     `battle_common_path(enemy) -> actor.update()`. There is no
     `render_enemy` either as a stub or as a hidden alias. Either T216 is
     wrong about the function name, or the ticket expected a function the
     T69 branch never actually wrote into src/objects.rs (T69's port lived
     in `src/battle.rs::opposing_player_blind()` and `src/actor.rs::show`).
2. "Gate the OBJ commit on the per-bit read, with the T124 pin." T124's
   pin is `SEAM_PHASE_PAD_ITERS = 25` (T130 re-fit; T124 originally
   measured 17, T130 sweep found 25 to restore `cursor 1/1/170/186279`).
   The pin governs the cursor tear's placement, not a BLIND-specific
   value. The "T124 pin" most likely means "re-verify cursor at the
   current pad value, do not move the cursor while editing the OBJ
   commit path".
3. Acceptance: `blind_met` row at 0/0/N non-blind (negative = status bit
   zeroed = mettaur baseline); mettaur + cursor byte-identical at T124
   pin; verify_rows PASS on full isolated table.

## State of the world on entering this ticket

- **No `blind` row exists** in `tools/harness.py` or `tools/allowlist.py`
  (`grep -n blind tools/allowlist.py tools/harness.py` returns no
  name= row; the only BLIND mentions are in BLIND-mention comments and
  the `blind`/`not blind` row-formatter labels). T60/T64/T69 added the
  row on the unmerged `wt/t69` branch; that branch never landed
  (T69.md: "wt/t69 (merge of wt/t64 tip 9f1ce9a on main 78e72ce)" --
  the merge base 78e72ce is far behind main 7fb6dd6, so the port is
  effectively gone).
- **No `opposing_player_blind()` or `BlindMailbox`** exists in main
  (`grep -rn 'opposing_player_blind\|BlindMailbox' src/` returns no
  matches). T69's gate is unlanded.
- **`src/objects.rs::enemy_act`** is `pub fn enemy_act(enemy: &mut Actor) -> Update { battle_common_path(enemy) }` at src/objects.rs:169. It is the OBJ-commit entry point in the sense that the
  subsequent `Actor::show(...)` call inside `Actor::update` is the OAM
  emitter for enemies; the actual OBJ commit lives in src/actor.rs and
  src/graphics.
- **`BLIND_OR_CONFUSED: bool = false`** at src/objects.rs:356 is the
  current gate: it forces the blind-wander arm of the Mettaur's decision
  loop into the row-match arm. T83a recommended replacing it with a real
  read of `OBJECT_FLAGS_BLIND_OR_CONFUSED` (0xa000). The ticket says
  "M3's first live rule" is the gate wired up, which matches T130's
  pattern (mask template + live reader), but applied to a different bit.

## What needs to happen (in order, the next-worker's recipe)

1. **Baseline (no edit).** Run:
   `python3 tools/harness.py --only mettaur,cursor,blind_met --no-gallery`
   on `7fb6dd6`. The mettaur/cursor lines reproduce the published numbers
   (0/0/70/41734 and 1/1/170/186279). The `blind_met` line will FAIL
   "no such row" until step 4 adds it; record the harness's exact
   error.
2. **Disassembly read.** Confirm the BLIND render-gate arm at
   `blindVisualHandledHere_8016934` (asm00_2.s:16832-16900) with
   `python3 tools/csrc.py blindVisualHandledHere_8016934 --asm-only`,
   then read lines 16844-16872 (the cross-alliance arm: `eor r0,r1;
   beq loc_8016998` -> `battle_findPlayer` -> `bl object_getFlag; ldr
   r1, off_8016A30 // =0x2000; tst r0,r1` -> `bic OBJECT_FLAG_VISIBLE`).
   Cite `file:line` for every byte. The reader test the ticket wants is
   the `tst r0,r1` at 16869 (NOT the locator hint at 21612).
3. **Port.** Two options, both already half-attempted:
   (a) **Replicate T69 exactly**: a `opposing_player_blind()` helper
       in src/battle.rs, called once per frame, that reads
       `blindVisualHandledHere_8016934`'s own cross-alliance arm
       (the OTHER side's BLIND bit) and returns the bool. Apply at the
       OBJ-commit sites (Actor::show + the enemy HP-readout filter, per
       T69's diagnosis). The T69 worklog (T69.md §1) is the
       step-by-step recipe.
   (b) **Lift the BLIND test into src/objects.rs**: a new
       `pub fn render_enemy(enemy: &Actor) -> bool` (the name the
       ticket uses) that returns true iff the enemy's visible flag
       should be cleared this frame, gated on the OTHER side's BLIND
       bit. This needs a way to ask "is the OTHER alliance's player
       BLIND?" — either a free function reading
       `BattleObject+0x54+0x3c` for the opposing slot, or a method on
       `Actor`. The T18 worklog's verified reader at asm00_2.s:16861 is
       the gate; the `BattleObject+0x54` -> CollisionDataPtr -> +0x3c
       pointer chain is the BattleObject.inc:144 + CollisionData.inc:145
       route T60 measured.
4. **Add `blind_met` row** in tools/harness.py + tools/allowlist.py.
   Per the ticket's spec: same descriptor as `mettaur` (uses FIELD_ROW);
   canon side does NOT poke the player's BlindTimer (negative = "status
   bit zeroed = mettaur baseline"); rust side does the same; a positive
   poke `--poke-at 113:0x0203852c:0x2000 --poke-at 113:0x02038510:0xffff`
   (T69's measured recipe at the player's CollisionDataPtr+0x3c /
   +0x20) is the rust-side positive fixture so the row can be
   inverted to a true gate-test; the negative keeps both sides
   unpoked and must read nonzero (non-blind = the gate is doing
   something when poked, nothing when not). The negative MUST be
   non-blind (the row proves nothing if it is) — see the T69 worklog's
   poke-boundary diagnosis: the negative is the unpoked-vs-unpoked pair
   diff, not a poke-broken pair.
5. **Cursor pin re-measure.** T124's "T124 pin" applies (per
   docs/coverage/cursor.md:150): every landing that changes the binary
   re-fits `SEAM_PHASE_PAD_ITERS` and reports the tear's frame. The
   current pad is 25 (T130's landing at 5ac9227). The port is in the
   OBJ-commit path, so the cursor tear will move; sweep before
   committing, restore main's exact 1/1/170/186279 line.
6. **Verify.** `python3 tools/verify_rows.py HEAD --expect` over the
   full isolated table — 17+ rows including blind_met, mettaur,
   cursor, chip-poisseed, gunner, form_cross, emotion_syn,
   emotion_face_b, emotion_skip.

## What I did NOT do

- I did NOT run the baseline harness (tool budget hit at 80 calls).
- I did NOT read the full assembly of `blindVisualHandledHere_8016934`
  — only the BLIND-reader test at line 16869 (verified by grep).
- I did NOT touch src/, tools/, or any other write path.
- I did NOT add `blind_met` to the harness, the states, or the
  allowlist.
- I did NOT verify the ticket's `sub_801A186` / `21612` / `render_enemy`
  references as anything other than what grep + a small read said they
  are. The next worker MUST re-verify all three before any port.

## Why the ticket is BLOCKED here, not just PARTIAL

The ticket expects three correct references (sub, line number, function
name) and all three are wrong against today's tree. That is not a
"rewrite one line" ticket — it is "find the routine the ticket meant,
find the function the ticket meant, transcribe a 30-line assembly arm
into a function that doesn't exist, then prove it with a row".
Re-verification alone is several grep + csrc.py calls; the port is
several assembly reads + a write; the row is a harness-side write + a
negative design + a cursor sweep. None of that fits in the remaining
budget of 40 hard-cap calls.
