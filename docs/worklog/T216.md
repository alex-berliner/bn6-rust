# T216 — BLIND status reticket (M3)

Ticket: transcribe the BLIND render-gate arm into `src/objects.rs::render_enemy`,
gate the OBJ commit on the per-bit read (T18's reader, `object_getFlag`), with the
T124 pad pin. Row `blind_met`, negative = status bit zeroed = mettaur baseline.

## Address drift note (read first)

The ticket cites "sub_801A186 (asm00_2.s:21612 vicinity, T18 cited reader)". On
today's asm00_2.s, `sub_801A186` (21772-21833, corrected by the verifier pass) is the POISON PANEL updater, not a
render routine; lines 21612..21665 are the `object_setFlag1`/`object_clearFlag`/
`object_getFlag` block (T132's 2026-09-18 comment insert shifted the ticket-time
line numbers; now corrected to 21739-21744 by the verifier pass). `object_getFlag` is T18's
cited reader: it returns `CollisionDataPtr->ObjectFlags1` (+0x3c). The BLIND
render-gate ARM itself is `blindVisualHandledHere_8016934`'s cross-alliance arm
asm00_2.s:16893-16961, cross-alliance arm 16918-16942 -- corrected by the verifier pass), as measured by T60/T64/T69. The transcription therefore
follows the measured arm and uses object_getFlag's per-bit read as the gate's
feeder; both cites given in code.

## Step 0 — baseline (no edit)

`python3 tools/harness.py --only mettaur,cursor --no-gallery` (HEAD 874a533,
tree clean, pad 25):

```
mettaur    isolated    PASS    total 0  worst 0  frames 70  | negative: not blind (total 41734)
cursor     isolated    FAILED  total 1  worst 1  frames 170 | negative: not blind (total 186279)
fitted constants: 17 (derived 449, peeked 146)
```

Capture path alive (mettaur 0/0/70). cursor 1/1/170/186279 = the known seam tear
(T124's class line).

## Plan

1. src/objects.rs: `OBJECT_FLAGS_BLIND` (0x2000, CollisionData.inc:16), the
   `BLIND_POKE_MAILBOX` (T64's delivery design; T69's black_box note — fat LTO
   folds a non-black_boxed volatile read of a .bss static the program never
   writes), `opposing_player_flags()`, and `render_enemy()` — the OBJ commit
   gate. Canon shape: the gate arm clears OBJECT_FLAG_VISIBLE on a cross-side
   object when `battle_findPlayer(alliance^1)` carries the bit; render-only —
   the blind Mettaur's wave still flies and hits (T64 step 2).
2. src/battle.rs (commit sites only, per T69's measured survivor list): the
   enemy HP readout loop and the enemy sprite loop — the gate must cover BOTH
   (T69: the readout alone was 315 px/frame the Actor::show gate never reached).
3. tools/harness.py: one new row `blind_met` + a `pokeoff` negative kind whose
   canon side is the SAME recipe with the status bit zeroed (0x0000) — i.e. the
   mettaur baseline — compared against the same poked rust capture; must FAIL
   (non-blind).
4. T124 pin: re-fit `SEAM_PHASE_PAD_ITERS` per docs/coverage/cursor.md's rule
   (every landing that changes the binary re-fits the pad; a branch whose re-fit
   cannot reach <=1/1 stays unmerged).
5. verify_rows PASS on the full isolated table.

## Step 1 — code change


## Step 2 — the row, first measure

blind_met with a band copied from the canon_ref-FIELD_ORIGIN guess (126..139)
read 107608/2288/70 — MISALIGNED: I had forgotten T69's measured +73 pairing
(rust capture = canon capture + 73; offset 205 for canon_ref 140, the mettaur
row's own band). Re-pointed the band at the mettaur row's own (193..214); the
row then read:

```
blind_met  isolated    PASS    total 0  worst 0  frames 70 | negative: not blind (total 60901)
```

(pokeoff negative = canon recipe with ObjectFlags1 low half poked 0x0000 --
the mettaur baseline -- vs the poked rust capture: 60901, comfortably
non-blind. First attempt at the same negative read 168060 under the
misaligned band; the corrected pairing's 60901 is the blind effect's size
over the window, i.e. the negative fails by the subject itself.)

## Step 3 — the T124 pin

The code change re-rolled the layout; cursor read exactly T69's post-port
signature 3/2/170/186277 (mettaur stayed 0/0/70/41734). T111 sweep on THIS
layout:

| pad | 17 | 21 | 23 | 25 | 27 | 29 | 33 |
|-----|----|----|----|----|----|----|----|
| cursor total/worst | 23/22 | **1/1** | 35/34 | 3/2 | **1/1** | 3/2 | 41/40 |

Pads 21 and 27 both restore main's exact line 1/1/170/186279 (negative
186279). Pinned 25 -> 21 (first in-class value); neg 186279 at the pin.
Mailbox re-nm'd after the pin: 0x020002f4, unchanged.

## Step 4 — full isolated table (this tree, pad 21)

Full `--only` pass over every isolated row: 67 PASS lines. EVERY row that is
zero on the landed record stayed zero: mettaur 0/0/70/41734, blind_met
0/0/70/60901, cannon 9505, all 43 chip rows 0, wave 0/0/90/3840, window
0/0/16/81056, windowclose 0/0/40/207166, result 0/0/40/111839, opening iso
0/0/40/86591, chip-use iso 0/0/30/7768, tiles/gauge iso+int 0, card, banner
0/0/58/7275, popup 0/0/80/1288, emotion_syn/face_b/skip 0/0/40/644,
form_cross, warp iso, buster iso. 13 FAILED lines, none a documented zero:
cursor 1/1/170/186279 (main's exact tear line), buster_charge 2498/188/32
(exactly T124's landed main line), field-bg1/2/3 and gunner/navi-gunner/
navi-gunner-ai (rows red on main's record; field-bg is pad-coupled per
T141's table and this tree sits at the re-fitted pad 21), and the
integrated/allowed variants (opening 18740, field 32357, warp 40628,
buster 54672, chip-use 275307 -- all under their AUDIT-6/F41 allowlist
entries or quoted known-red). NOT re-measured on today's main for budget:
field-bg1/2/3 and gunner/navi-gunner/navi-gunner-ai's current main lines;
the comparison rests on the landed record (T124/T141 quotes) plus the
every-documented-zero-stays-zero fact above.

verify_rows wt/t216 blind_met,mettaur,cursor from a clean checkout:
PASS, all three MATCH (blind_met=0/0/70/60901, mettaur=0/0/70/41734,
cursor=1/1/170/186279).

ROM at the pin: sha256 b76da93df917304f2c69d389f8eaf4658c877c3c948b1d17846a6bebcc8b959c,
597908 B. fitted 17 (derived 450, peeked 146) -- no new fitted constant; the
pad is the standing fitted one.

## Mechanism (one line)

BLIND's render gate is a per-bit read: object_getFlag returns the opposing
player's CollisionData ObjectFlags1 (+0x3c) and blindVisualHandledHere_8016934's
cross-alliance arm (asm00_2.s:16918-16942, corrected by the verifier pass) clears OBJECT_FLAG_VISIBLE when the
bit (0x2000) is set -- render-only, so we gate both enemy OBJ commit sites
(sprite + HP readout) on `blind_hides_enemy(opposing_player_flags())`.

## Unverified / next

- The gate's feeder is a POKE mailbox standing in for the opposing player's
  CollisionData; nothing in this project sets BLIND live yet (no chip), so the
  mailbox is the only route -- same delivery design T64/T69 measured.
- buster_charge's line matches T124's main quote exactly, but field-bg1/2/3
  (3360/3360/380175 vs T141's 3600/3600/380219 at a different base+pad) were
  not re-baselined against today's main; a coordinator verify_rows --with-main
  on field-bg closes that if wanted.
- The enemy-hit-path engine divergence T69 measured (k=75-77) sits outside
  this row's window (canon 140..209, the mettaur row's own proven-identical
  window) and remains M2/M3 engine-core work.

## Errata (2026-09-18, verifier cite-correction pass)

- Cite correction: this checkout's reference/bn6f has
  `blindVisualHandledHere_8016934` at asm00_2.s:16893-16961 (cross-alliance
  arm 16918-16942) and `object_getFlag` at asm00_2.s:21739-21744;
  `sub_801A186` is 21772-21833. Every 16846-16870 / 21655-21661 cite in this
  worklog, docs/coverage/emotion.md, src/objects.rs, src/battle.rs and the
  harness.py blind_met note is stale by ~47/+84 lines and was corrected in
  place (the pre-correction reads predate this checkout of the reference).
- Correction: `SEAM_PHASE_PAD_ITERS` lives at src/main.rs:323 (not
  tools/states.py -- this ticket's diff never touched tools/states.py).
- Correction: the pokeoff negative does NOT use T87's EVENT_681 lever
  (tools/states.py:268-341 only) -- it is a pure harness-side canon variant
  with the ObjectFlags1 poke value zeroed.
