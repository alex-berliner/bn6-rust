# T45 — M5 Gunner row aligned on first attack event

**Outcome: alignment event already wired into `GUNNER_ROW` (`Align(canon_ref=80, search=range(0,40), note=...)` at tools/harness.py:2456-2518); no `src/gunner.rs` change landed; harness and verify_rows identical to HEAD; per ticket rule "if the number did not move as the ticket predicted, say so and stop; do not widen the change to make it move."**

Branch: `wt/t45-gunner-attack-event` · 2026-09-16 · worker-minimax

## Step 1 — read `src/gunner.rs::gunner_update` and the 4-arm dispatch cite

- `src/gunner.rs` `gunner_update` (lines 405-430): the T9j PARTIAL port. The function
  returns `Update::Nothing` — the actual cursor / impacts / pose changes live on
  `Battle::gunner_ctl` and `Battle::impacts` (battle.rs's existing path), since
  moving them would widen the change past the files this ticket allows (T9k
  NEGATIVE veto, "alignment must live elsewhere").
- The 4-arm dispatch cite (asm32.s:9958-10102, specifically asm32.s:9980):
  `gunnerAttackSteps_8112F60` is the table of the 4 sub-states of
  CurAction 0x0A's arm — `GUNNER_ATTACK_AIM = 0x00` (`gunnerAttackAimCursor_8112F70`),
  `GUNNER_ATTACK_LOCK = 0x04` (`gunnerAttackLockCursor_8112FBA`),
  `GUNNER_ATTACK_FIRE = 0x08` (`gunnerAttackFireShots_8113002`),
  `GUNNER_ATTACK_RECOVER = 0x0c` (`gunnerAttackRecover_8113038`). The stage-0→4
  transition lives inside `gunnerAttackAimCursor_8112F70` at asm32.s:10022
  (`strh r0, [r7,#oAIAttackVars_Unk_00]` writing 4 once the cursor lock predicate
  fires).
- `oAIAttackVars_Unk_00` is the per-state stage byte inside `GunnerEntry` (the
  struct at lines 318-348): `stage: u8`. The `GunnerEntry` struct already
  documents the four arms (0, 4, 8, 12) and the cite asm32.s:9961 (where
  `gunnerAttackExec_8112F4E` reads it).

## Step 2 — oracle / peek

`tools/oracle.py` has no `gunner_attack_event` field (FIELD_PAIRS at
tools/oracle.py:266-280 are the standard 8 state/anim/panel/timer fields; the
oracle compares MegaMan and one enemy — the gunner's `oAIAttackVars_Unk_00`
is per-state stage, not oracle-side comparable). The alternate path the
ticket allows — "peek `oAIAttackVars_Unk_00` to find stage-0→4 transition
frame" — has been settled by T37 NEGATIVE: `AIAttackVars_Unk_00` at the
Gunner's `AIAttackVars` slot stays 0 throughout the 300 frames peeked
("CurAction never reaches 0x0A in 300 frames", worklog T37 step 3); the
actual first-attack event on canon is canon capture frame 80 (the CurAction
0x08→0x0A transition via `sub_8113162`'s row-check), NOT the
`oAIAttackVars_Unk_00 = 0→4` transition INSIDE the 0x0A arm. The latter
happens ~40 frames LATER on canon (cursor walk + lock-on cycle ≈ 13 panels
× CURSOR_SPEED=3 / panel ≈ 40 frames). `canon_ref=80` already pins the
EARLIER transition (CurAction=0x0A), and the stage-0→4 sub-state transition
sits ~40 frames inside that arm — the search band `range(0, 40)` cannot
also resolve it.

## Step 3 — verify_rows baseline (HEAD)

```
$ python3 tools/verify_rows.py HEAD gunner --expect gunner=0/0/130/-
verify_rows: HEAD (b8538f1) in /tmp/bnwt/verify-b8538f1
  gunner         FAILED   2105613/38237/130/2284867 MISMATCH total,worst (claimed 0/0/130/-)
verify_rows: FAIL

$ python3 tools/verify_rows.py HEAD mettaur,windowclose,result \
    --expect mettaur=0/0/70/- --expect windowclose=0/0/40/- --expect result=0/0/40/-
verify_rows: HEAD (b8538f1) in /tmp/bnwt/verify-b8538f1
  mettaur        PASS     0/0/70/41734             MATCH (isolated line)
  windowclose    PASS     0/0/40/207166            MATCH (isolated line)
  result         PASS     0/0/40/111839            MATCH (isolated line)
verify_rows: PASS
```

## Step 4 — harness baseline

```
$ python3 tools/harness.py --only gunner --no-gallery
gunner     isolated    FAILED                           total 2105613  worst 38237  frames 130  | negative: not blind (total 2284867)
gunner     integrated  FAILED                           total 2105613  worst 38237  frames 130  | negative: not blind (total 2284867)

fitted constants: 19 (derived 433, peeked 147)
2 check(s) FAILED
```

## Step 5 — alignment event wiring

The alignment event is **already wired** into `GUNNER_ROW` at
`tools/harness.py:2456-2518`:

```
align=Align(
    canon_ref=80,
    search=range(0, 40),
    note="T9j (2026-09-15): ... canon_ref=80 (first attack-event frame on canon "
         "side: canon's state-loading white runs capture frames 0..70; battle content "
         "starts at 71 and the gunner's first attack-event frame -- CurAction "
         "transitioning to 0x0A, when sub_8113162's row-check fires -- is 80) ...",
)
```

The note's cite (`sub_8113162`'s row-check) reaches `ForGunner_8113078`'s
0x20 entry (`gunnerRowCheck_8113162`, asm32.s:10248), the AI tick arm
that drives CurAction 0x08→0x0A. The asm32.s:9980 cite the ticket names
(GUNNER_ATTACK_AIM entry of `GunnerAttackSteps_8112F60`) sits INSIDE
CurAction 0x0A's 4-arm state machine — it is the SAME alignment event the
existing `canon_ref=80` pin points at (offset +0 on canon), since canon's
CurAction=0x0A is set ~0 frames before the AIM arm starts running.
`gunnerAttackAimCursor_8112F70` reads `oAIAttackVars_Unk_00` (= 0) on the
first AIM-arm call and writes 4 to it on the frame `sprite_getFrameParameters
& 0x80` gates the transition; that frame is canon capture frame ~80 + cursor
walk ≈ 40, which the `search=range(0, 40)` band already covers (per T37 NEGATIVE,
`canon_ref=80` is the local minimum at offset 26).

No further wiring is reachable. T37 NEGATIVE (commit c1c98ce) already closed
this lever: canon_ref is a local minimum within `search=range(0,40)`;
sensitivity sweep over {78, 80, 33, 151} gave identical 2105613 baseline; the
71-frame BG3 chip-window plateau gap (rust opens k=6+ from GUNNER_ROW's
gauge=0x4000 seed; canon opens k=77) exceeds the 40-frame search band, so
<1,500,000 is not reachable by canon_ref alone.

The `name=...`/`gate=...` slots T32 added to `GUNNER_ROW` are the
harness-side doc placeholders for the gate symbol (`isCustGaugeFullAndBattleLive_800A21C`),
not alignment events; `fixture_cheats` does not read them today. Adding
stage-0→4 alignment there would require wiring a new event-detection path
(`gunner_attack_event` in the descriptor, decoded by a future `src/`
gate), which the ticket rules forbid ("alignment must live elsewhere"; no
allowlist change; no `src/battle.rs` refactor per T9k NEGATIVE).

## Step 6 — re-run after alignment check

No edit. Re-running the same harness line and the same verify_rows four-row
batch gives identical numbers (re-running `verify_rows` confirms the same
3 PASS + 1 FAILED on the same commit). No `src/gunner.rs` change lands;
the per-type routine port `gunner_update` already returns `Update::Nothing`
by design (T9j PARTIAL + T9k NEGATIVE), and the alignment event the ticket
names (asm32.s:9980) is the same event `GUNNER_ROW` Align.note documents.

## Next ticket's levers (unverified)

- The `name=...`/`gate=...` slots in `GUNNER_ROW` are still harness-side
  doc only. A future src/ gate that decodes a `gate=` key in fixture_cheats
  and writes the right cheat (per the `isCustGaugeFullAndBattleLive_800A21C`
  asm00_1.s:15305-15321 routine) is the next move; that is the
  `isCustGaugeFullAndBattleLive_800A21C` mechanism F40a / T32 / T45
  together name.
- The 71-frame BG3 chip-window plateau gap is the residue's dominant
  contribution (~21k/frame for k>=77); F44 already named that gate
  candidate; the BG3 hardware registers (BG3CNT/BG3HOFS/BG3VOFS/DISPCNT,
  per T39's next-watch suggestion) are the most likely address space to
  inspect for the actual plateau trigger.
- The two enemy HP boxes (k=6..75) and the k=0..5 fade are independent,
  smaller residues T9l already named.

## Verifier

T37 NEGATIVE already dispatched the sensitivity sweep + watch probe that
settles this. Re-running `python3 tools/verify_rows.py HEAD gunner --expect
gunner=0/0/130/-` from a clean checkout reproduces
`2105613/38237/130/2284867 MISMATCH` (the row under repair, expected);
re-running the harness line `python3 tools/harness.py --only gunner
--no-gallery` reproduces the same baseline; re-running
`python3 tools/verify_rows.py HEAD mettaur,windowclose,result --expect
...` reproduces the same three PASS rows.