# T115 — panel types 0x0/0x5/0x8/0x9/0xA: writer hunt (field walk + data stream)

Ticket: measurement only. Zero src/ edits, ROM sha must not move. Product: writer cites
in tools/inventory.py, docs/coverage/panels.md, regenerated inventory.

## Baseline (before any edit)

- `python3 tools/inventory.py` → line 7:
  `panels (M3): FOUND: word_3007924 (IWRAM copy, asm/asm38.s:4242-4249) = IWRAMRoutinesROMLocation+0x1E24 = 0x081D7E24 in ROM (bn6f.map:34342; copied by start.s:57-63 to 0x3005B00 len 0x1ed4): 13 words, stride 4, one per panel type 0x0..0xC, OR-ed into oPanelData_Flags by _object_updatePanelParameters (asm/asm38.s:4213-4219) -- verified 8/13`
- M1 computed at tools/inventory.py:1979 (panels row counts `verified` statuses via parse_panels :645-653: writer startswith "GAP" → bounded-GAP).
- ROM sha256 before: a37c1028adb72082b51e142321fa437967bc54b6f46730a53f6581ad455ad670
- GAP rows: 0x0 hole, 0x5 holy, 0x8, 0x9, 0xA. T33's earlier walk: 4 `bl _object_setPanelType`
  sites (asm38.s:3581/3969/3980/3993, all r2=#2) + 14 inline `strb oPanelData_Type` sites
  (object.s:2220..2519, all 1/3) + object_panel_setPoison (type 4).
- Capture budget: ≤3 total (this is canon-side observation; no comparison against ours).

## Method (T18's, applied to the panel field)

Walk the FIELD not the helper:
1. every store to oPanelData_Type (direct strb/str incl. literal-offset forms) across ALL asm files,
2. inline orr/bic/add forms touching the panel-type bits of oPanelData_Flags
   (crackPanel's `Flags=(Flags&~0x3f0f)+3` shows the type lives in flag bits too),
3. every `bl _object_setPanelType` call site + its constant argument,
4. every arm of sub_80BAE16 (asm31.s:6104-6170) and t3_0x0_80C4E58 (asm31.s:27855-27872),
5. data stream: T65's 1240 records / 1076 formation arrays + T70's stage pairs byte_203CA50.

## Code-walk findings so far (step 1, in progress)

KEY STRUCTURAL FACT T33 missed: `object_setPanelType` (asm/object.s:2601-2611) is a
TRAMPOLINE (`ldr r4, =_object_setPanelType+1; bx r4`) to `_object_setPanelType`
(asm38.s:4309-4315). Total bl sites = **36**: 4 `bl _object_setPanelType` (asm38.s,
T33 walked those only) + 32 `bl object_setPanelType` (asm31.s:28, asm32.s:2, asm00_2.s:2).
The setter SKIPS the store when current Type==0 (`tst r3,r3; beq` asm38.s:4316-4317),
so a hole panel is never overwritten by the helper.

Also found: `object_setPanelTypeBlink` (object.s:2651-2663) — companion that records the
intended type into oPanelData_Unk_08 (+Unk_0d=1 blink flag) instead of writing Type; the
final arm of the same gimmick later writes Type for real.

Per-site table (file:line → routine → type source):
- asm31.s:6105 sub_80BAE16 `mov r2,#7`; :6169 `mov r2,#6` (known T33-era cites)
- asm31.s:27855-27856 / :27871-27872 t3_0x0_80C4E58 arms → 0xC / 0xB (known)
- asm31.s:27879-27896 t3_0x0_80C4E58 DEFAULT arm (UNWALKED by T33): Param1 0x22/0x24 gates
  above; default (plain buster/charge) writes RelatedObject1Ptr->CurState as the type
  (`ldrb r2,[r3,#oBattleObject_CurState]` :27893, skip 0xff :27895). Disassembly comment
  (already present) records a real-ROM measurement: Type stays NORMAL 140 frames → this
  arm writes CurState 2 in practice, no new type.
- asm31.s:81486 sub_80DE768 (in t3_0xc9_80DE404): type = dword_80DE79C[Param1]
  (:81488 = 0x060703FF → bytes FF,03,07,06: [0]=0xFF no-op, [1]=3, [2]=7, [3]=6),
  skip when 0xFF (:81485).
- asm31.s:47261 sub_80CE424 (in t3_0x4f_80CE24C): AREA setter — walks a
  PanelOffsetListsPointerTable list (0x7F-terminated pairs, inventory's second panel
  table) calling object_setPanelType(x+dx,y+dy, r5) per pair. Caller sub_80CE3C4 :47206-47218:
  type = byte_80CE41E[Param1] (:47212, :47216-47218; byte_80CE41E = 04 07 06 at :47201) →
  Param1 0→4 poison, 1→7, 2→6 grass.
- asm31.s:99515-99529 sub_80E6CAA (in t4_0x56_80E6BDC): WHOLE-FIELD seeder. base =
  byte_80E6D0C + Param1*12 + alliance*120 (:99486-99500); per column `ldr word` then per
  row `lsr (row-1)*4; and #0xf` → type = PACKED 4-BIT NIBBLE. Table byte_80E6D0C
  (:99543-99566, 240 bytes = 2 alliances x 10 Param1 entries x 12 bytes) contains ONLY
  nibbles {2,3,6,7,8} (+00 padding bytes). → **writer for type 0x8** (Param1 2/3/4 own
  side; 8 also in alliance-1 rows), never 5/9/A.
- asm31.s:88662-88960 t4_0x16_80E1E4C family: dispatch off_80E1E60[Param2] → 4 arms;
  blink phase sets Unk_08=Param1 via object_setPanelTypeBlink, final phase
  object_setPanelType(x,y,Param1) over whole field (:88755-88759 etc.) or enemy half
  (:88780-88781 → constant 3) — Param1 from stage data, arbitrary in principle.
- asm31.s:31652 applyShockwavePanelEffect_80C6CFC: 0xFF→skip, 3→crackPanel, 1→breakPanel,
  else setPanelType(r2) (shockwave Param2).
- asm31.s:30844 (#6 cornfiesta), :38398 (#2), :39065 (#6), :60577 (#4), :65592 (#7),
  :65798 (#6) — constant-type sites, all known types.
- asm31.s:90014/90040 t4_0x1f_80E28A8 — TODO walk.
- asm00_2.s:8306 sub_8012792 #2 (known), asm00_2.s:11156 sub_8013CC4 r4 — TODO walk.
- asm32.s:3241 sub_810F3F8 #2 — TODO walk.

Data find: type 0x8 IS reachable: byte_80E6D0C nibbles. Types 5/9/A absent from ALL
four ROM type tables so far (80E6D0C, 80CE41E, 80DE79C, and code constants).

## Final state (2026-09-18)

- Step 1 CLOSED: 36 bl sites (trampoline discovery), 13 direct strb sites, all
  data-fed writers walked. Per-type table in docs/coverage/panels.md and in the
  regenerated PANEL_TYPE_ROWS.
- Step 2 CLOSED: records byte[4] known-answer check passes ({7:192,8:1,255:268} =
  T65's backdrop census); no record/array byte reaches a panel writer; T70's
  byte_203CA50 is background-only (asm03_0.s:14639 battleSettings_802D2B2,
  asm00_1.s:18213 pool).
- Step 3: 0 captures — the only new writer (0x8) has an unreferenced spawner
  (sub_80E6C8C, byte-searched 0x080E6C8C/D in the whole ROM), so no scenario
  reaches it; the other four types have no firing writer per the finding.
- Step 4: tools/inventory.py re-cited, run exits 0, panels line now
  `-- verified 9/13` (was 8/13; 0x8 converted). Regenerated docs carry a
  reference line-number refresh (e.g. navis asm31.s:123447->123451) from the
  live reference — generator output, not a semantic change.
- ROM sha256 before = after = a37c1028adb72082b51e142321fa437967bc54b6f46730a53f6581ad455ad670.
  Zero src/ edits, no rebuild, fitted count untouched, 0 captures (budget 3),
  tool calls ~74 (soft cap 70, hard 90 — finished inside the hard cap).

## What I would try next

1. Find the real spawn path of the t4 gimmick wrappers (t4_0x56 sub_80E6C8C,
   t4_0x16, t4_0x1f): they are unreferenced in the labeled disassembly, so the
   spawn is computed or data-driven from an unlabeled table — the morning pass
   could look at SpawnBattleObjectCommon callers and the type-4 ctx consumers.
2. Type 5 (holy): the reader rule is live but no producer exists in this ROM's
   code or data — check whether BN6F's holy panel is link/BN6-Gregar trade
   content or simply unused; a probe of the chip-effect dispatch tables
   (byte_80EBD34 region) might settle it.
3. Name type 8 (ice candidate): the byte_80E6D0C patterns + Elec melt
   (asm38.s:3960-3983) fit ice; a scenario that reaches t4_0x56 would confirm
   visually and give the missing flip-frame evidence for the row.
