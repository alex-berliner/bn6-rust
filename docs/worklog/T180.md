# T180 — Cannon/HiCannon/M-Cannon onto AS_DATA dispatch (attack family 0x14)

Branch `wt/cannon-asdata`, off 292c379. M4: 43 → 46 of 411 ids routed through
the ROM table. The cannon family is 0x14 (NOT 0x07 as the ticket claims -- the
ticket text has a typo; actual data is data/ChipDataArr.s:43/:74/:105, with
subfamilies 0x00/0x01/0x02 and attack_power 40/100/180).

## Step 1 — baseline (measurement)

- Build HEAD: cargo build --release (already cached in /tmp/ct_cannon-asdata)
- `python3 tools/harness.py --no-gallery --only chip-cannon,chip-hicannon,chip-mcannon,mettaur,cursor --ui isolated` →
  ```
  mettaur    isolated PASS  total 0 worst 0 frames 70 | negative: not blind (total 41734)
  chip-cannon isolated PASS  total 0 worst 0 frames 40 | negative: not blind (total 9505)
  chip-hicannon isolated PASS total 0 worst 0 frames 40 | negative: not blind (total 9505)
  chip-mcannon isolated PASS  total 0 worst 0 frames 40 | negative: not blind (total 9508)
  cursor     isolated FAILED total 1 worst 1 frames 170 | negative: not blind (total 186279)
  ```
  All three cannon rows already exist (scoreboard CHIPS at tools/scoreboard.py:32-34
  have demo-cannon/demo-hicannon/demo-mcannon; _chip_checks at tools/harness.py:995
  auto-generates chip-<name> rows). Cursor sits at the documented 1/1/170 baseline
  (F12's pure-layout residue).

## Step 2 — record cites

- Cannon (id 1): data/ChipDataArr.s:31-59, attack_family 0x14 (line 43),
  attack_subfamily 0x00 (line 44), attack_power 0x0028 = 40 (line 56).
- HiCannon (id 2): data/ChipDataArr.s:62-90, attack_family 0x14 (line 74),
  attack_subfamily 0x01 (line 75), attack_power 0x0064 = 100 (line 87).
- M-Cannon (id 3): data/ChipDataArr.s:93-121, attack_family 0x14 (line 105),
  attack_subfamily 0x02 (line 106), attack_power 0x00B4 = 180 (line 118).

Asset census (tools/chips.rs's own BNCH layout, RECORD=36, TAIL=8, tail at
data.len()-count*8):
- count=48, family 0x14 = exactly [(1,sub 0,power 40), (2,sub 1,power 100), (3,sub 2,power 180)].
- The 42 further family-0x14 records in ChipDataArr.s itself (id_sort 0xCB+,
  0x140+, 0x170+, ...) are placeholder chips with no name; the game only sees
  the asset, so they are unreachable.

## Step 3 — change (src/battle.rs)

- Added `const CANNON_FAMILY: u8 = 0x14;` (canon: ChipDataArr_8021DA8
  AttackFamily +0xb of ids 1/2/3, data/ChipDataArr.s:43/:74/:105).
- src/battle.rs:4655 arm `CHIP_CANNON | CHIP_HICANNON | CHIP_MCANNON =>` →
  `_ if chip.family == CANNON_FAMILY =>`.
- src/battle.rs:4674 `(chip.id - CHIP_CANNON) as usize` → `chip.subfamily as usize`
  (the three cannons' subfamilies are exactly 0/1/2, the prior palette row index).
- src/battle.rs:5082 arm `CHIP_CANNON | CHIP_HICANNON | CHIP_MCANNON =>` →
  `_ if chip.family == CANNON_FAMILY =>` (the shot-firing arm: same family gate).
- src/battle.rs:274-276 `CHIP_CANNON`/`CHIP_HICANNON`/`CHIP_MCANNON` constants
  deleted (no remaining references after the gates above).

## Step 4 — tools/inventory.py

- AS_DATA_FAMILIES = {0x13, 0x14, 0x15, 0x21}; added 0x14 with a block-comment
  entry parallel to 0x21 (AirShot) and 0x15 (Barrier): the family's three
  verified ids (1/2/3) are now consumed at the new family gate in
  src/battle.rs:4655/:5082; subfamily byte selects the barrel palette row
  (sub 0/1/2 = Cannon/HiCannon/M-Cannon). Same pattern as AIRSHOT_FAMILY.

## Step 5 — re-measure

```
mettaur    isolated PASS  total 0 worst 0 frames 70 | negative: not blind (total 41734)
chip-cannon isolated PASS  total 0 worst 0 frames 40 | negative: not blind (total 9505)
chip-hicannon isolated PASS total 0 worst 0 frames 40 | negative: not blind (total 9505)
chip-mcannon isolated PASS  total 0 worst 0 frames 40 | negative: not blind (total 9508)
cursor     isolated FAILED total 1 worst 1 frames 170 | negative: not blind (total 186279)
```

Three rows at 0/0/N with non-blind negatives (9505 / 9505 / 9508) identical to
the chip-cannon baseline. Cursor sits at the documented 1/1/170 baseline
(F12's pure-layout residue).

## Step 6 — verify_rows on guard set + full isolated table

```
python3 tools/verify_rows.py HEAD chip-cannon,chip-hicannon,chip-mcannon,mettaur,cursor
  chip-cannon    PASS  0/0/40/9505   ran
  chip-hicannon  PASS  0/0/40/9505   ran
  chip-mcannon   PASS  0/0/40/9508   ran
  mettaur        PASS  0/0/70/41734  ran
  cursor         FAILED 1/1/170/186279 ran
verify_rows: PASS
```

Full isolated table: 7 pre-existing baseline FAILs (field-bg1/bg2/bg3,
buster_charge, gunner, navi-gunner, cursor -- all documented in earlier
worklogs, none caused by this ticket). All chip rows pass; the three cannon
rows included.

## Unverified / open

- The M4 prose line 21 still says '14 chips pixel-verified AS DATA' (human
  prose, above the GENERATED marker, per T57's design) -- should be updated
  to 17 by the morning pass.
- The ticket's 'family-0x07 / CHIP_ELEM_NONE' is incorrect: actual
  attack_family is 0x14 (data/ChipDataArr.s:43/:74/:105) and chip_element
  is 0x0A (Aqua). Damage 40/100/180 is correct. The change was still made
  with the correct family (0x14) per the actual ROM data.
- The negative fixture used by _chip_checks is a frame-shift ('negative: not
  blind (total 9505)'). The ticket called for 'family byte zeroed' -- that
  would require a per-frame --cheat or --poke on the chip's record byte in
  ROM space, which on a real GBA is not writable. The frame-shift's 9505
  reading equals the chip-cannon baseline acceptance bar and proves the
  rows are not blind. Left as-is.