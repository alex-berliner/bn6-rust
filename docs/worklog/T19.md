# T19 — loud chip-asset contract + SCOPE counts the eleven data chips

Worktree: wt/t19-loud-chip-contract (from main @ 50ad0c2).

## Step 1 — baseline (before any edit)

`python3 tools/harness.py --only chip-sword,chip-wideswrd,chip-muramasa --no-gallery`
(release build, `/tmp/t19_base.gba`, 583324 bytes):

```
chip-sword      PASS total 0 worst 0 frames 40 | negative: not blind (total 6397)
chip-wideswrd   PASS total 0 worst 0 frames 40 | negative: not blind (total 7977)
chip-muramasa   PASS total 0 worst 0 frames 40 | negative: not blind (total 8824)
fitted constants: 19 (derived 431, peeked 147)
```

docs/SCOPE.md before, verbatim (generated section):
- M4 line: `| M4 | Chips as data: chip object routines ported in coverage order, the custom-screen chip UI, then every standard chip, mega, giga, secret; Program Advances | per chip: scenario, port, trace, pixels | 43 chips pixel-verified through our own code; none "as data" yet |`
- M1 chips: `| M1 | chips (M4) | FOUND: data/ChipDataArr.s:2 ChipDataArr_8021DA8 (411 x chip_data_struct, stride 0x2c, include/rom_structs/ChipData.inc) | 43 / 411 |`

## Step 2 — the version guard in src/chips.rs::new

Added `const VERSION: u32 = 2; // canon: tools/chip_export.py's header field 0x04`
and an `assert_eq!(version, VERSION, "chips.bin v1 read by v2 code: tail would
be misread as behaviour")` right after the MAGIC check, before `count`/`tail`
are derived. The stale NOTE comment ("the version u32 at offset 4 is NOT
checked") replaced with the reason the check makes the from-file-end tail
search safe.

**Proof (before any capture).** Scratch host crate `/tmp/t19_scratch`
(OUTSIDE the repo): `#[path]`-includes the REAL `src/chips.rs` against a stub
`agb` (chips.rs touches only `TileSet`/`TileFormat`), and runs `Chips::new`:

- v2 asset (assets/chips.bin, 48780 bytes, version u32 = 2): `new` OK,
  48 records, record 0 id=1 family=0x14.
- v1 = the ACTUAL pre-T17 file (`git show 8c1a62d^:assets/chips.bin`,
  48396 bytes, version u32 natively 1 -- no byte forcing needed):
  `Chips::new` PANICS:
  `assertion 'left == right' failed: chips.bin v1 read by v2 code: tail would be misread as behaviour (left: 1, right: 2)` at src/chips.rs:67.
  The byte that triggers it: offset 4 = 0x01 (the v1 version u32).
- Pre-guard behaviour reproduced on the same v1 bytes: tail = 48012,
  guard `48012 >= 12 + 48*36 = 1740` passes, record 0 tail bytes read
  `33 35 85 87 53 53 73 58`.

**Correction to the ticket's quote:** the ticket says record 0 would read
`family = 0x85`; measured, the FAMILY byte (+0xb -> tail t[1]) reads **0x35**
and the SUBFAMILY (t[2]) reads **0x85**. The misdispatch claim stands either
way (truth is family 0x14 / sub 0x00), but 0x85 is the subfamily byte.

No row moved for this step (no capture spent; `new` runs at ROM boot on every
row, and the v2 asset still passes the guard -- verified by the v2 line above
and re-verified by the full table in step 5).

## Step 3 — the arc + shape match arms in src/battle.rs

Read EffectObjectRows_80E0398 straight out of bn6f.gba at 0x080E0398, stride
4 (sub_80E0568: Param1<<2; fields [0]=sprite, [1]=animData, [2]=CurAnim,
[3]=palette base):

| row | sprite | animData | anim | palette | existing arm? |
|---|---|---|---|---|---|
| 0x16 | 0x0c | 0x14 | 0 | 0 | yes |
| 0x17 | 0x0c | 0x14 | 1 | 0 | yes |
| 0x18 | 0x0c | 0x14 | 2 | 0 | yes |
| 0x19 | 0x0c | 0x14 | 0 | 5 | yes |
| 0x1a | 0x0c | 0x14 | 1 | 5 | yes |
| 0x1b | 0x0c | 0x15 | 0 | 0 | NEW |
| 0x1c | 0x0c | 0x15 | 0 | 2 | NEW |
| 0x25 | 0x0c | 0x14 | 3 | 0 | NEW |
| 0x28 | 0x14 | 0x05 | 0xb | 1 | NEW |
| 0x2d | 0x0c | 0x14 | 1 | 6 | yes |
| 0x5f | 0x10 | 0x41 | 0 | 0 | NEW |

Choices, and why:
- Arc `_ => (0,0)`: replaced by explicit arms for ALL 11 distinct rows
  SwordArcBySubfamily_80EBAD8 defines (values verified against the ROM bytes
  above) + a named panic arm for anything else. I chose explicit arms + panic
  rather than a debug-only panic because a release-mode panic is still loud
  (the ROM's agb panic handler prints the message), and a debug-only panic
  would reintroduce the silent door in every release capture.
- Shape `_ =>` (the default arm Sword's own shape 1 rides): added an explicit
  `SHAPE_PANEL_AHEAD: u8 = 1` arm (behaviour-identical, shape 1 still pushes
  the single panel ahead) + named panic default for the shapes the table
  carries but no asset record can (0x11/6/0xB -- other subfamilies'). A
  widened asset delivering one of those must fail loudly, not quietly slash
  one panel.

**The six absent family-0x13 ids.** `/tmp/t19_arc_check.py` parses
SWORD_ARC_ROW + the match arms OUT OF src/battle.rs itself (not a copy) and
evaluates the six ids. They CANNOT reach chip_strike through the asset today
(assets/chips.bin holds 48 records, ids 286/339/370/374/376/410 are not among
them -- no reachability is faked here); the check runs their ROM subfamilies
through the shipped mapping arithmetic:

```
BEFORE: 286 sub 0x06 -> row 0x1c -> (0,0) via _ => default
        339 sub 0x05 -> row 0x1b -> (0,0) via _ => default
        370 sub 0x09 -> row 0x25 -> (0,0) via _ => default
        374 sub 0x0a -> row 0x5f -> (0,0) via _ => default
        376 sub 0x0b -> row 0x1b -> (0,0) via _ => default
        410 sub 0x01 -> row 0x16 -> (0,0)   (explicit arm already)
AFTER:  286 -> 0x1c -> (0, 2); 339 -> 0x1b -> (0, 0);
        370 -> 0x25 -> (3, 0);    374 -> 0x5f -> (0, 0);
        376 -> 0x1b -> (0, 0);    410 -> 0x16 -> (0, 0)
        (all explicit; no _ => (0,0) remains)
```

Note 374 (row 0x5f) and 339/376 (row 0x1b) resolve to (0,0) HONESTLY -- that
is what their EffectObjectRows entries say (0x10/0x41/anim 0/pal 0 and
0x0c/0x15/anim 0/pal 0), not a default.

## Step 4 — SCOPE through the generator

Edited structure in tools/inventory.py: new module constant `AS_DATA_FAMILIES
= {0x13}` (canon: ChipDataArr_8021DA8 attack_family, the family src/battle.rs's
SWORD_FAMILY dispatches on) + parse_chips' status selection: verified ∩
family-0x13 -> "verified" (trace + pixel parity as data), else the old
verified-pixels/unrecorded. Derived from the ROM table — no per-chip list
invented. Regenerated with `python3 tools/inventory.py` (BN6F_REF resolved to
the main checkout's disassembly).

**FINDING — the ticket's "eleven" is off by one.** The generator derives TEN
promotions (ids 71-79, 85), not eleven: the eleventh sword chip, StepSwrd
(id 81), is family 0x13 and IS in the asset (record 32, sub 0x01), but was
NEVER in the 43-id pixel scoreboard (no StepSwrd row in scoreboard.py at any
commit), so it is "unrecorded" and cannot move FROM verified-pixels without
faking a pixel claim. Not widened to force the count. VarSwrd/NeoVari
(82/83) are sword-NAMED but not family 0x13 in the ROM table — untouched.

docs/SCOPE.md:
- M4 milestone line (hand prose ABOVE the generated marker — the generator's
  own rule keeps the head human) BEFORE: `43 chips pixel-verified through our
  own code; none "as data" yet` / AFTER: `10 chips pixel-verified AS DATA --
  the sword/blade family-0x13 rows (Sword..BambSwrd, Muramasa) dispatch
  through the record's AttackFamily and the ROM's own tables, T17 -- plus 33
  pixel-verified through our own code; StepSwrd (81) rides the same record
  dispatch without a pixel row of its own (tools/inventory.py
  AS_DATA_FAMILIES)`. Counts 10/33 read from the generator's
  docs/inventory/chips.json output.
- M1 chips line: `43 / 411` BEFORE AND AFTER (status_summary counts every
  status starting with "verified", so the promotion does not change it; the
  per-row change is what moved).
- Generated diff: exactly the ten chip rows' status cell, nothing else below
  the marker.

## Step 5 — the table

Harness re-run (step 1's rows, post-change build, /tmp/t19_after.gba 583852
bytes — code grew 528 bytes from the new arms; event alignment absorbed it):

```
chip-sword      PASS 0/0/40 | negative 6397   (baseline 6397)
chip-wideswrd   PASS 0/0/40 | negative 7977   (baseline 7977)
chip-muramasa   PASS 0/0/40 | negative 8824   (baseline 8824)
fitted constants: 19 (derived 431, peeked 147)
```

tools/verify_rows.py wt/t19-loud-chip-contract (c1a3288), all twelve expects:

```
chip-sword     0/0/40/6397   MATCH      chip-fireswrd 0/0/40/8499 MATCH
chip-wideswrd  0/0/40/7977   MATCH      chip-aquaswrd 0/0/40/8396 MATCH
chip-longswrd  0/0/40/8824   MATCH      chip-elecswrd 0/0/40/7367 MATCH
chip-wideblde  0/0/40/7977   MATCH      chip-bambswrd 0/0/40/7913 MATCH
chip-longblde  0/0/40/8824   MATCH      chip-muramasa 0/0/40/8824 MATCH
chip-use       0/0/30/7768   MATCH      cursor  1/1/170/186279 MATCH (known tear)
                                      mettaur 0/0/70/41734  MATCH
verify_rows: PASS
```

## What is unverified / for the next worker

- The panic's REACHABILITY on real hardware (that a v1 file actually boots
  into the guard) is proven only on the host via the /tmp scratch crate that
  include!s the real src/chips.rs — no harness row can see a v1 asset since
  assets/chips.bin is frozen at version 2. The coordinator asked for a
  verifier on exactly this claim.
- The five new arc arms (0x1b/0x1c/0x25/0x28/0x5f) are ROM-byte-verified
  against EffectObjectRows_80E0398 but are UNREACHABLE through the 48-record
  asset — no pixel row can exercise them; they cannot move any row by
  construction, which the twelve MATCHes confirm from the other side.
- StepSwrd (81): if a later ticket wants the true "eleven as data", the
  first step is a pixel row for StepSwrd (chip-stepswrd), not a generator
  exception.
