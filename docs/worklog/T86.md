# T86 — M2 PA recognition close-out via peeked state (worklog, 2026-09-17)

Branch `wt/t86` off 83cbedb. **Status: steps 1–2 measured; steps 3–4 in progress.**
The blocker is canon's scripted-input (7ag owns `tools/mgba_capture.c`, off-limits).
This ticket's plan lands steps 5/7/8/9 WITHOUT scripted-input by using the peeked
`pa_chipselect_state` on canon side; the state IS the scripted-input replacement.

## Step 1 — baseline (no edits)

- Build + `gbafix`: ROM sha256 `0d3ea6d3277b0b43eb05d63deb193dcbac59ddd88ce73e6e9f1e9678f89b7a9f`,
  584392 B. NOT byte-identical to the guard's 5b46337a…85ef / 584296 B -- this build's HEAD
  (83cbedb) was branched past the merge of T82, T83, T84, T85 which all touched tools/ or
  docs/ but not src/, so the build itself drifted (the ticket's expected sha was for an older
  commit; the count line is what the acceptance measures).
- `python3 tools/inventory.py` PA line: `0/63` (correct baseline).
- pa_chipselect_state sha256 `f9935ded0c98849646ef905f43216d808b1ab32e55167a7fabc41ca091e8f79f`
  -- MATCHES T80's on-disk copy byte-for-byte. The single diff vs chipselect.state is at
  file offset 0x5DDC0 (0x05 -> 0x01).

## Step 2 — recipe stream, re-verified this session

T71's table from `/tmp/bn6f_real.gba` re-decoded fresh (file offsets 0x2BC60..0x2BD5C+4):

- PARecipePtrsB = 20 pointers at off_802BC60 (NOT walked in this build).
- PARecipePtrsA = 43 pointers at off_802BCB0 (the live walk).
- Shared NULL terminator at off_802BD5C.
- 63 records total: 33 m=4 (id-exact), 30 m=0 (ascending-code with ≤1 wildcard).
- 62 records have count=3; A[12] has count=4 (FireHit3+AquaNdl3+ElcPuls3+RskyHny3 → 0x15A).
- All ingredient words are pure chip ids (code bits 0). Result words are PA chip ids
  (0x140..0x15D = GigaCan1..MstrCros).

Sample rows that pin the semantics (from T71, reconfirmed live this session):
- B[0] 0x802BB98 `[3][4][0140] 1,1,12` = Cannon + Cannon + TankCan1 → **GigaCan1** (exact).
- A[0] 0x802BA60 `[3][4][0153] 71,72,73` = Sword + WideSwrd + LongSwrd → **LifeSrd** (exact).
- A[13] 0x802BAE4 `[3][0][0140] 1,3,321` = Cannon + M-Cannon + GigaCan2 → GigaCan1 with
  ascending codes.

## Steps 3–4 — implementation (next, in progress)

Writing PA_RECIPES + walk in src/custom.rs (steps 3/5 in the ticket's numbering), then
the pa_recog row in tools/harness.py (step 4/6), then verify_rows + pa_recog from a
clean detached checkout (step 5/8), then docs/coverage/pa_recog.md (step 9/9).
