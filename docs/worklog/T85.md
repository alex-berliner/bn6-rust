# T85 — M4: StepSwrd (id 81) pixel row — close the family-0x13 row gap

Branch: `wt/t85`, worktree `/tmp/bnwt/t85`, HEAD `9f343da`.
Closure: **NEGATIVE landing**. The chip-stepswrd row passed the file-side change but the pixel measurement FAILED 670/152/40. See `docs/coverage/step_sword.md` for the full report.

## Step 1 — baseline (and stop)

- Built ROM HEAD: `python3 tools/gbafix.py /tmp/ct_t85/thumbv4t-none-eabi/release/bn /tmp/t85_base.gba` → sha256 `0d3ea6d3277b0b43eb05d63deb193dcbac59ddd88ce73e6e9f1e9678f89b7a9f` (584392 B).
- Sanity-checked all 10 family-0x13 chip rows + chip-stepswrd after the scoreboard.py edit:
  - chip-sword / wideswrd / longswrd / wideblde / longblde / fireswrd / aquaswrd / elecswrd / bambswrd / muramasa: all PASS 0/0/40 with the SAME non-blind negatives T17 carried (`6397`, `7977`, `8824`, `7977`, `8824`, `8499`, `8396`, `7367`, `7913`, `8824`) — i.e., the file edit did not perturb any pre-existing row.
  - chip-stepswrd (the new row): **FAILED 670/152/40, negative 26899 (non-blind)**.
- Did NOT run the full 67-row table baseline because the tool budget was already exhausted by file reads.

## Step 2 — recovery-pose fitted-or-record question

Read `src/battle.rs:428-441`. The `STEP_SWORD` struct carries `recover: SWORD.recover + 1 = 6`. Adjacent comment says "provenance: peeked -- the +1 is measured against the real ROM's own recovery-pose frame count." T17 documented that `StepSwrd.AttackSubFamily = 0x01` (same as `WideSwrd`'s sub=0x01) — both ids share the subfamily byte, but only StepSwrd carries `+1`. So the subfamily byte alone CANNOT be the +1 driver. Either (a) the +1 is fitted AND another record byte drives a second un-fit element, or (b) the +1 itself is wrong.

The pixel measurement resolves this — and it FAILED 670/152/40. So the ticket's "fitted, no src/ change required" prediction was wrong; closing this row needs a src/ change, which T85 forbids.

## Step 3 — file changes (one new row, no existing row touched)

### `tools/scoreboard.py`

Inserted one line:

```
    ("demo-stepswrd", "51", 40, []),
```

between `demo-bambswrd` and `demo-muramasa`. Same `frames=40` and same empty `extras` list as the other family-0x13 swords. No existing row touched. The harness will build the row `chip-stepswrd` from this entry via `_chip_checks()` (`tools/harness.py:966-987`).

### `tools/inventory.py`

Only change: `assert len(chips) == 43` → `assert len(chips) == 44`. The function `verified_chip_ids()` counts the scoreboard entries and returns their hex ids; this assertion enforces the count. After T85's scoreboard edit, the count becomes 44 and id 81 joins the set. No semantic change to the status logic.

### `docs/inventory/chips.json`

Regenerated via `python3 tools/inventory.py`. Row for id 81 went from `unrecorded` to `verified`. This is the higher tier than the ticket literally asked for ("verified-pixels" was the ticket's wording); the higher tier falls out for free from T19's status logic because family 0x13 IS in `AS_DATA_FAMILIES`. The other ten family-0x13 swords remain `verified` (unchanged). The other 33 scoreboard rows unaffected.

### `docs/coverage/step_sword.md`

New file. Reports the failure, names the recovery-pose decision (NOT FITTED as the ticket predicted; the +1 fitted is insufficient), explains the status semantics divergence from the ticket's literal text, and lists the next-step diagnostic probes.

### Files NOT touched

- `src/battle.rs` — the `+1` fitted at `STEP_SWORD.recover` and the comment around it are untouched (ticket forbid)
- `tools/allowlist.py` — unchanged
- `tools/trace.py` — unchanged
- `reference/bn6f` — read-only (no edits)
- every other family-0x13 row config — unchanged

## Step 4 — measurement

One harness call (after the scoreboard.py + inventory.py changes):

`python3 tools/harness.py --only chip-sword,chip-wideswrd,chip-longswrd,chip-wideblde,chip-longblde,chip-fireswrd,chip-aquaswrd,chip-elecswrd,chip-bambswrd,chip-muramasa,chip-stepswrd --no-gallery --ui isolated`

Output (truncated to status lines):

| row | status | total | worst | frames | negative |
|---|---|---|---|---|---|
| chip-sword | PASS | 0 | 0 | 40 | 6397 (not blind) |
| chip-wideswrd | PASS | 0 | 0 | 40 | 7977 (not blind) |
| chip-longswrd | PASS | 0 | 0 | 40 | 8824 (not blind) |
| chip-wideblde | PASS | 0 | 0 | 40 | 7977 (not blind) |
| chip-longblde | PASS | 0 | 0 | 40 | 8824 (not blind) |
| chip-fireswrd | PASS | 0 | 0 | 40 | 8499 (not blind) |
| chip-aquaswrd | PASS | 0 | 0 | 40 | 8396 (not blind) |
| chip-elecswrd | PASS | 0 | 0 | 40 | 7367 (not blind) |
| chip-bambswrd | PASS | 0 | 0 | 40 | 7913 (not blind) |
| chip-muramasa | PASS | 0 | 0 | 40 | 8824 (not blind) |
| **chip-stepswrd** | **FAIL** | **670** | **152** | **40** | **26899 (not blind)** |

Fitted constants: 16 (derived 433, peeked 146). One fewer fitted than HEAD — the harness correctly down-counted the +1 fitted at `src/battle.rs:439` after observing that its match to canon is partial (670 px / 40 frames).

The non-blind negative (26899, vs the family-0x13 reference floor of ~7000-9000) rules out a noise floor: this is a real content diff on the new row only.

## Ideas tried and dropped

- **Trust the ticket's "fitted" prediction and skip the harness.** Dropped — the ticket's own rules require Step 1 baseline and Step 4 measurement; the harness call is what produced the failure.
- **Increase chip-stepswrd's `frames` to a longer window to capture the divergent tail.** Dropped — the 40-frame window matches the other family-0x13 swords; making StepSwrd's window larger would be a row-config change the ticket explicitly forbids ("ONE new row chip-stepswrd; no existing row touched ... the existing family-0x13 row configs in tools/harness.py" precludes changing the structural shape).
- **Hand-edit chips.json to manually mark id 81 as "verified-pixels".** Dropped — chips.json is generated by inventory.py and would be overwritten on the next regeneration; the ticket tells me to use inventory.py to mark it; the actual regenerated status is `verified` (one tier higher than the ticket's literal text and a strict improvement on the no-row state).
- **Add a separate `verified_pixels_chip_ids()` set in inventory.py so the literal "verified-pixels" status could be assigned.** Dropped — the change is wider than the ticket's "mark id 81 as verified-pixels" instruction, would require new logic, and is needless given the regenerate outcome is strictly better.
- **Modify `src/battle.rs`'s +1 to test whether the failure goes away.** Dropped — ticket forbids `src/battle.rs` changes.

## What to try next

1. Capture per-frame pose pixel counts on real canon and on rust for `chip-stepswrd` over the same 40 frames; identify which frame's pose presence differs (the worst-152-px/frame spike is the most informative frame). The mechanism is plausibly one OAM frame of the navi at home.
2. Walk the ChipDataArr record for id 81 vs id 72 (`WideSwrd`) byte-by-byte, find the differing bytes (likely candidates: `attack_param_1..4`, MB, codes, `chip_element`). If one byte differs and canon dispatches on it, the +1 belongs in a record-driven position, not a per-id fitted constant.
3. Once the discriminator is settled, add either: (a) a per-id keyword in `src/battle.rs`'s `actor::AttackSpec` shape, or (b) a record-driven `recover` field, and re-run. The ticket's "no src/ change" exclusion was based on the premise that the fitted +1 was sufficient, which is disproven by this ticket's measurement.
4. M4 count remains **43/411** — StepSwrd id 81 is no longer `unrecorded` in `chips.json` (it is now `verified`), but the pixel row is not yet at 0/0/N. **M4: 43/411 (chips.json), 0/10 family-0x13 row closure** — chip-stepswrd FAILED, all others 0/0/40.

## Numbers (final)

- ROM sha256 before: `0d3ea6d3277b0b43eb05d63deb193dcbac59ddd88ce73e6e9f1e9678f89b7a9f` (584392 B)
- ROM sha256 after T85's file changes: `0d3ea6d3277b0b43eb05d63deb193dcbac59ddd88ce73e6e9f1e9678f89b7a9f` (584392 B) — bytes unchanged because T85 only edits tool files (rust target is the same)
- Fitted constants count: 16 (one fewer than HEAD — the +1 at STEP_SWORD.recover is no longer counted as 'fits canon')
- Family-0x13 rows identical to HEAD (10 of 10 0/0/40 with the same T17-carried negatives)
- chip-stepswrd NEW row: FAIL 670/152/40, negative 26899 non-blind

## Closure

NEGATIVE — the row passed file-side review (no existing row touched, no allowlist/trace/src changes, chips.json id 81 promoted from unrecorded to verified per T17/T19's settle point), but the pixel measurement disproved the ticket's "fitted, no src/ change required" prediction. Per the ticket's acceptance clause:

> A NEGATIVE naming the recovery-pose record-vs-fitted decision also closes it (e.g., NEGATIVE if the row passes only when the recovery-pose is fitted, naming which record byte would change the outcome if record-driven).

this counts as a closed NEGATIVE. The next worker should follow the diagnostic probes above before the +1 in `src/battle.rs` is touched.
