# T221 — element_hit row (OPEN 2026-09-18)

Objective: make the M3 `damage_element_mult` (src/battle.rs:944) reachable and
measured via a new `element_hit` harness row. Canaries: chip-cannon 0/0/40/9505,
mettaur 0/0/70/41734, cursor 1/1/170/186279 must stay byte-identical.

Mechanism per ticket: element-override bytes in the descriptor reserved region
(F38h/T105 precedent) + canon-side F5b one-shot `--poke-at` of the RAM chip
record's +2 element byte. x2 cell: byte_3007444 (ROM 0x081d7944, def*5+atk),
AQUA(atk) -> WOOD(def) = 1.

## Baseline (before any edit)

(to be filled after capture)

## Baseline (before any edit) — measured 2026-09-18

- `chip-cannon isolated PASS total 0 worst 0 frames 40 | negative: not blind (total 9505)`
- `mettaur    isolated PASS total 0 worst 0 frames 70 | negative: not blind (total 41734)`
- `cursor     isolated FAILED total 1 worst 1 frames 170 | negative: not blind (total 186279)` (the ticketed 1px)

## Canon archaeology so far (the +2 poke address hunt)

- Chip fires in the F5b replication: `--poke-at 3:0x020340a4:0x0001 --poke-at 3:0x02036822:0x0001`
  on /tmp/afterdissolve_0x0c.state, library pokes cc.library_pokes(1) = `--poke 0x020008a0:0x019d
  --poke 0x02004c20:0x8062`, hand slot poke 0x020349c2:0x0001. MegaMan CurAction 0x08->0x14 at
  frame 3 (probe.py watch receipts) — the press lands.
- `unk_2036720` (per-alliance current-attack record: u16 chip@+0, u16@+2, u32 dmg@+4, obj@+8;
  sub_802CE78 reader asm03_0.s:13909-13920, sole writer sub_802CE8A asm03_0.s:13923-13942 from
  sub_80E3504 asm31.s:91660-91684) stays ZERO for 60 frames across a chip-cannon use — NOT
  written on the normal chip path. Dropped as the poke target.
- Shot spawn chain: cannonAttack_80EBC28 (asm31.s:109536) -> sub_80C4FFE (asm31.s:27906) ->
  object_spawnType3 + BasicBattleObjectInitialize (asm00_2.s:5429-5438): `strb r3,[oBattleObject_Element]`
  — the shot object's Element byte comes from sub_80C4FFE's popped r3 = cannonAttack's r2 =
  `ldrh r2,[r7,#oAIAttackVars_AttackBoost]` (asm31.s:109650), i.e. AIAttackVars+6 low byte.
  sub_801056A (asm00_2.s:3480-3484) fills AttackBoost from sub_802CE78's +2 halfword — but that
  record is zero on this path, so the cannon's own init must write AttackBoost elsewhere (TBD).
- ChipData record: id u16@0, element byte@2 (CHIP_ELEM numbering, Cannon=0x0A NONE, assets/chips.bin
  rec0 = 01 00 0a 06 28 00), power u16@4 — src/chips.rs:149-171.
- open: where does the cannon chip-use write the element that reaches the shot object —
  AIAttackVars Unk_02@+2 (many writers asm00_2.s:6005..) is the next suspect; then poke that byte.

- MegaMan AIDataPtr = 0x02034080 (peeked 0x0203aa08 = [0x0203a9b0+0x58]), so AIAttackVars =
  AIData+0xa0 = 0x02034120. Watch 0x02034120:0x10 over the chip-cannon run: Damage(+8)=0x28=40 and
  Unk_0a=0x1e pre-populated at battle init (already set at capture frame 0); AttackLatch(+1)=4 at
  frame 4, AttackStage(+0)=4 at frame 33; Unk_02(+2)=0 and AttackBoost(+6)=0 the whole run.
- --watch-write 0x02034122/26/28 over the same run: ZERO hits — the cannon attack never writes the
  element carriers mid-attack; the shot object's Element stays 0 (NULL) => a cannon shot can never
  trigger the primary table in canon, matching mult=1 for every existing row.
- chips.bin layout note: RECORD=36 is the EXPORT's own layout (src/chips.rs:15), not the ROM's 44
  (0x2c, getChip8021DA8 mul #44, asm02.s:6-11); element@+2 is inside the shared prefix, so r[2]
  is the ROM record byte.

NEXT: catch an elemental chip (AquaSword 0x4d) writing its element into AIAttackVars/shot at
attack time (--watch-write on 0x02034120:0x10), to learn the numbering canon carries (CHIP_ELEM
raw vs +1 ELEM) and where the strike's attacker element lives for the sword path.

## Attempt: sword-strike row on the PAUSED mettaur route (2026-09-19, this session)

**Shipped (committed):** the byte-inert half of T221 — descriptor +65
`chip_elem_override` (CHIP_ELEM+1, 0=sentinel) and +66 `enemy_elem_override`
(ELEM 1..4, 0=sentinel), read in src/fixture.rs and wired into BOTH
`damage_element_mult` sites (sword strike src/battle.rs:5098-ish, AirShot
:5121-ish) via `self.fixture`. Canaries re-measured after the change, both
PASS 0/0 with unchanged negative totals: chip-cannon 0/0/40 (neg 9505),
mettaur 0/0/70 (neg 41734). Byte-inert confirmed: every existing fixture
leaves +65/+66 = 0.

**Not shipped: the element_hit row itself.** The canon-side A press is refused
on the PAUSED route and I ran out of budget solving it. Everything below is
measured fact for the next worker.

### The ROM table (settled)
byte_3007444 (ROM 0x081d7944, index def*5+atk, 1 = one extra hit) read back
this session, bytes 0..24:
`00 00 00 00 00 00 01 00 00 00 00 00 00 01 00 00 00 00 01 00 00 00 00 00 00`
i.e. def1(HEAT) weak to atk2(AQUA); def2(AQUA) weak to atk3(ELEC); def3(ELEC)
weak to atk4(WOOD); def4(WOOD) weak to atk1(HEAT). **The ticket's "AQUA->
WOOD = 1" matches no cell** — the real cell is WOOD defender weak to AQUA?
no: WOOD(def) weak to HEAT(atk), and ELEC(def) weak to WOOD(atk). Chosen
row pair: Sword(HEAT override) vs WOOD Mettaur => cell def4/atk1 = byte 21 = 1.

### Poke targets (measured, cited)
- Attacker element: MegaMan AIAttackVars.Unk_02 = AIData+0xa2. AIData = MM
  object (0x0203a9b0) +0x58 = 0x02034080 (peeked). Target **0x02034122**,
  ELEM-numbered (AquaSword watch wrote 0x02 = ELEM_AQUA at 0x08019FC2's
  store, lr 0x080C5301). Plain Sword leaves Unk_02=0 AND Unk_03=0 at frame
  155 (peeked) => halfword poke 0x0001 is safe (preserves Unk_03).
- Defender element: enemy collision PrimaryElement = CollisionData+2. Enemy
  object 0x0203aa88, CollisionDataPtr @0x0203aadc = **0x02038598** (peeked,
  PAUSED route, frame 155). Enemy Element byte (0x0203aa96) = 0 (Mettaur is
  NULL), so the collision byte must be poked directly: **0x0203859a := 0x0004**
  (halfword; +3 secondary element stays 0). Not verified whether the Mettaur's
  dive/emerge cycle rewrites it — poke late (>=150) and watch-write if the
  x2 doesn't land.
- Enemy HP 0x0203aaac := 0x0064 (poke at load; x2=160 kills HP-100, negative
  80 leaves it alive => guaranteed non-blind without damage digits).
- Enemy panel: PanelXY @0x0203aa9a reads 0x0204 => the Mettaur stands at
  panel X=4,Y=2 on the PAUSED route.

### THE BLOCKER: the chip press does not fire on the PAUSED-with-alive-enemy route
Recipe tried (probe.py watch, sterile + paused_superarmor.state):
library_pokes(0x49) + hand 0x020349c2:=0x0049 + enemy HP, plus
`--script "Start@10"`, plus poke-at 160 A press, in every proven variant:
(a) AIDATA_PRESSED 0x020340a4 + MIRROR 0x02036822, hold; (b) both with
release at 161; (c) the exact `_aidata_tap_pokes` shape (AIDATA press 160 +
AIDATA release 161, no mirror — chip-use row's recipe). Result every time:
MM CurAction (0x0203a9b9) stays 0x08 through 185, enemy HP never moves.
Sequencer word **0x0203CA70 = 0x001c the whole window** — the AGENT_GUIDE
says input reaches MegaMan only while it is 0x08 (0x0C accepts a one-shot).
The chip-use row's proven fire (CurAction 0x14, harness.py:2600) is on the
_zero_enemy route (cheats DELETE_ENEMY, harness.py:3480) — its sequencer is
0x08 there. Hypothesis for the next worker: with a live enemy the PAUSED
route sits in sequencer 0x1c (chip-select-ish state) and AIData taps are
ignored; either find the sequencer transition that returns to 0x08 with an
enemy alive, or revive a dead enemy on the afterdissolve route (whose press
at frame 3 is proven), or drive a real-joypad script press and check whether
the real KEYS path bypasses the AIData gate.

### Row design (ready to land once the press works)
- Chip: LongSwrd (0x49, subfamily hit-shape 2 = two panels ahead,
  SwordHitShapeBySubfamily_80EBA18 — src/battle.rs SWORD_HIT_SHAPE) so the
  strike reaches panel X=4 from MegaMan's standing panel with ZERO movement.
  MM's own X was peeked 0xffc40000 (u32 @0x0203a9e4) vs enemy 0x00140000:
  80 units apart = 2 panels => LongSwrd's far panel lands on the Mettaur.
- Canon: sterile + paused_superarmor.state + ALIVE + Start@10 + library_pokes
  + hand 0x49 + enemy HP poke, poke-at 158:0x02034122:0x0001 (HEAT attacker,
  preserves Unk_03=0), poke-at 158:0x0203859a:0x0004 (WOOD defender),
  press at 160 once the press puzzle is solved (strike lands ~+15).
- Rust fixture: FIELD_ROW with hand=[0x49], fire_frame = press-8,
  chip_elem_override=1, enemy_elem_override=4, everything else = mettaur row
  (enemy_col=5 etc. — note FIELD_ROW's enemy_col=5 maps to canon panel X=4).
- Negative: T225's `negative_rust` — same fixture with +65/+66 zeroed; the
  x2=160 kill vs 80-damage survive is the non-blind oracle (Mettaur dies
  ~15 frames after the press: banner/explosion frames both sides).

### Also measured, unrelated but useful
- unk_2036720 (per-alliance current-attack record) stays 0 on the normal
  chip path — dead end as a poke target, do not revisit.
- AIAttackVars (0x02034120..0x12a) is pre-populated at battle init from the
  chip record (Unk_02=element+1-bias? AquaSword read 0x02/Unk_03=0x0d,
  Sword 0x00/0x00) and is NOT written mid-attack (watch-write 0x02034120:0x10
  gave zero hits for Cannon and AquaSword).
- MegaMan X u32 @0x0203a9e4 = 0xffc40000, enemy X @0x0203aabc = 0x00140000
  on the PAUSED route (frame 155); high halfword = position (MM -60, enemy
  +20), 80 units = 2 panels => panel width 40 in this unit too, origin TBD.
