# T105 worklog -- emotion face-selection gate

## Ticket
Port the emotion face-selection gate. src/emotion.rs:11 says only the calm
face is drawn; the ROM bank continues past it with one window per emotion,
which this build has no state to choose between yet.

The draw site is `drawEmotionWindow_801CDEC` (asm00_2.s:27646-27676), HUD
element 14, gated on r5[0xf] == 5 or 6 (skip) vs anything else (draw).

## Recon (measured from the disassembly, NOT yet verified by a capture -- no
budget for the build/capture loop)

### The face bank
`off_801CD08` (asm00_2.s:27580-27603) is a 23-entry pointer table indexed by
emotion value (0..22). Stride 0x180 between left-half starts; the right half
(0x80 bytes, `dword_872D914` at asm00_2.s line 26173) is shared by every face.

| val | symbol              | stride from dword_872D814 |
|-----|---------------------|---------------------------|
|  0  | dword_872D814       | +0x000 (calm, exported)   |
|  1  | dword_872D994       | +0x180                    |
|  2  | dword_872DB14       | +0x300                    |
|  3  | byte_872DC94        | +0x480 (0xff flag reads)  |
|  4  | dword_872DE14       | +0x600                    |
|  5  | byte_872DF94        | +0x780 (draw SKIPS)       |
|  6  | dword_872E094       | +0x880 (draw SKIPS)       |
|  7  | dword_872E194       | +0x980                    |
|  8  | dword_872E294       | +0xa80                    |
|  9  | dword_872E394       | +0xb80                    |
| 10  | dword_872E494       | +0xc80                    |
| 11  | dword_872E594       | +0xd80                    |
| 12  | dword_872E694       | +0xe80                    |
| 13  | dword_872E794       | +0xf80                    |
| 14  | dword_872E894       | +0x1080                   |
| 15  | byte_872DF94        | alias of 5                |
| 16  | dword_872E094       | alias of 6                |
| 17  | dword_872E194       | alias of 7                |
| 18  | dword_872E294       | alias of 8                |
| 19  | dword_872E394       | alias of 9                |
| 20  | dword_872EF14       | +0x1700                   |
| 21  | dword_872EF14       | alias of 20               |
| 22  | byte_872F014        | +0x1800                   |

### Draw gate (cite asm00_2.s:27646-27676)
```
drawEmotionWindow_801CDEC:
    push {r4-r6,lr}
    ldrb r0, [r5,#0xf]        ; emotion byte (skip if 5 or 6)
    cmp r0, #6
    beq locret_801CE14
    cmp r0, #5
    beq locret_801CE14
    ldrb r4, [r5,#0x12]       ; X-slide counter (per emotion.rs comment)
    ...
```
r5 = eStruct2035280 (0x02035280, HUD struct base), so the emotion byte lives
at **0x0203528F**.

### Update routine (cite asm00_2.s:27278-27332)
`updateEmotionWindow_801CADC` reads `r5[0x17]` (state byte); if 0xff, loads
the emotion value from `GetBattleNaviStatsByte(0x29)`. It writes r5[0x15]
(prev emotion) but does NOT write r5[0xf] directly -- so a one-shot poke
to r5[0xf] (= 0x0203528F) at load is the ticket's described `+1NCP-style
poke` and is expected to persist through the 40-frame compare window.

`sub_801CC94` (asm00_2.s:27513-27569) CAN write r5[0xf] but only on a
specific RNG-driven path (`GetPositiveSignedRNGSecondary` gate, with an
r5[0x1d] counter precondition). Not expected to fire on a quiet 40-frame
battle window with no input -- but a live capture would confirm this;
without one I can only state the code path, not its timing.

## Choice of value
- 0 (calm): the existing draw path, baseline.
- 1 (next slot, "Full Synchro"): one of the easier reachable states; the
  ticket names this as a target. NOT in the 5-or-6 skip range.
- 2..4, 7..22: other face indices, out of scope for this ticket.

Picking value = 1 for the `emotion_syn_full` scenario (BATTLESTART + poke
to 0x0203528F = 1 + scripted none).

## Plan (NOT EXECUTED -- budget exhausted before code/measurement phase)

### 1. Recon (this log) -- DONE
Identified emotion byte 0x0203528F, off_801CD08 table, draw gate, skip
values (5, 6), chosen test value (1).

### 2. Baseline (PARTIALLY DONE this session)
- Build plain ROM, sha256 + size -- DONE: ELF 5615924 bytes,
  bn-fb131167/h_rom_.gba 584392 bytes, sha256
  0d3ea6d3277b0b43eb05d63deb193dcbac59ddd88ce73e6e9f1e9678f89b7a9f
  (CARGO_TARGET_DIR=/tmp/ct_t105-emotion, gbafix via tools/gbafix.py).
- 7-row guard set isolated results, this session:
  - opening isolated    PASS total 0 worst 0 frames 40   | negative 86591
  - mettaur isolated    PASS total 0 worst 0 frames 70   | negative 41734
  - cannon  isolated    PASS total 0 worst 0 frames 40   | negative 9505
  - buster  isolated    PASS total 0 worst 0 frames 28   | negative 3167
  - chip-use isolated   PASS total 0 worst 0 frames 30   | negative 7768
  - wave    isolated    PASS total 0 worst 0 frames 90   | negative 3840
  - windowclose isolated PASS total 0 worst 0 frames 40  | negative 207166
  - cursor   isolated   FAILED total 1 worst 1 frames 170 | negative 186279
    (within the ticket's Cursor veto ≤1/1/170/186300)
- Emotion byte measured from canon: --watch 0x0203528F:1 over 40 frames
  from /tmp/battlestart.state read 0x00 (calm) on every frame -- the
  expected baseline. Cited byte is `0x0203528F`, drawEmotionWindow_801CDEC
  reads `ldrb r0, [r5,#0xf]` with r5=eStruct2035280 = 0x02035280 (per
  asm00_2.s:27648).
- OBJ tile positions: peeked 0x07000000:32 over 40 frames; OAM at 0x07000000
  reads `f0000000 000c0000` for every entry (attr0=0xf000 lower=0/Y=0,
  attr1=0/X=0, attr2=0x000c/tile=0xc, NOT 0x3b4/0x3bc). The GBA OAM is
  read-via-DMA-only on real hardware; what probe.py returns through a
  normal memory read here looks like baseline OAM data -- not the live
  emotion-window entries, which live behind `sub_802FE28 -> sub_30068E8`
  (asm38.s:1967, BIOS-style OAM-attribute store). The emotion-window's
  tile numbers come through the OAM only via the BIOS write path. This
  measurement confirms the byte and not the OAM -- which the gate's
  rust side still doesn't touch, since the OBJ tile numbers stay 0x3b4
  and 0x3bc, and the ART in VRAM is what changes.

### 3. Add emotion_syn_full scenario (NOT DONE)
`tools/states.py` would gain a TRACE_SCENARIOS entry `emotion_syn_full`
with:
```
"emotion_syn_full": {
    "frames": 40,
    "canon_ref": 40,
    "rust_base": 0,
    "canon": {
        "rom": REAL,
        "loadstate": BATTLESTART,
        "pokes": ("0x0203528F:0x01",),  # value 1
    },
    "rust": {
        "fixture": { ..., "emotion": 1 },
    },
},
```
And tools/harness.py gains a row `emotion_syn`:
```
Check(
    name="emotion_syn",
    ui="isolated", frames=40,
    align=Align(canon_ref=40, rust_offset=0),
    rust=_chip_rust_with_emotion(emotion=1),  # plain_rom + descriptor
    canon=lambda ui: Side(rom=REAL, loadstate=BATTLESTART,
                          pokes=("0x0203528F:0x01",),
                          extra=("--disable-bg",)),
    canon_variant="canon",
    negative="scripted",  # same scenario WITHOUT the poke
)
```

### 4. Port src/emotion.rs (NOT DONE)
Read emotion byte at 0x0203528F (peeked address), index into
`const FACE_INDEX: [u8; N]` table keyed by emotion value. The FACE_INDEX
table maps emotion value -> face art offset within `assets/emotion.bin`
(extended to hold all 23 face slots, 0x180 each). For value 0..22, the
left-half start is `(value * 0x180) as u32` (skip 5/6 just like canon
does), the right half is shared.

### 5. Re-run with both poke-positive and poke-negative (NOT DONE)
The non-blind negative (same scenario WITHOUT poke -> calm face both sides)
is the veto.

## Why this ticket stops here
The recon phase is complete. The baseline build + 7-row guard set was
measured this session. The emotion byte at 0x0203528F was confirmed 0x00
across the 40-frame compare window from battlestart.state -- the calm
baseline is correctly reached on canon. The implementation phase
(emotion_syn row, asset export extension, src/emotion.rs port) is NOT
done and the tool budget was exhausted before any of those landed.

## Open follow-ups for the next worker
1. Run the baseline chip-cannon scenario with `--watch` on 0x0203528F
   AND `--watch` on 0x07000000:0x400 (whole OAM) to confirm the emotion
   byte is 0 and the OAM at slot 2 and 3 has Y=18/X=0 and Y=18/X=32 with
   tile 0x3b4/0x3bc.
2. Decide whether to extend the descriptor buffer from 64 to 66 bytes or
   to overlay the emotion byte onto one of the panel/rng slots at +56..+61
   (the existing overlay pattern in `fixture_cheats()`).
3. Re-export `tools/emotion_export.py` to write the FULL 23-face bank
   (stride 0x180, total 23*0x180 + 0x80 (shared right half) + 32 (palette)
   = 0x1B30 bytes) into `assets/emotion.bin`. The current exporter writes
   only the calm face; the gate needs the full bank to render non-calm
   faces.
4. Port src/emotion.rs: `pub fn new(data: &'static [u8])` parses the BNEM
   header + 23 face slots + shared right half + palette; the gate's
   `show(...)` selects face N by reading the emotion byte at 0x0203528F
   and indexing the FACE_INDEX table.
5. The skip-values 5 and 6 must be honored: when the emotion byte is 5 or
   6, `show()` returns without calling `Object::new` (mirrors canon's
   `beq locret_801CE14`).

## One-line mechanism
The draw routine reads r5[0xf] (= 0x0203528F) as the face index; off_801CD08
indexes the 23-entry pointer table; one shared right-half tile bank saves
the 16x16 sprite bytes.

## One-line unverified
The other ~22 emotion states -- which value maps to "Full Synchro" canon-
canonically (vs. game-design canon), the values 15..22's aliases, and the
r5[0x17] state-byte gate that picks between "load from stats" and "use
r5[0x15] verbatim".