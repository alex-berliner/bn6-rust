# T143 — M8 LOSER window row at 0: land T136 PARTIAL step 5

Worktree /tmp/bnwt/t143, branch wt/t143, base 4d48fa5. Tool budget ran out mid-ticket
(soft 80 reached while localising); everything measured is below so the next worker
starts from numbers, not guesses. NO src/ change was made; no allowlist change; the
three guard rows were baselined before any edit.

## Step 1 — baseline (all three, before any edit)

- `result` isolated: **PASS total 0 worst 0 frames 40** | negative: not blind (111839)
- `opening` integrated: **FAILED total 18740 worst 647 frames 40** | negative not blind
  (98649) — this is the KNOWN integrated state, not a regression: TODO.md:233 records
  `verify_rows HEAD opening --expect opening=18740/647/40/9` as the F44/F48 constant.
- `cursor` isolated: **FAILED total 1 worst 1 frames 170** | negative not blind
  (186279) — the pre-existing tear, exactly the ticket's 1/1/170.

## Step 2 — the alt branch, with the ticket's cite CORRECTED

The ticket says "asm/asm00_2.s (resultsWindowSlideTick_802BE36 alt)". **asm00_2.s
contains no result-window code** (grepped for 802BE36 / resultWindow / result+slide).
The routine lives at **reference/bn6f/asm/asm03_0.s:11701-11734**. Its alt branch is
**asm03_0.s:11716-11718**: `ldrb r0,[r5,#4]` (the variant byte dword_20364C4, stored
from the ending-kind record by showResultWindow at asm03_0.s:12446-12447),
`cmp r0,#2; beq locret_802BE66` — variant 2 (LOSER) skips the clear-time draw
(drawResultClearTime_802C4E8, asm03_0.s:11722) and the level draw
(drawResultLevel_802C6EC, asm03_0.s:11724) that the WIN slide performs every tick.

**Our port already contains this branch's observable behaviour**: src/results.rs:520
gates the whole time/level block on `if variant == WIN`, and the blob's variant 1 IS
canon's lose art (tools/results_export.py:47 packs tiles `dword_8731154` (0xCA0) +
map `dword_87324B4`; the ticket's "three tile sets" off_802C434 are three 24x18-cell
MAPS — 0x360 = 432 u16 cells — not tile sets).

Supporting reads (all asm03_0.s unless noted): the four ending wrappers share
showResultWindow + resultWindowDriver (asm00_1.s:13385/13976/14385/14838; lose
wrapper sub_8009916 at asm00_1.s:13970-13989 shows the window once then drives it,
Index_01=0x14 on driver return 0). The driver (11584-11627) saves prev slide x,
dispatches phase[eS20364C0], then blits the 24x18 variant map at (j,2) into screen
block 3 via CopyBackgroundTiles and calls sub_802C460 (incremental column redraw,
no-op once x==prev). showResultWindow (12432-12503) uploads per-variant tile GFX to
**0x06008020** (off_802C3FC table 12510-12518: win 0xE60 B / mid 0xF00 / lose 0xCA0)
and stages ResultWindowPalettes_8732814 (96 B = 3 banks). BGxCNTs on the lose route,
peeked at capture frame 250: BG1CNT=0x1d03 (prio 3, char base 1 = 0x06008000, screen
base 3) — **the result window and the battle backdrop are the SAME hardware layer
(BG1)**; BG2CNT read 0x3e02 and BG3CNT 0x3f05, differing from the normal-battle
0x1e02/0x1f09 recorded in the zero-layers comment (asm00_2.s:29138).

## Step 3/5 — why the row reads 1536000, measured (7 captures + numerics)

`result_lose` on unmodified src reproduces T136 exactly: **FAILED total 1536000
worst 38400 frames 40 | negative not blind (1536000)** — whole screen, every frame,
no alignment. The mechanism, measured this ticket:

1. **Our side never reaches the LOSER window.** T136's LOSE_ROW (megaman_hp=0 at
   spawn, flags=0x11, no enemy seed) leaves our ROM in the live fight for the whole
   capture: our frames 180..400 stay the dark-blue battle arena with ~5-8k px/frame
   of animation (consecutive-diff scan 24..400); canon at the same wall-clock shows
   the LOSER screen. T136's open hypothesis 1 is CONFIRMED: HP-0-at-spawn does NOT
   fire the defeat path, because `is_defeated()` is `Action::Gone` (src/actor.rs:523)
   and only `take_damage` (src/actor.rs:621-642) ever sets `Action::Dying`/`Gone`.
   NOTE take_damage DOES handle HP-0: `hp.saturating_sub(amount)` then
   `if self.hp == 0` → Dying. **One hit on HP-0 MegaMan fires the whole natural LOSE
   flow**: `decided` arms on megaman.is_dying/is_defeated (src/battle.rs:2881-2898),
   dissolve 35 → SEQ_10 (2919-2921) → MEGAMAN_DELETED banner (3247) →
   show_results(LOSE) (`won = !self.megaman.is_defeated()`).
2. **Canon's LOSER screen structure** (T136's /tmp/t136_lose, numerically): period-64
   CONFIRMED (diff(k,k+64)=0 for k=180..300); 2768 px/frame animate spread over the
   whole screen (x 0..239, y 5..155); 34490/38400 px are magenta-family
   (R>96,B>80,G<64); the only non-magenta pocket is y64..87 (3910 px, the art's own
   bright band). `--only-bg 1` capture shows the flame art ALONE, animating
   3860 px/frame (diff 200/201), 0 at +64 — **the animation is BG1's own backdrop
   machinery running under the result palettes**; `--only-bg 3` renders nothing
   (BG3 empty on the lose route). It is NOT a palette-swapped arena: bright-mask
   agreement vs a no-poke battle BG1 capture is 0.356.
3. **The route exists inside the ticket's file list**: seed the Mettaur's swing.
   enemy_state/enemy_action are applied ONLY on the custom-window opening path
   (src/battle.rs:3072, `f.enemy_state != 0 && f.enemy_action == 0x0b`, primed
   SWING_PRIME=9 executor ticks, frozen while the window is up, resumes on close —
   F37's "strike + rolling wave"). So: LOSE_ROW must carry enemy_state=4,
   enemy_action=0x0b, megaman on the enemy's row (megaman_row=3; enemy col 5 row 3,
   MegaMan col 3 = two panels in front), a hand so the custom screen has chips
   (hand=[1], hand_count=1, gauge=1 — the pattern of harness.py:1150), flags=0x11
   (OPEN_WINDOW|SKIP_INTRO — no RESOLVE bit: resolve_over fakes an ENEMY kill and
   would read WIN), and the rust side needs a scripted A-press to close the window
   so the fight resumes and the swing lands. SceneFlags (src/fixture.rs:48+): 0x01
   OPEN_WINDOW, 0x10 SKIP_INTRO, 0x20 resolve_over.
4. **No src/results.rs port is needed for the row to reach 0** (ticket step 3's
   premise): the alt branch is already ported (`variant == WIN` gate), the lose
   assets are packed, and show_results installs the result palettes on BOTH routes
   (src/battle.rs:2596-2598), which is what turns the still-running BG1 backdrop
   magenta behind the window map — canon's own composition. If the row still reads
   non-zero after the route works, the FIRST suspects are (a) our backdrop freezing
   after `over` while canon's keeps scrolling (check battle.rs's backdrop.update
   gate), (b) the alignment phase of the 64-frame cycle (search band must contain an
   offset ≡ the right phase mod 64), (c) the slide/mark — NOT a new results.rs
   branch.

## What landed (branch wt/t143)

- ef8d43c: tools/harness.py — T136's two hunks re-based onto main (LOSE_ROW dict +
  result_lose Check with T136's measured canon recipe: PAUSED + Start@10 +
  DELETE_ENEMY cheats 0x0203ab84/86:0 + poke-at 40:0x0203a9b8:0x0008, canon_ref 185,
  search range(0,200), frames 40, negative = default 1-frame canon shift). T136's
  stale-base deletions (form_cross row, `replace` import) were NOT carried.
- The unmodified-src row read (above) was measured on THIS commit's harness.

## Next worker's plan (cheapest first, all measured context above)

1. **LOSE_ROW v2** (harness-only): megaman_row 2→3, add enemy_state=4,
   enemy_action=0x0b, hand=[], hand_count=0, gauge=0 → hand=[1], hand_count=1,
   gauge=1 (custom screen needs chips); rust Side gains script="A@<F>" — F must land
   INSIDE the custom-open interval (unmeasured; probe our ROM with
   `python3 tools/probe.py frame <rom> 200 --at 40,60,80,100,120` + the fixture
   cheats from `harness.fixture_cheats(LOSE_ROW)` to find the open window, then set
   F inside it; a press landing in the FIGHT fires the hand chip and can kill the
   ENEMY → WIN). Widen the row's search honestly only if our stable window lands
   past offset 199 — arithmetic warning: our route adds custom-open + A-press +
   swing + Dying(35) + dissolve(35) + RESULTS_DELAY(110) before the slide, so the
   arrival may sit near/over offset 200 from canon_ref 185; T136's plan named this.
2. Run the row; if aligned-but-nonzero, diffmask the residual by region (window vs
   border vs band) BEFORE touching src — the composition analysis in step 3 above
   says the likely residual is backdrop-phase or backdrop-freeze, i.e. battle.rs
   (NOT in this ticket's file list — escalate, don't widen).
3. Only if the diff localises inside the window itself: that would be the first
   evidence the results.rs port is incomplete; the alt branch (no time/level on
   LOSE) is already there, so look at the mark sprite (show_results sets
   results_mark on both routes; canon's lose driver enqueues the mark only via
   enqueueResultMark_802CA5C on some state arms) and the dismiss pacing (canon
   auto-dismisses at ~349; the row's 185..225 window does not cover it).
4. verify_rows result/opening/cursor after any change; cursor 1/1/170 must not move.
   (This ticket baselined them BEFORE the harness edit; the edit is additive rows
   only, and they were not re-run after it.)

## Budget close (2026-09-18, second worker)

LOSE_ROW v2 + search 0..320 committed (578e70f) and run: **result_lose isolated
FAILED total 1536000 worst 38400 frames 40 | negative: not blind (1536000)** —
byte-identical read to v1: the route did NOT complete. The A@80 press was the one
unmeasured knob (declared so in the commit): either the custom window was not open
at frame 80 (press consumed by boot/intro), or the press landed after the window
closed into the FIGHT (fires the hand chip → can kill the ENEMY → WIN), or the
swing resumed but missed / was cancelled. The number did not move; per ticket rules
no further widening was attempted. NEXT: probe the v2 fixture's custom-open
interval (`probe.py frame` with `harness.fixture_cheats(LOSE_ROW)`, frames
40..140), set the A-frame inside it, re-run; if the route still fails, probe
frames around the close for the strike/dissolve before suspecting anything in src.

## Unverified (this ticket's line)

- Whether the seeded swing actually lands on our side (route never executed: budget
  ran out during localisation, before the LOSE_ROW v2 run).
- The custom-open frame on our side (drives the A-press frame).
- Whether our backdrop keeps animating after `over` (canon's does, measured).
- The dismiss pacing on the lose route (out of the row's compared window).
