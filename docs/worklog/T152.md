# T152 — M2 battle_full phase gap: close the PARKED-vs-fight divergence on T147's map

Base 7e17b94 (main), branch wt/t152, worktree /tmp/bnwt/t152. Files: src/battle.rs,
tools/trace.py, docs/coverage/battle_full.md + TWO coordinator-authorized lines outside the
ticket's Files list (tools/states.py `"start_state": 2` on battlestart_scripted's rust fixture,
FIXTURE.md +40 contract line) — granted via intercom need_decision after I showed no in-scope
discriminator exists (flags byte full, bit7=TRACE; battle_full and battlestart_scripted fixtures
byte-identical in flags/gauge/hand/deck; keying on enemy layout is dishonest AND would wrongly park
the opening row OPEN_ROW, also 3 enemies, which must keep SEQ_08).

## Baseline (before any edit)

- cursor isolated FAILED 1/1/170, negative 186279 (main's documented tear, byte-for-byte).
- trace map (T131 battlestart_scripted, canon 69+k <-> rust 0+k, 40 frames): sequencer 40/40
  canon 0x0 vs rust 0x8; mm_state_action 40/40 (4,0) vs (4,8); enemy_state_action 40/40 (4,0) vs
  (4,10); all other fields match. Reproduces T147 exactly.

## What canon actually does (the measurements that reshaped the ticket)

- probe.py watch 0x0203CA70:4 0x0203a9b8:2 0x0203aa90:2, battlestart_scripted state, 300 frames,
  WITH A@170 and WITHOUT (both runs): word 0x00000000 for all 300 frames either way — the A press
  changes NOTHING. mm and e1 read 0x0004 from populate (~capture 70) to ~capture 185, then both flip
  to 0x0104 (action 1) — clock-driven, press-independent. CONCLUSION: the parked word never releases
  on this route within any probe; the ticket's literal "word 0x8 then the fight word at the same k"
  is unsatisfiable on canon (supervisor adopted this as correcting the ticket: target is canon
  parity, i.e. the park).
- Mechanism (asm00_1.s): sub_80084C0 ZeroFillByWord 12 bytes at the word (:11112-11121) = the
  battlestart init; bannerSeqState00Settle_800840C (:11024-11046) holds 0x00 while
  sub_801483C (the slide machine, seeded once by sub_80147E4) reads busy, one-frame [r5+2] latch,
  then 0x04; bannerSeqState04BannerWait_8008064 (:10519-10562) writes 0x08 when the banner is done.
  Our port's entry (Sequencer::battle() = SEQ_08 unconditionally, battle.rs:1261) misses the arm.

## The port (all src/battle.rs)

start_state==2 -> Sequencer::battlestart() (SEQ_00); entry_park latch (init true, cleared at the
first window Done) pins the SEQ_00 age (the busy leg) so the existing age>=2 edge is the [r5+2]
latch leg; the window-open path's SEQ_20 write suppressed while parked (canon's word stays 0x00
through the entry window; 0x20 belongs to the fight state's own window path, :10609-10611); enemy
act/think/gunner-telegraph frozen under the park (canon runs no object logic in 0x00/0x04,
:12760 dispatch note, already cited in objects.rs's player gate); MegaMan parked in Appearing (the
T50 player arm = canon's PlayerSpawnAnimation byte 0); parked enemies' spawn-hold state translated
to canon's spawn byte 0 at BOTH export sites (oracle + TRC2; actor.rs's enemy export arms have no
0x00 — T50 mapped enemy Appearing to 0x0a for the joined-late rows).

## Result

Trace map after: sequencer 0x0==0x0 k=0..39; mm (4,0)/(4,0); enemy (4,0)/(4,0); FIRST DIVERGENCE
none; 0/40 judged (rng_cadence still matches — the frozen enemies never drew in-window). The
ticket's "first divergence k>=17" met a fortiori (none at all).

## The trap that cost the day: cursor 1/1/170 -> 38/37

First cut moved the cursor row (the ticket's own rule: it must not move). A/B with the src stashed
proved cursor is mine (opening integrated 18740 is NOT mine — it reads 18740 with my battle.rs
stashed too; pre-existing move from T147-era 72499 on this base, ungated: the pinned opening
expectation is the isolated line 0/0/40/86591, which matches). Behavior is inert for
start_state!=2, so the movement was timing: the added `self.entry_park` field loads sat in the
per-frame hot path and the 1-pixel tear is a knife-edge. Fix: hoist the flag into a local once per
update()/snapshot (9 sites). Cursor back to 1/1/170/186279 byte-for-byte. LESSON for the next
worker: on this tree, per-frame field loads in Battle::update are not free — cursor measures them.

## Rows on the final build

cursor 1/1/170/186279 (matches main's tear) · mettaur 0/0/70 PASS · windowclose 0/0/40 PASS ·
popup 0/0/80 PASS · opening isolated 0/0/40/86591 PASS. NOT run on the branch: the full verify_rows
69-row sweep (budget) — the land gate reruns it; every row I did run matches its pinned numbers.
battlestart_scripted's release shape (post-first-window-close: settle latch -> 0x04 -> fight) has
NO canon comparison on this route (canon never releases within 300 frames) — it follows the table's
rule per the supervisor's ruling; a future ticket with a release-capable scenario can pin it.

## Next worker notes

- canon row 'opening' integrated moved 72499 -> 18740 on main BEFORE this ticket (A/B-proven);
  worth a look if you touch OBJ work — it is not gated by the pinned expectation.
- states.py/FIXTURE.md lines are coordinator-authorized (intercom, this ticket); T151 owns a
  different states.py region concurrently — coordinator sequences the merges.

## 2026-09-19 re-measurement pass (branch fast-forwarded to main 5acb6b9)

The ticket NOTE warned main moved (T215 dca956a 0x0C predicate + T220 0033d2d banner
record + T216/T230). Confirmed on ff: `main..wt/t152` was empty -- every commit of the
original T152 park landed, and T220 has since ported the canonical BANNER_RECORD
(isBannerBusy_801E754 as `BannerRecord::is_done()`, battle.rs ~:4082) that T152b/c/T162
were blocked on; the fitted SEQ04_FRAMES arm is GONE from main. No src port left to
make unless the re-measured baseline shows divergence. Plan: baseline trace map on
battlestart_scripted -> cite re-anchor (thumb_func_start labels) -> full verify_rows
isolated table (never run whole on any T152 branch) -> coverage append -> commit.

## Baseline re-measured on main 5acb6b9 (2026-09-19)

trace map battlestart_scripted (canon 69+k <-> rust 0+k, 40 frames):
- sequencer word k=0..39: canon=0x0 rust=0x0 -- match 40/40 (0x0203CA70 parked on BOTH sides)
- mm CurAction canon/rust: k=0 (4,0)/(4,0), k=17 (4,0)/(4,0), k=39 (4,0)/(4,0)
- enemy CurAction canon/rust: k=0 (4,0)/(4,0), k=17 (4,0)/(4,0), k=39 (4,0)/(4,0)
- all other fields (anim/panel/timer/rank/zenny/rng_cadence) match 40/40
- FIRST DIVERGENCE: none; TOTAL 0/40 judged

Acceptance's trace condition (both actors (4,0) k=0..40, sequencer parked) HOLDS on current
main. No src port remains: T220's BANNER_RECORD port replaced the fitted SEQ04_FRAMES=60
arm T152b/c/T162 were blocked on.

## Cites re-anchored by thumb_func_start label (asm line cites drift)

- stepBannerSequencer_800801C: asm00_1.s:10476-10489 -- loads eBattleSequencerState_203CA70
  low byte, indexes BannerSequencerStates_8008038 (table :10512), calls handler, reads [r5+4].
- Predicate keeping the word at 0: table entry 0 = bannerSeqState00Settle_800840C
  (asm00_1.s:11024 region) holds SETTLE while isChipWindowSlideIdle_801483C reads busy plus
  the [r5+2] latch; the battlestart init zero-fills the word (sub_80084C0, asm00_1.s:11112
  region). Entry 1 = bannerSeqState04BannerWait_8008064 (asm00_1.s:10519 region) writes FIGHT
  only once isBannerBusy_801E754 (asm00_2.s:31233) reads 0.
- Actor init: playerObject_main_80EA460 asm31.s:107131-107152 dispatches on
  oBattleObject_CurState; canon's parked CurAction 0 = spawn-hold byte 0. Rust's old
  (4,8)/(4,10) came from fighting-state init order (battle() = SEQ_08 unconditionally,
  actors initialized for the fight); the entry-park gate (landed T152) plus the player gate
  (T7q) freeze both actors in spawn-hold while the word is 0x00.

## Row sweep (full isolated table, 21 rows)

verify_rows.py HEAD <all 21 rows> --expect cursor=1/1/170/186279
(measured after this note; see report)

## Result (ticket closed by re-measurement; no src delta)

- Acceptance: sequencer parked (canon=0x0 rust=0x0, 40/40, no first divergence) AND both
  actors (4,0) for k=0..39 -- the "both actors (4,0)" arm, the stronger shape.
- cursor 1/1/170/186279 unchanged (byte-identical, verify_rows MATCH).
- Full isolated verify_rows 21/21 lines: 17 zero-PASS; windowclose/buster_charge/gunner
  reproduce main's own pins (pre-existing main defects, not this branch's -- src untouched).
- Step 4 (port the gate) needed no new code: the gate has been on main since the original
  T152 landing, and the canonical predicate follow-up landed as T220's BANNER_RECORD.
- Unverified: battle_full's mid-fight 0x0C entry offset (separately ticketed); gunner's
  negative drift 2247584->2284867 vs the T161-era pin (total/worst/frames identical;
  negative captures a deliberately broken variant, drift not investigated here).

Branch wt/t152 tip 0c07b14; worklog + coverage only, src/ identical to main 5acb6b9.
