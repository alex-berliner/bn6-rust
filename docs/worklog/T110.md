# T110 worklog — M1 navicust battle-effect handler enumeration

**Outcome: PARTIAL (recon + route refutation landed; NO harness rows built).
The ticket's poke scenario is refuted by measurement on the battlestart
route — both the equipped-NCP channel poke and the stat-byte poke change
0 pixels over 40 frames, because the handler chain never runs inside the
battle window. Landed: the full 47-handler enumeration with per-handler
cites (docs/inventory/navicust_handlers.json via
tools/inventory.py parse_navicust_handlers), measured counts (24/47
battle-relevant handlers over 13 slots — the ticket's 19 is a slot count
produced by inventory.py's u8-only regex), the 8-row guard baseline, and
docs/coverage/navicust.md. Per the supervisor's q1–q4 decisions, no
BLIND-by-construction rows were added and the SCOPE 0/19 line was left
for the human to restate.**

## Supervisor decisions (q1–q4) — all four changed the plan

- q1: enumerate with the citable filter; never bend a count to 19. The
  19 is a parsing artifact (u8-only regex drops ChipRecovery) → do NOT
  "fix" silently; report 47 handlers / 24 battle-relevant / 13 slots.
- q2: rows must poke the INPUT channel (equipped-NCP array byte_2006DD8)
  so the handler runs; a direct NaviStats-byte poke is control-only.
  Measured: byte_2006DD8 itself is zero-filled and refilled from
  unk_2006CF8 (sub_813BD0C, asm37_0.s:859) by applyNavicustPrograms
  BEFORE dispatch — and the whole chain only runs from boot/navicust-
  exit/cutscene callers, never battle init (see route measurement).
- q3: both sides (canon real ROM + rust plain ROM) get the same poke;
  one shared baseline across rows is not legitimate; budget = 2–3 rows.
- q4: a row that can only read "match" (the no-op stub) is BLIND by
  construction — keep as control, not a row.

## Baseline (step 2, before any edit)

ROM: /tmp/bn-36ddea91/h_rom_.gba (plain build, gbafix) = **584392 bytes,
sha256 0d3ea6d3277b0b43eb05d63deb193dcbac59ddd88ce73e6e9f1e9678f89b7a9f**
(matches T105's published baseline).

8-row guard set isolated (harness, this session):
```
opening     PASS 0/0/40  | neg 86591
mettaur     PASS 0/0/70  | neg 41734
chip-cannon PASS 0/0/40  | neg 9505
wave        PASS 0/0/90  | neg 3840
window      PASS 0/0/16  | neg 81056
cursor      FAILED 1/1/170 | neg 186279   (pre-existing, matches T90)
windowclose PASS 0/0/40  | neg 207166
popup       PASS 0/0/80  | neg 1288
```
Identical to T90's published list (0/0/N ×7, cursor 1/1/170/186279).

## Recon: the 47 handlers (cites measured from asm37_0.s)

Jump table 2111–2158; bodies walked 2161–2780. Full table now in
docs/inventory/navicust_handlers.json + docs/coverage/navicust.md
(per-handler cite line, effect, slot, battle_relevant). Headline counts:
1 stub + 46 handlers; 24 battle-relevant (slots in the SCOPE 19) over 13
distinct slots; 6 of the 19 slots handler-less (BButton/BPwrAtk/Mood/
CustHPBug/EmotionBug/ProcessingBug); 22 handlers write 11 non-denominator
slots (0xd,0x1e,0x1f,0x25,0x26,0x27,0x33,0x36,0x5f,0x60,0x61); HPPlus50
…HPPlus500 bypass SetCurPETNaviStatsByte (direct r10 writes,
asm37_0.s:2704–2780). Composed: BodyPack/FldrPak1/FldrPak2 call other
handlers directly; BustPack bumps Attack/Speed/Charge +3 capped 4.

## Route measurement (6 captures, battlestart.state, canon real ROM)

1. peek 0x02009424 (toolkit+0x74 NaviStatsPtr) @f5 → **0x020047cc**.
2. peek 0x020047cc:0x64 @f5 → CustomLevel 4, MegaLevel 5, GigaLevel 5,
   BLeftAbility 0xff, rest 0 — handlers ran pre-state. Chain callers all
   map/boot/cutscene (reloadCurNaviStatBoosts: asm00_1.s cbGameState /
   sub_8005524, cutscenes, asm28_0/33/36/asm03_1_0) — no battle-init caller.
3. peek 0x02004190:0x200 + battle copies → program-record array
   halfword[0]=0 for ALL records (dispatch would pick the stub for every
   program); eBattleNaviStats203F4AC / 203CB10 read 0x0.
4. peek 0x0200940c:4 @f40 → r10[0x5c Unk2004190_Ptr] = **0x02004190**
   (confirms 3's target); 0x0203F4AC still 0 at f40.
5/6. FstBarr control: --poke 0x020047d2:0x0001 (slot 0x6→1, the value
   navicust_NCP_FstBarr writes) at load vs no-poke, 40 frames →
   **total 0 differing pixels, worst 0**.

Conclusion: on this route the battle window neither re-runs the handler
chain (input inert, callers all off-battle) nor reads the map NaviStats
record (poked byte changes nothing). BOTH row shapes are inert here →
no honest rows; PARTIAL per the supervisor's general note.

## Ideas tried and dropped

- Poking byte_2006DD8 (the ticket's 0x0203XXXX guess refined): dead —
  applyNavicustPrograms zero-fills it and refills from unk_2006CF8
  before dispatch; and the routine never runs in the window.
- Battle copies as the poke target: dead — all four copies read 0 and
  show no in-window writer from the state's frame 0.
- The no-op stub row: dropped (BLIND by construction, q4).

## Next worker

- Find the NaviStats consumer moment: who WRITES eBattleNaviStats203F4AC
  (master) and when relative to battlestart.state; who reads it for
  damage (BLeftAbility 0x7 consumers in the damage pipeline) and
  movement (FloatShoes/AirShoes). A pre-battle state may be the right
  scenario base.
- Consumer-side row (canon-reacts vs ours-doesn't): src/ reads NO
  NaviStats byte anywhere today (grep NaviStats in src/ → script.rs
  SwitchCaseFromNaviStats map only) — so a mid-battle --poke-at of
  SuperArmor/BLeftAbility with a scripted hit would give a genuine
  non-blind gap row; needs supervisor sign-off (shape (b), q2).
- SCOPE restatement for the human: 19 slots ≠ 24 battle-relevant
  handlers ≠ 47 entries; 6 slots handler-less; the u16 ChipRecovery
  regex note in tools/inventory.py's NAVICUST_FIELDS comment.
