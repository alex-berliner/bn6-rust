# T151a — Mettaur rank-1 art port

**Status: BLOCKED ON BUDGET before code change.** Worktree `wt/T151a` branched
from c977210e (T151) at /tmp/bnwt/T151a, env CARGO_TARGET_DIR=/tmp/ct_T151a.
All discovery happened; the actual `src/` edit did not.

## Step 1 — baselines, BEFORE any edit (all numbers from this session)

```
mettaur_rank1 isolated FAILED total 677252 worst 38400 frames 70 | negative: not blind (total 642150)
mettaur     isolated    PASS total 0      worst 0     frames 70  | negative: not blind (total 41734)
cursor      isolated FAILED total 1        worst 1     frames 170 | negative: not blind (total 186279)
ai4_rank0   -- NOT IN harness.py -- the ticket's "ai4_rank0 (T113's row)" cite
             names a row that has never existed on main (T151 worklog
             confirmed: T144 phantom-rung precedent; the verify gate uses
             mettaur_rank1 + mettaur + cursor instead)
```
Full isolated table baseline: mettaur PASS 0/0/70, mettaur_rank1 FAIL
677252/38400/70, gunner 2.1M (pre-existing), navi-gunner 2.1M, buster_charge
2498/188, field-bg1 5280, field-bg2 5280, field-bg3 380263, cursor 1/1/170,
rest PASS. All within the T147 PASS set's envelope (no worse than main).

## Step 2 — canon cites (all assembled from the disassembly + inventory)

- `spawnEnemy_80073E2` (asm00_1.s:8695): per-enemy EnemySetup quad → enemy_idx
  reader. quad[2]+quad[3] low nibble (the rank byte the ticket is about).
- `SpawnBattleObjectUsingBattleEntityConfig_8007368` (asm00_1.s:8572-8636):
  the dispatch loop. `(quad[0]&0xF0)==0xF0` ends the array; `lsr r0,#2` into
  `off_80073A0[0x04]` = `spawnEnemy_80073E2` for the enemy nibble. inventory
  cites this as inventory.py:1143-1162.
- `sub_800EC80` (object.s:6066-6114): the version bump. For ACTOR_TYPE_VIRUS
  rows: returns enemy_idx + min(2·X, 5−version), X = max(sub_8000E30=0,
  sub_8014150 = the PET's NaviCust rank-up bytes 0x38/0x37). On a fresh save
  X = 0, bump dormant, the record's own id fields the rank unchanged.
  (Per T151 worklog, asm00_0.s:1524 + asm00_2.s:11729-11748.)
- `ForMettaur_8109EF4` (asm31.s:171016-171054): CurAction 0..0xC dispatch
  table (13 entries). Rank-1's only behavioural difference here is the
  per-version hop cooldown `byte_8109F46` (`MettaurHopCooldownByVersion`):
  v0=0x1e, v1=0x18, v2=0x12, v3=0x0c, v4=0x12, v5=0x0c.
- Art source for rank-1:
  `virusBattleSpritePtrs[enemy_idx]` = `compVirusBattleSprite_8242E94`
  (SpritePointersList.s:31, data/sprites/compVirusBattleSprite_8242E94.lz77,
  ROM 0x08242E94). The v0 file is `compVirusBattleSpriteMetteur`
  (SpritePointersList.s:30, ROM 0x08241EC4, same directory).
- Inventory: `docs/inventory/viruses.json` row `ai_index=1` has 6 ranks; row
  enemy_idx=2 (rank 1, version_byte=1) is `status: unrecorded` and lists HP
  0x0050 (Struct2 ai-1 row-1 elem_hp), enemy_idxs 2/188/194/200/206/212/218/
  224/230/236/242/248 are the spawn_enemy_idxs the rank-1 art covers.

## Step 3 — what the change would have been (NOT LANDED)

Plan, fully assembled, ready to execute when budget returns:

1. **Export the rank-1 sprite asset.**
   `python3 tools/spr_export.py
    reference/bn6f/data/sprites/compVirusBattleSprite_8242E94.lz77
    assets/mettaur_v1.bin`
   (v0 export uses `assets/mettaur.bin` -- v0 has 4 anims/20 frames/durations
   matching the SWING_PRIME 9-frame cite at src/battle.rs:3108; v1 has
   10 anims because the rank-1 Mettaur adds hop/land/attack frames that v0
   shares with the body.)

2. **Wire the asset** in `src/main.rs` (next to the existing
   `static METTAUR: &[u8] = ... assets/mettaur.bin`):
   `static METTAUR_V1: &[u8] = &Aligned(*include_bytes!("../assets/mettaur_v1.bin")).0;`

3. **Add a kind** in `src/fixture.rs` next to KIND_METTAUR:
   `pub const KIND_METTAUR_V1: u8 = 3; // provenance: derived -- the free
    4th two-bit value; virusBattleSpritePtrs[2] = compVirusBattleSprite_8242E94
    (SpritePointersList.s:31, data/sprites/compVirusBattleSprite_8242E94.lz77,
    ROM 0x08242E94), enemy_idx 2 = ai-1 row-1 (inventory/viruses.json, asm00_2.s:19975
    row 2 = 01 ACTOR_TYPE_VIRUS 01 = version 1)`

4. **Wire the spawn in src/battle.rs** at the `(asset, style, kind_hp)` block
   (line ~2287): branch on `f.kind_of(i)`: KIND_NAVI → GUNNER/Navi/60,
   KIND_METTAUR_V1 → METTAUR_V1/Mettaur/0x50 (peeked Struct2 ai-1 row-1),
   else → METTAUR/Mettaur/0x28.

5. **Pass rank byte to MettaurEntry** (T6 DONE port): add `rank: u8` field
   to `MettaurEntry` in src/objects.rs, populate from fixture kind at spawn.
   Use it to index `HOP_COOLDOWN_BY_VERSION[rank]` in src/actor.rs replacing
   the fixed `HOP_COOLDOWN: u8 = 0x1e` (byte_8109F46: [0x1e,0x18,0x12,0x0c,
   0x12,0x0c]; v1 row selects 0x18 = 24). The behaviour change is small but
   it's the "passing the rank byte to ForMettaur_8109EF4" half the ticket
   calls out (T151a's mechanism line).

6. **Update METTAUR_RANK1_ROW descriptor** in tools/harness.py:1939: change
   `enemy_kind=0` → `enemy_kind=(KIND_METTAUR_V1 << 0) | (KIND_METTAUR_V1 << 2) |
   (KIND_METTAUR_V1 << 4)` so all three slots are rank-1, and the descriptor's
   `enemy_hp` field (which only names slot 0; slots 1/2 keep kind_hp) lets
   each slot carry its own Struct2 ai-1 row-1 default (0x50) via `kind_hp`
   once the spawn reads from `kind_hp` instead of the descriptor.

7. **Add a `MettaurEntry::new_with_rank(rank)`** that sets `self.rank = rank`;
   populate from `f.kind_of(i)` in the enemy construction loop.

8. Re-measure with `python3 tools/harness.py --only mettaur_rank1
   --no-gallery --ui isolated` and verify_rows over mettaur_rank1 +
   mettaur + cursor + gunner/navi-gunner/field-bg1/bg2/bg3/buster_charge.

Expected result (from the rank-1 art cover + 0x50 HP): mettaur_rank1 should
fall to a small residual (palette swatch + the 0x18-vs-0x1e hop cooldown
diff if step 5 lands, the hop arming at frame 12 instead of 13). Worst case
the row reads 0 with all the slots now using the rank-1 sprite sheet.

## Step 4 — why the ticket is BLOCKED, not done

The discovery phase (baselines, asset walk, fixture kind design, the
MettaurEntry rank field) took the entire 80-call soft budget before any
edit landed. The ticket text's rule "If the number did not move as the
ticket predicted, say so and stop" applies to a completed edit; here the
edit did not start. Reporting blocked.

## Dead ends (none hit this ticket; the T151 path is intact)

The T151 worklog already enumerated dead ends for the rank-byte lever:
runtime ROM poke impossible (mgba can't write ROM), post-load
0x02001b9c poke dead (the OPT adoption is consumed during state build).
Both untouched by this ticket; the rank byte comes from the recipe's
frame-59 OPT lever + rec226 0x080afc90.

## Would try next

1. Execute steps 1-8 above as a single commit, starting from a clean
   `wt/T151a` re-created from c977210e. The plan is fully assembled and
   every constant is named with provenance.
2. If mettaur_rank1 still doesn't reach 0/0/N, the residual is in the
   palette swap (v1 has its own palette per the SpritePointersList
   row, not a palette offset within mettaur.bin). A `--dump
   0x05000040:32` from the canon state at frame 80 will show the OBJ
   palette index canon uses; matching that on the rust side is the
   next lever.
3. Rank 2/v3 are the next-ticket pair: enemy_idx 4 (Mettaur v3, HP
   0xa0) and the corresponding `compVirusBattleSprite_82455B0.lz77`
   (SpritePointersList.s:33, ROM 0x082455B0). family-A rec97
   (ids 0x04/0x02) is the recipe template.
