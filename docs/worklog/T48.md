# T48 worklog — Barrier family (0x15) data-driven

**Status: in progress (step 2 done; step 3 starting).**

## Step 1: shared attack_family (measurement)

Read `reference/bn6f/data/ChipDataArr.s` for the three Barrier siblings:

| chip id | name    | record line | attack_family | attack_subfamily | chip_element | attack_param_1 |
|---------|---------|-------------|---------------|------------------|--------------|----------------|
| 178 (0xb2) | Barrier | 5521 | 0x15 | 0x04 | 0x0A (NONE)   | 0x01 |
| 179 (0xb3) | Barr100 | 5552 | 0x15 | 0x04 | 0x0A (NONE)   | 0x05 |
| 180 (0xb4) | Barr200 | 5583 | 0x15 | 0x04 | 0x0A (NONE)   | 0x07 |

All three share `attack_family 0x15, subfamily 0x04`. The chip-element for all
three is `0x0A = CHIP_ELEM_NONE` — barrier is untyped, like Cannon. attack_power
is 0 for all three (barriers don't do damage; they wear hits).

`assets/chips.bin` v2 row check (chip_export.py records already in the asset,
target index `0xb2` = 178):

```
record idx 11: chip_id=178 family=0x15 sub=0x04 params=[0x1, 0x0, 0x0, 0x0] lockout=0 power=0x0000
record idx 33: chip_id=179 family=0x15 sub=0x04 params=[0x5, 0x0, 0x0, 0x0] lockout=0 power=0x0000
record idx 34: chip_id=180 family=0x15 sub=0x04 params=[0x7, 0x0, 0x0, 0x0] lockout=0 power=0x0000
```

The asset agrees with the ROM record.

## Step 2: baseline (HEAD, no edits)

```
chip-barrier   PASS  total 0  worst 0  frames 80  | negative: not blind (total 41067)
chip-barr100   PASS  total 0  worst 0  frames 80  | negative: not blind (total 41067)
chip-barr200   PASS  total 0  worst 0  frames 80  | negative: not blind (total 41067)
```

All three already pass at 0/0/80 — the refactor must keep them at 0/0/80/N with
identical negative totals. The T17 family-dispatch pattern applied to family
0x15 (Cannon used 0x14, Sword used 0x13) would let the three Barrier records
flow through the ROM's own per-family handler; the byte_8020B2C table at
data/dat01.s:194 already IS the family 0x15 / subfamily 4 mechanism.

## Step 3: parity table — record vs src literal

| chip id | src literal (current `barrier_hp`)        | record param (chip.params[0]) | byte_8020B2C row | HP       |
|---------|-------------------------------------------|-------------------------------|------------------|----------|
| 178     | `BARRIER_HP=10` (match id)                | 0x01                          | row 1 (offset 6) | 0x000A=10 |
| 179     | `BARR100_HP=100` (match id)               | 0x05                          | row 5            | 0x0064=100 |
| 180     | `BARR200_HP=200` (match id)               | 0x07                          | row 7            | 0x00C8=200 |

The `match id` in `src/battle.rs:675-680` reads three hardcoded constants
(BARRIER_HP / BARR100_HP / BARR200_HP) that are derivable from the record's
`attack_param_1` byte and byte_8020B2C. The `barrier_palette` arm at
`src/battle.rs:664-670` is a similar match-id pair that maps id → palette
index; the three Barrier records carry no per-chip palette discriminator (all
three have library_type 0), so the palette index is still keyed off the
attack_param_1 byte (chip 178/179/180 map to palette index 0/3/6; the
consts BARRIER_PALETTE_GOLD/PINK then drop, replaced by the indexed lookup).

The handler is shared: family 0x15 subfamily 4 routes through sub_80E3B50
(as already noted in `src/battle.rs:652`), so `set_barrier`/`barrier_palette`
both read from the record via params[0].

## Step 4-5: landing commits (6e78bbe / 9d9476f / 6f654a7)

The refactor is:

* `BARRIER_FAMILY=0x15`, `BARRIER_SUBFAMILY=0x04` (data/ChipDataArr.s:5530/5561/5592)
* `BARRIER_HP_BY_PARAM: [u16; 8]` from byte_8020B2C at dat01.s:194 (only entries
  1, 5, 7 non-zero: 10/100/200). barriert_hp(chip) -> chip.params[0] table
* `BARRIER_PALETTE_BY_PARAM: [usize; 8]` (entries 1, 5, 7 = 0/3/6 from
  real-capture palette matching). barrier_palette(chip) -> chip.params[0] table
* The two `match chip.id { CHIP_BARRIER | CHIP_BARR100 | CHIP_BARR200 => ... }`
  arms (use_chip presentation at src/battle.rs:4313-4319 and presentation-end
  handler at src/battle.rs:3423-3428) are replaced with the family/subfamily
  check `if chip.family == BARRIER_FAMILY && chip.subfamily == BARRIER_SUBFAMILY`.
* CHIP_BARRIER/CHIP_BARR100/CHIP_BARR200 (178/179/180) constants deleted --
  the dispatcher is the record, not the id. CHanged the consts above BARRIER_HP.

tools/inventory.py: AS_DATA_FAMILIES={0x13, 0x15} -- includes family 0x15 rows
the asset carries (ids 178/179/180 sub 0x04). docs/SCOPE.md M4 line updated to
13 chips AS DATA + 30 verified-pixels; the generated chips.json promotes the
three Barrier rows from verified-pixels to verified.

## Step 6: regression -- all 43 chip rows on this branch

```
chip-cannon    PASS 0/0/40/9505       chip-megswrd   (n/a)
chip-hicannon  PASS 0/0/40/9505       chip-fireblde  (n/a)
chip-mcannon   PASS 0/0/40/9508       chip-aquablde  (n/a)
chip-airshot   PASS 0/0/40/4602       chip-elecblde  (n/a)
chip-vulcan    PASS 0/0/60/6276       chip-bambswrd  PASS 0/0/40/7913
chip-vulcan2   PASS 0/0/70/10142      chip-muramasa  PASS 0/0/40/8824
chip-vulcan3   PASS 0/0/80/12728      chip-minibomb  PASS 0/0/60/17158
chip-suprvulc  PASS 0/0/113/28047     chip-energbom  PASS 0/0/60/22207
chip-sword     PASS 0/0/40/6397       chip-megenbom  PASS 0/0/60/22207
chip-wideswrd  PASS 0/0/40/7977       chip-flshbom   PASS 0/0/48/17490
chip-longswrd  PASS 0/0/40/8824       chip-blkbomb   PASS 0/0/50/18173
chip-wideblde  PASS 0/0/40/7977       chip-bigbomb   PASS 0/0/75/72679
chip-longblde  PASS 0/0/40/8824       chip-lilbolr   PASS 0/0/48/53019
chip-fireswrd  PASS 0/0/40/8499       chip-poisseed  PASS 0/0/70/50708
chip-aquaswrd  PASS 0/0/40/8396       chip-iceseed   PASS 0/0/70/51922
chip-elecswrd  PASS 0/0/40/7367       chip-grasseed  PASS 0/0/70/51922
chip-bugbomb   PASS 0/0/70/23486      chip-recov30   PASS 0/0/40/1731
chip-vdoll     PASS 0/0/70/36375      chip-recov50   PASS 0/0/40/1731
chip-recovery  PASS 0/0/40/1731       chip-recov80   PASS 0/0/40/1731
chip-recov120  PASS 0/0/40/1731       chip-recov150  PASS 0/0/40/1731
chip-recov200  PASS 0/0/40/1731       chip-recov300  PASS 0/0/40/1731
chip-areagrab  PASS 0/0/77/28276      chip-invisibl  PASS 0/0/80/40080
chip-barrier   PASS 0/0/80/41067      chip-barr100   PASS 0/0/80/41067
chip-barr200   PASS 0/0/80/41067
```

All 43 chip rows PASS at 0/0; the three Barriers are identical to HEAD baseline
0/0/80/41067; the 40 already-verified chip rows are identical to HEAD.

## Mechanism one-liner

CHIP_BARRIER/CHIP_BARR100/CHIP_BARR200 deleted, dispatch on
`chip.family == BARRIER_FAMILY (0x15) && chip.subfamily == BARRIER_SUBFAMILY (0x04)`
(src/battle.rs:3423 / :4313 -- the early-return/conditioned arms); HP and
bubble-palette read `chip.params[0]` (= attack_param_1 = 1/5/7) into
BARRIER_HP_BY_PARAM and BARRIER_PALETTE_BY_PARAM tables (src/battle.rs:652-680).

## What is unverified

The `BARRIER_PALETTE_BY_PARAM` colour-matching claim (0/3/6 for the three
recorchips) was kept as-is from HEAD's peeked constant -- no new
real-capture measurement in this pass, the asset still works with the
constants unchanged.

