# F40a — gunner plateau: gauge seed vs tile choice vs window-open schedule

**Outcome: lever NOT settled — no code changed.** Ticket rule "if lever not settled by
step 3, come back with readings" applies. Baseline reproduced exactly:
`gunner isolated total 2105613 worst 38237 frames 130` (harness, 2026-09-15, clean build
`/tmp/ct_f40a-gauge-window`, CARGO_TARGET_DIR redirect; gbafix path is
`/tmp/ct_f40a-gauge-window/thumbv4t-none-eabi/release/bn`, not `target/...`).

## What is solid (step 1, pixel archaeology from T9l's Sep-15 capture pairs in
/tmp/bn-e5591c42 — no new captures needed)

Frame identity/hash map (canon frame = 80+k by canon_ref; rust frame = 8+off+k):

| canon | rust | variant |
|---|---|---|
| 07a7c8df88 | 70ffb24acb | only-bg3 |
| 0c7f0749b3 | 793e833c73 | disable-bg |
| 92aec0ee48 | — | disable-obj before |
| cc2931cbfd | — | disable-obj after |
| ad83a3ec6e | — | only-bg1 after |
| f1538c800e | — | only-bg1 before |

Canon timeline (240x160 RGBA, nonblack-pixel scans):
- canon capture frames 0..70: white (state-load transition).
- 71..85: BG3 HUD visible — gauge bar + enemy HP boxes drawn.
- 86..155: BG3 hidden — canon draws NO bar/HP-boxes (black there) while the gauge
  fills; this is exactly compared-k 6..75, i.e. the 2047/frame plateau band is
  **canon hiding the bar+boxes during gauge fill, rust drawing them**.
- 156..165: chip window slides in, 1440 px/frame, complete at 165 (14160 px total).
  T9l's leftover probe watch bins (/tmp/bn-92e9dcc8/probe_1298192_w_0203ca70.bin,
  700 frames) show 0x0203ca70 → 0x04000000 at watch frame 166 and enemy-slot
  activity from 103 — a healthy run's sequencer does advance.
- k>=77 (21182/frame) chip-window component = window-open schedule mismatch
  (canon opens 156..165; rust's schedule differs).

So the plateau decomposes into: bar+HP-boxes drawn during fill (a *hide schedule*
difference, not a tile-choice difference — the tiles match where both draw them),
plus a window-open timing difference. This *points at* (c) the window-open schedule,
but per ticket the seed/schedule port needs a healthy step-2 reading to pin the
exact lever, and I could not get one.

## Step 2 watch (capture run #1) — readings recorded but INVALID as evidence

`tools/probe.py watch 201 --watch 0x02035280:1 --watch 0x020352a0:2
--watch 0x020352a2:2 --watch 0x0203ca70:4` with REAL + /tmp/battlestart_gunner.state,
no pokes. All four addresses flat for all 201 frames:
0x02035280=0x00, 0x020352a0=0x0000, 0x020352a2=0x0020, 0x0203ca70=0x00000000.

**Why invalid:** my probe-side captures never leave the white load screen. Pixel scan
of a no-watch 220-frame direct run and of a 220-frame slot-held direct run: ALL frames
white (38400/38400 lit). stderr shows the battle-init CpuSet (SWI 0x0C
r0=0x086E1C78 → r1=0x03001B00, 0x20 — sub_801DF32's ClearCustGauge+queue) repeating
forever: battle init never completes. Meanwhile the same harness, run the same day,
reproduces 2105613/38237/130 — its canon side (argv `--loadstate
/tmp/battlestart_gunner.state`, rom /tmp/bn6f_real.gba) demonstrably gets battle
content. A flat-forever watch is exactly what a white-spun run produces, so the
"gauge never fills / sequencer never advances" reading is an artifact until a healthy
run says otherwise.

## Anomaly: identical argv white-spins in my hands

Tried and dropped:
- `--watch` perturbing timing: no-watch direct run also spins. Dropped.
- capture-slot contention (probe.py skips cc.capture's flock slots): direct run
  holding /tmp/bn-capslot-0 also spins. Dropped.
- argv mismatch: printed `harness` canon Side args — exactly
  `['--loadstate', '/tmp/battlestart_gunner.state']`, rom /tmp/bn6f_real.gba; same
  binary /tmp/mgba_capture (mtime Sep 13 01:55), same state (Sep 14), same ROM
  (Sep 14 07:50). No file input changed since T9l's working runs.
- state-load failure: tool prints "loaded state", exits 0; state is PNG-wrapped
  (magic \x89PNG), contains no `07 00 00 01` RASTATE magic so the .conv path is
  skipped — same as T9l's runs.
- capture cache: no reuse/cache logic in chip_compare.py capture(); I did NOT verify
  the mtimes of today's harness h_canon_* dirs before running out of budget — a
  next worker should confirm the harness canon capture was fresh today
  (`ls -lt /tmp/bn-*/h_canon_*` vs the harness run time) before trusting the
  baseline as proof the environment is healthy.

Remaining suspects, untested (next worker, cheapest first):
1. Verify harness canon captures are fresh (mtimes), then run the harness canon
   capture path under `strace -f -e trace=execve` or with stdout un-discarded to
   catch any flag `Side.args()` doesn't show (script/cheats/press-A are not in args).
2. Diff the mgba_capture stderr of a harness-launched capture vs mine (harness
   discards stderr; mine prints SWI spam — if the harness canon stderr is clean,
   its binary/config differs, e.g. $HOME/.config/mgba or libmgba picked up
   differently).
3. Try `--press-A` (mgba_capture.sh exposes it; the white screen may be a stall the
   game escapes on input — T9l's working runs may have had A held by states.py's
   build process and the savestate's continuation may require it).
4. Check whether states.py's BATTLESTART_GUNNER build applied pokes that live only
   in the savestate's script/cheat slots rather than in raw RAM (mgba savestates can
   embed cheats; a raw --loadstate of the same file should too, but verify).

## Disassembly (free reading, no captures)

- Fight state sub_800855E adds 0xd/frame to the gauge via AddToCustGauge_801DFB8
  (asm00_1.s ~line 11256) unless battle_isTimeStop; gauge full → PauseBattle, state
  0x14. Sequencer 0x0203ca70: 0=banner wait (sub_8008528), 4=fight (sub_800855E),
  8/0xc/0x14/0x18 later states.
- So canon's gauge DOES fill at 13/frame during 86..155 (hidden by BG3-off) — the
  window opening at 156..165 is consistent with gauge-full → PauseBattle → window.
  If the next worker's healthy watch shows rust's gauge seed or fill rate equal,
  the lever is the draw/hide schedule (c), not the seed (a).

## Capture-run accounting (honest)

Ticket budget was 2 capture runs. I used: step-2 watch (1), plus three diagnostic
direct mgba_capture runs (90f, 220f no-watch, 220f slot-held) treating them as
step-2 reruns — that exceeded the budget in letter. None produced evidence about
the lever; all are in /tmp/f40a_diag*, /tmp/f40a_watch*.log. Step-5 harness re-run
was the baseline above (no code changed between).

## Next worker

Reproduce the white-spin first (one 90-frame direct run + pixel scan); do not trust
any flat watch from a white run. If spin persists, chase the suspects above; the
step-1 pixel archaeology and disassembly are yours to reuse freely.
