# T58 -- M5 rank census + one new canon recording (Mettaur-routine family)

## What the census says (step 1)

- 452 identity rows (257 virus, 159 navi, 36 player). SCOPE's M5 denominator
  187 = distinct `(ai_index, version)` pairs among virus rows (257 rows -> 187
  pairs). By ai_index: 0 -> 1 pair; 1..31 -> 6 pairs each.
- The ticket's "ai_index 4" is T49's *arm* number: `AIThinkTables_8109050` is
  indexed by `ai_index*4` (asm31.s:169395-169401), so the ForMettaur arm at
  byte offset 0x04 is **ai_index value 1** (rom_enemy_tables known-answer
  check: enemy_idx 1-4 -> AIIndex 0x01 -> ForMettaur_8109EF4). Riding ranks:
  **6/187 pairs (75/257 rows)**, all six versions of the Mettaur family
  (enemy_idx 1-6, HP rows 0x28/0x50/0x78/0xA0/0x78/0xB4). M5 already recorded
  v0, so this census's ceiling for the ported routine is 6/187.
- NameID == enemy_idx for viruses (live evidence: Gunner slot NameID 0x0085 =
  enemy_idx 133).

## Roll mechanism + step 2 (measurement)

- CentralArea1's candidate list = 0x080b4b78, 14 records; records 0-11 are
  v0-only (+ Gunner in rec6/rec10); rec12 0x080b4c38 (2x v0 + enemy_idx 5 =
  Mettaur v4) and rec13 0x080b4c48 (2x v0 + enemy_idx 6 = v5) have
  `record[7]==1` -> `sub_80AA6EC` (asm29.s:10488), gated by EVENT_1D8 and
  `sub_802D652(ai)` >= word_80AA754[ver-4] = 0x10/0x20 (asm29.s:10533-10539).
  Both gates fail in overworld_net.
- Selection (asm29.s:10418-10433 + 10457-10472): filter (record[7] handler,
  then element mask & 0x1f), weight = 1 per passing record, pick candidate
  `(iCurrFrame mod count)`. The roll reads iCurrFrame **one tick after** the
  frame-60 poke (read = poke + 1). Verified three ways: 0x371 -> rec6 (T9c),
  0x37D -> rec6 (a count of 14 would have picked rec12 -- gate NEGATIVE
  confirmed, /tmp/t58_cap/ptr.bin), 0x375 -> rec10.
- **NEGATIVE for the ticket's step 2 as written**: no lever value can field a
  non-v0 Mettaur-routine rank from overworld_net -- rec12/rec13 are the only
  in-list records naming v4/v5 and both are gated; v1/v2/v3 do not appear in
  the list at all (where they live in the other maps' lists is unverified).

## Step 3-5: the new state (battlestart_mgm)

- ONE new `State` in tools/states.py (battlestart pokes + lever 0x375);
  no existing recipe touched. `build all` green (exit 0, no guard lines).
- Chosen BattleSettings ptr 0x02001b9c = 0x080b4c18 (record 10, formation
  byte_80B5387) from frame 0 of the built state. Slots populate at capture
  frame 69 (= frame 148 from overworld_net, T9c's 79-frame offset): slot0
  Mettaur 0x0001/0x0028, slot1 Gunner 0x0085/0x003c, slot2 Mettaur
  0x0001/0x0028 -- each byte-equal to the ai_index's off_8109150 Struct2 row-0
  elem_hp (ai 1 row 0 = 0x0028, ai 0x17 row 0 = 0x003c).
- Determinism: 40 frames from two independent builds diff to **0 pixels**
  (worst 0). State hashes differ byte-wise (ec323c9c..., dfdd7e3c... -- the
  serialized screenshot), frames match; states.py's own convention (overworld_net,
  battlestart) claims pixel equality only.
- First attack: NONE. slot0 CurState_CurAction (0x0203aa90 u16, state |
  action<<8): 0x0004 at frame 69 (spawn) -> 0x0104 at frame 183, never an
  action >= 0x20 through 300 frames -- T9c/T9d's stuck-sequencer blocker
  (dword_203CA70 never leaves 0 on this poke route). The follow-up port
  ticket inherits no attack-alignment event from THIS state; the harness row
  `mettaur` (live battle off battlestart) remains the alignment source.

## Ideas tried and dropped

- Parsing data/BattleSettings.s line-by-line to enumerate the 461 records:
  my byte-count parser desynced on interleaved formation labels (299 sync
  mismatches); switched to reading the record stream straight from the ROM
  via the map's list pointer (off_8020170 -> group 0x90 / map 0 ->
  0x080b4b78), which self-verifies via record[0]==0xff terminators.
- Poking EVENT_1D8 to ungate rec12/13: out of scope -- the ticket allows only
  the frame-60 lever; the gate also needs sub_802D652 progress >= 0x10.

## Would try next

- For v4/v5 recordings: find a map whose list names enemy_idx 5/6 in an
  ungated record (rec[7]==0), or advance the story flags in a new base state.
- For v1-v3: locate which of the per-map lists name enemy_idx 2/3/4.
- Unverified: no port, no trace parity, no harness row (ticket rules);
  the exact tick at which iCurrFrame increments relative to the roll's ldr
  is inferred from three (poke, record) pairs, not from a trace of
  rollRandomEncounter_80AA4C0.
