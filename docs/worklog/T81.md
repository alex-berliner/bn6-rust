# T81 — confused-state AI dispatch port (M2 milestone)

Branch `wt/t81` on main `1ed75a4` (TODO T80 PARTIAL tip). Verdict up front
TBD pending measurement. This file is updated per landed step.

## Step 1 — baseline (measurement, no src change)

Guard set baseline (this tree, main 1ed75a4):
- wave 0/0/90
- window 0/0/16
- opening 0/0/40
- chip-cannon 0/0/40
- mettaur 0/0/70
- windowclose 0/0/40
- popup 0/0/?? (run)
- cursor FAILED 1/1/170/186279 (pre-existing tear; not mine)
- areagrab 0/0/?? (run)
- invisibl 0/0/?? (run)
- barrier 0/0/?? (run)
- barr100 0/0/?? (run)
- barr200 0/0/?? (run)

ROM sha256: <measured>.

## T72 PARTIAL claim audit

Ticket T81 says T72 PARTIAL landed:
- confused status enum
- `Style::Confused` variant
- `set_confused()` hook
- per-AIIndex confused-stub at src/ai.rs:143-160

Git log shows T72 PARTIAL was worklog-only (commit `462ccda`,
"Worklog only, no source edit"). The scaffolding does NOT exist in the
tree. I will need to add it here, since T81 cites T72 as the source for
those names.

## Step 2 — code change (pending)

Decision rule from canon (`off_810AA84..off_810AB38`): the per-AIIndex
confused routine for navis dispatches on `oAIState_DecideState` to one
of three arms (sub_810AAD0 / sub_810AAE6 / sub_810AB40). The first arm
calls sub_810AC9A (the confused-tick decision: 4-tick "be confused,
store a coin to latch" loop and a 5th-tick "act normally" exit). For
viruses the gate is the `=0xa000` BLIND|CONFUSED check at asm31.s:171395
that already arms METTAUR_WANDER via MettaurEntry's existing arm.

## Step 3 — harness row (pending)

ONE new row `confused` in tools/harness.py. Boots a state where the
enemy is pre-confused (poke the status byte 0x0203867c at frame 40 plus
0xffff timer, same recipe as T72 §2 measured). Compare against canon's
the same image.

## Ideas tried and dropped
(filled in as the work goes)

## What I would try next
(filled in as the work goes)