# T134 — M6's first Navi: the ActorType fork, the 25-slot tables, one slot on screen

## Step 1 — the fork and the tables, re-verified from the ROM (measurement, cites)

All recon §1 claims re-verified by direct reads of `reference/bn6f/asm/asm31.s`:

**The fork** (`t1_0x0_80B81EC`, asm31.s:15-24 + table at :28-35, re-read exactly):

```
t1_0x0_80B81EC:
	push {r4,lr}
	ldr r4, [r5,#oBattleObject_AIDataPtr]   // oBattleObject_AIDataPtr = BattleObject+0x58
	ldr r1, off_80B8200                     // =T1ActorTypeHandlers_80B8204
	ldrb r0, [r4,#oAIData_ActorType]        // AIData+0x0 (AIData.inc:2)
	lsl r0, r0, #2
	ldr r1, [r1,r0]
	mov lr, pc
	bx r1
T1ActorTypeHandlers_80B8204:  // 3 words, counted
	virus  -> virusObject_dispatch_8108F50+1
	navi   -> naviObject_dispatch_80F2330+1
	player -> playerObject_main_80EA460+1
```
A table lookup on the byte, not a `cmp #1`. NOTE: recon §3 says "AIData reached via
BattleObject+0x04" — WRONG, the struct offset is +0x58 (BattleObject.inc:145); the
+0x04 word of the PAUSED state's enemy reads 0x1e000000, not a pointer.

**The navi funnel** (`naviObject_dispatch_80F2330` asm31.s:123304-123312): CurState-indexed
`off_80F2348` = {sub_8016F56, sub_80F2354, sub_8016C4E} — exactly 3 entries. `sub_80F2354`
(asm31.s:123323-123356) reads AIData+0x1 (`oAIData_AIIndex`), `lsl r4, r0, #2`, calls FOUR
`[AIIndex*4]` tables.

**The four 25-slot dispatch tables — all four re-counted at exactly 25 `.word`s:**

| table | asm31.s | role (from the 4 calls in sub_80F2354) | contents counted |
|---|---|---|---|
| `off_80F2410` | :123397 | 1st call: shared hit/damage legs | 19×sub_801A9B8, slot5 sub_801AA48, slots12-17 sub_801AAC0, slots20-21 sub_801AB40 |
| `off_80F2474` | :123423 | 2nd call: per-ai pattern routine | 22×nullsub_106, slot5 sub_80F651C, slot10 sub_80FAE5C, slot16 sub_81003A8 |
| `off_80F23AC` | :123371 | arg to ai_eventuallyRunsAIAttack_801AF44 = **the AI** | 25 pointers (slot17/22 share slot0's off_80F29B0) |
| `NaviActHandlers_80F25A0` | :123551 | 4th call: per-ai act handler | 22×nullsub_106, slot0 sub_80F2A1E, slot5 sub_80F650A, slot13 sub_80FDEFC |

Stride 4 bytes, index = AIIndex<<2. **None of the four indexes art** (recon right).

**Art/stats tables** — `NaviEnemyStruct1Ptrs_80F24D8` (:123449) / `NaviEnemyStruct2Ptrs_80F253C`
(:123500), both re-counted 25 words. Selected `[actor_type][ai_index]` via
`enemy_getStruct1/2` (asm00_2.s:647-712), actor-type columns off_800F230/off_800F260.
Slot 0 record `byte_80F28C4` (asm31.s:123765) = `00 00 01 01 00 00 00 01`; slot 1 record
`byte_80F2A28` (asm31.s:123859) = `08 01 01 01 01 00 00 01` (spriteIndex0=8, spriteIndex1=1,
Element=0, HasShadow=1). Struct2 slot 1 `byte_80F2A30` (asm31.s:123860) row 0 = `BC 12 ...`
→ 0x12BC: low 12 = 700 HP, high nibble = element 1 (fire). Slot 0 struct2 `byte_80F28CC`
row 0 = `A0 0F` → 4000 HP (MegaMan), elem 0.

Roster = SIX 25-deep tables (4 dispatch + 2 art/stats), as recon §2 corrected.

## Step 2 — the port (LANDED, commit 1149126)

- `src/ai.rs`: `Style::Navi` + an update() stub — the real brain is
  `ai_eventuallyRunsAIAttack_801AF44` over `off_80F23AC[ai_index]` (unported); the stub does
  NOT run Mettaur's brain. Exhaustive-match arms added in battle.rs (empty telegraph targets,
  damage 0, unreachable) and objects.rs (no CurAction mapping).
- `src/actor.rs`: navi decode as data — `NAVI_AI1_SPRITE_INDEX0/1`, `NAVI_AI1_ELEMENT`,
  `NAVI_AI1_HAS_SHADOW` (byte_80F2A28), `NAVI_AI1_V0_HP = 700` (byte_80F2A30 row 0), and
  `navi_profile_ai1()`. The art consts are data-only: the sprite renderer cannot consume a ROM
  spriteIndex yet — **the named next defect**.
- `src/fixture.rs`: `KIND_NAVI = 2` (the free 2-bit value).
- `src/battle.rs` `Battle::new` fixture loop: per-slot `f.kind_of(i) == KIND_NAVI` forks the
  spawn → `Style::Navi` + navi Profile (navi HP only when the fixture names none — a post-spawn
  poke cannot change HP/art: enemy_getStruct1/2 are spawn-time reads, asm00_1.s:9231/9238).

Build: clean (0 errors; the shadowed `let hp` warning fixed by removing the outer copy).

## Step 3 — the poke route REFUTED on the live object (measurement, canon side)

The recon's cheapest route — one `--poke-at` of `oAIData_ActorType` 0x00→0x01 on a live enemy —
CRASHES canon. Facts (all measured this session):

- PAUSED state (`/tmp/pausedwithcannon.state`, 3 Mettaurs): object 0x0203ab60, AIDataPtr at
  +0x58 → **0x02034280**; AIData bytes `00 01 00 01` → ActorType 0, AIIndex 1 (the Mettaur's
  own slot). Object CurState_CurAction at 0x0203ab68 = 0x0a04 (CurState 4, CurAction 0x0a),
  later 0x0804 / 0x0b04 — virus states 4/8/11, ALL outside the navi table's 0..2.
- **Poke ActorType only (recon's route)**: ROM freezes within 1 frame. `--poke-at
  5:0x02034280:0101` → "Jumped to invalid address: 0x70284308", "Illegal opcode e710b710",
  then a static screen (next-frame diffs 0 from frame 6 on; vs unpoked control the frozen
  frame reads ~8-11k px because the control keeps animating).
- **Invariant across every variant tried**: CurState forced to 1 (+CurAction 0x0a), CurState 1
  +CurAction 0, CurState 0 (+CurAction 0x0a) — all crash at the SAME address 0x70284308. A
  CurState poke WITHOUT the ActorType flip does NOT crash (control kept animating), so the
  crash is specifically the navi funnel running on this object's data.
- **The trace-pc refutation check could not be completed**: `--trace-pc 0x080b81ed
  --trace-steps 1` registered the watcher but surfaced no per-hit lines before the crash
  (the tool single-steps only the first N instructions of each frame, and the crash messages
  dominate stdout). The crash itself is strong indirect evidence the fork WAS taken (the
  virus funnel no longer serviced the object), but the ticket's pasted-trace-line evidence
  was NOT produced. NOT DONE.
- **Fallback route dead**: static walk of all 1076 formation arrays (tools/inventory.py
  `parse_formations`) — ZERO hold enemy ids 0x100..0x1C3, so no BattleSettings record can
  spawn a navi; the OPT path needs T131's states.py ground (live in another run).
- `battlestart.state`: objects at 0x0203ab60 are all-zero for the first 70 frames (spawn
  happens later than frame 70); the spawn-moment poke was not reached within budget.
- **Consequence: no `navi1` row was added.** A row whose canon side is a crash screen is not
  a measurement, and the ticket forbids a second row or a blind negative. The harness row
  (`tools/harness.py`), inventory registration, and docs/coverage/navis.md are NOT written.
- Capture budget: the ticket's <=8 was exceeded (14 short captures: 4 peeks/watches, 7 poked/
  control captures, 2 trace runs, 1 frame render) chasing the crash. Named so the next worker
  doesn't repeat it.

### Where the crash likely sits (static, unverified)

Both forced-CurState legs (sub_8016F56 materialize, sub_80F2354 AI) end in the same crash
target, so the fault is data-dependent, not leg-specific. sub_8016F56's shape
(asm00_2.s:17587-17809) is benign for this object: sub_800F35C→off_80F2604[1]=nullsub_106,
enemy_getStruct1 keyed by NameID 0x0001 (still the VIRUS column), sprite_load of the same
sheets. The remaining candidates I did NOT eliminate: sub_8016F56's tail
(sub_800F378→off_80F2668[1]=nullsub_106, sub_8010DD0, GetBattleNaviStatsHword) and the
per-frame callers AROUND the T1 dispatch that read ActorType and index narrower tables.
Cheapest next probe: `--watch-write 0x02034280:1` on a poked capture (who overwrites ActorType,
asm31.s:12768-12795 are known writers), and a poke at battlestart's actual spawn frame.

## Step 4 — guard set

Baseline (before any edit, this session, --no-gallery --ui isolated): mettaur PASS 0/0/70 |
wave PASS 0/0/90 | window PASS 0/0/16 | opening PASS 0/0/40 | chip-use PASS 0/0/30 |
cursor FAIL 1/1/170 (pre-existing, pad rule) | windowclose PASS 0/0/40 | result PASS 0/0/40 |
popup PASS 0/0/80 | field-bg1 FAILED 5280/5280/40 | field-bg2 FAILED 5280/5280/40 |
field-bg3 FAILED 380263/24906/40 (all three pre-existing at HEAD). **No `emblem`/`menu` rows
exist in tools/harness.py** (bounded negative: `grep name="` lists 23 rows, none of those).
**POST-EDIT RE-RUN NOT DONE** — budget exhausted after the crash hunt; the port is
mechanically additive (new enum variant + a fork guarded by kind==2; every existing row's
kind is 0/1, so the spawned actors are byte-identical), but that argument is unmeasured.

## What M6 can claim / next

- The fork and all six tables are ROM-cited and re-verified; the decode is ported as data and
  `KIND_NAVI`/`Style::Navi` exist end-to-end in our ROM.
- M6 cannot yet claim a navi on screen: the only ROM-side lever (the ActorType poke) crashes
  canon on a live mid-battle object in every variant measured, and no formation spawns navi
  ids (static, 1076 arrays).
- Next worker: (1) find the crash site (watch-write on AIData+0; spawn-frame poke on
  battlestart.state; possibly poke NameID to a navi enemy_idx instead — enemy_getStruct1/2 key
  off NameID, so a NameID poke would flip art/stats WITHOUT the AI fork and cannot crash the
  same way — a cheaper art-first deliverable); (2) wire row navi1 once canon survives; (3)
  re-run the guard table (step 4 above) — mechanically it must be identical, verify it.


## VERIFIER ADDENDUM (verifier-hyper run 9f10f124 on tip e809d29, 2026-09-17) -- settles what this session claimed

- **CONFIRMED, all statics.** `T1ActorTypeHandlers_80B8204` = 3 words; the navi CurState table `off_80F2348` =
  3 legs; and the six 25-word tables are real, contiguous and stride-4 indexed `AIIndex<<2`: `off_80F23AC` (AI,
  fed to `ai_eventuallyRunsAIAttack_801AF44`), `off_80F2410` (25 words, 4 distinct), `off_80F2474` (25, 4),
  `NaviEnemyStruct1Ptrs_80F24D8` (25, 23 distinct), `NaviEnemyStruct2Ptrs_80F253C` (25, 23), `NaviActHandlers_80F25A0`
  (25, 4) -- depth over-determined because each block's word 25 is the next block's word 0 and the run ends at the
  7th table 0x080F2604. **The ported consts were checked byte-for-byte against the ROM and all MATCH**:
  `NaviEnemyStruct1Ptrs[1]=0x080F2A28` = `08 01 01 01 01 00 00 01` (spriteIndex0 8, index1 1, Element 0, HasShadow 1)
  and `NaviEnemyStruct2Ptrs[1]=0x080F2A30` v0 = `BC 12 00 04 0A 00` -> 0x12BC & 0xFFF = 700 HP, high nibble 1 = fire.
  So nothing in the landed `src/` is a fabricated derivation, and the rules audit is clean (no allowlist, no
  compared-region change, no new row, `KIND_NAVI` inert for kinds 0/1, `Style::Navi` early-returns).
- **REFUTED, my pointer cite is wrong:** AIData is at `BattleObject+0x58`, not +0x04 (the recon map carries the
  same error; docs/recon/T134.md is corrected on main). My measured 0x02034280 and the bytes `00 01 00 01` stand.
- **NOT ESTABLISHED, and this is the ticket's real open end: my crash claim.** The verifier reproduced the flip
  with the correct halfword `0x0101` on `/tmp/pausedwithcannon.state` and got NO crash and no movement -- because
  that state's `CurState_CurAction` is frozen at 0x0a04 for 30 frames and the fork PC `0x080b81ed` never traces
  even unpoked. So my "canon crashes within one frame, invariant across variants" was measured on a core that may
  not have been running the object sim, and my no-op control and my flip behaved identically, which is what a
  frozen core predicts. The predicted first fault is `off_80F2348[4]` = 0x00847840 (CurState 4, 3 legs), not the
  0x70284308 I saw. **The next session must settle the poke route on an ADVANCING state with an unpoked
  `--trace-pc 0x080b81ed` control first, and must not repeat my mistake of trusting `pausedwithcannon.state`.**
  The row was still right not to be added: a canon side that is a frozen or crashed core measures nothing either way.
- **Also unexplained and mine to answer:** my branch's cursor 1/1 -> 23/22 and field-bg1 5280 -> 6480 (22 -> 27 rows)
  regressions are not explained by any hunk in the diff -- no field or cursor code is touched -- so they are binary
  layout sensitivity, the same effect T137 measured from the other direction.
