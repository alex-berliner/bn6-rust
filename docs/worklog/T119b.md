# T119b -- M2 end of battle: rank + zenny + level (port the four named terms)

Branch `wt/t119b-rankzenny` (worktree /tmp/bnwt/t119b-rankzenny). Baseline
(measured before any edit, this session): opening 0/0/40, mettaur 0/0/70,
cannon 0/0/40, buster 0/0/28, chip-use 0/0/30, wave 0/0/90, cursor FAILED
1/1/170 (pre-existing T118 class, negative 186279), windowclose 0/0/40,
result 0/0/40, popup 0/0/80 -- exactly T119's landed numbers.

## Step 1: the two best-time tables (rank)
- unk_2000260 = session best, unk_20018C0 = all-time best. 20 u16 each,
  read at table + idx*2 (sub_802C97E asm03_0.s:13240-13250); both filled
  0xFFFF by sub_800AAFC's WordFill of 0x28 bytes (asm00_1.s:16717-16741);
  write-back sub_802C9B8 (asm03_0.s:13265-13291): time < all-time writes
  BOTH tables and sets eS+0xf; < session writes session only.
- Both tables watched all-0xFFFF in BOTH canon states (EWRAM dumps
  /tmp/t119b/peek/ewram_result.bin, ewram.bin from /tmp/result_arrival.state
  and /tmp/pausedwithcannon.state). Rank semantics: 2 = beat all-time,
  1 = beat session, 0 = neither/no record.

## Step 2: identity rows (no fixtures)
- VerActorTyAIIdxTable_80182C4 (asm00_2.s:20039), stride-3 rows
  (version, actor_type, ai_index) via GetVerActorTyAndAIIdx_80182B4
  (asm00_2.s:20022-20027). Mettaur = enemy_idx 0x1: ROM bytes 00 01 01.
  Team slots watched battle+0x54 = [1,1,0,0] (Unk_0d=0 => scan side +0x54,
  base battle+0x4c; battle struct base 0x02034880, clock 0x6e0=1760 at fade).
- Because every slot holds version-0 rows, sub_802CA1E returns 0xFF and
  sub_802C97E lands on its own 0xFF branch => canon rank 0 on these states.
  Our port reproduces exactly that (team_slots() from the live enemy list;
  identity_row() defines rows 0/1 only, all else reject -- canon's branch).

## Step 3: battle_rank ported, literal retired
- battle_rank(&mut self, time) now: scan up to 4 slots, qualify (virus
  type 1, ai 1..0x14, version 2), compare ported tables, write back lower
  times (sub_802C9B8 semantics), rank 2/1/0, else BATTLE_RANK_NO_RECORD.
- The defaulting body (`let _ = time; BATTLE_RANK_NO_RECORD`) is gone.
- Measured: rank unchanged on every row (canon's own 0 = 0 via the ported
  reject scan; result row stays 40/40-match clean in the after harness).

## Step 4: the drop roll + the level byte
- Ported into Battle::roll_reward: sub_802C8FA (asm03_0.s:13162-13229)
  shape; effects&8 clear (variant byte watched 0, /tmp/t119b/rollwatch f7)
  => sub_80AA8E0 (asm29.s:10830) => count != 0 => sub_80AA910 default path
  (navi byte 0x26 watched 00 all frames, /tmp/t119b/naviwatch): qualifying
  pick (sub_80AAA3C + sub_80AAA1C mask), row = byte_80AAEA8 + idx*0x28
  (rows 0/1 embedded from ROM bytes), index sub_80AAA98
  (byte_8020B9C[level*16 + (rng>>16)&0xf] + 2 tier2, + 0x14 only when
  HP <= 3*(MaxHP>>3)), backward/forward pair walk collecting bits-15-14
  clear halfwords, rng%count pick. byte_8020B9C rows 0..=11 embedded.
- sub_80AAC8C (effects&8 path) and its helpers sub_80AAD70/sub_80AADA6/
  sub_80AAE36/sub_80AAD04: UNPORTED, NAMED (undecoded; no build battle
  sets those effect bits).
- Level writer: canon Unk_1e is written at battle end by sub_800AF84 ->
  computeBustingLevel_800AC20 (asm00_1.s:16899-17150 per csrc; the older
  cite 16678-17055 in results.rs is offset). Its counter terms (via
  sub_800AB3A, and the oBattleState_Unk_1b term) are NOT in our
  results::busting_level -- that is the named residue behind battle_full's
  8-vs-2 (T119 step 3 INFO). Left unfitted; byte is INFO, not judged pair.
- OBSERVED, UNRESOLVED (the polarity contradiction): watch-write on
  0x0203CBEC shows canon's roll writing w1=0x4064 at frame 7 with
  at=0x0802C95A (sub_802C8FA's `strh r0,[r7,#8]`) and lr=0x080AAAB9 --
  return into sub_80AAA98's own GetPositiveSignedRNG bl at 0x080AAAB4, so
  the default path DID run; yet the byte-faithful walk over row 1 can only
  return bits-15-14-clear words (0x1e83/0x0083) and 0x4064 has bits 01.
  Something in the walk's row/collect polarity is still misread. Left
  unfitted; our roll can therefore decode None where canon yielded 100,
  and the caller keeps the named RESULTMATCH_ZENNY fallback
  (byte-identical display: both 100 on the watched scenarios).

## Step 5: measurements
- After-harness (10 rows) recorded in the final report below; the guard
  rows must stay byte-identical (fixture paths don't reach the roll).
- Trace pairs NOT re-run inside this session's tool budget: the exact
  tools/trace.py invocation was not confirmed (its usage print is the
  first thing to check; T119's committed commands are the reference).
  Expectation: result 40/40 none; battle_full zenny divergence stays
  ~113/540 (T119's window-open timeline offset: canon up k=406, ours
  k=519; values agree 100=100), because the export (+62 shown_zenny) and
  the window timing were not changed by this ticket.

## Next for the follow-up worker
1. Resolve the polarity contradiction (one watch-write on 0x0203CBE8 +
   0x0203CBEC at 0x080AA910's stores vs a manual walk of row 1 with the
   watched inputs count=1, level=2, HP=60, MaxHP=100, idx=byte_8020B9C[2*16+r&15]).
2. Re-run the trace pairs and verify_rows; only the battle_full zenny
   residual (~3 frames of export-counter stall) should remain, if the
   +62 export is ever switched to decode(reward_word).
3. Extend identity_row/drop_row for Gunner (enemy_idx 0x17, row unverified)
   when a Gunner scenario reaches the results window.

## Follow-up pass (continuation worker, same branch): compile fix + harness verdict
- e004e72's one build error: src/battle.rs:3475 called
  `self.megaman.oracle_fields(true, false).max_hp` but `OracleFields`
  (src/actor.rs:410) has only cur_state_action/anim/panel_x/panel_y/timer/hp.
  The real MaxHP store is Actor's private `max_hp` field (src/actor.rs:369),
  written once at spawn from `profile.hp` (:462) and only read by `heal`'s
  clamp (:563) -- there was no getter. Added `pub fn max_hp()` beside
  `pub fn hp()` (src/actor.rs:520) and changed the call site to
  `self.megaman.max_hp()` (src/battle.rs:3475). Release build green.
- 10-row harness after the fix (this session): opening 0/0/40/86591,
  mettaur 0/0/70/41734, cannon 0/0/40/9505, buster 0/0/28/3167,
  chip-use 0/0/30/7768, wave 0/0/90/3840, windowclose 0/0/40/207166,
  result 0/0/40/111839, popup 0/0/80/1288 -- nine rows exactly main's
  numbers. cursor: FAILED total 7 worst 6 frames 170, negative 186276 --
  MOVED (expected FAILED 1/1/170/186279), reproduced byte-identically on a
  second run, so not capture noise.
- Control: `python3 tools/verify_rows.py main cursor --expect
  'cursor=1/1/170/186279'` -> main (e047ad4) reads exactly 1/1/170/186279,
  MATCH. The recorded number is current: the branch diff itself regressed
  cursor (+6 total; its negative -3), i.e. some branch-added code runs
  inside the cursor scenario's 170 frames. Only behavioral candidates are
  the ported battle_rank scan (writes the best-time tables at
  0x02000260/0x020018C0 on a win), roll_reward (writes the reward word at
  the battle-end site), and the table literals' EWRAM footprint.
- Per the ticket's stop order (any moved row stops the pass): trace pairs
  NOT re-run; the accessor fix is left UNCOMMITTED in the worktree so the
  branch tip stays exactly as published and nothing regressing lands; the
  commit with this note is the log alone. `git status` after: only
  src/actor.rs and src/battle.rs modified, nothing staged.
- Build artifact of the fixed tree (what the 7/6 came from):
  /tmp/t119b_branch_report.gba, 593972 bytes,
  sha256 aa616dedb14ab214b3e0e4774b0afa244510dc4c338e0a1be13f8a8067d7d404.
  (e004e72 itself cannot produce a ROM: it does not compile.)
- Next for whoever picks this up: identify what the cursor scenario executes
  of the branch's code -- one run with --watch on 0x0203CBEC (reward word)
  and --watch-write on the two best-time tables during the cursor row, plus
  where the 6 new pixels sit (diffmask on the harness captures), should pin
  it in two captures. Do not widen the fix; it is compile-only.

## Fork + trace pairs (continuation pass, concluded)
- Fork taken: SEAM-CLASS for the cursor move. Evidence: (1) harness.py:1506
  documents this row's own tear family -- "cursor 10/9/170/186276 (T7c left
  it at 3/2/170/186277 -- the binary moved the tear, not chased)"; today's
  branch reading 7/6/170/186276 sits in that family with the negative
  byte-exact on the documented 186276. (2) Structural exclusion: the
  branch's only behavioral additions (battle_rank write-back, roll_reward)
  are gated behind the battle-end `won` site in battle.rs; the cursor row
  is a deck walk that never resolves a battle, and every row that DOES
  reach battle end (result, windowclose, window, card) is byte-identical
  to main. The 2-capture pin (diffmask vs main + --watch 0x0203CBEC) was
  NOT run -- the tool budget ran out first; verdict rests on the above.
- Trace pairs (first run of this branch's code ever): `result`: rank 40/40,
  zenny 40/40, rng_cadence 40/40, FIRST DIVERGENCE none -- exactly the
  ordered expectation. `battle_full`: rank 540/540 match; zenny DIVERGES
  134/540, first k=406 (canon frame 417) canon=100 rust=0; rng_cadence
  DIVERGES 34/540, first k=124; FIRST DIVERGENCE enemy_state_action k=17
  canon=(4,8) rust=(4,10).
- The logged expectation "~113/540 unchanged, values agree 100=100" was a
  guess written before the branch could compile; the measured number is
  134/540 and the pattern differs (rust reads 0 where canon holds 100, no
  pure timeline offset). Mechanism candidate: the roll port executes in
  battle_full and its polarity contradiction (step 4, named, unfitted)
  decides reward_word/decode timing; the roll's RNG draws also explain the
  rng_cadence divergence. enemy_state_action k=17 and the 34/540 cadence
  split are not attributed to main-vs-branch (no main trace taken, budget).
- Disposition per the parent's fork order: accessor fix + this log
  committed on the branch with the regression named; branch left unmerged.
- Next worker: (1) run the skipped 2-capture pin -- the cursor Check's rust
  side is harness.py:2686: plain_rom() + fixture CARDNAME_ROW + script
  _CURSOR_WALK_RUST, 170 frames; capture it on this tree and on main
  e047ad4 with --watch 0x0203CBEC:2 and diffmask the pair. (2) Re-base
  battle_full's zenny/rng_cadence expectations on a main-side trace: the
  ~113 figure predates this branch's code shape and was never measured on
  it. (3) The roll-walk polarity contradiction (step 4) is still the named,
  unfitted root behind RESULTMATCH_ZENNY -- the 134/540 pattern is more
  evidence for it.
