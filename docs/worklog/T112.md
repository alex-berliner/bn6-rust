# T112 — M2 end of battle: rank byte + zenny reward computed, trace target `rank`+`zenny`, first divergence none

## Step 1 — canon's storage (measurement: 2 captures)

Probe capture /tmp/t112/canon_probe (64 frames, loadstate /tmp/result_arrival.state,
watches 0x02035260:0xc results slot, 0x020364C0:0x20 window struct eS, 0x0203F4A4:0xc
live stats) and /tmp/t112/canon_ww (same load, --watch-write on 0x0203F4A4:0xc,
0x020364CE, 0x020364CF).

Found (all cites verified against the watches):

- **Battle-stats record, live**: 0x0203F4A4 — byte +1 = busting level, word +4 =
  clear time, halfword +8 = reward (zenny halfword). Latched at the fade by the
  battle-transcript transfer sub_801FF18 (asm01.s:49-60): --watch-write shows
  0x0203F4A8 0xFFFFFFFF -> 0x6E0 (=1760, the row's own result_frames) at frame 10 and
  0x0203F4A4 -> 0x01010200 (level 2, flags) at frame 11.
- **Results slot**: 0x02035260 (+variant*0xc, oBattleState_Unk_0d) — copied from the
  live record by sub_800B444 (asm00_1.s:17986-17991, watched at frame 12).
- **Window struct eS20364C0** — showResultWindow_802C34E (asm03_0.s:12441-12455)
  copies level -> eS+8, reward halfword -> eS+0x14, time word -> eS+0x1c.
  drawResultLevel_802C6EC draws eS+8 (asm03_0.s:11724/12906);
  resultWindowSlideTick_802BE36 reads time eS+0x1c and RANK eS+0xe
  (asm03_0.s:11719-11724).
- **Rank byte = eS20364C0+0xe (0x020364CE)**, written once at the window's first
  frame by sub_802C97E (asm03_0.s:13232-13257; --watch-write 0x020364CE frame 13,
  old 0 -> new 0): sub_802CA1E (asm03_0.s:13260-13286) scans the opponent actor
  slots for the first whose byte_80182C4[3*enemy_idx] row
  (GetVerActorTyAndAIIdx_80182B4, asm00_2.s:19965-19974) reads version 2, type 1
  (virus), ai_index 1..0x14 -> index = ai_index-1; 0xFF when none. With an index:
  rank 2 = beats all-time best unk_20018C0[idx], 1 = beats only session best
  unk_2000260[idx] (tables maintained by sub_802C9B8, asm03_0.s:13259-13290), else 0.
  Watched values on the result route: rank 0, level 2, reward halfword
  0xFFFF -> 0x4064 (= 0x4000 flag | 100, frame 9-13), time 1760 — all 64 frames.
- **Reward decode** sub_802C54C (asm03_0.s:12782-12812): 0xFFFF = none; bits 15-14
  clear = chip (id = word>>9, count = word & 0x1FF); set = amount & 0x3FFF.
- The reward ROLL writer: sub_802C8FA (asm03_0.s:13163+) calls sub_80AA8E0 /
  sub_80AAC8C (asm29.s:10830, 11354) — the drop tables walked with
  GetPositiveSignedRNG. NOT watch-write-resolved on the live buffer (the observed
  frame-9 write lands via a BIOS-copy path the watchpoint does not see), but the
  only reward-word producers in the disassembly.

## Step 2 — baseline ours (measurement)

Before any src change: rank = literal 0 (src/battle.rs results.show call), zenny =
descriptor +44 (f.result_zenny) on fixture rows, peeked RESULTMATCH_ZENNY = 100 on
the real-play path. Canon watched: rank 0, zenny decoded 100. Divergence: none on
values — the missing piece was provenance, not the numbers, on this route.

## Step 3 — the port (code change)

- `fn battle_rank` — sub_802CA1E + sub_802C97E structure, every branch cited.
  **Missing input stat (named, unported)**: our Actor carries no canon
  enemy_idx/ai_index (FIXTURE.md enemy_kind = 2 bits) and the build keeps no
  best-time tables -> the scan never qualifies -> canon's own 0xFF branch, rank 0
  (BATTLE_RANK_NO_RECORD, derived cite). Fed at the show site unconditionally.
- `fn battle_zenny` — sub_802C54C decode of `reward_word` (init RESULT_REWARD_NONE
  0xFFFF; the roll is unported/named, sub_80AA8E0/sub_80AAC8C cites). Fed at the
  FIXTURE_UNSET site (unwrap_or 0) and the real-play site (unwrap_or
  RESULTMATCH_ZENNY, the named fallback).
- TRC2 v3 -> v4: +60 rank, +61 level, +62 zenny (the dead GFX zeros were never
  read); trace.py gained the `results` watch (eS20364C0:0x20) and the T112 pair.

## Step 4 — trace (measurement: 4 recordings)

- result, 40 frames: `rank match 40/40`, `zenny match 40/40`,
  **FIRST DIVERGENCE: none**.
- battle_full (second ending scenario — the 540-frame scripted battle DOES reach
  the ending on both sides): `rank match 540/540`; `zenny DIVERGES first k=406
  (canon frame 417) canon=100 rust=0; 113/540` — the window-up timing, not a value
  mismatch: canon's real roll lands 100 and our RESULTMATCH_ZENNY fallback is 100
  (both sides' zenny sets are {0, 100}); the 113 frames are the export-counter
  stall drift (6 repeated/21 jumped rows, F33d) mis-pairing the two windows' first
  frames. Byte-equality of the STORED WORDS holds once both windows are up.
- FIRST DIVERGENCE overall on battle_full is enemy_state_action k=17 — the known
  recon-scenario divergence, outside this ticket's pair.

## Step 5 — guard set + cursor (measurement)

- Baseline (pre-change): mettaur/wave/window/opening/chip-cannon/windowclose/popup
  PASS, cursor FAILED 1/1/170/186279 (pre-existing), result PASS 0/0/40.
- The change moved the binary: cursor 10/9/170/186276. F12 pad ladder re-measured:
  64 -> 13/12, 96 -> 10/9, 128 -> 7/6, 144 -> 88/56. Kept 128 (best); cursor
  7/6/170/186276 — the tear did NOT restore to <=1/1 by pad size; not chased
  further per the ticket.
- Final binary: result 0/0/40, guard set byte-identical to baseline (same totals),
  fitted count 16 (derived 435, +2 new derived constants).

## Ideas tried and dropped

- Watching 0x020018C0/0x02000260 (the best-time tables) to decide whether canon's
  rank 0 on the result route is the 0xFF branch or a beaten-by-none comparison —
  dropped: both branches produce 0 and our side cannot evaluate either without the
  ai_index; the capture budget went to --watch-write instead, which pinned the
  writers.
- Full reward-roll port — dropped: the drop-table system (sub_80AA910 family,
  asm29.s) is a whole ticket; the decode + named unported roll is what the ticket's
  rules prescribe.

## Next worker

- The enemy model needs canon's enemy_idx/ai_index (byte_80182C4 rows) before
  battle_rank's record branches can be evaluated; the best-time tables
  (unk_2000260/unk_20018C0, EWRAM session state, sub_802C9B8 writer) come after.
- The reward roll (sub_80AA8E0/sub_80AAC8C + drop tables) replaces
  RESULTMATCH_ZENNY.
- Unverified: our busting level fed the window exports 8 on battle_full where
  canon's eS+8 reads 2 (INFO field, not judged here — the existing
  results::busting_level's numbers were never traced against canon's latched
  level byte).

## Unverified (ticket line)

- The reward ITEMS — chips/PAs granted — are not in this ticket's two words; only
  the rank byte and the zenny halfword are traced.
