# T222 — Mettaur rank-1 art and row, one landing

Branch: wt/t222 (worktree /tmp/bnwt/t222), base = main d4f9acb (TODO T231 tip; d491354 + TODO commit).

## Plan
1. Cherry-pick the T151 scenario/row commit(s) from wt/t151 (c977210e carries row+state+inventory+coverage; 8ff1a02/393070c are worklogs).
   - NOTE: ticket said `git cherry-pick c977210e~5..c977210e` but re-checking `git log wt/t151 -5` shows the 4 commits below c977210e (cd7404a..7e17b94) are TODO.md-only parser/boilerplate commits, not the row+state. Picking those onto current main's TODO.md would conflict / land noise. Picking c977210e (the whole feature commit) and the two T151 worklog commits instead.
2. Re-run baseline `mettaur_rank1` on current main (+picked row) — report drift vs the old 677252/38400/70 neg 642150 (windowclose now red at 63962 per T230 worklog).
3. Port rank byte to art + cooldown: export v1 sheet to assets/mettaur_v1.bin, branch on `f.kind_of(i)` at the kind-0 hardwire in src/battle.rs, version-indexed cooldown from byte_8109F46.
4. Verify: mettaur_rank1 0/0/70 non-blind; canaries mettaur 0/0/70/41734, cursor 1/1/170/186279 byte-identical.

## Log
- worktree created, endpoints re-checked: wt/t151 tip = 393070c; feature commits identified (see plan step 1).

## Step: port landed (pending fix)
- exported assets/mettaur_v1.bin from compVirusBattleSprite_8242E94.lz77 (12402 B, 13 gfx, 1 pal, 11 anims, 31 frames, 169 OAM).
- src/fixture.rs KIND_METTAUR_V1=3; src/actor.rs METTAUR_HOP_COOLDOWN table (asm31.s:171052-3) + hop_cooldown field + set_mettaur_version; src/battle.rs kind branch (V1 art + 0x50 hp + version 1); src/main.rs METTAUR_V1 static; tools/harness.py row enemy_kind 0->0x3F, align note updated.
- BASELINE on current base (post-pick): mettaur_rank1 677252/38400/70 neg 642150 -- IDENTICAL to the old-base log, no drift.
- AFTER PORT: mettaur_rank1 FAILED 728067/38400/70 neg 693002 -- WORSE. worst=38400 means full-frame diff on some frames; art or anim-index mismatch suspected (v1 sheet has 11 anims vs v0's 4; anim indices may shift). Next: dump both sheets' anim tables, diffmask a divergent frame.

## Diagnosis (budget stopped here -- numbers all measured this session)

The port did NOT move the row to zero: 728067/38400/70 neg 693002, WORSE than
the 677252 baseline. Stopped without widening the change (role rule).

Canon OAM watch (mgba_capture --watch 0x07000000:0x400, real ROM +
battlestart_mettaur_rank1.state, 200 frames, /tmp/t222_oam2.bin):
- Group A: 4 parts (65,102 8x16 t29)(41,102 16x16 t25)(49,70 64x64 t9)(42,100 64x32 t1),
  pal 0, STATIC from ~frame 74 to 199, x 41..113 => MegaMan at panel (2,2)
  (col->x mapping: F37d cite puts col-5 enemy at x~163; 40px/panel => col 2 = ~43).
- Groups B/C/D: the THREE rank-1 Mettaurs materialize SEQUENTIALLY ~35 frames
  apart (col4 parts appear ~119, col5 ~154, col6 ~184) -- only col4+col5 are
  inside the row's compared window (canon 69..139). Each shows a STATIC 3-part
  OAM: 8x8 at (126,68 t43), 64x64 at (134,64 t35), 16x16 at (123,80 t31),
  OBJ PALETTE ROW 1 -- identical from appearance to frame 199 (no idle cycle).
- The exported v1 sheet (compVirusBattleSprite_8242E94, assets/mettaur_v1.bin)
  anim 0 is a 4-frame cycle (5-frame durations) with 5-7 parts per frame
  (64x32 body + 32x32/32x64 + 16x16s + 8x8) -- it does NOT reproduce canon's
  static 3-part 8x8/64x64/16x16 layout at pal 1. So either 8242E94 is the
  WRONG sheet for rank 1, or canon renders rank 1 through a different
  anim/OAM path than our spr player applies (e.g. the sheet's other anims;
  note anim 9/10 have 32x64 mirrored pairs, anim 8/10 have 64x64-ish
  compositions).

Next steps for the follow-up worker:
1. Identify the sprite id canon loads for EnemySetup quad id 2 (rank 1):
   spawn path spawnEnemy_80073E2 -> sub_800768C (asm00_1.s:9128) stores
   version/actortype/aiindex into AIData; the sprite loader itself was NOT
   traced (SpritePointerArr811C3A4 asm32.s:28115 indexes by sprite id,
   0x88242E94 at [2], compVirusBattleSpriteMetteur = 0x88241EC4 at [1]).
   Dump OBJ VRAM (0x06010000..) tiles at a settled frame and match against
   candidate sheets' gfx blobs -- that names the sheet decisively.
2. Check OBJ palette row 1 contents (watch 0x05000200:0x20) -- rank 1 renders
   in pal 1, our port left palette handling untouched (export takes pal 0 of
   the sheet; BNSP OamRec flags carry no palette offset here, all 0x0).
3. The static 3-part pose with pal 1 may be the SPAWN/materialize pose held
   through the whole window (spawn stagger ~35 frames/enemy): the compared
   window may simply never show the settled idle. Match THAT pose, not anim 0.
