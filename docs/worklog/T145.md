# T145 — M6 first Navi, second slot: T134 step 3 redo

Worktree note: the first worktree (based 22f8d71) and its uncommitted log were
deleted mid-session by another process; rebuilt on 292dca0 (docs-only delta,
src identical — guard baselines carry over). Commits: worklog-only, then the
port, on wt/t145.

## Result in one line

Slot 0x17 (ForGunner_8113078) ported end-to-end as data + fork wiring; row
`navi-gunner` lands a NAMED reduction: `FAILED 2092176/38237/130 neg 2247584`
— the gunner row's own documented residue line (2105613/38237/130, T9l) with
the HP-box band improved (real per-kind enemy_hp vs the borrowed 0xFFFF).
Mechanism finding that reframes the ticket: **the t1 ActorType fork is
spawn-latched, not per-frame** — a live flip is measured inert.

## Baselines (before any edit)

- `python3 tools/inventory.py`: navis + cybeasts (M6): FOUND: asm/asm31.s
  off_80F24D8/off_80F253C/off_80F25A0 -- verified 25/25.
- mettaur isolated PASS total 0 worst 0 frames 70 | negative not blind (41734).
- cursor isolated FAILED total 1 worst 1 frames 170 | negative not blind
  (186279) — the documented pad rule.
- Post-change verify_rows (clean checkout a0f8049): navi-gunner
  2092176/38237/130/2247584, mettaur 0/0/70/41734, cursor 1/1/170/186279 —
  mettaur/cursor byte-identical to baseline; cursor did not move.

## The ticket's four tables: corrected cites

The ticket says asm/asm00_2.s; the labels exist only in **asm/asm31.s**, and
the tables are **32-word** (bounded by off_8109250), not 25:
AIThinkTables_8109050 :169448, AIEnemyStruct1Ptrs_81090D0 :169513,
AIEnemyStruct2Ptrs_8109150 :169578, AIActHandlers_81091D0 :169643. All four
ported word-for-word into src/navi.rs (derived, ROM-read this session).

## Slot pick and its words

ai_index 0x17 = ForGunner_8113078 (asm31.s:169496) — the only ROM-named think
arm besides T134's 0x01. Identity row byte_80182C4[3*0x185] = (0, navi, 0x17),
ROM-read; T76 roster: first enemy_idx 0x185, not otherwise reached.

- think AIThinkTables_8109050[0x17] = ForGunner_8113078 (asm31.s:169496)
- Struct1 (virus col) AIEnemyStruct1Ptrs[0x17] = GunnerEnemyStruct1_8112B94
  `04 17 01 00 17 00 00 01` (asm31.s:169561)
- Struct2 (virus col) AIEnemyStruct2Ptrs[0x17] = GunnerEnemyStruct2_8112B9C
  row0 `3c 00 00 08 0a 00` → HP 60/elem 0/dmg 10 (asm31.s:169626)
- act AIActHandlers_81091D0[0x17] = nullsub_13+1 (asm31.s:169691)
- navi cols (what a navi spawn reads, off_800F230/off_800F260,
  asm00_2.s:647-720): NaviEnemyStruct1Ptrs_80F24D8[0x17] = byte_81067FC
  `00 0b 01 01 17 00 00 01` (asm31.s:123497); NaviEnemyStruct2Ptrs_80F253C[0x17]
  = byte_8106804 row0 `84 03 00 03 0a 00` → **HP 900** (asm31.s:123548)
- navi family: off_80F23AC[0x17] = off_81068E8 (asm31.s:123395) — AI arm, 15
  entries: [0..7] shared generic CurAction arms, [8..E] own executors
  0x08106925..0x08107751 (UNPORTED — the named follow-up); off_80F2410[0x17]
  = sub_801A9B8+1 (123421); off_80F2474[0x17] = nullsub_106+1 (123447);
  NaviActHandlers_80F25A0[0x17] = nullsub_106+1 (123575); CurState legs
  off_80F2348 = {sub_8016F56, sub_80F2354, sub_8016C4E} (123317-123321).

## Canon route: what was measured (probes, all this session)

1. ROM pokes are no-ops: `--poke 0x080b5351:0x0185` (Gunner id in record 6's
   setup array at 0x080b5347) does not land (probe peek still 0x0085).
2. From battlestart_gunner.state, slot 1 populates at capture **frame 69**:
   NameID 0x0085, AIDataPtr +0x58 = 0x02034280, AIData halfword **0x1700**
   (ActorType 0, AIIndex 0x17 — T12's known-answer measured live), HP 0x003c,
   CurState_CurAction 0x0004.
3. **Fork latch finding**: poke-at 70 {AIData→0x1701, NameID→0x0185} holds
   (watched to frame 110) and changes NOTHING else — no crash, CurState stays
   4. Both dispatchers are UNBOUNDED CurState bx tables
   (virusObject_dispatch_8108F50 asm31.s:169291-169301;
   naviObject_dispatch_80F2330 asm31.s:123304-123312); a per-frame navi arm
   with CurState 4 would read the word at 0x080F2358 (mid-instruction,
   0x00847801 shape — the T134 verifier's predicted fault) and crash. Neither
   happens ⇒ the ActorType fork runs at spawn only. This rehabilitates T134's
   step 3: the live flip was never going to reroute behaviour; whatever crash
   was seen that session was the frozen-core artifact its verifier named.
4. True-spawn routes closed: CurBattleDataPtr (0x02001b9c) repointed to a fake
   record in free EWRAM 0x0203E000 (zeros verified) with setup id 0x0185 —
   the repoint sticks but the spawn still builds the Gunner (spawn reads ids
   from a RAM copy made earlier in init); RASTATE is PNG+compressed (setup
   pattern absent from the raw file, so no state-file patch); watch-write on
   NameID shows the spawn write from an init fill (0x08007762, lr 0x0800F243)
   — hunting the RAM setup array was cut off by budget. **Next worker**: find
   that RAM EnemySetupArr (watch-write on the AIData alloc, or diff EWRAM
   around init), build a states.py recipe that flips its id to 0x185 pre-spawn
   → TRUE navi spawn (art byte_81067FC indices, HP 900) → point the row's
   canon side at that state and swap `profile_ai17().hp` to
   `true_spawn_hp`.

## The port (commit a0f8049)

- src/navi.rs (new): the four ticket tables (32 words each), both struct
  columns, CurState legs, the four navi-family rows, the AI_ARM table, and
  `profile_ai17()` (hp = 60 = the flip-route scenario's measured value;
  true_spawn_hp = 900 kept alongside). Module doc carries the latch finding.
- src/fixture.rs: KIND_NAVI = 2 (the free two-bit value).
- src/ai.rs: Style::Navi; update() arm = Gunner's (same shared think arm).
- src/objects.rs: enemy_think navi arm rides the Gunner arm, with the latch
  cite.
- src/battle.rs: fixture spawn consumes kind_of for KIND_NAVI only (GUNNER
  art + profile HP + Style::Navi; kind 0/1 spawns byte-identical to before —
  the gunner row's rust side untouched); gunner_ctl gate, telegraph targets
  and Strike damage matches extended with Style::Navi (= Gunner's arms).

## Row

`navi-gunner` (tools/harness.py): rust = NAVI_GUNNER_ROW (GUNNER_ROW with
enemy_kind=0x08, enemy_hp=0 → per-kind HP 40/60 matching canon exactly);
canon = REAL+BATTLESTART_GUNNER + poke-at 70 flip. ui both, frames 130,
canon_ref 80 / band 0..40 (gunner row's own, same spawn timing), serial.

- isolated: `FAILED total 2092176 worst 38237 frames 130 | negative: not
  blind (2247584)`.
- The residue is the gunner row's own NAMED residue (T9j/T9l note: fade
  k=0..5, BG3 HP-box/gauge k=6..75, chip window k>=77) minus a slice of the
  HP-box band (real enemy_hp vs 0xFFFF): 2092176 < 2105613.
- verify_rows clean-checkout: navi-gunner 2092176/38237/130/2247584,
  mettaur 0/0/70/41734, cursor 1/1/170/186279. (The tool prints MISMATCH/
  FAIL from my --expect encoding; every number reproduced.)

## Unverified / next

- The trace surface (tools/trace.py diff, first divergent field/frame) was
  NOT run — capture budget went to the canon-route hunt. Expected: with the
  flip inert and both sides running the shared think arm, actor_state pairs
  with the gunner row's documented pairing.
- The navi-family brain (off_81068E8 arms 0x08..0x0E) is unported; the day a
  true navi spawn exists, that is the next port and `profile_ai17` flips to
  true_spawn_hp 900.
- tools/inventory.py untouched (budget): its navi section could cite the
  think/Struct1/Struct2/act 0x8109050-family next to the 0x80F2xxx family —
  the ticket's four tables vs T134's six is exactly that split.
- The --expect encoding of verify_rows (T/W/F/NEG per row) needs a doc look;
  numbers reproduced regardless.
