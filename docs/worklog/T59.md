# T59 — M2: the battle folder becomes a measured field

Ticket: canon's own 30 folder words on the trace, then the un-ported shuffle
rules (reg / tag / giga re-insertion).

## References pulled from the tree (asm cites, verified by reading)

- `eBattleFolder:: // 0x203cdb0` — reference/bn6f/ewram.s:3097, `.space 56`,
  followed by `word_203CDE8` (+0x38) and `byte_203CDEA` (+0x3a). The battle
  folder proper is 30 halfwords = 60 bytes, 0x203cdb0..0x203cdeb: the tag
  words at +0x38/+0x3a are battle-folder slots 28/29, written by
  buildBattleFolder_800A3E4's copy loop (asm00_1.s: loc_800A474..800A4A6,
  loop bound `cmp r2,#0x3c` = 60 bytes / 30 halfwords).
- Writer: `buildBattleFolder_800A3E4` (asm00_1.s:15414) copies the PET navi's
  30-halfword folder (per-navi stride 0x3c bytes, `mov r2,#0x3c; mul r2,r0`)
  into eBattleFolder: reg chip (navi-stats byte 0x2d) to slot 0, tag chips
  (bytes 0x56/0x57 + 2*regIdx... offsets [sp,#4]/[sp,#8]) to slots 28/29,
  the rest packed from slot 0 or 1; then calls
  `shuffleBattleFolder_800A570` (asm00_1.s:15568) with (hasReg =
  BattleState+0x17, hasTag = BattleState+0x44).
- `shuffleBattleFolder_800A570` (asm00_1.s:15568):
  1. splits all 30 entries order-preserving by `ChipData.elemIdx==2` (giga,
     `ldrb r1,[r0,#0x7]; cmp r1,#2`, loc_800A582..800A5A8, loop bound
     `cmp r4,#0x1e`) into ePAScratch_2033000 (non-giga, count r5) and
     word_2033040 (gigas, count r6).
  2. `ShuffleHwordList_SecondaryRNG(scratch, r1, r2)` (asm00_0.s:1349: r2
     swaps of two `GetPositiveSignedRNGSecondary % r1` indices); the caller
     passes r1=r2=r5, minus 1 if hasReg (loc_800A5C4 `// reg?`), minus 2
     more if hasTag (loc_800A5CE `// tag?`) — the last `adj` scratch
     positions are never touched by the shuffle.
  3. giga re-insertion, non-crossover (loc_800A5E8): per giga,
     `GetPositiveSignedRNGSecondary % (r5-0xc)`, insertion index = that +0xa;
     crossover (loc_800A612): `% 0xb`, index +0x8; `sub_800A672`
     (asm00_1.s:15934) inserts by shifting, r5 grows by 1 per giga.
  4. tag re-position (loc_800A664, hasTag): `GetPositiveSignedRNGSecondary %
     0x13`, +1, stored to BattleState+0x45, then swaps scratch[0x38] and
     scratch[0x3a] (the two protected tag slots) into halfword offsets
     2*(r1+1) and 2*(r1+2).
  5. copies 0x3c bytes (30 halfwords) back to eBattleFolder
     (`CopyHalfwords ... mov r2,#0x3c` — the "halfwordCount" comment is
     wrong: buildBattleFolder's per-navi stride proves the arg is bytes).
- Readers: `offerDeckChipsToWindow_8027EE8` (asm03_0.s:3510) hands window
  slots `str r1,[r4,#8]` with r1 = eBattleFolder + 2*i;
  `blankPickedDeckSlots_80293F8` (asm03_0.s:6413) and the pack-to-front at
  :6470 (both via off_8029510 = eBattleFolder, :6518/6565/6603);
  `getChipID_802A54E` (asm03_0.s:8820 area — off_8027F0C = eBattleFolder).
- Secondary RNG: `GetRNGSecondary` (asm00_0.s:2649) — seed at
  `eSecondaryRngSeed:: // 0x2001120` (ewram.s:241), step
  `rotl(seed,1)+1 ^ 0x873ca9e5` — exactly src/deck.rs::Rng.

## Step 1 — baseline (no edits)

- HEAD ROM (worktree == a8ec310 == main's T57 line): `584296` B,
  sha256 `5b46337aa27da9ca881f2321fe1e210f54717285b4dfa5256c1680bac4b985ef`
  — byte-identical to T56's HEAD reference, so the tree is the one the
  coordinator's cursor numbers were measured on.
- nm (fresh build): `BATTLE_MARKER` at 0x02000000, next symbol
  `INTERRUPT_TABLE` at 0x02000100, `SPRITE_LOADER` at 0x020001a8 -> the
  reservation is 0x100 = 256 B. TRC2 v4 grows to 124 B at offset 128:
  128+124 = 252 <= 256, 4 B headroom. Now a compile-time assert
  (`TRACE_REGION_FITS`, src/main.rs).
- `trace.py record canon battle_full --out /tmp/tr_t59_c`: 13 streams (v3
  watch set), 540 frames. verify_rows on the guard set: running (see below).

## Step 2 — the live folder is eBattleFolder 0x0203cdb0 (measurement)

Address from the map: `eBattleFolder:: // 0x203cdb0` (ewram.s:3097); writer
buildBattleFolder_800A3E4/shuffleBattleFolder_800A570 (asm00_1.s:15414/15568,
copy-back `CopyHalfwords ... mov r2,#0x3c` = 60 BYTES = 30 halfwords — the
comment "halfwordCount" is wrong, the per-navi stride `mov r2,#0x3c; mul`
proves the unit); readers offerDeckChipsToWindow_8027EE8 (asm03_0.s:3510
hands window slots `str r1,[r4,#8]` against eBattleFolder + 2*i),
blankPickedDeckSlots_80293F8 / pack-to-front at :6470 (off_8029510 =
eBattleFolder), getChipID's off_8027F0C = eBattleFolder (asm03_0.s:8820
area).

Proof by watch (one 555-frame canon capture of the battle_full recipe with
`--watch 0x203CDB0:0x40` + `--dump 0x2002178:0xC0` (the PET/S_Chip folder)
+ `--dump 0x203CCE0:0xC8` (navi stats) + `--watch 0x2001120:4`
(eSecondaryRngSeed)):

- frame 0 words (settled): `0605 3404 2447 0236 ffff 1636 2447 2448 0201
  169a 0605 2447 3404 0605 169a 34b8 34c0 1636 2448 3459 3459 0201 2447
  009a 24a3 34af 009a 0236 34c0 ffff`
- every live word: low 9 bits < 411 (ids 1..192), bits 9+ are the code
  letter (3=D, 26=*, 18=S, 1=B — the capture's own window line-up).
- the folder is stable from frame 0 to 47, then changes ONCE (frame 48 =
  the recipe's window open ~48): the blank at index 4 is removed and the
  live chips shift left by one, `ffff` appended at 28,29 —
  blankPickedDeckSlots/pack-to-front, exactly sub_802945A's shape. That is
  the second independent read of the same structure at a different frame.
- the fixture's Cannon (id 1, code A = 0x0001): the PET/S_Chip block at
  0x2002178 carries 30 chips incl. TWO `0001`; the battle folder carries
  both as blanks (index 4 = the window pick, index 29 = the reg Cannon
  drawn to the hand at battle start) — the Cannon is present in the source
  folder and accounted for in the battle folder, not as live words in this
  post-pick capture.
- PET folder (pre-shuffle source, 0x2002178): `0001 0001 0201 0201 3404
  3404 0605 0605 0605 0236 0236 1636 1636 2447 2447 2447 2447 2448 2448
  34af 3459 3459 009a 009a 169a 169a 24a3 34c0 34c0 34b8` — the battle
  folder is a permutation of it minus the two blanked `0001`.
- navi stats 0x203CCE0: byte 0x2d (reg idx) = 0, 0x56/0x57 (tag idxs) = 0,
  0 — ambiguous (0 could be "none"); see step 4.
- NO giga in this folder: ChipData.elemIdx (+7 of ChipDataArr_8021DA8,
  44 B stride, read straight off the canon ROM, sha1
  0676ecd4d58a976af3346caebb44b9b6489ad099 = reference/bn6f's own) is 0 for
  every id present (1,4,5,54,71,72,89,154,163,175,184,192). So the giga
  split/re-insertion is a no-op ON THIS SCENARIO.
- eSecondaryRngSeed (0x2001120) advances EVERY frame of the capture (554
  changes/554 frames), first `3d761b9b`, last `a9917cf6` — the save state's
  seed is post-shuffle, so the shuffle-time seed is only reachable by
  inverting the generator (rotl/rottr makes each step invertible) or by a
  full seed scan.

## Step 4 — host-side replay against Deck::new's plain shuffle

Written before any tuning: replay = scratch = pre-shuffle folder,
ShuffleHwordList_SecondaryRNG semantics (r2 swaps of two
GetPositiveSignedRNGSecondary % r1 indices, asm00_0.s:1349), caller's
adjustments (reg -1 asm00_1.s:15822, tag -2 more :15836; the reg branch also
passes scratch+2 bytes, excluding the reg's slot 0 from the shuffled
region), tag step (rand%0x13+1 -> swap slots 28/29 in, :15934 area
loc_800A664). Target: frame-0 folder with sim[4]==1 && sim[29]==1 and the
other 28 words exact.

- Inversion scan (seed = frame-0 seed inverted k steps, k <= 100k) x all
  four (hasreg, hastag) combos x regIdx variants (reg chip at PET[j] for
  every j): best 11/28 scattered matches at k=1358, blanks never right —
  scattered matches at duplicate-heavy words are noise (multiset carries
  4x2447, 3x0605, five pairs; chance level ~1.7).
- FULL 32-bit seed scan (rust, /tmp/t59_scan/scan.rs, 12 shards, no
  regIdx variants — hasreg is ruled out structurally: the reg sits at
  slot 0, gets drawn to the hand at battle start, and the observed slot 0
  is live `0605`): **combo 0 (plain shuffle, deck.rs::Deck::new's own
  rule): EXHAUSTED, 0 hits.** Combo 1 (tag variant) also finished: 0 hits.
  -> NO 32-bit seed reproduces canon's folder from the PET-block input
  under the plain or the tag-adjusted shuffle. Either the pre-shuffle
  input differs from the 0x2002178 block (the block may have been rebuilt
  from the folder at some point pre-save), or an extra draw interleaves
  between the shuffle's stages (e.g. sub_800A7EA), or the shuffle ran on
  a source this save no longer shows. The seed is NOT derivable from this
  capture by my model. That kills steps 4/5's localization-on-this-scenario
  and with it the predicted matching-position increase; see the report.

## Step 3 — folder watch + TRC2 v4 (code change)

tools/trace.py: CANON_WATCHES gains `folder: (0x0203CDB0, 0x40)` (14th
stream, cap 16); TRC2_VERSION 4, TRC2_LEN 128 (watch width; the block is
124 B); parse_canon_frame/parse_trc2_row carry `folder` (30 halfwords).
src/battle.rs: TRACE_SNAPSHOT_LEN 64->124, TRACE_VERSION 3->4,
trace_snapshot writes `self.deck.words()` at 64..124 (Battle already owns
the `deck` field — no struct change); src/deck.rs: `Deck::words()`; src/main.rs:
`write_trace_block(&[u8; 124])` + the `TRACE_REGION_FITS` assert.
New ROM `585288` B (+992), sha256 `d464258a808550e23fc3cbe43d8411146e4efa26ad073887361148cddc79890d`.


## Step 5 — port (NOT landed: NEGATIVE on localization)

Step 4 exhausted the seed space for the two rule groups that apply to this
folder (no gigas: elemIdx 0 for every id present; hasreg ruled out
structurally by the live slot 0): plain shuffle 0/2^32 seeds, tag-adjusted
0/2^32 seeds (full 32-bit scan, 12 shards, /tmp/t59_scan/scan.rs). The seed
is not derivable from this capture under my model, so no rule group is
localized and no port can honestly move the matching-position counts; per
the ticket that closes it as the NEGATIVE branch ("the rule whose count is
not derivable"). Nothing was tuned.

Step 3 numbers (the instrument landed): 14 canon streams (cap 16); TRC2 v4,
124 B, 4 B reservation headroom; first divergent WORD on battle_full: rust
export frame 0, word index 4 — canon 0xffff (the pre-pick blank) vs our
fixture-stacked 0x0001 (Cannon A) — i.e. the fixture deck vs canon folder
disagree from the first compared frame at the already-picked slot.
Matching positions (canon capture 48/81/261 = window-open/confirm/post-fire,
aligned row:battle_full, rust frames 37/70/250): 6/30 at all three — flat,
as step 4 predicts: our deck for this scenario is the fixture stack, and no
port of shuffle rules can move a stacked deck.

deck.rs:14-15 status: the "not reproduced" note stands, now with the
measured reason — the plain shuffle (Deck::new) is seed-exhaustively NOT
what canon ran on this folder, so the gap is not the reg/tag/giga count
adjustments alone; something about the pre-shuffle input (the 0x2002178
block vs what the battle actually read) or an interleaved draw is still
unidentified. What I would try next: watch eSecondaryRngSeed and the PET
block from a FRESH battle (BATTLESTART recipe) where the shuffle happens
inside the capture and every input is on-screen.
