**A decompiled map of the original exists.** `python3 /home/box/Code/bn/tools/csrc.py <symbol or address>` prints Ghidra's decompiled C for a routine of the original game followed by the disassembly's own lines for it. Use it to find where a behaviour lives and what shape it has before reading the assembly. The C is a map, not evidence: cite the assembly's file and line, never the C, and verify every number by measurement as always.
