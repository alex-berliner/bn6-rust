The custom screen cursor row across F26b. The battle behind the chip window had never had its backdrop phase seeded: canon's save state is thousands of frames into a battle, and the backdrop scroll counters count down exactly 8 and 4 per frame from battle init, so the two backdrops were a constant translation apart, and that error had even pulled the row's alignment 11 frames off its event. Deriving each row's seed from canon's counters and animation state at the reference frame, with our engine's two pipeline lags measured once on the window-close row and predicted here, took the row from 620802 to 272341 on the event-locked offset. The backdrop now differs by 2 pixels on a single frame; what remains is on the sprite layer: the emotion window sits 120 pixels to the right while the custom screen is open, and the enemy's phase.

cursor (isolated), before and after: canon on top; ours at c70237b (620802 px over 170 frames, canon 15+k, rust offset 226); ours at f76360e (272378 px, canon 15+k, rust offset 237). Each build on its own commit's alignment, from a clean checkout (tools/progress_gif.py).
