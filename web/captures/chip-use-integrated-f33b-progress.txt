The chip-use row in the integrated variant (whole screen, HUD included) across F33b. The fixture these rows share had never seeded the backdrop's phase, while its canon side is thousands of frames into a battle, so the backdrop layer differed on every frame; deriving each row's seed from canon's scroll counters and animation state at that row's own reference frame, with the same derivation that had already produced the cursor and window-close seeds, took the backdrop and field layers to 0 on every frame. The HUD's custom gauge and hand icon now hide when canon tears those elements down, and the queued chip's name shows as canon keeps showing it. What remains is canon's results window sliding in at the end of the window, because canon's battle resolves there and this fixture never does.

chip-use (integrated), before and after: canon on top; ours at 47d936e (643191 px over 32 frames, canon 150+k, rust offset 105); ours at 381cb8a (567780 px, canon 150+k, rust offset 100). Each build on its own commit's alignment, from a clean checkout (tools/progress_gif.py).
