HISTORY (2026-09-07): made by a tool that no longer exists, under that day's method (a boxed region or a demo-feature build), so any 'pixels a frame' figure below is not today's full-screen standard. Today's measurement for this subject is chip-recov300-isolated.

Recov300, canon (sterile) left and this build right: 0.0 pixels a frame. The largest heal: the counter's step is |difference|/8 + 4 a frame, so a bigger number climbs faster, exactly as the real ROM's does.
