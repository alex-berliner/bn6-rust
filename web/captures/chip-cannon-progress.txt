The Cannon chip, and the shared artifact behind 27 chip rows. On the canon side, the save state every chip row started from had a stray portrait box and the deleted Mettaur's corpse inside the window (visible at the right of the field in the middle pane's canon frames of the old build's run). F5b starts canon from a state past the dissolve instead; because the game stops reading the joypad in that state, the chip press is written straight into MegaMan's input record. Every chip row lost the same 14388 px; 27 of them went to 0.

chip-cannon (isolated), before and after: canon on top; ours at b21fce8 (14388 px over 40 frames, canon 43+k, rust offset 122); ours at 3fa1eba (0 px, canon 5+k, rust offset 122). Each build on its own commit's alignment, from a clean checkout (tools/progress_gif.py).
