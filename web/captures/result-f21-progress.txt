The results screen across F21. Canon draws the WIN reward on the first confirm press, not when the window is shown: its driver chain reveals the reward over 42 steps and then holds a 30-frame cooldown before it accepts the dismiss, so ours had been showing the reward too early and dismissing on the wrong frame. Following that chain (waiting, a 42-step reveal, a 30-frame cooldown, wait for the reward, dismiss) took the row from 497967 to 408337. The inside of the window is nearly matched now (3734 to 904 pixels on the plateau); what remains is the window's slide timing and the backdrop tail behind it, which F21b and F21d take on.

result (isolated), before and after: canon on top; ours at 09474d1 (497967 px over 40 frames, canon 21+k, rust offset 15); ours at 8bcd73c (408337 px, canon 21+k, rust offset 15). Each build on its own commit's alignment, from a clean checkout (tools/progress_gif.py).
