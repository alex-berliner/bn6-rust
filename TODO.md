# Work that can be handed to a subagent

Each entry is meant to be pasted into a ticket more or less as it stands. Anything
without a way to *measure* whether it worked is not ready to hand out — write the
measurement first.

## Every ticket runs in its own git worktree

    tools/worktree.sh <name>

prints a directory, a branch, and the `CARGO_TARGET_DIR` to export. The agent works THERE and
commits to ITS OWN BRANCH; the coordinator merges back with `git merge --no-ff`. Nothing else
gets an agent's half-finished edits, and nobody has to remember not to build at the wrong moment.

Why this exists: on 2026-09-07 four separate incidents came from agents and coordinator sharing
one checkout. A build taken while a worker held `src/` compiled its half-finished file and
returned a normal-looking number; the same collision later made a build fail outright; and I told
a worker its correct measurement was contamination on the strength of one of those numbers.

What the script handles that a bare `git worktree add` does not:
- `reference/bn6f` is a submodule and a fresh worktree checks it out EMPTY. It is symlinked to
  the main checkout, because it is only ever READ -- disassembly comments are made by the
  coordinator in the main tree, so an agent must not commit inside it. The script marks the
  path `--skip-worktree` so that a stray `git add -A` cannot stage the symlink over the
  gitlink -- doing that once merged a self-referential symlink into main and deleted the
  disassembly from the working tree. Agents should stage by path regardless.
- Each worktree needs its own `CARGO_TARGET_DIR` or the builds serialise on one lock. A build in
  a fresh worktree takes about 30 seconds and produces a BYTE-IDENTICAL ROM to the main tree's --
  verified, three ways, so a number measured in a worktree is directly comparable to one measured
  anywhere else.
- Capture scratch paths are keyed on the checkout path by `chip_compare.scratch()`, so two agents
  cannot overwrite each other's packed ROM between its build and its capture. That needed no
  argument and no convention; it just works out.

An agent that commits on its own branch is also free to commit MIDWAY, which the old "never
commit" rule forbade -- and that rule existed only because everyone shared one tree.

## Rules for every ticket

- **Give each agent its own `CARGO_TARGET_DIR`** under `/tmp` (`tools/worktree.sh` prints one). `cargo` locks the shared
  target directory and every build overwrites the same `target/.../bn` that
  `tools/gbafix.py` reads, so two agents building at once will silently pack each
  other's ROM. This has already caused two wrong captures.
  This rule was itself a trap until 2026-09-07: `regress.py`'s `build()` hardcoded
  `ROOT/target/...`, so setting `CARGO_TARGET_DIR` made cargo build in one place and the
  ROM get packed from a stale ELF in another -- no error, just numbers for a different
  build (`mettaur` 95436 instead of 345). It now asks `cargo metadata` where the target
  directory actually is, so the rule and the harness finally agree. Any OTHER script that
  packs a ROM should be checked for the same assumption before being trusted under a
  private target dir.
- **Baseline first.** The agent must record the number with its own script *before*
  changing anything, and re-run the identical script afterwards, and report both. A
  change nobody can show improves a number is not a fix — see 7ah in `TRANSFER.md`,
  where a well-cited change backed by a disassembly line and the animation data turned
  out to move nothing and to be worse on a wider window.
- **Say which window was measured.** Two agents measuring the same residue in different
  frame windows reached opposite conclusions once. Windows go in the report.
- Only `src/` changes unless the ticket says otherwise. Never `tools/`, `reference/`,
  `assets/` or `Cargo.toml` without a reason in the report.
- A precise negative result is a good outcome and gets written into `TRANSFER.md`. A
  plausible-sounding change that does not move the number is not.
- **DO THE WORK YOURSELF.** Do not delegate a ticket onward to a further agent. You
  are the worker. A ticket that turns out to be bigger than it looked comes back as a
  report saying so, not as a subcontract. (Which model runs which role is decided in
  `.pi/agents/` and HANDOFF §13, not by the worker.)

## Rules for whoever is handing the tickets out

Written after breaking all three of these in one evening.

- **Do not build or measure from the tree while a worker holds `src/`.** Not just "do not edit" --
  a build taken mid-edit compiles whatever state the worker's file is in at that instant and gives
  a number that looks completely normal. This produced a `tiles` reading of 3295 and then one of 0
  within the hour, the second being a build with the worker's fix reverted. Reuse an already-built
  ROM from /tmp, or wait.
- **When a worker's number disagrees with yours, rebuild it yourself before contradicting them.**
  On the strength of the above I told a worker its correct measurement was contamination. It had
  done three from-scratch builds and was right. Its EXPLANATION was wrong (it blamed target-path
  length; three target directories, one deliberately long, give a byte-identical ROM) -- but a
  wrong explanation is not a wrong measurement, and the two must be judged separately.
- **`git add -u <path>` limits to that path.** A commit meant to carry the fix, the harness change
  and two write-ups carried only the submodule bump, and the rest sat unstaged behind a clean-
  looking `git status` line. Check `git show --stat` after committing, not just the exit code.

---

## R. Root states as recipes (HANDOFF §13 step 1)

- R1 DONE -- Rebuild the fixture chain from power-on. 
- R2 NEGATIVE -- Move the chip rows onto the never-had-an-enemy fixture. At the event-pinned alignment (canon CurState leaves 0x0804 at frame 3
- R3 PARTIAL -- A battle that never spawns an enemy. The roll's choice lands in `GameState->CurBattleDataPtr` at `0x02001b9c` (stored at
asm29.s:10286, read by `sub_800531C` asm00_1.s:4455)
- R4 BLOCKED -- Keep the enemy-less battle alive, then finish R3. Step 1 done, and it overturns the recon: none of the five literal `SubsystemIndex=0x10`
stores, the warp entry, `cs_warp_cmd` or the script 
- R5 PARTIAL -- A write watchpoint in the capture tool, then find what releases the empty battle. Step 1 done, verified, committed (17dc00f, plus HANDOFF §5 row)
- R6 NEGATIVE -- The state oracle: first divergent field, not just a pixel count. On `wt/r6-oracle` at `c504eee`, the export was pixel-neutral: `wave` 0/0/90 (negative 3840), `window` 0/0/16 (81056), `mettaur` 30864/1566/7
- R7 DONE -- Land the state oracle on the facts R6 found. Merged `a9d08b7` as `2b35d4b`
- R8 WITHDRAWN -- Usage logging: primitive profiles for the 43-chip corpus. 
## F. Convergence: every existing row to 0 differing frames before anything new (user, 2026-09-12)

Tickets run in this order; the coordinator takes the first OPEN one. Queue by leverage, from the
2026-09-12 full table (isolated unless noted): F4 opening 1160 (was 0) -> F5 the chip-row fixture
(28 rows at exactly 14388, 11 more above it) -> F6 tiles/gauge 538 (one capture, two rows) -> field
1048 -> banner 2248 -> buster 3172 -> warp 9198 -> card 18486 -> mettaur 31075 -> F3 (the chip window's
ghost; adds its row) -> popup 107511 -> result 497967 -> cursor 620802 -> the integrated variants.
No new content (viruses, bosses, chips) until the table is at zero. A reported 0 always says what it
compares: canon vs ours, or before vs after.

- F1 DONE -- MegaMan takes a hit one frame late. PARTIAL
- F2 DONE -- The mettaur row compares the wrong Mettaur attack. DONE and merged as `332e658`
- F4 DONE -- The opening row reads 1160 px since R1 rebuilt its canon state. The 1160 was the FIXTURE, not the opening
- F5 PARTIAL -- The chip rows' fixture: fire a chip after the corpse has dissolved. The prescribed route is dead, the gate is found (with corrections), and the next
intervention is named but untested
- F5b DONE -- Fire a chip in state 0x0C by delivering the press to MegaMan's AIData directly. The Sol-named gap closed with a measured fire
- F6 DONE -- tiles/gauge isolated: 538 px over 8 frames. Landed at the documented residue
- F3 DONE -- After the chip window closes, a faded copy of it stays on the field. The ghost is gone by mechanism, not masking
- F7 DONE -- The legacy `cannon` row still compares against PAUSED: 14388. Exactly as predicted: `cannon` re-pointed onto F5b's route and Align (`_chip_canon_0c("01")`
+ `ALIGN_CHIP_0C` on `afterdissolve_0x0c`), 143
- F8 DONE -- `field` isolated: 1048 px over 40 frames. DONE
- F9 DONE -- `banner` isolated: 2248 px over 58 frames. DONE
- F10 BLOCKED -- `buster` isolated: 3172 px over 32 frames. BLOCKED after the second miss, moving on
- F10b BLOCKED -- `buster`: fire in the 0x08 window with the miss-pose fix, or close the row. 
- F11 DONE -- `warp` isolated: 9198 px over 30 frames. DONE
- F17 DONE -- `mettaur` isolated: enemy_anim k=61, the Mettaur's own animation. mettaur 19698/1245/70/60824 unchanged pixels, oracle enemy_anim 2/70->1/70 (first k=61, mm_timer 70/70)
- F20b DONE -- `tiles`/`gauge` integrated: match the fixture's sprite positions to canon's state. tiles/gauge integrated 25979/3282->3865/678/8 (negative 15891 not blind), isolated still 0/0/8
- F21 PARTIAL -- `result` isolated: 497967 px over 40, its canon side is result_arrival. result 497967/31882/40/476424 -> 408337/31895/40/383600 (-89630)
- F22 NEGATIVE -- `cursor` isolated: 620802 px over 170 frames. cursor 620802/6884/170/728447 unchanged (nothing landed, branch empty, deleted)
- F22b NEGATIVE -- `cursor` isolated: match the x>=112 unshared mid-battle content on the fixture side. cursor fixture backdrop-seed probe refuted, nothing landed
- F18b PARTIAL -- `windowclose` isolated: the k=11 transient and the slide residue outside the window layer. windowclose 666451/28784/40->651885/28430/40 (k=11 BG3 transient 2447->0
- F18c PARTIAL -- `windowclose` isolated: the relocated k=9/k=10 close-frame blank/redraw. windowclose 651885/28430->650544/27555/40 (BG3 k9 1898->0, k10 1408->0, k11 0->1596
- F18d DONE -- `windowclose` isolated: the k11 gauge-body single-step redraw. windowclose 650544/27555/40->648948/27391/40 (BG3 k11 1596->0, x48-191 y0-15)
- F21d DONE -- `result`: write the window's tilemap by block copy, not 576 managed tile writes. result 408337/31895->102547/14866/40 (neg 195579 not blind), field 1048/177->0/0/40
- F12 DONE -- The chip rows' own residues, family by family -- multi-pass, stays OPEN until every chip row is 0. every chip row reads 0: the last two were areagrab (orb draw order + burst palette walk, ad653c0) and chip-use, re-cut F31b-style so canon f
- F33d PARTIAL -- Post-0x0C simulation: act after the countdown starts instead of freezing in `over`. verifier verdict: canon citations CONFIRMED (sub_800801C:10422, off_8008038 table, sub_80081A4:10617, 0x0C@0x0800811E, sub_8012DFC:8977 not 
- F37 PARTIAL -- `cursor` and `windowclose`: the sprite layer, object by object. per-object tables landed 1bee9d8 (notes only, descriptors untouched): cursor 154361/909/170 (camera-pan +15Y + Mettaur pose 4,11-vs-4,8), wi
- F37b PARTIAL -- `cursor` and `windowclose` to 0: the camera pan while the chip window is open, the Mettaur's held pose, the hand icon and bracket. pan+pose+icon landed a6bf21c: cursor 154361/909/170->130221/767/170, windowclose 27819/1938/40->19168/1878/40 (neg 316499/214890 not blind)
- F37f PARTIAL -- `windowclose` k=0..9: the 162 px marcher on the slide frames (custom.rs). mark-during-Closing KEPT unmerged (branch wt/f37f 0c05e00, custom.rs only): windowclose 1458/162/40->0/0/40, cursor 20->3/3/170
## T. Trace-driven porting (phase "porting", 2026-09-14; the plan in HANDOFF §1)

**Common to the T tickets.** The harness rows stay as free regression tests (every landing runs verify_rows on
wave, window, mettaur, popup, buster, result, field and the row a ticket names). Acceptance in this phase is
stated as state parity: "first divergence at frame N or later on scenario S", with pixels as the gate on the
same recording. Canon never changes; provenance rules as before; cite reference/bn6f file:line.

- T12 DONE -- The enemy roster out of the ROM's own index tables: every enemy_idx with its think and act entry. LANDED as 1d23394
- T7d PARTIAL -- Drive the sequencer's window states with a fixture that closes the window, and put canon's predicates under the edges. Kept UNMERGED at wt/t7d @ 24d9ef7 (8daa88d + 24d9ef7 on a merge of wt/t7c @ 9be540c)
- T9c PARTIAL -- The Gunner: implement what T9b measured (the frame-60 lever, enemy_kind, the routine, the art, the row). CORRECTION (re-stamps my earlier entry, which cited verifier findings before the verifier had returned -- the merge message 1f7efc9's claim 
- T10 DONE -- Inventory of the battle engine from the ROM's own tables: the completion checklist gets its counts. LANDED as d0e0574 (two passes: 3723b5d + fix 82c31bd)
- T7c PARTIAL -- The sequencer's missing states: the custom-screen states our side never reads, and the end edge nine frames early. Kept UNMERGED at wt/t7c @ 510b5c6 (2 commits 3def49e, 510b5c6)
- T9b PARTIAL -- The second virus (Gunner): widen the fixture to field a non-Mettaur, build the canon recipe, port its routine. Measurement pass, zero edits (worktree /tmp/bnwt/t9b-gunner clean at 974f154) -- T9's blocker is SOLVED and reproducible, the implementation
- T8 DONE -- Port the script VMs, per section 3 of the plan (map-script and chatbox text-script dispatch). Landed 752146e (branch wt/t8-script-vms @ cdd976e)
- T9 BLOCKED -- The second virus as a ported per-type routine, from a real battle recording. Blocked on a file-set widening I cannot grant
- T6 DONE -- The Mettaur as canon's per-type routine: replace the hand-written brain with the ported AI entry. Mettaur brain now canon per-type entry MettaurEntry ForMettaur_8109EF4 (objects.rs), hand-written MettaurState removed (ai.rs), battle.rs 1 
- T7 PARTIAL -- The battle end as canon's sequencer table: banner states, teardown, results hand-off. Sequencer states + TRC2 v3 export landed pixel-neutral (verify_rows: isolated all 0 incl mettaur, cursor 10/9 tear-smaller, both-rows 0
- T7b DONE -- Re-establish the sequencer evidence: fresh v3 traces, loud judge, kill-to-slide gap. Landed 0fb54d4 (branch wt/t7b-sequencer-evidence @ dce93d4, carrying wt/t7's Sequencer states)

**Common to F8-F23 (and their b-tickets) unless the ticket says otherwise.** Baseline the row (harness line plus
`tools/diffmask.py` region, plus `tools/oracle.py` where the row is supported); localize the residue to
frames and an element; find canon's routine for that element in reference/bn6f and cite it; fix src/
(or the fixture/descriptor when the residue is the fixture, with `peeked` provenance); re-run the row,
wave, window, opening, chip-cannon and the full table -- nothing may get worse, and a row that does is
reported with its numbers. No allowlist change; no alignment or region change except by measured event
(F2's rule). **Coordinator:** verify_rows on every row the report names; the verifier only for claims
beyond harness lines; a PARTIAL from a wrong guess about the cause gets one follow-up ticket; if that
also fails, mark the ticket BLOCKED and move on to the next OPEN ticket.

- T13 DONE -- Audio parity, first measurement: both sides dumped and compared sample-exact. LANDED as 9d52135
- T9d DONE -- Why the poked Gunner battle never goes live: write-watch the sequencer on the working scenario and name the difference. no writer for dword_203CA70 on either battlestart route (200f watch-write, both empty)
- T13b DONE -- The two audio defects T13 measured: the ungated hit sample, and the 6.6 ms onset offset. src/battle.rs: hit_in now armed only when the strike's take_damage() landed
- T7e DONE -- End-edge nine frames early on battle_full and the two wrong citations in src/battle.rs. battle_full sequencer 540/540 (165 window-setup k=31..195 + 8 kill-timing k=297..304, both named)
- T7f PARTIAL -- Closing-window fixture: drive 0x24 -> 0x00 -> 0x04 -> 0x08 and replace the fitted edges with canon's predicates. windowclose_full scenario confirmed 0x00 enter+leave (144..146, 22..24) and 0x04 enter+leave (147..206, 25..84) on both sides (100/100 seque
- T9e NEGATIVE -- Gunner live trigger: find what writes dword_203CA70 on the poked battle and add the row aligned by the Gunner's first attack event. trigger poke unblocks sequencer (Index_01 0x02034881 4->0->8->0x0C at f165
- T7h NEGATIVE -- battle_full's first divergence: the enemy's opening action and animation (canon CurAction 0x0A, ours 0x00). spawn arm ported: METTAUR_ACT_SPAWN=0 const + MettaurEntry init + SPAWN arm in think() at src/objects.rs:243-251,298-308,343-362
- T7i BLOCKED -- battle_full's MegaMan tail: the k=179 action/timer/anim group, then name the RNG residue. third M2 ticket in this run after T7f (PARTIAL: windowclose_full validates but SEQ04_FRAMES=60 stays peeked because asm timer arms 0x1e/0x29
- T9f NEGATIVE -- Does `eS20364C0.JumpOffset00 = 8` start the poked battle: one-shot poke test, and the first attack event's frame. one-shot poke-at 155:0x020364C0:0x08 set eS20364C0.JumpOffset00=8 and held through f199
- T9g NEGATIVE -- The Gunner attack event: the 234-frame settle window (sub_8112D9C) and MegaMan's panel row, so the row ticket becomes writable. Three captures, three negatives on docs/trace-only commit (no src/tools changes, wt/t9g-gunner-extended 801ecda, landed d43e4c0)
- T13c BLOCKED -- Audio comparison on a second scenario: chip-fire and chip-damage SFXs on the cannon route, gated by the cited predicates. Worker audited src/ before editing
- T7j PARTIAL -- Model the banner composite for sub_801E754's leave predicate: replace SEQ04_FRAMES=60 with the canonical banner-idle check. branch wt/t7j-banner-composite aa3486d (docs-only on src/battle.rs, SEQ04_FRAMES=60 unchanged in code
- T13d NEGATIVE -- Audio probe on the cannon route: what canon fires, what ours doesn't, then gate. docs-only commit landed as e87cc7f

- T7l BLOCKED -- battle_full opening action: seed canon's HOP pose at k=0 so the spawn fade no longer parks CurAction=0x00. T7l BLOCKED: worker hit tool soft-limit (80) on baseline only
- F21e DONE -- result isolated: decompose the remaining 102547/14866/40 per-object, name the next mechanism. F21e was a no-op: HEAD result isolated already 0/0/40 PASS (neg 111839, not blind) via F34b's intro_fade=0/start_state==1 + megaman_col=2 + 
- F37h NEGATIVE -- cursor: close the 3 px residue at k=37/97 from the window mark landed in F37g. F37h NEGATIVE: cursor 10/9/170 unchanged on HEAD (regression set: windowclose 0/0/40, mettaur 0/0/70, result 0/0/40 all PASS)
- F37i BLOCKED -- cursor: close the 3 px residue at k=37/97 via the BG1 backdrop drain in src/backdrop.rs. F37i BLOCKED: worker hit tool soft-limit (60) on assembly research only
- T7m BLOCKED -- battle_full MegaMan tail at k=179: the mm_state_action/mm_anim/mm_timer group, finish the release-edge gate port. T7m BLOCKED: worker hit tool soft-limit (60) on baseline capture only
- T7n PARTIAL -- battle_full RNG cadence: name the rng_cadence first-divergence at k=271 on 10/540 frames, port the read-site if a clean mirror exists. T7n PARTIAL: rng_cadence divergence named with citation
- F37j NEGATIVE -- cursor: port canon's QueueEightWordAlignedGFXTransfer drain into src/backdrop.rs so the k=37/97 BG1 seam closes. F37j NEGATIVE: both drain-timing ports regressed cursor
- T7o PARTIAL -- battle_full release-edge at k=179: relocate the SEQ04 seq-match block past t1_player_entry so the gate frees before the executor. T7o PARTIAL: SEQ04 seq-match block relocated from src/battle.rs:2533 to :3268 (after t1_player_entry), matching canon's call chain at asm00_
- T7p DONE -- battle_full RNG cadence: port the per-site mirror at sub_80C7EC8 / cbGameState_80050EC so rng_cadence drops below 10/540. T7p was a no-op: HEAD rng_cadence 9/540, already below 10/540 acceptance threshold (T7o's PARTIAL landing 784c8e5 reduced it from 10→9 by re
- F38c PARTIAL -- opening integrated 72499: port canon's intro-hunk palette/sheet assignment and the enemy's materialize animation. F38c PARTIAL: opening integrated 72499 measurement-only diagnostic
- T9h PARTIAL -- Gunner as data: port canon's per-type routine from byte_80182C4, add the harness row. T9h PARTIAL: Gunner as data per-type routine cite landed
- T7g NEGATIVE -- Land T7c's custom-screen states from the unmerged branch so battle_full's sequencer coverage extends. T7g NEGATIVE: ticket premise wrong — divergence is a fixture-level difference, not a src/battle.rs sequencer-dispatch port
- T7q PARTIAL -- battle_full k=179 group: add the seq.state gate in Actor::update so the MegaMan executor's per-tick work respects SEQ_20/SEQ_24/SEQ_00/SEQ_04. T7q PARTIAL: seq.state gate added to Actor::update (t1_player_entry early-returns Update::Nothing when seq.state in {SEQ_20, SEQ_24, SEQ_00,
- F38d PARTIAL -- opening integrated 72499: port the PAL_OBJ slot allocation in src/actor.rs and the second-cluster materialize animation in src/objects.rs. F38d PARTIAL: opening integrated 72499 unchanged
- T9i PARTIAL -- Gunner as data: fix the harness capture race so the gunner row reads 0/0/N. T9i PARTIAL: gunner race fix landed

- T7r PARTIAL -- battle_full fixture: scripted L input so self.seq.state traverses {SEQ_20, SEQ_24, SEQ_00, SEQ_04}. T7r PARTIAL: battle_full fixture seeds gauge=1 + window_pick OK + scripted A@170
- F38e BLOCKED -- opening integrated 72499: fix the diagonal spawn cells (src/battle.rs:1907-1908) and the materialize y offset (src/actor.rs:528). F38e worker found two rule conflicts that prevent landing: (1) spawn-cell fix needs per-enemy panel data per spawnEnemy_80073E2 asm00_1.s:86
- T9j PARTIAL -- Gunner as data: port the per-type routine into src/gunner.rs and align the harness row on the Gunner's first attack event. T9j PARTIAL: gunner_update per-type routine port at ForGunner_8113078 (asm32.s:10123-10142) + 4-arm state machine sub_8112F70/sub_8112FBA/su
- T7s NEGATIVE -- battle_full sequencer-timing fix: window opens at k=124 vs canon k=31 so self.seq.state traverses SEQ_20 by canon's k~31. T7s NEGATIVE: ticket acceptance unreachable under rules
- F38f PARTIAL -- opening integrated 72499: per-enemy panel bytes via src/fixture.rs and the diagonal spawn-cell fix in src/battle.rs:1907-1908. F38f PARTIAL with revert: branch (9a938d0 + 3eea157 + 8ee4346) landed on main but introduced cursor regression 1/1/170 -> 26/25/170
- T9k NEGATIVE -- Gunner as data: move Battle::gunner_ctl + Battle::impacts in src/battle.rs into GunnerEntry so gunner_update drives per-tick visible content. T9k NEGATIVE: refactor preserves gunner behavior 2850534/38237/130 (unchanged from T9j PARTIAL -- the ctl/impacts migration is relocation-on
- F38g BLOCKED -- opening integrated 72499: F38f re-land with cursor fixture state file regenerated for 67-byte descriptor. F38g BLOCKED: per-enemy panel port cherry-picked/re-implemented (commit 7c3b33f on wt/f38g-reland, diff matches F38f: src/fixture.rs +17, sr
- F36a PARTIAL -- warp integrated ~363658: baseline the residue, decompose by frame and region, port the canon mechanism. F36a PARTIAL with no code change: actual warp integrated on HEAD is 40628/11744/30 (NOT 363658 headline -- F38b event-lock pin rust_offset=5
- F35a BLOCKED -- buster integrated ~643698: baseline the residue, decompose by frame and region, port the canon mechanism. no code change, tool budget soft-cap hit at 80 before port
- T7u BLOCKED -- battle_full sequencer: port sub_801E754's banner-idle check to replace SEQ04_FRAMES=60 in src/battle.rs. code change made, but caused cursor regression: cursor 1/1/170 -> 38/37/170 (37x worse)
- F36b BLOCKED -- warp integrated ~40628: per-region decomposition, then port the canon mechanism for each non-zero region. no code change, tool budget soft-cap hit at 80 before port
- F38h PARTIAL -- opening integrated 72499: descriptor-preserving per-enemy panel encoding so cursor fixture state file stays valid. opening integrated 72499/2691 -> 25829/931 (residual is PAL_OBJ allocation order src/spr.rs:701-761 per F38e BLOCKED, not in scope)
- F37k BLOCKED -- cursor 3 px residue at k=37/97 via per-scanline BG1 backdrop seam port. infra failures (bash exit 1, then model cold-start on resume)
- T7v BLOCKED -- battle_full RNG cadence 9/540 → 0/540: port the per-site mirror at the remaining divergence frames. no code change
- F38i PARTIAL -- opening integrated 25829: PAL_OBJ allocation order port in src/spr.rs to clear the per-object OAM colour residue. opening integrated already at 0/0 on main HEAD (8b314eb) per verify_rows — NOT 25829/931 as F38h PARTIAL commit message stated
- F37l BLOCKED -- cursor 3 px residue at k=37/97 via per-element BG1 seam cite at sub_8001C94 (asm00_0.s:3752). cursor 1/1/170 isolated: residue is 1 px at (183,5) on k=97 ONLY (k=37 already 0 after F38h merge 2d1e639), NOT 3 px at k=37/97 as ticket cl
- F36c NEGATIVE -- warp integrated 40628: port sub_8001C94's BG1 seam transition directly into src/backdrop.rs. Premise refuted by measurement, no code change
- T7w NEGATIVE -- Battle end as canon's per-state counts: name the layer of the integrated rows' 16-px band, then retire the fitted RESULTS_DELAY=110. Premise refuted by measurement
- T9l PARTIAL -- gunner 2850534/38237/130: derive this row's own backdrop seed, then split the residue by layer. LANDED as 424814d
- F39a NEGATIVE -- opening integrated 25829/931/40: the four OBJ entries canon does not draw, and the per-sprite PAL_OBJ slot. Premise refuted by measurement
- F40a BLOCKED -- gunner 2105613/38237/130: settle whether the BG3 plateau is gauge-driven, then port the chip-window auto-open that canon takes at capture ~157. Lever not settled

- T7x NEGATIVE -- battle_full SEQ_04's leave predicate is a bit test: port isBannerBusy_801E754, delete SEQ04_FRAMES and BANNER_FRAMES. Ported and measured, but NOT landable: the bit-15 lifecycle is real and our mask series comes out canon-shaped, yet spawning the banner reco
- T7y NEGATIVE -- The custom screen's open edge: canon's gauge→open chain replaces our peeked 60-frame countdown (gunner's two rows, battle_full's 83-frame window offset). No code landed
- T7z BLOCKED -- The window's own duration: what ends canon's SEQ_24 after 100 exports, then the 0x20/0x00 edges and battle_full's kill→0x0C hand-off. NOT DISPATCHED -- this is the third ticket in a row on battle_full's sequencer timeline and the two before it both came back NEGATIVE on the
- T14 PARTIAL -- Panels as a ROM table: canon's type-indexed flag word at 0x3007924 and PanelOffsetListsPointerTable, so M1's panels line stops being 13 routine names. LANDED as 55c6b51 (docs/tools only, no src/, byte-identical build, verify_rows skipped as a no-op
- Q1 DONE -- Name the last bare numbers in the HUD walk and the backdrop wrap. LANDED as 57c62b3 (all 61 compared rows identical across the merge
- Q2 DONE -- The scene's switches as a flags type, not a bare byte. LANDED as 61fc751 with its acceptance line amended at merge on the verifier's judgment
- Q3 DONE -- One safe wrapper for the boot mark and the state block. LANDED as 184b9b5, re-stamped with the verifier's own retraction so the record is not read as cleaner than it was
- Q4 DONE -- The forty-byte state block as named fields, not byte offsets. LANDED as 17012e8 (60/61 rows identical across the merge, verify_rows PASS)
### Q5. Retire the dead `FLAG_*` names in tools/harness.py's row notes and document SceneFlags' raw-byte escape hatch  *(DONE -- 2026-09-15, LANDED as 0c0d6f1)*

**Result.** LANDED as 0c0d6f1. All 13 dead FLAG_* names in tools/harness.py's row notes rewritten to SceneFlags::<NAME> (grep -c 'FLAG_[A-Z_]' now 0 on main; every one had an exact newtype counterpart, so no NEGATIVE was needed), and both SceneFlags From impls in src/fixture.rs now say in prose what the raw-byte door is for (the harness's poke/cheat descriptor path) and what it is not for (call-site bit tests). Coordinator-checked the diff is comment/note-text only: no non-comment line changed, and the worker's build read byte-identical to baseline (cmp 0 diffs), so no row could move. verify_rows on wt/q5-flag-names PASSes the four named isolated rows -- mettaur 0/0/70 neg 41734, result 0/0/40 neg 111839, opening 0/0/40 neg 86591, popup 0/0/80 neg 1288 -- and the only FAILED line printed is the pre-existing allowlisted opening-integrated 25829/931, present identically at baseline. No verifier dispatched: the ticket's own rule says a comment-only diff with identical rows needs none. Fitted constants 19 unchanged. Child worker-hyper glm-5.3-flash:high; the run's first attempt stopped after the baseline with no edits and was resumed on the same session to finish.
**Why.** Q2 landed the `SceneFlags` newtype at 61fc751 and its verifier-hyper recorded two follow-ups the ticket itself was right to leave out of scope: `tools/harness.py` still names the deleted consts in 13 comment spots (first three read at :1195 `FLAG_OPEN_WINDOW`, :1489 `0x11 | FLAG_RESOLVE_OVER`, :2888 `FLAG_HUD_LIVE`), so a reader following a row note into the code hits names that resolve to nothing; and `SceneFlags`' `From<u8>` impl is the one door through which a later ticket can write `f.flags.0 & 0x20` again, which is exactly the raw-bit test Q2 exists to remove -- it needs a written contract, not a silent door. Milestone M2 (the engine core's tooling).
**Files.** tools/harness.py (comment lines only -- the 13 spots that name a deleted const, and nothing else), src/fixture.rs (doc comments on the two `From` impls only).
**Do.** 1. `grep -n "FLAG_[A-Z_]*" tools/harness.py` and rewrite each hit to the accessor or const path a reader should follow now (`SceneFlags::OPEN_WINDOW` / `flags.open_window()`), keeping the number being explained unchanged -- these notes exist to justify a row's fixture byte, so the value in each line is the thing under test. 2. Report the before/after grep counts and quote the three oldest lines both ways. 3. Put one doc comment on each `From` impl in src/fixture.rs saying what the raw-byte conversion is for (the harness's poke path, which needs the byte) and what it is not for (call-site bit tests). 4. Rebuild and run `python3 tools/harness.py --only mettaur,result,opening,popup --no-gallery`; the four lines must read identical to HEAD (a comment-only change; the .gba may differ only by panic-line bytes, and if any byte outside them differs, the change was not comment-only -- say which).
**Rules.** No behaviour change and no non-comment line anywhere. tools/harness.py's row definitions, allowlist, compared regions and negative fixtures are not to be edited -- this ticket touches text in `#` comments and nothing else. Do not touch src/spr.rs or src/script.rs: their `FLAG_ACTIVE`/`EVENT_FLAG_*` families are ROM facts (ObjectHeader.inc:8-12, asm03_0.s:18525) and Q2's verifier judged that folding them into `SceneFlags` would mislabel provenance. Cite hygiene: the 357da2a re-cut moved reference/bn6f line numbers, so re-check any file:line you cite against the disk. Zero capture runs beyond step 4's four rows and the one `--only` call that checks them.
**Acceptance.** `grep -c "FLAG_[A-Z_]" tools/harness.py` reports only hits that are legitimately a ROM symbol name (list them with their cite), the four named rows identical to HEAD, and both `From` impls documented. A NEGATIVE that shows a hit cannot be rewritten without touching a non-comment line also closes it -- name the line.
**Measure and report.** rows: mettaur, result, opening, popup (isolated). frames: 70/40/40/80. total/worst before and after (all four are 0/0 at HEAD with non-blind negatives 41734/111839/86591/1288). region: the 13 comment spots. commit. one line of mechanism. one line of what is unverified.
**Coordinator:** a comment-and-doc ticket: no verifier needed when the four rows are identical and the diff is comment-only (`git diff -U0 <branch> | grep '^[+-]' | grep -v '^[+-][+-]'` should show only comment lines). Dispatch alone; nothing else in this run owns tools/harness.py.

### Q6. cursor's single-frame tear tracks how the boot marker's frame word is written -- decide which, and stop calling it noise  *(NEGATIVE -- 2026-09-15, CLOSES as NEGATIVE, no code change [branch wt/q6-cursor-tear @ ddb031d kept UNMERGED, worktree removed])*

**Result.** CLOSES as NEGATIVE, no code change (branch wt/q6-cursor-tear @ ddb031d kept UNMERGED, worktree removed). Step 1's five-run spread on one unchanged commit (35e6efa) is ZERO: cursor 1/1/170, negative 186279, region checksum 5a772d64782bcae2 -- one pixel at (183,5), k=97, canon frame 112 -- five runs, plus five more plain runs before instrumentation, and the coordinator's own verify_rows on main HEAD after Q5's landing reproduces 1/1/170/186279. So the tear is a real fixed-position property of the build, not run-to-run instability, and the Q3-era 13/12/170/186277 was an unrepeated real state (note the NEGATIVE itself moved 186279->186277 there, a content difference, not an alignment shift). Step 2 refutes the mechanism: no torn frame word in 120 mettaur samples or 260 cursor samples, and none is possible -- tools/mgba_capture.c:968-974 takes each watch sample only after core->runFrame() returns, so the harness can observe completed stores and nothing else (coordinator read the site), and the word the aligner keys on is the constant magic: find_marker_origin (tools/harness.py:93, unpack at :105) returns the first frame whose word == 0x42415454 and never reads the frame counter at +4. No stable-two-word protocol is warranted for this reader; had the read path been instruction-level, the report names even/odd sequence-before-value as the right shape. Fitted constants untouched, allowlist untouched, no row's frames/alignment/descriptor/region changed. No verifier dispatched: nothing merged, and the two forward claims were source-cites checked by reading. Child worker-hyper glm-5.3-flash:high. What stays open is only why the Q3-era 13/12 state occurred -- unrepeated, and this ticket's own rules forbid re-tuning to find out.
**Why.** Q3 wrapped src/main.rs's marker writes in `BattleMarker`, whose `put_u32` went through `put_bytes` as four byte-wide volatile stores; Q4 restored word-wide stores for aligned writes. cursor's tear, which this run was told to report and not chase, moved with exactly those two changes: 1/1/170/186279 at 2f32355 (pre-Q3 shape, coordinator baseline) and again on Q4's branch after the word-store restore, but 13/12/170/186277 on the tree where the frame word was written as four bytes. Q4's verifier-hyper named that correlation itself -- "the one number that differs is the one number this change could plausibly have caused", so the phase rule's "capture instability" reading is not established and behaviour-neutrality of the write-shape change is not proven by that row. A torn frame word is the kind of thing that shifts a harness frame boundary by one sample, and this row is a cursor-position row, so the cheap hypothesis is: the marker's frame word is read mid-update when it is written as bytes, the harness aligns on a torn value, and the compared window starts one frame off. Milestone M8 (presentation), touching M2's contract.
**Files.** src/main.rs (the marker write, already word-wide), tools/harness.py (the marker/frame-alignment read path only -- read, do not edit, unless the alignment really is sampling-dependent), docs/worklog/Q6.md.
**Do.** 1. Re-measure cursor five times on one unchanged commit and report the five (total, worst, region checksum) -- if the spread is zero, the Q3/Q4 correlation is coincidence and this ticket closes as NEGATIVE with that table. 2. `--watch 0x02000000:8` across a mettaur run and report whether any sampled frame word is observed mid-update (a value that is neither the previous nor the next frame's word), i.e. whether the harness can witness a torn word at all. 3. If a torn sample is possible, name the reader that takes the alignment (file:line in tools/harness.py) and say whether it should read the marker with a stable-two-word protocol (write frame after a magic bump, or an even/odd sequence number) rather than trusting one 32-bit store; do not change the protocol here, just name it.
**Rules.** Read-mostly. One code change at most and only if step 3 shows the harness genuinely can sample mid-update; no change to any row's frames, alignment, descriptor or compared region without saying so in the report; cursor's number must be quoted with its region checksum every time, since total/worst alone hide it. Do not "fix" the tear by re-tuning anything.
**Acceptance.** Either a five-run spread table that shows cursor is stable on a fixed commit (which retires this ticket and means the Q3-era 13/12 was a real, unrepeated state) or a named torn-word witness with the reader's file:line and a proposed protocol -- NEGATIVE closes it, a bare "it moved again" does not.
**Measure and report.** rows: cursor (five times, one commit). frames: 170. total/worst/region checksum each time. Then the watch series and whether a torn frame word appeared. commit. one line of mechanism. one line of what is unverified.
**Coordinator:** dispatch alone; Q1..Q5 own none of these files. A verifier is needed only if the branch changes src/main.rs's write protocol.

### F41. chip-use integrated: the last 2 px, then delete the chip-use allowlist entry  *(NEGATIVE -- 2026-09-15, CLOSES as NEGATIVE, and the branch DOES land for its one honest change [allowlist re-title only])*

**Result.** CLOSES as NEGATIVE, and the branch DOES land for its one honest change (allowlist re-title only). The ticket's premise was a misread, CONFIRMED: chip-use integrated measures 275307/18091/30 (negative 283978 non-blind), never 'total 2' -- so there were no 2 px to name and the entry cannot be deleted at a measured 0; isolated stays 0/0/30 with negative 7768 (verify_rows PASS on wt/f41-chip-use-2px @ 233adfe). verifier-hyper CONFIRMED the layer story digit-for-digit with its own runs at 233adfe using --only-bg <n> (the flag that exists; --disable-bg takes no value): BG3-only total 44306 with per-k [0]x24 then 2090/4186/6282/8378/10573/12797 and bboxes x0..15 -> x0..95 y16..154, i.e. one 16-px column per frame confined to k=24..29; BG1-only 443520 (T7w's number reproduces on this commit); BG0-only 0; BG2-only 0; OBJ+HUD 0. Caveat the next ticket must carry: each split picks its own alignment offset (BG1-only 94, BG3-only 103, the row's honest lock 101), so 443520+44306 does NOT decompose 275307. Mechanism cite CONFIRMED on disk: resultWindowSlideTick_802BE36 at reference/bn6f/asm/asm03_0.s:11701 (ends :11734), .word at :11674, .equiv RESULT_SLIDE_STEP_COLUMNS=2 '// 16 px a frame' at :11583; 'dispatched by resultWindowDriver_802BD60' is a two-hop chain (802BD60 -> off_802BDA0 -> sub_802BDC0 -> off_802BDDC -> sub_802BDF0 -> :11674), not a direct dispatch. Why not a 2-px port: our gate is SceneFlags::RESOLVE_OVER (src/fixture.rs:92 bit5, read at :146, used src/battle.rs:2621-2628 as (flags.resolve_over() || !enemies.is_empty()) && ...), and CHIPUSE_ZERO (tools/harness.py:1570) carries NO flags key (unlike ZERO_ENEMY_RESOLVED at :1489 flags=0x31), so decided never fires, the results show never runs and BG3 can never match under the frozen alignment; F38b's retry measured chip-use integrated 275307->269172 with isolated 0->4026 and was reverted (record at tools/harness.py:~2160). Allowlist 4 -> 4 entries, max_px 28000 and date untouched, no deletion, the other three entries byte-identical. UNVERIFIED: 'sequencer stays 0x08 through the window' is not freshly captured -- the only in-tree sequencer number is canon 0x0C at canon 47. CORRECTION OF THIS STAMP'S OWN HISTORY: my first F41 stamp, written before the verifier returned, asserted the BG3 band was refuted by a --disable-bg 0,1,2 run reading total 2 -- no such run was ever executed and that claim was false; main's b5e1936 carried it into docs/worklog/F41.md and is reverted by this landing's parent. Child worker-hyper glm-5.3-flash:high; verifier-hyper qwen3.8-flash:high CONFIRMED 4/4 claims.
**Result.** CLOSES as NEGATIVE, branch wt/f41-chip-use-2px @ 233adfe kept UNMERGED (its tools/allowlist.py re-title refuted, so nothing of it lands; worktree removed). CONFIRMED by verifier-hyper: the ticket's premise is a misread -- chip-use integrated really measures 275307/18091/30 (negative 283978 non-blind) at 8d38d39, reproduced twice by the coordinator's own harness run on HEAD, so there were never 2 px to name and the entry cannot be deleted at a measured 0; isolated stays 0/0/30 (neg 7768, verify_rows PASS); the k=0..23 residue IS backdrop art (BG1-only 274756, BG2-only 2, BG3-only 2). REFUTED, and this is the sentence the next ticket must not cite: 'BG3 alone reads 0 for k=0..23 and then exactly the band 2090/4186/6282/8378/10573/12797 for k=24..29, i.e. canon's RESULT window slide-in' -- the verifier's own --disable-bg 0,1,2 run gives BG3 totals 2,1,1,0,0,0,0,0,2,1,1,1,1,1,1,0,1,1,1,1,1,0,1,1,1,0,1,0,0,1 (sum 2), and its diffmask bboxes show both sides' result windows already open at k=24 (canon x24..399 y0..123 vs ours x24..383 y0..159), so no slide band exists on either side. Also refuted: resultWindowSlideTick_802BE36 (asm03_0.s:11701) does not advance 16 px/frame -- sub_802BE48 is a one-shot 8-px copy with a 160-px jump between entries -- and the report's 'F38b 0 -> 443520 -> 10000' chain is a mis-cite (443520 is T7n's GUNNER_ROW BG1 number; T7n's own line is 'BG1-only 443520 -> 230400 -> 0'), though F38b's underlying 0->4026 regression claim does check out in TODO_ARCHIVE.md:414-429. The real state, per the verifier: max_px=28000 is 9x BELOW the measured 275307 and only passes because no per-frame ceiling is enforced -- the row is tolerated by an unenforced bound, not by a 2-px residue. Consequence for F42: do not carry F41's result-window mechanism over; field integrated's '1589' is probably the same class of misread and its k=0..23 residue needs its own layer split. Child worker-hyper glm-5.3-flash:high; verifier-hyper qwen3.8-flash:high.
**Why.** The 2026-09-15 scoreboard (main at `1f81767`, 60/67 rows at 0) prints `chip-use integrated FAILED total 2` — the smallest non-zero row on the board — carried by `tools/allowlist.py`'s `"chip-use:integrated": (28000, "AUDIT-6: HUD vs a zero-enemy arena, not compared before", "2026-09-08")`. The row's `isolated` variant is 0/0/30 and isolated is just `--disable-bg` on **both** sides (`tools/harness.py:2131-2181`), so the OBJ layer is already exact and all 2 px sit in a BG layer of the integrated compare. Two candidate mechanisms, both precedent-backed: (a) the fixture's borrowed backdrop seed — T9l derived `GUNNER_ROW`'s own seeds and took BG1 from 1524416 → 230400 with a src/ change of one seed table (`docs/worklog/T9l.md`); (b) the `canon (sterile)` result-mark stop noted in the row's own text ("our `over` at capture 8 with show at battle ~110 straddles this window"). The cross-cutting invariant is *no allowlist entries*, and a 2-px tolerance at `max_px=28000` is 14000x the defect: this row is the cheapest entry-retirement on the board. Milestone **M4** (chips as data — this row's content is the Cannon's fire), and the "no allowlist entries" invariant.

**Files.** src/fixture.rs, src/backdrop.rs, src/field.rs, src/hud.rs, src/hudtiles.rs, src/battle.rs, tools/allowlist.py (deletion only), docs/worklog/F41.md.

**Do.** 1. Baseline: `python3 tools/harness.py --only chip-use --no-gallery` and report both variants' total/worst/frames plus the negative fixture's total. 2. Name the pixels: `tools/diffmask.py` per-k bbox for the integrated variant, and say the (x,y) and which k; if the 2 px are not on one frame, say the frames. 3. Decide the layer by measurement, not by guess: re-run the row's pair with the BG flags from `chip_compare` so each of BG1/BG2/BG3 is seen alone on both sides, and name the one BG carrying the difference. 4. Once the layer is named, test the cheap lever first: derive this row's own backdrop/HUD seed in src/fixture.rs the T9l way (no other change), re-run step 1 and report. 5. If step 4 does not clear it, find the writer of that layer's content in reference/bn6f (cite file:line), port the mechanism in src/, re-run step 1. 6. If total reads 0 on both variants, delete the `"chip-use:integrated"` entry from tools/allowlist.py and run `python3 tools/harness.py --no-gallery --ui integrated` to show the suite still passes without it.
**Rules.** Baseline before any edit and the same command after; report both. No change to any row's frames, `align`, `canon_ref`, `search` band or compared region — an alignment "fix" is a NEGATIVE, not a landing. Canon never changes; no further `patch_sterile.py` use. Do not raise or edit `max_px` on any entry — only delete an entry at a measured 0. Never touch `tools/allowlist.py` for another row. The row's negative fixture must stay non-blind; quote its total at every step. Cites: re-check file:line against disk (the 357da2a re-cut moved bn6f line numbers). Any literal you edit gets a `// provenance:` or `// canon:` name. At most 8 capture runs before reporting; a ticket that turns out bigger comes back as a report, not a subcontract.
**Acceptance.** `chip-use integrated 0/0/30` with `chip-use isolated` unchanged at 0/0/30 and the negative still non-blind, and the `"chip-use:integrated"` key absent from tools/allowlist.py. Or a NEGATIVE that names the layer (BG1/BG2/BG3/OBJ), the k and (x,y) of both pixels, the routine that writes it (file:line) and why it is not a 2-pixel port — that closes the ticket and re-titles the allowlist entry honestly.
**Measure and report.** rows: chip-use (isolated + integrated). frames: 30/30. total/worst before and after, the negative fixture's total before and after, the per-k bbox, the BG the 2 px live in, allowlist entry count before/after (4 → 3). commit. one line of mechanism. one line of what is unverified.
**Coordinator:** free tier first: `verify_rows.py <branch> chip-use --expect chip-use=0` plus the regression set (wave, window, mettaur, popup, buster, result, tiles, gauge, field, opening) — none may move. A verifier is needed only if the branch edits tools/allowlist.py, to read the diff and confirm the deletion is justified by a 0 and not by a re-tuned region. Dispatch alone (owns tools/allowlist.py); F42 runs after it.

### F42. field integrated: 1589 px to 0 and the second AUDIT-6 allowlist entry retired  *(NEGATIVE -- 2026-09-15, CLOSES as NEGATIVE)*

**Result.** CLOSES as NEGATIVE; branch wt/f42-field-integrated @ 0a4d840 kept UNMERGED (its only content is docs/worklog/F42.md, which this stamp carries to main; merging the branch would revert T15's landed tools/inventory.py + docs/SCOPE.md, since it branched before 152ce3c). CONFIRMED: the premise number does not exist -- field integrated measures 158935/5606/40 with negative 261034 non-blind (coordinator verify_rows PASS on field isolated 0/0/40 neg 1139 and chip-use 0/0/30 neg 7768; verifier-hyper reproduced 158935/5606/40/261034 on its own clean build), so the entry cannot be deleted and tools/allowlist.py stays 4 -> 4 with max_px 28000 and the date untouched. CONFIRMED: this is genuinely NOT a never-resolves case, unlike F41 -- FIELD_ZERO (tools/harness.py:1564) inherits flags=0x31 from ZERO_ENEMY_RESOLVED (:1489), bit5 being SceneFlags::RESOLVE_OVER (src/fixture.rs:92, read at :145), so src/battle.rs:2621 short-circuits true and :2632 sets dissolve_in=Some(0); the fight resolves immediately and the window frames compare clean. CONFIRMED: no aligning shift -- offset scan s=0..149 has a unique minimum at the row's own pairing, and the shift test reads (0,0) as the minimum with any +/-1/+/-2 at least 2x worse, so the residue is not an artefact of where the window starts. REFUTED BY VERIFIER-HYPER -- the sentence that must not be cited by the next ticket: 'BG1 owns all 158935 px. BG2 contributes 0.' The containment was guaranteed, not found: the BG1-only mask used for it is 100% SATURATED (all 38400 px differing) on 34 of the 40 frames, and those frames carry 139125 of the 158935 px (87.5%), so 'integrated ∩ BG1 = integrated' discriminates nothing there; and per the repo's own note at tools/harness.py:1362-1370, --only-bg N is a same-content comparison on this row ONLY for k=0..4 because from capture 121 our side drops one hardware BG (BG0 backdrop, BG1 panels, BG2 HP box, BG3 RESULT window) -- the verifier's per-frame BG1 density jumps 29.0% at k=3..4 to 92.5% at k=5 to 100.0% for k=6..39, exactly that documented boundary, so for 35 of 40 frames --only-bg 1 compared different hardware planes and its 1370701 is an artefact of that. The layer attribution therefore survives only on k=0..5, 19810 px = 12.5% of the row. Two more verifier findings: BG2-only is NOT measurable as prescribed -- --only-bg is a per-Side extra= passthrough (tools/harness.py:292-293), not a harness CLI flag (argparse at :3211-3214 takes only --only/--list/--no-gallery/--ui), so a BG2 split for this fixture needs a new row, not a command line; and the worklog's own-offset label 's=116' is wrong -- the numbers it lists reproduce at 108, the offset the verifier recomputed. So the NEGATIVE's DIRECTION (a port of canon's GFXAnim art-content engine is not available as a 1-ticket fix, and the cheap T9e seed lever was already spent on this row: BG1-only 660992 -> 645888, ~15k of 660k) stands, but its EVIDENCE for BG1 is 12.5% coverage and the row's layer decomposition is still open. UNVERIFIED: BG2's share has never been measured at all; which GFXAnim schedule step diverges on which frame is unidentified (next step named: dump eGFXAnimStates[0] per frame on both sides). Children: worker-hyper glm-5.3-flash:high x2 (first pass halted after baseline with 0 commits, resumed and finished), verifier-hyper qwen3.8-flash:high.
**Why.** Follows F41: same fixture family (`_zero_enemy_canon("Start@10")`, `FIELD_ZERO`, `tools/harness.py:1964-2001`), same class of residue, the next-cheapest non-zero row: the 2026-09-15 scoreboard prints `field integrated FAILED (allowed: AUDIT-6 ... <=28000) total 1589` — i.e. 1589 px against a 28000-frame-worst tolerance, the entry's own comment admitting the HUD "has never been compared against a genuinely empty (zero-enemy) arena before". `field isolated` is 0/0/40, so — exactly as for chip-use — the OBJ layer is exact and the 1589 px (~40/frame averaged over 40) sit in BG1/BG2/BG3. The row's note also flags the shape to look for: the result window BG3 slide-in at canon ~154..167 and the mark OBJ at canon 163..166 vs our capture ~121, plus the k=34 navi transition. F41's layer verdict applies here directly, so this ticket runs second on purpose. Milestone **M3/M8** (the field's own pixels and the HUD/HP displays), and the "no allowlist entries" invariant.

**Files.** src/field.rs, src/hud.rs, src/hudtiles.rs, src/backdrop.rs, src/fixture.rs, src/results.rs, tools/allowlist.py (deletion only), docs/worklog/F42.md.

**Do.** 1. Baseline `python3 tools/harness.py --only field --no-gallery`; report both variants' total/worst/frames and the negative fixture's total. 2. Decompose per k: `tools/diffmask.py` on the integrated variant, report the per-frame totals and the bbox of the worst frame — say whether the 1589 is spread over all 40 frames or concentrated (a slide frame). 3. Decompose per layer: run the field pair with the `chip_compare` BG flags so BG1, BG2, BG3 are each alone on both sides; name the layer(s) contributing, with each layer's total. 4. Take the first layer named and find its writer in reference/bn6f with a cite (file:line), then port or seed-fix in src/; re-run step 1 and report both numbers. 5. Repeat 4 for the next layer only if step 3 attributed more than 200 px to it. 6. At a measured 0 on both variants, delete `"field:integrated"` from tools/allowlist.py and run the full `--ui integrated` table to show nothing else depended on it.
**Rules.** Baseline first, same command after. No row-alignment, region or frame-count changes; `canon_ref=130`, `search=range(60,110)` and the event-lock pairing are not to be retuned — if the residue is an event-timing artefact, that is a NEGATIVE with the frames named. No new allowlist entry and no `max_px` edit; only deletion at 0. No `patch_sterile.py`. Negative fixture must stay non-blind at every step. Do not re-open the sequencer timeline (T7f/T7x/T7y): if the residue turns out to be the result-window slide timing, name it and stop — that is a precise negative, and it belongs to the end-sequence state machine, not here. ≤10 capture runs. Any literal edited gets a provenance name.
**Acceptance.** `field integrated 0/0/40` (isolated unchanged at 0/0/40, negative non-blind), `"field:integrated"` absent from tools/allowlist.py, and the full table showing 62 of 67 rows at 0 with no row worse than the 2026-09-15 scoreboard. Or a NEGATIVE carrying the per-layer attribution table (BG1/BG2/BG3 totals) and the cite of the mechanism that owns the remaining pixels.
**Measure and report.** rows: field (isolated + integrated). frames: 40/40. totals/worst before and after, per-k and per-layer attribution, negative totals, allowlist count before/after. commit. one line of mechanism. one line of what is unverified.
**Coordinator:** dispatch only after F41 lands or returns, and read F41's layer verdict into the worker's task. `verify_rows.py <branch> field,chip-use,tiles,gauge,opening --expect field=0,chip-use=-` first; a verifier only for the per-layer attribution claim (that is the finding F-next would build on). If both F41 and F42 retire their entries, record the allowlist count (4 → 2) in the landing message.

### T15. M1's last two tables: the status sources and the NaviCust program-id table, from the ROM  *(PARTIAL -- 2026-09-15, LANDED as 152ce3c -- all three lines measured and written into tools/inventory.py with cites checked twice [ve)*

**Result.** LANDED as 152ce3c -- all three lines measured and written into tools/inventory.py with cites checked twice (verifier-hyper, then the coordinator re-grepping each corrected cite on disk), but the M1 counts do NOT move: docs/SCOPE.md still reads 0/69, 0/19, 0/3, so the ladder's rung stays unmet and only the evidence behind each line changed. FOUND: NaviCust's asm/asm37_0.s:2111 navicust_jt_NCPs -- 47 .word entries at stride 4 (45 navicust_NCP_* + navicust_GigFldr1 + a push/lr-pop no-op stub), dispatched by applyNavicustPrograms_813C684 (:2012) at :2046-2050/2067-2071/2090-2094 via lsr #2/lsl #2, index from sub_813B9FC(id-1) which is r10[oToolkit_Unk2004190_Ptr]+8*id (an 8-byte record array, low 2 bits masked) -- and its bodies are 32 SetCurPETNaviStatsByte + 11 GetCurPETNaviStatsByte in :2161-2600, so it is a battle-effect HANDLER table, not the enumeration behind the line's 19 NaviStats slots (those stay DERIVED-FROM-HEADERS). First pass said 48 and called it a program-id table; verifier-hyper refuted both (47 words at 2112-2158; several programs share one handler) and the correction is what landed. Statuses: no bit-indexed table exists -- nearest per-status data is off_80209EC (data/dat01.s:155, 6 pointers stride 4) whose records are stride 8 ([+0] flag2 mask, [+4] hword, [+6] CollisionData offset), consumer sub_801A554 (asm00_2.s:22208-22230, table load :22218) indexed by (StatusEffectFinal>>4)-1 with (StatusEffectFinal&7)<<3, families of 6,6,6,7,6,6 = 37 records: neither 69 nor 19, so the line stays DERIVED-FROM-HEADERS with the mismatch quantified. Backdrops: an honest written GAP -- BattleSettings+0x4 has NO reader anywhere in asm/ beyond the store at asm03_0.s:14594, battleSettings_setBackground has exactly two callers (asm03_0.s:14617, asm33.s:16529), and CopyBackgroundTiles (asm00_0.s:3105, thumb_func_start 3102, thunk to iCopyBackgroundTiles asm38.s:424) gets tile ids from per-scene compare chains (asm33.s:4168-4195) over byte_812489C/81248C0/81248E4/812492C -- a compare chain over four arrays, not a table, so the next worker need not re-walk it. Population measured whole, not sampled: 0x07 x192 + 0x08 x1 + 0xff x268 = 461 = List0 269 + List1 192; near-degenerate (List1's 192 records all read 0x07), recorded as a caution, not as a count. No denominator, status column or compared region touched (verifier diffed the count columns before/after: identical); no src/ in the diff, worker's two-build cmp = 0 differing bytes, result row verify_rows PASS 0/0/40/111839 as the build check. UNVERIFIED: the 47 handlers are not individually classified battle-vs-PET, and exhaustive-ness of a negative search (no backdrop table) cannot be proven. Children worker-hyper glm-5.3-flash:high x2 (first pass + correction pass) + verifier-hyper qwen3.8-flash:high; this run's ledger total 3.43 USD.
**Why.** M1 is the lowest unmet rung and T14 closed its panels line (FOUND: `word_3007924`, 8/13 verified) — three lines are still not table-backed. `tools/inventory.py` reads statuses as the named struct_const bits of headers (`parse_statuses`, tools/inventory.py:581-594 → SCOPE `statuses (M3) DERIVED-FROM-HEADERS 0/69`), declares NaviCust a **GAP** for the program-id enumeration with its search trail recorded in-file (tools/inventory.py:688-713 → SCOPE `0/19`), and finds no backdrop table at all (tools/inventory.py:650 → SCOPE `backdrops (M8) 0/3`, "search trail: 'backdrop', 'arena'"). Those 91 items are the per-item rows every M3, M7 and M8 ticket has to name, so until they are table-backed the ladder's counts are guesses and per-item status vocabulary (`unrecorded/recorded/ported/verified`) has nothing to index. Docs-and-tools only: a byte-identical ROM, so zero regression risk and the cheapest milestone advance available (T14 cost $0.06 for its panels line). Milestone **M1** (and the counts M3/M7/M8 are measured against).

**Files.** tools/inventory.py, docs/SCOPE.md, docs/inventory/, reference/bn6f (read-only), docs/worklog/T15.md.

**Do.** 1. Record the baseline: `python3 tools/inventory.py` and quote the three section headers as they print (`statuses (M3) 0/69`, `navicust battle effects (M7) 0/19`, `backdrops (M8) 0/3`). 2. Statuses: start from the apply/get pair the headers name, walk to the routine that indexes per-status data, and report whether a single table exists — symbol, file:line, stride, record count, and the number of distinct statuses it enumerates (compare against 69). 3. NaviCust: read the Give/Take pair's address arithmetic to the base of the program definitions it indexes, dump that region with the `tools/rom_enemy_tables.py` pattern, and report the symbol/file:line/stride/count and how many battle-relevant programs it enumerates (compare against 19). 4. Backdrops: enumerate the distinct `BattleSettings.Background` byte values with their record counts, then find what maps that value to art/palette (one table, or an arithmetic offset — say which, with the cite and the stride) and report the count of arenas that way (compare against 3). 5. Encode each verdict in tools/inventory.py's section notes and regenerate: `python3 tools/inventory.py` must rewrite the SCOPE table, and quote the three new lines. 6. Prove the build did not move: `cargo build --release` twice and `cmp` the packed .gba against the baseline, reporting the diff count (must be 0).
**Rules.** No src/ and no other tools/ file; reference/bn6f is read-only and never committed. Never edit docs/SCOPE.md below the generated line by hand — regenerate it. A count is never inferred from a header's bit names alone, and never fitted: no table found is a GAP with its search trail written into the tool's own comment, and a GAP is a result, not a failure. Do not touch `tools/harness.py` or any allowlist entry. ≤25 tool calls before reporting.
**Acceptance.** For each of the three lines, either FOUND (table symbol + file:line + stride + count, with the count reconciled against the line's current total — 69, 19, 3 — or the mismatch quantified) and SCOPE's summary column updated by the regenerated tool, or a written GAP naming the routines walked and the pointer arithmetic that failed to reach a base. Acceptance is the regenerated SCOPE showing FOUND, or showing a GAP whose trail is new information rather than the trail already recorded at tools/inventory.py:694.
**Measure and report.** rows: none (byte-identical ROM, `cmp` diff count 0). Three section lines before and after, each with symbol/file:line/stride/count. commit. one line of mechanism. one line of what is unverified.
**Coordinator:** no row check needed — the ticket's own `cmp` step is the no-op proof (T14 precedent, landed 55c6b51). A verifier only if a FOUND table is claimed, to open the cited file:line and count the stride. Dispatch in parallel with F41/F42: disjoint `**Files.**`.

### F43. The HP-readout emission gate: opening integrated's four spurious digit OBJs, from canon's HUD updater call site  *(PARTIAL -- 2026-09-15, PARTIAL, branch wt/f43-hp-readout-gate @ 1542df0+5bf7ff6 kept UNMERGED [a row regressed])*

**Result.** PARTIAL, branch wt/f43-hp-readout-gate @ 1542df0+5bf7ff6 kept UNMERGED (a row regressed). The gain is real and coordinator-measured in the branch's own worktree: opening integrated 25829/931/40 (neg 105749) -> 18740/647/40 (neg 98649, still non-blind), opening isolated stays 0/0/40 (neg 86591), gunner 2105613 -> 2094676/38237/130 (same mechanism, canon-aligned, its own intro digits), mettaur 0/0/70, result 0/0/40, windowclose 0/0/40, chip-cannon 0/0/40 all PASS, fitted constants 19 before and after (verifier re-grepped: no provenance tag added, removed or retagged). THE BLOCKER: cursor 1/1/170 (neg 186279) -> 54/43/170 (neg 186281), reproduced on two independent verify_rows runs by the coordinator -- the worker's report claimed cursor 1/1/170 MATCH, which does not hold on its own branch. verifier-hyper diagnosed it from code, no capture needed, and CONFIRMED the ROM structure: canon reaches updateBattleHudElements_801BEE0 from exactly one call site, asm00_1.s:9790 inside battle_update_8007A44, gated on oBattleState_Index_00 == 4 (handover written at :9716-9721 by the intro's last-materialised step) and eStruct2038160_getBattleTerminate01() == 0 (:9757-9776), with BattlePaused checked AFTER the chain (:9795) -- so omitting a paused term was right and cursor's own row proves it (canon frozen under BattlePaused for 170 frames yet drawing the digits). What is wrong is which arms F43 chose: intro_fade == 0 and intro_next >= enemies.len() are our fixture plumbing, not canon's predicate. For cursor's fixture (tools/harness.py:1707, CUSTMATCH flags=0x11 = OPEN_WINDOW|SKIP_INTRO) src/battle.rs:2088-2097 picks INTRO_SKIP_FADE = 0x10*2 (:3381) and counts it down only inside the intro ramp (:2867-2869), so a row that starts 3171 battle frames into a live fight closes the gate for 32 compared frames; and intro_next advances only when !is_busy() (:2873, src/actor.rs:769-771) while F37b's frozen-pose seeding (:2805-2833) parks this enemy non-Idle forever under paused including custom.is_some() (:2919-2924) -- that arm can be false for all 170 frames, which fits 54 px/43 sites far better than 32 frames does. Banner and SEQ_0C|SEQ_10 arms are vacuous on this row (banner is the closing ENEMY-DELETED one, armed only when over; Sequencer::battle() starts at SEQ_08), so the two fixture-plumbing arms are the whole cause. Forward-blocking claim that must NOT be reused: 'The intro's own states never reach the chain. Modelled as Battle::hp_readout_live (intro_fade/intro_next/banner-done + !SEQ_0C|SEQ_10)' -- the faithful predicate is the battle state index, not our intro ramp. Two cite defects the follow-up must fix: every asm00_1.s cite in the report is 2-11 lines early (9780 -> 9790, 9747-9750 -> 9753-9756, 9784 -> 9795), and labelling eStruct203F7D8_getUnk01() & 8 a 'chip-window suppress' is unsupported -- it is a battle/transition state word (asm01.c:63-79 writes 2/4; asm00_1.c:11121 tests bits 1 and 3), and the mask applies to getUnk01's own value, not to dispatch_801FEEE's result. Deviation judged: placing the gate on Battle not Actor is defensible for the fight-state arm (single per-battle call site) but the intro arms are per-object materialisation facts in canon; an Actor predicate would NOT have avoided the regression since both breaking arms are battle-level fields -- the arms were wrong, not the placement. VERIFIER ENVIRONMENT NOTE: mid-run its calls were re-routed between two hosts and the other tree held a DIFFERENT hp_readout_live (blackout_timer/battle_terminated, SEQ_CHIP_CANNON|SEQ_TEAM_BANNER) -- if another run is implementing F43 concurrently, its numbers and this branch's are not comparable. Child worker-hyper glm-5.3-flash:high; verifier-hyper qwen3.8-flash:high.
**Why.** The 2026-09-15 scoreboard (main at 1f81767, 60 of 67 rows at 0) prints `opening integrated FAILED total 25829 worst 931 frames 40 | negative: not blind (total 105749)` — the only failing row with no allowlist cover besides gunner and cursor, and `opening isolated` is 0/0/40. F39a (NEGATIVE at 0c44aaa) already did the attribution and named the fix site as *outside its own files*: OBJ counts canon/rust 12/14 at k=0, 12/14 at k=20, 15/19 at k=32, 15/19 at k=39; the two rust-only pairs sit at y=84 x=172/180 (k=0..39) and y=132 x=172/180 (k=30..39) and are the **enemy HP readout digits** ('4','0' = Mettaur's 40 HP; `GLYPH_W`=8, src/hud.rs:133-135), emitted by src/battle.rs's per-enemy loop — the filter at src/battle.rs:4968-4972 (`is_present() && hp()>0 && is_targetable()`) with its single `self.hud.draw_number_in` at :4991, the only HUD emission call in the file. Canon draws **no** HP digits through its whole intro; F39a refuted the per-object VISIBLE flag as the gate (sub_801641A *sets* VISIBLE 0x02 during materialise, asm00_2.s:16120-16156) and left the live hypothesis named: canon's HUD update is a gated call — `updateBattleHudElements_801BEE0` (asm00_2.s:25587-25635) is called from the per-frame battle chain at asm00_1.s:9790, inside the `!(v5 & 8)` / `eStruct2038160_getBattleTerminate01()` guard (docs/decomp/asm00_1.c:5905-5923), i.e. under an intro/terminate-state predicate we do not honour. Same class of draw-hide difference carries T9l/F40a's gunner band (canon draws no bar and no HP boxes for compared-k 6..75, ~2047/frame on BG3, on top of the 2105613/38237/130 that row now reads). Milestone **M8** (HP displays) and **M2**'s integrated parity.
**Files.** src/battle.rs (the per-enemy readout emission loop :4968-4998 and the state/flag it reads), src/actor.rs (one new predicate on `Actor` mirroring canon's guard, next to `is_targetable` :538), docs/coverage/opening_integrated.md, docs/worklog/F43.md.
**Do.** 1. Baseline: `python3 tools/harness.py --only opening --ui integrated --no-gallery` → report total/worst/frames and the negative's total (HEAD 25829/931/40, neg 105749); note F39a's caveat that `verify_rows.py --expect` keys on row name only, so quote the direct harness call for the integrated variant. 2. `tools/probe.py --oam --pal` both sides at k=0, 20, 32, 39 → report OBJ count per side per frame and per entry x/y/tile/palette; list the entries with no canon counterpart (expect 12/14, 12/14, 15/19, 15/19). 3. Read asm00_1.s:9760-9800 and asm00_2.s:25587-25635 → report the exact predicate that guards the call to `updateBattleHudElements_801BEE0`, with the symbol/word and offset it tests, and the frame range canon's intro holds it false in. 4. Implement that predicate at our emission site (a state/flag read — never `k < N`), re-run steps 1 and 2 → report total/worst before/after and the OBJ counts per side (the two rust-only pairs must be gone at k=0 and k=32). 5. Regression: `python3 tools/verify_rows.py <branch> opening,mettaur,cursor,result,windowclose,chip-cannon --expect ...` plus `--only gunner --no-gallery` → report every line, and say whether gunner's k=6..75 band moved (report its per-k totals honestly).
**Rules.** Only the named files. Do NOT edit src/hud.rs, src/hudtiles.rs, src/field.rs, src/backdrop.rs, src/fixture.rs, src/results.rs or tools/allowlist.py — F42 (in flight) owns them; if the gate can only live in one of them, stop and report the exact file:line as a boundary. No frame-count or per-object palette literal (fitted-constant count HEAD 19 must not rise; report the count). No change to any row's frames, `align`, `canon_ref`, `search` band or compared region. Canon never changes; no `patch_sterile.py`. Negative fixtures stay non-blind at every step. ≤6 captures, one at a time; tool budget ≤70; a ticket bigger than it looked comes back as a report.
**Acceptance.** OBJ counts equal canon's at k=0 (12) and k=32 (15), opening integrated total strictly below 25829 with the gate carrying an asm00_1.s/asm00_2.s cite in the diff, and every isolated row unchanged (mettaur 0/0/70, chip rows 0/0/30, cursor ≤1/1/170, result/windowclose 0/0/40); 0/0/40 closes the row outright. A precise negative that names canon's guard word and shows our loop already honours it also closes it.
**Measure and report.** rows: opening (integrated + isolated), gunner, cursor, mettaur, the chip rows. frames: 40/130/170/70/30. total and worst before/after at each step, the negatives' totals, the OBJ count table per side per k. region: y=84 x=172/180 and y=132 x=172/180. commit. one line of mechanism. one line of what is unverified.
**Coordinator:** dispatch first and alone (src/battle.rs; T17 follows it). Re-baseline opening integrated after F42 lands — F42 may move that number. Free tier: verify_rows on opening plus the isolated regression set; a verifier only for step 3's guard-predicate claim, which is what the next ticket builds on. ≤$0.30 expected, ≤$0.60 cap, tool budget ≤70. Advances **M8**/M2.

---

**Ticket T17**

### T17. Chips as data, first family: the sword/blade arm driven from ChipDataArr's own attack bytes  *(OPEN -- 2026-09-16)*

**Why.** docs/SCOPE.md's M4 line reads "43 chips pixel-verified through our own code; **none 'as data' yet**", and M1's chips line reads 43/411 `verified-pixels`. The data exists and we throw it away: `chip_data_struct` (reference/bn6f/include/rom_structs/ChipData.inc, ChipDataArr_8021DA8, 411 × stride 0x2c) carries AttackFamily +0xb, AttackSubFamily +0xc, AttackParam1..4 +0x10..0x13, EffectFlags +0x9, LockoutFrames +0x14, LockOnEnable +0xf, LockOnType +0x17 — and `tools/chip_export.py` writes only id/element/mb/power/codes + the icon/image/palette offsets (struct.pack at :221, record 36 B, `RECORD` in src/chips.rs:11). So src/battle.rs:4277-4338 (`chip_strike`) is a `match chip.id` over eleven sword/blade ids whose comments already name the ROM rows the literals came from: byte_80EBA18's first byte per subfamily = hit shape, 1 panel ahead / 4 column ahead / 2 two panels (asm31.s:109246); byte_80EBAD8 = the arc's animation per subfamily, 0x18 Sword, 0x16 WideSwrd and every elemental, 0x17 LongSwrd (asm31.s:109264), its rows indexing byte_80E0398 / effect entry 0x14 sprite_830F144 (asm31.s:109180-109200); the elemental swords' palette = (subfamily − 0xb) ORed into Param3 (asm31.s:109198-109206, sub_80E0568 asm31.s:85852). The interpreters that make a data-driven chip possible landed already: T4b's animation bytecode player (6a2876e), T5's object dispatcher (163293b), T8's script VMs (752146e). The 43 chip rows are free regression tests, so this is the cheapest real M4 advance available. Milestone **M4**, and M1's chips line.
**Files.** tools/chip_export.py (append the behavioural bytes to the record), assets/chips.bin (regenerated by the tool), src/chips.rs (the record stride + accessors), src/battle.rs (only `chip_strike`'s sword/blade arm :4277-4338), docs/worklog/T17.md.
**Do.** 1. Baseline: `python3 tools/harness.py --no-gallery --ui isolated` → report the 43 chip rows (expect 0/0/30 each) and the count of distinct `CHIP_*` ids named inside `chip_strike` (expect 11). 2. Extend `tools/chip_export.py` to dump EffectFlags/AttackFamily/AttackSubFamily/AttackParam1..4/LockoutFrames from ChipDataArr_8021DA8, and report the sword/blade eleven's values side by side with the literals in src/battle.rs:4309-4330 → report every agreement and every mismatch (this is the table-vs-code parity test; a mismatch is the finding, not something to patch around). 3. Regenerate assets/chips.bin append-only and prove nothing moved: `cmp` the first 36 bytes of every record against the pre-change asset → report the diff count (must be 0) and the new stride. 4. Add the accessors in src/chips.rs and rewrite the arm to read shape/animation/palette from the record; delete the eleven ids → report the new `CHIP_*` id count inside `chip_strike` and the fitted-constant count (HEAD 19, must not rise; report the drop). 5. Re-run step 1 → report all 43 chip rows again (each must still read 0/0/30, negative fixtures non-blind). 6. `python3 tools/harness.py --only chip-use,cannon,field,tiles,gauge --no-gallery` and `verify_rows` on the isolated regression set → report the lines (mettaur 0/0/70, cursor ≤1/1/170, result/windowclose 0/0/40 unchanged).
**Rules.** Record changes are append-only; existing offsets never move. No per-chip id survives as a special case inside the arm — that is the deliverable. If step 2 shows the ROM's bytes do not reproduce a measured literal, stop and report it per chip with both values; do not add a fix-up table and do not keep a `match` fallback. Only the named files: src/hud.rs, src/hudtiles.rs, src/field.rs, src/backdrop.rs, src/fixture.rs, src/results.rs and tools/allowlist.py belong to F42 and are off-limits. No new harness row, no region/alignment/frame change, canon never changes, ≤8 captures, tool budget ≤90. Any literal you edit gets `// provenance:` or the `// canon:` symbol; re-check every cited file:line against disk (the 357da2a re-cut moved bn6f's numbers).
**Acceptance.** The 43 chip rows read 0/0/30 with all eleven sword/blade ids gone from `chip_strike` (before/after counts quoted), every record's first 36 bytes byte-identical to the old asset, and the fitted-constant count ≤19. Otherwise a report naming, per chip, the ROM byte that contradicts the measured literal — that converts the remaining 33 chips from guesswork into a list and is an accepted outcome.
**Measure and report.** rows: the 43 chip rows (isolated), chip-use, cannon, field, tiles, gauge, mettaur, cursor. frames: 30/30/40/40/8/70/170. totals before/after, the `CHIP_*` id count in `chip_strike` before/after, the record stride before/after, the cmp diff count, the fitted-constant delta. commit. one line of mechanism. one line of what is unverified.
**Coordinator:** dispatch after F43 (both name src/battle.rs). Free tier: verify_rows across the isolated set — a chip row that moves is a revert. A verifier is needed for step 2's table-vs-literal parity claim, since the next 33 chips build on it. ≤$0.40 expected, ≤$0.80 cap. Advances **M4**; it is sequenced ahead of M3/M5 because M2's remaining clauses are data-path work and its sequencer timeline is exhausted (T7x NEGATIVE, T7y NEGATIVE, T7z NOT DISPATCHED).

---

**Ticket T18**

### T18. M3's first rung: every status bit's setter, reader and battle-visibility, out of the disassembly  *(OPEN -- 2026-09-16)*

**Why.** M3 ("field and damage system … statuses …") is **not started**, and its per-item rows have nothing to index: docs/SCOPE.md's statuses line reads `DERIVED-FROM-HEADERS … 0/69`, which T15 (landed 152ce3c) confirmed by measurement rather than by search — no bit-indexed table exists; the nearest per-status data is off_80209EC (data/dat01.s:155, 6 pointers stride 4) whose records are stride 8 ([+0] flag2 mask, [+4] hword, [+6] CollisionData offset), consumed by sub_801A554 (asm00_2.s:22208-22230, table load :22218) indexed by `(StatusEffectFinal>>4)-1` with `(StatusEffectFinal&7)<<3`, family sizes 6,6,6,7,6,6 = **37 records — neither 69 nor 19**. Meanwhile our statuses are hand-picked flag combinations: src/ai.rs:41 documents `OBJECT_FLAGS_IMMOBILIZED` (bit 14, freeze) and the blind/confuse pair, and src/objects.rs:296 cites off_8109FF0[2]/sub_810A0BA (asm31.s:171311-171376) for blind/confused — nobody has established which of the 69 named bits canon ever *reads*, so no M3 scenario can be chosen on evidence. This is the SCOPE vocabulary's DERIVED-FROM-CODE state (routine + per-item sites), it is docs/tools-only, and it is the cheapest unmet-rung advance left: a byte-identical ROM, zero regression risk (T14's precedent, 55c6b51). Milestone **M3**, and M1's statuses line.
**Files.** tools/inventory.py (the statuses section), docs/SCOPE.md (regenerated, never hand-edited), docs/inventory/, src/ai.rs and src/objects.rs (**comments only**), reference/bn6f (read-only), docs/worklog/T18.md.
**Do.** 1. Baseline: `python3 tools/inventory.py` → quote the statuses line exactly as it prints (0/69, DERIVED-FROM-HEADERS). 2. Walk every `object_setFlag`/`object_setFlag2` call site in reference/bn6f/asm and headers, and per named status bit report: bit name, setter file:line, clearer file:line, and whether any routine *reads* it (reader file:line) — report the counts: distinct bits written, bits with at least one reader, bits with none. 3. Reconcile with off_80209EC's 37 records: for each record, which bit its [+0] flag2 mask reaches; report the set difference **both ways** (table records with no named bit, named bits unreachable from the table) and the two counts. 4. Encode the verdict in tools/inventory.py so the line prints DERIVED-FROM-CODE with the routine and the per-site rows, regenerate, and quote the new line plus the per-item table's row count. 5. Name the three statuses most worth an M3 scenario — those with a reader *and* reachable from a chip we already fire on an existing fixture — with the inflicting chip id and the reader cite for each → report the three names, chips and cites. 6. Prove the no-op: `cargo build --release` twice and `cmp` the packed .gba against baseline → report the diff count (must be 0).
**Rules.** No src/ behaviour change: src/ai.rs and src/objects.rs take comments only, and step 6's cmp is the proof. Never hand-edit docs/SCOPE.md below the generated line — regenerate it. A count is never inferred from a header's bit names alone and never fitted (T15's rule); "no table" stays a written GAP with its trail. Do not touch tools/harness.py, tools/allowlist.py, or any of F42's files (src/field.rs, src/hud.rs, src/hudtiles.rs, src/backdrop.rs, src/fixture.rs, src/results.rs). reference/bn6f is read-only and never committed. ≤25 tool calls, ≤2 captures (zero if step 6 needs none beyond builds).
**Acceptance.** The regenerated SCOPE statuses line reads `DERIVED-FROM-CODE` and carries (i) per-bit rows — name → setter file:line → reader file:line → battle-visible yes/no — with the three counts from step 2, (ii) the 37-vs-N reconciliation quantified in both directions, and (iii) three named M3 scenario candidates each with its inflicting chip and reader cite; the .gba cmp reads 0 differing bytes. A NEGATIVE that shows fewer than 3 written bits have any reader also closes it — that is the answer M3 needed.
**Measure and report.** rows: none (byte-identical ROM, cmp diff count 0). The statuses line before/after; the three counts from step 2; the step-3 set differences; the step-5 three names with cites. commit. one line of mechanism. one line of what is unverified.
**Coordinator:** no row check — step 6's cmp is the no-op proof (T14/T15 precedent). A verifier only to open two of the cited file:lines and confirm the bit name and the reader. Dispatch in parallel with F43: `**Files.**` lines are disjoint. ≤$0.10 expected.

