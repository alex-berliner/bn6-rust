# Work that can be handed to a subagent

Each entry is meant to be pasted into a ticket more or less as it stands. Anything
without a way to *measure* whether it worked is not ready to hand out — write the
measurement first.

## Every ticket runs in its own git worktree

    tools/worktree.sh <name>

prints a directory, a branch, and the `CARGO_TARGET_DIR` to export. The agent works THERE and
commits to ITS OWN BRANCH; the coordinator merges back with `git merge --no-ff`. Nothing else
gets an agent's half-finished edits, and nobody has to remember not to build at the wrong moment.

Why this exists: on 2026-09-07 four separate incidents came from agents and coordinator sharing
one checkout. A build taken while a worker held `src/` compiled its half-finished file and
returned a normal-looking number; the same collision later made a build fail outright; and I told
a worker its correct measurement was contamination on the strength of one of those numbers.

What the script handles that a bare `git worktree add` does not:
- `reference/bn6f` is a submodule and a fresh worktree checks it out EMPTY. It is symlinked to
  the main checkout, because it is only ever READ -- disassembly comments are made by the
  coordinator in the main tree, so an agent must not commit inside it. The script marks the
  path `--skip-worktree` so that a stray `git add -A` cannot stage the symlink over the
  gitlink -- doing that once merged a self-referential symlink into main and deleted the
  disassembly from the working tree. Agents should stage by path regardless.
- Each worktree needs its own `CARGO_TARGET_DIR` or the builds serialise on one lock. A build in
  a fresh worktree takes about 30 seconds and produces a BYTE-IDENTICAL ROM to the main tree's --
  verified, three ways, so a number measured in a worktree is directly comparable to one measured
  anywhere else.
- Capture scratch paths are keyed on the checkout path by `chip_compare.scratch()`, so two agents
  cannot overwrite each other's packed ROM between its build and its capture. That needed no
  argument and no convention; it just works out.

An agent that commits on its own branch is also free to commit MIDWAY, which the old "never
commit" rule forbade -- and that rule existed only because everyone shared one tree.

## Rules for every ticket

- **Give each agent its own `CARGO_TARGET_DIR`** under `/tmp` (`tools/worktree.sh` prints one). `cargo` locks the shared
  target directory and every build overwrites the same `target/.../bn` that
  `tools/gbafix.py` reads, so two agents building at once will silently pack each
  other's ROM. This has already caused two wrong captures.
  This rule was itself a trap until 2026-09-07: `regress.py`'s `build()` hardcoded
  `ROOT/target/...`, so setting `CARGO_TARGET_DIR` made cargo build in one place and the
  ROM get packed from a stale ELF in another -- no error, just numbers for a different
  build (`mettaur` 95436 instead of 345). It now asks `cargo metadata` where the target
  directory actually is, so the rule and the harness finally agree. Any OTHER script that
  packs a ROM should be checked for the same assumption before being trusted under a
  private target dir.
- **Baseline first.** The agent must record the number with its own script *before*
  changing anything, and re-run the identical script afterwards, and report both. A
  change nobody can show improves a number is not a fix — see 7ah in `TRANSFER.md`,
  where a well-cited change backed by a disassembly line and the animation data turned
  out to move nothing and to be worse on a wider window.
- **Say which window was measured.** Two agents measuring the same residue in different
  frame windows reached opposite conclusions once. Windows go in the report.
- Only `src/` changes unless the ticket says otherwise. Never `tools/`, `reference/`,
  `assets/` or `Cargo.toml` without a reason in the report.
- A precise negative result is a good outcome and gets written into `TRANSFER.md`. A
  plausible-sounding change that does not move the number is not.
- **DO THE WORK YOURSELF.** Do not delegate a ticket onward to a further agent. You
  are the worker. A ticket that turns out to be bigger than it looked comes back as a
  report saying so, not as a subcontract. (Which model runs which role is decided in
  `.pi/agents/` and HANDOFF §13, not by the worker.)

## Rules for whoever is handing the tickets out

Written after breaking all three of these in one evening.

- **Do not build or measure from the tree while a worker holds `src/`.** Not just "do not edit" --
  a build taken mid-edit compiles whatever state the worker's file is in at that instant and gives
  a number that looks completely normal. This produced a `tiles` reading of 3295 and then one of 0
  within the hour, the second being a build with the worker's fix reverted. Reuse an already-built
  ROM from /tmp, or wait.
- **When a worker's number disagrees with yours, rebuild it yourself before contradicting them.**
  On the strength of the above I told a worker its correct measurement was contamination. It had
  done three from-scratch builds and was right. Its EXPLANATION was wrong (it blamed target-path
  length; three target directories, one deliberately long, give a byte-identical ROM) -- but a
  wrong explanation is not a wrong measurement, and the two must be judged separately.
- **`git add -u <path>` limits to that path.** A commit meant to carry the fix, the harness change
  and two write-ups carried only the submodule bump, and the rest sat unstaged behind a clean-
  looking `git status` line. Check `git show --stat` after committing, not just the exit code.

---

## R. Root states as recipes (HANDOFF §13 step 1)

- R1 DONE -- Rebuild the fixture chain from power-on. 
- R2 NEGATIVE -- Move the chip rows onto the never-had-an-enemy fixture. At the event-pinned alignment (canon CurState leaves 0x0804 at frame 3
- R3 PARTIAL -- A battle that never spawns an enemy. The roll's choice lands in `GameState->CurBattleDataPtr` at `0x02001b9c` (stored at
asm29.s:10286, read by `sub_800531C` asm00_1.s:4455)
- R4 BLOCKED -- Keep the enemy-less battle alive, then finish R3. Step 1 done, and it overturns the recon: none of the five literal `SubsystemIndex=0x10`
stores, the warp entry, `cs_warp_cmd` or the script 
- R5 PARTIAL -- A write watchpoint in the capture tool, then find what releases the empty battle. Step 1 done, verified, committed (17dc00f, plus HANDOFF §5 row)
- R6 NEGATIVE -- The state oracle: first divergent field, not just a pixel count. On `wt/r6-oracle` at `c504eee`, the export was pixel-neutral: `wave` 0/0/90 (negative 3840), `window` 0/0/16 (81056), `mettaur` 30864/1566/7
- R7 DONE -- Land the state oracle on the facts R6 found. Merged `a9d08b7` as `2b35d4b`
- R8 WITHDRAWN -- Usage logging: primitive profiles for the 43-chip corpus. 
## F. Convergence: every existing row to 0 differing frames before anything new (user, 2026-09-12)

Tickets run in this order; the coordinator takes the first OPEN one. Queue by leverage, from the
2026-09-12 full table (isolated unless noted): F4 opening 1160 (was 0) -> F5 the chip-row fixture
(28 rows at exactly 14388, 11 more above it) -> F6 tiles/gauge 538 (one capture, two rows) -> field
1048 -> banner 2248 -> buster 3172 -> warp 9198 -> card 18486 -> mettaur 31075 -> F3 (the chip window's
ghost; adds its row) -> popup 107511 -> result 497967 -> cursor 620802 -> the integrated variants.
No new content (viruses, bosses, chips) until the table is at zero. A reported 0 always says what it
compares: canon vs ours, or before vs after.

- F1 DONE -- MegaMan takes a hit one frame late. PARTIAL
- F2 DONE -- The mettaur row compares the wrong Mettaur attack. DONE and merged as `332e658`
- F4 DONE -- The opening row reads 1160 px since R1 rebuilt its canon state. The 1160 was the FIXTURE, not the opening
- F5 PARTIAL -- The chip rows' fixture: fire a chip after the corpse has dissolved. The prescribed route is dead, the gate is found (with corrections), and the next
intervention is named but untested
- F5b DONE -- Fire a chip in state 0x0C by delivering the press to MegaMan's AIData directly. The Sol-named gap closed with a measured fire
- F6 DONE -- tiles/gauge isolated: 538 px over 8 frames. Landed at the documented residue
- F3 DONE -- After the chip window closes, a faded copy of it stays on the field. The ghost is gone by mechanism, not masking
- F7 DONE -- The legacy `cannon` row still compares against PAUSED: 14388. Exactly as predicted: `cannon` re-pointed onto F5b's route and Align (`_chip_canon_0c("01")`
+ `ALIGN_CHIP_0C` on `afterdissolve_0x0c`), 143
- F8 DONE -- `field` isolated: 1048 px over 40 frames. DONE
- F9 DONE -- `banner` isolated: 2248 px over 58 frames. DONE
- F10 BLOCKED -- `buster` isolated: 3172 px over 32 frames. BLOCKED after the second miss, moving on
- F10b BLOCKED -- `buster`: fire in the 0x08 window with the miss-pose fix, or close the row. 
- F11 DONE -- `warp` isolated: 9198 px over 30 frames. DONE
- F17 DONE -- `mettaur` isolated: enemy_anim k=61, the Mettaur's own animation. mettaur 19698/1245/70/60824 unchanged pixels, oracle enemy_anim 2/70->1/70 (first k=61, mm_timer 70/70)
- F20b DONE -- `tiles`/`gauge` integrated: match the fixture's sprite positions to canon's state. tiles/gauge integrated 25979/3282->3865/678/8 (negative 15891 not blind), isolated still 0/0/8
- F21 PARTIAL -- `result` isolated: 497967 px over 40, its canon side is result_arrival. result 497967/31882/40/476424 -> 408337/31895/40/383600 (-89630)
- F22 NEGATIVE -- `cursor` isolated: 620802 px over 170 frames. cursor 620802/6884/170/728447 unchanged (nothing landed, branch empty, deleted)
- F22b NEGATIVE -- `cursor` isolated: match the x>=112 unshared mid-battle content on the fixture side. cursor fixture backdrop-seed probe refuted, nothing landed
- F18b PARTIAL -- `windowclose` isolated: the k=11 transient and the slide residue outside the window layer. windowclose 666451/28784/40->651885/28430/40 (k=11 BG3 transient 2447->0
- F18c PARTIAL -- `windowclose` isolated: the relocated k=9/k=10 close-frame blank/redraw. windowclose 651885/28430->650544/27555/40 (BG3 k9 1898->0, k10 1408->0, k11 0->1596
- F18d DONE -- `windowclose` isolated: the k11 gauge-body single-step redraw. windowclose 650544/27555/40->648948/27391/40 (BG3 k11 1596->0, x48-191 y0-15)
- F21d DONE -- `result`: write the window's tilemap by block copy, not 576 managed tile writes. result 408337/31895->102547/14866/40 (neg 195579 not blind), field 1048/177->0/0/40
- F12 DONE -- The chip rows' own residues, family by family -- multi-pass, stays OPEN until every chip row is 0. every chip row reads 0: the last two were areagrab (orb draw order + burst palette walk, ad653c0) and chip-use, re-cut F31b-style so canon f
- F33d PARTIAL -- Post-0x0C simulation: act after the countdown starts instead of freezing in `over`. verifier verdict: canon citations CONFIRMED (sub_800801C:10422, off_8008038 table, sub_80081A4:10617, 0x0C@0x0800811E, sub_8012DFC:8977 not 
- F37 PARTIAL -- `cursor` and `windowclose`: the sprite layer, object by object. per-object tables landed 1bee9d8 (notes only, descriptors untouched): cursor 154361/909/170 (camera-pan +15Y + Mettaur pose 4,11-vs-4,8), wi
- F37b PARTIAL -- `cursor` and `windowclose` to 0: the camera pan while the chip window is open, the Mettaur's held pose, the hand icon and bracket. pan+pose+icon landed a6bf21c: cursor 154361/909/170->130221/767/170, windowclose 27819/1938/40->19168/1878/40 (neg 316499/214890 not blind)
- F37f PARTIAL -- `windowclose` k=0..9: the 162 px marcher on the slide frames (custom.rs). mark-during-Closing KEPT unmerged (branch wt/f37f 0c05e00, custom.rs only): windowclose 1458/162/40->0/0/40, cursor 20->3/3/170
## T. Trace-driven porting (phase "porting", 2026-09-14; the plan in HANDOFF §1)

**Common to the T tickets.** The harness rows stay as free regression tests (every landing runs verify_rows on
wave, window, mettaur, popup, buster, result, field and the row a ticket names). Acceptance in this phase is
stated as state parity: "first divergence at frame N or later on scenario S", with pixels as the gate on the
same recording. Canon never changes; provenance rules as before; cite reference/bn6f file:line.

- T12 DONE -- The enemy roster out of the ROM's own index tables: every enemy_idx with its think and act entry. LANDED as 1d23394
- T7d PARTIAL -- Drive the sequencer's window states with a fixture that closes the window, and put canon's predicates under the edges. Kept UNMERGED at wt/t7d @ 24d9ef7 (8daa88d + 24d9ef7 on a merge of wt/t7c @ 9be540c)
- T9c PARTIAL -- The Gunner: implement what T9b measured (the frame-60 lever, enemy_kind, the routine, the art, the row). CORRECTION (re-stamps my earlier entry, which cited verifier findings before the verifier had returned -- the merge message 1f7efc9's claim 
- T10 DONE -- Inventory of the battle engine from the ROM's own tables: the completion checklist gets its counts. LANDED as d0e0574 (two passes: 3723b5d + fix 82c31bd)
- T7c PARTIAL -- The sequencer's missing states: the custom-screen states our side never reads, and the end edge nine frames early. Kept UNMERGED at wt/t7c @ 510b5c6 (2 commits 3def49e, 510b5c6)
- T9b PARTIAL -- The second virus (Gunner): widen the fixture to field a non-Mettaur, build the canon recipe, port its routine. Measurement pass, zero edits (worktree /tmp/bnwt/t9b-gunner clean at 974f154) -- T9's blocker is SOLVED and reproducible, the implementation
- T8 DONE -- Port the script VMs, per section 3 of the plan (map-script and chatbox text-script dispatch). Landed 752146e (branch wt/t8-script-vms @ cdd976e)
- T9 BLOCKED -- The second virus as a ported per-type routine, from a real battle recording. Blocked on a file-set widening I cannot grant
- T6 DONE -- The Mettaur as canon's per-type routine: replace the hand-written brain with the ported AI entry. Mettaur brain now canon per-type entry MettaurEntry ForMettaur_8109EF4 (objects.rs), hand-written MettaurState removed (ai.rs), battle.rs 1 
- T7 PARTIAL -- The battle end as canon's sequencer table: banner states, teardown, results hand-off. Sequencer states + TRC2 v3 export landed pixel-neutral (verify_rows: isolated all 0 incl mettaur, cursor 10/9 tear-smaller, both-rows 0
- T7b DONE -- Re-establish the sequencer evidence: fresh v3 traces, loud judge, kill-to-slide gap. Landed 0fb54d4 (branch wt/t7b-sequencer-evidence @ dce93d4, carrying wt/t7's Sequencer states)

**Common to F8-F23 (and their b-tickets) unless the ticket says otherwise.** Baseline the row (harness line plus
`tools/diffmask.py` region, plus `tools/oracle.py` where the row is supported); localize the residue to
frames and an element; find canon's routine for that element in reference/bn6f and cite it; fix src/
(or the fixture/descriptor when the residue is the fixture, with `peeked` provenance); re-run the row,
wave, window, opening, chip-cannon and the full table -- nothing may get worse, and a row that does is
reported with its numbers. No allowlist change; no alignment or region change except by measured event
(F2's rule). **Coordinator:** verify_rows on every row the report names; the verifier only for claims
beyond harness lines; a PARTIAL from a wrong guess about the cause gets one follow-up ticket; if that
also fails, mark the ticket BLOCKED and move on to the next OPEN ticket.

- T13 DONE -- Audio parity, first measurement: both sides dumped and compared sample-exact. LANDED as 9d52135
- T9d DONE -- Why the poked Gunner battle never goes live: write-watch the sequencer on the working scenario and name the difference. no writer for dword_203CA70 on either battlestart route (200f watch-write, both empty)
- T13b DONE -- The two audio defects T13 measured: the ungated hit sample, and the 6.6 ms onset offset. src/battle.rs: hit_in now armed only when the strike's take_damage() landed
- T7e DONE -- End-edge nine frames early on battle_full and the two wrong citations in src/battle.rs. battle_full sequencer 540/540 (165 window-setup k=31..195 + 8 kill-timing k=297..304, both named)
- T7f PARTIAL -- Closing-window fixture: drive 0x24 -> 0x00 -> 0x04 -> 0x08 and replace the fitted edges with canon's predicates. windowclose_full scenario confirmed 0x00 enter+leave (144..146, 22..24) and 0x04 enter+leave (147..206, 25..84) on both sides (100/100 seque
- T9e NEGATIVE -- Gunner live trigger: find what writes dword_203CA70 on the poked battle and add the row aligned by the Gunner's first attack event. trigger poke unblocks sequencer (Index_01 0x02034881 4->0->8->0x0C at f165
- T7h NEGATIVE -- battle_full's first divergence: the enemy's opening action and animation (canon CurAction 0x0A, ours 0x00). spawn arm ported: METTAUR_ACT_SPAWN=0 const + MettaurEntry init + SPAWN arm in think() at src/objects.rs:243-251,298-308,343-362
- T7i BLOCKED -- battle_full's MegaMan tail: the k=179 action/timer/anim group, then name the RNG residue. third M2 ticket in this run after T7f (PARTIAL: windowclose_full validates but SEQ04_FRAMES=60 stays peeked because asm timer arms 0x1e/0x29
- T9f NEGATIVE -- Does `eS20364C0.JumpOffset00 = 8` start the poked battle: one-shot poke test, and the first attack event's frame. one-shot poke-at 155:0x020364C0:0x08 set eS20364C0.JumpOffset00=8 and held through f199
- T9g NEGATIVE -- The Gunner attack event: the 234-frame settle window (sub_8112D9C) and MegaMan's panel row, so the row ticket becomes writable. Three captures, three negatives on docs/trace-only commit (no src/tools changes, wt/t9g-gunner-extended 801ecda, landed d43e4c0)
- T13c BLOCKED -- Audio comparison on a second scenario: chip-fire and chip-damage SFXs on the cannon route, gated by the cited predicates. Worker audited src/ before editing
- T7j PARTIAL -- Model the banner composite for sub_801E754's leave predicate: replace SEQ04_FRAMES=60 with the canonical banner-idle check. branch wt/t7j-banner-composite aa3486d (docs-only on src/battle.rs, SEQ04_FRAMES=60 unchanged in code
- T13d NEGATIVE -- Audio probe on the cannon route: what canon fires, what ours doesn't, then gate. docs-only commit landed as e87cc7f

- T7l BLOCKED -- battle_full opening action: seed canon's HOP pose at k=0 so the spawn fade no longer parks CurAction=0x00. T7l BLOCKED: worker hit tool soft-limit (80) on baseline only
- F21e DONE -- result isolated: decompose the remaining 102547/14866/40 per-object, name the next mechanism. F21e was a no-op: HEAD result isolated already 0/0/40 PASS (neg 111839, not blind) via F34b's intro_fade=0/start_state==1 + megaman_col=2 + 
- F37h NEGATIVE -- cursor: close the 3 px residue at k=37/97 from the window mark landed in F37g. F37h NEGATIVE: cursor 10/9/170 unchanged on HEAD (regression set: windowclose 0/0/40, mettaur 0/0/70, result 0/0/40 all PASS)
- F37i BLOCKED -- cursor: close the 3 px residue at k=37/97 via the BG1 backdrop drain in src/backdrop.rs. F37i BLOCKED: worker hit tool soft-limit (60) on assembly research only
- T7m BLOCKED -- battle_full MegaMan tail at k=179: the mm_state_action/mm_anim/mm_timer group, finish the release-edge gate port. T7m BLOCKED: worker hit tool soft-limit (60) on baseline capture only
- T7n PARTIAL -- battle_full RNG cadence: name the rng_cadence first-divergence at k=271 on 10/540 frames, port the read-site if a clean mirror exists. T7n PARTIAL: rng_cadence divergence named with citation
- F37j NEGATIVE -- cursor: port canon's QueueEightWordAlignedGFXTransfer drain into src/backdrop.rs so the k=37/97 BG1 seam closes. F37j NEGATIVE: both drain-timing ports regressed cursor
- T7o PARTIAL -- battle_full release-edge at k=179: relocate the SEQ04 seq-match block past t1_player_entry so the gate frees before the executor. T7o PARTIAL: SEQ04 seq-match block relocated from src/battle.rs:2533 to :3268 (after t1_player_entry), matching canon's call chain at asm00_
- T7p DONE -- battle_full RNG cadence: port the per-site mirror at sub_80C7EC8 / cbGameState_80050EC so rng_cadence drops below 10/540. T7p was a no-op: HEAD rng_cadence 9/540, already below 10/540 acceptance threshold (T7o's PARTIAL landing 784c8e5 reduced it from 10→9 by re
- F38c PARTIAL -- opening integrated 72499: port canon's intro-hunk palette/sheet assignment and the enemy's materialize animation. F38c PARTIAL: opening integrated 72499 measurement-only diagnostic
- T9h PARTIAL -- Gunner as data: port canon's per-type routine from byte_80182C4, add the harness row. T9h PARTIAL: Gunner as data per-type routine cite landed
- T7g NEGATIVE -- Land T7c's custom-screen states from the unmerged branch so battle_full's sequencer coverage extends. T7g NEGATIVE: ticket premise wrong — divergence is a fixture-level difference, not a src/battle.rs sequencer-dispatch port
- T7q PARTIAL -- battle_full k=179 group: add the seq.state gate in Actor::update so the MegaMan executor's per-tick work respects SEQ_20/SEQ_24/SEQ_00/SEQ_04. T7q PARTIAL: seq.state gate added to Actor::update (t1_player_entry early-returns Update::Nothing when seq.state in {SEQ_20, SEQ_24, SEQ_00,
- F38d PARTIAL -- opening integrated 72499: port the PAL_OBJ slot allocation in src/actor.rs and the second-cluster materialize animation in src/objects.rs. F38d PARTIAL: opening integrated 72499 unchanged
- T9i PARTIAL -- Gunner as data: fix the harness capture race so the gunner row reads 0/0/N. T9i PARTIAL: gunner race fix landed

- T7r PARTIAL -- battle_full fixture: scripted L input so self.seq.state traverses {SEQ_20, SEQ_24, SEQ_00, SEQ_04}. T7r PARTIAL: battle_full fixture seeds gauge=1 + window_pick OK + scripted A@170
- F38e BLOCKED -- opening integrated 72499: fix the diagonal spawn cells (src/battle.rs:1907-1908) and the materialize y offset (src/actor.rs:528). F38e worker found two rule conflicts that prevent landing: (1) spawn-cell fix needs per-enemy panel data per spawnEnemy_80073E2 asm00_1.s:86
- T9j PARTIAL -- Gunner as data: port the per-type routine into src/gunner.rs and align the harness row on the Gunner's first attack event. T9j PARTIAL: gunner_update per-type routine port at ForGunner_8113078 (asm32.s:10123-10142) + 4-arm state machine sub_8112F70/sub_8112FBA/su
- T7s NEGATIVE -- battle_full sequencer-timing fix: window opens at k=124 vs canon k=31 so self.seq.state traverses SEQ_20 by canon's k~31. T7s NEGATIVE: ticket acceptance unreachable under rules
- F38f PARTIAL -- opening integrated 72499: per-enemy panel bytes via src/fixture.rs and the diagonal spawn-cell fix in src/battle.rs:1907-1908. F38f PARTIAL with revert: branch (9a938d0 + 3eea157 + 8ee4346) landed on main but introduced cursor regression 1/1/170 -> 26/25/170
- T9k NEGATIVE -- Gunner as data: move Battle::gunner_ctl + Battle::impacts in src/battle.rs into GunnerEntry so gunner_update drives per-tick visible content. T9k NEGATIVE: refactor preserves gunner behavior 2850534/38237/130 (unchanged from T9j PARTIAL -- the ctl/impacts migration is relocation-on
- F38g BLOCKED -- opening integrated 72499: F38f re-land with cursor fixture state file regenerated for 67-byte descriptor. F38g BLOCKED: per-enemy panel port cherry-picked/re-implemented (commit 7c3b33f on wt/f38g-reland, diff matches F38f: src/fixture.rs +17, sr
- F36a PARTIAL -- warp integrated ~363658: baseline the residue, decompose by frame and region, port the canon mechanism. F36a PARTIAL with no code change: actual warp integrated on HEAD is 40628/11744/30 (NOT 363658 headline -- F38b event-lock pin rust_offset=5
- F35a BLOCKED -- buster integrated ~643698: baseline the residue, decompose by frame and region, port the canon mechanism. no code change, tool budget soft-cap hit at 80 before port
- T7u BLOCKED -- battle_full sequencer: port sub_801E754's banner-idle check to replace SEQ04_FRAMES=60 in src/battle.rs. code change made, but caused cursor regression: cursor 1/1/170 -> 38/37/170 (37x worse)
- F36b BLOCKED -- warp integrated ~40628: per-region decomposition, then port the canon mechanism for each non-zero region. no code change, tool budget soft-cap hit at 80 before port
- F38h PARTIAL -- opening integrated 72499: descriptor-preserving per-enemy panel encoding so cursor fixture state file stays valid. opening integrated 72499/2691 -> 25829/931 (residual is PAL_OBJ allocation order src/spr.rs:701-761 per F38e BLOCKED, not in scope)
- F37k BLOCKED -- cursor 3 px residue at k=37/97 via per-scanline BG1 backdrop seam port. infra failures (bash exit 1, then model cold-start on resume)
- T7v BLOCKED -- battle_full RNG cadence 9/540 → 0/540: port the per-site mirror at the remaining divergence frames. no code change
- F38i PARTIAL -- opening integrated 25829: PAL_OBJ allocation order port in src/spr.rs to clear the per-object OAM colour residue. opening integrated already at 0/0 on main HEAD (8b314eb) per verify_rows — NOT 25829/931 as F38h PARTIAL commit message stated
- F37l BLOCKED -- cursor 3 px residue at k=37/97 via per-element BG1 seam cite at sub_8001C94 (asm00_0.s:3752). cursor 1/1/170 isolated: residue is 1 px at (183,5) on k=97 ONLY (k=37 already 0 after F38h merge 2d1e639), NOT 3 px at k=37/97 as ticket cl
- F36c NEGATIVE -- warp integrated 40628: port sub_8001C94's BG1 seam transition directly into src/backdrop.rs. Premise refuted by measurement, no code change
- T7w NEGATIVE -- Battle end as canon's per-state counts: name the layer of the integrated rows' 16-px band, then retire the fitted RESULTS_DELAY=110. Premise refuted by measurement
- T9l PARTIAL -- gunner 2850534/38237/130: derive this row's own backdrop seed, then split the residue by layer. LANDED as 424814d
- F39a NEGATIVE -- opening integrated 25829/931/40: the four OBJ entries canon does not draw, and the per-sprite PAL_OBJ slot. Premise refuted by measurement
- F40a BLOCKED -- gunner 2105613/38237/130: settle whether the BG3 plateau is gauge-driven, then port the chip-window auto-open that canon takes at capture ~157. Lever not settled

- T7x NEGATIVE -- battle_full SEQ_04's leave predicate is a bit test: port isBannerBusy_801E754, delete SEQ04_FRAMES and BANNER_FRAMES. Ported and measured, but NOT landable: the bit-15 lifecycle is real and our mask series comes out canon-shaped, yet spawning the banner reco
- T7y NEGATIVE -- The custom screen's open edge: canon's gauge→open chain replaces our peeked 60-frame countdown (gunner's two rows, battle_full's 83-frame window offset). No code landed
- T7z BLOCKED -- The window's own duration: what ends canon's SEQ_24 after 100 exports, then the 0x20/0x00 edges and battle_full's kill→0x0C hand-off. NOT DISPATCHED -- this is the third ticket in a row on battle_full's sequencer timeline and the two before it both came back NEGATIVE on the
- T14 PARTIAL -- Panels as a ROM table: canon's type-indexed flag word at 0x3007924 and PanelOffsetListsPointerTable, so M1's panels line stops being 13 routine names. LANDED as 55c6b51 (docs/tools only, no src/, byte-identical build, verify_rows skipped as a no-op
- Q1 DONE -- Name the last bare numbers in the HUD walk and the backdrop wrap. LANDED as 57c62b3 (all 61 compared rows identical across the merge
- Q2 DONE -- The scene's switches as a flags type, not a bare byte. LANDED as 61fc751 with its acceptance line amended at merge on the verifier's judgment
- Q3 DONE -- One safe wrapper for the boot mark and the state block. LANDED as 184b9b5, re-stamped with the verifier's own retraction so the record is not read as cleaner than it was
- Q4 DONE -- The forty-byte state block as named fields, not byte offsets. LANDED as 17012e8 (60/61 rows identical across the merge, verify_rows PASS)
- Q5 DONE -- Retire the dead `FLAG_*` names in tools/harness.py's row notes and document SceneFlags' raw-byte escape hatch. LANDED as 0c0d6f1
- Q6 NEGATIVE -- cursor's single-frame tear tracks how the boot marker's frame word is written -- decide which, and stop calling it noise. CLOSES as NEGATIVE, no code change (branch wt/q6-cursor-tear @ ddb031d kept UNMERGED, worktree removed)
- F41 NEGATIVE -- chip-use integrated: the last 2 px, then delete the chip-use allowlist entry. CLOSES as NEGATIVE, and the branch DOES land for its one honest change (allowlist re-title only)
- F42 NEGATIVE -- field integrated: 1589 px to 0 and the second AUDIT-6 allowlist entry retired. CLOSES as NEGATIVE
- T15 PARTIAL -- M1's last two tables: the status sources and the NaviCust program-id table, from the ROM. LANDED as 152ce3c -- all three lines measured and written into tools/inventory.py with cites checked twice (verifier-hyper, then the coordin
- F43 PARTIAL -- The HP-readout emission gate: opening integrated's four spurious digit OBJs, from canon's HUD updater call site. PARTIAL, branch wt/f43-hp-readout-gate @ 1542df0+5bf7ff6 kept UNMERGED (a row regressed)
- F44 DONE -- Gate the enemy HP readout on canon's battle-state latch, not our intro ramp -- finish F43 without cursor's regression. integrated constant re-gated under F48's fixed matcher (4df0c52) and it REPRODUCES: verify_rows HEAD opening --expect opening=18740/647/40/9
- F45 PARTIAL -- Attribute `field`'s 158935-px integrated residue with a saturation-gated per-layer capture, split at the `shown` boundary -- finish what F42 tried to claim. field's integrated constant RE-GATED under F48's fixed matcher: F45's landed '158935/5606/40 neg 261034' is superseded, not reproduced -- to
- F46 NEGATIVE -- Settle what field's backdrop residue IS: a register-level art-versus-timing split of the 139125-px bulk on the pairing F45 just landed. OUTCOME STANDS, REASON CORRECTED BY A LATER AUDIT (2026-09-15, F47's verifier
- F47 DONE -- Read the backdrop's REAL scroll registers on both sides, and name the captures the shift test runs on -- finish what F46 aimed at the wrong halfwords. STAMP AMENDED 2026-09-15 by T23 (landed a0468e7) -- the substance of F47 STANDS (canon's backdrop art steps at k=5,13,21,29,37 and ours at k
- F48 DONE -- Measure the run-to-run drift on the non-zero integrated rows, and give `verify_rows` a policy for it -- F46's verifier found the drift, this ticket sizes it. landed 4df0c52
- T17 DONE -- Chips as data, first family: the sword/blade arm driven from ChipDataArr's own attack bytes. the sword/blade arm is data-driven, landed 8c1a62d
- T19 DONE -- Make the chip-asset contract loud, and let SCOPE say that eleven chips are now data. landed 2baad23
- T18 DONE -- M3's first rung: every status bit's setter, reader and battle-visibility, out of the disassembly. statuses now DERIVED-FROM-CODE under a STRICT per-bit rule, landed 3f09aa7
- T20 BLOCKED -- Scripted-input scenarios: one button log drives both sides. NOT DISPATCHED -- needs a hand-played input, which is the coordinator's hard stop and yours to supply
- T21 BLOCKED -- The horizontal-blank tile writer: replace backdrop tiles at the original's scanline. HELD FOR YOUR DECISION, not dispatched
- T22 DONE -- Settle F47's open (c): rebuild the backdrop art-content test on the ROM's OWN per-step upload list, not the s\*37+k grid. landed 55549f2 (docs/SCOPE.md, docs/coverage/field_integrated.md, docs/worklog/T22.md, tools/inventory.py only
- T25 DONE -- Name the source of the <=2 px period-4 scroll translation on `field-bg1` -- the dominant term left after T24 harvested the art-phase one. landed 7129c23: FIELD_ZERO scroll_xq 466->471 + scroll_yq 745->748 takes field-bg1 354486/14743 -> 4800/4800 (-98.6%, residue = k=0 transiti
- T24 DONE -- Test the SEED verdict where it can actually be wrong: put our art-clock countdown at canon's 4 and see whether the backdrop lines move. landed 98fd371 (+ wording applied on main f2c7632
- T23 DONE -- Locate F47's 3-frame art-clock offset: read canon's OWN anim-state record at one paired frame and say SEED or FREE-RUN. landed a0468e7 (docs/coverage/field_integrated.md pure append + docs/worklog/T23.md
- T26 NEGATIVE -- Canon's battle-state 0x14 ladder: what each custom-screen edge waits on, and the pad path that arms it. landed 9963a00 (docs-only, 949f0f0 + cite corrections 35708fe): the per-state-count model of canon's custom-screen ladder is DEAD -- SEQ_20/
- T27 NEGATIVE -- The custom-screen open edge as canon's press→0x14 ladder: replace GAUGE_PAUSE and the two uncited `seq.age` edges. NOT MERGED (kept as the base for any dwell ticket): branch wt/t27-open-edge-ladder at 711d876 + 11ec3b7, worktree removed
- T28 NEGATIVE -- Chips as data, second arm: no chip id indexes an amount — Recov, Barrier and Vulcan read the record's own bytes. landed b4a941a (a4a22fb, worklog-only
- T29 DONE -- Reconcile the two contradictory scroll-seed derivations T25 left living in `tools/harness.py`, and settle whether `field-bg1`'s k=0 residue is the same band as `field-bg2`'s. follow-up pass MERGED as da68f18 (this run's verifier-hyper returned GO on 546fc6e after re-deriving everything from the dumps with its own 
- T30 DONE -- Repair the stale file:line cites baked into `src/` comments: eight `derived --` provenance tags point at asm lines that are not the table they name, and the `SPRITE_LOADER` warning prose is out of date. branch landed as 8eb9517
- T31 DONE -- Measure the `k=0..2` group of `field`'s integrated residue instead of arguing it away -- 49% of the 32623 is currently excluded from the scroll model by reasoning alone. MERGED 9985842 (tip b9a1224
- T32 DONE -- The gunner row's gauge band: name canon's custom-gauge word and `oBattleState_Index_00`, then seed `GUNNER_ROW` from the measurement. branch landed as 5a5fcd9
- T33 NEGATIVE -- M3's first rung from the ROM: locate the writer of every panel type by walking `_object_setPanelType`'s call sites, and close M1's 8/13. branch landed as ea5d284
- T34 NEGATIVE -- Chips as data, third pass: the Recov, Barrier and Vulcan amounts come from their own subfamily tables, not from a `match chip.id`. branch landed as 36c7bf6
- T35 BLOCKED -- M3's first panel-effect port: cracked panel breaks on MegaMan's step, from object_crackPanel at object.s:2218-2222. branch unmerged, worktree removed
- T36 PARTIAL -- M4's third chip-data arm: Cannon, HiCannon and M-Cannon read AttackPower +0x1a and Element, removing their `match chip.id` arms. branch landed as be6e706
- T37 NEGATIVE -- M5's first virus-port completion: settle Gunner's live attack event frame and bring gunner integrated below 1,500,000. branch landed as c1c98ce
- T40 NEGATIVE -- M3's first panel-effect port: holy panel halves the damage sum at sub_801A7F4 asm00_2.s:22788-22791, with a new panel-state fixture that loads type-5 panels. branch unmerged, worktree removed
- T38 NEGATIVE -- M4's Cannon family: port the three chips' `match chip.id` arm to AttackPower +0x1a, finishing T36 PARTIAL's deferred rewrite. branch unmerged, worktree preserved
- T39 NEGATIVE -- M5's Gunner port: watch-write the BG3 plateau region at k=77..129 for the chip-window auto-open trigger that T37 NEGATIVE couldn't reach. branch landed as 4c2ce9f
- T41 NEGATIVE -- M3's first panel-effect port via the per-panel-type flag word at word_3007924: OR the panel-type's flag bit before the panel-step damage path, bypassing T40 NEGATIVE's cursor-fragile sub_801A7F4 direct port. T41 NEGATIVE on branch wt/t41-panel-port (worklog-only commit 386dc8a from main 04397be)
- T42 NEGATIVE -- M5's Gunner port: watch-write BG3 hardware registers (BG3CNT / BG3HOFS / BG3VOFS / DISPCNT) at k=77..129 to find the chip-window BG3 enable flip T39 NEGATIVE named but couldn't reach. T42 NEGATIVE on branch wt/t42-gunner-bg3 (worklog-only commit eb3af7e from main 6714bc9)
- T43 NEGATIVE -- M8's HP-display emission gate on the damage-taken path: port canon's HP-digit OBJ emitter call site at the damage-event handler, distinct from F44 DONE's intro-path gate T38 NEGATIVE couldn't reach without cursor regression. T43 NEGATIVE on branch wt/t43-hp-digit-gate (worklog-only commit 87061b4 from main 73b46cc)

- T44 NEGATIVE -- battle_full sequencer first-divergence on T7r scripted fixture. wt/t44-battlefull-divergence @ 6b676b4
- T45 NEGATIVE -- M5 Gunner row aligned on first attack event. wt/t45-gunner-attack-event @ 79b6e6d
- T46 DONE -- M4 chips as data: Bomb family reads AttackPower +0x1a. wt/t46-bomb-family merged to main at 85f021c (resolving conflict: take branch's bomb_anim=(id, thrown) which verify_rows PASS proves correct
- T47 DONE -- M2 battle_full trace tool fix (oracle/diffmask/trace). wt/t47-trace-fix landed as 12f10c2
- T48 DONE -- M4 chips as data: Barrier family reads amount from the record. wt/t48-barrier landed as 079531c
- T49 DONE -- M2 battle_full first-divergence recon with fixed trace tools. wt/t49-battlefull-recon landed as 34a4dd8

### T50. M2: Port sub_8109CE6 (0x0A hop executor) into MettaurEntry to align enemy_state_action at k=0  *(DONE -- 2026-09-16, trace: enemy_state_action first divergence k=0 -> k=17 [oracle: canon=[4,8], rust=[4,10] at k=17, rust [4,10] )*

**Result.** trace: enemy_state_action first divergence k=0 -> k=17 (oracle: canon=(4,8), rust=(4,10) at k=17, rust (4,10) at k=0 matches canon (4,10)); sequencer divergence 1387 -> 1370 (-17); sub_8109CE6 cite at asm31.s:170689 in src/actor.rs; fitted 19 unchanged; verify_rows PASS on wave 0/0/90/3840, window 0/0/16/81056, opening 0/0/40/86591, chip-cannon 0/0/40/9505, mettaur 0/0/70/41734; verifier confirmed all 4 claims; landed 21fde8f; verifier-minimax + worker-minimax, both sub ~/bin/bash.40
**Why.** T49 (DONE 34a4dd8) recon on the fixed trace tools named first divergence at k=0 (frame 11), field enemy_state_action, canon=(4,10), rust=(4,0). 10,827,099 px diff / 35,308 worst at k=0 / 540 frames. Cite sub_8109CE6 (reference/bn6f/asm/asm31.s:170689, the 0x0A hop executor) + ForMettaur_8109EF4 (asm31.s:170982). T7h NEGATIVE (spawn arm at src/objects.rs:243-251,298-308,343-362) and T7l BLOCKED closed the previous attempts; this is a fresh port under T49's recon. Milestone **M2**.

**New evidence.** T49 DONE (2026-09-16, landed 34a4dd8) recon: oracle.py battle_full --both names first divergence at k=0, field enemy_state_action (canon=(4,10), rust=(4,0)), cite sub_8109CE6 + ForMettaur_8109EF4. T47 DONE (12f10c2) fixed tools/trace.py:56 ORCL_ADDR TypeError; T49 ran the recon on the fixed tools. T7p DONE drove rng_cadence below 10/540; battle_full trace remains at 273/540 with first divergence k=0 (T49 supersedes T26's k=31 sequencer reading).

**Files.** src/objects.rs (Style::Mettaur arm at :243-251 / MettaurEntry init / think() at :298-308, :343-362 — extend the same shape for the CurAction 0x0A hop arm), docs/coverage/battle_full.md, docs/worklog/T50.md. **NOT** tools/harness.py (T32 owns), tools/allowlist.py, tools/trace.py, tools/oracle.py, tools/states.py; reference/bn6f read-only.

**Do.** 1. Baseline, no edit: `python3 tools/oracle.py battle_full --both` → expect first divergence at k=0 enemy_state_action (canon=(4,10), rust=(4,0)); `python3 tools/verify_rows.py HEAD wave,window,opening,chip-cannon,mettaur --expect 0/0/N` → expect all PASS; sha256sum of the built ROM. **measurement.** 2. Re-derive from disk: read sub_8109CE6 (asm31.s:170689) and ForMettaur_8109EF4 (asm31.s:170982) on disk → report each cite's exact site that names CurAction=0x0A and the per-tick work it dispatches. **measurement.** 3. Compare to MettaurEntry's CurAction 0x0A arm in src/objects.rs (the equivalent ForMettaur_8109EF4 call site) → name every field and arm that the Mettaur arm drives and the gap to canon at k=0. **measurement.** 4. Port the CurAction 0x0A arm: extend MettaurEntry's shape to the same field set, replacing (4,0) → (4,10) at k=0. **code change.** 5. Re-run step 1: oracle.py names first divergence at k>=1 (or matching canon state-action sequence), all five guard rows identical to HEAD, fitted count unchanged at 19. **measurement.**

**Rules.** Only the named src/objects.rs ranges. No new count: an arm that cannot be expressed as T49's cited field is reported, not tuned. No row's frames/align/canon_ref/search/region change, no new fixture field, no allowlist edit, no patch_sterile.py, canon never changes. Any zero row that stops being zero, or cursor above 1/1/170, or windowclose above 0 → keep the branch unmerged and report. ≤5 capture runs, tool budget ≤90.

**Acceptance.** Trace target: field = enemy_state_action as exported by tools/oracle.py on the battle_full scenario, frames = 540, **first divergent frame moves from k=0 to k>=1** with canon state-action matching rust at k=0 ((4,10) == (4,10)); sequencer divergence count strictly less than HEAD's 273/540; sub_8109CE6 cite carried in the diff; fitted ≤19; verify_rows identical on the step-1 guard set as the absolute veto. A NEGATIVE naming the cite's actual frame and field, with the row that blocked a landing, also closes it.

**Measure and report.** rows: battle_full (oracle), wave, window, opening, chip-cannon, mettaur. frames: 540/90/16/40/40/70. totals/worst/negatives before/after per row, both sides' state-action k=0 sequence, the divergence count, both ROM sha256s, fitted count; region: enemy_state_action k=0..540; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch alone (it owns src/objects.rs). Free tier: verify_rows on the guard set using F48's fixed matcher. A verifier for step 2's cite vs asm claim (memory finding every later M2 ticket builds on). ≤$0.40 expected, ≤$0.80 cap. Advances **M2**.

---

### T51. M4: Fix T38's cursor regression on the Cannon family port — port Cannon/HiCannon/M-Cannon to AttackPower +0x1a without breaking cursor  *(NEGATIVE -- 2026-09-16, T51 worker diagnosed but did not fix)*

**Result.** T51 worker diagnosed but did not fix. Cursor regression (1/1/170/186279 -> 29/28/170/186277) traced to T38's change 5 (REVERT of T48 Barrier dispatch from chip.params[0]-indexed to match id) NOT the Cannon family port itself (changes 3-4 are byte-equivalent: chip.subfamily for ids 1/2/3 = 0/1/2 = id-1). F26b's cursor-OBJ band confines diff to chip-window OBJ; cursor deck has no Barrier/Cannon ids so use_chip/chip_strike cannot fire. chips.bin sha256 unchanged. T51 step 5 cannot reach M4=16 from T38's base=13 — would also need Barrier re-port. Branch wt/t51-cannon-fix (=wt/t38-cannon-port 4869370) unmerged. Sub ~/bin/bash.20 (diagnosis only, no code change). worker-minimax.
**Why.** T36 PARTIAL (landed be6e706) verified parity: Cannon 0x0028=40, HiCannon 0x0064=100, M-Cannon 0x00B4=180; Element 0x0A=CHIP_ELEM_NONE for all three. T38 NEGATIVE attempted the actual src/battle.rs rewrite (CANNON_FAMILY=0x14 const, both use_chip and chip_strike arms converted from match chip.id to early-return if chip.family==0x14, barrel palette (chip.id - CHIP_CANNON) replaced with chip.subfamily) but introduced **CURSOR REGRESSION: cursor 1/1/170/186279 → 29/28/170/186277** (28x worse). Branch unmerged, worktree preserved. T17/T46/T48 precedent: each chip-family port landed cleanly because the AS_DATA_FAMILIES addition changed the dispatch WITHOUT regressing other rows. Milestone **M4**.

**New evidence.** T38 NEGATIVE (2026-09-16) measured the exact regression: cursor 1/1/170/186279 → 29/28/170/186277 (28x worse at frames 170/170); branch wt/t38-cannon-family preserved; worker-minimax self-report missed the regression. The chip-export v2 contract (T17/T19) shows attack_family=0x14 for Cannon (ids 1-3) per data/ChipDataArr.s:33-126. T30 DONE (landed 8eb9517) repaired stale src/ cites, so the parity cite is at correct line numbers.

**Files.** src/battle.rs (Cannon arm in chip_strike :4277-4338 and use_chip arm — NEVER landed sword/blade arm, NEVER fight_latch/hp_readout_live from F44), src/chips.rs (no change — accessors exist), tools/inventory.py (AS_DATA_FAMILIES extension only), docs/SCOPE.md (regenerated only), docs/worklog/T51.md. **NOT** tools/chip_export.py, assets/chips.bin, tools/harness.py, tools/allowlist.py, src/hud*.rs, src/backdrop.rs, src/fixture.rs, src/spr.rs; reference/bn6f read-only.

**Do.** 1. Check out wt/t38-cannon-family (T38's preserved branch). **measurement.** 2. Baseline from the preserved branch's commit: `python3 tools/harness.py --no-gallery --only chip-cannon,chip-hipcannon,chip-mcannon,cursor` → expect chip-cannon/hipcannon/mcannon 0/0/40 (negative non-blind), cursor 29/28/170/186277; sha256sum. **measurement.** 3. Localize the cursor regression: run `python3 tools/diffmask.py cursor --frames 0..170` and identify the exact frame(s) where the new px appear and the OBJ layer → report each new px's row/col and which sprite layer it sits on. **measurement.** 4. Diagnose: compare T38's diff to T36's parity table — name the byte that the worker changed which the cursor's OBJ layout reads (likely the use_chip Cannon arm at :4206 or the early-return at the family==0x14 gate). **measurement.** 5. Port only the Cannon arm (no barrel palette change), keep chip_strike's match chip.id for non-data families intact, write the new branch → re-run step 2 → expect chip-cannon/hipcannon/mcannon 0/0/40 and cursor 1/1/170/186279 (zero regression). **code change.** 6. Run `python3 tools/verify_rows.py <branch> chip-cannon,chip-hipcannon,chip-mcannon,cannon,field,tiles,gauge,mettaur,windowclose,cursor,opening --expect ...` → report all lines; **any cursor regression keeps the branch unmerged**. **measurement.** 7. `python3 tools/inventory.py` and quote SCOPE's M4 line and M1 chips count before/after.

**Rules.** v2 asset is the file (T17/T19 contract; F48 saw T17's ROM change move field by -23 deterministically, so cite both shas). No match surviving a value step 3 proves equal — a mismatch is the finding (T17's rule). No fix-up table. Zero rows are the gate: any chip row non-zero, or cursor >1/1/170, or windowclose/mettaur/tiles/gauge/field non-zero → branch unmerged. provenance_counts() fitted ≤19 (scans only ROOT/src/*.rs). Every literal gets // canon: or // provenance:; re-check cites against disk. ≤6 captures, tool budget ≤80.

**Acceptance.** Cursor regression eliminated (cursor 1/1/170/186279 byte-identical pre-regression), Cannon arm ported so src/battle.rs:4277-4338 no longer matches chip.id for ids 1-3, AS_DATA_FAMILIES = {0x13, 0x15, 0x14}, all 43 chip rows still 0/0 with their negatives, SCOPE's M4 line moved (13 → 16 of 411 as data). A NEGATIVE naming which cursor OBJ the port affects closes it.

**Measure and report.** rows: chip-cannon, chip-hipcannon, chip-mcannon, cannon, field, tiles, gauge, mettaur, windowclose, cursor, opening. frames: 40/40/40/40/8/8/70/40/170/40. totals/worst/negatives before/after per row, both ROM sha256s, the localized cursor regression frame(s)+region, the byte the Cannon port reads that the cursor OBJ reads, AS_DATA_FAMILIES before/after, SCOPE's M4/M1 lines before/after; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch alone (it owns src/battle.rs). Free tier: verify_rows on the chip set plus cursor's regression set with landed expects. A verifier for step 4's diagnosis (memory finding every later M4 chip-data port builds on). ≤$0.40 expected, ≤$0.80 cap. Advances **M4**.

---

### T52. M4: AirShot (id 4) as data — port from match chip.id to chip.family == 0x21  *(DONE -- 2026-09-16, chip-airshot 0/0/40/4602 unchanged with non-blind negative)*

**Result.** chip-airshot 0/0/40/4602 unchanged with non-blind negative; cursor 1/1/170/186279 unchanged (known tear); all 9 regression rows identical; verify_rows PASS; AS_DATA_FAMILIES {0x13,0x15} -> {0x13,0x15,0x21}; SCOPE M4 13 -> 14; fitted 19 unchanged; landed 50e4d49; worker-minimax + verifier confirmed parity table all agree.
**Why.** AirShot is "verified-pixels" (43/411): the chip works through our own code but not from the chip record. The arm at src/battle.rs:4281 `CHIP_AIRSHOT => { ... }` is keyed by id 4; the canonical dispatch is chip.family=0x21 (per src/battle.rs:617 "AirShot (attack family 0x21, sub_80EC884)"). Per chip_data_struct (include/rom_structs/ChipData.inc), AttackPower=20 at +0x1a matches src literal, Element=CHIP_ELEM_WIND matches src default. T17/T46/T48 precedent: each chip-family port lands zero rows unchanged because F48's drift band cannot make a mispaired frame read 0. The v2 asset (T17/T19) ships the per-record attack_family byte. Milestone **M4**.

**Files.** src/chips.rs (no change — accessors exist), src/battle.rs (**only** the AirShot arm in use_chip at :4281-4293 and any chip_strike AirShot arm — never landed sword/blade arm, never fight_latch/hp_readout_live from F44), tools/inventory.py (AS_DATA_FAMILIES extension only), docs/SCOPE.md (regenerated only), docs/worklog/T52.md. **NOT** tools/chip_export.py, assets/chips.bin, tools/harness.py, tools/allowlist.py, src/hud*.rs, src/backdrop.rs, src/fixture.rs, src/spr.rs; reference/bn6f read-only.

**Do.** 1. Baseline, one session, with the built ROM's sha256: `python3 tools/harness.py --no-gallery --only chip-airshot` → expect 0/0 with non-blind negative (T5b row); quote SCOPE's M4 line. **measurement.** 2. Parity table before any edit: read attack_family=0x21 (chip_export.py v2 record), AttackPower=20 at +0x1a, Element=0x04=CHIP_ELEM_WIND straight out of ChipDataArr_8021DA8 at data/ChipDataArr.s:127 → report every agreement and mismatch. **measurement.** 3. Convert only what step 2 proves equal: replace the `CHIP_AIRSHOT =>` arm at src/battle.rs:4281 with a single early-return if chip.family == 0x21; delete the `CHIP_AIRSHOT` constant → report `CHIP_*` ids remaining in the AirShot arm before/after. **code change.** 4. Re-run step 1 → chip-airshot must still read 0/0 with the negative non-blind. **measurement.** 5. Regression: `python3 tools/verify_rows.py <branch> chip-airshot,cannon,field,tiles,gauge,mettaur,cursor,windowclose,opening --expect ...` → report every line; cursor 1/1/170/186279 unchanged. **measurement.** 6. `python3 tools/inventory.py` → quote SCOPE's M4 line and M1 chips count before/after.

**Rules.** v2 asset is the file (T17/T19 contract; F48 saw T17's ROM change move field by -23 deterministically, so cite both shas). No match surviving a value step 2 proved equal — a mismatch is the finding (T17's rule). No fix-up table. Zero rows are the gate: any chip row non-zero, or cursor >1/1/170, or mettaur/tiles/gauge/field/windowclose non-zero → branch unmerged. provenance_counts() fitted ≤19 (scans only ROOT/src/*.rs). Every literal gets // canon: or // provenance:; re-check cites against disk. ≤4 captures, tool budget ≤60.

**Acceptance.** The parity table with zero unexplained mismatches, no `CHIP_*` id indexing an amount in the AirShot arm (before/after counts quoted), chip-airshot still 0/0 with negative non-blind, all 43 chip rows still 0/0 with their negatives, AS_DATA_FAMILIES = {0x13, 0x15, 0x21}, SCOPE's M4 line moved (13 → 14 of 411 as data). A NEGATIVE naming the byte which contradicts the measured literal closes it.

**Measure and report.** rows: chip-airshot, cannon, field, tiles, gauge, mettaur, cursor, windowclose, opening. frames: 40/40/8/8/70/170/40/40. totals/worst/negatives before/after per row, both ROM sha256s, the parity table, the `CHIP_*` id count before/after, AS_DATA_FAMILIES before/after, SCOPE's M4/M1 lines before/after; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch alone (it owns src/battle.rs and AS_DATA_FAMILIES in tools/inventory.py). Free tier: verify_rows on the chip set plus the guard list with landed expects — any chip row that moves is a revert. A verifier for step 2's parity claim (the same ROM read T17's verifier did). ≤$0.20 expected, ≤$0.40 cap. Advances **M4**.

### T53. M2: Continue MettaurEntry after T50 — port the second arm that drives enemy_state_action 0x08 at k=17  *(NEGATIVE -- 2026-09-16, T53 port did not move the divergence)*

**Result.** T53 port did not move the divergence. Sequencer 1370/540 unchanged (not strictly less than HEAD's 1370). First divergence still at k=17 enemy_state_action (canon=(4,8), rust=(4,10)). Plumbed MettaurEntry::cur_action through Ai::oracle_cur_action into Actor::oracle_fields (Action::Idle if !is_player => ai_cur_action); cite ForMettaur_8109EF4[0x20]=MettaurDecide_8109FD6 at asm31.s:170982/171029 carried. New arm didn't fire because rust's mettaur sits in Action::Appearing (T50's 0x0a arm, higher precedence) and never reaches Action::Idle during the compared window. Fitted 19 unchanged. verify_rows PASS on guard set (wave 0/0/90/3840, window 0/0/16/81056, opening 0/0/40/86591, chip-cannon 0/0/40/9505, mettaur 0/0/70/41734). Branch wt/t53 0c35405 unmerged; next ticket should add a debug print naming which Action::* arm matches at k=17 and what MettaurEntry::cur_action reads. Sub ~.50 (long reasoning on cite vs Action-state modelling). worker-minimax.
**Why.** T50 DONE (landed 21fde8f, 2026-09-16) moved first divergence k=0 → k=17 on enemy_state_action (478/540 → 458/540 divergent; sequencer 1387 → 1370). Pre-T50 canon was (4,10) rust (4,0); post-T50 both sides match at k=0 (4,10). The next gap sits at k=17 where canon transitions from action 0x10 to action 0x08 (canon=(4,8) vs rust=(4,10), which held 0x10). Re-derive the second arm (ForMettaur_8109EF4 / asm31.s:170982 — cite the exact sub-arm that fires when CurAction transitions out of the 0x0A hop) and port it in the same shape as T50's first arm. Milestone **M2**.

**New evidence.** T50 oracle names the new first divergence at k=17, field enemy_state_action, canon (4,8) vs rust (4,10). T50's src/objects.rs diff already extended MettaurEntry to dispatch the 0x0A hop arm; the 0x08 arm sits in the same per-tick work block (cite specific lines in step 2). T49 (34a4dd8) recon fixed the trace tool; T47 (12f10c2) fixed oracle.py.

**Files.** src/objects.rs (Style::Mettaur arm / MettaurEntry init / think() at T50's ranges :243-251, :298-308, :343-362 — extend for the 0x08 arm), docs/coverage/battle_full.md, docs/worklog/T53.md. **NOT** tools/harness.py (T32 owns), tools/allowlist.py, tools/trace.py, tools/oracle.py, tools/states.py; reference/bn6f read-only.

**Do.** 1. Baseline, no edit: `python3 tools/oracle.py battle_full --both` → expect first divergence at k=17 enemy_state_action (canon=(4,8), rust=(4,10)); `python3 tools/verify_rows.py HEAD wave,window,opening,chip-cannon,mettaur --expect 0/0/N` → expect all PASS; sha256sum. **measurement.** 2. Re-derive from disk: read ForMettaur_8109EF4 (asm31.s:170982) and the action-0x08 dispatcher (cite the exact asm line + per-tick work it arms) → report each cite that names CurAction=0x08 and the field it touches. **measurement.** 3. Compare to MettaurEntry's 0x0A arm from T50 → name every field, byte and arm the new arm drives and what is missing for canon's (4,8) at k=17. **measurement.** 4. Port the second arm in the same shape as T50's 0x0A arm. **code change.** 5. Re-run step 1: oracle.py names first divergence at k>=18; all five guard rows identical to HEAD; fitted count ≤19. **measurement.**

**Rules.** Only the named src/objects.rs ranges. No new count. No row's frames/align/canon_ref/search/region change. Zero rows are the absolute veto: any zero row that stops being zero, or cursor above 1/1/170, or windowclose above 0 → keep the branch unmerged. ≤5 captures, tool budget ≤90.

**Acceptance.** Trace target: field = enemy_state_action on battle_full, frames = 540, **first divergent frame moves from k=17 to k>=18** with canon state-action matching rust at k=17 ((4,8) == (4,8)); sequencer divergence count strictly less than HEAD's 1370 (T50); ForMettaur_8109EF4 cite carried in the diff; fitted ≤19; verify_rows identical on the guard set as the absolute veto. A NEGATIVE naming the cite's actual frame and field closes it.

**Measure and report.** rows: battle_full (oracle), wave, window, opening, chip-cannon, mettaur. frames: 540/90/16/40/40/70. totals/worst/negatives before/after per row, both sides' state-action k=17 sequence (showing the new arm firing both sides), divergence count, both ROM sha256s, fitted count; region: enemy_state_action k=0..540; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch alone (it owns src/objects.rs). Free tier: verify_rows on the guard set using F48's fixed matcher. A verifier for step 2's cite vs asm claim. ≤$0.40 expected, ≤$0.80 cap. Advances **M2**.

---

### T54. M4: Vulcan1/2/3/SuprVulc (ids 5-8) as data — dispatch on chip.family, not match chip.id  *(NEGATIVE -- 2026-09-16, T54 verifier caught a cursor regression the worker missed)*

**Result.** T54 verifier caught a cursor regression the worker missed. Worker self-reported cursor 1/1/170/186279 (unchanged) but verify_rows with --expect and direct harness measurement show cursor 6/6/170/186280 on the branch (5 new px at frame 170, total +1). Cursor is above the 1/1/170 ceiling — branch unmerged per ticket rule. All 4 chip-vulcan rows 0/0 with non-blind negatives (6276/10142/12728/28047), chip-airshot 0/0/40/4602 unchanged, AS_DATA_FAMILIES extended {0x13,0x15,0x21} -> {0x13,0x15,0x17,0x21}, SCOPE M4 14 -> 18. The port itself is byte-correct (parity table all agree, VULCAN_SHOTS_BY_SUB [3,4,5,10] matches dword_80EBFEC, 4 chips carry family 0x17), but it touched a byte the cursor OBJ reads. Next ticket: localize the cursor OBJ affected (F26b band x2..45 vs x122..189) and decide whether to revert the cannon arm or find the canonical fix. Branch wt/t54 b96ba9e unmerged. worker-minimax + verify_rows caught it.
**Why.** Vulcan1/2/3/SuprVulc are 4-element-NONE chips with ascending damage (10/15/20/20) at data/ChipDataArr.s:158,189,220,251 — a second multi-chip family analogous to Cannon (1-3) but never attempted as data. The src arms at battle.rs:4277 (use_chip) and :4626 (chip_strike) remain keyed by `CHIP_VULCAN | CHIP_VULCAN2 | CHIP_VULCAN3 | CHIP_SUPRVULC => { ... }` (4-arm OR pattern with no per-record read), while battle.rs:505 already does `CHIP_VULCAN2 => VULCAN2_SHOTS` (per-record SHOTS — a small in-family data path). T17/T46/T48 precedent: each chip-family port landed cleanly because the AS_DATA_FAMILIES addition changes dispatch WITHOUT regressing other rows. Milestone **M4**.

**New evidence.** T51 NEGATIVE (713d5864, 2026-09-16) diagnosed the Cannon regression: "T38's change 5 (REVERT of T48 Barrier dispatch from chip.params[0]-indexed to match id) NOT the Cannon family port itself (changes 3-4 are byte-equivalent: chip.subfamily for ids 1/2/3 = 0/1/2 = id-1). F26b's cursor-OBJ band confines diff to chip-window OBJ; cursor deck has no Barrier/Cannon ids so use_chip/chip_strike cannot fire." So the chip-family port pattern itself does NOT regress cursor when the cursor-deck's chip.params[0] reads stay intact; Vulcan has no chip.params[0] reads in cursor's chip-window OBJ layout. T52 DONE (50e4d49) proved single-chip + family-typed dispatch lands clean.

**Files.** src/battle.rs (Vulcan arms in use_chip :4277-4293 and chip_strike :4626-4678 — replace both `CHIP_VULCAN | CHIP_VULCAN2 | CHIP_VULCAN3 | CHIP_SUPRVULC =>` arms with `if chip.family == VULCAN_FAMILY`; PRESERVE the SHOTS sub-arm at :505 as a follow-on; do not delete `CHIP_VULCAN*` constants if :505 reads them), tools/inventory.py (AS_DATA_FAMILIES extension only), src/chips.rs (no change), docs/SCOPE.md (regenerated only), docs/worklog/T54.md. **NOT** tools/chip_export.py, assets/chips.bin, tools/harness.py, tools/allowlist.py, src/hud*.rs, src/backdrop.rs, src/fixture.rs, src/spr.rs; reference/bn6f read-only.

**Do.** 1. Baseline, one session, with sha256: `python3 tools/harness.py --no-gallery --only chip-vulcan1,chip-vulcan2,chip-vulcan3,chip-suprvulc` → expect 0/0 with non-blind negatives; quote SCOPE's M4 line. **measurement.** 2. Parity table: read attack_family (=VULCAN_FAMILY value), AttackPower=10/15/20/20 at +0x1a, Element=CHIP_ELEM_NONE straight out of ChipDataArr_8021DA8 at data/ChipDataArr.s:158,189,220,251 → report every agreement and any mismatch. **measurement.** 3. Replace the two `CHIP_VULCAN | CHIP_VULCAN2 | CHIP_VULCAN3 | CHIP_SUPRVULC =>` arms at :4277 and :4626 with `if chip.family == VULCAN_FAMILY` (keep the SHOTS lookup at :505 untouched). **code change.** 4. Re-run step 1 → all 4 Vulcan rows must still read 0/0 with non-blind negatives. **measurement.** 5. Regression: `python3 tools/verify_rows.py <branch> chip-vulcan1,chip-vulcan2,chip-vulcan3,chip-suprvulc,cannon,field,tiles,gauge,mettaur,cursor,windowclose,opening --expect ...` → report every line; cursor 1/1/170 unchanged. **measurement.** 6. `python3 tools/inventory.py` → quote SCOPE's M4 line and M1 chips count before/after.

**Rules.** v2 asset is the file (T17/T19 contract; F48's fixed matcher gates drift). No match surviving a value step 2 proves equal — a mismatch is the finding. No fix-up table. Zero rows are the gate: any chip row non-zero, or cursor >1/1/170, or mettaur/tiles/gauge/field/windowclose non-zero → branch unmerged. provenance_counts() fitted ≤19. Every literal gets // canon: or // provenance:; re-check cites against disk. ≤4 captures, tool budget ≤60.

**Acceptance.** Parity table with zero unexplained mismatches, no `CHIP_VULCAN*` id indexing an amount in the :4277 / :4626 arms (before/after counts quoted), all 4 Vulcan rows still 0/0 with non-blind negatives, all 43 chip rows still 0/0, AS_DATA_FAMILIES extended with VULCAN_FAMILY, SCOPE's M4 line moved (14 → 18 of 411 as data). A NEGATIVE naming which chip.params[] byte contradicts the parity table closes it.

**Measure and report.** rows: chip-vulcan1, chip-vulcan2, chip-vulcan3, chip-suprvulc, cannon, field, tiles, gauge, mettaur, cursor, windowclose, opening. frames: 40/40/40/40/40/8/8/70/170/40/40. totals/worst/negatives before/after per row, both ROM sha256s, the parity table, the `CHIP_VULCAN*` id count before/after, AS_DATA_FAMILIES before/after, SCOPE's M4/M1 lines before/after; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch alone (it owns src/battle.rs and AS_DATA_FAMILIES in tools/inventory.py). Free tier: verify_rows on the chip set plus the guard list with landed expects. A verifier for step 2's parity claim. ≤$0.20 expected, ≤$0.40 cap. Advances **M4**.

---

### T55. M5: Continue gunner_update past T9j PARTIAL — apply T32's gauge band to BG3 plateau at capture ~157  *(NEGATIVE -- 2026-09-16, T55 worker localized the BG3 plateau to chip-window left-half [x=0..116, y=24..151] with 3-4x spike at capture)*

**Result.** T55 worker localized the BG3 plateau to chip-window left-half (x=0..116, y=24..151) with 3-4x spike at capture frame ~157. The src/battle.rs:2181 wiring (f.gauge != 0 ? GAUGE_FULL : 0) already exists. Port blocked by harness.py:156 & 0xFF mask on descriptor.gauge — T32's documented byte-identical contract lands 0x4000 as 0x00 in rust descriptor. Neither widening pub gauge: u8 to u16 (only byte 18 is written) nor editing harness.py fits the no-touch list. 4-arm sub_8112F70/sub_8112FBA state machine drives OBJ+BG1, not BG3 chip-window. verify_rows PASS on guard set. Fitted 19 unchanged. Branch wt/t55-gunner-plateau 6507edc unmerged. Next ticket: widen tools/harness.py gauge read (T32 owns harness.py) OR add a debug oracle field for the gunner scenario (oracle.py is in no-touch list). worker-minimax.
**Why.** T9j PARTIAL (wt/t9j-gunner-update, gunner.rs ForGunner_8113078 + 4-arm state machine sub_8112F70/sub_8112FBA/...) brought gunner per-type routine from raw residue to 2850534/38237/130 then 2105613/38237/130 after T9l PARTIAL (landed 424814d). T40a BLOCKED on "settle whether the BG3 plateau is gauge-driven, then port the chip-window auto-open that canon takes at capture ~157 — lever not settled." T32 DONE (landed 5a5fcd9) named canon's custom-gauge word and oBattleState_Index_00 — gunner now seeded from a measured gauge band, NOT the prior peeked value, which is the lever T40a couldn't settle. T42 NEGATIVE tried BG3 hardware-register watch-write (BG3CNT/BG3HOFS/BG3VOFS/DISPCNT) and couldn't reach; the 130-frame plateau stayed un-named by layer. Milestone **M5**.

**New evidence.** T32 DONE (5a5fcd9, 2026-09-16) named canon's custom-gauge word and oBattleState_Index_00 — the lever T40a needed settled. T9l PARTIAL (424814d) split gunner's 130 frames by layer (backdrop seed + chip-window timing) but not the BG3 plateau specifically. T49 (34a4dd8) + T47 (12f10c2) fixed oracle/trace tools so gunner and battle_full can be compared frame-by-frame under the same harness.

**Files.** src/gunner.rs (gunner_update + the 4-arm sub_8112F70/sub_8112FBA handlers — extend one step past T9j's PARTIAL port toward the BG3 plateau at capture ~157, using T32's settled custom-gauge word), src/battle.rs (only the gauge-band wiring if T32's cite requires it), docs/coverage/gunner.md, docs/worklog/T55.md. **NOT** tools/harness.py, tools/allowlist.py, tools/trace.py, tools/oracle.py, tools/states.py, src/backdrop.rs (T42 NEGATIVE's BG3 hardware territory stays out of scope until this layer hypothesis settles); reference/bn6f read-only.

**Do.** 1. Baseline: `python3 tools/harness.py --no-gallery --only gunner` → expect 2105613/38237/130 (T9l); `python3 tools/oracle.py gunner --both` → expect first divergence field+frame; `python3 tools/verify_rows.py HEAD wave,window,opening,chip-cannon,mettaur --expect 0/0/N` → expect all PASS; sha256sum. **measurement.** 2. Read T32's custom-gauge word cite from disk + ForGunner_8113078 (asm32.s:10123-10142) → identify the BG3 plateau frame in canon and the field(s) canon touches there (gauge word, battle-state index). **measurement.** 3. Watch-write `tools/trace.py record/diff --align row:gunner` at capture ~157 → name which bytes differ between canon and rust in the plateau window → categorize by layer (gauge bit / battle-state index / OAM). **measurement.** 4. Port the highest-priority layer from step 3 into src/gunner.rs (one handler arm, the smallest diff that closes at least one frame of the plateau); no chip-window background change outside this layer. **code change.** 5. Re-run step 1 → gunner integrated drops ≥25% (2105613 → ≤1,579,000); oracle first divergence moves later or matches canon; guard rows identical; fitted ≤19. **measurement.**

**Rules.** Only src/gunner.rs (and src/battle.rs only for T32's gauge-band wiring) — NOT src/backdrop.rs or src/spr.rs in this ticket. No new count: a handler arm that the disassembly cannot site is reported. Any zero row that stops being zero, or cursor above 1/1/170 → branch unmerged. ≤5 captures, tool budget ≤90.

**Acceptance.** gunner integrated ≤ 1,579,000 (from 2,105,613, ≥25% drop) with the BG3 plateau named per layer; oracle first divergence on gunner moves later or matches canon; guard rows identical to HEAD; fitted ≤19; verify_rows identical on the guard set as the absolute veto. A NEGATIVE naming which watch-write byte contradicts the cited disassembly site closes it.

**Measure and report.** rows: gunner (oracle), wave, window, opening, chip-cannon, mettaur. frames: 130/90/16/40/40/70. totals/worst/negatives before/after per row, both ROM sha256s, the BG3 plateau per-layer attribution, fitted count, ForGunner_8113078 cite; region: gunner BG3 plateau at capture ~157; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch alone (it owns src/gunner.rs). Free tier: verify_rows on the guard set using F48's fixed matcher. A verifier for step 2's cite vs asm claim. ≤$0.40 expected, ≤$0.80 cap. Advances **M5**.

### T56. M4: Is `cursor`'s veto a logic quantity? Code-size control test, then decide the gate that rejects every chip-data port  *(NEGATIVE -- 2026-09-16, The spread, named: cursor isolated = HEAD 86a5b18 [ROM 5b46337a...85ef, 584296 B] 1/1/170/186279 x4)*

**Result.** The spread, named: cursor isolated = HEAD 86a5b18 (ROM 5b46337a...85ef, 584296 B) 1/1/170/186279 x4; pad wt/t56-pad 6331ae2 (ROM 7a7ecf62...ffed, 584552 B, +256) 1/1/170/186279 x4; wt/t54 b96ba9e (ROM a15183be...3eb2, 583396 B, -900) 6/6/170/186280 x4. Align offset 237 in all 12 reps. NEITHER offered verdict holds: the pad moved nothing (so not a size/pairing artifact, refuting (a)) and no byte of T54's chip data is read (so (b)'s required citation does not exist); the 6 px are 5 extra px on HEAD's own pre-existing seam frame (183,5)k=97 -- verifier-hyper measured HEAD's residue = {(183,5)@k=97} a STRICT SUBSET of t54's {(183,5),(119,6),(120,5),(120,6),(121,5),(121,6)}, all k=97, and worst==total==6 forces one frame independently. Verifier CONFIRMS the pad control (src diff is only the never-read static; +256 B exactly; shas differ; cursor identical to its own HEAD run) and the subset; DOWNGRADES 'not an OBJ entry' to worker-only (no OAM/layer capture re-run; landed harness.py:2682-2683 x+128 twin period supports BG1) and flags the report's 'BG1-local 18 px at k=97' as colliding with landed F37h (18 px at k=37, 2 px at k=97) -- unmeasured. CORRECTION to the ticket's own framing: the pad is not code size -- readelf: .text byte-identical (08000178/02a3a8), the array landed at the head of .rodata and shifted every asset address by 0x100; so the control answers 'does the searched pairing shift with size/addresses' (NO) and says nothing about code layout, which is what t54's -900 B varies. GATE PROPOSAL NOT ACCEPTED by the coordinator: docs/measurement-drift.md section 7 on wt/t56 (25fca1e, docs-only, UNMERGED) proposes cursor isolated <=6/<=6/170 @237 localized to the k=97 BG1 seam; the verifier showed it is calibrated off the one branch it exists to excuse (HEAD=1, ceiling=6), its localizing half is unenforceable by the free tier (run_check deletes the capture dirs), and cursor cannot see chip data at all (OBJ chip window is HIDDEN by BG3, docs/coverage/cursor.md F37h), so it buys a 6-px licence on a blind row for no M4 protection. Q3's unreproduced 13/12/170/186277 is still live; note 186277 is exactly HEAD's total at offset 238, suggesting an alignment event, not logic drift (inference only). Nothing merged: wt/t56, wt/t56-pad, wt/t54 all unmerged branches; main's tree carries only the worklog. FORWARD-BLOCKING for the user: the 411-chip M4 pipeline is gated on a cursor ceiling that is neither logic nor size -- it is F35/F37h's sub-frame BG1 tile-copy seam, so either the seam is fixed (M2-adjacent) or the owner rules on the ceiling; T54's 4 Vulcan rows stay 0/0 non-blind (6276/10142/12728/28047) on an unmerged branch.
**Why.** Three chip-family ports died on one number: T54's Vulcan arm was byte-correct (4 rows 0/0, negatives 6276/10142/12728/28047, `AS_DATA_FAMILIES` {0x13,0x15,0x21}→+0x17, SCOPE M4 14→18) yet `cursor` read 6/6/170/**186280** vs HEAD's 1/1/170/186279 → branch wt/t54 (b96ba9e) unmerged. T51 (NEGATIVE) found the same veto killed the Cannon arm and blamed a different change. `cursor` is a **searched** row (`tools/harness.py:2613-2619`: frames 170, `Align(canon_ref=15, search=range(225,250))`), so its pairing offset is chosen by a minimum search, and F48 wrote the landed hole itself: `docs/measurement-drift.md` §1 measured 4 reps × 5 rows at spread 0 and states "That is NOT a harness-wide claim… `cursor` not", citing Q3's merge `184b9b5`, where `cursor` read **1/1/170/186279 and then 13/12/170/186277 at the SAME commit** `7cd1ec1`. F37l localized the residue to 1 px at (183,5) on k=97 (F35's mid-frame tile-copy timing); F26b confined it to two OBJ bands (x2..77, x122..189). Nobody has ever asked whether the number moves on a change that cannot execute. The answer decides whether the 411-chip M4 pipeline (and every M2/M3/M5 port, all gated on the same ceiling) is landable at all. Milestone **M4**.

**New evidence.** T54's own measurement (a port with an unchanged byte sequence and non-zero rows still moved `cursor` by 5 px at k=170) and T51's (the cursor deck holds no Barrier/Cannon id, so `use_chip`/`chip_strike` cannot fire on it) are mutually inconsistent with a logic cause; `docs/measurement-drift.md` §1's un-measured-`cursor` boundary and Q3's same-commit 13/12 reading are the landed record.

**Files.** src/battle.rs (**only** a never-read size pad, on a measurement branch that is never merged), docs/measurement-drift.md, docs/coverage/cursor.md, docs/worklog/T56.md. **NOT** tools/verify_rows.py, tools/harness.py (row config, frames, align, negatives: read only), tools/allowlist.py, tools/diffmask.py, tools/inventory.py, src/chips.rs, src/spr.rs, src/backdrop.rs; reference/bn6f read-only.

**Do.** 1. Baseline: `python3 tools/harness.py --no-gallery --only cursor` ×4 fresh processes on HEAD → report all four total/worst/frames **and the chosen alignment offset printed in each row's align line**, plus the built ROM's sha256. **measurement.** 2. Control: commit a pad on its own branch -- `#[used] static T56_PAD: [u8; 256] = [0u8; 256];` tagged `// unnamed: T56 code-size control, never read` -- build (ROM must be 256 B larger, sha256 must differ) and run `cursor` ×4 the same way → report the four totals and offsets. **measurement.** 3. Check out wt/t54 (b96ba9e), run `cursor` ×4 + `chip-vulcan1,chip-vulcan2,chip-vulcan3,chip-suprvulc` once → report every line and offset. **measurement.** 4. Verdict, one of exactly two: (a) the pad alone moves `cursor` off HEAD's spread, or any tree's chosen offset differs from HEAD's → the ceiling is a pairing/size artifact, not a render difference; (b) all three trees pick the same offset and only t54 moves → localise the moved pixels on k=170 with `python3 tools/diffmask.py <canon_dir> <rust_dir> <out.png> --frames 165..169` and name the band (x2..77 or x122..189) and the OBJ entry. **measurement.** 5. Write the gate proposal (measured per-tree envelope, provenance = these 12 reps, `verify_rows` untouched, `land.sh` untouched) into docs/measurement-drift.md for the coordinator/user to rule on. **docs change.**

**Rules.** No src change of any kind lands: the pad branch and wt/t54 both stay unmerged this ticket. `cursor`'s Check entry (frames, `Align`, negative, region) is never edited. No DRIFT_ENVELOPE edit, no `--with-main` in a landing call (F48 §5 reserves that for an owner decision). No row's number is quoted without its ROM sha256 (F48 §6 rule). Fitted ≤19. ≤14 captures, tool budget ≤80.

**Acceptance.** A 12-rep table (HEAD ×4, pad ×4, t54 ×4) with `cursor` total/worst/offset/sha256 per rep, and exactly one verdict: (a) pad-vs-HEAD Δtotal ≥1 px or any offset differs → `cursor` is not a logic quantity, plus the gate proposal with the measured envelope; or (b) pad == HEAD == t54-offset and t54's moved pixels localized to a band and frame → the byte of T54's diff that `cursor` reads, cited. Either way: wt/t54's 4 Vulcan rows reported, `git status` clean of any merged change. A NEGATIVE naming the spread closes it.

**Measure and report.** rows: cursor, chip-vulcan1/2/3, chip-suprvulc. frames 170/60/70/80/113. Per-rep totals/worst/offsets for the three trees, the three ROM sha256s, the negative totals, the localized band+frame if verdict (b); commit (measurement branches unmerged); one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch alone (it owns src/battle.rs for the pad only). Free tier: none to land -- run verify_rows on `cursor` + the 4 Vulcan rows on wt/t54 purely to record, and confirm nothing merged. A verifier for the verdict, since every M4/M2/M3/M5 gate quote builds on it. ≤$0.30 expected, ≤$0.60 cap. Advances **M4**.

---

### T57. M4/M1: Audit which of the 43 pixel-verified chips dispatch through the record, and census the other 368 ids by AttackFamily  *(DONE -- 2026-09-16, 43-row record-vs-id table [verifier-hyper re-parsed all 411 rows: data cites match, all 14 'record' rows + 15 )*

**Result.** 43-row record-vs-id table (verifier-hyper re-parsed all 411 rows: data cites match, all 14 'record' rows + 15 'id' rows confirmed at the quoted src/battle.rs lines). AS_DATA_FAMILIES {0x13,0x15,0x21}->{0x13,0x21}; as-data 14->11 (10 swords + AirShot 4). NEGATIVE closure confirmed by verifier: family 0x15's other verified ids 163 AreaGrab (:3434/:3459/:4348) and 177 Invisibl (:3430/:4321) are chip.id-keyed and the barrier gate (:3444/:4363) tests family AND subfamily 0x04, so no family-only rule carries it. 368-id census reproduced independently (0x15->79, 0x1b->70, 0x1c->54, 0x14->42, 0x0a->10 ids 400-409; AIAttackJumptable asm31.s:107666 with :107671/:107672/:107678/:107679 cites correct; 0x0a GAP honest). ROM byte-identical: verifier rebuilt ca569b1 -> 5b46337a...85ef == HEAD baseline; src/ untouched. verify_rows wt/t57: chip-cannon 0/0/40/9505, chip-sword 0/0/40/6397, chip-barrier 0/0/80/41067 all MATCH (isolated); fitted 19. Landed 954ed81. Left open for the follow-up ticket: SCOPE.md:21 prose still says 14/{0x13,0x15,0x21} (worker forbidden: 'regenerated only'); inventory.py comment 'the one byte this family still keys by id is :4144' is wrong -- src/battle.rs:3288-3294 keys the sword sprite/palette by chip.id for FIRE/AQUA/ELEC/BAMBSWRD; cite slips :4266->:4269, :4596-4604->:4578, :1165->:1184; duplicate wildcard arms :3435/:3454 emit 'unreachable pattern' so the :3444 barrier gate runs in the catch-all.
**Why.** SCOPE quotes "43/411 chips, 14 verified as data" from one literal set, `AS_DATA_FAMILIES = {0x13, 0x15, 0x21}` (tools/inventory.py:209), matched against `attack_family` -- not against what src actually reads. T46 (DONE, landed 85f021c) ported the Bomb family to `AttackPower +0x1a` and its family is **not** in that set, while src/battle.rs:5441 already reads `chip.family == BOMB_FAMILY_BLK_BIG` inside a `match chip.id` arm at :4482. Remaining id-keyed arms: :3429, :4143, :4482, plus Cannon :4250/:4659, Vulcan :4277/:4626 and the SHOTS lookup :505-507; record-keyed arms: AirShot :4128/:4468, Sword :4391, Barrier :3444/:4363. So the number every M4 ticket quotes is unverified in both directions, and no one has measured which family owns how many of the 411 ids -- the queue order for the rest of M4. Milestone **M4** (and M1's chips line).

**New evidence.** T52 DONE (50e4d49) established the per-id parity-table method; T54 NEGATIVE measured that 4 chips carry family 0x17 and `VULCAN_SHOTS_BY_SUB` [3,4,5,10] equals dword_80EBFEC, i.e. the family census predicts arm content -- the census is now a measurable instrument, not a guess.

**Files.** tools/inventory.py (`AS_DATA_FAMILIES` and its comment only), docs/SCOPE.md (regenerated only), docs/inventory/ (generated tables), docs/worklog/T57.md. **NOT** src/ (zero changes -- the build must be byte-identical), tools/harness.py, tools/scoreboard.py, tools/chip_export.py, tools/verify_rows.py, assets/, reference/ (read-only).

**Do.** 1. No-edit baseline: build HEAD, quote its ROM sha256; `python3 tools/harness.py --no-gallery --only chip-cannon,chip-sword,chip-barrier` → 0/0 with non-blind negatives. **measurement.** 2. Per-id table for all 43 ids in tools/scoreboard.py's `CHIPS`: from data/ChipDataArr.s give each one's `attack_family`/`attack_subfamily`/`AttackPower(+0x1a)`/`Element` cite, and from src/battle.rs the exact line that consumes it, marked `record` (reached by a `chip.family ==` / record-byte read) or `id` (reached by a `match chip.id`/`CHIP_*` arm) -- 43 rows, no blanks. **measurement.** 3. Set `AS_DATA_FAMILIES` to exactly the families whose every verified id in step 2 reads `record`; a family stays out with the single byte still keyed by id named. Run `python3 tools/inventory.py` → quote SCOPE's chips line and the M1 table line before/after. **tools change.** 4. Census the remaining 368 ids by `attack_family` (descending id count), and for the top 5 families name their record dispatch site in reference/bn6f (file:line) plus how many of those ids already have a pixel row. **measurement.** 5. Prove `build sha256 == HEAD's` and `git diff --stat` touches only the three named paths; `python3 tools/verify_rows.py HEAD chip-cannon --expect chip-cannon=0/0/40/9505`. **measurement.**

**Rules.** Zero src/ changes: any ROM byte difference invalidates the ticket (quote both sha256s). No family enters `AS_DATA_FAMILIES` without step 2's per-id evidence; if the honest set is *smaller* than {0x13,0x15,0x21}, say so and shrink it -- the count may go down. No new fixture, no row change, no allowlist, no patch_sterile. Every cite re-checked against disk. ≤2 captures, tool budget ≤50.

**Acceptance.** The complete 43-row record-vs-id table with a src cite per row; `AS_DATA_FAMILIES` before/after with the reason for every add and drop; SCOPE's chips line moved to the number the 43-row table supports (with the arithmetic shown); the 368-id family census with the top 5 by count and their disassembly cites; ROM byte-identical to HEAD; `git diff --stat` = the three named paths. A NEGATIVE naming a verified chip whose dispatch no family rule can express closes it.

**Measure and report.** rows: chip-cannon, chip-sword, chip-barrier. frames 40/40/80. Totals/worst/negatives before/after (expect identical -- no ROM change), both ROM sha256s, the 43-row table's record/id split, AS_DATA_FAMILIES before/after, SCOPE's M4 and M1-chips lines before/after, the top-5 families with id counts; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch alone (files are disjoint from T56's). Free tier: verify_rows on the 3 spot rows with landed expects; the byte-identity check is the gate. A verifier for step 2's table -- it is the number every M4 ticket quotes. ≤$0.15 expected, ≤$0.30 cap. Advances **M4** and **M1**.

---

### T58. M5: Rank census and one new canon recording for a virus whose routine T6 already ported  *(OPEN -- 2026-09-17)*

**Why.** SCOPE's M5 is 1/187. T6 (DONE) landed the Mettaur as canon's per-type routine `ForMettaur_8109EF4` (reference/bn6f/asm/asm31.s:170982), reached through `off_8109050`'s AIIndex-4 arm (T49's cite) -- so every virus rank whose identity row carries ai_index 4 needs **no port**, only a recording, a trace and pixels. T12's roster (byte_80182C4 identity rows → `ai_index` + version, `off_8109150` Struct2 rows → `elem_hp` at 0x00, high nibble = element per asm00_2.s:683, via tools/rom_enemy_tables.py) makes that count mechanical, and nobody has taken it. The recording route is proven and legal: tools/states.py:224-247 fields a chosen encounter by the frame-60 lever `60:0x0200a210:0x371` (T9b's), which makes the ROM's own roll pick BattleSettings 0x080b4bd8; slots populate at capture frame 148 and the built state is verified against the ROM's tables (T9c). Milestone **M5**.

**New evidence.** T55 NEGATIVE (2026-09-16) closed the *Gunner* objective, and its result states the wiring it found: `src/battle.rs:2181` already carries the gauge band (`f.gauge != 0 ? GAUGE_FULL : 0`) -- so a rank that rides the ported Mettaur routine does not need any of the Gunner's unsettled levers. The rank census itself has never been printed.

**Files.** tools/states.py (ONE new `State` recipe plus its note; every existing recipe untouched), docs/coverage/mettaur.md (append the census), docs/worklog/T58.md. **NOT** src/ (zero changes), tools/harness.py (no row is added here), tools/rom_enemy_tables.py, tools/trace.py, tools/oracle.py, tools/allowlist.py, assets/, reference/ (read-only).

**Do.** 1. `python3 tools/rom_enemy_tables.py` → group all 187 identity rows by `ai_index`; report how many ranks sit at ai_index 4 (routine already ported), how many at other ai_index values, and the rank list of the mettaur row's NameID 0x0001 family. **measurement.** 2. For each ai_index-4 rank that is not the rank the live `battlestart` trio already fields, walk data/BattleSettings.s (`battleSettingsList0:2`, `battleSettingsList1:1505`; SCOPE's T10 counts: 461 records, 297 0xF0-terminated formations) and report the record address whose formation names that rank, with the bytes read -- and the value the frame-60 lever must take to make the roll land on it. **measurement.** 3. Add ONE `State` (base `/tmp/overworld_net.state`, battlestart's pokes plus the frame-60 lever at step 2's value), build it, and `--watch` it: report the chosen BattleSettings pointer, the three populated slots' NameID/version/HP at the populating frame, and compare each HP against that version's `off_8109150` `elem_hp` row. **measurement.** 4. Determinism, mettaur's own standard: 40 frames from two independent builds of the new state diff to **0** pixels; report the pixel count and both state hashes. **measurement.** 5. Record the frame at which canon's new enemy first arms an attack (trace/peek read-only), so the follow-up port ticket inherits an alignment event; quote the field and frame. **measurement.**

**Rules.** No src/ change and no harness row: this ticket's product is a recording and a count. Only a NEW State entry -- no existing recipe's rom/base/script/pokes/frames change, no root state touched (the inputs guard must stay green). The new state must be rebuildable by `tools/states.py build all`. Every claimed byte cites reference/bn6f file:line or the ROM table. ≤4 captures, tool budget ≤60.

**Acceptance.** The 187-rank census by ai_index with the ai_index-4 count stated; one new state whose chosen BattleSettings pointer, enemy NameID/version and HP match the ROM's own identity and Struct2 rows byte-for-byte at the populating frame (or a NEGATIVE naming the record addresses that made that impossible); 0 px across two builds over 40 frames; the first-attack event frame named; `git diff --stat` = tools/states.py + the two docs paths.

**Measure and report.** rows: none (canon-side state only). frames: 40 determinism + the state's own build frames. Totals: the 0-px determinism reading, the state hashes, the slot NameIDs/HP vs table values, the populating frame and first-attack frame, ai_index-4 rank count / 187; commit; one line of mechanism (which lever steered the roll); one line of what is unverified (no port, no trace parity, no row).

**Coordinator:** dispatch alone (it owns tools/states.py; disjoint from T56/T57). Free tier: none -- no harness row moves; run `python3 tools/states.py build all` and confirm the new state builds and the guard passes. A verifier for step 3's byte-for-byte table match: the next M5 port ticket builds on it. ≤$0.20 expected, ≤$0.40 cap. Advances **M5**.

### T59. M2: The battle folder becomes a measured field — canon's own 30 words on the trace, then the un-ported shuffle rules  *(NEGATIVE -- 2026-09-16, Step 2 instrument is real and step 4/5 is a precise NEGATIVE, but the branch FAILS the ticket's own cursor vet)*

**Result.** Step 2 instrument is real and step 4/5 is a precise NEGATIVE, but the branch FAILS the ticket's own cursor veto, measured by the coordinator and not by the worker: verify_rows wt/t59 (fb799ea) wave 0/0/90/3840, window 0/0/16/81056, opening 0/0/40/86591, chip-cannon 0/0/40/9505, mettaur 0/0/70/41734, windowclose 0/0/40/207166 all MATCH, but cursor reads 29/28/170/186277 vs HEAD's 1/1/170/186279 -> verify_rows FAIL, branch kept unmerged, worktree removed, nothing landed. The worker never ran verify_rows on its own branch: its report quotes 'verify_rows.py HEAD ... PASS ... cursor 1/1/170/186279 ... the gated TRC2 v4 export moved no pixel row' -- that line is HEAD's, so the no-pixel-row claim is unsupported and now refuted. wt/t59 grows the ROM 584296 -> 585288 B (+992), sha 5b46337a...85ef -> d464258a808550e23fc3cbe43d8411146e4efa26ad073887361148cddc79890d; cursor's negative also shifted 186279 -> 186277, which is exactly the value verifier-hyper measured for HEAD's captures at align offset 238 (one frame off the chosen 237) -- so this is the third independent instance of the same phenomenon T54 died on and T56 measured: a code-layout change moves cursor's searched pairing/tear without touching chip data (T56: pad +256 B in .rodata moved nothing; t59 +992 B of code moves it by 28 px). FINDINGS THAT STAND ON THE BRANCH: (i) live battle folder address proven as eBattleFolder 0x0203CDB0 (ewram.s:3097; writers buildBattleFolder_800A3E4 / shuffleBattleFolder_800A570 asm00_1.s:15414/15568 with a 60-BYTE CopyHalfwords; readers offerDeckChipsToWindow_8027EE8 asm03_0.s:3510, blank/pack :6413/:6470) with two-frame proof (30 halfwords, low 9 bits < 411, code letters above, frame-48 pack-to-front, fixture Cannon id 1 present, PET/S_Chip source block 0x2002178 dumped with 30 chips); (ii) canon watch 'folder' (0x0203CDB0, 0x40) as a 14th stream and TRC2 v4 = 124 B at offset 128 with 4 B of headroom inside BATTLE_MARKER's 0x02000000..0x02000100, made a compile-time TRACE_REGION_FITS assert; (iii) first divergent folder word on battle_full = export frame 0 (canon capture 11), word index 4: canon 0xffff (pre-pick blank) vs our fixture-stacked 0x0001 -- i.e. the divergence is the fixture override, not the shuffle; (iv) NEGATIVE on the shuffle localization: a full 2^32 scan of eSecondaryRngSeed 0x2001120 (generator inverted and verified) over Deck::new's plain shuffle AND the tag-adjusted variant gives 0 hits against canon's settled folder with the two 0x0001 blanks as constraints; no gigas exist in this deck (ChipData.elemIdx 0 for every id) and hasreg is structurally impossible (reg sits at slot 0, observed slot 0 is live 0605); matching positions stay flat at 6/30 at window-open/confirm/post-fire (canon 48/81/261 vs rust 37/70/250), so deck.rs:14-15's 'not reproduced' note stands with a measured reason and NO constant was tuned (fitted unchanged 19). ALSO: sequencer first divergence k=31 (canon frame 42) canon=0x20 rust=0x8, 273/540 frames -- unchanged from T53's map. UNVERIFIED (worker): why no seed reproduces canon's shuffle -- 0x2002178 may not be the input the original battle read, or an unaccounted draw interleaves the shuffle stages; the fix is a fresh battlestart capture where the shuffle runs on-camera. Branch fb799ea (wt/t59, worktree removed) holds the instrument for a later ticket that can pay for it; per the coordinator's ruling the cursor ceiling is NOT to be widened here -- T56's gate proposal was already rejected as unsound, so a folder-instrument landing needs either a cursor-neutral export (no code-size change visible to the searched row) or an owner ruling.
**Why.** SCOPE's M2 line "chip selection rules (codes, Regular, Tag, PA recognition, folder draw/reshuffle)" has **no instrument**: `tools/oracle.py`'s FIELD_PAIRS (:273-292) compare only mm/enemy state-action, anim, panel x/y, timer + rng_cadence, and `tools/trace.py`'s CANON_WATCHES (:65-79) watch rng/mm/e1-e3/banner/hud/gauge/camera/backdrop/gfx/mmbase — no folder, no hand. Our own module says the rules are missing: `src/deck.rs:14-15` "The reg chip, tags and the giga re-insertion that the shuffle also does (asm00_1.s:15398-15698) are not reproduced: the folder here has none", and `src/main.rs:318-324` says the fixture "overrides the shuffled deck outright rather than comparing it", so `Deck::new` (deck.rs:69-78) has never been compared to canon's shuffle. The rules are on disk and read today: `sub_800A540` (asm00_1.s:15758) splits the 30 entries by `ChipData.elemIdx==2` (giga) at :15801-15812 into `dword_2033000` (non-giga, :15805) and `word_2033040` (gigas, :15812), subtracts 1 from the shuffle count for a reg chip (:15822, `// reg?`) and 2 for a tag (:15836, `// tag?`) before `ShuffleHwordList_SecondaryRNG` (:15832/:15845), then re-inserts each giga at a `GetPositiveSignedRNGSecondary` index +0xa (+0x8 in crossover mode, branch :15847-15853, loop :15856-15888 → `sub_800A672` :15934). The path is hot on the existing scenario: docs/coverage/battle_full.md rank 5 `sub_800A2F8` (asm00_1.s:15337), 14,488 calls from frame 78.

**New evidence.** T20 BLOCKED closed only the *authored button-log* route. This ticket needs no new hand-play: it reads canon's folder words out of the existing battle_full capture (its script already opens the window and confirms), and compares halfword arrays. T57 DONE (954ed81) and T53's per-field counts (enemy_state_action 458/540, mm_timer 270/540) are the freshest maps; neither touches the folder.

**Files.** src/deck.rs (the rules), src/battle.rs (**only** `trace_snapshot`'s TRC2 writer + the Deck construction call site), src/main.rs (:242 block-size assert, :318-324 seed step), docs/oracle_layout.json, tools/trace.py (CANON_WATCHES + parse_canon_frame), tools/oracle.py (INFO_FIELDS only), docs/coverage/battle_full.md, docs/worklog/T59.md. **NOT** tools/harness.py (T60 owns), tools/states.py (T58 owns), src/custom.rs, src/objects.rs, src/actor.rs, tools/allowlist.py; reference/bn6f read-only.

**Do.** 1. Baseline, no edit: `python3 tools/trace.py record canon battle_full --out /tmp/tr_t59_c` (13 streams), `python3 tools/verify_rows.py HEAD wave,window,opening,chip-cannon,mettaur,cursor,windowclose --expect …`, ROM sha256+bytes, and from the nm/map evidence confirm 0x02000080+TRC2_LEN still sits inside BATTLE_MARKER's 0x02000000..0x02000100. **measurement.** 2. Locate the LIVE folder: from reference/bn6f's map plus the four cited consumers (`getChipID_802A54E` asm03_0.s:8820, `sub_8027EE8` asm03_0.s:3510 offers, `sub_80293F8` asm03_0.s:6413 blanks to 0xffff at OK, `sub_802945A` asm03_0.s:6470 packs to the front), report the address and prove it with `--dump` at two frames: 30 halfwords, low 9 bits < 411, bits 9+ the code letter, the fixture's Cannon (id 1) present. Two disagreeing reads → NEGATIVE with the search trail. **measurement.** 3. Add canon watch `folder` (addr, 0x40) as the 14th stream (cap 16) and grow the rust export to TRC2 v4 carrying our Deck's same 30 words **under the existing FLAG_TRACE gate**; re-record both sides and name the first divergent word index+frame, plus matching-position counts at canon's window-open, confirm and post-fire frames. **code change.** 4. Host-side, feed step 2's earliest folder dump and the seed into our `Deck::new` and report matching positions per rule group: what the plain shuffle reproduces and which words only the giga split / reg / tag / re-insertion explain. **measurement.** 5. Port only the group step 4 localizes, with cites, and re-run step 3: the matching-position count at each of the three frames strictly increases. **code change.**

**Rules.** Pixel rows' frame path untouched (AGENT_GUIDE's gated export). No new harness row, no fixture field, no states.py, no allowlist, canon never changes. No fitted constant: an index that only the RNG draw explains is reported, not tuned (`deck.rs::Rng` already carries the secondary generator). cursor ≤1/1/170, windowclose 0 are the veto; report ROM sha256 **and byte size** (T56 measured cursor reacting to a −900 B tree). ≤6 captures, tool budget ≤90.

**Acceptance.** The live folder address cited (map + writer + reader) and confirmed by two independent reads; the `folder` stream + TRC2 v4 present in both tables, first divergent word index and frame named on battle_full (frames 540), and the three matching-position counts strictly better after step 5; deck.rs:14-15's "not reproduced" note updated or falsified with lines; fitted ≤19; verify_rows identical on the guard set as the absolute veto. A NEGATIVE naming the unconfirmable address, or the rule whose count is not derivable, closes it.

**Measure and report.** rows: battle_full (trace), wave, window, opening, chip-cannon, mettaur, cursor, windowclose. frames 540/90/16/40/40/70/170/40. Before/after: stream count, TRC2 version/length/reservation headroom, both ROM sha256s+sizes, first divergent word+frame, matching positions at the three frames, guard lines, fitted count; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch alone (owns src/battle.rs; pairable with T60 only, files disjoint). Free tier: verify_rows on the guard set + cursor/windowclose — TRC2 v4 must not move a pixel row. A verifier for step 2's address (every later folder/PA/chip-selection ticket builds on it). ≤$0.40 expected, ≤$0.90 cap. Advances **M2** (and M4's chip-selection half).

---

### T60. M3: The first status bit end to end — force BLIND on canon's enemy, size its render effect, then port it  *(PARTIAL -- 2026-09-16, Steps 1-3 measured and committed on wt/t60 [b825b83, docs-only: adds docs/coverage/statuses.md + worklog])*

**Result.** Steps 1-3 measured and committed on wt/t60 (b825b83, docs-only: adds docs/coverage/statuses.md + worklog); steps 4-5 not started, no src/ touched, so no row and no cursor veto risk. Step 2 is NOT the NEGATIVE case the ticket expected: BLIND forced on canon's enemy (--poke-at 40:0x0203867c:0x2000 AND 40:0x02038660:0xffff -- the timer poke is required, the tick clears the bit at 0, and a load-time poke is overwritten by CollisionData init ~frame 31) = 262,824 px over 240 frames, first divergence k=139, worst 4,972 at k=238, region the Mettaur's own panel; frames 0-138 byte-identical, so a 130-frame window reads 0 and would have falsely closed NEGATIVE. Addresses: CollisionDataPtr at 0x0203abb4 reads 0x02038640 for base 0x0203ab60, so ObjectFlags1=0x0203867c (CollisionData.inc:145, +0x3c), BlindTimer=0x02038660 (:134, +0x20). Mechanism correction that changes the ticket's framing: the visible consequence runs through the AI decide gate MettaurDecideCheckStatusAndRow_810A004 (asm31.s:171395, 0xa000 BLIND|CONFUSED -> wander), NOT the render-hide blindVisualHandledHere_8016934 (asm00_2.s:16799-16889), which re-asserts VISIBLE and clears it only when the OPPOSING PLAYER (battle_findPlayer(alliance^1)) is blind -- an enemy-side poke exercises neither side. Wiring blocker for step 4: the cross-actor hide needs the player's flags at the enemy's draw, and the draw loop lives in src/battle.rs (T59's file), outside this ticket's Files. Baseline guard set on HEAD 782bdde: wave 0/0/90/3840, window 0/0/16/81056, opening 0/0/40/86591, chip-cannon 0/0/40/9505, mettaur 0/0/70/41734, cursor 1/1/170/186279, windowclose 0/0/40/207166. SCOPE 0/69 does not move (inventory.py's status table is generated from T18's walk, not rows). Unverified: everything port-side; our wander arm's hop timing differs by one frame from canon's sub_810A10D4, so 0/0 on a blind row is plausible but unmeasured. verifier-hyper in flight on the addresses, the A/B px count and the gate split.
**Why.** SCOPE's M3 reads "not started" and M1's statuses line 0/69 (SCOPE:47). T18 (DONE, 3f09aa7) landed the per-bit map with cites (SCOPE:846-852): **BLIND** 0x2000, setter asm/object.s:5530, per-bit reader asm/asm00_2.s:16861, battle-visible *yes*; CONFUSED 0x8000 setter object.s:5483 reader asm00_2.s:1894; IMMOBILIZED 0x4000 setter object.s:5510 reader asm31.s:171386; PARALYZED 0x800 setter object.s:5333 reader asm00_2.s:17033. Not one has a scenario, so M3 has never been measured at all. BLIND is the cheapest rung because its reader is a *render* gate that canon already executes every frame of an existing capture: docs/coverage/battle_full.md ranks `blindVisualHandledHere_8016934` (asm00_2.s:16788) at 1005 calls / 35,817 instr and `handleRenderBlindForSpecificObjectsMaybe_80169BE` (asm00_2.s:16858) at 1005 calls, both first frame 149.

**New evidence.** T53's measurement (2026-09-16, wt/t53 0c35405 — made after T41 NEGATIVE closed the panel route) found the status tests inside the Mettaur's decide chain: `MettaurDecideCheckStatusAndRow_810A004` (asm31.s:171279) gates on BLIND|CONFUSED and IMMOBILIZED reads at asm31.s:171386, so BLIND has both a pixel consequence and a behavioural reader in code T6 already runs. T18's per-bit cite list is now landed, not inferred.

**Files.** src/objects.rs (the object flags word: the four bits as named consts with include/structs/CollisionData.inc:14-18 cites, the setter and the per-bit reader), src/actor.rs and src/spr.rs (the render gate's consequence), tools/harness.py (ONE new row on the existing PAUSED-rooted scene, aligned by the poked frame, negative = frame shift), docs/coverage/statuses.md (new), docs/worklog/T60.md. **NOT** src/battle.rs (T59 owns), tools/states.py (T58 owns), tools/inventory.py (T61 owns), tools/trace.py, tools/oracle.py, tools/allowlist.py; reference/bn6f read-only.

**Do.** 1. Baseline, no edit: `python3 tools/verify_rows.py HEAD wave,window,opening,chip-cannon,mettaur,cursor,windowclose --expect …`; ROM sha256; derive and report the populated enemy's flags-word address (CollisionData.inc offset + the e2 base 0x0203ab60 that tools/trace.py already watches). **measurement.** 2. Size the effect on canon alone: capture the same scene twice, once with `--poke addr:val` setting 0x2000 at load and once without, diff the two canon captures over the same frames with `tools/diffmask.py`, and report px total, frames, region. **If that reads 0 px, the bit is not pixel-visible on this subject: close NEGATIVE naming which categories pass the "SpecificObjects" test at asm00_2.s:16858 and what subject would show it.** **measurement.** 3. Read asm00_2.s:16788-16861 and object.s:5530 from disk: report what the gate changes (alpha / palette / hidden), which categories it admits, and how/when the bit clears. **measurement.** 4. Port: the named bit in our flags word plus the render gate at the equivalent point in src/actor.rs/src/spr.rs, with the bit delivered to our side the same way (a harness poke, never a timer tuned to the picture); add the row. **code change.** 5. Re-run step 2's diff on OUR side and the new row: poked-vs-unpoked residue 0 px, row 0/0 with a non-blind negative, guard rows identical, cursor ≤1/1/170, windowclose 0, fitted ≤19. **measurement.**

**Rules.** Canon never changes: a poke is the established fixture (F5b's AIData poke, the ALIVE cheat), never a patch. No new root state (T58 owns states.py). No allowlist edit. The bit value is CollisionData.inc's. Any zero row that stops being zero → branch unmerged, reported. ≤6 captures, tool budget ≤80.

**Acceptance.** Step 2's canon-vs-canon px total with frames+region (or the NEGATIVE that closes it); the new row 0/0 with its negative not blind; our poked-vs-unpowned diff equal to canon's before the port and 0 px after; the four bits as named consts with their CollisionData.inc lines; verify_rows identical on the guard set as the absolute veto. Report explicitly that SCOPE's 0/69 does **not** move in this ticket — inventory.py's statuses table is generated from T18's walk, not from rows.

**Measure and report.** rows: the new blind row, wave, window, opening, chip-cannon, mettaur, cursor, windowclose. frames: the row's + 90/16/40/40/70/170/40. Before/after totals/worst/negatives, both ROM sha256s, step 2's px/region, the address and byte poked, fitted count; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** pairable with T59 (no shared path). Free tier: verify_rows on the guard set plus the new row. A verifier for step 2's px claim — it is M3's first rung and every later status ticket copies the route. ≤$0.30 expected, ≤$0.60 cap. Advances **M3**.

---

### T61. M4: Re-admit the Barrier family — port AreaGrab (163) and Invisibl (177) onto the record  *(OPEN -- 2026-09-17)*

**Why.** `tools/inventory.py:228` now reads `AS_DATA_FAMILIES = {0x13, 0x21}`: T57 (DONE, landed 954ed81) shrank the set and M4's as-data count from 14 to 11, with its reason at :223-227 — family 0x15's other verified ids, 163 AreaGrab (src/battle.rs:3434/:3459/:4348) and 177 Invisibl (:3430/:4321), are consumed by `match chip.id`, while the barrier gate (:3444) tests family **and** subfamily, so "no AS_DATA_FAMILIES rule can carry it". Family 0x15 owns 79 of the 368 unverified ids (T57's census, the largest after 0x1b's 70), so two id-keyed reads are the gate on the biggest single block of M4. I read the block today: the presentation arm does `CHIP_INVISIBL => set_invisible(INVISIBL_FRAMES)`, `CHIP_AREAGRAB => {}` with the orb comment, then `if chip.family == BARRIER_FAMILY && chip.subfamily == BARRIER_SUBFAMILY` **inside** the catch-all, followed by a duplicate `_ => {}` (T57: the unreachable-pattern arm).

**New evidence.** T57's per-id table was built after T54 and T56 closed their objectives and proves the barrier trio already reads the record (`barrier_hp(&chip)`), so only two chips plus one hoist stand between 11 and a family rule. T52 DONE (50e4d49) is the per-id parity-table method. T56's 12 reps are the envelope to report against: HEAD cursor 1/1/170/186279; a pad that shifted every asset address by 0x100 moved nothing; a −900 B tree moved 5 px at the k=97 seam.

**Files.** src/battle.rs (**only** the presentation block :3427-3462 and the two sites T57 names near :4321/:4348 — never `trace_snapshot`, which T59 owns), tools/inventory.py (`AS_DATA_FAMILIES` + its comment), docs/worklog/T61.md. **NOT** src/chips.rs, tools/chip_export.py, assets/chips.bin, tools/harness.py, tools/states.py, tools/oracle.py, tools/allowlist.py, src/objects.rs/src/actor.rs/src/spr.rs (T60 owns); reference/bn6f read-only.

**Do.** 1. Baseline: `python3 tools/harness.py --no-gallery --only <the rows for ids 163,177,178,179,180 — names taken from `harness.py --list`>` → expect 0/0 with non-blind negatives; guard verify_rows; ROM sha256 **and byte size**; `cargo build --release` warning list. **measurement.** 2. Parity table for ids 163/177/178/179/180 from data/ChipDataArr.s: attack_family, attack_subfamily, AttackPower +0x1a, Element, and every record byte, against the src literals INVISIBL_FRAMES, AREAGRAB_PRESENTATION, AREAGRAB_SPAWN_AGE, AREAGRAB_ORB_PALS, AREAGRAB_ORB_PAL_PHASE and barrier_hp/barrier_palette's fields — report each agreement and every literal with **no** record byte. **measurement.** 3. Convert only what step 2 proves equal: family/subfamily gates at the five sites, and hoist the barrier test out of the catch-all so the duplicate `_ => {}` disappears; anything step 2 cannot source stays id-keyed and is named as the family's blocker. **code change.** 4. Re-run step 1's five rows and the build: rows 0/0 unchanged, the unreachable-pattern warning gone. **measurement.** 5. `python3 tools/inventory.py`: add 0x15 to AS_DATA_FAMILIES iff every verified 0x15 id now reads `record`; quote SCOPE's M4 line and M1 chips count before/after. **tools change.** 6. Regression verify_rows over all 43 chip rows + cannon,field,tiles,gauge,mettaur,cursor,windowclose,opening: report every line. **measurement.**

**Rules.** T17's rule: no match survives a value step 2 proved equal, and a mismatch is the finding. No fix-up table, no new row/fixture/allowlist/asset change. Zero rows are the gate: any chip row non-zero, cursor >1/1/170, or windowclose/mettaur/tiles/gauge/field non-zero → branch unmerged, reported. fitted ≤19. Every literal gets `// canon:` or `// provenance:`; re-check T57's cite slips against disk. ≤5 captures, tool budget ≤70.

**Acceptance.** The parity table with each literal sourced or named; before/after counts of `chip.id ==` tests at the five sites (0 for the converted ones); the unreachable-pattern warning gone; all 43 chip rows 0/0 with non-blind negatives; AS_DATA_FAMILIES {0x13,0x21} → {0x13,0x15,0x21} **iff** step 2's evidence holds (otherwise unchanged, with the byte named); SCOPE's M4 as-data count moved with the arithmetic shown; guard rows + cursor identical as the absolute veto. A NEGATIVE naming the one literal with no record byte closes it.

**Measure and report.** rows: the five family rows, cannon, field, tiles, gauge, mettaur, cursor, windowclose, opening. frames 40/40/8/8/70/170/40/40. Before/after totals/worst/negatives per row, both ROM sha256s + byte sizes, the parity table, id-test counts, AS_DATA_FAMILIES before/after, SCOPE's M4/M1 lines, warning counts, fitted count; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch alone (shares src/battle.rs with T59 and tools/inventory.py with any statuses change). Free tier: verify_rows on the five family rows + the guard list with landed expects — any chip row that moves is a revert. A verifier for step 2's table. ≤$0.20 expected, ≤$0.40 cap. Advances **M4** (and M1's chips line).


### T62. M4/M1 bookkeeping: SCOPE's M4 prose line still quotes the count T57 superseded, and T57's own comment understates family 0x13's id keying  *(OPEN -- 2026-09-17)*

**Why.** T57 (DONE, landed 954ed81) shrank `tools/inventory.py:228` to `AS_DATA_FAMILIES = {0x13, 0x21}`, and its verifier-hyper run (35 tool calls, all 411 rows re-parsed) confirmed the generated side now reads **11 `verified` / 32 `verified-pixels` / 368 `unrecorded`** -- but `docs/SCOPE.md:21` is human prose ABOVE the `GENERATED` marker at :36, so `inventory.py` never rewrites it and it still says "**14** chips pixel-verified AS DATA ... (tools/inventory.py AS_DATA_FAMILIES={0x13,0x15,0x21})". Every M4 ticket quotes that line, so main currently carries two different answers to "how many chips are data". The same verifier REFUTED one clause of the new comment at tools/inventory.py:210-215 ("the one byte this family still keys by id is the use_chip ENTRY arm src/battle.rs:4144"): `src/battle.rs:3288-3294` picks the sword's SPRITE and PALETTE by `match self.chip_in_use.map(|c| c.id)` with four `CHIP_FIRESWRD|CHIP_AQUASWRD|CHIP_ELECSWRD|CHIP_BAMBSWRD` arms -- a second, behaviour-visible id key inside family 0x13, exactly the kind of asymmetry that dropped 0x15. Three cite slips in the same artifacts need the same pass (:4266 is `spr::Player::new`, the `chip.id - CHIP_CANNON` byte is :4269; `ener:` is :4578 not :4596-4604; the status counter is tools/inventory.py:1184 not :1165). Milestone **M4** (and M1's chips line): this ticket changes no number, only the text that quotes them.

**New evidence.** verifier-hyper on wt/t57 ca569b1 (independent rebuild: ROM `5b46337aa27da9ca881f2321fe1e210f54717285b4dfa5256c1680bac4b985ef`, byte-identical to HEAD; `git diff --quiet 86a5b18 -- src/` clean; the 11/32/368 split recomputed from the rule, not read from the file). Its two named leftovers are this ticket. T56 (NEGATIVE, stamped) is why the ROM sha must be quoted here: a −900 B tree moved `cursor` by 5 px at k=97, so any accidental src touch is visible.

**Files.** docs/SCOPE.md (**only** the human prose at :21, never the generated part), tools/inventory.py (**only** the `AS_DATA_FAMILIES` block comment at :200-227 -- the set literal itself must not change), docs/worklog/T57.md (its cite slips, corrected in place with a dated note), docs/worklog/T62.md. **NOT** src/ (zero changes), tools/inventory.py's `AS_DATA_FAMILIES` literal, tools/harness.py, tools/verify_rows.py, tools/states.py, tools/allowlist.py, assets/, reference/ (read-only).

**Do.** 1. Baseline, no edit: `python3 tools/inventory.py` then read the generated chips line (SCOPE.md:40 `| 43 / 411 |`) and the `verified` / `verified-pixels` / `unrecorded` counts from docs/inventory/chips.json; `sha256sum` the built ROM; quote `docs/SCOPE.md:21` verbatim. **measurement.** 2. Rewrite `docs/SCOPE.md:21` so its as-data number and its `AS_DATA_FAMILIES={...}` list are **read from step 1's generated output**, never hard-coded from memory, and state the reason the family rule does not cover the rest in one clause with the src cite that shows it; leave the milestone text and every other prose line byte-identical. **docs change.** 3. Fix the inventory comment: name BOTH id keys inside family 0x13 (:4144's entry arm and :3288-3294's sprite/palette `match`) and say explicitly why the family still qualifies (the entry arm is common to all swords and the sprite/palette key is cosmetic-per-id, so the *behaviour* is record-dispatched) -- or, if step 2 of that argument fails on the code, say the family does not qualify and name it as T61's blocker; do NOT edit the literal either way. **tools change (comment only).** 4. Correct the three cite slips in docs/worklog/T57.md and tools/inventory.py against disk, each with a dated `corrected by T62` note rather than a silent edit. **docs change.** 5. Rebuild: ROM sha256 must equal step 1's byte-for-byte, `git diff --name-only` must list only the four named files, and `python3 tools/inventory.py` must leave the generated part of SCOPE.md unchanged (`git diff --stat docs/SCOPE.md` = the prose line only). **measurement.**

**Rules.** The `AS_DATA_FAMILIES` literal is frozen: adding 0x15 back is **T61's** job (it owns src/battle.rs), not this one's; if this ticket's step-3 argument says 0x13 no longer qualifies, the fix is a named blocker, not an edit. Zero src/ changes -- quote both ROM sha256s (T56's rule). No row, fixture, allowlist, state or scope-order change; SCOPE's milestone list and §3 order are untouched. Every cite re-checked against disk before it is written. ≤1 capture, tool budget ≤30.

**Acceptance.** `docs/SCOPE.md:21` agrees with the generated table it sits above (the same as-data count, the same family set, with the reading of that number shown); the family-0x13 comment names both id keys or names the family's disqualification; the three cite slips are corrected with dated notes; ROM sha256 identical before/after; `git diff --name-only` = the four named files. A NEGATIVE closing it = the prose and generated parts cannot be made to agree without editing the literal (i.e. T61 must land first) -- report that, change nothing.

**Measure and report.** rows: none (no ROM change). The ROM sha256 (before = after), the generated 11/32/368-or-current split, SCOPE.md:21 before/after verbatim, the two family-0x13 id-key sites as read from disk, the corrected cites with their disk line numbers, `git diff --stat`; commit; one line of mechanism; one line of what is unverified.

**Coordinator:** dispatch when `tools/inventory.py` and `docs/SCOPE.md` are free -- it textually overlaps T61 (which owns that literal and src/battle.rs) and must not run beside it. Free tier: no row moves (ROM byte-identical is the gate); run `python3 tools/inventory.py` yourself and diff the generated part. No verifier unless the step-3 argument changes a family's qualification, in which case the claims are the two src cites. ≤$0.10 expected, ≤$0.20 cap. Keeps **M4**/M1 quoting honest.

---
