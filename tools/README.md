# tools — what each one is for

Generated by `python3 tools/index.py` from each tool's own first line; edit the tool, not this file.
A worker's own loop is in AGENT_GUIDE.md; this is the index for everything else.

- `tools/allowlist.py` — Known, ticketed defects the harness (tools/harness.py) tolerates instead of
- `tools/annotate_asm.py` — Carry the day's findings into the disassembly (reference/bn6f, branch bn-notes) as comment-only notes, the
- `tools/api_pacer.py` — A local pacing proxy in front of a provider's API, so a subscription's short-term rate limit is never seen
- `tools/apply_renames.py` — Apply the disassembly's symbol renames (reference/bn6f/docs/renames.md, `old -> new` lines) to this repo's own
- `tools/archive_tickets.py` — Move closed tickets out of TODO.md into TODO_ARCHIVE.md, leaving a one-line index entry each, so
- `tools/audio_probe.py` — tools/audio_probe.py -- T13: sample-exact comparison of two per-frame PCM
- `tools/backdrop_export.py` — Export the battle backdrop's tileset, tilemap and palette for the ROM.
- `tools/banner_export.py` — Export the battle BANNER -- "BATTLE START!", "ENEMY DELETED" and the rest.
- `tools/battle_gif.py` — A full-battle GIF of the current release ROM, played by a fixed "player" script: pick chips in the
- `tools/bench_free.sh` — Screen every tool-capable free model on OpenRouter with the replay benchmark on one archived ticket,
- `tools/bench_provider.py` — Benchmark a provider against the others on this project's own archived work.
- `tools/blog.py` — The project blog: dated milestone posts on the website (web/blog/). The user reads it as the main view
- `tools/bnasm.py` — Helpers for pulling binary data back out of the bn6f disassembly's .s files.
- `tools/bnsview.py` — Render an exported BNSP asset (tools/spr_export.py output) to a contact sheet.
- `tools/budget_watch.sh` — Stop a coordinator run when a subscription provider its coordinator or worker sits on is exhausted (under
- `tools/build_roms.sh` — Build the one ROM the site lists in its dropdown besides the canon
- `tools/capture.sh` — Drive the ROM in mGBA on a virtual display and capture PNGs, so changes can
- `tools/capture_real_chip.sh` — Capture a chip's real animation in the sterile arena.
- `tools/captures_manifest.py` — Build web/captures/manifest.json from the .gif files in web/captures/.
- `tools/check_inputs.sh` — Refuse to measure against a changed input: the real and sterile ROMs in /tmp must match the backup
- `tools/chip_compare.py` — Compare one chip's animation between the real ROM and the Rust build,
- `tools/chip_export.py` — Export battle chip data and card art for the ROM.
- `tools/compare_frames.py` — Per-pixel compare a real-ROM frame against the Rust port's frame.
- `tools/compare_stereo.py` — Build a clean real-vs-Rust per-pixel comparison.
- `tools/coverage.py` — Canon routine coverage per scenario (T2 coverage ticket).
- `tools/csrc.py` — Show one MMBN6F routine two ways at once: Ghidra's decompiled C and the
- `tools/custom_export.py` — Export the in-battle chip selection window for the ROM.
- `tools/daily_review.sh` — The daily cost-per-progress review (agreed 2026-09-13): joins ticket outcome to model and cost,
- `tools/diffmask.py` — Produce a differential mask over frames, showing where two captures differ.
- `tools/digest_post.py` — The automated daily digest: one blog post per day that had progress, built from the repo's own record
- `tools/docs_check.py` — Do the instructions still describe the project? A worker only knows what AGENT_GUIDE.md, AGENTS.md and its
- `tools/emotion_export.py` — Export the emotion window: the navi's face at the top left of the battle.
- `tools/f16_slot_probe.py` — F16 one-off probe: for every row whose fixture has enemies, capture the
- `tools/field_dump.py` — Render the bn6f 6x3 battle field to a PNG.
- `tools/field_export.py` — Export the bn6f battle field tileset, tilemap and palette for the ROM.
- `tools/font_export.py` — Export the battle HP digit font for the ROM.
- `tools/gbafix.py` — Turn the linked ELF into a bootable .gba: raw binary plus a valid header.
- `tools/ghidra_decompile.sh` — Decompile the real MMBN6F cartridge image with Ghidra, headlessly, using the
- `tools/hand_icon_export.py` — Export the palette for the chip-in-hand icon.
- `tools/harness.py` — One harness, one job (AUDIT.md pair 6): capture both sides of a check,
- `tools/hud_tiles_export.py` — Export the HUD's tile font and HP-box frame for the ROM.
- `tools/hyper_credits.py` — Charm Hyper credits: one tiny request, prints the remaining hypercredits and today's use against the
- `tools/index.py` — Regenerate tools/README.md: one line per tool, taken from its own docstring or header comment, so an agent
- `tools/inventory.py` — T10: inventory of the battle engine out of bn6f's own tables.
- `tools/judge_append.py` — Admit the judge's proposed tickets into TODO.md without a human, if each passes the template check:
- `tools/land.sh` — Land a worker's branch: reproduce its harness lines from a clean checkout (verify_rows), merge with
- `tools/learn_slides.py` — New slides for the learn feed (web/learn/slides.js), written by a model under the feed's rules and checked
- `tools/log_entry.py` — Add an entry to the web/log screenshot log (the /log.html page).
- `tools/mgba_capture.sh` — Native GBA frame capture for the bn project.
- `tools/mgba_frames.py` — Decode raw frames written by tools/mgba_capture.c into PNGs.
- `tools/minimax_quota.py` — The MiniMax coding plan's remaining quota, from its own endpoint (GET /v1/coding_plan/remains): a rolling
- `tools/next_ticket.py` — Print the first OPEN ticket in TODO.md -- and only that -- so agents read ~1k tokens instead of the
- `tools/note_audit.py` — Spend the day's last credits on checking our own notes: for a few `// bn ...` notes in the disassembly, one
- `tools/opencode_quota.py` — The OpenCode Go plan has no balance endpoint (2026-09-16): its limits are per model, per 5-hour window and per
- `tools/or_spend.py` — Print real OpenRouter spend (from OpenRouter, not pi's estimate) and exit 1 if what is actually
- `tools/oracle.py` — The state oracle (TODO R6/R7): the first divergent FIELD and frame, not
- `tools/oracle_layout.py` — The oracle block's layout, from its one Rust source to the Python readers.
- `tools/pacer.sh` — Start, stop or show the pacing proxy for a provider whose providers.toml block has a [providers.X.pacer]
- `tools/patch_sterile.py` — Patch the canon bn6f ROM for the capture harness.
- `tools/pi_audit.sh` — The auditor (agreed 2026-09-13): a one-shot, read-only pi session that reads a DIGEST (the latest
- `tools/pi_coordinator.sh` — Launch a detached pi coordinator session that runs TODO.md tickets through a provider's roles until a
- `tools/pi_judge.sh` — One-shot "judge" session on the cheap tier: reads the short HANDOFF, the open ticket list, the
- `tools/pi_watch.sh` — Print ONLY ticket-level events from a coordinator run, one line each, and exit when the run ends:
- `tools/premise_check.py` — Re-measure the rows an OPEN ticket's **Why.** cites, for free, before a worker spends credits on a premise that
- `tools/probe.py` — The measurements workers kept rewriting as throwaway scripts, as one tool.
- `tools/progress_gif.py` — A before/after progress GIF for one harness row: canon on top, then our build at <before>, then our
- `tools/publish_site.sh` — Publish web/ to the GitHub Pages branch (gh-pages) of this repo's origin.
- `tools/replay_bench.py` — Replay benchmark: re-run an archived ticket with a candidate model from the ticket's base commit and
- `tools/restore_inputs.sh` — Restore the /tmp working set after a reboot. /tmp is not durable (HANDOFF.md §1); this has
- `tools/results_export.py` — Export the battle results windows for the ROM.
- `tools/roles.py` — providers.toml as a tool: resolves a run profile's jobs to models by current budget, renders the pi agent
- `tools/rom_enemy_tables.py` — T12: enemy roster out of the ROM's own index tables (bn6f).
- `tools/run_day.sh` — The launcher, run by cron every 30 minutes. Reads providers.toml's schedule: for each run profile in order,
- `tools/sample_export.py` — Export a DirectSound SAMPLE -- an M4A/MP2K `WaveData` -- from the disassembly to a WAV.
- `tools/scoreboard.py` — The 43-chip table tools/harness.py's `_chip_checks()` builds its rows
- `tools/serve.py` — Serve web/ on the LAN so the ROM can be played in a browser.
- `tools/spend_ledger.py` — Where the OpenRouter money went: turns, cost and cache reads per role and model, from pi's session
- `tools/spr.py` — Parser for the bn6f `.spr` battle sprite container.
- `tools/spr_dump.py` — Render frames from a bn6f .spr file to a PNG contact sheet.
- `tools/spr_export.py` — Export a bn6f `.spr` file to the compact binary asset format the ROM embeds.
- `tools/spr_export_range.py` — Export a trimmed frame-range of a bn6f .spr into a BNSP asset.
- `tools/states.py` — The save-state manifest (AUDIT.md pair 5): every /tmp state this project
- `tools/tail.sh` — A subscription's last credits. A run needs about a ticket's worth (start_above in providers.toml) to reach
- `tools/text_font_export.py` — Export the battle text font: the glyphs the chip name is drawn from.
- `tools/throw_dump.py` — Read a thrown chip object's launch constants straight out of the game.
- `tools/ticket_ledger.py` — Ticket outcome joined to who did it and what it cost -- the table the daily review reads.
- `tools/ticket_result.py` — Stamp a ticket's status in TODO.md and add its **Result.** paragraph, then commit TODO.md alone.
- `tools/ticket_trace.py` — Everything that happened on one ticket, as one readable timeline: which model played which part, what it
- `tools/trace.py` — The state trace (T1): record canon's battle state per frame, replay ours,
- `tools/verify_rows.py` — Reproduce a report's harness lines from a clean detached checkout -- the free, model-less tier of
- `tools/waste_report.py` — What the day's model usage spent on nothing: the waste the outcome ledger cannot see.
- `tools/watch_run.py` — A live, readable view of a coordinator run for a human at a terminal (the "view" window of the run's tmux
- `tools/web_rom.sh` — Build the dev-profile ROM into web/ for tools/serve.py, with the commit it
- `tools/worktree.sh` — Set up an isolated git worktree for one agent, so two agents can never build,
- `tools/zai_quota.py` — The Z.ai GLM coding plan has no balance endpoint (2026-09-16): its limits are per model, per 5-hour window and per
